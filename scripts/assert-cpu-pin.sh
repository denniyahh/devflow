#!/usr/bin/env bash
# RED STUB (46-04 Task 3). Exits 0 and prints nothing — which is exactly the
# C-03 defect in miniature: the step it replaces was three bare `echo`s under a
# comment claiming they were a negative control. Task 3's GREEN commit replaces
# this body.
exit 0
