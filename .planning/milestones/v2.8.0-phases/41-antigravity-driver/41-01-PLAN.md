---
phase: 41-antigravity-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/agents/antigravity.rs
  - crates/devflow-core/src/state.rs
  - crates/devflow-core/src/agents/mod.rs
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-core/src/canary.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-core/tests/agent_kind_antigravity.rs
  - crates/devflow-cli/tests/phase7_cli.rs
autonomous: true
requirements: [ANTG-01, ANTG-02, ANTG-03]
estimate:
  tokens: 85000
  raw_tokens: 42000
  tasks: 8
  confidence: med

must_haves:
  truths:
    - "`--agent antigravity` resolves through `AgentKind`/`driver_for`/`agent_program`, and `devflow doctor` reports it installed (ANTG-01)"
    - "`build_command()` returns `(\"agy\", vec![\"--input-format\", \"stream-json\", \"--output-format\", \"stream-json\", \"--print-timeout\", \"60m\"])` — no `-p` (Go-flag string flag), no `--dangerously-skip-permissions` (D-01), prompt on stdin (D-02), `--print-timeout` above the 5m default (F3)"
    - "The first turn on the Antigravity child's stdin is `{\"event\":\"user\",\"message\":{...}}` via `user_turn_line_for` — and the REAL PipeOwning writer is tested, not just the pure helper (codex-3)"
    - "`evaluate_layer1` resolves Antigravity streams (event-key gate, `result.response` marker extraction) AND returns `Some(Failed{reason})` on a `status:\"ERROR\"` envelope (D-03, antigravity notice (c))"
    - "The monitor CLOSE RULE is agent-aware: `event == \"result\"` + `result.response` string → `parse_marker_lines` closes the Antigravity stream; the `type:\"system\"` background/open-task drain arms are stated as vacuously satisfied for Antigravity, not inherited (B1)"
    - "The delivery canary is agent-specific: `AntigravityCanaryLauncher` (agy-based, event-key turn) replaces the hardcoded Claude launcher for Antigravity runs — no Claude invocation is spent, no `claude`-absent refusal (B2/F2)"
    - "The PipeOwning idle-timeout policy is agent-specific: `idle_timeout_setting_for(agent)` with a DECIDED Antigravity default, never a silent inheritance of the Claude-measured 120s floor (B3)"
    - "The stream-launch predicate widens to Antigravity with `!legacy_opt_out` applied ONLY for Claude — `DEVFLOW_CLAUDE_LEGACY_LAUNCH=1` must not route Antigravity to Legacy (D-10, antigravity notice (b))"
    - "Unattended mode: `--mode auto` stays REFUSED for the undogfooded Antigravity driver (preflight C2 decision, tested); AutoChainGuard engages for Antigravity on PipeOwning (F5)"
    - "AntigravityDriver is enrolled in the shared conformance suite (hardcoded array 4 → 5, 7 checks per driver), proven by the uniquely-named `antigravity_conformance_enrollment` test (F6)"
    - "A stubbed `agy` with no stream events and exit 0 does NOT advance a commit-gated stage — regression gates at `Stage::Plan` via `wait_for_gate` + `gate_pending`; happy path advances only on an antigravity-shaped marker (ANTG-03, F4)"
    - "Every `<automated>` verify command names tests UNIQUE to the new work and FAILS against the unmodified tree — 0 passed with exit 0 is a FAIL, not a pass (F6/codex-6)"
  artifacts:
    - `crates/devflow-core/src/agents/antigravity.rs` — AntigravityDriver (argv incl. `--print-timeout`, `parse_completion` delegate, spawn smoke test)
    - `crates/devflow-core/src/agent_result.rs` — `is_antigravity_event_stream`, `parse_antigravity_event_result` (+ ERROR envelope), `event_is_top_level_antigravity_result_marker`, `evaluate_layer1` wiring
    - `crates/devflow-core/src/monitor.rs` — `user_turn_line_for`, agent-aware `CloseRule`, `idle_timeout_setting_for`, PipeOwning writer uses the agent-aware turn
    - `crates/devflow-core/src/canary.rs` — `AntigravityCanaryLauncher`
    - `crates/devflow-cli/src/pipeline_launch.rs` — widened stream predicate (legacy opt-out Claude-only), canary dispatch by agent, AutoChainGuard comment
    - `crates/devflow-cli/src/preflight.rs` — C2 explicit decision + test
    - `crates/devflow-cli/src/commands.rs` — doctor entry + assertable seam
    - `crates/devflow-core/src/state.rs` + `agents/mod.rs` — AgentKind variant, dispatch, conformance enrollment
    - `crates/devflow-core/tests/agent_kind_antigravity.rs` — public-API integration tests
    - `crates/devflow-cli/tests/phase7_cli.rs` — canary-aware agy stub, marker-less/happy/discrimination tests, `MonitorReapGuard` defined here
  key_links:
    - "`build_command` argv ↔ `user_turn_line_for` ↔ PipeOwning writer ↔ agent-aware CloseRule ↔ `evaluate_layer1` parser — the full write/read/close transport, all agent-aware (D-02/D-03/B1)"
    - "`stream-launch predicate` ↔ canary dispatch (`AntigravityCanaryLauncher`) ↔ preflight C2 ↔ AutoChainGuard — every consumer of the widened predicate, decided and tested (B2/F5)"
    - "`idle_timeout_setting_for(agent)` ↔ PipeOwning loop ↔ `fire_idle_timeout` — the bounded-silence policy (B3)"
    - "`render_claude_style()` ↔ prompt `DEVFLOW_RESULT` contract ↔ parser validates marker (D-05)"
    - "`wait_for_gate` ↔ commit-gated `Stage::Plan` ↔ marker discrimination control (ANTG-03)"
    - "`MonitorReapGuard` (defined here) ↔ 41-02 Task 1 systematic pass (antigravity notice (a))"
    - "`doctor_checks()` seam ↔ `agy` presence test ↔ `agent_program`/`ensure_agent_binary` (D-04/F7)"
assumption_delta_decision: "round-3 rework — round-2 review (aa16ccd) BLOCKED on transport-integration defects: CloseRule (B1), canary (B2), idle-timeout policy (B3), print-timeout (F3), canary-aware stubs (F4), preflight C2 (F5), unique verify names (F6), spawn/doctor falsifiability (F7), e2e schema test (codex-3), suite reap audit (codex-4), ROADMAP wording (codex-5), executor notices (a/b/c). All absorbed; 41-01 grows to 8 tasks and adds canary.rs + preflight.rs to scope"
---

<objective>
Deliver the `AntigravityDriver` end-to-end, including the three transport sides the round-2 review
proved Claude-hardcoded: the **close rule** (B1), the **delivery canary** (B2), and the
**idle-timeout policy** (B3), plus `--print-timeout` (F3), canary-aware integration stubs (F4),
the preflight unattended-mode decision (F5), uniquely-named discriminating verifies (F6),
falsifiable spawn/doctor steps (F7), an end-to-end stdin-schema test (codex-3), and the
`MonitorReapGuard` definition 41-02 depends on (antigravity notice (a)). This is the round-3
REWORK — round-2 adversarial review (codex + claude BLOCK, antigravity conditional pass) confirmed
all round-1 fixes landed and blocked on these NEW defects. Decisions: 41-CONTEXT.md (round-3).
Do not follow RESEARCH.md / PATTERNS.md claims marked superseded there.

Purpose: `devflow start --agent antigravity` launches the Antigravity CLI headless and drives a
stage to completion with honest completion detection — including the close/exit behaviour, not just
the write/parse halves.

Output: see `must_haves.artifacts` — the driver, parser (+ ERROR envelope), close rule,
canary launcher, idle-timeout policy, launch routing, preflight decision, doctor entry, and the
hard regression gates.
</objective>

<execution_context>
@.planning/CONTEXT.md (round-3 — D-01..D-08, D-10; authoritative decisions file)
@.planning/reviews/phase-41/review-2/SUMMARY.md + claude.md + codex.md + antigravity.md (round-2 evidence — every finding below traces to these)
@crates/devflow-core/src/agent_result.rs (event_is_top_level_result_marker:1208 — the close predicate to make agent-aware; parse_claude_event_result/is_claude_event_stream; claude_stream_envelope_failure; evaluate_layer1:1852/1866; evaluate_layer2:2037)
@crates/devflow-core/src/monitor.rs (user_turn_line:726, idle_timeout_setting:236-237/372, CloseRule:582-715 constructed at 882, PipeOwning writer:820, fire_idle_timeout:963)
@crates/devflow-core/src/canary.rs (ClaudeCanaryLauncher:286/323 + its CloseRule:~360, run_delivery_canary, token_reported_in_capture)
@crates/devflow-cli/src/pipeline_launch.rs (canary_gate call site:141-148, claude_stream_launch_enabled:707, resolve_launch_shape:192, canary_gate:462, AutoChainGuard:870-875)
@crates/devflow-cli/src/preflight.rs (unattended_launch_shape_condition:974 — C2, second predicate consumer)
@crates/devflow-cli/src/commands.rs (doctor checks:2287, cmd_check nested fn:2202)
@crates/devflow-cli/tests/phase7_cli.rs (pi_marker_less model:1218-1254, wait_for_gate:1206, stubbed-claude canary fixture:249-263, canary-refusal caveat:111-125)
@CLAUDE.md (verification habits: `-p devflow-core --lib` valid; devflow binary-only → `--test phase7_cli` for integration; assert a REAL `1 passed` with non-zero `filtered out`; 0 passed with exit 0 is a FAIL)
@.planning/RESEARCH.md + @.planning/PATTERNS.md (SUPERSEDED on pre-round-2 claims — follow 41-CONTEXT.md)
</execution_context>

<context>
@.planning/ROADMAP.md § Phase 41 (criteria 2/3/6 corrected rounds 2+3)
@.planning/REQUIREMENTS.md § Antigravity (ANTG-01/02/03)
@crates/devflow-core/src/agents/mod.rs (trait, driver_for, contract_checks 7 results, conformance array :274-279)
@crates/devflow-core/src/state.rs (AgentKind, FromStr/Display, AgentParseError)
@crates/devflow-core/src/prompt.rs (render_claude_style — D-05, verified sound)
@crates/devflow-core/src/test_support.rs (git_command etc.) and crates/devflow-cli/src/test_support.rs (ReapMonitorOnDrop:573 — the 41-02 reap model; MonitorReapGuard is defined in phase7_cli.rs by Task 8)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Antigravity stream parser + ERROR envelope + agent-aware close-rule predicate (agent_result.rs)</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    crates/devflow-core/src/agent_result.rs (855-880) — is_claude_event_stream
    crates/devflow-core/src/agent_result.rs (1483-1560) — parse_claude_event_result
    crates/devflow-core/src/agent_result.rs (1195-1218) — event_is_top_level_result_marker (the CLOSE predicate — requires type:result + STRING result; Antigravity's object result never matches — B1)
    crates/devflow-core/src/agent_result.rs (1605-1645) — parse_marker_lines
    crates/devflow-core/src/agent_result.rs (1852-1875) — evaluate_layer1 chain
    claude_stream_envelope_failure — the ERROR-envelope model
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): `is_antigravity_event_stream` true on `{"event":"init",...}`, false on Claude framing (discriminates)
    - Test 2: `parse_antigravity_event_result` extracts the marker from the live shape: last `{"event":"result","result":{"status":"SUCCESS","response":"DEVFLOW_RESULT: {\"status\":\"success\"}\n"}}` → `Some(Success)`
    - Test 3: marker-less stream → `None` (defers to Layer 2)
    - Test 4 (RED then GREEN, antigravity notice (c)): `{"event":"result","result":{"status":"ERROR","response":"","error":"stream input message is missing the \"event\" field"}}` → `Some(AgentResult { status: Failed, reason: Some(<error>), decided_by_layer: Some(1) })` — the CLI's explicit reason survives Layer 1
    - Test 5 (RED then GREEN, B1): `event_is_top_level_antigravity_result_marker` — true for `{"event":"result","result":{"status":"SUCCESS","response":"DEVFLOW_RESULT: ..."}}`, false for Claude-shaped events and for marker-less antigravity results; `event_is_top_level_result_marker` (Claude) is unchanged
    - Test 6: Claude-shaped events fed to the antigravity parser → `None`, and vice versa (no cross-contamination); existing Claude parser tests stay green
    - Test 7: torn JSON tail after the last result → fail-closed, mirroring the Claude path's constraint-9 guard
  </behavior>
  <action>
    Edit `crates/devflow-core/src/agent_result.rs`:
    1. `fn is_antigravity_event_stream(events: &[Value]) -> bool` — true when any event has `"event" == "init"`. Doc: why `event`-key, and why it cannot collide with the Claude (`type`/`subtype`) or Codex (`thread.started`/`turn.*`) gates.
    2. `fn parse_antigravity_event_result(stdout: &str) -> Option<AgentResult>` mirroring `parse_claude_event_result`:
       - gate on `is_antigravity_event_stream`; torn-tail guard identical to the Claude path
       - take the LAST top-level `event == "result"` object
       - **ERROR envelope first (mirror `claude_stream_envelope_failure`):** if `result.status == "ERROR"` or a non-empty `result.error` string is present → `Some(Failed { reason: Some(error), decided_by_layer: Some(1), ... })`
       - else extract `result.response` (STRING — the Antigravity `result` is an OBJECT) → `parse_marker_lines`; non-Success marker → decisive; Success marker → hold; no marker → `None`
    3. `pub(crate) fn event_is_top_level_antigravity_result_marker(event: &Value) -> bool` — `event.get("event") == "result"` AND `is_top_level(event)` AND `event["result"]["response"]` as string → `parse_marker_lines` → `Some`. Doc: this is the agent-aware CLOSE predicate for `CloseRule` (B1); do NOT change the Claude predicate.
    4. Wire `parse_antigravity_event_result` into `evaluate_layer1`'s `.or_else` chain (1866) directly AFTER `parse_claude_event_result` — the exact slot, not "alongside".
    5. Unit tests for Tests 1-7 with literal JSONL captures matching the LIVE stream shapes from the review evidence.
  </verify>
  <automated>cargo test -p devflow-core --lib antigravity_event -- --nocapture</automated>
  <done>
    - Parser pair exists; ERROR envelope → `Some(Failed{reason})`; close predicate `event_is_top_level_antigravity_result_marker` exists (B1)
    - `evaluate_layer1` slot pinned; Claude/Codex parser behavior unchanged (existing suite green)
    - Verify must show a real `1 passed` with non-zero `filtered out` — this filter matches 0 tests on the unmodified tree (F6)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Agent-aware monitor transport — user_turn_line_for, agent-aware CloseRule, agent-specific idle timeout (monitor.rs)</name>
  <files>crates/devflow-core/src/monitor.rs</files>
  <read_first>
    crates/devflow-core/src/monitor.rs (715-745) — user_turn_line (type-key shape; CLI rejects it for Antigravity)
    crates/devflow-core/src/monitor.rs (582-715) — CloseRule (marker_seen via event_is_top_level_result_marker; open_tasks via type:system only; should_close at 705)
    crates/devflow-core/src/monitor.rs (880-970) — the PipeOwning loop (rule constructed at 882; recv_timeout at 887; fire_idle_timeout at 963)
    crates/devflow-core/src/monitor.rs (200-240, 365-380) — idle_timeout_setting / parse_idle_timeout_secs (DEVFLOW_CLAUDE_IDLE_TIMEOUT_SECS; resolved at 372 INSIDE spawn_monitor_inner where state.agent is in scope)
    crates/devflow-core/src/monitor.rs (810-830) — the PipeOwning writer (user_turn_line call at 820)
    crates/devflow-core/src/monitor.rs (275-345) — spawn_monitor / spawn_monitor_inner / the PipeOwning arm and how the __monitor re-exec passes args
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): `user_turn_line_for(AgentKind::Antigravity, prompt)` → JSON with `"event":"user"`, no `"type"` key; `user_turn_line_for(AgentKind::Claude, prompt)` → byte-identical to today's `user_turn_line`
    - Test 2 (RED then GREEN, B1): the PipeOwning CloseRule with the antigravity predicate — feeding the live antigravity event lines (`event:init`, `event:step_update`, `event:result` with marker in `response`) sets `marker_seen` and `should_close()` returns true; feeding Claude lines leaves it false; the Claude CloseRule is byte-identical in behaviour for Claude input
    - Test 3 (B1, stated not inherited): for Antigravity input, `background_tasks == NeverAnnounced` and `open_tasks` stays empty (Antigravity emits no `type:"system"` events) — assert the drain arms are vacuously satisfied, with a comment stating this explicitly
    - Test 4 (RED then GREEN, B3): `idle_timeout_setting_for(AgentKind::Claude)` reads `DEVFLOW_CLAUDE_IDLE_TIMEOUT_SECS` exactly as today; `idle_timeout_setting_for(AgentKind::Antigravity)` reads `DEVFLOW_ANTIGRAVITY_IDLE_TIMEOUT_SECS` with the DECIDED default (120s floor, documented) — never Claude's variable
    - Test 5 (codex-3): the REAL PipeOwning writer path — spawn a child whose stdin is read and validated; assert the bytes written by the writer (via `user_turn_line_for(agent)`) are `{"event":"user",...}` for Antigravity (unit-test the writer's turn construction through the actual code path, not just the pure helper)
  </behavior>
  <action>
    Edit `crates/devflow-core/src/monitor.rs`:
    **Part A — agent-aware first turn (round-1 carry, kept):**
    1. `pub fn user_turn_line_for(agent: AgentKind, prompt: &str) -> String` — Antigravity → `{"event":"user",...}`; others → delegate to `user_turn_line` (Claude byte-identical).
    2. Update the PipeOwning writer (820) to use `user_turn_line_for(agent, prompt)` — thread the agent into the writer (it is available at the launch site; pass it into the monitor/`__monitor` invocation like `program`/`args`).
    **Part B — agent-aware CloseRule (B1):**
    3. Parameterise `CloseRule` with the marker predicate (or add a constructor `CloseRule::for_agent(agent)`): for Antigravity, `observe` sets `marker_seen` via `event_is_top_level_antigravity_result_marker`; for Claude, the existing `event_is_top_level_result_marker`. Keep `should_close` (705) unchanged.
    4. Construct the agent-aware rule at BOTH sites: the PipeOwning loop (882) and — via Task 3's canary — the canary's own close rule (canary.rs:~360).
    5. Add an explicit doc comment in `CloseRule`/the PipeOwning loop stating that for Antigravity the `type:"system"` background-task/open-task drain arms are **vacuously satisfied** (Antigravity never emits them) — stated, not inherited (B1).
    **Part C — agent-specific idle timeout (B3):**
    6. `fn idle_timeout_setting_for(agent: AgentKind) -> IdleTimeoutSetting` — Claude: `DEVFLOW_CLAUDE_IDLE_TIMEOUT_SECS` (unchanged); Antigravity: `DEVFLOW_ANTIGRAVITY_IDLE_TIMEOUT_SECS` with the DECIDED default (120s floor as a documented starting point — revisit after the first real cadence measurement). Both clamp at the existing floor.
    7. Call it at spawn_monitor_inner:372 (where `state.agent` is in scope) instead of `idle_timeout_setting()`.
    8. Update the doc comment at monitor.rs:1022-1027 — the "applying it to an unmeasured agent" caveat is resolved by the explicit per-agent decision.
    **Part D — tests for Tests 1-5 above** (monitor unit tests; names below are the verify filters).
  </verify>
  <automated>cargo test -p devflow-core --lib user_turn_line_for -- --nocapture && cargo test -p devflow-core --lib close_rule_antigravity -- --nocapture && cargo test -p devflow-core --lib idle_timeout_setting_for -- --nocapture</automated>
  <done>
    - `user_turn_line_for` exists; writer uses it; Claude byte-identical (no regression)
    - CloseRule agent-aware at the PipeOwning site; vacuous drain arms stated; Claude behaviour unchanged
    - `idle_timeout_setting_for` with a decided Antigravity default; Claude variable untouched
    - Real-writer test proves the event-key bytes reach the child (codex-3)
    - All three filters show a real `1 passed` with non-zero `filtered out` (each matches 0 tests on the unmodified tree)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widen the stream predicate (legacy opt-out Claude-only) + AntigravityCanaryLauncher + canary dispatch (pipeline_launch.rs, canary.rs)</name>
  <files>
    crates/devflow-cli/src/pipeline_launch.rs
    crates/devflow-core/src/canary.rs
  </files>
  <read_first>
    crates/devflow-cli/src/pipeline_launch.rs (665-715) — STREAM_JSON_STAGES + claude_stream_launch_enabled (707)
    crates/devflow-cli/src/pipeline_launch.rs (188-215) — resolve_launch_shape
    crates/devflow-cli/src/pipeline_launch.rs (135-150) — the canary_gate call site (hardcoded ClaudeCanaryLauncher)
    crates/devflow-cli/src/pipeline_launch.rs (455-475) — canary_gate (gated on stream_launch ONLY)
    crates/devflow-cli/src/pipeline_launch.rs (865-880) — AutoChainGuard + the "implies a Claude + stream launch" comment
    crates/devflow-core/src/canary.rs (270-420) — ClaudeCanaryLauncher (build_command ~286, type-key turn ~323, CloseRule ~360) + run_delivery_canary + token_reported_in_capture (raw token search — Antigravity-shaped captures match)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): the stream predicate is true for `AgentKind::Antigravity` on a stream-json stage; still exactly as before for Claude; still false for Codex/OpenCode/Pi
    - Test 2 (RED then GREEN, D-10/antigravity notice (b)): `DEVFLOW_CLAUDE_LEGACY_LAUNCH=true` (legacy opt-out) keeps Claude on Legacy BUT does NOT affect Antigravity — Antigravity stays stream-only (evaluates on STREAM_JSON_STAGES membership alone)
    - Test 3 (RED then GREEN, B2): `resolve_launch_shape` returns `PipeOwning { prompt }` for Antigravity; the canary launcher selected for Antigravity is `AntigravityCanaryLauncher` (agy-based), NOT `ClaudeCanaryLauncher` — no Claude invocation, no `claude`-absent refusal
    - Test 4 (B2): `AntigravityCanaryLauncher::run` writes the `event`-key turn (`user_turn_line_for(Antigravity, ...)`) and its close rule is the agent-aware one; `run_delivery_canary` returns `Confirmed` when the capture's `event:result.response` echoes the token (raw-token search)
    - Test 5: AutoChainGuard comment corrected ("implies a Claude + stream launch" is false) and its engagement for Antigravity asserted in the appropriate unit test
  </behavior>
  <action>
    **Part A — predicate (pipeline_launch.rs):**
    1. Generalise `claude_stream_launch_enabled` (707) to a stream predicate covering `AgentKind::Claude | AgentKind::Antigravity`:
       `matches!(agent, Claude | Antigravity) && STREAM_JSON_STAGES.contains(&stage) && !(agent == AgentKind::Claude && legacy_opt_out)`.
       The `!legacy_opt_out` term applies ONLY to Claude (D-10). Update the doc comment (which says "the sole adapter routed through this arm — Claude").
    2. Update `resolve_launch_shape` (192) if needed so Antigravity reaches `PipeOwning` (the widened predicate handles it; assert via Test 1).
    3. Update the AutoChainGuard comment (870-875) — the "already implies a Claude + stream launch" claim is now false; state the guard engages for any PipeOwning launch (Claude + Antigravity today). No behaviour change beyond the comment; engagement for Antigravity is tested.
    **Part B — canary (canary.rs + the call site):**
    4. Add `AntigravityCanaryLauncher` in canary.rs mirroring `ClaudeCanaryLauncher`:
       - `run` uses `AntigravityDriver.build_command(PhaseId::new(0), prompt, &[])` (argv incl. `--print-timeout`) 
       - writes the turn via `monitor::user_turn_line_for(AgentKind::Antigravity, prompt)` (event-key)
       - builds its `CloseRule` with the agent-aware antigravity predicate (Task 2 Part B)
       - the trust decision stays `run_delivery_canary` / `token_reported_in_capture` (raw search — the token JSON-escaped inside `response` still matches)
    5. At the canary_gate call site (pipeline_launch.rs:141-148), select the launcher by agent: Claude → `ClaudeCanaryLauncher`; Antigravity → `AntigravityCanaryLauncher`. `canary_gate` itself is unchanged (still gated on `stream_launch`).
    6. Tests for Tests 1-5; name the new tests with unique prefixes (`stream_launch_includes_antigravity_*`, `canary_antigravity_*`, `antigravity_legacy_opt_out_*`, `auto_chain_guard_antigravity_*`).
  </verify>
  <automated>cargo test -p devflow-core --lib canary_antigravity -- --nocapture && cargo test -p devflow --bin devflow stream_launch_includes_antigravity -- --nocapture && cargo test -p devflow --bin devflow auto_chain_guard_antigravity -- --nocapture</automated>
  <done>
    - Predicate widened with the Claude-only legacy opt-out; Claude behaviour unchanged; Codex/OpenCode/Pi untouched
    - `AntigravityCanaryLauncher` exists and is dispatched for Antigravity; no Claude invocation on Antigravity runs
    - AutoChainGuard comment corrected and engagement tested
    - All filters show a real `1 passed` with non-zero `filtered out` (unique to new work — the old `stream_launch` filter matched a pre-existing canary test and is NOT used, F6)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Unattended-launch decision for Antigravity — preflight C2 stays refused until dogfooded (preflight.rs)</name>
  <files>crates/devflow-cli/src/preflight.rs</files>
  <read_first>
    crates/devflow-cli/src/preflight.rs (955-1000) — unattended_launch_shape_condition (C2; asks claude_stream_launch_enabled; cause branch names "not claude")
    crates/devflow-cli/src/preflight.rs (84-97) — agent_program / ensure_agent_binary (round-1 finding 6 territory — unchanged; do NOT touch)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN, F5): `unattended_launch_shape_condition` for `AgentKind::Antigravity` returns `DoesNotHold` with a cause naming antigravity — `--mode auto` stays REFUSED for the undogfooded driver (the widened predicate alone must NOT flip C2 to Holds)
    - Test 2: for Claude the condition is unchanged (Holds on the stream path)
    - Test 3: the refusal is a launch-shape refusal, not a binary-absence refusal — `agent_program`/`ensure_agent_binary` untouched
  </behavior>
  <action>
    Edit `crates/devflow-cli/src/preflight.rs`:
    1. DECISION (F5, per 41-CONTEXT.md D-04): unattended `--mode auto` is permitted ONLY for stream agents with a real dogfooded run — Claude today. Antigravity joins when dogfooded (tracked as a follow-up; the round-3 plan does not dogfood). Implement C2 so the widened predicate alone does not make Antigravity `Holds`: add the per-agent dogfood condition to `unattended_launch_shape_condition` (e.g. `matches!(agent, AgentKind::Claude)` alongside the stream predicate), and extend the cause branch so an Antigravity refusal names `antigravity` explicitly (the current cause branch `"the agent is {agent}, not claude"` already produces the right string — keep it, add the test that pins it).
    2. Do NOT change `agent_program`/`ensure_agent_binary` (round-1 finding 6 was about those; rescinded preflight "do not touch" applies only to them).
    3. Add unit tests for Tests 1-3 with the unique prefix `unattended_launch_shape_condition_antigravity_*`.
  </verify>
  <automated>cargo test -p devflow --bin devflow unattended_launch_shape_condition_antigravity -- --nocapture</automated>
  <done>
    - Antigravity `--mode auto` refused with an explicit cause; Claude unchanged
    - `agent_program`/`ensure_agent_binary` untouched; preflight unit tests pass (real `1 passed`, non-zero `filtered out`)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: AntigravityDriver — argv incl. --print-timeout, parse_completion delegate, SPAWN smoke test (antigravity.rs)</name>
  <files>crates/devflow-core/src/agents/antigravity.rs</files>
  <read_first>
    crates/devflow-core/src/agents/claude.rs (1-70) — ClaudeDriver shape (minus `-p`; the round-2 probe argv carries --print-timeout)
    crates/devflow-core/src/agents/mod.rs (30-100) — AgentDriver trait (parse_completion default → None)
    crates/devflow-core/src/prompt.rs — render_claude_style (D-05)
    crates/devflow-core/src/agent_result.rs — Task 1's parse_antigravity_event_result (the delegate target)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): `build_command()` returns EXACTLY `("agy", vec!["--input-format", "stream-json", "--output-format", "stream-json", "--print-timeout", "60m"])` — no `-p`, no `--dangerously-skip-permissions`, no prompt in argv (F3: `--print-timeout` above the 5m default)
    - Test 2: `render_prompt()` delegates to `render_claude_style()` and includes "DEVFLOW_RESULT" (D-05)
    - Test 3: `parse_completion()` delegates to `parse_antigravity_event_result` — `Some(Success)` for the live shape, `Some(Failed{reason})` for the ERROR envelope, `None` marker-less
    - Test 4 (RED then GREEN, F7 — "argv spawn-tested, not assumed"): SPAWN the driver's argv — a temp stub `agy` (records its argv to a file, reads one stdin line, emits `{"event":"init",...}` + `{"event":"result",...}`) run via `Command::new(program).args(&args)` with the stub on PATH; assert the stub received exactly the five tokens above AND the stream round-trips through the parser
    - Test 5: `health()` NOT overridden (trait default; presence via preflight + doctor — dead-code prohibition from round 1)
    - Test 6: `name()` returns "Antigravity"
  </behavior>
  <action>
    Create new file `crates/devflow-core/src/agents/antigravity.rs`:
    1. Module docstring: stream-json transport, D-01..D-10 citations (41-CONTEXT.md round-3), the `-p` removal, the event-key schema, and `--print-timeout 60m` (F3) — all deliberate, review-driven.
    2. `pub struct AntigravityDriver;` + `impl AgentDriver`:
       - `name()` → "Antigravity"
       - `render_prompt()` → `render_claude_style(intent)`
       - `build_command()` → `("agy", vec!["--input-format","stream-json","--output-format","stream-json","--print-timeout","60m"])`
       - `parse_completion(output)` → `crate::agent_result::parse_antigravity_event_result(output)`
       - no `health` override
    3. `#[cfg(test)] mod tests` with the unique prefix `antigravity_driver_*` (incl. `antigravity_driver_spawn_argv_smoke` for Test 4 — write the stub with `std::fs` into a temp dir, put it on PATH via a test-scoped env, spawn, assert).
  </verify>
  <automated>cargo test -p devflow-core --lib antigravity_driver -- --nocapture</automated>
  <done>
    - Exact argv incl. `--print-timeout 60m`; no `-p`/skip-permissions/prompt
    - Spawn smoke test proves the argv reaches a real child and the stream round-trips (F7)
    - `parse_completion` delegates; `health()` inherited; name correct
    - Verify shows a real `1 passed` with non-zero `filtered out` (matches 0 tests on the unmodified tree)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: AgentKind::Antigravity + driver dispatch + conformance enrollment with unique test names (state.rs, agents/mod.rs, agent_kind_antigravity.rs)</name>
  <files>
    crates/devflow-core/src/state.rs
    crates/devflow-core/src/agents/mod.rs
    crates/devflow-core/tests/agent_kind_antigravity.rs
  </files>
  <read_first>
    crates/devflow-core/src/state.rs — AgentKind enum + Display/FromStr + AgentParseError
    crates/devflow-core/src/agents/mod.rs — driver_for (~173), exports (~183-191), driver_for_returns_correct_names (~196), every_driver_passes_the_conformance_suite (~274, hardcoded 4-driver array)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): `agent_kind_antigravity_from_str` — `"antigravity".parse::<AgentKind>()` → `Ok(Antigravity)` (case-insensitive); Display/serde round-trip lowercase; error message lists antigravity
    - Test 2 (RED then GREEN): `agent_kind_antigravity_driver_for` — `driver_for(Antigravity).name() == "Antigravity"`
    - Test 3 (RED then GREEN, F6): `antigravity_conformance_enrollment` — the hardcoded conformance array is `[Box<dyn AgentDriver>; 5]` and the Antigravity driver passes all 7 contract checks (1 name + 5 per-stage DEVFLOW_RESULT prompts + 1 program). UNIQUELY NAMED — the old `conformance` filter matched 2 pre-existing tests and would pass with zero Antigravity code
  </behavior>
  <action>
    **Part A: state.rs** — add `Antigravity` variant + Display arm + FromStr arm + AgentParseError list; unit tests named `agent_kind_antigravity_*` (unique prefix).
    **Part B: agents/mod.rs** — `driver_for` arm, `pub mod antigravity;` + `pub use antigravity::AntigravityDriver;`, the `driver_for_returns_correct_names` assertion, and the conformance-array enrollment (4 → 5). Add the uniquely-named test `antigravity_conformance_enrollment` (assert array length 5 AND iterate the antigravity driver through `test_contract()`).
    **Part C: crates/devflow-core/tests/agent_kind_antigravity.rs** — public-API integration tests (FromStr/Display/serde/driver_for through `devflow_core::`).
  </verify>
  <automated>cargo test -p devflow-core --lib agent_kind_antigravity -- --nocapture && cargo test -p devflow-core --lib antigravity_conformance_enrollment -- --nocapture && cargo test -p devflow-core --test agent_kind_antigravity -- --nocapture</automated>
  <done>
    - Variant + dispatch + exports; conformance array is 5 drivers; Antigravity passes all 7 checks
    - `antigravity_conformance_enrollment` is the only test matching its filter (0 on the unmodified tree — F6)
    - Integration file passes (real `1 passed`, non-zero `filtered out`); cargo compiles
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: devflow doctor Antigravity entry with an assertable seam (commands.rs)</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    crates/devflow-cli/src/commands.rs (2195-2210) — doctor() opens; cmd_check is a NESTED fn (2202) with no assertable seam (F7)
    crates/devflow-cli/src/commands.rs (2286-2340) — the hardcoded checks vec (git/sh/cargo/gh/claude/codex/opencode/pi/pi_subagent)
    crates/devflow-cli/src/preflight.rs (84-97) — agent_program/ensure_agent_binary (unchanged)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): `doctor_checks()` (the extracted seam) contains an entry for `agy` with the `--version` probe and an "antigravity" label
    - Test 2 (F7, env-dependence made explicit): with a stubbed `agy` on PATH, `devflow doctor` reports the antigravity check present; with `agy` absent, the check reports absent/warn — never a hard failure, never a false green
    - Test 3: preflight untouched (agent_program/ensure_agent_binary)
  </behavior>
  <action>
    Edit `crates/devflow-cli/src/commands.rs`:
    1. **Extract a seam (F7):** move the checks-vec construction out of `doctor()` into a named function `fn doctor_checks() -> Vec<Check>` (the `cmd_check` nested fn moves with it or becomes module-level) so a unit test can assert the list. `doctor()` calls `doctor_checks()`.
    2. Add `cmd_check("antigravity", "agy", "--version", "<install hint>")` to the list (presence-only, D-04; `agy --version` never invokes the model — the `-p --help` hazard does not apply to `--version`).
    3. Unit tests for Tests 1-2 with the unique prefix `doctor_includes_antigravity_*`.
  </verify>
  <automated>cargo test -p devflow --bin devflow doctor_includes_antigravity -- --nocapture</automated>
  <done>
    - `doctor_checks()` seam exists; antigravity/`agy` entry present with `--version`
    - PATH-based test proves present/absent reporting; preflight untouched
    - Verify shows a real `1 passed` with non-zero `filtered out` — the old `doctor` filter (19 pre-existing tests) is NOT used (F6)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: phase7_cli.rs — canary-aware agy stub, marker-less/happy/discrimination regressions, MonitorReapGuard defined here (ANTG-03, F4, codex-3 e2e, antigravity notice (a))</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    crates/devflow-cli/tests/phase7_cli.rs (1190-1254) — pi_stub, wait_for_gate, pi_marker_less_run_does_not_advance (THE model)
    crates/devflow-cli/tests/phase7_cli.rs (100-145) — run_devflow / run_devflow_inner (asserts success at ~142) / run_devflow_legacy_launch (111-125: NOT the escape hatch for stream-path tests)
    crates/devflow-cli/tests/phase7_cli.rs (248-263) — the stubbed-`claude` canary fixture (echoes DEVFLOW_DELIVERY_CANARY_ token; the pattern Task 8's agy stub follows)
    crates/devflow-cli/src/test_support.rs (495-630) — ReapMonitorOnDrop / reap_monitor_pid (the per-PID reap model MonitorReapGuard mirrors; integration tests use public devflow-core APIs — no lib.rs)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): `marker_less_antigravity_never_advances` — stubbed `agy` exits 0 with no events; `wait_for_gate` → `stage == Stage::Plan` AND `gate_pending` (Define advanced legitimately; the FIRST commit-gated stage gates)
    - Test 2: `antigravity_parses_devflow_result_from_stream` — stub emits ANTIGRAVITY-shaped events with the marker inside `result.response`; stage advances past the commit gate; NOT gated
    - Test 3: `antigravity_init_without_marker_gates_at_plan` — discrimination control (transport alone never gates; only the marker does)
    - Test 4 (codex-3, e2e schema): the happy-path stub REJECTS a non-`{"event":"user",...}` first turn (exits 1 with a diagnostic) — an impl that left the writer on the Claude `type`-form FAILS this test, not just the unit helper
    - Test 5 (F4): the fake_bin_dir includes the canary-aware agy stub — the stub answers `*DEVFLOW_DELIVERY_CANARY_*` turns by echoing the token in an antigravity-shaped `event:result` (`response`), so the AntigravityCanaryLauncher at Stage::Define gets `Confirmed`; `run_devflow_legacy_launch` is NOT used (the subject IS the stream path)
    - Test 6 (antigravity notice (a)): `MonitorReapGuard` is DEFINED in this task (phase7_cli.rs) and bound in these tests — 41-02 Task 1 later does the systematic pass across pre-existing tests
  </behavior>
  <action>
    Append to `crates/devflow-cli/tests/phase7_cli.rs`:
    1. `fn antigravity_stub(mode: StubMode) -> String` — a fake `agy` script with three behaviours (canary-echo, marker-stream, marker-less-exit-0) and a schema check: `read -r turn`, verify the line parses as JSON with `"event":"user"` (else exit 1 — codex-3), then act per mode. Model on the stubbed-claude fixture at 249-263.
    2. `marker_less_antigravity_never_advances` / `antigravity_parses_devflow_result_from_stream` / `antigravity_init_without_marker_gates_at_plan` — run `devflow start --phase 07 --agent antigravity --mode supervise` with the fake PATH; use `wait_for_gate`; NOT `#[ignore]` (default suite, like the pi tests).
    3. `struct MonitorReapGuard` (+ `after_launch(&State)`) mirroring `ReapMonitorOnDrop` (test_support.rs:573) using public `devflow_core::agent::terminate_and_verify` + `agent_running`; bind in all three tests. Define it HERE (41-01 Task 8); 41-02 Task 1 does the systematic pass and the suite audit.
  </verify>
  <automated>cargo test -p devflow --test phase7_cli antigravity -- --nocapture</automated>
  <done>
    - Three regression tests pass in the DEFAULT suite (no `#[ignore]`); gate at Plan via wait_for_gate + gate_pending
    - Canary-aware stub confirms AntigravityCanaryLauncher at Define; no legacy-launch escape hatch used
    - Schema-rejecting stub proves the event-key writer end-to-end (codex-3)
    - `MonitorReapGuard` defined and bound (antigravity notice (a)); all tests reap their monitors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI argv → process spawn | `--agent antigravity` → driver argv (no `-p`, `--print-timeout 60m`) → subprocess |
| Child stdin (prompt injection) | Monitor writes the first turn as `{"event":"user",...}` (agent-aware writer); child parses it |
| Child stdout → close + parse | CloseRule (agent-aware) releases stdin on the marker; parser extracts `result.response`; ERROR envelope → Failed |
| Delivery canary | Once-per-run foreground proof that a stream session wakes and returns planted work — now agy-based for Antigravity |
| Process exit code | Layer-2 fallback when the stream lacks a marker |
| Unattended mode | `--mode auto` gating for undogfooded drivers (preflight C2) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-01 | Tampering | DEVFLOW_RESULT marker in stream | medium | mitigate | `parse_antigravity_event_result` validates structure (marker ONLY from `result.response` → `parse_marker_lines`); torn-tail fail-closed; ERROR envelope → `Some(Failed{reason})` |
| T-41-02 | Tampering | Untrusted agent stdout parsed as completion | high | mitigate | Marker-less → `None` → Layer 2 → gate; regression gates at commit-gated Plan via `wait_for_gate` + `gate_pending`; discrimination control (Task 8 Test 3) |
| T-41-03 | Tampering | Supply chain on `agy` → `antigravity-cli` | medium | mitigate | Presence-only (D-04); no version pin (drifted 1.1.14→1.1.15→1.1.16); marker-less contract backstops. `--print-timeout` default-5m hazard mitigated by the 60m override (F3) + 6-minute probe in 41-VALIDATION |
| T-41-04 | Availability | Close rule never fires → successful stage recorded as IdleTimeout | **high** | mitigate | Agent-aware CloseRule (B1): `event:"result"` + `result.response` marker closes the stream; drain arms stated vacuously satisfied; Task 2 Test 2/3 prove both |
| T-41-05 | Tampering | Widened predicate arms the Claude canary on Antigravity runs | **high** | mitigate | `AntigravityCanaryLauncher` (B2/D-07): agy-based, event-key turn, agent-aware close rule; dispatch by agent at canary_gate; Task 3 Test 3/4 |
| T-41-06 | Availability | Claude-measured idle timeout silently applied to Antigravity | medium | mitigate | `idle_timeout_setting_for(agent)` (B3/D-08) with a DECIDED Antigravity default + its own env var; doc comment resolved |
| T-41-07 | Denial | `--mode auto` silently permitted for an undogfooded driver | medium | mitigate | Preflight C2 decision (F5/D-04): Antigravity auto refused with explicit cause until dogfooded; Task 4 |
| T-41-08 | Tampering | Legacy opt-out env routes Antigravity to Legacy (dead stdin) | medium | mitigate | `!legacy_opt_out` applies only to Claude (D-10); Task 3 Test 2 |
| T-41-09 | Denial | `devflow doctor` silently omits Antigravity | low | mitigate | `doctor_checks()` seam + entry + PATH test (D-04/F7, Task 7) |
| T-41-10 | Denial | Verify commands pass with zero new code (false green) | medium | mitigate | Every `<automated>` filter names tests unique to the new work and FAILS on the unmodified tree (F6/codex-6); "0 passed = FAIL" rule enforced in every done section |
| T-41-11 | Tampering | Executor follows superseded RESEARCH/PATTERNS argv or close-rule claims | medium | mitigate | 41-CONTEXT.md round-3 marks them superseded; this plan carries the corrected facts inline |
| T-41-SC | Tampering | Package legitimacy (new Rust dependencies) | N/A | accept | No new crate dependencies — argv/process-level only |

**Disposition rationale:**
- **T-41-04/05/06** are the round-2 blockers made first-class tasks: close rule (Task 2), canary (Task 3), idle policy (Task 2). A run that resolves, conforms, and doctors but idle-times-out on every real stage is exactly the shape this phase's reviews exist to prevent.
- **T-41-03:** the 5m print-timeout default is below the documented 47m stage; the 60m override plus the scheduled 6-minute probe covers it.

**Related ASVS controls:** V4 (Access Control — skip-permissions embedded in wrapper), V5 (Input Validation — JSON structure validation + marker contract + ERROR envelope).
</threat_model>

## Artifacts this phase produces

### Code Artifacts

**New files:**
1. `crates/devflow-core/src/agents/antigravity.rs` — `AntigravityDriver` (argv incl. `--print-timeout 60m`; `parse_completion` delegate; spawn smoke test)
2. `crates/devflow-core/tests/agent_kind_antigravity.rs` — public-API FromStr/Display/serde/driver_for tests

**Modified files:**
3. `crates/devflow-core/src/agent_result.rs` — `is_antigravity_event_stream`, `parse_antigravity_event_result` (+ ERROR envelope), `event_is_top_level_antigravity_result_marker`, `evaluate_layer1` wiring
4. `crates/devflow-core/src/monitor.rs` — `user_turn_line_for`, agent-aware `CloseRule`, `idle_timeout_setting_for`, PipeOwning writer uses the agent-aware turn
5. `crates/devflow-core/src/canary.rs` — `AntigravityCanaryLauncher`
6. `crates/devflow-cli/src/pipeline_launch.rs` — widened stream predicate (Claude-only legacy opt-out), canary dispatch by agent, AutoChainGuard comment
7. `crates/devflow-cli/src/preflight.rs` — C2 decision (auto refused until dogfooded) + tests
8. `crates/devflow-cli/src/commands.rs` — `doctor_checks()` seam + antigravity/`agy` entry + tests
9. `crates/devflow-core/src/state.rs` + `agents/mod.rs` — `AgentKind::Antigravity`, dispatch, conformance enrollment (4→5, unique test name)
10. `crates/devflow-cli/tests/phase7_cli.rs` — canary-aware agy stub, 3 regression tests, `MonitorReapGuard` defined here

### Semantic Artifacts

- **Full transport:** write (`user_turn_line_for`), read/parse (`parse_antigravity_event_result` + ERROR envelope), close (`event_is_top_level_antigravity_result_marker`) — all agent-aware (B1/D-02/D-03)
- **Canary:** `AntigravityCanaryLauncher` — delivery contract proven with agy, not Claude (B2/D-07)
- **Policy:** `idle_timeout_setting_for` — decided, not inherited (B3/D-08)
- **Routing:** stream predicate + Claude-only legacy opt-out + AutoChainGuard comment (D-10, F5)
- **Preflight:** `--mode auto` refused until dogfooded (F5/D-04)
- **Doctor:** `doctor_checks()` seam + entry (D-04/F7)
- **Regression gates:** marker-less never advances a commit-gated stage; happy path only on a real marker; schema-rejecting stub (ANTG-03, codex-3)

### Non-Code Artifacts

- Inline doc comments recording the re-derived argv/schema/close/canary/timeout facts with review evidence references
