---
phase: 44-codex-end-to-end-verification
plan: 02
type: execute
wave: 3
depends_on: ["44-01"]
files_modified:
  - crates/devflow-cli/src/pipeline_gate.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-core/src/recover.rs (tests only — zero production-code changes)
autonomous: true
requirements: [CODE-01]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A relaunch that fails before the monitor pid is persisted leaves the phase's cron-instructions record on disk, available for a later retry (D-15)."
    - "A workflow that reaches terminal ship completion has no cron-instructions record left behind, even if the resume path never ran (D-17)."
    - "Every cron-record deletion this phase introduces writes a `cron_instructions_consumed` audit line naming its trigger (D-18)."
    - "A phase's event stream still ends in `workflow_finished` — the new ship-side event does not displace the terminal event existing tests assert on."
    - "`recover::clean` still deletes unconsumed cron records as a reset path; consumption did not redefine cleanup (D-19)."
  artifacts:
    - "crates/devflow-cli/src/pipeline_gate.rs — ship-side deletion inside `finish_workflow_with_gate_timeout`"
    - "crates/devflow-cli/src/pipeline_launch.rs — failed-launch negative-control test"
    - "crates/devflow-core/src/recover.rs — D-19 regression test pinning existing cleanup semantics"
  key_links:
    - "workflow::clear_state -> ship::delete_cron_instructions -> events::emit (ordered strictly before `workflow_shipped`/`workflow_finished`)"
    - "monitor::spawn_monitor failure (`?`) -> deletion never reached -> record survives (the load-bearing ordering from 44-01)"
---

<objective>
Make cron-instruction records disappear exactly when they have genuinely been consumed and never
before: prove the failed-relaunch case preserves the record, add the ship-completion belt-and-braces
deletion, and pin `recover::clean`'s unchanged reset semantics.

Purpose: #153. A record deleted too early strands a rate-limited phase with no auto-resume
instruction and no operator signal; a record left behind after a successful ship re-fires a cron
against a workflow that no longer exists.

Output: two audited deletion triggers with a proven-preserving failure path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-codex-end-to-end-verification/44-CONTEXT.md
@.planning/phases/44-codex-end-to-end-verification/44-RESEARCH.md
@.planning/phases/44-codex-end-to-end-verification/44-PATTERNS.md
@.planning/phases/44-codex-end-to-end-verification/44-00-SUMMARY.md
@.planning/phases/44-codex-end-to-end-verification/44-01-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files **created or changed by this plan**:

| Kind | Symbol / path | Notes |
|---|---|---|
| event name | `cron_instructions_consumed` with `trigger: "ship_complete"` | second emission site; the `"resume_consumed"` site was created in 44-01 |
| test fn | `failed_relaunch_preserves_the_phase_cron_instructions_record` | D-15 negative control, `pipeline_launch.rs` |
| test fn | `ship_completion_deletes_any_remaining_cron_instructions_record` | D-17, `pipeline_gate.rs` |
| test fn | `ship_completion_cron_deletion_precedes_the_terminal_workflow_finished_event` | ordering guard, `pipeline_gate.rs` |
| test fn | `clean_still_deletes_unconsumed_cron_instructions` | D-19, `recover.rs` |

Sibling plans produce: `resume --agent` / `agent_handoff` (44-01);
`ship::hermes_schedule_from_retry_after`, `RetryTimestamp::to_iso_utc` (44-03).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove a failed relaunch never consumes the record (D-15 negative control)</name>

  <files>crates/devflow-cli/src/pipeline_launch.rs</files>

  <read_first>
    - `crates/devflow-cli/src/pipeline_launch.rs` — `spawn_agent_and_record` (~line 969) as 44-01 left
      it, paying attention to which steps can return early via `?` above the deletion call, and the
      existing test `launch_stage_inner_clears_monitor_pid_on_early_failure` (~line 2089), which is
      the exact harness analog for forcing a launch failure and asserting on state afterwards.
    - `.planning/phases/44-codex-end-to-end-verification/44-01-SUMMARY.md` — how 44-01 actually
      implemented the deletion call and its event gating, which may differ in detail from this plan's
      expectation.
    - `crates/devflow-core/src/ship.rs` — `cron_instructions_path`, `write_cron_instructions`,
      `build_single_agent_cron_instructions`, for constructing the fixture record.
  </read_first>

  <behavior>
    - Test 1: a phase with a cron-instructions record on disk, relaunched such that
      `ensure_agent_binary` fails (agent binary absent from PATH). The call returns `Err`, and the
      cron-instructions record still exists.
    - Test 2: the same fixture but with `monitor::spawn_monitor` failing rather than
      `ensure_agent_binary`. The call returns `Err`, and the record still exists. This is the case
      that actually matters — `spawn_monitor` is the last `?` above the deletion, so it is the
      narrowest failure the ordering has to survive.
    - Test 3 (paired positive control): the same fixture with a working stub agent. The call
      succeeds, the record is gone, and a `cron_instructions_consumed` event with
      `trigger: "resume_consumed"` exists.
  </behavior>

  <action>
    Add the three tests to `pipeline_launch.rs`'s test module.

    Test 3 is not redundant with 44-01's tracer: it is the negative control for Tests 1 and 2. A
    deletion call site that was accidentally never reached at all — for example left behind a
    condition that is always false — passes both failure tests and would look correct. Only the
    paired success case distinguishes "the record survives failure" from "the record always
    survives". Give it a doc comment saying so.

    Force the failures the way the existing harness does rather than by mocking: read
    `launch_stage_inner_clears_monitor_pid_on_early_failure` and reuse its PATH-manipulation and
    `ENV_MUTEX` discipline. If `spawn_monitor` cannot be made to fail by fixture alone, do not fake
    it — instead assert the ordering structurally (the deletion appears after the second
    `workflow::save_state` in `spawn_agent_and_record`) AND state plainly in the test's doc comment
    that Test 2 covers the reachable failure only, so the SUMMARY does not later over-claim coverage
    the tests do not have.

    Assert the record's presence/absence with
    `devflow_core::ship::cron_instructions_path(root, phase).exists()`, not by listing the `.devflow`
    directory — an empty glob and a real absence look identical in a directory listing and the
    distinction is exactly what these tests exist to make.

    Assert the event by parsing each `.devflow/events.jsonl` line as JSON and indexing the `event`
    and `trigger` fields, letting a missing field fail the test rather than reading as a benign
    `None`.
  </action>

  <verify>
    <automated>cargo test -p devflow --bin devflow cron_instructions -- --nocapture 2>&amp;1 | rg 'test result: ok.'</automated>
  </verify>

  <acceptance_criteria>
    - The three named tests exist and pass; the filtered run reports a non-zero passed count AND a
      non-zero `filtered out` count.
    - Each failure test was observed failing against a deliberately mis-ordered deletion (move the
      call above `spawn_monitor`, confirm the test reports `test result: FAILED`, then restore) —
      record the observed failure output in the SUMMARY. A revert that hangs or errors out is not
      evidence; the failing direction must actually print a failure.
    - Test 3's doc comment states it is the negative control for Tests 1 and 2.
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` are clean.
  </acceptance_criteria>

  <done>
    A relaunch that fails keeps the record; a relaunch that succeeds consumes it; and the difference
    has been demonstrated in both directions, not asserted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ship-completion deletion and its audit event (D-17, D-18)</name>

  <files>crates/devflow-cli/src/pipeline_gate.rs</files>

  <read_first>
    - `crates/devflow-cli/src/pipeline_gate.rs` — `finish_workflow_with_gate_timeout` (~line 228) in
      full, especially lines ~272-305: the `Gates::cleanup` calls, `workflow::clear_state`,
      `registry::deregister`, and the comment at ~line 289 explaining that existing tests assert a
      phase's event stream ENDS in `workflow_finished`. Also read the file's import block (~lines
      22-35) and the existing tests
      `advance_ship_success_emits_workflow_shipped_and_ship_evidence_reports_shipped` (~line 749) and
      the assertion at ~line 1178 that pins the terminal event.
    - `crates/devflow-core/src/ship.rs` — `delete_cron_instructions` (~line 139), already idempotent
      with its own `delete_cron_instructions_is_idempotent` test, so no existence check is needed
      before calling it.
    - `crates/devflow-core/src/recover.rs` — the fail-soft call-site idiom at ~lines 117 and 140.
    - `.planning/phases/44-codex-end-to-end-verification/44-01-SUMMARY.md` — the exact
      `cron_instructions_consumed` field shape 44-01 settled on, so this second site matches it.
  </read_first>

  <behavior>
    - Test 1: a phase driven to terminal ship completion with a cron-instructions record still on
      disk. After `finish_workflow_with_gate_timeout`, the record is gone and a
      `cron_instructions_consumed` event with `trigger: "ship_complete"` exists.
    - Test 2: the same run's `.devflow/events.jsonl` still has `workflow_finished` as its LAST line,
      with `workflow_shipped` before it — the new event must not displace the terminal event.
    - Test 3: ship completion with NO cron record on disk succeeds unchanged and emits no
      `cron_instructions_consumed` event.
  </behavior>

  <action>
    In `finish_workflow_with_gate_timeout`, add the ship-side deletion immediately after
    `registry::deregister(project_root, state.phase)` and strictly BEFORE the `workflow_shipped`
    emission. The ordering is load-bearing and already documented in that function's own comment:
    existing tests assert the phase's event stream ends in `workflow_finished`, so anything appended
    after it breaks them.

    Mirror 44-01's resume-side shape exactly rather than inventing a second convention:
    use the `devflow_core::ship::consume_cron_instructions(project_root, state.phase)` helper.
    When it returns `Ok(Some(path_kind))`, emit `cron_instructions_consumed` with `trigger: "ship_complete"`
    and `path_kind: path_kind`. If deletion returns `Err` (e.g. disk permission issue), log on `Err`
    and never propagate (a deletion failure must not fail a completed ship), but do NOT emit the
    consumption event since the file was not deleted (Codex Terra Finding 5). Do not include `phase`
    in the fields object; `events::emit` stamps it into the envelope and payload keys lose to envelope keys.

    Add `ship` to this file's existing combined `use devflow_core::{events, lock, registry,
    workflow};` import rather than introducing a separate `use` line — the file groups core imports
    on one line and a second spelling invites drift.

    Test 3 is the negative control: without it, an unconditional emission that fires on every ship
    completion regardless of whether a record existed passes Tests 1 and 2 and produces a permanently
    misleading audit log claiming records were consumed that never existed.
  </action>

  <verify>
    <automated>cargo test -p devflow --bin devflow ship_completion -- --nocapture 2>&amp;1 | rg 'test result: ok.'</automated>
    <automated>cargo test -p devflow --bin devflow advance_ship_success_emits_workflow_shipped_and_ship_evidence_reports_shipped 2>&amp;1 | rg 'test result: ok. 1 passed'</automated>
    <automated>cargo test -p devflow --bin devflow until_stop_never_emits_workflow_shipped_and_ship_evidence_reports_not_shipped 2>&amp;1 | rg 'test result: ok. 1 passed'</automated>
    <automated>cargo test --workspace 2>&amp;1 | rg -c '^test result: FAILED' | rg '^0$'</automated>
  </verify>

  <acceptance_criteria>
    - The three named tests exist and pass; the filtered run reports non-zero passed and non-zero
      `filtered out` counts.
    - The two pre-existing terminal-event tests each pass as their own one-test invocation — proof
      the new emission did not displace `workflow_finished`.
    - `rg -n 'delete_cron_instructions' crates/devflow-cli/src/pipeline_gate.rs` shows the call on a
      line number lower than the `"workflow_shipped"` emission's line number in the same function.
    - `rg -c 'use devflow_core::ship' crates/devflow-cli/src/pipeline_gate.rs` is 0 — the import was
      folded into the existing combined `use devflow_core::{...}` line as instructed.
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` are clean.
  </acceptance_criteria>

  <reversibility rating="costly">
    Deleting an operator's cron-instruction record is irreversible for that record — if the trigger
    fires wrongly, the auto-resume instruction for a parked run is gone. The code change reverts
    cleanly, but a record already deleted in a real run cannot be recovered, which is why the
    pre-existence check and the D-15 negative control in Task 1 are required rather than optional.
  </reversibility>

  <done>
    A workflow that reaches terminal ship completion leaves no cron-instructions record, records why
    it deleted one when it did, and still ends its event stream in `workflow_finished`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin `recover::clean`'s unchanged reset semantics (D-19)</name>

  <files>crates/devflow-core/src/recover.rs</files>

  <read_first>
    - `crates/devflow-core/src/recover.rs` — `clean` (~line 87) and `clean_phase` (~line 130), their
      two `delete_cron_instructions` call sites (~lines 117 and 140), the existing test module
      (~line 199), and the closest existing analogs `clean_clears_stale_phase_state` (~line 385) and
      `clean_phase_clears_only_the_named_phase` (~line 421) for fixture construction.
    - `crates/devflow-core/src/ship.rs` — `build_single_agent_cron_instructions`,
      `write_cron_instructions`, `cron_instructions_path`.
  </read_first>

  <behavior>
    - Test 1: a stale phase with an UNCONSUMED cron-instructions record. `recover::clean` still
      deletes it — cleanup is a reset path and did not become conditional on consumption.
    - Test 2: `recover::clean_phase` on a named phase deletes only that phase's record and leaves a
      second phase's record intact.
  </behavior>

  <action>
    Add the two tests to `recover.rs`'s existing test module. This task adds NO production code —
    `recover.rs` must be behaviorally unchanged by this phase (D-19). The tests exist because #153
    introduces two new deletion triggers elsewhere, and the risk they create is that a future reader
    concludes "deletion means consumption" and narrows `recover::clean` to match. These tests make
    that narrowing fail loudly.

    Each test's doc comment must state that D-19 keeps `recover::clean` a reset/cleanup path,
    explicitly distinct from the consumption triggers added in `pipeline_launch.rs` and
    `pipeline_gate.rs`, and that the record it deletes is deliberately one that was never consumed.

    If either test cannot be written without modifying `recover.rs`'s production code, stop and
    report it rather than modifying it — a required production change here would mean D-19's premise
    ("existing semantics remain") is false, which is a finding for the SUMMARY, not something to
    patch through.
  </action>

  <verify>
    <automated>cargo test -p devflow-core --lib clean_still_deletes_unconsumed_cron_instructions 2>&amp;1 | rg 'test result: ok. 1 passed'</automated>
    <automated>cargo test -p devflow-core --lib clean_phase_deletes_only_the_named_phase_cron_record 2>&amp;1 | rg 'test result: ok. 1 passed'</automated>
    <automated>cargo test -p devflow-core --lib recover 2>&amp;1 | rg 'test result: ok.'</automated>
  </verify>

  <acceptance_criteria>
    - Both named tests exist and pass as separate one-test invocations.
    - `git diff crates/devflow-core/src/recover.rs` shows changes confined to the `mod tests` block —
      zero production-code lines changed (D-19).
    - Both tests' doc comments name D-19 and distinguish reset from consumption.
    - `cargo test --workspace` reports zero `FAILED` result lines.
  </acceptance_criteria>

  <done>
    `recover::clean` still deletes unconsumed records, and a regression test now prevents the new
    consumption triggers from being mistaken for the cleanup contract.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow -> `.devflow/cron-instructions-NN.json` | the only durable record of how a parked run resumes itself |
| DevFlow -> external Hermes scheduler | a surviving record can fire a cron against a workflow that no longer exists |
| workflow state -> `.devflow/events.jsonl` | the audit trail readers use to reconstruct why a record vanished |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-06 | Denial of Service | premature deletion on a failed relaunch | high | mitigate | Deletion sits downstream of every `?` in `spawn_agent_and_record`; Task 1 proves both directions (record survives failure, record is consumed on success) and requires the failing direction to actually print a failure |
| T-44-07 | Denial of Service | a surviving record firing a resume cron against a shipped workflow | medium | mitigate | Ship-side belt-and-braces deletion at `finish_workflow_with_gate_timeout` after `workflow::clear_state` (D-17), Task 2 Test 1 |
| T-44-08 | Repudiation | a record disappearing with no recorded reason | medium | mitigate | `cron_instructions_consumed` event at both triggers carrying `trigger` and `path_kind` (D-18); Task 2 Test 3 prevents the inverse failure of claiming consumption that never happened |
| T-44-09 | Tampering | deletion widening to another phase's record | low | mitigate | `cron_instructions_path` is per-phase by construction (14a / 13-DEFERRED-CR-03); Task 3 Test 2 pins the named-phase scoping |
| T-44-10 | Denial of Service | a deletion I/O error aborting a completed ship | low | mitigate | Fail-soft call sites (log, never propagate), matching `recover.rs`'s existing idiom |
| T-44-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; 44-RESEARCH.md's Package Legitimacy Audit records N/A for the phase |
</threat_model>

<verification>
- `cargo test --workspace` — zero `FAILED` result lines.
- The two pre-existing terminal-event tests in `pipeline_gate.rs` each report `1 passed`.
- `git diff crates/devflow-core/src/recover.rs` touches the test module only.
- `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check` clean.
</verification>

<success_criteria>
Cron-instruction records are consumed at exactly two proven points — a durable relaunch and a
terminal ship — every deletion is audited, a failed relaunch demonstrably preserves the record, and
`recover::clean`'s reset semantics are unchanged and pinned.
</success_criteria>

<output>
Create `.planning/phases/44-codex-end-to-end-verification/44-02-SUMMARY.md` when done.
</output>
