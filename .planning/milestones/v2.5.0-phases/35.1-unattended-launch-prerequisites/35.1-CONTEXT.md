# Phase 35.1: Unattended-Launch Prerequisites - Context

**Gathered:** 2026-08-07
**Status:** Ready for planning

<domain>
## Phase Boundary

`devflow start --mode auto` can complete a phase with no human present: ordinary GSD
`gate="blocking"` checkpoints resolve instead of stalling the run, and a preflight refuses the
launch — loudly and before it starts — when the conditions that make that true do not hold.

This phase is about *whether an unattended run can proceed at all*. It is not about how an
unattended agent decides. The first-option `decision` checkpoint behaviour is 999.94 and stays
out of scope.

</domain>

<decisions>
## Implementation Decisions

### Chain-flag lifetime and leak repair

- **D-01:** `_auto_chain_active` is cleared by **two independent mechanisms**: an RAII guard that
  fires on every in-process exit (normal, error, panic-unwind), and a force-clear on every
  `devflow start` / `devflow resume` before launching. A SIGKILL between them is repaired by the
  next run rather than carried forward. The guard alone was rejected: it cannot survive SIGKILL,
  and a leaked `true` is exactly the committed-and-fires-next-phase failure criterion 2 names.

  **Two additions from adversarial review — the planner must satisfy both:**

  1. **The repair must reach the *committed* tree, not only the working tree.** One review lane
     cleared this decision by arguing a leak cannot propagate because "Ship is impossible after a
     SIGKILL". That reasoning is wrong: `devflow resume` exists precisely to continue a killed run.
     The live path is — flag leaks into a commit on the feature branch → kill → operator resumes →
     run completes → Ship merges the branch → `develop` carries `_auto_chain_active: true` → the
     next phase forks from it and chains. A force-clear that only edits the working tree leaves
     that path open.
  2. **The happy path leaks too, and it leaves the tree dirty.** For the whole duration of the Code
     stage, DevFlow will have modified a *tracked* file (`.planning/config.json`). Consequences to
     design against: any `git add -A` sweeps the `true` into a commit — this repo has already had
     exactly that incident, recorded in `CLAUDE.md`; a `git reset --hard` restores a committed
     `true`; and DevFlow's own `workflow_started` event records a `dirty` field (observed
     `"dirty":"false"` on the 2026-08-07 run), so a self-inflicted dirty tree may interact with
     DevFlow's staleness and dirty-tree checks.
     *Scope note:* the review lane that raised this claimed GSD commits `config.json` automatically
     via `commit_docs`. **That mechanism was checked and does not hold** — every `query commit` in
     `execute-phase.md` passes an explicit `--files` list and none includes `config.json`. The risk
     is a sweeping `git add`, not an automatic GSD commit. Narrower than reported, still real.
- **D-02:** The regression tests must exercise **both** failure paths as separate cases — one that
  SIGKILLs the process mid-stage and asserts the next start clears the stale flag, one that forces
  an in-stage error and asserts the guard cleared it. Kept separate so a regression in either
  mechanism is attributable instead of masked by the other. A single combined test was rejected
  for that reason.
- **D-03:** A repaired stale flag is **not silent**: emit an event to `.devflow/events.jsonl` and
  print a notice on stdout. A stale flag is evidence a prior run was killed; repairing it quietly
  discards that signal, and this project logs repairs and escape hatches loudly rather than
  applying them quietly.

### Auto-mode stage set

- **D-04 (REVISED 2026-08-07 after adversarial review):** Checkpoint auto-approval covers **the
  Code stage and the `--gaps-only` fix loop only**. The Plan stage is **excluded**.

  This reverses the original decision, which included Plan. Both external review lanes and an
  independent source trace agreed it is unimplementable: **the flag that would enable checkpoint
  auto-approval at Plan is the same flag that makes plan-phase chain into execute-phase.**
  Verified in source:
  - `~/.claude/gsd-core/bin/lib/check-command-router.cjs:107` — `active: autoChainActive || autoAdvance`.
    There is no third key for "bypass without chaining".
  - `plan-phase.md:1564` — *"If `--auto` or `--chain` flag present OR `AUTO_CHAIN` is true OR
    `AUTO_CFG` is true"* → banner *"GSD ► AUTO-ADVANCING TO EXECUTE"* → chains via
    `Skill(skill="gsd-execute-phase", args="${PHASE} --auto --no-transition ${GSD_WS}")`.
  - `plan-phase.md:540`/`:555` — the same `AUTO_CHAIN` selects Branch 5 vs Branch 6, and Branch 6
    is what evaluates blocking gates. So the flag genuinely would fix the Plan stall; it just
    cannot do so without also chaining.
  - G-03 blocks the obvious dodge: clearing the flag after launch does not help, because the
    decision at `:1564` reads the value loaded into INIT at step 1, before the clear at `:1547`.
  - `plan-phase.md:1560` blocks the other dodge: passing `--auto` **sets** the flag to true.

  **Known limitation, to be stated plainly in the phase's own docs and not papered over:** an
  unattended run can still stall at a Plan-stage checkpoint. DevFlow's Define stage is a no-op, so
  Plan is the earliest stallable stage — meaning 35.1 does not fully deliver "can complete a phase
  with no human present". Closing it requires an upstream GSD change splitting `_auto_chain_active`
  into separate bypass and chaining flags. Operator considered making that change a dependency of
  35.1 and declined; it stays upstream work tracked as G-01.
- **D-05:** The stage set is expressed as a **named constant** consulted by the launch path,
  following the existing `STREAM_JSON_STAGES` precedent (`crates/devflow-cli/src/pipeline_launch.rs:541`,
  widened by Phase 34). One greppable place to read and change it. An inline per-stage match was
  rejected — implicit policy in control flow is how the `{phase:02}` glob hazard stayed hidden.
  Config-driven was rejected as speculative configurability that also adds a way to silently
  disable the whole fix.

  **Required doc comment (review finding).** The `STREAM_JSON_STAGES` analogy is imperfect and the
  constant must say so: that constant lists all five stages because the transport choice is purely
  DevFlow-internal and has no upstream side effects, whereas **this** constant's flag has
  *different upstream effects per stage* — harmless at Code, chaining at Plan. Without that note,
  the constant's uniform shape invites a future contributor to "complete" it by adding `Stage::Plan`
  and silently reintroduce the D-04 defect.
- **D-06:** This is checkpoint approval **only**. `workflow.auto_advance` is still NOT set
  persistently (criterion 3, upstream G-01) — stage-chaining stays banned.
  **Corrected by review:** the original wording claimed the two flags are separable concerns. They
  are not — see D-04. `_auto_chain_active` is safe to set for Code because *execute-phase* does not
  chain from it; it is unsafe for Plan because *plan-phase* does. The separation is per-stage
  behaviour, not per-flag.

### Preflight refusal policy

- **D-07:** The preflight reports three distinct per-condition states — holds / does not hold /
  could not determine — and **refuses on anything that is not a definite pass**. Fail closed.
  This applies the 999.86 lesson from one phase earlier (a predictor that guessed rather than
  probed false-negatived twice) and the roadmap's existing "unreachable ≠ absent" rule.
  Warn-and-proceed was rejected in the criterion's own words: a warning is read by nobody in an
  unattended run.
- **D-08:** The preflight **gates `--mode auto` and reports on `--mode supervise`**. One code
  path, two consequences: refusal only where the prerequisites actually matter, but the
  per-condition report still prints in supervise so an operator can rehearse viability before
  committing to an unattended run. Gating every launch was rejected — it would block supervise
  runs that are fine with a human answering checkpoints.
- **D-09:** **No override.** No `--force-unattended` flag, no environment variable. A refusal is
  final; fix the condition or launch in supervise. — **Reversibility:** costly — adding an
  override later is easy, but removing one after operators build habits around it is not, and
  the standing principle is that DevFlow advances as far as the rules permit and stops at the
  first hard gate rather than routing around one. The `--legacy-claude-launch` precedent for a
  loudly-logged escape hatch was considered and explicitly declined here.

  **Objection on record from both review lanes; the decision stands.** A buggy preflight that
  false-positives makes unattended runs impossible with no in-product recovery — the operator's
  only options are to fix the condition, run in supervise with a human answering checkpoints
  (which defeats the point of an overnight run), or edit Rust source and rebuild. One lane rated
  this MEDIUM and judged the supervise fallback adequate; the other rated it MEDIUM and called it
  a denial-of-service. Recorded here so the planner does not silently "fix" it by adding the
  override the operator declined. If the preflight's own reliability becomes a problem in practice,
  reopen this as a decision rather than working around it.

### The simulated unattended run

- **D-10:** Criterion 5's run drives a **throwaway fixture project with one trivial plan carrying
  one real GSD `blocking` checkpoint, launched against a real Claude agent**. Cheapest workload
  that still crosses every seam under test, and repeatable. Driving Phase 35.2 for real was
  rejected — a failed drill damages real planning artifacts and cannot be re-run identically. A
  fully mocked agent was rejected because a mock cannot demonstrate that GSD actually
  auto-approves a real checkpoint, which criterion 1 requires be established by driving one.

  **Three constraints added by adversarial review — a fixture that misses these is a proxy
  measurement, which is the exact defect criterion 1 guards against:**

  1. **The fixture must actually enter the `--gaps-only` fix loop.** A single trivial plan
     succeeds at Code and passes Validate on the first try, so the loop never runs — yet the run
     would be reported as covering it. Criterion 1 names the fix loop explicitly, and after D-04's
     narrowing the fix loop is *half of the remaining scope*. The fixture needs a plan that fails
     Validate at least once on purpose.
  2. **The fixture needs a negative control.** Run the same fixture with the flag NOT set and
     require the checkpoint to actually **block**. Without it, an agent that simply proceeds
     without ever hitting a checkpoint is indistinguishable from a successful auto-approval.
  3. **It does not establish DevFlow's own flag management.** A fixture with a pre-baked config
     proves *GSD auto-approves when the flag is true* — not *DevFlow sets it at the right moment
     and clears it at the right moment*. Those need their own coverage; do not let a green fixture
     stand in for D-01/D-02.
- **D-11:** The fixture is **pre-baked through CONTEXT.md**; Plan and Code run live. Criterion 5
  names Plan → Code as the seam, so those stages must be real. Pre-baking the plans was rejected
  because it would skip Plan, which D-04 just established as the first place an unattended run
  can stall. Running from Define was rejected as a Claude launch that exercises nothing new.

### Claude's Discretion

The operator did not choose to discuss these; they are open for the planner unless it finds a
reason to escalate:

- Where criterion 6's per-phase Validate-failure observation gets recorded.
- The concrete condition list the preflight checks, and how the required NOT-viable negative
  control fixture is constructed.
- Whether `--yes-ship` interacts with auto-mode at all.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The upstream defects this phase works around
- `.planning/UPSTREAM-GSD-ISSUES.md` §G-01 — `workflow.auto_advance` conflates checkpoint-bypass
  with stage-chaining; setting it persistently makes plan-phase launch execute-phase itself.
  **This file is a symlink** to `../../gsd-core/scratch/UPSTREAM-GSD-ISSUES.md`; if it reads as
  missing, recreate the link (it is gitignored) — do not track it.
- `.planning/UPSTREAM-GSD-ISSUES.md` §G-02 — "`execute-phase`'s `--auto` only clears the chain
  flag, never sets it", so auto-mode stays inactive and checkpoints still block. This is *why*
  DevFlow has to set the flag itself.
- `.planning/UPSTREAM-GSD-ISSUES.md` §G-03 — a stage-at-a-time driver has the chain flag wiped
  between stages; `plan-phase` clears it on any invocation lacking `--auto`/`--chain`.
- **`.planning/UPSTREAM-GSD-ISSUES.md` §G-04 — read this before touching config.** GSD init wrote
  `workflow.auto_mode` into this project's `.planning/config.json` (line 5, currently `true`), and
  **nothing reads that key** — it is absent from gsd-core's schema, defaults, and docs. It sits
  directly above the real `_auto_chain_active` and reads as though auto-mode is already enabled.
  During Phase 35 it cost **two separate executors** an escalation each. Confirmed inert here:
  `check auto-mode` returns `active: false` while `auto_mode` is `true`. Do not wire anything to it.

### GSD's checkpoint and auto-mode contract
- `~/.claude/gsd-core/references/checkpoints.md` — the auto-mode bypass rules. Note rule 6:
  `gate="blocking-human"` is **never** auto-approved in any mode. This phase targets ordinary
  `gate="blocking"` only.
- `~/.claude/gsd-core/workflows/execute-phase.md:161-165` — the chain-flag sync block and the
  `check auto-mode` read.
- `~/.claude/gsd-core/references/planning-config.md` — `workflow._auto_chain_active` documented as
  internal runtime state.

### DevFlow's own scope statement
- `.planning/ROADMAP.md` § "Phase 35.1: Unattended-Launch Prerequisites (999.93)" — goal and the
  six success criteria. Criterion 6 is an observation to collect, not a criterion to satisfy.
- `.planning/ROADMAP.md` § "Phase 999.93" and § "Phase 999.94" — the backlog entries; 999.94 is
  explicitly out of scope.

### Code the phase must touch or follow
- `crates/devflow-cli/src/preflight.rs` — the existing preflight framework (see code context).
- `crates/devflow-cli/src/pipeline_launch.rs:541` — `STREAM_JSON_STAGES`, the precedent D-05 follows.
- `crates/devflow-core/src/prompt.rs` — `define_stage_prompt` and the D-14 rationale for why
  Define is a no-op, which is what makes Plan the first stallable stage.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- **`crates/devflow-cli/src/preflight.rs` (2910 lines) already implements exactly the shape
  criterion 4 asks for.** Stage-scoped checks that return `Result<(), String>` and refuse a
  launch: `preflight_interactivity_check` (:610), `preflight_gh_auth_check` (:643),
  `preflight_major_bump_check` (:733), dispatched by `generic_preflight_checks` (:870), with
  `*_applies(stage)` predicates gating per-stage applicability. **Criterion 4 needs a new check
  inside this framework, not a new mechanism.**
- **`devflow_core::events::emit`** — the append-only event log D-03's notice writes to.
- **`State::preflight_retries`** (`crates/devflow-core/src/state.rs:72`) — an existing persisted
  counter for preflight outcomes; check whether a refusal should touch it before adding anything new.

### Established Patterns

- **Named stage-set constants.** `STREAM_JSON_STAGES` (`pipeline_launch.rs:541`) currently lists
  all five stages; Phase 34 widened it deliberately. D-05 follows this shape.
- **Loudly-logged escape hatches.** `--legacy-claude-launch` logs to stdout, the monitor log, and
  an event. D-09 declines to add another one, but this is the shape if that is ever revisited.
- **Fail-closed preflights.** 999.86 replaced a *predictor* with a real probe after two live
  false negatives. D-07 generalizes that stance.

### Integration Points

- **`.planning/config.json` — DevFlow does not currently touch it at all.** Verified 2026-08-07 by
  grep across `crates/`: there are no reads, no writes, and no `gsd-tools config-set` invocations.
  **The entire chain-flag mechanism is new surface**, including deciding whether DevFlow shells out
  to `gsd-tools query config-set` or edits the JSON directly. This is the single largest unknown
  in the phase and the planner should treat it as such.
- **The file is tracked and `commit_docs` is `true`** (`.planning/config.json:2,6`), and it already
  carries `"_auto_chain_active": false` (:8). Confirmed live: `gsd-tools query check auto-mode`
  currently returns `{active: false, source: "none", auto_chain_active: false, auto_advance: false}`.
  This is precisely why a leaked `true` gets committed and fires on the next phase.
- **The launch path** in `pipeline_launch.rs` is where the stage-set const is consulted and where
  the RAII guard's scope is decided.

</code_context>

<specifics>
## Specific Ideas

- The operator's framing throughout was **fail closed and stay loud**: every one of D-01, D-03,
  D-07, and D-09 chose the option that refuses or reports over the option that proceeds or stays
  quiet. Downstream agents should resolve ambiguity in that direction rather than toward
  convenience.
- **Criterion 4's negative control is not optional.** "A preflight that cannot fail is the defect
  it exists to prevent." The NOT-viable fixture must be demonstrated failing, not assumed.
- Likewise for D-02: the tests must **print a failure** in the failing direction, not merely
  not-pass. A wedged harness and a failed assertion are different observations and only one is
  evidence (the 35-02 lesson).

</specifics>

<deferred>
## Deferred Ideas

- **`devflow parallel` racing on `.planning/config.json`.** Two phases running concurrently would
  contend on the same chain flag — one clearing it while the other needs it set. Raised during
  discussion, not selected for discussion, and not decided here. Related to backlog 999.26
  (`devflow parallel` git object-store race). Belongs in its own entry if the planner confirms it
  is real.
- **999.94 — the first-option `decision` checkpoint.** Explicitly out of scope per ROADMAP.md: a
  correctness question about *how* an unattended agent decides, separable from *whether* it can
  proceed at all.
- **Upstream GSD: split `_auto_chain_active` into separate bypass and chaining flags.** This is
  what would let the Plan stage get checkpoint auto-approval without chaining into execute-phase,
  closing the limitation D-04 now documents. Operator considered making it a dependency of 35.1
  and declined. Tracked upstream as G-01 in `.planning/UPSTREAM-GSD-ISSUES.md`; belongs to
  gsd-core, not DevFlow.

</deferred>

---

*Phase: 35.1-unattended-launch-prerequisites*
*Context gathered: 2026-08-07*
