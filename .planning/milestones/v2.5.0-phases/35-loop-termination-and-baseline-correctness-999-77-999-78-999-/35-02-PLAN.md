---
phase: 35-loop-termination-and-baseline-correctness-999-77-999-78-999-
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/src/pipeline_launch.rs
autonomous: true
requirements: [HARDEN-04]

estimate:
  tokens: 25000
  raw_tokens: 50000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "A worktree-mode GateReview checkpoint auto-decide is covered by a test that drives a real synchronous advance() with worktree_path set, the blocking-human PLAN present only under the worktree, and a decoy PLAN for the same phase under project_root (D-05)"
    - "The new test fails when the checkpoint call site is reverted to read project_root, demonstrated by performing that revert and watching it fail, not asserted from reading the fix (D-06, NC-8)"
    - "The same test carries a mechanical opposite-result assertion that phase_has_blocking_human_checkpoint returns false for project_root, so the fixture cannot silently degrade into measuring that a PLAN exists somewhere (D-06)"
    - "The pre-existing no-worktree call-site test remains byte-unchanged and passing, so this plan adds worktree coverage without trading away the coverage that already existed"
    - statement: "HARDEN-04 has no residual unclassified edge beyond the root-selection axis: the arm's other four preconditions (agent kind, session id, capture confirmation, resume ceiling) are held constant by the fixture and are therefore not exercised by this test"
      verification: backstop
  artifacts:
    - path: "crates/devflow-cli/src/pipeline_launch.rs"
      provides: "the worktree-mode GateReview regression test and its decoy-PLAN fixture helper"
      contains: "advance_with_worktree_declared_checkpoint_reads_the_execution_root"
  key_links:
    - from: "crates/devflow-cli/src/pipeline_launch.rs"
      to: "crates/devflow-core/src/verify.rs"
      via: "the GateReview arm passes execution_root, not project_root, into phase_has_blocking_human_checkpoint"
      pattern: "phase_has_blocking_human_checkpoint\\(execution_root"
---

<objective>
Put a regression test behind the worktree-mode `GateReview` checkpoint auto-decide call site, which
is correct today purely by construction.

Purpose: reverting that one argument to `project_root` reintroduces the exact defect 999.76 was
filed for, and the whole suite still passes. That measurement was run twice independently. An
unguarded argument on the path that keeps plan-28-03's checkpoint resume alive in worktree mode is
a silent regression waiting to happen.

Output: one new test driving a real `advance()`, its D-05 decoy fixture, D-06's re-running
mechanical control, and the performed-revert demonstration recorded as evidence.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-CONTEXT.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-PATTERNS.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-VALIDATION.md
</context>

<planner_findings>
**Citation correction, verified against source during planning — use these lines, not the ones in
the briefing.** The orchestrator's confirmed-anchor list and `35-VALIDATION.md`'s Manual-Only
Verifications table both state that the `execution_root` binding is at `pipeline_launch.rs:1070`
and the `phase_has_blocking_human_checkpoint` call at `:1071`. That pair is off by two. Verified
this session:

- `:1068` — `let execution_root = state.worktree_path.as_deref().unwrap_or(project_root);`
- `:1070` — `&& verify::phase_has_blocking_human_checkpoint(execution_root, phase)`
- `:1071` — `&& agent_result::checkpoint_reported_in_capture(project_root, phase);`

`35-PATTERNS.md` and `35-RESEARCH.md` both have this right, and ROADMAP criterion 4 is also right
(it names `:1070` as the line *passing* `execution_root`). Only the restatement drifted. Re-derive
the lines yourself before editing anyway — this is the third stale citation found in this phase's
own source material, which is why the phase treats every `file:line` as needing re-checking.

**Deliberate deviation from RESEARCH.md and VALIDATION.md: add a sibling test, do not extend the
base in place.** Both documents say to extend
`advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records` (`:2302`) by moving its
PLAN under a worktree. Doing that literally would *delete* the only call-site-level coverage of the
no-worktree path, trading one gap for another. Instead: leave `:2302` byte-unchanged and add a
sibling modelled on it. The two then form a call-site-level opposite-result pair — `:2302` with no
worktree and the PLAN at the root, the new test with a worktree and a decoy at the root — and under
the revert the new one fails while `:2302` keeps passing, which localises the failure to root
selection rather than to checkpoint machinery in general.
</planner_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the worktree-mode GateReview regression test with D-05's decoy and D-06's control</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_launch.rs` lines 1060-1080 — the `Action::GateReview` arm,
      so you see the five-condition guard and confirm the binding and call lines yourself.
    - `crates/devflow-cli/src/pipeline_launch.rs` lines 2242-2300 — `HUMAN_GATE_VALUE_FOR_TEST`,
      `write_declared_checkpoint_plan`, `write_confirmed_checkpoint_capture`. Read these in full;
      the decoy must mirror the PLAN fixture's discovery shape exactly.
    - `crates/devflow-cli/src/pipeline_launch.rs` lines 2302-2360 — the base test to model. Note it
      is a plain synchronous `advance()` call with `env_lock()`, `init_repo`, `stub_agent_binary`
      plus `prepend_path`, and a `ReapMonitorOnDrop` guard.
    - `crates/devflow-cli/src/pipeline_launch.rs` around 2364 — the negative sibling, for the shape
      of an assertion that no auto-decide happened.
    - `crates/devflow-core/src/verify.rs` — `phase_has_blocking_human_checkpoint` and its two
      root-sensitivity tests at `:351` and `:377`. Those tests are the exact opposite-result idiom
      D-06 requires you to reproduce inside the new test.
    - `35-PATTERNS.md` section 4 — the quoted base test body and the labelled *wrong* base
      (`code_unknown_does_not_transition_to_validate`), which uses a scoped thread and gate-file
      polling. If your new test spawns a thread or polls a gate file, you copied the wrong one.
  </read_first>
  <behavior>
    New test `advance_with_worktree_declared_checkpoint_reads_the_execution_root`:

    - A worktree directory created with `create_dir_all` under the tempdir root (D-05: a plain
      directory, not a real `git worktree add` — the argument under test resolves a path, and a
      linked worktree's files are ordinary files).
    - The `blocking-human` PLAN written ONLY under that worktree.
    - A decoy PLAN under `project_root` for the SAME phase number, identical in discovery shape but
      declaring no blocking-human gate — so a revert fails because the wrong root was read, not
      because the main checkout happened to be empty.
    - `state.worktree_path` set to the worktree before `save_state`.
    - Assert exactly one `checkpoint_auto_decided` event, with the fixture's session id and stage,
      and no `gate_fired` event for that stage — the same observables the base test uses.
    - Assert mechanically, in the same test, that `phase_has_blocking_human_checkpoint(root, phase)`
      is false and that the same call against the worktree is true. A pair returning the same answer
      for both roots would be measuring the presence of a file somewhere rather than which root is
      read.
  </behavior>
  <action>
    Add a decoy fixture helper beside `write_declared_checkpoint_plan`, named
    `write_plan_without_checkpoint`, taking a root and a phase. It writes to the same
    `.planning/phases/{phase:02}-checkpoint-fixture/{phase:02}-01-PLAN.md` location under whatever
    root it is given, with a body of the same front-matter-plus-task shape but declaring a
    non-blocking checkpoint gate value, so the literal the production predicate searches for is
    absent from it. Do not reuse `HUMAN_GATE_VALUE_FOR_TEST` in the decoy body.

    Add the test described in the behavior block, modelled on the base at `:2302`. Reuse
    `write_declared_checkpoint_plan` unchanged by passing it the worktree path — it is generic over
    whatever path it receives. Keep the base test's `env_lock()`, `init_repo`, `stub_agent_binary`
    plus `prepend_path` PATH handling, its PATH restoration, and its `ReapMonitorOnDrop` guard
    exactly as they are; this test needs a real `git` and a real shell, so it uses the prepend
    family, never the replacing `NeutralPath`/`NoGitPath` family.

    Leave `advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records` byte-unchanged.
  </action>
  <verify>
    <automated>cargo test -p devflow --lib pipeline_launch::tests::advance_with_worktree_declared_checkpoint_reads_the_execution_root -- --exact 2>&1 | grep -E 'test result: ok\. 1 passed;.*[1-9][0-9]* filtered out'</automated>
  </verify>
  <acceptance_criteria>
    - The named test prints a real `test result: ok. 1 passed` line with a non-zero `filtered out` count. Exit code alone is not evidence — `cargo test --exact` on a name matching nothing also exits 0, and the package here is `devflow`, not `devflow-cli`.
    - `git diff -- crates/devflow-cli/src/pipeline_launch.rs` shows no change inside `advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records`, and that test still passes in the same run.
    - The new test body contains both halves of D-06's mechanical control: an assertion that the predicate is true for the worktree and an assertion that it is false for the project root.
    - The decoy PLAN exists on disk under `project_root` during the test and the production predicate does not match it: `rg -n 'fn write_plan_without_checkpoint' crates/devflow-cli/src/pipeline_launch.rs` matches, and the helper's body does not contain the blocking gate literal.
    - The new test contains no `std::thread::scope` and no gate-file polling loop — those belong to the wrong base.
    - `cargo test -p devflow --lib pipeline_launch:: 2>&1` prints `test result: ok.` with `0 failed`.
  </acceptance_criteria>
  <done>A test drives a real synchronous `advance()` through the worktree-mode `GateReview` arm with the decoy in place, carries its own re-running opposite-result control, and the pre-existing no-worktree test is untouched and still green.</done>
</task>

<task type="auto">
  <name>Task 2: Perform the revert demonstration and record both halves as evidence</name>
  <precondition>Task 1's test is committed and passing, and no other executor holds this working tree — the revert temporarily makes production code wrong, so it must not overlap another plan's run.</precondition>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_launch.rs` lines 1060-1080 — the exact expression to revert
      and restore.
    - `35-VALIDATION.md` — NC-8 and the Manual-Only Verifications row for HARDEN-04, including its
      statement of what the mechanical control does and does not establish.
    - `35-CONTEXT.md` D-06 — why the performed revert and the mechanical assertion are each
      necessary and neither substitutes for the other.
  </read_first>
  <action>
    Perform criterion 4's binding demonstration. Revert the checkpoint predicate's first argument so
    the arm reads the project root instead of the execution root — either by rebinding
    `execution_root` to `project_root` at `:1068` or by passing `project_root` directly at the call
    on `:1070`; both express the same regression, so pick one and record which.

    Run the new test and capture its full failure output verbatim. Then restore the file to its
    committed state and re-run, capturing the passing output verbatim.

    Record BOTH outputs in the plan SUMMARY, together with a statement of the distinction D-06
    draws: the mechanical assertion inside the test proves the two roots disagree for this fixture,
    which is what makes the revert meaningful, and only the performed revert establishes that the
    call site passes the execution root. Neither replaces the other.

    Do not commit the reverted state, and do not create a `35-evidence/` capture directory — D-06
    rejects that as disproportionate. The SUMMARY is where this lives.

    Also state in the SUMMARY what this demonstration does NOT establish, carried from
    `35-VALIDATION.md`: the fixture is a plain directory standing in for a worktree, so nothing here
    establishes git-worktree-specific semantics, and the arm's other four preconditions are held
    constant and unexercised.
  </action>
  <verify>
    <automated>git diff --exit-code -- crates/devflow-cli/src/pipeline_launch.rs && cargo test -p devflow --lib pipeline_launch::tests::advance_with_worktree_declared_checkpoint_reads_the_execution_root -- --exact 2>&1 | grep -E 'test result: ok\. 1 passed;.*[1-9][0-9]* filtered out'</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY contains the verbatim failing output from the reverted run, including a `test result: FAILED` line with `1 failed`. An assertion that it "would fail" is not acceptable — NC-8 requires the failure to have been seen.
    - The SUMMARY contains the verbatim passing output from the restored run.
    - The SUMMARY names which of the two revert forms was applied.
    - `git diff --exit-code -- crates/devflow-cli/src/pipeline_launch.rs` reports no differences, proving the revert was fully restored before the plan closed.
    - `git status --porcelain crates/devflow-cli/src/pipeline_launch.rs` is empty.
    - No directory named `35-evidence` exists in the phase directory.
    - The SUMMARY carries the two "what this does not establish" statements.
  </acceptance_criteria>
  <done>The revert has been performed, the new test watched to fail, the revert restored, and both outputs plus the D-06 distinction recorded in the SUMMARY.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan CREATES (new — exclude from drift verification):
- `advance_with_worktree_declared_checkpoint_reads_the_execution_root` — test,
  `crates/devflow-cli/src/pipeline_launch.rs`
- `write_plan_without_checkpoint` — test fixture helper, same file

Symbols this plan CHANGES: none. Criterion 4 adds no production change; the production code is
already correct and the revert in Task 2 is temporary and fully restored.

Symbols this plan REMOVES: none.

Files this plan creates: none.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted `State` → the stage dispatch arm | `worktree_path` selects which filesystem root the checkpoint declaration is read from; a wrong root reads an attacker-irrelevant but operationally wrong file set |
| Test fixture → the working tree | Task 2 deliberately makes committed production code wrong for the duration of one test run |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-08 | Spoofing | `Action::GateReview` checkpoint auto-decide root selection | medium | mitigate | Pin the argument with a test that drives the real dispatch arm and fails under the reverted form, so a future refactor cannot silently restore the wrong-root read (Tasks 1 and 2) |
| T-35-09 | Tampering | the temporarily reverted working tree during Task 2 | medium | mitigate | The revert is never committed; `git diff --exit-code` on the file is an acceptance criterion, and the task carries a precondition that no other executor holds the tree, per this repository's standing rule about git operations during an executor's run |
| T-35-10 | Denial of Service | checkpoint resume path in worktree mode | low | accept | A wrong-root read causes the run to fall through to the generic gate rather than resume — it stalls a run rather than corrupting state, and the phase's own gate machinery still fires |
| T-35-SC | Tampering | package-manager installs | low | accept | This plan installs no packages; `35-RESEARCH.md` records zero new dependencies for the phase and marks the Package Legitimacy Audit not applicable |
</threat_model>

<verification>
- `cargo test -p devflow --lib pipeline_launch::` prints `test result: ok.` with `0 failed`, and the
  count is at least one higher than before this plan.
- The performed revert produced a real failure, captured verbatim; the restored tree produces a real
  pass, captured verbatim.
- `scripts/check.sh all` exits with fmt clean, clippy clean under `-D warnings`, and `0 failed`.
</verification>

<success_criteria>
- ROADMAP criterion 4 (999.84) satisfied: the worktree-mode `GateReview` checkpoint call site is
  covered by a test that fails when the argument is reverted, demonstrated by actually performing
  that revert, with its negative control recorded alongside it.
</success_criteria>

<output>
Create `.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-02-SUMMARY.md` when done.
It must carry the verbatim failing and passing outputs from Task 2, the revert form used, and the
two "what this does not establish" statements.
</output>
