# Phase 35.3 — Plan 03 Summary: analysis record + fixture-label correction

**Status:** complete — 2/2 tasks.

## Per-path verdict (criterion 2)

Both paths **observed** — no negative, no inconclusive result:

- **Sub-agent dispatch** → per-task vocabulary only (`task_started`/`task_progress`/
  `task_updated`/`task_notification`, `local_agent`), zero `background_tasks_changed`.
  Observed by the `open_tasks` arm (`47ea511`). Validated by both synthetic n=2 AND a real
  `devflow start` probe.
- **Backgrounded shell** → `background_tasks_changed` ×2 (`local_bash` announce, then drained
  `[]`) AND the per-task vocabulary. Observed by BOTH arms. Synthetic n=2 only (the real run
  backgrounded nothing — stated, not assumed).

The answer is **positive**: the gate, as widened, observes both mechanisms on CLI 2.1.228.

## Validate-failure observation (criterion 5)

`phase_validate_failures` = **0**, read from the plan-02 real-capture probe's
`.devflow/state-01.json` at `stage: "validate"` — a real first-attempt Validate pass with no
Code loop-back, but of a trivial single-file probe phase. Phase 35.3 itself ran inline (no
`devflow start` pipeline), so it has no Validate-failure counter of its own. The provisional
`MAX_PHASE_VALIDATE_FAILURES = 10` remains unmeasured by a real multi-wave run.

## Fixture-comment correction (criterion 3)

Added to `crates/devflow-core/src/monitor.rs`:

1. **`open_tasks` doc comment** — corrected "production does not emit
   `background_tasks_changed`" to "production does not emit that event *for sub-agent
   dispatch*", and added the Phase 35.3 refinement: the current CLI DOES emit it for
   backgrounded shells (`local_bash`), so the drain arm is scoped to that path and `open_tasks`
   covers the sub-agent path.
2. **Close-rule fixture block comment** — added a CLI-version pin: the Phase 30 provenance is
   real but legacy; `local_agent` inside `background_tasks_changed` was a Phase 30 behavior the
   current CLI never produces (`local_bash` there, `local_agent` only in `task_started`).
3. **`bg_tasks_line` doc comment** — cross-references the pin.

`cargo check -p devflow-core` passes (comment-only, no logic change).

## Backlog filing (D-03)

**None.** No residual gap was measured — the widened gate observes both paths — so no `999.x`
backlog entry was filed. (The specific-gap rule in D-03 would only fire on a measured blind
spot, and there was none.)
