# Phase 35.3 — Plan 02 Summary: real-capture validation

**Status:** complete — 1/1 task.

## What happened

A dedicated `devflow start --phase 1 --no-worktree --agent claude --mode auto --until validate`
probe on a `scratch-dogfood-repo.sh` scaffolded single-file phase ran Define→Plan→Code→Validate
unattended (CLI 2.1.228, build `6314eff`). Capture copied out of `.devflow/` into
`35.3-evidence/real/`, PII-scrubbed, counted by `jq`.

## Stages reached

Define → Plan → Code → Validate (4 stages, 4 `DEVFLOW_RESULT` markers). Code dispatched **1
executor sub-agent** ("Execute plan 01-01"); Validate dispatched **1 verifier sub-agent**
("Verify phase 01 goal achievement").

## Per-family MATCH/DIVERGE vs the synthetic mapping

| Family | Real | MATCH? |
|---|---|---|
| `task_started` (local_agent) | 2 | ✅ |
| `task_progress` | 35 | ✅ |
| `task_updated` | 2 | ✅ |
| `task_notification` | 2 | ✅ |
| `background_tasks_changed` | 0 | ✅ (both synthetic-A and real emit zero) |
| `run_in_background:true` | 0 | ✅ (neither backgrounds) |

**The synthetic sub-agent mapping is validated** — real sub-agent dispatch emits the same
per-task vocabulary and no `background_tasks_changed`.

## What this did NOT exercise

- **Backgrounded shell** — the trivial probe gave the agent no reason to background anything,
  so this run says nothing about that path. The backgrounded-shell mapping (Variant B, n=2)
  rests on the synthetic capture alone, and COUNTS.md states that explicitly rather than
  assuming the real run confirms it.

## Notes (infrastructure friction recorded, not filed)

- `scratch-dogfood-repo.sh` (phase 23a) is stale against the current preflight: it writes
  `## Phase 1:` (two hashes) where preflight now requires `### Phase 1:` (three hashes), and
  omits `.planning/config.json` which the 35.1 unattended-launch chain-flag prerequisite
  requires. Both were fixed by hand on the probe repo. Worth a DevFlow backlog note, not a
  GSD issue.

## PII

Scrubbed (home path, username, session ids, MCP tool list); zero-match `rg` over
`35.3-evidence/real/`.
