---
phase: 35.3-drain-gate-concurrency-measurement-999-83
verified: 2026-08-13T00:00:00Z
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 35.3: Drain Gate Concurrency Measurement — Verification Report

**Phase Goal:** Operator has a real, evidence-based answer to whether the drain gate — the safety
net the v2.4.0 stream-json widening depends on — actually observes real sub-agent concurrency, in
place of the untested assumption its synthetic fixture currently encodes.

**Verified:** 2026-08-13
**Status:** passed
**Re-verification:** No — initial verification

## Method

This phase is investigation-shaped: its deliverable is measured evidence, not a code change. For
every roadmap criterion I read the produced evidence directly — the four synthetic captures and the
real capture, the `jq`-derived counts in `COUNTS.md`, the analysis record in `README.md`, and the
`monitor.rs` comment diff — rather than trusting the SUMMARY files. The one code edit (the fixture
provenance comment) is verified as comment-only by `git diff --stat` and `cargo test -p
devflow-core --lib monitor` (36 passed, 0 failed, 593 filtered out).

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria 1–5)

| # | Truth (ROADMAP criterion) | Status | Evidence |
|---|---|---|---|
| 1 | The event family (or families) the CLI emits for each concurrent-work mechanism is established from live evidence beyond 34-evidence's n=1, not inferred from source reading | ✓ VERIFIED | `35.3-evidence/` holds 4 synthetic captures (n=2 per path) + 1 real capture. Counts are `jq`-derived in `COUNTS.md`, with the Variant A `task_started` positive control (1/run) proving the counting path works. Sub-agent → per-task vocabulary (`task_started`→`task_progress`→`task_updated`/`task_notification`, `local_agent`), zero `background_tasks_changed`. Backgrounded shell → `background_tasks_changed` ×2 (`local_bash`) plus per-task vocabulary. |
| 2 | The question "does the drain gate see real sub-agent concurrency" is answered honestly per observed condition, negatives/inconclusive recorded as-is | ✓ VERIFIED | `README.md` states the per-path verdict: both paths observed — sub-agent via the `open_tasks` arm, backgrounded shell via BOTH arms. The answer is positive, and it records the two paths resolve through *different* arms (the legacy `background_tasks_changed` arm is vacuous for sub-agents). The one unvalidated half — backgrounded-shell mapping is synthetic-only — is stated as a scope limit, not assumed. |
| 3 | One of two outcomes delivered (widen OR in-source explanation) + the SYNTHETIC fixture label corrected to match the measurement | ✓ VERIFIED | The widening already landed in `47ea511` (pre-phase). This phase corrected the label: `monitor.rs`'s `open_tasks` doc comment now says production does not emit `background_tasks_changed` *for sub-agent dispatch* (it does for backgrounded shells, `local_bash`), and the close-rule fixture block gained a CLI-version pin recording that `local_agent` inside `background_tasks_changed` was a Phase 30 behavior the current CLI never produces. Comment-only (`git diff --stat`: 30 insertions, 8 deletions, all `///`/`//`). |
| 4 | The measurement's scope and strength are stated explicitly (sample size, CLI version, workload shape, what it can/cannot support) | ✓ VERIFIED | `README.md` scope table: CLI 2.1.228 pinned, synthetic n=2/path, real sample = 1 probe (2 sub-agent dispatches), workload shapes named, and an explicit "what this cannot support" (backgrounded-shell mapping synthetic-only; one CLI version; n=2 is a floor). |
| 5 | Observation recorded: the per-phase Validate-failure count this run reaches | ✓ VERIFIED | `README.md` records `phase_validate_failures` = **0**, read from the plan-02 real-capture probe's `.devflow/state-01.json` at `stage: "validate"` — with the honesty note that it is a trivial single-file probe, and phase 35.3 itself ran inline (no `devflow start` pipeline, no counter of its own). `MAX_PHASE_VALIDATE_FAILURES = 10` is still unmeasured by a real multi-wave run. |

**Score:** 5/5 truths verified. Each criterion is confirmed against the produced evidence files, not
transcribed from the SUMMARYs.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `scripts/drain-drill.sh` | The measurement drill (scaffold, guard, launch, parse-based count) | ✓ EXISTS + SUBSTANTIVE | Ran it live: 4 synthetic captures produced; T-23-01 guard demonstrated refusing an in-checkout destination |
| `35.3-evidence/COUNTS.md` | Event-family mapping table + real-capture comparison | ✓ EXISTS + SUBSTANTIVE | `jq`-derived counts per path per run; MATCH verdict table |
| `35.3-evidence/real/raw_output.jsonl` | PII-scrubbed real capture | ✓ EXISTS + SUBSTANTIVE | 358 events, 2 sub-agent dispatches, zero-match PII `rg` |
| `35.3-evidence/README.md` | Honest analysis record (answer + scope + observation) | ✓ EXISTS + SUBSTANTIVE | Per-path verdict, scope table, Validate-failure observation, residual-gap section |
| `crates/devflow-core/src/monitor.rs` | Fixture provenance comment CLI-version-pinned | ✓ EXISTS | Comment-only; `cargo test -p devflow-core --lib monitor` 36 passed |

**Artifacts:** 5/5 verified

### Decision Coverage (CONTEXT.md D-01..D-04)

| Decision | Status | Evidence |
|---|---|---|
| D-01 drill + ≥1 real capture | ✓ | Synthetic drill (n=2/path) + 1 real `devflow start` probe |
| D-02 both mechanisms | ✓ | Variant A (sub-agent) + Variant B (backgrounded shell), both captured |
| D-03 no re-widening; conditional backlog | ✓ | No residual gap measured → no `999.x` filed (recorded in README + SUMMARY) |
| D-04 CLI version pin | ✓ | 2.1.228 pinned in every run.log + README |

## Residual Gaps

None. The measurement did not find a path the widened gate fails to observe. No `999.x` backlog
item was filed (D-03's specific-gap rule did not fire).
