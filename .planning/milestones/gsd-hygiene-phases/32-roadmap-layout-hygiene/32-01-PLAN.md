---
phase: 32-roadmap-layout-hygiene
plan: 01
type: verify
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - CLAUDE.md
autonomous: false
requirements: [HYGIENE-01, HYGIENE-02, HYGIENE-03]
must_haves:
  truths:
    - "`gsd-tools query roadmap.analyze` run against this repo reports a non-zero phase_count for the active milestone and lists Phase 32"
    - "`gsd-tools query milestone.complete gsd-hygiene --dry-run` resolves this milestone's own phase set instead of the pass-all degrade that sweeps every directory on disk"
    - "`.planning/ROADMAP.md` contains a `## Progress` table whose derived phase counts are non-null, so the roadmap-derived completion check is used instead of the legacy STATE.md-comparison fallback"
    - "The three closed-milestone sections (v2.3.0, v2.0.0, v1.0) and the Backlog section's content are unchanged by this phase — it only inserts a new section (plus the operator-approved 999.72/999.72a status-label edit)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 32 heading + Progress table inside the active milestone's own window"
      contains: "## Progress"
    - path: ".planning/phases/32-roadmap-layout-hygiene/32-VERIFICATION.md"
      provides: "goal-backward verification report, status: passed"
  key_links:
    - from: "gsd-roadmapper's milestone-creation write (0b1ad74)"
      to: "roadmap.analyze / milestone.complete --dry-run"
      via: "### Phase 32: heading + ## Progress table landing inside the active milestone's heading-to-next-heading window"
      pattern: "parses cleanly, phase_count: 1"
---

<objective>
**This PLAN.md is backfilled, not prospective.** It is being authored after the fact, once the
phase's actual work (a `/gsd-discuss-phase 32` session plus a direct `gsd-verifier` run) was
already complete and committed. It exists to satisfy `gsd-tools`' `buildPhaseCompletionProjection`
predicate — `implementationComplete = planCount > 0 && summaryCount >= planCount` — which
otherwise hardcodes `verification_status: "not_required"` for any phase with zero plan files and
never reads the real VERIFICATION.md that already exists and already says `status: passed`. This
is an operator-approved workaround (2026-08-04) for that gsd-core tooling gap, filed to the
upstream issue ledger. Same convention as this project's other backfilled artifacts (Phase 22's
SUMMARY/VERIFICATION, backfilled 2026-07-25 per `PROJECT.md`).

Purpose: Fix `.planning/ROADMAP.md`'s layout so `gsd-tools`' milestone-scoped parsers
(`roadmap.analyze`, `milestone.complete --dry-run`) correctly find this milestone's own phase
instead of misfiring, and so a `## Progress` table exists for the roadmap-derived completion
check. No crates code — pure `.planning/` documentation structure.
Output: the goal was already satisfied incidentally by `gsd-roadmapper`'s write when creating this
milestone (commit `0b1ad74`) — confirmed live, twice independently, in the discuss-phase session
and again by a standalone `gsd-verifier` run. This plan's own work was the discuss-phase decisions
(D-01/D-02/D-03 in `32-CONTEXT.md`) and their two follow-on edits: the 999.72/999.72a backlog
status flip, and a new CLAUDE.md convention section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/verify-phase.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/32-roadmap-layout-hygiene/32-CONTEXT.md
@.planning/phases/32-roadmap-layout-hygiene/32-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Discuss phase context and capture decisions</name>
  <files>.planning/phases/32-roadmap-layout-hygiene/32-CONTEXT.md, .planning/phases/32-roadmap-layout-hygiene/32-DISCUSSION-LOG.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 32 section, and the 999.72/999.72a backlog entries)
    - .planning/REQUIREMENTS.md (HYGIENE-01/02/03)
  </read_first>
  <behavior>
    Live-verify (not trust) that the three success criteria already pass, since `gsd-roadmapper`'s
    milestone-creation write may have already delivered them. Record the finding and the
    resulting decisions (D-01 plan scope, D-02 backlog status, D-03 durability convention) in
    CONTEXT.md, flagged by provenance (operator decision vs. Claude's discretion default).
  </behavior>
  <action>
    Ran via `/gsd-discuss-phase 32` (interactive). No gray areas were initially selected by the
    operator; two were reopened directly afterward in the same session ("flip the backlog items'
    status" → D-02, "option 1 would be good enough for now" → D-03).
  </action>
  <acceptance_criteria>
    - 32-CONTEXT.md exists with `<decisions>` recording D-01/D-02/D-03 and their reversibility.
    - `roadmap.analyze` and `milestone.complete gsd-hygiene --dry-run` independently re-verified
      live to confirm the goal, not assumed from a prior session's claim.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply D-02 (backlog status flip) and D-03 (durability convention)</name>
  <files>.planning/ROADMAP.md, CLAUDE.md</files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 999.72" (line 325 at time of writing)
    - CLAUDE.md (existing section style, to match)
  </read_first>
  <behavior>
    `.planning/ROADMAP.md`'s `999.72`/`999.72a` backlog entries carry a `(RESOLVED — 2026-08-04,
    Phase 32)` status suffix and an inline resolution paragraph. `CLAUDE.md` carries a new
    "Keep the active milestone's phase headings inside its own window" section documenting the
    two root causes and a spot-check command, so the next milestone declare/archive has the
    convention in front of it.
  </behavior>
  <action>
    Direct edits — no `gsd-tools` handler exists for backlog-heading status text or for
    CLAUDE.md content (checked: neither `roadmap` nor `phase` subcommands touch freeform prose).
    `roadmap.analyze` re-run after the edit to confirm the file still parses cleanly
    (`phase_count: 1`, unchanged).
  </action>
  <acceptance_criteria>
    - `git diff 810ffdf..HEAD -- .planning/ROADMAP.md` shows exactly the Phase 32 insertion plus
      the 999.72/999.72a status-label edit — nothing else moved (Success Criterion 4).
    - `gsd-tools query roadmap.analyze` still parses cleanly post-edit.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Independently verify phase goal achievement</name>
  <files>.planning/phases/32-roadmap-layout-hygiene/32-VERIFICATION.md</files>
  <read_first>
    - $HOME/.claude/gsd-core/workflows/verify-phase.md (Option B: Success Criteria from ROADMAP.md)
  </read_first>
  <behavior>
    A `gsd-verifier` subagent, spawned directly (this phase has no PLAN.md at the time it ran —
    circular with this very backfill — so it worked from ROADMAP.md's Success Criteria per
    Option B), independently re-ran all 4 success criteria live against HEAD and wrote
    `32-VERIFICATION.md` with `status: passed`, `score: 4/4`.
  </behavior>
  <action>
    `Agent(subagent_type: "gsd-verifier", ...)`, report committed as `ceb3b01`.
  </action>
  <acceptance_criteria>
    - `gsd-tools query verification.status .planning/phases/32-roadmap-layout-hygiene` returns
      `{"status": "passed", ...}`.
  </acceptance_criteria>
</task>

</tasks>

<verification>
All three tasks' acceptance criteria were checked live at the time of writing this backfilled
plan, not assumed. See `32-VERIFICATION.md` for the full goal-backward report.
</verification>
