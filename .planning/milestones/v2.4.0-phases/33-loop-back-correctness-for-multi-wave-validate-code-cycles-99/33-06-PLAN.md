---
phase: 33-loop-back-correctness-for-multi-wave-validate-code-cycles-99
plan: 06
type: execute
wave: 4
depends_on: ["33-05"]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/test_support.rs
autonomous: true
gap_closure: true
requirements: [DOGFOOD-01]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: medium

must_haves:
  truths:
    - "`phase_verification_exists` and `phase_review_path` name their root parameter `evidence_root`, not `project_root`, and `phase_verification_exists`' doc comment states that the root must follow the agent's cwd because `.planning/` is tracked and lands on the phase branch inside the worktree (WR-07)"
    - "The evidence root is resolved ONCE in `handle_validate_outcome` as an owned `PathBuf` and all three loop-back arms use that single binding — `rg -c 'worktree_path.as_deref().unwrap_or(project_root)' crates/devflow-cli/src/pipeline_outcomes.rs` returns 0 inside `handle_validate_outcome`'s body (WR-08)"
    - "A `NeutralPath` RAII guard exists in `test_support.rs` and is used by BOTH regions 33-05 added, so a panic inside either region restores `PATH` via `Drop` rather than a trailing statement that unwinding skips (WR-05, new instances only)"
    - "`worktree_mode_mid_arc_loop_back_issues_plain_execute`'s two scenarios are two separate `#[test]` functions, so a scenario-A failure can no longer abort before scenario B — the suite's ONLY discriminator against a probe-both-roots-and-OR-them implementation — is ever asserted (IN-06)"
    - "Every test that drives `handle_validate_outcome` sets `state.stage = Stage::Validate`, the only stage from which production reaches it, so the emitted `loop_back` event's `from` field is not misleading (IN-07)"
    - "Behavior is unchanged: the full workspace suite passes at the same counts as the 33-05 baseline (271 bin / 547 core), with the two split tests accounting for exactly one net new test in the bin count (272)"
  artifacts:
    - path: "crates/devflow-core/src/agent_result.rs"
      provides: "`phase_verification_exists(evidence_root: &Path, phase: u32)` and `phase_review_path(evidence_root: …)` with the renamed parameter and the added contract sentence"
      contains: "evidence_root"
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "a single owned `evidence_root: PathBuf` binding in `handle_validate_outcome` consumed by all three arms, and the two split worktree-mode mid-arc tests"
      contains: "let evidence_root: PathBuf"
    - path: "crates/devflow-cli/src/test_support.rs"
      provides: "`NeutralPath` RAII guard modeled on the existing `ReapMonitorOnDrop`, restoring `PATH` in `Drop`"
      contains: "impl Drop for NeutralPath"
  key_links:
    - from: "crates/devflow-cli/src/test_support.rs"
      to: "crates/devflow-cli/src/test_support.rs"
      via: "`NeutralPath` is modeled on `ReapMonitorOnDrop` in the same file, whose doc comment already states the principle: it is the language's own `Drop` guarantee, not a call-ordering convention, that makes the cleanup unconditional"
      pattern: "ReapMonitorOnDrop"
    - from: "crates/devflow-cli/src/pipeline_outcomes.rs"
      to: "crates/devflow-core/src/agent_result.rs"
      via: "the single hoisted `evidence_root` binding is passed to `select_loop_back_fix`, which passes it to the renamed `phase_verification_exists(evidence_root, …)` — caller and callee finally agree on what the parameter means"
      pattern: "evidence_root"
  prohibitions:
    - "NO production behavior may change. This plan is entirely naming, binding-hoisting, test-structure and test-hygiene. `select_loop_back_fix`'s decision logic, `handle_validate_outcome`'s counter branch, and every arm's dispatched `FixType` must be byte-identical in effect. If any assertion in any existing test has to change to accommodate this work, STOP and report — that is evidence a behavior change leaked in."
    - "`phase_commit_count`'s root must NOT be changed. It is called with `project_root` deliberately and correctly — git refs and the object database are shared across worktrees. Unchanged from 33-05, and this plan edits its immediate neighbourhood, so it is again the moment of maximum risk. Do NOT rename its parameter to `evidence_root` — for THAT function `project_root` is the correct and intended name, and renaming it would invert the very distinction WR-07 asks us to make explicit."
    - "Do NOT fix CR-01 in `agent_result.rs:2041-2042` (`evaluate_layer0`). It is filed as backlog 999.76 / DEN-98 with a deliberate scope including a test rewrite. Touching it here would half-do a planned piece of work and break the green test at `:5259` that 999.76 exists to rewrite. You are editing the same FILE — stay out of that function."
    - "Do NOT fix WR-03 / the transient-git counter reset. Filed as backlog 999.77 / DEN-99. `handle_validate_outcome`'s counter branch at `:334-358` must be left alone apart from consuming the new `evidence_root` binding."
    - "Do NOT retrofit `NeutralPath` to the ten PRE-EXISTING PATH regions listed in WR-05. This plan covers only the two regions 33-05 added (`pipeline_outcomes.rs:1615-1631`, `:1709-1729`). Widening to all twelve is a bigger mechanical sweep with its own regression surface and belongs in its own plan; the guard this plan adds is what makes that sweep cheap later."
    - "The two preserved `--no-worktree` tests (`genuine_gaps_loop_back_still_issues_gaps_only`, `mid_arc_loop_back_issues_plain_execute_command`) must NOT be deleted, renamed, merged, or have any assertion removed or loosened. Carried forward unrelaxed from 33-05."
    - "No test may be weakened, ignored, deleted, or excluded from the default run to reach green, and no assertion may be softened. Splitting IN-06's test into two is the ONE sanctioned structural change, and it must PRESERVE both scenarios' assertions verbatim — it moves them, it does not edit them."
    - "Neither split test may be able to spawn a real agent process. Both still drive `handle_validate_outcome`'s Failed branch, which reaches `loop_back_to_code` → `launch_stage`, so both must hold `ENV_MUTEX` and neutralize `PATH` — now via the `NeutralPath` guard."
---

<objective>
Close the five code-review findings that are Phase 33's own new code. Three of them are the same
failure mode as the defect this phase exists to fix — the fix closed the instance and left the class
open — and one of them shields the suite's only discriminator against the most plausible wrong fix
behind an unrelated assertion.

Purpose: 33-05 corrected `select_loop_back_fix`'s caller but left `phase_verification_exists` still
naming its parameter `project_root` (WR-07), left the resolution triplicated so a fourth arm's
correctness depends on its author noticing a pattern (WR-08), and added two more non-RAII `PATH`
regions to a crate that already documents that exact hazard and solves it with `ReapMonitorOnDrop`
(WR-05). Separately, the new negative control packs both its scenarios into one `#[test]` (IN-06),
so scenario B — the only case in the workspace that kills a probe-both-roots-and-OR-them
implementation — is never reached if scenario A fails first; and the new tests set a `state.stage`
production cannot be in (IN-07).

Output: a callee whose signature is the contract a future caller reads, one owned binding that makes
a fourth arm correct by construction, an RAII `PATH` guard, two independently-reportable scenario
tests, and honest `Stage::Validate` fixtures. Zero production behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-REVIEW.md
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-05-SUMMARY.md
@CLAUDE.md
</context>

## What is already established — do not re-derive it

`33-REVIEW.md`'s third pass located every one of these with a `file:line` and, for WR-07 and WR-05,
supplied the exact patch. Two independent external peer reviews (DeepSeek v4 Pro, Gemini 3.1 Pro)
confirmed the surrounding fix is otherwise correct and complete. Spend budget on the change, not on
re-proving the findings.

| Finding | Location | What is wrong |
|---|---|---|
| WR-07 | `agent_result.rs:2567-2578`, `:2549` | callee still says `project_root`; public item, so it is the contract |
| WR-08 | `pipeline_outcomes.rs:322`, `:378`, `:393` | resolution triplicated; a fourth arm is correct only by attention |
| WR-05 | `pipeline_outcomes.rs:1615-1631`, `:1709-1729` | `PATH` restored by a trailing statement unwinding skips |
| IN-06 | `pipeline_outcomes.rs:1665-1746` | scenario A asserts first and aborts before scenario B |
| IN-07 | `pipeline_outcomes.rs:1599`, `:1678`, `:1694` | `Stage::Code` set on a path production only reaches at `Stage::Validate` |

**The borrow-checker note that matters (WR-08).** 33-05's inline comments claim the local binding is
forced because the shared borrow of `state` must end before `loop_back_to_code` takes it mutably.
That is true for a *borrowed* binding. Taking an **owned** `PathBuf` sidesteps it entirely, because
the owned value holds no borrow of `state`. `PathBuf` is already imported at `pipeline_outcomes.rs:34`.

<tasks>

<task type="auto">
  <name>Task 1 — WR-07: make the callee's signature the contract</name>

  <read_first>
  - `crates/devflow-core/src/agent_result.rs:2540-2600` (both functions and their doc comments)
  - `33-REVIEW.md` WR-07 (carries the exact doc sentence to add)
  </read_first>

  <steps>
  1. Rename the first parameter of `phase_verification_exists` from `project_root` to
     `evidence_root`, updating every use inside the body.
  2. Rename the first parameter of `phase_review_path` (`agent_result.rs:2549`) the same way. It is
     already called with an evidence root at `:2472` while naming the parameter `project_root`.
  3. Add the contract sentence to `phase_verification_exists`' doc comment, stating that
     `evidence_root` is the root the Validate agent actually wrote to — the phase's worktree when
     `state.worktree_path` is set, else the project root — and WHY: `.planning/` is tracked, so in
     worktree mode the artifact lands on `feature/phase-N` and is invisible from the main checkout
     for the phase's entire in-flight duration.
  4. Do NOT touch `phase_commit_count`. For that function `project_root` is the correct name.
  </steps>

  <verify>
  Capture each exit code directly; do not read a pipeline's status.
  - `rg -n "fn phase_verification_exists|fn phase_review_path" crates/devflow-core/src/agent_result.rs`
    shows `evidence_root` in both signatures.
  - `rg -n "fn phase_commit_count" crates/devflow-core/src/agent_result.rs` still shows
    `project_root` — this is the negative control for the prohibition; if it changed, revert it.
  - `cargo build --workspace` exits 0.
  </verify>

  <commit>refactor(33-06): name the evidence root in the callee, not just the caller</commit>
</task>

<task type="auto">
  <name>Task 2 — WR-08: resolve the evidence root once, as an owned binding</name>

  <read_first>
  - `crates/devflow-cli/src/pipeline_outcomes.rs:262-400` (the function and all three arms)
  - `33-REVIEW.md` WR-08 (carries the patch sketch)
  </read_first>

  <steps>
  1. Near the top of `handle_validate_outcome`, before the match that reaches the arms, bind:
     `let evidence_root: PathBuf = state.worktree_path.clone().unwrap_or_else(|| project_root.to_path_buf());`
  2. Replace all three inline `state.worktree_path.as_deref().unwrap_or(project_root)` expressions
     at `:322`, `:378`, `:393` with `&evidence_root`.
  3. Collapse the three near-identical inline comments into ONE comment at the binding explaining
     the evidence-root rule, and leave a one-line pointer at each call site rather than a repeat.
  4. Keep the `phase_commit_count(project_root, …)` call at `:336` exactly as-is, and keep the
     existing CR-01 doc block at `:288-298` that explains why the two reads use different roots —
     update its wording only if the hoist makes it inaccurate.
  </steps>

  <verify>
  - `rg -c "worktree_path.as_deref\(\).unwrap_or\(project_root\)" crates/devflow-cli/src/pipeline_outcomes.rs`
    returns 0 (the idiom survives elsewhere in the codebase; this file's `handle_validate_outcome`
    should have none).
  - `rg -n "phase_commit_count\(project_root" crates/devflow-cli/src/pipeline_outcomes.rs` still
    returns exactly 1 hit — negative control for the prohibition.
  - `cargo test -p devflow --lib` passes at the pre-existing count with zero failures.
  </verify>

  <commit>refactor(33-06): bind the evidence root once so a fourth arm cannot get it wrong</commit>
</task>

<task type="auto">
  <name>Task 3 — WR-05 + IN-06 + IN-07: RAII PATH guard, split the discriminator, honest stage</name>

  <read_first>
  - `crates/devflow-cli/src/test_support.rs` — especially `ReapMonitorOnDrop` and its doc comment,
    and `agent_free_git_only_path_dir`
  - `crates/devflow-cli/src/pipeline_outcomes.rs:1590-1750` (both new tests)
  - `33-REVIEW.md` WR-05 (carries the `NeutralPath` implementation), IN-06, IN-07
  </read_first>

  <steps>
  1. Add `NeutralPath` to `test_support.rs`, modeled on `ReapMonitorOnDrop`: `install()` captures the
     current `PATH`, sets it to `agent_free_git_only_path_dir()`, and owns the `TempDir` so it
     outlives the guard; `Drop` restores the original (or removes the var if there was none).
     Document that the caller must already hold `ENV_MUTEX`.
  2. Convert the two regions 33-05 added (`pipeline_outcomes.rs:1615-1631`, `:1709-1729`) to use it.
     Leave the ten pre-existing regions alone — that sweep is out of scope.
  3. Split `worktree_mode_mid_arc_loop_back_issues_plain_execute` into two `#[test]` functions, one
     per scenario, preserving both assertions verbatim. Name the second so its role is unmissable —
     it is the only test in the workspace that fails a probe-both-roots-and-OR-them implementation —
     and carry the explanatory doc comment from `:1690-1697` onto it.
  4. Set `state.stage = Stage::Validate` in all three places currently setting `Stage::Code`
     (`:1599`, `:1678`, `:1694`), matching the sibling tests at `:1766` and `:1831` that already do.
     If any assertion changes as a result, STOP — that means gate cleanup was being exercised
     differently and it is a finding, not something to adjust.
  </steps>

  <verify>
  Assert on real counts with a non-zero `filtered out`; the package is `devflow`, not `devflow-cli`.
  - Both split tests pass by exact name, each showing `1 passed` with a non-zero `filtered out`.
  - Run one fabricated test name as a negative control and record that it reports `0 passed` —
    proving the exact-name runs above matched real tests.
  - `cargo test --workspace --no-fail-fast` exits 0. Bin count should be 272 (271 + 1 net new from
    the split); core count unchanged at 547. Reconcile any difference explicitly.
  - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all -- --check` exit 0.
  </verify>

  <commit>test(33-06): RAII path guard, split the OR-both-roots discriminator, honest stage</commit>
</task>

</tasks>

<success_criteria>
- The callee's signature no longer invites the defect the caller was fixed for; the contract is
  written down where a future caller reads it first.
- A fourth loop-back arm is correct by construction rather than by its author's attention.
- A panic inside either new PATH region restores `PATH` through `Drop`, so it cannot cascade into
  `ENV_MUTEX` poisoning across parallel tests.
- Scenario B is independently reportable and can no longer be hidden by a scenario-A failure.
- Zero production behavior change, demonstrated by an unchanged suite apart from the one net new
  test the split creates.
- The deferred items (999.76 / CR-01, 999.77 / WR-03, the ten pre-existing PATH regions, and the
  carried WR-01/02/04/06) are visibly untouched, not quietly absorbed.
</success_criteria>

<output>
Create `.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-06-SUMMARY.md`
when done.

Record in it: the before/after signatures for both renamed functions and the `phase_commit_count`
negative control proving it was NOT renamed; the `rg -c` count showing the triplicated idiom is gone
from `handle_validate_outcome` with the `phase_commit_count(project_root` control still at 1; the
two split test names with their exact-name run output AND the fabricated-name negative control
output; the reconciled workspace counts against the 271/547 baseline with the net-new test
accounted for; and a plain statement of which findings this plan deliberately did NOT close
(999.76, 999.77, the ten pre-existing PATH regions, WR-01, WR-02, WR-04, WR-06) so they are not
mistaken for resolved.
</output>
