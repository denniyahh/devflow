---
phase: 33-loop-back-correctness-for-multi-wave-validate-code-cycles-99
plan: 03
type: execute
wave: 2
depends_on: ["33-01", "33-02"]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/test_support.rs
autonomous: true
requirements: [DOGFOOD-02]
user_setup: []

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A phase running three or more Code<->Validate waves in auto mode, with new commits landing on its feature branch between waves, does not trip the three-consecutive-failures gate (ROADMAP criterion 3)"
    - "A phase failing repeatedly with no new commits between waves still reaches MAX_CONSECUTIVE_FAILURES and still forces the gate (ROADMAP criterion 4)"
    - "The forward-progress count is derived from git's own object graph, not from anything the agent self-reports"
    - "One commit-counting mechanism serves both evaluate_layer2 and the new reset decision, so the two cannot silently diverge"
    - "When git is unavailable or the feature branch does not exist, the count is zero every cycle, so the counter accumulates and the safety gate stays reachable — the failure direction is toward gating, not away from it"
  artifacts:
    - path: "crates/devflow-core/src/agent_result.rs"
      provides: "phase_commit_count — the single git-derived commit count for a phase's feature branch"
      contains: "pub fn phase_commit_count"
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "the reset-vs-accumulate branch replacing the unconditional increment in handle_validate_outcome"
      contains: "consecutive_failures_made_progress"
    - path: "crates/devflow-cli/src/test_support.rs"
      provides: "commit_on_feature_branch — test helper landing a real commit on feature/phase-NN"
      contains: "fn commit_on_feature_branch"
  key_links:
    - from: "crates/devflow-cli/src/pipeline_outcomes.rs"
      to: "crates/devflow-core/src/agent_result.rs"
      via: "handle_validate_outcome calls phase_commit_count to obtain the current count before deciding"
      pattern: "phase_commit_count"
    - from: "crates/devflow-cli/src/pipeline_outcomes.rs"
      to: "crates/devflow-core/src/mode.rs"
      via: "handle_validate_outcome passes the stored baseline and the fresh count to consecutive_failures_made_progress"
      pattern: "consecutive_failures_made_progress"
    - from: "crates/devflow-core/src/agent_result.rs"
      to: "crates/devflow-core/src/agent_result.rs"
      via: "evaluate_layer2 obtains its commit count from phase_commit_count rather than its own inline git block"
      pattern: "phase_commit_count"
  prohibitions:
    - "prepare_loop_back_to_code (pipeline_gate.rs:130-158) must not gain any consecutive_failures reset or accumulate logic — it fires on every loop-back unconditionally, so putting the decision there is structurally the forbidden reset-on-every-loop-back bug"
    - "The {N}-VERIFICATION.md existence signal from 33-01 must not be reused as the forward-progress signal — that artifact is absent for the entire duration of a multi-wave phase, so it degenerates to always-reset"
    - "consecutive_failures_made_progress must not be paired with, or replaced by, a comparison of the Validate agent's self-reported findings or reason text"
---

<objective>
Close 999.66 (DOGFOOD-02): wire the forward-progress signal into `handle_validate_outcome`'s
counter so healthy wave-by-wave progress stops false-gating a 3+ wave phase at wave 3, without
disabling the safety gate that same counter protects.

Purpose: this is the second of the phase's two defects and depends on the first — 999.66's false
gate is only observable at wave 3, which requires 33-01's working loop-back to reach a second
loop-back at all. It also depends on 33-02's persisted baseline and pure predicate.

Output: one shared git-derived commit-count helper consumed by both the existing Layer-2 evaluation
and the new decision, the rewritten counter block, a test-support commit helper, and a matched pair
of multi-wave tests — one with new commits between cycles, one without — proving criterion 3 and
criterion 4 respectively.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-CONTEXT.md
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-RESEARCH.md
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-PATTERNS.md
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-01-SUMMARY.md
@.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-02-SUMMARY.md
@.claude/skills/ai-change-acceptance/SKILL.md
@CLAUDE.md
</context>

## Interface contracts this plan consumes

Landed by 33-01 and 33-02; do not redefine them.

| Symbol | Signature | From |
|---|---|---|
| `State::last_validate_failure_commit_count` | `Option<u32>`, `#[serde(default)]`, `None` = no prior failure recorded | 33-02 |
| `mode::consecutive_failures_made_progress` | `(previous: Option<u32>, current: u32) -> bool` | 33-02 |
| `agent_result::phase_verification_exists` | `(project_root: &Path, phase: u32) -> bool` | 33-01 |
| `pipeline_outcomes::select_loop_back_fix` | `(project_root: &Path, phase: u32) -> FixType` | 33-01 |

`GitFlowConfig::default()` is the established construction inside `pipeline_outcomes.rs` — it is
already used at line 514 in `run_hooks` and the type is already imported at line 22.

## Artifacts this phase produces (this plan's share)

| Symbol | File | Kind |
|---|---|---|
| `agent_result::phase_commit_count` | `crates/devflow-core/src/agent_result.rs` | new `pub fn` |
| `test_support::commit_on_feature_branch` | `crates/devflow-cli/src/test_support.rs` | new test-only `pub(crate) fn` |
| `phase_commit_count_reports_zero_without_a_branch` | `crates/devflow-core/src/agent_result.rs` | new test |
| `healthy_multi_wave_progress_does_not_reach_the_ceiling` | `crates/devflow-cli/src/pipeline_outcomes.rs` | new test |
| `repeated_failure_without_new_commits_still_reaches_the_ceiling` | `crates/devflow-cli/src/pipeline_outcomes.rs` | new test |

33-01 produces the 999.65 artifacts; 33-02 produces the `State` field and the `mode` predicate. The
phase-level flagged assumption about the edge-probe engine's non-classification is recorded once,
in 33-01.

<tasks>

<task type="auto">
  <name>Task 1: Extract one shared commit-count helper from evaluate_layer2</name>
  <files>crates/devflow-core/src/agent_result.rs</files>

  <precondition>`crates/devflow-core/src/agent_result.rs` contains `pub fn phase_verification_exists` — 33-01's contribution to this file. If it is absent, wave 1 has not merged and this plan must halt rather than edit a stale file.</precondition>

  <read_first>
    - `crates/devflow-core/src/agent_result.rs` lines 1820-1935 — `evaluate_layer2` in full, including the commit-count block at 1862-1881 and the four reason strings at 1900-1928 that consume `branch` and `commits`
    - `crates/devflow-core/src/git.rs` lines 60-90 — `git_command`, the environment-scrubbed subprocess constructor this must use
    - `crates/devflow-core/src/config.rs` lines 20-45 — `GitFlowConfig` and its `Default`
    - `crates/devflow-core/src/agent_result.rs` — the existing `evaluate_layer2` tests, to confirm which behaviors the refactor must leave observably identical
  </read_first>

  <action>
Extract the commit-count computation into
`pub fn phase_commit_count(project_root: &Path, git_flow: &GitFlowConfig, phase: u32) -> u32`,
placed adjacent to `evaluate_layer2`. Its body is the block currently inline at lines 1862-1881:
derive the branch name from `git_flow.feature_prefix` and the zero-padded phase, guard with
`rev-parse --verify` on that branch, and on success run `rev-list --count` over
`{git_flow.develop}..{branch}`, parsing stdout and falling back to 0. Preserve every existing
fallback exactly — an unrunnable `git`, a non-existent branch, and unparseable output all yield 0
today, and all three must keep yielding 0.

Then rewrite `evaluate_layer2` to obtain its `commits` value by calling the helper. Keep
`evaluate_layer2`'s own `branch` binding: four of its reason strings interpolate it, and removing
it would force a wider rewrite than this refactor warrants. The value that must not diverge between
the two callers is the count, not the branch-name format string, and the count now has exactly one
implementation.

This is a pure refactor with no behavior change. 33-RESEARCH.md's Don't Hand-Roll table names
duplicating this block as a likely source of a subtle divergence bug, which is why the extraction
happens before the new caller is written rather than after.

Doc-comment the helper with what it counts (commits on the phase's feature branch that are not on
develop), where it must be called from (the main `project_root`, never a worktree path — git
worktrees share refs and the object database, so a commit made inside a linked worktree is
immediately visible to a count run from the main checkout, which is the property `evaluate_layer2`
already relies on at every one of its call sites), and what a zero means (either genuinely no
commits, or the branch does not exist, or git could not be run — deliberately indistinguishable,
because every consumer treats all three the same way).

Add `phase_commit_count_reports_zero_without_a_branch`, asserting the helper returns 0 against a
freshly `init_repo`-style repository that has no `feature/phase-NN` branch. This is the case the
existing `consecutive_failures_reaches_ceiling_across_cycles` regression guard silently depends on,
and today nothing asserts it directly.
  </action>

  <verify>
    <automated>cargo test -p devflow-core --lib agent_result::tests::phase_commit_count_reports_zero_without_a_branch -- --exact 2>&1 | rg "1 passed"</automated>
    <automated>cargo test -p devflow-core --lib agent_result:: 2>&1 | rg "test result: ok"</automated>
  </verify>

  <acceptance_criteria>
    - The `--exact` command prints a literal `1 passed` line.
    - The full `agent_result::` slice reports `test result: ok`. This is the refactor's real gate: the existing Layer-2 tests already assert commit counts across the branch-missing, zero-commit, and non-zero-commit paths, so an extraction that changed behavior fails here rather than passing quietly.
    - `rg -c "phase_commit_count" crates/devflow-core/src/agent_result.rs` counts at least 3 — the definition, `evaluate_layer2`'s call, and the new test.
    - `evaluate_layer2` no longer builds its own count: reading its body confirms `commits` is bound from the helper's return value, and the `rev-parse --verify` / `rev-list --count` pair now appears only inside `phase_commit_count`. Verify by reading, and state the confirmation in the SUMMARY.
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` are clean.
  </acceptance_criteria>

  <reversibility rating="reversible">A pure extraction; inlining the helper restores the prior source exactly.</reversibility>

  <done>
`phase_commit_count` exists as the single implementation of the count, `evaluate_layer2` consumes
it, and the whole `agent_result::` test slice is green with no behavior change.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace the unconditional increment with the reset-vs-accumulate branch</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>

  <read_first>
    - `crates/devflow-cli/src/pipeline_outcomes.rs` lines 232-295 — `handle_validate_outcome`, and specifically the increment block at 264-270 with its overflow-safety comment
    - `crates/devflow-cli/src/pipeline_outcomes.rs` lines 14-35 — the `use` block, confirming `mode`, `agent_result`, and `GitFlowConfig` are already imported
    - `crates/devflow-cli/src/pipeline_outcomes.rs` line 514 — `GitFlowConfig::default()`, the established construction in this file
    - `crates/devflow-cli/src/pipeline_gate.rs` lines 87-158 — `transition`'s existing reset and `prepare_loop_back_to_code`, to see why neither is the right home for this logic
    - `.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-RESEARCH.md` § Common Pitfalls 1 and 2
  </read_first>

  <action>
Rewrite the `if result == ValidateResult::Failed` block in `handle_validate_outcome` so it decides
between beginning a fresh streak and continuing the existing one, instead of always continuing:

1. Compute the current count by calling `agent_result::phase_commit_count` with `project_root`,
   `&GitFlowConfig::default()`, and `state.phase`. Pass `project_root`, never
   `state.worktree_path` — refs are shared across a repository's worktrees, and every existing
   caller of the underlying git query already relies on that.
2. Ask `mode::consecutive_failures_made_progress(state.last_validate_failure_commit_count, current)`.
3. On progress, set `state.consecutive_failures` to 1 — not 0. This failure is the first of a new
   streak, and the gate context rendered a few lines below interpolates the counter into a message
   naming how many times validation has failed; zeroing it would make that message read zero on a
   real failure.
4. On no progress, keep the existing `saturating_add(1)`. Preserve the existing overflow comment —
   it explains why the arithmetic is saturating, and that reason is unchanged by this edit.
5. Update `state.last_validate_failure_commit_count` to `Some(current)` on both branches, before
   `save_state`. The baseline advances on every recorded failure regardless of which branch ran;
   updating it only on the progress branch would let a stale low baseline report progress forever.

Everything after this block — the `should_gate` check, the gate context strings, the loop-back and
transition arms — stays as it is. The counter's meaning is narrowed; the ceiling and its
consequences are not touched.

Do not add any of this to `prepare_loop_back_to_code`. That function fires on every loop-back
unconditionally and never touches `consecutive_failures` today; putting a reset there is
structurally the reset-on-every-loop-back fix that CONTEXT.md's D-03 forbids as a hard constraint,
because it would reintroduce 18d's unreachable-ceiling bug in a different form. Do not widen
`transition_resets_consecutive_failures` either — the counter's only increment site is here, which
is why the decision belongs here.

Extend the doc comment on `handle_validate_outcome` to record what the new branch decides and, as
plainly, what it does not: a progress result means new commits exist on the feature branch since
the last recorded failure, not that those commits addressed what Validate reported. State that this
narrows the ceiling's guarantee to loops that produce no commits at all, that it is the accepted
weakness of the commit-count signal recorded in 33-RESEARCH.md, and that the failure direction is
toward gating — an unrunnable git or a missing branch counts zero every cycle, so the counter
accumulates and the gate stays reachable.
  </action>

  <verify>
    <automated>cargo test -p devflow --lib pipeline_outcomes:: 2>&1 | rg "test result: ok"</automated>
    <automated>cargo test -p devflow --lib pipeline_outcomes::tests::consecutive_failures_reaches_ceiling_across_cycles -- --exact 2>&1 | rg "1 passed"</automated>
    <automated>cargo test -p devflow --lib pipeline_gate:: 2>&1 | rg "test result: ok"</automated>
  </verify>

  <acceptance_criteria>
    - `consecutive_failures_reaches_ceiling_across_cycles` passes **unmodified** — no edit to its body, its setup, or its assertions. It drives its cycles against a root with no git repository, so every cycle counts zero and, after the first failure records a `Some(0)` baseline, every later cycle correctly reports no progress and accumulates. This is criterion 4's direct regression guard and the single clearest signal that the fix narrowed the false positive rather than disabling the gate. If it needs editing to pass, the fix is wrong — stop and report rather than adjusting the test.
    - The whole `pipeline_outcomes::` and `pipeline_gate::` slices report `test result: ok`. The new git shell-out runs on every Validate failure, so any existing test that neutralizes PATH without a resolvable `git` would surface here.
    - `sed -n '1,/^#\[cfg(test)\]/p' crates/devflow-cli/src/pipeline_gate.rs | grep -c 'state\.consecutive_failures ='` prints `1` — the only production assignment in that file remains `transition()`'s existing reset, confirming no counter logic migrated into the loop-back helper.
    - `rg -n "consecutive_failures_made_progress|phase_commit_count" crates/devflow-cli/src/pipeline_outcomes.rs` shows both called from inside `handle_validate_outcome`.
    - `rg -n "saturating_add" crates/devflow-cli/src/pipeline_outcomes.rs` still matches on the `consecutive_failures` accumulate branch.
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` are clean.
  </acceptance_criteria>

  <reversibility rating="reversible">Restoring the four-line unconditional increment reverts the behavior; the persisted field 33-02 added is the one-way part and is not re-litigated here.</reversibility>

  <done>
`handle_validate_outcome` decides reset-vs-accumulate from the git-derived count and the persisted
baseline, updates the baseline on both branches, and leaves the ceiling, the gate check, and the
loop-back arms untouched. Every existing test in both pipeline slices still passes, including the
criterion-4 regression guard, unmodified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The matched multi-wave pair — progress passes, no progress still gates</name>
  <files>crates/devflow-cli/src/test_support.rs, crates/devflow-cli/src/pipeline_outcomes.rs</files>

  <read_first>
    - `crates/devflow-cli/src/test_support.rs` lines 40-105 — `ENV_MUTEX`, `init_repo`, and the `git` closure idiom to copy
    - `crates/devflow-cli/src/test_support.rs` lines 190-250 — `agent_free_git_only_path_dir`
    - `crates/devflow-cli/src/pipeline_outcomes.rs` lines 1120-1205 — `consecutive_failures_reaches_ceiling_across_cycles` in full, including its doc comment explaining why the gate response is re-seeded every iteration and why `state.stage` is forced back to Code before each `transition`
    - `crates/devflow-core/src/monitor.rs` lines 2384-2412 — `init_repo_with_feature_commits`, the closest existing analog for seeding commits on a `feature/phase-NN` branch (test-module-private to `devflow-core`, so it cannot be imported — mirror its approach)
    - `.claude/skills/ai-change-acceptance/rules/test-signal-rejection.md` — the four test shapes this repo rejects outright
  </read_first>

  <behavior>
    - Four consecutive Validate failures, each preceded by a real new commit on the phase's feature branch, leave the counter at one and do not force the gate
    - Three consecutive Validate failures on a repository whose feature branch exists but gains no new commits between cycles reach MAX_CONSECUTIVE_FAILURES and do force the gate
  </behavior>

  <action>
Add `pub(crate) fn commit_on_feature_branch(root: &Path, phase: u32, label: &str)` to
`test_support.rs`, following `init_repo`'s asserting-`git`-closure idiom. It must: resolve the
branch name as `feature/phase-{phase:02}`; create it with `checkout -b` if `rev-parse --verify` on
it fails and plain `checkout` it otherwise (do not use `checkout -B`, which resets an existing
branch to HEAD and would silently discard the commits an earlier call in the same test made); write
a file named from `label`; `add` it; and `commit`. Doc-comment that it is designed to be called
repeatedly within one test so each call adds exactly one commit to the develop..branch range, and
that it assumes `init_repo` has already run.

Then add the two tests to `pipeline_outcomes.rs`'s test module. Both hold `ENV_MUTEX`, call
`init_repo(root)` so a real repository with `develop` exists, replace PATH with
`agent_free_git_only_path_dir()` so no agent CLI can spawn while `git` stays resolvable, build a
`State` in `Mode::Auto` with `stage = Stage::Code`, `save_state`, and restore PATH before
asserting — the same shape `consecutive_failures_reaches_ceiling_across_cycles` uses. Both re-seed
a rejecting gate response at `Gates::response_path(root, phase, Stage::Validate)` at the top of
every loop iteration, for the reason that test's doc comment already records: the loop-back's gate
cleanup deletes a response written only once, and the final gate-triggering cycle would then block
on the multi-day default timeout instead of resolving.

- `healthy_multi_wave_progress_does_not_reach_the_ceiling` — ROADMAP criterion 3. Run
  `mode::MAX_CONSECUTIVE_FAILURES + 1` cycles, calling `commit_on_feature_branch` **before**
  `handle_validate_outcome` on each one so the count observed at each failure strictly exceeds the
  previously recorded baseline. Force `state.stage` back to Code and call `transition` to Validate
  at the end of each cycle, exactly as the existing guard does. Assert the counter equals 1 and
  that `should_gate(Stage::Validate, state.consecutive_failures)` is false. Running one cycle more
  than the ceiling is deliberate: at exactly the ceiling a passing assertion is also consistent
  with an off-by-one, and the extra cycle removes that reading.

  Doc-comment this test with the limitation it cannot exclude: it proves the counter does not
  accumulate when new commits land, and it cannot distinguish a commit that fixed something from a
  commit that did not. An agent committing anything at all on every cycle resets the streak every
  cycle. That is the accepted weakness of the commit-count signal recorded in 33-RESEARCH.md's D-03
  Recommendation and Assumptions Log A1, and it belongs beside the test that would otherwise be
  read as proving more than it does.

- `repeated_failure_without_new_commits_still_reaches_the_ceiling` — ROADMAP criterion 4, and the
  negative control for the test above. Identical setup and identical loop, with exactly one
  variable removed: call `commit_on_feature_branch` **once** before the loop so the branch exists
  and carries a non-zero count, then make no further commits. Run `mode::MAX_CONSECUTIVE_FAILURES`
  cycles. Assert the counter equals `mode::MAX_CONSECUTIVE_FAILURES` and that `should_gate` is
  true. This exercises a path the pre-existing guard does not reach at all: that test runs against
  a root with no repository, so its count is zero because the branch is missing, whereas this one
  has an existing branch returning a stable non-zero count. Two different routes to "no progress",
  and only this one proves the comparison itself works rather than the branch-missing fallback.

Write both tests before Task 2's change if executing them out of order is unavoidable; otherwise
run them against the pre-fix code once to capture RED. Record the RED output — the first test must
fail by reporting a counter of 4 rather than 1, and the second must already pass. A first test that
fails for any other reason, or a second test that fails, means the setup is wrong rather than the
production code, and this repo's acceptance rules (requirement 3) treat that distinction as
load-bearing.
  </action>

  <verify>
    <automated>cargo test -p devflow --lib pipeline_outcomes::tests::healthy_multi_wave_progress_does_not_reach_the_ceiling -- --exact 2>&1 | rg "1 passed"</automated>
    <automated>cargo test -p devflow --lib pipeline_outcomes::tests::repeated_failure_without_new_commits_still_reaches_the_ceiling -- --exact 2>&1 | rg "1 passed"</automated>
    <automated>cargo test -p devflow --lib pipeline_outcomes::tests::consecutive_failures_reaches_ceiling_across_cycles -- --exact 2>&1 | rg "1 passed"</automated>
    <automated>scripts/check.sh all</automated>
  </verify>

  <acceptance_criteria>
    - All three `--exact` commands print a literal `1 passed` line. The package is `devflow`, not `devflow-cli`, and a bare exit code is not evidence — a name matching nothing exits 0.
    - The RED evidence for `healthy_multi_wave_progress_does_not_reach_the_ceiling` is recorded in the SUMMARY as an assertion failure naming an accumulated counter, not a compile error, a git-setup panic, or a timeout.
    - `repeated_failure_without_new_commits_still_reaches_the_ceiling` is confirmed to pass against the pre-fix code as well as after. A negative control that only passes after the change is not controlling for anything, and that result must be reported rather than quietly accepted.
    - `consecutive_failures_reaches_ceiling_across_cycles` passes with a byte-unchanged body.
    - `rg -n "fn commit_on_feature_branch" crates/devflow-cli/src/test_support.rs` matches, and `rg -c "commit_on_feature_branch" crates/devflow-cli/src/pipeline_outcomes.rs` counts at least 2 — both new tests use it.
    - `scripts/check.sh all` exits 0. Capture `check.sh`'s own exit code, not that of a pipeline it feeds.
  </acceptance_criteria>

  <reversibility rating="reversible">Test-only additions plus one test-support helper.</reversibility>

  <done>
Both multi-wave tests pass, the pre-existing ceiling guard passes unmodified, the negative control
is confirmed to have passed before the change as well as after, and `scripts/check.sh all` is
green. ROADMAP criteria 3 and 4 both have a direct automated assertion, and the limitation neither
of them excludes is written into the source.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `git` subprocess stdout → parsed `u32` | Output of an external binary becomes an input to the safety-gate decision |
| `.devflow/state-NN.json` baseline → reset decision | Persisted, locally-writable value compared against the fresh count |
| Agent-produced commits → forward-progress signal | The agent under supervision can influence the signal that governs its own supervision |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-33-06 | Elevation of privilege | commit-count forward-progress signal | medium | accept | A stuck agent that commits something trivial each cycle resets the streak each cycle and never reaches `MAX_CONSECUTIVE_FAILURES`, weakening the gate in exactly the scenario it exists for. Accepted per 33-RESEARCH.md's D-03 analysis and Assumptions Log A1: the same weakness already exists unaddressed in `evaluate_layer2`'s "no work done" gate, which one trivial commit also already defeats, so this introduces no new risk class. Mitigation is legibility, not a control — Task 2 writes the limitation into `handle_validate_outcome`'s doc comment and Task 3 writes it beside the criterion-3 test, so a future strengthening pass finds it stated rather than having to rediscover it. Below the `high` blocking threshold. |
| T-33-07 | Tampering | `phase_commit_count` parsing of git stdout | low | mitigate | Unrunnable git, a missing branch, and unparseable output all yield 0, unchanged from `evaluate_layer2`'s existing fallbacks. A permanent 0 makes every cycle report no progress once a baseline is recorded, so the counter accumulates and the gate stays reachable — the degradation direction is toward gating. `phase_commit_count_reports_zero_without_a_branch` (Task 1) and the unmodified no-repository ceiling guard (Task 2) are the evidence. |
| T-33-08 | Tampering | `State::last_validate_failure_commit_count` at the decision site | low | accept | Covered by T-33-04 in 33-02's register; `.devflow/state-NN.json` is already fully trusted local state with no integrity check on any counter. No new control. |

ASVS level 1; block-on threshold `high`. Nothing reaches `high`, so nothing here blocks. Per
33-RESEARCH.md § Security Domain only V5 Input Validation applies; V2/V3/V4/V6 have no boundary in
scope. No package installs occur, so no legitimacy checkpoint applies.
</threat_model>

<verification>
- `scripts/check.sh all` exits 0.
- Criterion 3 has a direct automated assertion; criterion 4 has two, on structurally different
  paths (branch absent, and branch present but static).
- The pre-existing criterion-4 regression guard passes with an unchanged body.
- The `agent_result::` slice is green after the extraction, proving the refactor changed no
  behavior.
- The accepted limitation is stated in source in two places, and the SUMMARY quotes it.
</verification>

<success_criteria>
- A 3+ wave phase making genuine forward progress no longer false-gates.
- A phase making no forward progress still reaches the ceiling and still gates.
- One commit-counting implementation serves both consumers.
- Neither `prepare_loop_back_to_code` nor `transition_resets_consecutive_failures` gained
  counter logic or a widened signature.
</success_criteria>

<output>
Create `.planning/phases/33-loop-back-correctness-for-multi-wave-validate-code-cycles-99/33-03-SUMMARY.md` when done.
Record in it: the RED output for the criterion-3 test, explicit confirmation that the negative
control passed both before and after the change, the quoted limitation sentence, and confirmation
that `consecutive_failures_reaches_ceiling_across_cycles` was not edited.
</output>
