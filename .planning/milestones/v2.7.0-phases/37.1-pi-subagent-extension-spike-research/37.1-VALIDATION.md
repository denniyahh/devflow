---
phase: 37.1
slug: pi-subagent-extension-spike-research
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-17
---

# Phase 37.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

This phase produces a research document (comparison + recommendation + decision gate), not
executable code. There is no test framework, no build, and no `Req ID → Test` map in the
conventional sense — see `37.1-RESEARCH.md`'s own "## Validation Architecture" section for the
falsifiability equivalent used here (every architectural claim is tagged with the live command
or exact source file/line that backs it).

| Property | Value |
|----------|-------|
| **Framework** | n/a — research-only phase, no code produced |
| **Config file** | none |
| **Quick run command** | n/a |
| **Full suite command** | n/a |
| **Estimated runtime** | n/a |

---

## Sampling Rate

- **After every task commit:** n/a — no code in this phase
- **After every plan wave:** n/a
- **Before `/gsd-verify-work`:** the decision-gate verdict in `37.1-RESEARCH.md` is the phase's
  single pass/fail artifact, consumed by Phase 39's planning
- **Max feedback latency:** n/a

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| — | — | — | — | — | — | manual (doc review) | n/a | ✅ 37.1-RESEARCH.md | ⬜ pending |

*No code tasks — the plan's tasks produce/refine the comparison doc and decision gate, verified
by manual review against the sources cited in `37.1-RESEARCH.md`.*

---

## Wave 0 Requirements

*None: this is a research-only phase — existing infrastructure (none required) covers all phase
requirements.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|--------------------|
| Decision-gate verdict is defensible | Phase 37.1 goal | The deliverable is a recommendation, not code — correctness is judged by whether the cited evidence (live `pi --help`/`pi list` output, source line ranges) actually supports the "not viable for full-dispatch" conclusion | Re-run `pi --help` and `pi list` against the installed 0.84.1 binary; spot-check the cited `examples/extensions/subagent/index.ts` and `crates/devflow-core/src/monitor.rs:582-609` line ranges; confirm Phase 39 planning references this verdict |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies — n/a, no code tasks
- [x] Sampling continuity: no 3 consecutive tasks without automated verify — n/a
- [x] Wave 0 covers all MISSING references — n/a, no MISSING references (research-only)
- [x] No watch-mode flags — n/a
- [x] Feedback latency < Ns — n/a
- [ ] `nyquist_compliant: true` set in frontmatter — deferred to validate-phase §6

**Approval:** pending
