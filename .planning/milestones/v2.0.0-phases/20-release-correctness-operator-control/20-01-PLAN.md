---
phase: 20-release-correctness-operator-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/version.rs
  - crates/devflow-cli/tests/workspace_version_pin.rs
autonomous: true
requirements: [20a]

must_haves:
  truths:
    - "After VersionBump, every [workspace.dependencies] entry with a local path= key holds the same version as [workspace.package] version — no manual edit needed (D-20a)."
    - "A third-party dependency that has a version but no path (e.g. serde) is left byte-for-byte unchanged (edge-probe 20a/adjacency)."
    - "A Cargo.toml with no [workspace.dependencies] section, or a member entry with no version key, does not panic and no-ops cleanly (edge-probe 20a/empty)."
    - "Trailing comments, single/double quote style, and trailing commas are preserved on every rewritten line, exactly as the single-field path preserves them (GAP-6)."
    - "The version sub-value of a single-line inline-table self-pin is rewritten regardless of whether version appears before or after path in the inline table; replacement is anchored strictly to the path=/version= tokens (review: inline-table key-order)."
    - "The rewrite assumes single-line [workspace.dependencies] entries — a documented limitation; a verify step confirms the repo's own Cargo.toml self-pin entries are single-line (review: multi-line inline table out of scope)."
    - "The existing RED-proven guard crates/devflow-cli/tests/workspace_version_pin.rs passes without any manual pin edit and is neither weakened nor deleted."
  artifacts:
    - crates/devflow-core/src/version.rs
    - crates/devflow-cli/tests/workspace_version_pin.rs
  key_links:
    - "write_version (version.rs:197) → new additive inline-table pass over [workspace.dependencies] → same std::fs::write, so hooks::version_bump's single commit_path call captures both fields in one commit for free."
---

<objective>
20a — `VersionBump` must rewrite workspace member self-pins.

`version::write_version` rewrites exactly one dotted field
(`field_for()` returns `workspace.package.version` for a workspace `Cargo.toml`).
A published Cargo workspace states its version twice: once in
`[workspace.package] version` and again as an explicit `version` pin on every
`[workspace.dependencies]` entry that points at a workspace member by `path`.
Nothing rewrites the second one, so the self-pin ships stale and `cargo publish`
rejects the upload as a duplicate on release day — shipped broken two for two
(v1.5.0 patched by `7ad260c`, v1.6.0 by PR #15).

Purpose: make DevFlow's own (and any user's) published-workspace release cut
correct by construction; turn the existing PR #17 guard from a manual tripwire
into a no-op-by-construction.
Output: an additive inline-table rewrite pass in `version.rs`; the guard test
green without manual edits.

Root cause (verified at HEAD): `find_version_in_contents` (version.rs:226) and
`replace_version_in_contents` (version.rs:253) both `continue` past values that
`starts_with('{')` — inline tables — which is exactly why `[workspace.dependencies]`
self-pins are invisible today. The fix is an ADDITIVE pass over inline-table
entries with a local `path`, NOT a modification of that guard (single-field
callers still need it).

**Path note:** `version.rs` lives at `crates/devflow-core/src/version.rs`, NOT
`devflow-cli` — CONTEXT.md's canonical-refs originally said `cli`; corrected
during research (Pitfall 1).
</objective>

<review_incorporation>
## Review Incorporation (Phase 20 cross-AI review)

Findings from `20-REVIEWS.md` touching this plan (full text in that file):

- **[MEDIUM — Antigravity + Hermes] Inline-table parser assumes single-line entries** → INCORPORATED. A multi-line `[workspace.dependencies]` self-pin (`path` and `version` on different lines) would defeat the line-by-line scan, and the existing `starts_with('{')` guard is a line-level check that would NOT detect the interior `version =` line as inside an inline table. Fix: Task 2 anchors the rewrite strictly to the `path = "..."` and `version = "..."` tokens WITHIN a single inline-table line, and documents the single-line-entry assumption; Task 2 adds a verify step confirming the repo's own `[workspace.dependencies]` local-path entries are single-line (`Cargo.toml:20` — `path` and `version` on one line); Task 3 adds `write_version_rewrites_self_pin_regardless_of_key_order` asserting the `version` sub-value is rewritten whether it appears BEFORE or AFTER `path`. Multi-line inline tables are an explicit documented limitation (out of scope for this fix), not a silent gap. New must_have truth added.
- **[LOW — Hermes] "don't pull in a parser dependency" rationale is inaccurate** → INCORPORATED. `toml` is ALREADY in `Cargo.lock` (`Cargo.toml:23`, consumed by `config.rs`/`doc_check.rs`). The real reason to hand-roll is GAP-6 (comment/quote/format/ordering preservation, which `toml`'s serializer breaks), NOT dependency avoidance. Task 2 read_first and the threat model now state GAP-6, not "avoid a dependency."
- **[LOW — Hermes] Threat model undersells blast radius** → INCORPORATED. T-20-01a raised to `high`: a bad inline-table rewrite can break `cargo publish` (DoS) or, if the wrong version already exists on the registry, cause supply-chain substitution. Disposition stays `mitigate` (local-path-only guard + byte-identical regression test + key-order test). The ASVS line is revised from "V4 non-applicable" to name Tampering as the applicable category.
</review_incorporation>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-correctness-operator-control/CONTEXT.md
@.planning/phases/20-release-correctness-operator-control/20-RESEARCH.md
@.planning/phases/20-release-correctness-operator-control/20-PATTERNS.md
</context>

<artifacts_produced>
New symbols this plan creates (source-grounding drift pass MUST exclude these):
- A new inline-table rewrite pass inside `write_version` / a new private helper
  in `crates/devflow-core/src/version.rs` (e.g. `rewrite_workspace_member_pins`
  — exact name at executor discretion) that rewrites `version` sub-values of
  `[workspace.dependencies]` entries carrying a local `path`.
- New `#[test]` functions in `version.rs`'s test module:
  `write_version_rewrites_workspace_dependency_self_pin`,
  `write_version_leaves_third_party_version_only_dep_untouched`,
  `write_version_no_ops_on_missing_workspace_dependencies_section`,
  `write_version_rewrites_self_pin_regardless_of_key_order` (review: inline-table key-order).
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED — prove write_version leaves the self-pin stale</name>
  <files>crates/devflow-core/src/version.rs</files>
  <read_first>
    - crates/devflow-core/src/version.rs:197-206 (`write_version`) — the single-field rewrite to extend.
    - crates/devflow-core/src/version.rs:226-251 (`find_version_in_contents`) — the `if value.starts_with('{') { continue; }` guard at :244 that skips inline tables.
    - crates/devflow-core/src/version.rs:454-495 (`write_version_replaces_in_workspace_cargo_toml`) — the exact fixture style (temp dir + workspace Cargo.toml string) to mirror.
    - crates/devflow-cli/tests/workspace_version_pin.rs — the existing PR #17 guard that already RED-proves the product defect end-to-end.
    - PATTERNS.md § version.rs (this repo) — inline-table fix shape.
  </read_first>
  <behavior>
    - Given a workspace Cargo.toml containing `[workspace.package] version = "1.6.0"` and a `[workspace.dependencies]` entry `devflow-core = { path = "crates/devflow-core", version = "1.6.0" }`, calling `write_version` to bump to `1.7.0` currently rewrites only the package version, leaving the self-pin at `1.6.0`.
    - The new test asserts BOTH lines read `1.7.0` after `write_version`; it MUST fail against current HEAD (self-pin still `1.6.0`).
  </behavior>
  <action>Add `write_version_rewrites_workspace_dependency_self_pin` to `version.rs`'s test module, mirroring `write_version_replaces_in_workspace_cargo_toml`'s temp-dir fixture. Write a workspace Cargo.toml with a `[workspace.package] version` and one `[workspace.dependencies]` member pinned by `path` + `version`. Call `write_version(root, &"1.7.0".parse()?)`, read the file back, assert the `[workspace.dependencies]` line's `version` sub-value now equals `1.7.0`. Confirm it fails RED against unmodified `write_version`. Do NOT touch `hooks::version_bump` — it calls `write_version` once and commits the same `Cargo.toml`, so both fields land in one commit once `write_version` rewrites both.</action>
  <verify>
    <automated>cargo test -p devflow-core write_version_rewrites_workspace_dependency_self_pin 2>&1 | grep -q "1 failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow-core write_version_rewrites_workspace_dependency_self_pin` reports `1 failed` (RED) against unmodified `write_version`.
    - The test asserts on `1 passed`/exact count, not a bare name match (bare test-name filters match nothing and still exit 0 — see project MEMORY `cargo-test-exact-false-green`).
    - `hooks.rs` is not in the diff.
  </acceptance_criteria>
  <done>A committed failing test that pins the exact defect: the self-pin is left stale by `write_version`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Additive inline-table rewrite pass — make RED + guard green</name>
  <files>crates/devflow-core/src/version.rs</files>
  <read_first>
    - crates/devflow-core/src/version.rs:59-73 (`field_for`) — only the workspace branch (`contents.contains("[workspace.package]")`) is in scope; package/pyproject/package.json are untouched.
    - crates/devflow-core/src/version.rs:226-280 (`find_version_in_contents`, `parse_section_header`, `replace_version_in_contents`) — reuse `parse_section_header` + line iteration verbatim; add a small inline-table `key = { ... }` extractor. Hand-roll the reason is GAP-6 (comment/quote/format/ordering preservation), NOT dependency avoidance — `toml` is ALREADY a dependency (Cargo.toml:23, used by config.rs/doc_check.rs); it is deliberately not used here because its serializer would not preserve GAP-6 formatting.
    - Cargo.toml:19-24 (this repo's `[workspace.dependencies]`) — confirm each local-path self-pin is a SINGLE-LINE inline table (line 20: `path` and `version` on one line; line 21 `serde` is the path-less third-party case). The rewrite assumes single-line entries; a multi-line inline table is a documented out-of-scope limitation (review: Antigravity/Hermes MEDIUM).
    - PATTERNS.md § version.rs — "second, additive pass" shape: enter `[workspace.dependencies]`, for each `<crate> = { ... path = "crates/..." ... version = "..." }` line, rewrite ONLY the `version` sub-value, leaving `path` and other keys untouched.
  </read_first>
  <behavior>
    - Test 1 (from Task 1): the workspace self-pin is rewritten to the new version. Now GREEN.
    - Test 2: a `[workspace.dependencies]` entry with `version` but NO `path` (third-party, e.g. `serde = { version = "1" }`) is left unchanged.
    - Test 3: the PR #17 guard `workspace_version_pin.rs` passes with no manual pin edit.
  </behavior>
  <action>Extend `write_version`: after the existing single-field `replace_version_in_contents` call, run an additive pass over the result that scans for the `[workspace.dependencies]` section header via `parse_section_header`, and for each SINGLE-LINE inline-table entry (`<crate> = { ... }` opening and closing `}` on the same line) whose table contains a `path` key beginning with `crates/` (a local workspace member), rewrites that line's `version = "..."` sub-value to the new version. Anchor the rewrite strictly to the `path = "..."` and `version = "..."` tokens within the line — locate the `version` sub-value by its `version =` token independent of its position relative to `path` (key-order-independent), NOT by column offset — preserving surrounding whitespace, quote style, trailing comma, and trailing comment. Only touch entries with a local `path` — an entry with a `version` and no `path` MUST be skipped (that is the general rule: a dependency on a crate in this workspace carries this workspace's version). Treat multi-line inline tables as a documented out-of-scope limitation (see the single-line assumption in read_first); do NOT attempt cross-line accumulation in this fix. Keep the existing `starts_with('{')` guards in `find_version_in_contents`/`replace_version_in_contents` intact — the new pass is additive, not a modification of those. Re-run Task 1's RED test to confirm GREEN, then run the PR #17 guard.</action>
  <verify>
    <automated>cargo test -p devflow-core write_version 2>&1 | grep -qE "test result: ok" && cargo test -p devflow --test workspace_version_pin 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow-core write_version` → `test result: ok` (all `write_version*` unit tests pass, including Task 1's).
    - `cargo test -p devflow --test workspace_version_pin` → `test result: ok`, with NO edit to the pins in the repo's own `Cargo.toml` and NO edit to `workspace_version_pin.rs`.
    - Diff shows the `starts_with('{')` guard lines in `find_version_in_contents`/`replace_version_in_contents` unchanged (additive pass, not guard modification).
    - Single-line-assumption verify: `rg -n 'path = "crates/' Cargo.toml` shows each matched line ALSO contains `version = "` (path and version on the same line) — confirming the repo's self-pins satisfy the single-line assumption the pass relies on.
    - `cargo clippy -p devflow-core --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`write_version` rewrites both the package version and every local-path self-pin; RED test and PR #17 guard both green; single-field callers unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Edge coverage — empty section, no-version member, quote/comment preservation</name>
  <files>crates/devflow-core/src/version.rs</files>
  <read_first>
    - crates/devflow-core/src/version.rs:572-655 (`write_version_preserves_trailing_comma_in_package_json`, `_preserves_trailing_comment_in_toml`, `_in_single_quoted_toml`) — the GAP-6 preservation-test style to mirror for the new inline-table path.
    - COVERAGE probe reconciliation (below) — 20a/empty and 20a/adjacency map to these edges.
  </read_first>
  <behavior>
    - Empty (probe 20a/empty): a workspace Cargo.toml with `[workspace.package]` but no `[workspace.dependencies]` section rewrites the package version and no-ops the new pass without panic; a member inline table with a `path` but no `version` key is left unchanged (nothing to rewrite).
    - Adjacency (probe 20a/adjacency): a third-party `version`-only dep sitting adjacent to a local path member — only the path member is rewritten.
    - Preservation: a self-pin line with a trailing comment and single-quoted values keeps its comment and quote style after rewrite.
    - Key-order (review: inline-table key-order): a self-pin written `{ version = "1.6.0", path = "crates/devflow-core" }` (version BEFORE path) has its `version` rewritten to the new value, identical to the `path`-before-`version` case.
  </behavior>
  <action>Add `write_version_no_ops_on_missing_workspace_dependencies_section`, `write_version_leaves_third_party_version_only_dep_untouched` (a `serde = { version = "1" }` line adjacent to a rewritten path member), `write_version_preserves_comment_and_quote_in_workspace_dependency_pin`, and `write_version_rewrites_self_pin_regardless_of_key_order` (a fixture whose inline table lists `version` before `path`, asserting the `version` sub-value is still rewritten — proves the token-anchored, not column-anchored, replacement). Assert exact byte content for the untouched third-party line and the preserved comment/quote. These lock the edge-probe truths; no production change if Task 2 is correct.</action>
  <verify>
    <automated>cargo test -p devflow-core version:: 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - All four new edge tests pass; `cargo test -p devflow-core version::` → `test result: ok`.
    - Third-party `version`-only dep line is asserted byte-identical before/after.
    - Comment + single-quote preservation asserted on a rewritten self-pin line.
    - `write_version_rewrites_self_pin_regardless_of_key_order` passes with a `version`-before-`path` inline table (key-order independence).
    - Full workspace suite green: `cargo test --workspace` → 0 failed.
  </acceptance_criteria>
  <done>Empty/adjacency/preservation edges are locked by tests; the additive pass is provably surgical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| release automation → workspace Cargo.toml | `VersionBump` rewrites a file that `cargo publish` then trusts as the source of truth for dependency versions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01a | Tampering | `write_version` inline-table pass | high | mitigate | **Blast radius (review — Hermes):** a wrong inline-table rewrite corrupts a dependency `version` that `cargo publish` trusts — breaking the publish (DoS) or, if the mis-written version already exists on the registry, resolving against the WRONG published crate (supply-chain substitution). Mitigation: rewrite ONLY inline-table entries with a local `path` key, anchored to the `path=`/`version=` tokens; regression test `write_version_leaves_third_party_version_only_dep_untouched` asserts a `version`-only third-party dep is byte-identical after the pass, and `write_version_rewrites_self_pin_regardless_of_key_order` proves the token anchor is not fooled by key order. |
| T-20-01b | Denial of Service | `write_version` on malformed/empty section | low | mitigate | `write_version_no_ops_on_missing_workspace_dependencies_section` proves no panic and clean no-op on absent section / no-version member. |
| T-20-SC | Tampering | package installs | low | accept | This phase introduces zero new external dependencies (RESEARCH § Package Legitimacy Audit — `toml` already present at Cargo.toml:23, deliberately not used per GAP-6 comment/format preservation, not dependency avoidance). No npm/pip/cargo install task exists in this plan. |

ASVS L1 mapping (review — Hermes reframe): **V… Tampering IS applicable** — `write_version` mutates `Cargo.toml`, a file `cargo publish` trusts for dependency resolution, so a rewrite bug is a genuine Tampering vector (T-20-01a, mitigated). Non-applicable: V2 Authentication (no auth surface), V3 Session (CLI, no session), V4 Access Control (no privilege boundary crossed — the file is already writable by the operator), V6 Cryptography (no crypto).
</threat_model>

<verification>
## Edge-probe reconciliation (no silent drop)
- 20a/adjacency → truth: third-party `version`-only dep adjacent to a path member is untouched (Task 3).
- 20a/empty → truth: missing `[workspace.dependencies]` / no-version member no-ops cleanly (Task 3).
- 20a/ordering → **flagged assumption, N/A**: `write_version` produces no ordered collection output — it rewrites in-place in file order; the generic "stable order when elements compare equal" template does not target a file-mutation unit.

## Phase checks
- `cargo test -p devflow-core version::` → 0 failed.
- `cargo test -p devflow --test workspace_version_pin` → 0 failed, no manual pin edit.
- `cargo test --workspace` → 0 failed.
- `cargo clippy -p devflow-core --all-targets -- -D warnings` and `cargo fmt --check` clean.
</verification>

<success_criteria>
- `write_version` rewrites `[workspace.package] version` AND every local-path `[workspace.dependencies]` self-pin in one write.
- Third-party (path-less) deps and non-workspace file formats are provably untouched.
- PR #17 guard is green by construction and remains in the tree unmodified.
</success_criteria>

<output>
Create `.planning/phases/20-release-correctness-operator-control/20-01-SUMMARY.md` when done.
</output>
