---
phase: 12-bootstrap-housekeeping
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/tests/phase7_cli.rs
  - crates/devflow-core/src/agent_result.rs
autonomous: true
requirements: [WR-10, WR-09]
must_haves:
  truths:
    - "The dead v1 config test helpers are gone, and an explicit test asserts devflow ignores a stray .devflow.yaml"
    - "parse_marker_lines is clearer about its ASCII-marker assumption and a test proves the LAST DEVFLOW_RESULT marker wins in long output"
  artifacts:
    - path: "crates/devflow-cli/tests/phase7_cli.rs"
      provides: "removed write_config/write_last_ship; a non-regression test that a .devflow.yaml is ignored"
    - path: "crates/devflow-core/src/agent_result.rs"
      provides: "documented/simplified parse_marker_lines tail scan with a last-marker-wins guard test"
  key_links:
    - from: "devflow (any command)"
      to: ".devflow.yaml"
      via: "non-regression test asserts config file is not read"
      pattern: "devflow.yaml"
---

<objective>
Two Phase-11 test/parser hygiene items:

- WR-10 (`phase7_cli.rs:46-53`): the `write_config`/`write_last_ship` helpers write a v1-style `.devflow.yaml` into temp repos, but nothing asserts devflow ignores it — if `.devflow.yaml` parsing were ever reintroduced, no test would catch the regression. `write_last_ship` is additionally dead (defined, never called).
- WR-09 (`agent_result.rs:181-209`): `parse_marker_lines` reverses the tail by Unicode char rather than byte. Reviewer-confirmed no correctness bug for ASCII `DEVFLOW_RESULT` markers — clarify the assumption / simplify and guard it with a test.

Note: the workspace-root `.devflow.yaml` decoy REMOVAL is explicitly Phase 13 (per STATE.md) — this plan does NOT delete the root `.devflow.yaml`; it only cleans up the TEST helpers and adds a non-regression guard.

Purpose: Guarantee a regression that starts reading `.devflow.yaml` would be caught, and make the marker tail-scan intent explicit and tested.
Output: dead helpers removed + a `.devflow.yaml`-ignored non-regression test; documented/simplified `parse_marker_lines` + a last-marker-wins test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@crates/devflow-cli/tests/phase7_cli.rs
@crates/devflow-core/src/agent_result.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-10 — remove dead v1 config helpers + add a .devflow.yaml non-regression test</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    - crates/devflow-cli/tests/phase7_cli.rs (init_repo at 31-44 which calls write_config; write_config at 46-54; write_last_ship at 117; the run_devflow/fake_bin_dir helpers at 12-140; the existing test bodies at 144-355 that call init_repo)
  </read_first>
  <action>
    Remove the dead v1 config test infrastructure and replace it with a real guard. Delete the `write_config` helper and its call inside `init_repo` (line 43); delete the unused `write_last_ship` helper (line 117, no call sites). Confirm nothing else references either helper. Then add one explicit non-regression test `devflow_ignores_stray_devflow_yaml`: init a temp repo (via the trimmed `init_repo`), write a `.devflow.yaml` with arbitrary/garbage content into the repo root, run a devflow command that must succeed regardless of config (e.g. `devflow status` or `devflow doctor` via the existing `run_devflow` helper), and assert the command behaves normally (exit status / expected output) — proving devflow never reads `.devflow.yaml`. Do not delete the workspace-root `.devflow.yaml` (that is Phase 13).
  </action>
  <acceptance_criteria>
    - `write_config` and `write_last_ship` are gone; `init_repo` no longer writes a `.devflow.yaml`.
    - New test `devflow_ignores_stray_devflow_yaml` passes and demonstrates a stray `.devflow.yaml` does not change behavior.
    - `cargo test -p devflow --test phase7_cli` passes (all existing e2e tests still green).
    - `cargo clippy --workspace --tests -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-09 — clarify/simplify parse_marker_lines with a last-marker-wins test</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs (parse_marker_lines at 181-209 — the `.chars().rev().take(4000)...` tail extraction and the reverse line scan; json_scalar_to_string at 173-179 for style)
  </read_first>
  <action>
    Make the tail-scan intent explicit and guard it. Add a doc comment on `parse_marker_lines` stating the assumption: `DEVFLOW_RESULT` markers are ASCII, and the scan takes the last ~4000 chars and walks lines in reverse so the agent's FINAL status wins over any earlier echo of the marker. Optionally simplify the double-reverse `chars()` dance to a clearer tail extraction that stays on a char boundary (only if it preserves behavior exactly — do not risk a behavior change). Then add a test proving the last-marker-wins behavior: build a long stdout (well over 4000 chars) containing an EARLIER `DEVFLOW_RESULT` line with one status and a LATER `DEVFLOW_RESULT` line with a different status, and assert `parse_marker_lines` returns the LATER one. Use the existing AgentResult/serde shapes in the file for the marker JSON.
  </action>
  <acceptance_criteria>
    - `parse_marker_lines` carries a doc comment stating the ASCII-marker + last-wins + tail-window assumptions.
    - New test passes: with two markers in long output, the later status is returned.
    - `cargo test -p devflow-core agent_result::` green; `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project root config file → DevFlow | v2 must NOT read `.devflow.yaml`; silent reintroduction would let a stray/hostile config file influence behavior. |
| agent stdout → completion parser | Untrusted agent output is scanned for the completion marker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-07-T | Tampering (config injection regression) | `.devflow.yaml` handling | low | mitigate | Non-regression test asserts devflow ignores `.devflow.yaml`, catching any future accidental config-reading regression. |
| T-12-07-S | Spoofing (wrong completion status) | `agent_result::parse_marker_lines` | low | mitigate | Documented last-marker-wins semantics + a test ensure an earlier echoed marker cannot spoof the final agent status. |
</threat_model>

## Artifacts this phase produces

New symbols introduced by this plan (exclude from source-drift verification):
- New test fn `devflow_ignores_stray_devflow_yaml` in `crates/devflow-cli/tests/phase7_cli.rs`.
- New test fn in `crates/devflow-core/src/agent_result.rs` for last-marker-wins (name at executor's discretion, e.g. `parse_marker_lines_returns_last_marker`).
- Removed: `write_config`, `write_last_ship` helpers (deletions, not additions).

<verification>
- `cargo test -p devflow --test phase7_cli` and `cargo test -p devflow-core agent_result::` green.
- `cargo clippy --workspace --tests -- -D warnings` and `cargo fmt --check` clean.
</verification>

<success_criteria>
- WR-10: dead helpers removed; a stray `.devflow.yaml` is provably ignored.
- WR-09: marker tail scan documented + guarded by a last-wins test.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-07-SUMMARY.md` when done.
</output>
