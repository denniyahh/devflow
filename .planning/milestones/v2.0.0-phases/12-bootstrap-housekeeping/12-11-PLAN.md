---
phase: 12-bootstrap-housekeeping
plan: 11
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03, 12-04, 12-07, 12-08, 12-09]
files_modified:
  - crates/devflow-core/src/state.rs
  - crates/devflow-core/src/agents/mod.rs
  - crates/devflow-core/src/agents/claude.rs
  - crates/devflow-core/src/agents/codex.rs
  - crates/devflow-core/src/agents/opencode.rs
  - crates/devflow-core/src/lib.rs
  - crates/devflow-core/src/recover.rs
  - crates/devflow-core/src/workflow.rs
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-core/src/agent.rs
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-core/tests/monitor_e2e.rs
  - crates/devflow-cli/src/main.rs
autonomous: true
requirements: [IN-02, IN-03]
must_haves:
  truths:
    - "The State struct no longer carries the never-populated agent_result / agent_stdout_path fields"
    - "The agent enum is named AgentKind directly (no `pub type AgentKind = Agent` alias) and the adapter trait is named AgentAdapter"
    - "The whole workspace builds, tests, clippy-clean, and fmt-clean after the rename"
  artifacts:
    - path: "crates/devflow-core/src/state.rs"
      provides: "AgentKind enum (renamed from Agent), unused fields removed, alias deleted"
    - path: "crates/devflow-core/src/agents/mod.rs"
      provides: "AgentAdapter trait (renamed from Agent)"
  key_links:
    - from: "agents::adapter_for"
      to: "Box<dyn AgentAdapter>"
      via: "trait renamed; enum matched as AgentKind"
      pattern: "AgentAdapter"
---

<objective>
Two info-level cleanups deferred from Phase 11. Sequenced LAST (wave 3) because the IN-03 rename touches nearly every source file — running it after all other Phase-12 edits lets it sweep the new code too and avoids same-file wave collisions.

- IN-02 (`state.rs:46-51`): the `agent_result` and `agent_stdout_path` fields on `State` are `#[serde(skip)]` and only ever set to `None` in `State::new` — never populated anywhere. Remove them (and the now-unused `AgentResult` import) rather than leaving dead fields. (Per project standard: delete dead code; no TODO without a companion issue reference.)
- IN-03 (`state.rs` + `agents/mod.rs`): `pub type AgentKind = Agent` exists only so the agents module can reference the enum without colliding with its own `Agent` adapter trait. Rename the ENUM `Agent` → `AgentKind` directly (dropping the alias) and the adapter TRAIT `Agent` → `AgentAdapter`, eliminating the naming confusion.

Purpose: Remove dead state fields and resolve the Agent/AgentKind/Agent-trait naming collision.
Output: a clean, alias-free rename and field removal across the workspace, with all checks green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@.planning/phases/12-bootstrap-housekeeping/12-PATTERNS.md
@crates/devflow-core/src/state.rs
@crates/devflow-core/src/agents/mod.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: IN-02 — remove the never-populated State fields</name>
  <files>crates/devflow-core/src/state.rs</files>
  <read_first>
    - crates/devflow-core/src/state.rs (the two `#[serde(skip)]` fields agent_result/agent_stdout_path at 45-51; State::new initializing them to None at 100-114; the `use crate::agent_result::AgentResult;` import at line 12)
    - grep the workspace first to confirm neither field is read/written anywhere outside state.rs before deleting
  </read_first>
  <action>
    Remove the `agent_result: Option<AgentResult>` and `agent_stdout_path: Option<PathBuf>` fields from the `State` struct and their `None` initializers in `State::new`. Remove the now-unused `use crate::agent_result::AgentResult;` import (keep `PathBuf` if still used by `project_root`/`worktree_path`). First verify (grep) that nothing outside state.rs reads or assigns these fields; since both are `#[serde(skip)]`, removal does not change the persisted JSON schema. Do not add a TODO in their place.
  </action>
  <acceptance_criteria>
    - The two fields and their initializers are gone; the unused `AgentResult` import is removed.
    - `cargo build -p devflow-core` compiles; existing state.rs serde round-trip tests still pass.
    - `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: IN-03 — rename enum Agent → AgentKind and trait Agent → AgentAdapter</name>
  <files>crates/devflow-core/src/state.rs, crates/devflow-core/src/agents/mod.rs, crates/devflow-core/src/agents/claude.rs, crates/devflow-core/src/agents/codex.rs, crates/devflow-core/src/agents/opencode.rs, crates/devflow-core/src/lib.rs, crates/devflow-core/src/recover.rs, crates/devflow-core/src/workflow.rs, crates/devflow-core/src/monitor.rs, crates/devflow-core/src/agent.rs, crates/devflow-core/src/agent_result.rs, crates/devflow-core/tests/monitor_e2e.rs, crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-core/src/state.rs (enum Agent at 54-63; `pub type AgentKind = Agent` at 65-67; Display/FromStr impls at 69-96 — FromStr is what clap's `#[arg] agent: AgentKind` relies on)
    - crates/devflow-core/src/agents/mod.rs (trait Agent at 9-20; adapter_for returning Box<dyn Agent> at 22-29; `use crate::state::AgentKind` at line 7)
    - crates/devflow-core/src/agents/claude.rs, codex.rs, opencode.rs (each has `impl Agent for XAgent`)
    - crates/devflow-core/src/lib.rs (re-export `pub use state::{Agent, AgentKind, State};` at line 77)
    - crates/devflow-cli/src/main.rs (import `state::{Agent, State}` at line 10; the `#[arg] agent: Agent` field at 36-38; all `Agent::` usages)
  </read_first>
  <action>
    Perform the mechanical rename across the whole workspace:
    (1) In state.rs rename the enum `Agent` to `AgentKind` (variants unchanged: Claude, Codex, OpenCode) and DELETE the `pub type AgentKind = Agent;` alias. Update the enum's own Display/FromStr/AgentParseError impls to reference `AgentKind`.
    (2) In agents/mod.rs rename the adapter trait `Agent` to `AgentAdapter`; update `adapter_for` to return `Box<dyn AgentAdapter>`; the `use crate::state::AgentKind` stays valid.
    (3) In agents/claude.rs, codex.rs, opencode.rs change each `impl Agent for XAgent` to `impl AgentAdapter for XAgent`.
    (4) In lib.rs change the re-export to `pub use state::{AgentKind, State};` (the `Agent` enum name and the alias no longer exist).
    (5) Update every remaining reference to the enum (`Agent::Claude`, type annotations `Agent`, `state::{Agent}` imports, the clap `agent: Agent` arg) to `AgentKind` across main.rs, recover.rs, workflow.rs (tests), monitor.rs (tests), agent.rs, agent_result.rs, and monitor_e2e.rs. Update any `dyn Agent`/`impl Agent`/trait-bound references to the adapter trait to `AgentAdapter`.
    The full-workspace build + test is the completeness gate — after the rename nothing may reference the old `Agent` enum name, the alias, or the old trait name. Do not change variant names, serde rename attributes (`rename_all = "lowercase"`), or CLI-visible agent strings (claude/codex/opencode).
  </action>
  <acceptance_criteria>
    - No `pub type AgentKind = Agent` alias remains; the enum is `AgentKind`; the adapter trait is `AgentAdapter` with three `impl AgentAdapter for …`.
    - `cargo build --workspace` compiles with zero references to a bare `Agent` enum or trait.
    - `cargo test --workspace` passes (all existing tests plus the tests added by earlier Phase-12 plans).
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` pass.
    - CLI behavior unchanged: `devflow start --agent claude …` still parses (FromStr on the renamed enum).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a (internal refactor) | Pure rename + dead-field removal; no trust boundary is added, moved, or crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-11-T | Tampering (accidental behavior change during rename) | workspace-wide Agent→AgentKind / trait rename | low | mitigate | Full `cargo build --workspace` + `cargo test --workspace` + clippy gate ensures the mechanical rename preserves behavior; serde `rename_all` and CLI agent strings are explicitly left unchanged so persisted state and CLI parsing are unaffected. |
</threat_model>

## Artifacts this phase produces

Renamed/removed symbols introduced by this plan (exclude from source-drift verification):
- `AgentKind` — the enum (renamed from `Agent`); the `pub type AgentKind = Agent` alias is REMOVED.
- `AgentAdapter` — the adapter trait (renamed from `Agent`); `impl AgentAdapter for {Claude,Codex,OpenCode}Agent`.
- `adapter_for` now returns `Box<dyn AgentAdapter>`.
- Removed: `State.agent_result`, `State.agent_stdout_path` fields and the `AgentResult` import in state.rs.
- Updated lib.rs re-export: `pub use state::{AgentKind, State};`.

<verification>
- `cargo build --workspace` and `cargo test --workspace` green.
- `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` clean.
- Grep confirms no bare `Agent` enum/trait name and no `AgentKind = Agent` alias remain.
</verification>

<success_criteria>
- IN-02: dead State fields + unused import removed; serde schema unchanged.
- IN-03: enum is `AgentKind` (alias gone), trait is `AgentAdapter`; workspace builds/tests/clippy/fmt clean; CLI parsing unchanged.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-11-SUMMARY.md` when done.
</output>
