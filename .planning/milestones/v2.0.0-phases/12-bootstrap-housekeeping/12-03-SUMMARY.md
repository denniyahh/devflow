---
phase: 12-bootstrap-housekeeping
plan: 03
subsystem: monitor
tags: [rust, process, argv, command-injection]
requires:
  - phase: 11-refactor-gsd-native
    provides: detached monitor-owned agent execution
provides:
  - Literal argv delivery from spawn_monitor to the detached agent process
  - Regression coverage for shell-metacharacter-bearing agent arguments
affects: [monitor, agent-launch, security]
tech-stack:
  added: []
  patterns: [shell wrapper receives external commands through positional parameters]
key-files:
  created: []
  modified:
    - crates/devflow-core/src/monitor.rs
key-decisions:
  - "Retain the detached shell wrapper but pass program and args after sh -c's argv0 placeholder for literal \"$@\" execution."
patterns-established:
  - "Never interpolate an external program or its arguments into a shell script body."
requirements-completed: [WR-01]
coverage:
  - id: D1
    description: "Monitor agent commands are passed as separate argv values rather than interpolated shell text."
    requirement: WR-01
    verification:
      - kind: unit
        ref: "crates/devflow-core/src/monitor.rs#monitor::tests::spawn_monitor_captures_agent_pid_and_output"
        status: pass
      - kind: integration
        ref: "crates/devflow-core/tests/monitor_e2e.rs#monitor_owns_fake_agent_and_records_devflow_result"
        status: pass
    human_judgment: false
  - id: D2
    description: "Shell metacharacters in an agent argument remain literal and create no injected side effect."
    requirement: WR-01
    verification:
      - kind: unit
        ref: "crates/devflow-core/src/monitor.rs#monitor::tests::spawn_monitor_treats_agent_args_as_literal_argv"
        status: pass
    human_judgment: false
duration: 3min
completed: 2026-07-08
status: complete
---

# Phase 12 Plan 03: Literal Monitor Argv Summary

**Detached monitor launches now preserve agent program and argument boundaries through shell positional parameters, eliminating command interpolation.**

## Performance

- **Duration:** 3 min
- **Started:** 2026-07-08T21:53:00Z
- **Completed:** 2026-07-08T21:56:00Z
- **Tasks:** 2
- **Files modified:** 1

## Accomplishments

- Removed shell escaping and concatenation of the agent program and arguments.
- Passed agent argv after the `sh -c` argv0 placeholder and executed it with `"$@"`.
- Added regression coverage proving a semicolon-bearing argument remains literal and creates no injected file.

## Task Commits

1. **Task 1: Pass the agent command as argv** - `06aa0c4` (fix)
2. **Task 2: Guard argv with a metacharacter-bearing argument** - `55b8945` (test)

## Files Created/Modified

- `crates/devflow-core/src/monitor.rs` - Launches agent argv literally and verifies metacharacters are not interpreted by the wrapper.

## Decisions Made

- Kept internal path interpolation unchanged because those values are escaped and generated by DevFlow; only agent command data crosses the positional-parameter boundary.

## Deviations from Plan

None - plan executed as specified.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

WR-01 is complete. Remaining Phase 12 plans can proceed.

## Self-Check: PASSED

- `cargo test -p devflow-core monitor::`: 9 passed.
- `cargo test -p devflow-core --test monitor_e2e`: 1 passed.
- `cargo clippy -p devflow-core -- -D warnings`: passed.
- `cargo fmt --check`: passed.
- Grep found no `shell_escape(program)`, argument escaping, or `agent_cmd` construction in `spawn_monitor`.

---
*Phase: 12-bootstrap-housekeeping*
*Completed: 2026-07-08*
