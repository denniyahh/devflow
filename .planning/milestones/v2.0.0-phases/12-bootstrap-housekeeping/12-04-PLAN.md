---
phase: 12-bootstrap-housekeeping
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/agent.rs
  - crates/devflow-core/src/hooks.rs
autonomous: true
requirements: [WR-02, WR-03, 12f-transition-hooks]
must_haves:
  truths:
    - "The PID existence check uses libc::pid_t with a documented truncation assumption"
    - "BranchCleanup distinguishes 'branch not merged yet' from a genuine git error, so 'phase shipped' is not printed over a silent unmerged-branch failure"
    - "A test confirms the Validate→Ship transition fires DocsUpdate + ChangelogAppend and that ChangelogAppend actually writes a versioned entry"
  artifacts:
    - path: "crates/devflow-core/src/agent.rs"
      provides: "agent_running using libc::pid_t with an assumption doc-comment"
    - path: "crates/devflow-core/src/hooks.rs"
      provides: "branch_cleanup with a distinguishable warning; a test exercising Validate→Ship hook side effects"
  key_links:
    - from: "hooks_for_transition(Validate, Ship)"
      to: "changelog_append side effect"
      via: "the returned hook set is run against a temp repo and appends a CHANGELOG entry"
      pattern: "ChangelogAppend"
---

<objective>
Close three low-risk Phase-11 code-review items in the agent/hooks layer:

- WR-02 (`agent.rs:67`): `libc::kill(pid as i32, 0)` truncates PIDs > i32::MAX. Use `libc::pid_t` and document the assumption.
- WR-03 (`hooks.rs:95-104`): `BranchCleanup` uses non-force delete and only `warn!`s on failure, so the workflow prints "phase shipped" even when cleanup silently failed on an unmerged branch. Distinguish "not merged yet" from a genuine git error.
- 12f transition-hook coverage: no test confirms the Validate→Ship transition fires `DocsUpdate`/`ChangelogAppend` and that those hooks produce real side effects.

Purpose: Harden the PID check, make branch-cleanup failures legible, and cover the transition hook wiring with a real side-effect assertion.
Output: `agent_running` uses `libc::pid_t`; `branch_cleanup` emits a distinguishable warning; a hooks test drives the Validate→Ship hook set against a temp git repo.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@.planning/phases/12-bootstrap-housekeeping/12-PATTERNS.md
@crates/devflow-core/src/agent.rs
@crates/devflow-core/src/hooks.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-02 — use libc::pid_t in agent_running with an assumption doc</name>
  <files>crates/devflow-core/src/agent.rs</files>
  <read_first>
    - crates/devflow-core/src/agent.rs (agent_running at lines 63-68 — the `libc::kill(pid as i32, 0)` call)
  </read_first>
  <action>
    Change the cast in `agent_running` from `pid as i32` to `pid as libc::pid_t` so the intent (a POSIX process id type) is explicit. Add a `///`/`//` note on the function documenting the assumption that process ids fit in `libc::pid_t` on the supported platforms (Linux/macOS), which is why the `u32 -> pid_t` cast is safe in practice. Keep the `kill(pid, 0)` existence-probe semantics unchanged.
  </action>
  <acceptance_criteria>
    - `agent_running` casts to `libc::pid_t` and carries a documented assumption comment.
    - `cargo build -p devflow-core`, `cargo clippy -p devflow-core -- -D warnings`, and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-03 — distinguishable BranchCleanup warning</name>
  <files>crates/devflow-core/src/hooks.rs</files>
  <read_first>
    - crates/devflow-core/src/hooks.rs (branch_cleanup at lines 95-105; the GitFlow::delete_branch call with force=false)
    - crates/devflow-core/src/git.rs (delete_branch signature and the error it returns when a branch is not fully merged — read enough to know how to distinguish the unmerged case from a generic git error)
  </read_first>
  <action>
    Update `branch_cleanup` so a failed non-force delete produces a warning that distinguishes "feature branch not merged yet — left in place" from a genuine git error. Inspect the `Err` from `delete_branch(&branch, false)`: when the failure indicates the branch is not fully merged (e.g. the git error text signals an unmerged branch), emit a warn message that names the not-merged condition and states the branch was intentionally kept; otherwise emit the existing generic git-error warning. Keep the non-force behavior (do NOT switch to force delete) and keep the hook returning Ok so terminal ship flow still completes — but the message must now make an unmerged-branch skip legible rather than indistinguishable from a hard failure. Add a brief comment documenting that non-force is intentional (never discard unmerged work at cleanup).
  </action>
  <acceptance_criteria>
    - `branch_cleanup` emits two distinguishable warn paths (not-merged vs. git error) and still returns Ok; non-force delete is retained and documented.
    - `cargo build -p devflow-core`, `cargo clippy -p devflow-core -- -D warnings`, and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 12f — test the Validate→Ship transition hooks fire and append the CHANGELOG</name>
  <files>crates/devflow-core/src/hooks.rs</files>
  <read_first>
    - crates/devflow-core/src/hooks.rs (hooks_for_transition at ~70-81 returning [DocsUpdate, ChangelogAppend] for (Validate, Ship); changelog_append at 129-139; the tests module + init_repo/ctx helpers at 175-279, including the existing transition_map_finalizes_docs_and_changelog_before_ship test)
    - crates/devflow-core/src/ship.rs (prepend_changelog at 478-496 — what ChangelogAppend writes)
  </read_first>
  <action>
    Add a test that goes beyond the existing map-only assertion: build a temp git repo via the module's `init_repo` helper (it seeds a Cargo.toml + an initial commit), construct a `HookContext` for the Ship stage via the `ctx` helper, then for each hook returned by `hooks_for_transition(Stage::Validate, Stage::Ship)` call `hook.run(&ctx)` and assert the real side effect: after running, `CHANGELOG.md` exists in the repo and contains a `## ` version entry (proving ChangelogAppend executed against the computed version). Treat DocsUpdate as fail-soft (it runs `cargo doc` and may skip) — assert it returns Ok, not that docs were regenerated. This confirms the transition hook set both maps correctly AND produces the ChangelogAppend side effect end-to-end.
  </action>
  <acceptance_criteria>
    - New test passes: it runs the Validate→Ship hook set against a temp repo and asserts CHANGELOG.md gained a version entry; DocsUpdate returns Ok.
    - `cargo test -p devflow-core hooks::` green.
    - `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow → OS process table (`libc::kill`) | A PID value crosses into a raw syscall; truncation could probe the wrong process. |
| DevFlow → git (branch delete) | A cleanup failure can be masked, hiding unmerged work from the user. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-04-Spoof | Spoofing (wrong-process signal) | `agent::agent_running` PID cast | low | mitigate | Use `libc::pid_t` and document the fits-in-pid_t assumption; the probe uses signal 0 (no signal delivered), so worst case is a read-only existence miscount, not a kill. |
| T-12-04-R | Repudiation (masked failure) | `hooks::branch_cleanup` warn-only path | low | mitigate | Distinguishable warning surfaces an unmerged-branch skip so a cleanup failure is no longer silently reported as "shipped". |
</threat_model>

## Artifacts this phase produces

New symbols introduced by this plan (exclude from source-drift verification):
- New test fn in `crates/devflow-core/src/hooks.rs` for Validate→Ship hook side effects (name at executor's discretion, e.g. `validate_to_ship_hooks_append_changelog`).
- Possibly a small private helper in hooks.rs to classify the unmerged-branch error (if the executor extracts one).

<verification>
- `cargo test -p devflow-core hooks::` green (existing + new test).
- `cargo build -p devflow-core`, `cargo clippy -p devflow-core -- -D warnings`, `cargo fmt --check` clean.
</verification>

<success_criteria>
- WR-02: `agent_running` uses `libc::pid_t` with a documented assumption.
- WR-03: `branch_cleanup` warning distinguishes not-merged from git error; non-force retained + documented.
- 12f: a test proves the Validate→Ship transition fires DocsUpdate + ChangelogAppend and appends a CHANGELOG entry.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-04-SUMMARY.md` when done.
</output>
