---
phase: 12-bootstrap-housekeeping
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/devflow-core/Cargo.toml
  - crates/devflow-cli/Cargo.toml
autonomous: true
requirements: [12c]
must_haves:
  truths:
    - "Both workspace crates carry complete crates.io publish metadata (description, license, repository, readme, keywords, categories)"
    - "The devflow bin's path dependency on devflow-core carries a version so it is publish-ready"
    - "cargo publish --dry-run for devflow-core exits 0 and cargo package produces .crate artifacts"
    - "No cargo publish is run and no publish task exists"
  artifacts:
    - path: "Cargo.toml"
      provides: "[workspace.package] readme + keywords + categories added"
    - path: "crates/devflow-cli/Cargo.toml"
      provides: "devflow-core path dep with a version for publish-readiness"
  key_links:
    - from: "crates/devflow-cli/Cargo.toml"
      to: "devflow-core"
      via: "path dependency gains version.workspace = true"
      pattern: "devflow-core"
---

<objective>
12c — crates.io publish-PREP only (per locked decision 2). Complete the package metadata both publishable crates need and verify publish-readiness via dry-run and packaging. The actual `cargo publish` is explicitly OUT OF SCOPE (irreversible; deferred to after Phases 13–14).

Current state (verified 2026-07-08): `[workspace.package]` already has version=1.2.0, edition, license, repository, description, authors. Each crate inherits license/repository and sets its own description. Missing for publish-readiness: `readme`, `keywords`, `categories`, and a `version` on the `devflow-core` path dependency in the CLI crate.

Purpose: Make the workspace ready to publish on demand, provably, without publishing.
Output: complete metadata + green `cargo publish --dry-run -p devflow-core` + `cargo package` artifacts. No new dependencies are added.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@Cargo.toml
@crates/devflow-core/Cargo.toml
@crates/devflow-cli/Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete crates.io package metadata (no new dependencies)</name>
  <files>Cargo.toml, crates/devflow-core/Cargo.toml, crates/devflow-cli/Cargo.toml</files>
  <read_first>
    - Cargo.toml (root — the existing `[workspace.package]` block with version/edition/license/repository/description/authors, and `[workspace.dependencies]`)
    - crates/devflow-core/Cargo.toml (inherits license/repository via `.workspace = true`, sets local description)
    - crates/devflow-cli/Cargo.toml (same inheritance; note the `devflow-core = { path = "../devflow-core" }` dep has no version)
    - README.md (exists at workspace root — the readme to reference)
  </read_first>
  <action>
    Add the missing crates.io metadata to `[workspace.package]` in the root Cargo.toml: `readme = "README.md"`, `keywords` (max 5, each ≤20 chars — e.g. ["cli", "workflow", "automation", "git", "devops"]), and `categories` (valid crates.io slugs — e.g. ["command-line-utilities", "development-tools"]). In each crate's `[package]`, inherit the new fields via `readme.workspace = true`, `keywords.workspace = true`, `categories.workspace = true` (keep each crate's own `description`). Give the `devflow-core` path dependency in `crates/devflow-cli/Cargo.toml` a version so it is publish-ready: `devflow-core = { path = "../devflow-core", version = "1.2.0" }` (or `version.workspace = true` if a workspace dep entry is used). Do NOT add any new third-party dependency and do NOT change the workspace `version` value. If `cargo publish --dry-run` (Task 2) reports the workspace-root README cannot be packaged for a member, fall back to a minimal per-crate `README.md` and point that crate's `readme` at it — let the dry-run be the arbiter.
  </action>
  <acceptance_criteria>
    - Both crates resolve `readme`, `keywords`, and `categories` (via workspace inheritance) and keep their existing `description`.
    - `crates/devflow-cli/Cargo.toml`'s `devflow-core` dependency carries a version.
    - No new entry was added to `[workspace.dependencies]` or any `[dependencies]` table.
    - `cargo build --workspace`, `cargo clippy -- -D warnings`, and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify publish-readiness via dry-run and package (NO publish)</name>
  <files>Cargo.toml, crates/devflow-core/Cargo.toml, crates/devflow-cli/Cargo.toml</files>
  <read_first>
    - .planning/phases/12-bootstrap-housekeeping/CONTEXT.md ("Planning-Time Decisions" — decision 2, PREP ONLY)
  </read_first>
  <action>
    Verify publish-readiness without publishing. Run `cargo publish --dry-run -p devflow-core` (the leaf crate — it has no unpublished path deps, so its dry-run is a full, meaningful gate) and ensure it exits 0. Run `cargo package -p devflow-core` and `cargo package -p devflow` and ensure both produce `.crate` artifacts under `target/package/` (packaging validates metadata + that all included files are present). Do NOT run `cargo publish`. Do NOT create any task, script, or hook that runs `cargo publish`. If the bin crate's own `cargo publish --dry-run -p devflow` fails solely because `devflow-core` is not yet on crates.io (registry resolution of the versioned path dep), that is expected and acceptable for prep — record it in the SUMMARY; the bin's real publish happens after `devflow-core` is published (post-phase). Fix any metadata error the dry-run/package surfaces (missing readme file, invalid category slug, etc.).
  </action>
  <acceptance_criteria>
    - `cargo publish --dry-run -p devflow-core` exits 0.
    - `cargo package -p devflow-core` and `cargo package -p devflow` each produce a `.crate` under `target/package/`.
    - No `cargo publish` was executed and no publish task/script/hook exists anywhere in the plan output.
    - `cargo fmt --check` passes.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local workspace → crates.io registry | Publishing is irreversible; premature/accidental publish would burn a version number permanently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06-SC | Tampering (supply chain) | crates.io publish | medium | mitigate | PREP ONLY: no `cargo publish` is run and no publish task exists; only dry-run + package verification. No new third-party dependencies are added, so there is no npm/pip/cargo install to legitimacy-audit this plan. |
| T-12-06-I | Information Disclosure | package metadata contents | low | accept | Metadata (description, keywords, repository, readme) is intentionally public; no secrets are added. |
</threat_model>

## Artifacts this phase produces

New metadata keys introduced by this plan (exclude from source-drift verification):
- `readme`, `keywords`, `categories` added to root `[workspace.package]`.
- `readme.workspace`/`keywords.workspace`/`categories.workspace` inheritance keys in each crate `[package]`.
- `version` (or `version.workspace`) added to the `devflow-core` path dependency in `crates/devflow-cli/Cargo.toml`.
- (fallback, only if dry-run requires) a minimal per-crate `README.md`.

<verification>
- `cargo publish --dry-run -p devflow-core` exits 0.
- `cargo package -p devflow-core` and `-p devflow` produce `.crate` files.
- `cargo build --workspace`, `cargo clippy -- -D warnings`, `cargo fmt --check` clean.
- No `cargo publish` invocation exists.
</verification>

<success_criteria>
- Complete, valid crates.io metadata on both crates; path dep is versioned.
- Publish-readiness proven by dry-run + package.
- Zero publish actions taken; no publish task exists.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-06-SUMMARY.md` when done.
</output>
