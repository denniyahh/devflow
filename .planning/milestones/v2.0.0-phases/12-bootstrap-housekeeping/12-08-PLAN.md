---
phase: 12-bootstrap-housekeeping
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/gates.rs
  - crates/devflow-core/src/git.rs
  - crates/devflow-core/tests/monitor_e2e.rs
autonomous: true
requirements: [12f-gate-timeout, 12f-branch-divergence, 12f-monitor-failure]
must_haves:
  truths:
    - "poll_response returns immediately when a response file exists even at the real 7-day timeout — it does not block on the full deadline"
    - "list_feature_branches reports ahead/behind with correct semantics (ahead = commits the branch has over develop, behind = commits develop has over the branch)"
    - "The monitor path is covered for a devflow advance that fails on missing/corrupt state"
  artifacts:
    - path: "crates/devflow-core/src/gates.rs"
      provides: "a test asserting poll_response early-returns at GATE_TIMEOUT_SECS without sleeping"
    - path: "crates/devflow-core/src/git.rs"
      provides: "an ahead/behind correctness test (and the label fix if the assertion reveals a swap)"
    - path: "crates/devflow-core/tests/monitor_e2e.rs"
      provides: "coverage for advance failing on missing/corrupt state"
  key_links:
    - from: "Gates::poll_response(deadline=7d, response present)"
      to: "immediate Some(response)"
      via: "the read-file check precedes the deadline check"
      pattern: "poll_response"
---

<objective>
Close three untested orchestration-core paths from 11-VALIDATION.md (12f), each in a file not otherwise touched this wave:

- Gate timeout at the real 7-day value: only `timeout_secs=0` is tested. Prove `poll_response` returns promptly once a response file exists even when the deadline is `GATE_TIMEOUT_SECS` (7 days) — i.e. the early-return-on-file-found path is checked before the backoff sleep, so a present response never blocks for the full window.
- `list_feature_branches` ahead/behind correctness: only display formatting is tested. Assert the counts are semantically correct.
- Monitor behavior when `devflow advance` fails (missing state file / corrupt JSON).

Purpose: Lock in the correctness of the gate poll's non-blocking fast path, the branch-divergence reporting, and the monitor's failure handling.
Output: three tests (plus a git.rs label fix if the ahead/behind assertion exposes a swap).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@.planning/phases/12-bootstrap-housekeeping/12-PATTERNS.md
@crates/devflow-core/src/gates.rs
@crates/devflow-core/src/git.rs
@crates/devflow-core/tests/monitor_e2e.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: 12f — gate timeout fast path at the real 7-day deadline</name>
  <files>crates/devflow-core/src/gates.rs</files>
  <read_first>
    - crates/devflow-core/src/gates.rs (poll_response at 138-164 — note the read-file check at 151-155 runs BEFORE the `waited >= deadline` check; existing tests poll_response_returns_when_file_appears at 240-254 and poll_response_times_out_when_absent at 256-260)
    - crates/devflow-cli/src/main.rs (GATE_TIMEOUT_SECS = 7*24*60*60 at line 16 — the real deadline value; devflow-core has no such const, so re-derive it locally in the test)
  </read_first>
  <action>
    Add a test `poll_response_returns_immediately_at_full_timeout` proving the fast path does not block for the full deadline. Write a response file first (mirror `poll_response_returns_when_file_appears`), then call `Gates::poll_response(root, phase, stage, SEVEN_DAYS)` where `SEVEN_DAYS = 7 * 24 * 60 * 60` is a local const in the test (devflow-core cannot import the CLI const). Assert it returns `Some(response)`. To prove it did not sleep on the deadline, wrap the call so the test fails if it takes anywhere near the real timeout — e.g. assert the whole call completes in well under a second by measuring elapsed `Instant` around the call and asserting it is small (e.g. < 5s). Do NOT sleep 7 days.
  </action>
  <acceptance_criteria>
    - `cargo test -p devflow-core gates::poll_response_returns_immediately_at_full_timeout` passes and completes in well under the 7-day deadline (elapsed assertion present).
    - `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 12f — list_feature_branches ahead/behind correctness (fix label if swapped)</name>
  <files>crates/devflow-core/src/git.rs</files>
  <read_first>
    - crates/devflow-core/src/git.rs (list_feature_branches at 276-312 — `ahead = rev_count("{name}..{develop}")`, `behind = rev_count("{develop}..{name}")`; divergence_from_develop at 259-274 for the correct reference semantics where `behind = rev_count("{current}..{develop}")`; BranchInfo struct; the git test helpers further down the file)
    - crates/devflow-core/src/hooks.rs (init_repo/git helpers at 179-202 as a temp-repo pattern reference)
  </read_first>
  <action>
    Add a correctness test for `list_feature_branches` and treat the test as the source of truth for the intended semantics: build a temp git repo with `develop`, branch a `feature/phase-NN` off it, commit N (e.g. 2) commits ON the feature branch (making it 2 AHEAD of develop), then add M (e.g. 1) commit ON develop not on the feature (making it 1 BEHIND develop). Call `list_feature_branches` and assert the returned `BranchInfo` has `ahead == 2` and `behind == 1` — i.e. `ahead` counts commits the branch has that develop lacks, `behind` counts commits develop has that the branch lacks (matching `divergence_from_develop`'s convention). NOTE: the current implementation assigns `ahead = rev_count("{name}..{develop}")` and `behind = rev_count("{develop}..{name}")`, which is the reverse of this convention — if the assertion fails, correct the labeling in `list_feature_branches` so `ahead`/`behind` match the documented/divergence semantics, then re-run. Keep the existing display/sort behavior.
  </action>
  <acceptance_criteria>
    - New test asserts `ahead == 2` and `behind == 1` for a branch 2 ahead / 1 behind develop, and passes (fixing the field labeling in `list_feature_branches` if needed).
    - Any existing git.rs tests that reference `list_feature_branches` output remain green (update them only if they encoded the swapped semantics).
    - `cargo test -p devflow-core git::` green; `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 12f — monitor coverage when devflow advance fails on missing/corrupt state</name>
  <files>crates/devflow-core/tests/monitor_e2e.rs</files>
  <read_first>
    - crates/devflow-core/tests/monitor_e2e.rs (git/init_repo helpers at 26-58; the existing monitor_owns_fake_agent_and_records_devflow_result test at 59+ — the full-flow integration style to mirror)
    - crates/devflow-cli/src/main.rs (advance at 338-385 — load_state errors with MissingState; evaluate_agent_result reads capture files)
  </read_first>
  <action>
    Add an integration test covering the monitor/advance failure path when state is missing or corrupt. Two realistic sub-cases (pick the one that fits the existing harness, or cover both): (a) run the advance path against a project root with NO `.devflow/state.json` and assert it surfaces a missing-state failure rather than panicking or silently succeeding; (b) write a corrupt/partial `state.json` (invalid JSON) and assert advance/load_state reports a parse failure. Mirror the existing monitor_e2e harness (temp repo, real devflow binary via `CARGO_BIN_EXE_devflow` if the existing test uses it, or the library `workflow::load_state`/`advance` seam the harness already exercises). Assert on the observable failure signal (non-zero exit / Err), not on internal state.
  </action>
  <acceptance_criteria>
    - New test passes and demonstrates advance fails cleanly (surfaced error / non-zero exit) on missing and/or corrupt state — no panic, no false success.
    - `cargo test -p devflow-core --test monitor_e2e` green.
    - `cargo clippy --workspace --tests -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted `.devflow/state.json` → advance | Corrupt/missing state must fail loudly, not silently mis-drive the workflow. |
| gate response file → poll loop | A present response must be honored immediately; a mis-ordered check could block an unattended run for days. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-08-DoS | Denial of Service (availability) | `Gates::poll_response` fast path | low | mitigate | Test proves a present response returns immediately even at the 7-day deadline, so a responded gate can never wedge for the full window. |
| T-12-08-R | Repudiation (silent mis-drive) | `advance` on corrupt/missing state | low | mitigate | Test asserts advance fails loudly on missing/corrupt state rather than silently proceeding. |
</threat_model>

## Artifacts this phase produces

New symbols introduced by this plan (exclude from source-drift verification):
- `poll_response_returns_immediately_at_full_timeout` — new test fn in gates.rs (with a local `SEVEN_DAYS` const in the test).
- A new ahead/behind correctness test fn in git.rs (name at executor's discretion), plus a possible label fix inside `list_feature_branches` (no new public symbol).
- A new integration test fn in monitor_e2e.rs for advance-on-bad-state.

<verification>
- `cargo test -p devflow-core gates:: git::` and `cargo test -p devflow-core --test monitor_e2e` green.
- `cargo clippy --workspace --tests -- -D warnings` and `cargo fmt --check` clean.
</verification>

<success_criteria>
- Gate poll fast path proven non-blocking at the real 7-day deadline.
- `list_feature_branches` ahead/behind semantics correct and tested (swap fixed if present).
- Monitor/advance failure on missing/corrupt state covered.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-08-SUMMARY.md` when done.
</output>
