---
phase: 12-bootstrap-housekeeping
plan: 12
type: execute
wave: 2
depends_on: [12-01, 12-02, 12-03]
files_modified: []
autonomous: false
requirements: [12g]
must_haves:
  truths:
    - "The live Hermes gate round-trip has been executed by a human and observed to work end-to-end"
    - "A real credentialed agent CLI (Claude/Codex/OpenCode) has been launched through devflow at least once and observed"
    - "The DocsUpdate fail-soft skip path has been observed to be visible to the user (not silently swallowed)"
    - "The Full-Ship-workflow verification is explicitly recorded as BLOCKED on the out-of-scope ship.rs rewrite, not silently skipped"
  artifacts:
    - path: ".planning/phases/12-bootstrap-housekeeping/12-12-SUMMARY.md"
      provides: "recorded outcomes/notes for each manual verification, including the blocked item"
  key_links:
    - from: ".devflow/gates/*.json"
      to: "human response file → devflow ack"
      via: "live Hermes session round-trip"
      pattern: "gates"
---

<objective>
12g — execute the manual/e2e verifications that were marked "manual-only" at Phase 11 sign-off but were never actually run. These require live credentials / a live Hermes session and cannot be covered by fake-shell tests, so they are human-verify checkpoints with exact steps to run and observe. Do NOT pretend a fake-shell test covers them.

One 12g item — the Full Ship review/merge workflow — is BLOCKED on the `ship.rs` GSD-native rewrite, which is explicitly OUT OF SCOPE for Phase 12 (see CONTEXT.md "Explicitly Out of Scope"). It is documented here as blocked rather than planned around.

Purpose: Get real human confirmation of the gate round-trip, real agent launch, and DocsUpdate visibility; record the blocked item honestly.
Output: recorded outcomes in the SUMMARY for each item.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@crates/devflow-core/src/gates.rs
@crates/devflow-core/src/hooks.rs
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: live Hermes gate round-trip end-to-end</name>
  <action>Human runs the steps in how-to-verify against a live Hermes session and reports the outcome; nothing is auto-executed.</action>
  <what-built>The gate-file protocol (Gates::write_gate / poll_response / ack) and Hermes cron integration are already implemented; this verifies the live round-trip end-to-end.</what-built>
  <how-to-verify>
    1. Start a supervised phase so a Validate gate fires: `devflow start --phase <N> --agent claude --mode supervise` in a scratch/test repo (or drive a phase to the Validate gate).
    2. Confirm DevFlow writes the gate request: check `.devflow/gates/<NN>-validate.json` exists and contains the phase/stage/context.
    3. Through a LIVE Hermes session, deliver/answer the gate: drop a response file `.devflow/gates/<NN>-validate.response.json` with `{"approved": true}` (or reject with a note), as Hermes would.
    4. Observe DevFlow pick up the response: it prints the gate resolution and writes the ack file `.devflow/gates/<NN>-validate.ack.json` so the Hermes poller can clean up.
    5. Confirm the workflow advances (approve) or loops back (reject) accordingly.
    Record: did the request→human→response→ack round-trip complete through a real Hermes session? Note any friction.
  </how-to-verify>
  <resume-signal>Type "approved" if the live Hermes gate round-trip worked end-to-end, or describe what broke.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: real credentialed agent CLI launch + capture</name>
  <action>Human runs the steps in how-to-verify with a real agent CLI and reports the outcome; nothing is auto-executed.</action>
  <what-built>Agent adapters (Claude/Codex/OpenCode) and the monitor that owns the launched agent are implemented; tests use fake shell agents. This verifies a real credentialed agent CLI actually launches and is captured.</what-built>
  <how-to-verify>
    1. Ensure at least one real agent CLI is installed and authenticated (e.g. `claude --version` works; run `devflow doctor` to confirm it is detected and reports version 1.2.0 for devflow).
    2. Launch a real phase against it in a scratch repo: `devflow start --phase <N> --agent claude --mode auto` (or codex/opencode).
    3. Observe the monitor: `.devflow/phase-<NN>-stdout.log` fills with the agent's real output; `.devflow/phase-<NN>-agent.pid` and the exit-code file are written when it finishes; `devflow advance` runs afterward.
    4. Confirm the agent's `DEVFLOW_RESULT` completion marker (or exit code) is parsed and the stage machine reacts.
    Record: which agent CLI was exercised, whether capture + advance worked with a real paid/credentialed agent, and any deviation from the fake-agent behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if a real agent CLI launched, was captured, and advanced the workflow, or describe the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: DocsUpdate fail-soft skip is visible to the user</name>
  <action>Human runs the steps in how-to-verify and reports whether the DocsUpdate skip/failure was visible and non-fatal; nothing is auto-executed.</action>
  <what-built>The DocsUpdate hook (Validate→Ship) runs `cargo doc` and is intentionally fail-soft: on failure it warns and skips the commit rather than aborting the ship. This verifies the skip path is visible to the user, not silently swallowed.</what-built>
  <how-to-verify>
    1. In a scratch repo where `cargo doc` will not cleanly commit (e.g. no doc changes, or a deliberately broken doc build), drive a phase through the Validate→Ship transition (or invoke the ship flow) so the DocsUpdate hook runs.
    2. Observe the output: DevFlow should emit a visible warn-level message for the DocsUpdate skip/failure (e.g. "DocsUpdate: cargo doc reported a failure; skipping commit" or "DocsUpdate: commit failed") — confirm it appears to the user and the workflow still proceeds.
    3. Confirm the fail-soft path did NOT abort the ship and did NOT silently hide the skip.
    Record: was the DocsUpdate skip/failure clearly visible to the user, and did the workflow continue?
  </how-to-verify>
  <resume-signal>Type "approved" if the DocsUpdate fail-soft skip was visible and non-fatal, or describe what you saw.</resume-signal>
</task>

</tasks>

<blocked_items>
- **Full Ship review/merge workflow (12g)** — BLOCKED. It depends on the `ship.rs` GSD-native rewrite (`ship_phase()`, `/gsd-ship` + `/gsd-code-review` integration, `ReviewFailed`/`AgentFailed` handling), which is explicitly OUT OF SCOPE for Phase 12 per CONTEXT.md. No verification task is created for it here; it must be verified after that rewrite lands (unassigned phase). Record this block in the SUMMARY so it is not lost.
</blocked_items>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human/Hermes → gate response file | The gate protocol accepts a human decision from an external session; this plan only observes it, it changes no code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-12-R | Repudiation (unverified claims) | manual sign-off items never actually run | low | mitigate | Execute the round-trip/agent/docs verifications with a human and record outcomes; explicitly record the blocked Full-Ship item rather than silently marking it done. |
</threat_model>

## Artifacts this phase produces

This plan produces NO code artifacts. It records verification outcomes in `12-12-SUMMARY.md` (including the blocked Full-Ship item). No new symbols.

<verification>
- Each human-verify checkpoint resolved with a recorded outcome.
- The blocked Full-Ship item recorded in the SUMMARY.
</verification>

<success_criteria>
- Live Hermes gate round-trip, real agent launch, and DocsUpdate visibility each verified by a human and recorded.
- Full Ship workflow recorded as blocked on the out-of-scope ship.rs rewrite.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-12-SUMMARY.md` when done, recording each verification outcome and the blocked item.
</output>
