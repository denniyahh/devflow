---
phase: 12-bootstrap-housekeeping
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/version.rs
  - crates/devflow-core/src/config.rs
  - crates/devflow-core/src/prompt.rs
  - crates/devflow-core/src/stage.rs
autonomous: true
requirements: [WR-04, IN-05, 12f-workspace-write]
must_haves:
  truths:
    - "The TOML section tracker correctly handles [[array-of-tables]] headers and does not misread an inline-table value line as a section key/value"
    - "version::write_version correctly targets workspace.package.version in a workspace Cargo.toml (covered by a test)"
    - "Code module docs no longer assert v2.0.0 as the current/shipped version; devflow doctor reports 1.2.0"
  artifacts:
    - path: "crates/devflow-core/src/version.rs"
      provides: "robust section-header parsing (array-of-tables + inline-table safety) and a workspace-package write_version test"
    - path: "crates/devflow-core/src/config.rs"
      provides: "module docs corrected to not claim v2.0.0 as current"
  key_links:
    - from: "field_for -> workspace.package.version"
      to: "replace_version_in_contents"
      via: "section tracker matches [workspace.package] and skips [[...]] / inline-table noise"
      pattern: "workspace.package"
---

<objective>
Complete the versioning-hygiene slice of Phase 12 (12b):

- WR-04 (`version.rs:176-203`): `parse_section_header` strips only a single `[`/`]`, so `[[array-of-tables]]` headers become a malformed section name, and inline-table value lines (`key = { version = "…" }`) can be misparsed. Harden the section tracker so an edge-case Cargo.toml cannot misroute the version lookup.
- 12f test #7: `version::write_version` is only tested against a simple `[package]` Cargo.toml; add a sibling test proving it rewrites `workspace.package.version` in a workspace Cargo.toml.
- IN-05 (per locked decision 1): correct premature *current-version* "v2.0.0" claims in code module docs to reflect the real current version (1.2.0). Do NOT bump the workspace version; v2.0.0 is the target that ships at the end of the Phase 11–14 arc.

Note on 12b scope: the pre-Phase-11 "extend version.rs beyond pyproject-only" concern is ALREADY RESOLVED in current code — `detect_version_file` checks Cargo.toml→pyproject.toml→package.json and `field_for` already selects `workspace.package.version` vs `package.version` (confirmed by existing passing tests `detect_prefers_cargo_then_pyproject_then_package_json` and `read_major_from_workspace_package`). No new detection work is needed beyond WR-04.

Purpose: Make version detection/writing robust to real Cargo.toml shapes and stop the docs from claiming a not-yet-shipped version.
Output: robust section parsing + a workspace write test + corrected module docs; `devflow doctor` verified to report 1.2.0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@.planning/phases/12-bootstrap-housekeeping/12-PATTERNS.md
@crates/devflow-core/src/version.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-04 — robust TOML section parsing (array-of-tables + inline tables)</name>
  <files>crates/devflow-core/src/version.rs</files>
  <read_first>
    - crates/devflow-core/src/version.rs (parse_section_header at 176-180; find_version_in_contents at 182-203; replace_version_in_contents at 205-249; split_field at 168-174; field_for at 58-72)
  </read_first>
  <behavior>
    - `[[bin]]` / `[[workspace.metadata.x]]` array-of-tables headers are parsed to their real dotted table path (double brackets stripped), not a name with a stray bracket.
    - A `version` key inside a `[workspace.dependencies]` inline table (e.g. `serde = { version = "1" }`) is never returned when the requested field is `workspace.package.version`.
    - Existing detection/read/write tests remain green; the correct `workspace.package.version` is still found and replaced regardless of where `[workspace.dependencies]` sits relative to `[workspace.package]`.
  </behavior>
  <action>
    Extend `parse_section_header` to detect and correctly strip array-of-tables headers: when the trimmed line is wrapped in double brackets, strip both pairs so `current` reflects the real dotted table path rather than a value containing a stray bracket. In `find_version_in_contents` and `replace_version_in_contents`, guard the key/value split so a value-line that is an inline table (its value begins with `{`) is not matched as `key = version` — only accept a scalar assignment for the target `key`. Keep the existing `split_field`/`field_for` contract. Add a regression test seeding a Cargo.toml where `[workspace.dependencies]` (with an inline `version = "1"` inside a `serde = { ... }` line) appears BEFORE `[workspace.package]\nversion = "1.2.0"`, and assert `read_major_version` returns 1 and `write_version` rewrites the `workspace.package` version, not the dependency's.
  </action>
  <acceptance_criteria>
    - New regression test passes: an inline-table `version` in `[workspace.dependencies]` does not shadow `workspace.package.version`.
    - Existing version.rs tests (`read_major_from_workspace_package`, `write_version_replaces_in_cargo_toml`, `detect_prefers_cargo_then_pyproject_then_package_json`) still pass.
    - `cargo test -p devflow-core version::` green; `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 12f #7 — write_version against a workspace Cargo.toml</name>
  <files>crates/devflow-core/src/version.rs</files>
  <read_first>
    - crates/devflow-core/src/version.rs (existing write_version_replaces_in_cargo_toml at 356-375 — the exact structural analog to mirror; write_version at 156-166; field_for at 58-72)
  </read_first>
  <action>
    Add a sibling test `write_version_replaces_in_workspace_cargo_toml` mirroring the existing `write_version_replaces_in_cargo_toml`, but seed the file with `"[workspace.package]\nversion = \"0.1.0\"\nedition = \"2024\"\n"`. Call `write_version` with a `Version { major, minor, patch }` and assert the resulting file contents contain the new `version = "…"` under the workspace-package section, proving `field_for` selects `workspace.package.version` and `replace_version_in_contents` targets it.
  </action>
  <acceptance_criteria>
    - `cargo test -p devflow-core version::write_version_replaces_in_workspace_cargo_toml` passes.
    - `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: IN-05 — correct premature v2.0.0 current-version claims in module docs</name>
  <files>crates/devflow-core/src/version.rs, crates/devflow-core/src/config.rs, crates/devflow-core/src/prompt.rs, crates/devflow-core/src/stage.rs</files>
  <read_first>
    - crates/devflow-core/src/config.rs (module doc lines 1-6 and the "fixed to the v2.0.0 constants" comment at ~15)
    - crates/devflow-core/src/version.rs (module doc line 3 "v2.0.0 derives the version…")
    - crates/devflow-core/src/prompt.rs (module doc line 3 "v2.0.0 prompts are minimal…")
    - crates/devflow-core/src/stage.rs (line 1 "DevFlow v2.0.0 stage machine.")
    - .planning/phases/12-bootstrap-housekeeping/CONTEXT.md ("Planning-Time Decisions" — decision 1)
  </read_first>
  <action>
    Reword the module/doc comments that assert "v2.0.0" as the current/shipped generation so they describe current behavior without pinning to a not-yet-shipped version. In each of the four files, change phrasings like "v2.0.0 derives the version…", "v2.0.0 has no `.devflow.yaml`…", "fixed to the v2.0.0 constants", "v2.0.0 prompts are minimal…", and "DevFlow v2.0.0 stage machine." to describe DevFlow's current behavior (drop the version pin), OR — where a forward reference is genuinely intended — phrase it explicitly as the in-progress v2 line (target 2.0.0), never as the current version. Do NOT change any workspace/crate `version = "1.2.0"` value and do NOT touch README.md / ARCHITECTURE.md prose (their rewrite is Phase 13). This is a comment-only change to the four `.rs` files.
  </action>
  <acceptance_criteria>
    - The four module docs no longer state or imply that v2.0.0 is the current/shipped version (reviewer check of the reworded comments).
    - `cargo run -p devflow -- doctor` (or `cargo build -p devflow && ./target/debug/devflow doctor`) reports the devflow version as 1.2.0.
    - No source `version = "1.2.0"` value was changed; `cargo build`, `cargo clippy -- -D warnings`, and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project version file (Cargo.toml) → version parser | An unusual-but-valid Cargo.toml shape (array-of-tables, inline tables) could steer the version lookup to the wrong field. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05-T | Tampering (wrong-field write) | `version::find/replace_version_in_contents` | low | mitigate | Array-of-tables + inline-table hardening ensures the version is read/written from `workspace.package.version` only, not a dependency's inline `version`. |
| T-12-05-I | Information Disclosure (misleading version) | module docs claiming v2.0.0 | low | mitigate | Correct the docs so users are not told a not-yet-shipped version is current; `devflow doctor` already reports the real 1.2.0. |
</threat_model>

## Artifacts this phase produces

New/changed symbols introduced by this plan (exclude from source-drift verification):
- New test fns in `crates/devflow-core/src/version.rs`: `write_version_replaces_in_workspace_cargo_toml` and a WR-04 regression test (name at executor's discretion, e.g. `inline_table_version_does_not_shadow_workspace_package`).
- Possibly a small change to `parse_section_header` return handling for `[[...]]` (no new public symbol).
- Reworded module doc comments in config.rs, version.rs, prompt.rs, stage.rs (comments only).

<verification>
- `cargo test -p devflow-core version::` green.
- `devflow doctor` reports 1.2.0.
- `cargo clippy -- -D warnings` and `cargo fmt --check` clean.
</verification>

<success_criteria>
- WR-04: section parser handles array-of-tables + inline tables; version lookup cannot be misrouted.
- 12f #7: workspace-package `write_version` covered by a passing test.
- IN-05: module docs corrected to 1.2.0-consistent wording; no version bump; doctor reports 1.2.0.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-05-SUMMARY.md` when done.
</output>
