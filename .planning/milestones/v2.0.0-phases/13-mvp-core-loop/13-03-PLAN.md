---
phase: 13-mvp-core-loop
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/agent_result.rs
autonomous: true
requirements: [13b]
must_haves:
  truths:
    - "A Claude JSON envelope with `is_error: true` is treated as a Layer-1 failure even when no DEVFLOW_RESULT marker is present."
    - "`is_error: true` OVERRIDES a success DEVFLOW_RESULT marker in the same envelope: the envelope is authoritative for errors, so a stale/echoed success marker inside an errored envelope cannot win."
    - "A Codex `--json` JSONL stream is parsed: a final `turn.failed` event is a decisive Layer-1 failure (reason from error.message). A final `turn.completed` with NO marker is NOT classified as unconditional Success — it returns None and defers to the exit-code+commit Layer 2, so a marker-less Codex turn cannot silently advance (esp. at Validate)."
    - "The Claude single-envelope path is tried before the Codex JSONL path; the Codex path is only decisive when the captured stdout is actually a multi-line `turn.*` event stream (≥1 `turn.*` line), so the two formats never cross-consume each other."
    - "Layer 2 keeps `exit≠0 ⇒ Failed` for ALL stages; only the `exit=0 && commits=0 ⇒ Failed` branch is stage-scoped — it applies to `Stage::Plan`/`Stage::Code` only (explicit matches!, NOT is_agent_stage which includes Define). Define/Validate zero-commit exit=0 is not Failed."
  artifacts:
    - "crates/devflow-core/src/agent_result.rs (Claude envelope is_error/num_turns reads with marker override; Codex JSONL parser that defers turn.completed; stage-scoped Layer 2 with explicit matrix)"
  key_links:
    - "evaluate_layer1 → Claude envelope is_error check (overrides success marker) + Codex event-stream parse (turn.failed decisive, turn.completed defers)"
    - "evaluate_agent_result → evaluate_layer2 with state.stage → commit gate only for matches!(Stage::Plan | Stage::Code)"
---

<objective>
Parse the native per-adapter completion signals that DevFlow already requests but never
reads. For Claude (`--output-format json`): read the top-level `is_error` boolean (the
authoritative error signal) and `num_turns`, treating `is_error: true` as a Layer-1 failure
independent of the DEVFLOW_RESULT marker. For Codex (`--json`): add a JSONL event-stream
parser that finds the last `turn.completed` / `turn.failed` event (the current single-JSON
parser silently returns None on Codex's per-line stream). Scope the Layer-2 commit-count
gate to Code-like stages so Define and Validate — which legitimately produce zero commits —
are no longer mis-flagged as failures.

Purpose: 13b's completion-protocol correctness. `DEVFLOW_RESULT: success` only proves the
agent's turn ended without self-reported failure; cross-checking the native envelope closes
the "verdict vs. ran" gap for every stage. This plan is agent_result.rs-only (parsing
layer), independent of the main.rs failure wiring, so it runs in Wave 1.
Output: extended Claude envelope reader, a new Codex JSONL parser, and a stage-aware Layer 2.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mvp-core-loop/CONTEXT.md
@.planning/phases/13-mvp-core-loop/13-RESEARCH.md
@.planning/phases/13-mvp-core-loop/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan (exclude from drift/source-grounding review):
- New/extended Claude envelope reader in `agent_result.rs` (e.g. `detect_claude_envelope_failure` reading `is_error`/`num_turns`), wired so `is_error: true` overrides a success marker
- New Codex JSONL event-stream parser in `agent_result.rs` (e.g. `parse_codex_event_result`) that treats `turn.failed` as decisive and `turn.completed` as defer-to-Layer-2
- `evaluate_layer2` gains a stage parameter (or its caller branches on `state.stage`) to apply the commit gate only to `Stage::Plan`/`Stage::Code`
- New tests: `claude_envelope_is_error_detected`, `claude_is_error_overrides_success_marker`, `claude_envelope_is_error_false_defers`, `codex_event_stream_parses_turn_failed`, `codex_turn_completed_no_marker_defers`, `codex_event_stream_ignores_progress_and_unparseable_lines`, `claude_envelope_not_consumed_by_codex_parser`, `layer2_nonzero_exit_is_failed_all_stages`, `layer2_skips_commit_gate_for_define_and_validate`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Read Claude envelope is_error / num_turns as an authoritative failure signal</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs lines 52-138 (parse_devflow_result, extract_json_result_text, detect_claude_rate_limit, json_has_str/json_has_i64/json_find_key — the sibling-field extraction idioms to copy)
    - crates/devflow-core/src/agent_result.rs lines 216-229 (evaluate_layer1 — where the new is_error check plugs in)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "Code Examples" (confirmed Claude envelope schema: `{"type":"result","subtype":"success","is_error":false,"num_turns":4,"result":"...","session_id":"..."}`) and Pitfall 5 (treat is_error as primary; specific non-success subtype strings are [ASSUMED], not load-bearing)
  </read_first>
  <behavior>
    - `claude_envelope_is_error_detected`: a Claude envelope with `is_error: true` and NO DEVFLOW_RESULT marker in the `result` text yields an AgentResult with status Failed.
    - `claude_is_error_overrides_success_marker`: a Claude envelope with `is_error: true` whose `result` DOES contain a `DEVFLOW_RESULT: {"status":"success"}` marker yields status Failed — the errored envelope wins over the (stale/echoed) success marker.
    - `claude_envelope_is_error_false_defers`: a Claude envelope with `is_error: false` and no marker returns None from Layer 1 (defers to Layer 2), preserving current behavior.
    - `claude_envelope_marker_still_wins`: an `is_error: false` envelope whose `result` contains a valid success DEVFLOW_RESULT marker is still parsed by the marker path as Success (no regression).
  </behavior>
  <action>
    Add a function that parses the Claude JSON envelope for the top-level `is_error` boolean and `num_turns`, using the same `serde_json::Value::get(...).and_then(Value::as_bool/as_u64)` idiom as detect_claude_rate_limit. Wire it into `evaluate_layer1` with this precedence (Codex 13-03 MEDIUM — envelope authoritative for errors):

    1. If the stdout is a single JSON Claude envelope with `is_error == true`, return `status: AgentStatus::Failed` with a reason derived from the envelope (the `result` text or `subtype`, falling back to "agent reported is_error"). This check runs BEFORE accepting a success marker, so `is_error: true` OVERRIDES a success DEVFLOW_RESULT marker embedded in the same envelope. (A `failed` marker and `is_error: true` agree, so order does not matter there.)
    2. Otherwise (`is_error` absent or `false`), fall through to the existing `parse_devflow_result` marker path — a valid success/failed marker is honored as today; `is_error: false` with no marker returns None so Layer 2 runs.

    Do NOT special-case specific non-success `subtype` strings beyond what already exists — `is_error` is the authoritative signal per RESEARCH Pitfall 5. Optionally surface `num_turns` in the reason string for observability. Add `///` doc comments. Use `.ok()`/`and_then` throughout — never `.unwrap()` on parsed input (a malformed envelope must not panic; it simply is not a Claude envelope and defers).
  </action>
  <verify>
    <automated>cargo test -p devflow-core claude_envelope claude_is_error</automated>
  </verify>
  <acceptance_criteria>
    - `agent_result.rs` reads `is_error` (source assertion: `.get("is_error")` present)
    - The is_error check is ordered so `is_error: true` returns Failed even when a success marker is present (behavior assertion: `claude_is_error_overrides_success_marker` passes)
    - `cargo test -p devflow-core claude_envelope claude_is_error` exits 0
    - `cargo test -p devflow-core` full crate exits 0 — existing envelope/marker tests unregressed
  </acceptance_criteria>
  <done>A Claude `is_error: true` envelope is a Layer-1 failure even without a marker AND overrides a success marker; `is_error: false` deferral and normal marker precedence preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse the Codex --json JSONL event stream</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs lines 97-127 (detect_codex_rate_limit — the closest "parse captured Codex stdout" precedent; the new parser is a JSONL sibling)
    - crates/devflow-core/src/agent_result.rs lines 216-229 (evaluate_layer1 wiring point)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "Code Examples" (confirmed Codex JSONL shapes: thread.started / turn.started / item.* / turn.completed with usage / turn.failed with error.message) and Pitfall 4 (Codex stdout is per-line JSONL, NOT a single JSON document — a single-document parser silently returns None)
    - crates/devflow-core/src/agent.rs (capture_agent_output — confirms full stdout is buffered to a string before advance() runs, so a batch line-by-line parse is sufficient; no streaming/async needed)
  </read_first>
  <behavior>
    - `codex_event_stream_parses_turn_failed`: captured stdout of several JSONL lines ending in a `turn.failed` event with `error.message` yields status Failed with reason = that message (turn.failed is DECISIVE).
    - `codex_turn_completed_no_marker_defers`: JSONL ending in `turn.completed` with NO DEVFLOW_RESULT marker returns None from the Codex parser — it does NOT return unconditional Success (so the exit-code+commit Layer 2 decides; a marker-less Codex turn cannot silently advance). This is the composition fix for consensus #1.
    - `codex_event_stream_ignores_progress_and_unparseable_lines`: interleaved `item.started`/`item.updated` and any non-JSON progress lines are skipped without error; the LAST terminal event decides.
    - `claude_envelope_not_consumed_by_codex_parser`: a single-document Claude envelope (one JSON object, `type: "result"`, no `turn.*` lines) is NOT treated as a Codex stream — the Codex path returns None so the Claude path handles it.
  </behavior>
  <action>
    Add a Codex JSONL parser: iterate `stdout.lines()`, `serde_json::from_str::<serde_json::Value>(line).ok()` each non-empty line (skip parse failures — progress lines are not part of the completion contract), and find the LAST event whose `type` is `turn.completed` or `turn.failed`. Mapping (consensus #1 / Cursor 13-03):
    - `turn.failed` → AgentResult status Failed, reason from `error.message` (fall back to a generic message if absent). DECISIVE.
    - final `turn.completed` → return None (defer to Layer 2). Do NOT map `turn.completed` to unconditional Success — a clean terminal turn without a DEVFLOW_RESULT marker must fall through to the exit-code+commit gate, otherwise a marker-less Validate run could advance to Ship (the exact false-pass bug 13b closes). The success signal for advancement stays the marker (Layer 1 first check) or Layer 2's commit gate — never a bare `turn.completed`.
    - no terminal event → None.

    DISCRIMINATION (Cursor 13-03 MEDIUM): the Claude single-envelope path (Task 1 / parse_devflow_result) is tried FIRST in evaluate_layer1. Only invoke the Codex parser as decisive when the captured stdout is actually a Codex event stream — require ≥1 line that parses to an object whose `type` is `thread.started` or starts with `turn.` (a Claude single JSON object has `type: "result"` and no `turn.*` lines, so it will not be consumed here). Do NOT add a new NDJSON dependency — plain serde_json line-by-line per RESEARCH "Don't Hand-Roll".

    KNOWN-LIMITATION (OpenCode HIGH #3): add a `///` doc comment on the Codex parser noting it is written against the documented `--json` event schema (RESEARCH Pitfall 4/5) and is not yet verified against the installed Codex CLI version — the 13-06 dogfood run captures real output and reconciles any delta (same empirical practice 12-12-SUMMARY.md used for Claude). Do NOT place any literal that a negative-grep gate depends on inside this comment.
  </action>
  <verify>
    <automated>cargo test -p devflow-core codex_event_stream codex_turn_completed claude_envelope_not_consumed</automated>
  </verify>
  <acceptance_criteria>
    - `agent_result.rs` contains a parser iterating `.lines()` and matching `turn.completed` / `turn.failed` (source assertion: both event-type literals present)
    - `turn.completed` without a marker returns None, not Success (behavior assertion: `codex_turn_completed_no_marker_defers` passes)
    - The Codex parser is not decisive for a Claude single-envelope (behavior assertion: `claude_envelope_not_consumed_by_codex_parser` passes)
    - `cargo test -p devflow-core codex_event_stream codex_turn_completed claude_envelope_not_consumed` exits 0
    - No new dependency added to `crates/devflow-core/Cargo.toml` (source assertion: git diff of Cargo.toml is empty)
  </acceptance_criteria>
  <done>Codex JSONL turn.failed is a decisive failure; turn.completed defers to Layer 2 (no silent advance); Claude envelopes are not cross-consumed; progress lines ignored; parser carries an unverified-schema note; no new dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scope the Layer-2 commit gate to Code-like stages</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs lines 231-359 (evaluate_layer2, evaluate_layer3, evaluate_agent_result — the commit-count gate and its caller which already has `state` in scope)
    - crates/devflow-core/src/stage.rs lines 16-70 (Stage enum + is_agent_stage — Define/Plan/Code are agent stages; Define legitimately produces zero commits, and Validate produces zero commits)
    - .planning/phases/13-mvp-core-loop/CONTEXT.md § 13b ("Commit-count fallback (Layer 2) scoped to Code-like stages only — Define and Validate legitimately produce zero commits")
  </read_first>
  <behavior>
    - `layer2_skips_commit_gate_for_define_and_validate`: with exit_code 0 and zero commits, a Define-stage and a Validate-stage evaluation do NOT return status Failed for "no commits" (they succeed or defer), while a Code-stage evaluation with the same inputs still returns Failed "no commits" (existing behavior preserved).
    - `layer2_nonzero_exit_is_failed_all_stages`: a NONZERO exit code returns status Failed for EVERY stage — including Define and Validate (a crashed Define/Validate agent, exit≠0 commits=0, is still Failed; only the exit=0 branch is stage-scoped). Closes consensus #9 / OpenCode #5.
    - Existing Layer-2 tests (evaluate_layer2_exit_zero_no_commits_is_failed for Code, nonzero-exit failure) remain green.
  </behavior>
  <action>
    Make ONLY the Layer-2 "exit=0, commits=0 → failed" branch stage-aware; keep `exit≠0 → Failed` unconditional for ALL stages. Add a `stage: Stage` parameter to `evaluate_layer2` (threading `state.stage` from the `evaluate_agent_result` caller which already holds `state`), OR branch in `evaluate_agent_result` before invoking Layer 2 — prefer the parameter so the logic is unit-testable in isolation.

    Implement this explicit decision matrix (OpenCode suggestion 2 — make it explicit; use `matches!`, NOT `Stage::is_agent_stage()`, because is_agent_stage() includes Define which must NOT be commit-gated — Codex 13-03 MEDIUM):
    - `exit ≠ 0` → Failed (ALL stages — the agent crashed/errored)
    - `exit = 0`, `matches!(stage, Stage::Plan | Stage::Code)`, `commits = 0` → Failed ("no work done")
    - `exit = 0`, `matches!(stage, Stage::Plan | Stage::Code)`, `commits > 0` → Success
    - `exit = 0`, other stages (Define, Validate, Ship), `commits = 0` → NOT Failed (return Success or defer to Layer 3; "no commits expected"). Note: this leaves the Validate SUCCESS decision to the marker/verdict (Layer 1 / Plan 05), not to a bare zero-commit — consistent with Task 2's turn.completed-defers rule so Validate cannot false-pass.

    Update the one existing call site and any existing tests that call evaluate_layer2 with the new signature. Add `///` doc comments reproducing the matrix and the rationale.
  </action>
  <verify>
    <automated>cargo test -p devflow-core evaluate_layer2 layer2_skips_commit_gate_for_define_and_validate layer2_nonzero_exit_is_failed_all_stages</automated>
  </verify>
  <acceptance_criteria>
    - `evaluate_layer2` signature includes a `Stage` (source assertion: `stage: Stage` in the fn signature) OR the caller branches on stage before Layer 2
    - The commit gate uses `matches!(stage, Stage::Plan | Stage::Code)` (source assertion) — NOT `is_agent_stage()`
    - `exit≠0` is Failed regardless of stage (behavior assertion: `layer2_nonzero_exit_is_failed_all_stages` passes)
    - `cargo test -p devflow-core evaluate_layer2` exits 0 (existing Code-stage zero-commit-is-failed test still passes)
    - `cargo test -p devflow-core layer2_skips_commit_gate_for_define_and_validate` exits 0
    - `cargo test -p devflow-core` full crate exits 0
  </acceptance_criteria>
  <done>Define/Validate zero-commit exit=0 runs no longer trip Layer 2; exit≠0 is still Failed for every stage; commit gate scoped via explicit matches!(Plan|Code); Code-stage gating unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent stdout → parser | Captured agent stdout (Claude envelope / Codex JSONL) is untrusted input parsed by serde_json; malformed input must not panic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-08 | Denial of Service | parser panic on malformed envelope/JSONL | medium | mitigate | Use `.ok()` / `and_then` throughout (no `.unwrap()` outside tests); unparseable lines are skipped, absent fields default. Tests cover malformed/partial input. |
| T-13-09 | Spoofing | agent emits `is_error: false` while having actually failed | low | accept | Layer 2/3 commit-gate and exit-code remain as defense-in-depth; single-operator trust boundary. `is_error` is the documented authoritative signal (RESEARCH Pitfall 5). |
| T-13-10 | Tampering | undocumented Codex/Claude schema drift silently mis-classifies | low | accept | RESEARCH flags exact subtype enumeration as an upstream doc gap; is_error and turn.completed/turn.failed are the stable signals. The Codex parser carries an explicit unverified-schema doc note; the 13e dogfood run captures a real invocation and reconciles any delta. |
| T-13-19 | Tampering | a Codex `turn.completed` (or zero-commit Validate) advancing to Ship without a real pass signal | high | mitigate | `turn.completed` without a marker returns None (defers to Layer 2); Layer 2 does not treat zero-commit Validate as Success; Validate advancement requires an explicit verdict (Plan 05). Composed, a marker-less/verdict-less Validate cannot reach Ship. |

No new external packages are added by this plan (no npm/pip/cargo install) — package-legitimacy checkpoint not applicable.
</threat_model>

<review_findings>
Cross-AI review (13-REVIEWS.md) findings addressed by this plan:

| Finding | Severity | Reviewers | Disposition |
|---------|----------|-----------|-------------|
| Codex `turn.completed` → unconditional Success bypasses Layer 2 / composes with Plan 05 to false-pass Validate | HIGH | Codex, OpenCode, Cursor (consensus #1) | INCORPORATED — Task 2 makes `turn.completed` defer (return None), only `turn.failed` decisive; Layer 2 no longer treats zero-commit Validate as Success; coordinated with Plan 05 verdict requirement. |
| Marker precedence over `is_error` lets a stale success marker win inside an errored envelope | MEDIUM | Codex 13-03 | INCORPORATED — Task 1 orders `is_error: true` BEFORE the success-marker check so the envelope overrides the marker. |
| Layer-2 stage-scoping must keep `exit≠0 ⇒ Failed` for ALL stages; use explicit matches!(Plan\|Code), not is_agent_stage() | MEDIUM | Codex, OpenCode (consensus #9) | INCORPORATED — Task 3 explicit matrix; `layer2_nonzero_exit_is_failed_all_stages` test; commit gate scoped via `matches!(Stage::Plan | Stage::Code)`. |
| Codex/Claude discrimination — heuristic must not consume Claude envelopes | MEDIUM | Cursor 13-03 | INCORPORATED — Task 2 tries Claude single-envelope first; Codex path decisive only with ≥1 `turn.*` line; `claude_envelope_not_consumed_by_codex_parser` test. |
| Codex parser has no integration test vs real Codex output | HIGH (deferred to dogfood) | OpenCode #3 | INCORPORATED (documented) — parser carries an unverified-schema doc note; real-output reconciliation is a mandatory 13-06 checkpoint. |
| Ship remains commit-gated (unmarked zero-commit /gsd-ship → Failed) | MEDIUM | Cursor 13-03 | ACCEPTED (deliberate fail-safe) — Ship is not in `{Plan, Code}` so its zero-commit exit=0 defers rather than auto-failing; Ship advancement is separately gated by the human approve-merge gate + Plan 01 failure branch, so no silent auto-ship. Documented here. |
</review_findings>

<verification>
- `cargo test --workspace` exits 0
- `cargo clippy --workspace -- -D warnings` exits 0
- `cargo fmt --check` exits 0
- `git diff --stat crates/devflow-core/Cargo.toml` shows no change (no new dependency)
</verification>

<success_criteria>
- Claude `is_error: true` detected as failure without a marker (13b)
- Codex JSONL turn.failed/turn.completed parsed correctly (13b)
- Layer 2 no longer mis-flags zero-commit Define/Validate (13b)
</success_criteria>

<output>
Create `.planning/phases/13-mvp-core-loop/13-03-SUMMARY.md` when done
</output>
