---
phase: 15-oss-readiness
plan: 05
type: execute
wave: 2
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified: []
autonomous: false
requirements: [15b]
user_setup:
  - service: crates.io
    why: "Publishing the devflow-core and devflow crates to the public registry"
    env_vars:
      - name: CARGO_REGISTRY_TOKEN
        source: "crates.io -> Account Settings -> API Tokens (or run `cargo login` interactively)"
    dashboard_config:
      - task: "Create a publish-scoped API token"
        location: "https://crates.io/settings/tokens"

must_haves:
  truths:
    - "devflow-core is published to crates.io and resolvable from a clean registry query"
    - "devflow (CLI) is published to crates.io AFTER devflow-core is live, resolving its core path/version dep from the registry"
    - "No crates.io API token is committed, echoed, or written into any repo-tracked file"
  artifacts: []
  key_links:
    - "publish ordering <-> devflow-core (leaf) must be live on the registry before devflow (CLI) publishes"
    - "operator-held CARGO_REGISTRY_TOKEN <-> cargo login credentials outside the repo (~/.cargo/credentials.toml) or CI secret"
  prohibitions:
    - "Publish `devflow` (the CLI crate) before `devflow-core` is live and resolvable on crates.io — the CLI's path+version dependency will fail to resolve"
    - "Commit, echo, log, or write a crates.io API token (CARGO_REGISTRY_TOKEN) into any repo-tracked file or shell history"
    - "Attempt to script or automate acquisition of the crates.io credential — it is operator-held; the human-action checkpoint is the only path"
    - "Bump crate versions to work around a name/version conflict without operator confirmation — a taken name is a blocking stop, not a silent rename"
---

<objective>
Publish the two crates to crates.io in dependency order, gated behind the one human touchpoint of
this phase: the operator supplying their crates.io credential. This is the only genuinely
non-autonomous task in 15b — a first publish needs an operator-held API token and no scriptable
fallback exists (RESEARCH Environment Availability: blocking dependency, no fallback). Publish is
irreversible per version, so it runs LAST (after every doc-accuracy and license plan is done) and
only after an explicit operator go-ahead.

Purpose: make DevFlow installable via `cargo install devflow` now that the docs describe the real
product and the dual license is backed by both files.
Output: `devflow-core` and `devflow` live on crates.io, published leaf-first.
</objective>

<artifacts_produced>
## Artifacts this plan produces
No repo files are created or modified. This plan performs external registry actions only:
- `devflow-core` crate version 1.2.0 published to crates.io
- `devflow` crate version 1.2.0 published to crates.io
No new CLI flags, subcommands, or test files. The crates.io API token is operator-held and never
enters the repo.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-oss-readiness/15-RESEARCH.md
@.planning/phases/15-oss-readiness/15-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Operator authenticates to crates.io (human-only credential gate)</name>
  <action>
    Block until the operator has authenticated to crates.io with their own API token and confirmed
    intent to publish 1.2.0. The agent cannot and must not acquire or script this credential — it
    is operator-held. Do not proceed to Task 2 until the resume-signal is given. See how-to-verify
    for the exact operator steps.
  </action>
  <what-built>
    All 15b doc-accuracy plans (15-01, 15-02, 15-03) and the dual-license fix + publish dry-run
    (15-04) are complete and green. The tree is ready to publish `devflow-core` then `devflow` to
    crates.io at version 1.2.0.
  </what-built>
  <how-to-verify>
    This step requires the operator's crates.io credential, which cannot be scripted or acquired
    by the agent:
    1. Create a publish-scoped API token at https://crates.io/settings/tokens (if you don't have one).
    2. Authenticate locally WITHOUT committing anything: run `cargo login` and paste the token when
       prompted (it is stored in `~/.cargo/credentials.toml`, outside the repo), OR export
       `CARGO_REGISTRY_TOKEN` in this shell only. Never paste the token into a repo file, a commit,
       or a command that echoes it.
    3. Confirm the crate names are still available/owned by you: open
       https://crates.io/crates/devflow-core and https://crates.io/crates/devflow — if either name
       is now owned by someone else, STOP and report (do not rename crates to dodge a conflict).
    4. Publish is irreversible per version — confirm you intend to publish 1.2.0 now.
  </how-to-verify>
  <resume-signal>Type "approved" once you are authenticated to crates.io and confirm publish of 1.2.0, or describe the blocker (e.g. name taken, no token).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Publish devflow-core (leaf) and wait for registry propagation</name>
  <files>(no repo files — external registry action)</files>
  <read_first>
    - crates/devflow-core/Cargo.toml (version 1.2.0, publish metadata)
    - .planning/phases/15-oss-readiness/15-RESEARCH.md (Common Pitfall 3 — publish ordering + propagation lag; Code Examples — publish commands)
    - .planning/phases/15-oss-readiness/15-04-SUMMARY.md (the verified dry-run baseline)
  </read_first>
  <action>
    Publish the leaf crate first: `cargo publish -p devflow-core`. Then wait for the registry to
    make it resolvable before publishing the CLI — crates.io propagation can lag a few minutes.
    Poll for availability from a scratch directory (`cargo add devflow-core@1.2.0 --dry-run` in a
    throwaway crate, or fetch https://crates.io/crates/devflow-core) until it resolves. Do NOT
    proceed to the CLI publish until devflow-core@1.2.0 is confirmed live. Never echo the token.
  </action>
  <acceptance_criteria>
    - `cargo publish -p devflow-core` exits 0 (crate accepted by the registry)
    - devflow-core@1.2.0 resolves from a clean query (dry-run add in a scratch dir succeeds, or the crates.io page shows version 1.2.0)
    - No token value appears in any command output, log, or committed file
  </acceptance_criteria>
  <verify>
    <automated>cd "$(mktemp -d)" && cargo new _probe --bin >/dev/null 2>&1 && cd _probe && cargo add devflow-core@1.2.0 --dry-run</automated>
  </verify>
  <done>devflow-core 1.2.0 is live on crates.io and resolvable from a clean registry query.</done>
</task>

<task type="auto">
  <name>Task 3: Publish devflow (CLI) after core is live, then verify both crates</name>
  <files>(no repo files — external registry action)</files>
  <read_first>
    - crates/devflow-cli/Cargo.toml (the `devflow-core` path+version dependency that must resolve from the registry)
    - .planning/phases/15-oss-readiness/15-RESEARCH.md (Pitfall 3 — CLI publish fails if core is not yet resolvable)
  </read_first>
  <action>
    Only after Task 2 confirms devflow-core@1.2.0 is live, publish the CLI crate:
    `cargo publish -p devflow`. Then verify both crates are installable: confirm `devflow` resolves
    from a clean registry query. Do NOT bump versions or rename to work around a conflict — if the
    publish fails on a name/ownership conflict, STOP and report to the operator. Never echo the token.
  </action>
  <acceptance_criteria>
    - `cargo publish -p devflow` exits 0
    - devflow@1.2.0 resolves from a clean query (dry-run add in a scratch dir succeeds, or the crates.io page shows 1.2.0)
    - Both crates are live; `cargo install devflow --dry-run` (or the crates.io page) confirms the CLI is installable
    - No token value appears in any command output, log, or committed file
  </acceptance_criteria>
  <verify>
    <automated>cd "$(mktemp -d)" && cargo new _probe2 --bin >/dev/null 2>&1 && cd _probe2 && cargo add devflow@1.2.0 --dry-run</automated>
  </verify>
  <done>Both devflow-core and devflow 1.2.0 are published and installable from crates.io, in the correct order.</done>
</task>

</tasks>

<flagged_assumptions>
Surfaced (not silently resolved). None auto-resolve.
- **15b-6 (crates.io publish) — spec-less probe returned `unclassified`/`unresolved`.** Publish is an irreversible external action, not a numeric/collection/stateful edge case; correctness is proven by post-publish registry-resolution checks and the leaf-before-dependent ordering, plus the blocking human-action gate. Flagged for reviewer awareness.
- **RESEARCH Assumption A4 (name availability):** the dry-run in Plan 15-04 does NOT fully validate registry-side name ownership. If `devflow` or `devflow-core` was claimed by another account since the 2026-07-08 availability check, the real publish fails with a name conflict — the human-action checkpoint step 3 explicitly has the operator confirm ownership before proceeding, and a taken name is a blocking STOP, not a silent rename.
- **RESEARCH Environment Availability (credentials):** crates.io publish credentials are operator-held with no automatable fallback — this is intentional for a first publish. The whole phase runs as a DevFlow dogfood (`devflow start --phase 15 --mode auto`); this checkpoint is the single human touchpoint, aligned with the phase's Ship gate.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator credential -> publish tooling | The crates.io API token authorizes publishing under the project's account; it must never enter the repo or logs |
| local build artifact -> public crates.io registry | Published crate contents become permanently public and immutable per version |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-05-SC | Information Disclosure | crates.io API token handling (ASVS V14) | high | mitigate | Token supplied via `cargo login` (stored in `~/.cargo/credentials.toml`, outside the repo) or a shell-only `CARGO_REGISTRY_TOKEN`; never committed, echoed, or logged; the human-action checkpoint is the only credential path and no automation attempts to acquire it |
| T-15-05 | Tampering | publish order (core before CLI) | medium | mitigate | Publish leaf-first and poll the registry until devflow-core@1.2.0 resolves before publishing the CLI, so the CLI's path+version dep resolves from the live registry |
| T-15-05b | Repudiation | irreversible version publish | medium | accept | crates.io versions are immutable; the blocking human-action gate requires explicit operator confirmation of publishing 1.2.0 before either crate ships |
</threat_model>

<verification>
- devflow-core@1.2.0 and devflow@1.2.0 both resolve from clean scratch-dir registry queries
- Publish order honored (core live before CLI publish)
- `git status` / `git log` show no token committed; no token value in any captured output
</verification>

<success_criteria>
Both crates are live on crates.io at 1.2.0, published leaf-first after operator authentication;
`cargo install devflow` works; no credential ever entered the repo. This closes the last 15b
checklist item and completes OSS packaging.
</success_criteria>

<output>
Create `.planning/phases/15-oss-readiness/15-05-SUMMARY.md` when done
</output>
