---
phase: 15-oss-readiness
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - CONTRIBUTING.md
  - CODE_OF_CONDUCT.md
  - .devcontainer/devcontainer.json
  - .github/workflows/devcontainer.yml
autonomous: true
requirements: [15b]

must_haves:
  truths:
    - "CONTRIBUTING.md PR Process lists the three required CI checks (test, clippy -D warnings, fmt --check) mirroring ci.yml"
    - "CONTRIBUTING.md documents the .devcontainer flow alongside the existing distrobox subsection"
    - "A .devcontainer/devcontainer.json exists, pins an explicit (non-:latest) Rust base image, and installs clippy+rustfmt"
    - "A devcontainer CI job builds the container and runs cargo build && cargo test && cargo clippy inside it"
    - "CODE_OF_CONDUCT.md is present and its contact/scope wording is spot-checked as current"
  artifacts:
    - CONTRIBUTING.md
    - .devcontainer/devcontainer.json
    - .github/workflows/devcontainer.yml
  key_links:
    - "CONTRIBUTING required-checks note <-> .github/workflows/ci.yml three jobs (test/clippy/fmt)"
    - "devcontainer toolchain pin <-> rust-toolchain.toml (channel stable, components clippy+rustfmt) and ci.yml dtolnay/rust-toolchain pattern"
    - "CONTRIBUTING 'Adding a New Agent' anchor <-> ARCHITECTURE.md#extension-points--adding-an-agent (stays a pointer)"
  prohibitions:
    - "Pin the devcontainer base image to `:latest` or an unversioned tag — use an explicit, official mcr.microsoft.com/devcontainers/rust version tag"
    - "Build a `devflow devcontainer` subcommand or add a new npm dependency (@devcontainers/cli) — the OQ3 decision is to ship config only, no new CLI surface"
    - "Duplicate ARCHITECTURE.md's extension-points/adding-an-agent checklist into CONTRIBUTING.md — keep CONTRIBUTING's anchor-link pointer as-is"
    - "Under-cache the devcontainer by mounting only the registry cache — mount the workspace target/ as well, or explicitly document the rebuild-cost tradeoff"
---

<objective>
Refresh CONTRIBUTING.md for OSS contributors and add the greenfield `.devcontainer/` reproducible
dev environment with a real container test. CONTRIBUTING is already accurate (fork -> branch ->
test -> PR); it needs only a required-checks note (the "PR gate status" checklist item) and a
short devcontainer subsection paralleling the existing distrobox one. The devcontainer is 100%
new work — no repo analog exists — so it is patterned on ci.yml's toolchain-pinning convention.

Purpose: contributors need a one-command reproducible environment and a clear statement of which
CI checks gate a PR; the container test gives real CI-parity instead of prose.
Output: refreshed CONTRIBUTING.md, a working `.devcontainer/devcontainer.json`, and a
`.github/workflows/devcontainer.yml` job that actually builds and tests inside the container.
</objective>

<artifacts_produced>
## Artifacts this plan produces
NEW files:
- `.devcontainer/devcontainer.json` (new — base image, cargo/target cache mounts, postCreate, VS Code extensions)
- `.github/workflows/devcontainer.yml` (new CI workflow — builds the devcontainer and runs cargo build/test/clippy inside it)
MODIFIED files:
- `CONTRIBUTING.md` (required-checks note + devcontainer subsection)
- `CODE_OF_CONDUCT.md` (spot-check only; edit only if contact/scope wording is stale)
Explicitly NOT produced (OQ3 decision): no `devflow devcontainer` CLI subcommand, no
`.devcontainer/Dockerfile` unless the prebuilt image genuinely needs extension, no new npm dependency.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-oss-readiness/15-RESEARCH.md
@.planning/phases/15-oss-readiness/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CONTRIBUTING.md — required-checks note + devcontainer subsection + CODE_OF_CONDUCT spot-check</name>
  <files>CONTRIBUTING.md, CODE_OF_CONDUCT.md</files>
  <read_first>
    - CONTRIBUTING.md (current state — Distrobox subsection at line 14-22, PR Process at line 79-92, "Adding a New Agent" anchor at line 156-171)
    - CODE_OF_CONDUCT.md (spot-check contact email/enforcement scope)
    - .github/workflows/ci.yml (the three jobs: `test` -> `cargo test`, `clippy` -> `cargo clippy -- -D warnings`, `fmt` -> `cargo fmt --check`, at lines 14-40 — the required-checks list must mirror these verbatim)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (CONTRIBUTING.md section — where to add the note and the devcontainer subsection)
  </read_first>
  <action>
    In the PR Process section (line 79-92), add a note listing the three CI checks a PR must pass
    before merge, mirroring ci.yml's three jobs exactly: `cargo test`,
    `cargo clippy -- -D warnings`, and `cargo fmt --check`. This is the concrete resolution of the
    "PR gate status" checklist item (there is no separate native GitHub badge for merge gating).
    Add a short `.devcontainer` subsection modeled on the existing Distrobox subsection's tone and
    depth (line 14-22): tell contributors to open the repo in VS Code and "Reopen in Container"
    (or run `devcontainer up`) to get the pinned toolchain. Do NOT touch the "Adding a New Agent"
    section beyond, at most, its trailing docs-file list — it already correctly points at
    ARCHITECTURE.md#extension-points--adding-an-agent as authoritative; keep that pointer. Spot-check
    CODE_OF_CONDUCT.md; edit it ONLY if the contact/enforcement wording is stale (no product coupling).
  </action>
  <acceptance_criteria>
    - `rg -n 'cargo clippy' CONTRIBUTING.md` and `rg -n 'cargo fmt' CONTRIBUTING.md` and `rg -n 'cargo test' CONTRIBUTING.md` all hit in the PR Process area (required checks listed)
    - `rg -n 'devcontainer' CONTRIBUTING.md` returns at least one hit (subsection added)
    - `rg -n 'extension-points' CONTRIBUTING.md` still returns the single anchor pointer (not a duplicated checklist)
    - CODE_OF_CONDUCT.md present at repo root (`test -f CODE_OF_CONDUCT.md`)
  </acceptance_criteria>
  <verify>
    <automated>rg -q 'devcontainer' CONTRIBUTING.md && rg -q 'cargo clippy' CONTRIBUTING.md && rg -q 'cargo fmt' CONTRIBUTING.md && test -f CODE_OF_CONDUCT.md</automated>
  </verify>
  <done>CONTRIBUTING lists the three required checks and documents the devcontainer flow; the extension anchor is untouched; CODE_OF_CONDUCT verified present.</done>
</task>

<task type="auto">
  <name>Task 2: .devcontainer/devcontainer.json — pinned Rust dev environment with cargo+target cache</name>
  <files>.devcontainer/devcontainer.json</files>
  <read_first>
    - rust-toolchain.toml (channel = "stable", components = ["clippy", "rustfmt"] — the pin the container must match)
    - .github/workflows/ci.yml (dtolnay/rust-toolchain@stable + explicit `components:` — the pinning convention to mirror)
    - .planning/phases/15-oss-readiness/15-RESEARCH.md (Code Examples: devcontainer.json starting point; Pitfall 5: mount registry AND target; A1: verify base image tag at build time)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (.devcontainer section — cache-mount and postCreate guidance)
  </read_first>
  <action>
    Create `.devcontainer/devcontainer.json` based on the official Microsoft Rust devcontainer
    template. Set `image` to an explicit, currently-published `mcr.microsoft.com/devcontainers/rust`
    version tag (NOT `:latest` — verify the tag resolves at build time; the research's
    `1-1-bookworm` is illustrative and must be confirmed against the registry). Add two named-volume
    mounts — one for the cargo registry cache (`/usr/local/cargo/registry`) and one for the
    workspace `target/` (`${containerWorkspaceFolder}/target`) — so a 2-crate workspace does not
    recompile from scratch on every rebuild. Set `postCreateCommand` to `rustup component add clippy
    rustfmt && cargo build --workspace` so the container matches rust-toolchain.toml's components
    explicitly rather than relying on implicit base-image defaults. Add the VS Code extensions
    rust-analyzer, CodeLLDB, and Even Better TOML. Do NOT build a `devflow devcontainer` subcommand.
  </action>
  <acceptance_criteria>
    - `test -f .devcontainer/devcontainer.json`
    - `node -e "JSON.parse(require('fs').readFileSync('.devcontainer/devcontainer.json','utf8').replace(/\/\/.*$/gm,''))"` exits 0 (valid JSON after stripping line comments)
    - `rg -n 'mcr.microsoft.com/devcontainers/rust' .devcontainer/devcontainer.json` hits and the tag is NOT `:latest`
    - `rg -c ':latest' .devcontainer/devcontainer.json` returns 0
    - `rg -n 'target' .devcontainer/devcontainer.json` and `rg -n 'registry' .devcontainer/devcontainer.json` both hit (both caches mounted)
    - `rg -n 'rustup component add clippy rustfmt' .devcontainer/devcontainer.json` hits (postCreate installs matching components)
  </acceptance_criteria>
  <verify>
    <automated>test -f .devcontainer/devcontainer.json && test "$(rg -c ':latest' .devcontainer/devcontainer.json)" = "0" && rg -q 'mcr.microsoft.com/devcontainers/rust' .devcontainer/devcontainer.json && rg -q 'rustup component add clippy rustfmt' .devcontainer/devcontainer.json</automated>
  </verify>
  <done>devcontainer.json is valid, pins an explicit official Rust image, mounts both caches, and installs clippy+rustfmt on create.</done>
</task>

<task type="auto">
  <name>Task 3: .github/workflows/devcontainer.yml — container-parity CI test</name>
  <files>.github/workflows/devcontainer.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the existing job shape: on-push/pull_request triggers, concurrency group, `actions/checkout@v4` — the structure to reuse)
    - .devcontainer/devcontainer.json (the image/config the workflow builds; produced in Task 2)
    - .planning/phases/15-oss-readiness/15-RESEARCH.md (Wave 0 Gaps + Validation Architecture: the recommended `.github/workflows/devcontainer.yml` job running cargo build/test/clippy inside the built container)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (CI job shape for a new devcontainer test workflow)
  </read_first>
  <action>
    Create `.github/workflows/devcontainer.yml` that builds the devcontainer and runs the CI-parity
    check inside it. Trigger on push and pull_request like ci.yml. Use `actions/checkout@v4`, then
    build/run the devcontainer via the `devcontainers/ci` action (or `devcontainer build` +
    `devcontainer exec`) with a run command of `cargo build --workspace && cargo test --workspace &&
    cargo clippy --workspace -- -D warnings`. Pin the devcontainer action to an explicit major
    version (not a floating ref). This must actually EXECUTE the toolchain inside the container, not
    merely lint the JSON — the point is real CI-parity proof.
  </action>
  <acceptance_criteria>
    - `test -f .github/workflows/devcontainer.yml`
    - `rg -n 'devcontainer' .github/workflows/devcontainer.yml` hits (builds/runs the container)
    - `rg -n 'cargo build' .github/workflows/devcontainer.yml` and `rg -n 'cargo test' .github/workflows/devcontainer.yml` and `rg -n 'cargo clippy' .github/workflows/devcontainer.yml` all hit (real in-container checks)
    - `rg -n 'actions/checkout@v4' .github/workflows/devcontainer.yml` hits (mirrors ci.yml structure)
    - Workflow is valid YAML (`node -e "require('js-yaml')" ` not required; use a YAML parse check if available, otherwise the file parses under the repo's existing yaml tooling)
  </acceptance_criteria>
  <verify>
    <automated>test -f .github/workflows/devcontainer.yml && rg -q 'cargo build' .github/workflows/devcontainer.yml && rg -q 'cargo test' .github/workflows/devcontainer.yml && rg -q 'cargo clippy' .github/workflows/devcontainer.yml && rg -q 'devcontainer' .github/workflows/devcontainer.yml</automated>
  </verify>
  <done>A devcontainer CI workflow builds the container and runs the full cargo build/test/clippy parity check inside it.</done>
</task>

</tasks>

<flagged_assumptions>
Surfaced (not silently resolved). None auto-resolve.
- **15b-3 (CONTRIBUTING) and 15b-4 (devcontainer) — spec-less probe returned `unclassified`/`unresolved`.** No numeric/collection/stateful edge case; the devcontainer's correctness is proven by the CI-parity job actually running cargo build/test/clippy in-container, and CONTRIBUTING by the required-checks grep assertions. Flagged for reviewer awareness.
- **RESEARCH Assumption A1 (base image tag):** `mcr.microsoft.com/devcontainers/rust:1-1-bookworm` is illustrative and MEDIUM confidence — the executor MUST verify the chosen tag resolves against the registry at build time and pick a current published tag; the tag must not be `:latest`.
- **RESEARCH Assumption A2 / Open Question 2 ("PR gate status" meaning):** interpreted as "document the required CI checks in CONTRIBUTING" (recommendation b) rather than a new third-party badge; the existing CI badge already satisfies the badge itself. If the operator meant a coverage/external-gate badge, that is out of scope (would require new CI tooling) — surfaced, not decided silently.
- **RESEARCH Open Question 3 (`devflow devcontainer` subcommand):** decided NO — config-only ship; building the subcommand would add a new npm dependency and a `--help` snapshot maintenance burden. Recorded as a locked decision, not an omission.
- **RESEARCH Environment Availability (Docker):** if the execution sandbox lacks Docker, the devcontainer cannot be built locally — the `.github/workflows/devcontainer.yml` job IS the real test surface (runs on GitHub's runners). Local `devcontainer build` is a convenience, not the acceptance gate.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| upstream container registry -> devcontainer build | The base image is fetched from mcr.microsoft.com; an unpinned tag lets its contents change under the project |
| external GitHub Action -> CI runner | The devcontainer-build action executes in CI with repo checkout |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03-SC | Tampering | .devcontainer base image (supply chain, ASVS V14) | high | mitigate | Pin `mcr.microsoft.com/devcontainers/rust` to an explicit official version tag, never `:latest`; the `:latest`-absent grep gate enforces this; revisit the pin deliberately, not automatically |
| T-15-03b | Tampering | devcontainers CI action reference | medium | mitigate | Pin the devcontainer build action to an explicit major version rather than a floating ref |
| T-15-03c | Elevation of Privilege | postCreateCommand contents | low | accept | postCreate only installs rustfmt/clippy and builds the workspace — no privileged or network-fetching custom scripts introduced |
</threat_model>

<verification>
- `.devcontainer/devcontainer.json` parses as JSON, pins an explicit non-:latest official image, mounts both caches, installs clippy+rustfmt
- `.github/workflows/devcontainer.yml` runs cargo build/test/clippy inside the container
- CONTRIBUTING lists the three required checks and documents the devcontainer flow; extension anchor untouched
- `cargo test -p devflow-cli --test help_snapshot` green (no CLI source touched)
</verification>

<success_criteria>
A contributor can open the repo in a pinned, cache-warmed Rust devcontainer; CONTRIBUTING states
the three required PR checks and documents the container flow; a CI job proves the container
builds and passes cargo build/test/clippy. No `devflow devcontainer` subcommand or new npm
dependency was added.
</success_criteria>

<output>
Create `.planning/phases/15-oss-readiness/15-03-SUMMARY.md` when done
</output>
