---
phase: 15-oss-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - SECURITY.md
  - DEPENDENCIES.md
autonomous: true
requirements: [15b]

must_haves:
  truths:
    - "README command table documents the `gate` and `logs` subcommands (both shipped in 15a)"
    - "README references per-phase `.devflow/state-NN.json`, not a single `.devflow/state.json`"
    - "README Documentation section links OPERATIONS.md"
    - "SECURITY.md points responders at real state files (state-NN.json, events.jsonl), not a nonexistent audit log"
    - "DEPENDENCIES.md doctor sample shows version 1.2.0 and no removed config file"
  artifacts:
    - README.md
    - SECURITY.md
    - DEPENDENCIES.md
  key_links:
    - "README/DEPENDENCIES command claims <-> crates/devflow-cli/src/main.rs `enum Command` + `enum GateCmd`"
    - "state-file naming in README/SECURITY <-> crates/devflow-core/src/workflow.rs + state.rs"
    - "`--help` snapshot guard (crates/devflow-cli/tests/help_snapshot.rs) proves CLI surface matches docs"
  prohibitions:
    - "Reconstruct any command, flag, file path, or version claim from memory of what DevFlow used to do instead of tracing it to a named source file"
    - "Leave a phantom file reference (a nonexistent config file, audit log, or removed command) in any doc touched by this plan"
    - "Modify or delete the crates/devflow-cli/tests/help_snapshot.rs guard or its committed snapshot to make docs pass"
    - "Edit CLI or core source code — this plan is docs-only; source files are read-only ground truth"
---

<!-- planner-discipline-allow: audit.log -->
<!-- planner-discipline-allow: .devflow.yaml -->
<!-- planner-discipline-allow: state.json -->

<objective>
Bring the three "small accuracy pass" root docs into agreement with the real v2 CLI and
architecture: README.md (add the two 15a commands, correct the state-file model, link the
operator reference), SECURITY.md (drop the phantom audit-log pointer), and DEPENDENCIES.md
(fix the stale `doctor` sample). Every factual claim is verified against a named source file
before it is written — this is a docs-as-source-of-truth pass, not prose cleanup.

Purpose: crates.io renders README on the package page and contributors read these root docs
first; stale claims here undermine the whole OSS-readiness goal and misdirect incident
responders.
Output: three corrected root docs, all consistent with `main.rs`, `workflow.rs`, `state.rs`,
and the shipped OPERATIONS.md.
</objective>

<artifacts_produced>
## Artifacts this plan produces
This plan creates NO new files and NO new symbols. It edits three existing files in place:
- `README.md` (modified)
- `SECURITY.md` (modified)
- `DEPENDENCIES.md` (modified)
No new CLI flags, subcommands, or test files are added.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-oss-readiness/15-RESEARCH.md
@.planning/phases/15-oss-readiness/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: README accuracy pass — gate/logs commands, per-phase state, OPERATIONS.md link</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state — command table, prose at line 50, Configuration section at line 120-122, Documentation section at line 176)
    - crates/devflow-cli/src/main.rs (`enum Command` at line 61 and `enum GateCmd` at line 226 — the authoritative command surface; confirm `Gate` and `Logs` variants exist and their subcommands)
    - crates/devflow-core/src/workflow.rs and crates/devflow-core/src/state.rs (per-phase state-file naming — confirm the `state-NN.json` form)
    - OPERATIONS.md (the operator reference to link; confirm it exists at repo root)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (README section — exact current excerpts to diff against)
  </read_first>
  <action>
    Add command-table rows for `gate` (with its list/approve/reject subcommands) and `logs`,
    matching the descriptions to the `Gate` and `Logs` variants in main.rs `enum Command` and
    the `enum GateCmd` variants (List, Approve, Reject). Replace both occurrences of the single
    `.devflow/state.json` claim (the prose near line 50 and the Configuration section near line
    122) with the per-phase `.devflow/state-NN.json` model that shipped in Phase 14a — verify the
    exact file-name shape against workflow.rs/state.rs before writing. Add an OPERATIONS.md entry
    to the Documentation list (near line 176) describing it as the operator reference (gate
    protocol, env vars, `.devflow/` file inventory). Do NOT touch the License badge/section or the
    CI badge — both are already correct. Do NOT rewrite accurate sections; this is a targeted diff.
  </action>
  <acceptance_criteria>
    - `rg -n 'gate' README.md` shows a command-table entry for the gate subcommand
    - `rg -n 'logs' README.md` shows a command-table entry for the logs subcommand
    - `rg -n 'state-' README.md` matches the per-phase `state-NN.json` naming (at least one hit)
    - `rg -n 'OPERATIONS\.md' README.md` returns at least one hit in the Documentation section
    - `rg -c 'state\.json' README.md` returns 0 (the bare single-state-file claim is gone; `state-NN.json` does not match this pattern)
    - `cargo test -p devflow-cli --test help_snapshot` exits 0 (CLI surface unchanged — docs now match the real `--help`)
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p devflow-cli --test help_snapshot && rg -q 'OPERATIONS\.md' README.md && rg -q 'state-' README.md && test "$(rg -c 'state\.json' README.md)" = "0"</automated>
  </verify>
  <done>README lists gate + logs, describes per-phase state-NN.json, links OPERATIONS.md, and the help-snapshot guard is green.</done>
</task>

<task type="auto">
  <name>Task 2: SECURITY.md — replace phantom audit-log pointer, align supported versions</name>
  <files>SECURITY.md</files>
  <read_first>
    - SECURITY.md (current state — Supported Versions table at line 17-20, Best Practices bullet at line 37)
    - crates/devflow-core/src/events.rs and crates/devflow-core/src/workflow.rs (confirm the real state/event file names: `state-NN.json`, `events.jsonl`)
    - Cargo.toml (line 9 `version = "1.2.0"` — the real current version for the supported-versions wording)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (SECURITY.md section — exact line-37 excerpt to fix)
  </read_first>
  <action>
    In the Best Practices bullet near line 37, remove the reference to the nonexistent
    `.devflow/audit.log` file (verified by RESEARCH.md to have zero source hits) and replace it
    with the real per-phase state file and the events log — `.devflow/state-NN.json` and
    `.devflow/events.jsonl` — as the sensitive artifacts not to expose to untrusted contexts.
    Cross-check the Supported Versions table (line 17-20) against Cargo.toml's `1.2.0`; if the
    `v1.0.0+` wording is now stale relative to the shipped version, align it so it does not
    understate the current release line. Do not otherwise restructure the file.
  </action>
  <acceptance_criteria>
    - `rg -c 'audit\.log' SECURITY.md` returns 0
    - `rg -n 'events\.jsonl' SECURITY.md` returns at least one hit
    - `rg -n 'state-' SECURITY.md` matches the per-phase state naming
    - Supported Versions wording is consistent with Cargo.toml `1.2.0` (no version below the real current line marked unsupported in a way that contradicts Cargo.toml)
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c 'audit\.log' SECURITY.md)" = "0" && rg -q 'events\.jsonl' SECURITY.md && rg -q 'state-' SECURITY.md</automated>
  </verify>
  <done>SECURITY.md references only real state/event files and its version table matches Cargo.toml.</done>
</task>

<task type="auto">
  <name>Task 3: DEPENDENCIES.md — fix stale doctor sample and confirm command reference</name>
  <files>DEPENDENCIES.md</files>
  <read_first>
    - DEPENDENCIES.md (current state — "Required for Shipping" header at line 15-17, doctor sample at line 44-57)
    - crates/devflow-cli/src/main.rs (`enum Command` at line 61 — confirm there is no `Confirm` variant; Ship is gate-driven)
    - Cargo.toml (line 9 `version = "1.2.0"`)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (DEPENDENCIES.md section — exact stale lines 46-57 and 15-17)
  </read_first>
  <action>
    In the `doctor` output sample (line 44-57), bump the shown `devflow v1.0.0` to the real
    `1.2.0` from Cargo.toml, and remove the line that claims a `.devflow.yaml` config file was
    found — that config file no longer exists (decoy removed in 15a; v2 has no config file).
    In the "Required for Shipping" section (line 15-17), drop the `devflow confirm` reference:
    `confirm` is not a `Command` variant in main.rs; Ship is driven through the gate protocol
    (`devflow gate approve <phase> --stage ship`). Keep the rest of the dependency matrix intact.
  </action>
  <acceptance_criteria>
    - `rg -c '\.devflow\.yaml' DEPENDENCIES.md` returns 0
    - `rg -c 'devflow confirm' DEPENDENCIES.md` returns 0
    - `rg -n '1\.2\.0' DEPENDENCIES.md` returns at least one hit (doctor sample version bumped)
    - `rg -c 'v1\.0\.0' DEPENDENCIES.md` returns 0 in the doctor sample block
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '\.devflow\.yaml' DEPENDENCIES.md)" = "0" && test "$(rg -c 'devflow confirm' DEPENDENCIES.md)" = "0" && rg -q '1\.2\.0' DEPENDENCIES.md</automated>
  </verify>
  <done>DEPENDENCIES.md doctor sample matches the real version and file model, and no phantom `confirm` command remains.</done>
</task>

</tasks>

<flagged_assumptions>
These are surfaced (not silently resolved) for the executor/operator. None auto-resolve.
- **15b-1 (README rewrite) — spec-less probe returned `unclassified`/`unresolved`.** No numeric/collection/stateful edge case exists for a doc-accuracy pass; correctness is proven by the `--help` snapshot guard plus the source-traced grep assertions above, not by an edge-coverage test. Flagged for reviewer awareness.
- **15b-5 (SECURITY refresh) — spec-less probe returned `unclassified`/`unresolved`.** Same shape as above; the audit-log fix is a source-verified string removal, not an edge case.
- **RESEARCH Assumption A4 (crates.io name availability):** does not affect this plan (docs only) but is the load-bearing risk for Plan 15-05.
- **RESEARCH Open Question 1 (is DEPENDENCIES.md in scope?):** RESEARCH recommends folding it in because README links it; this plan includes it. If the operator intended DEPENDENCIES.md to be left untouched, revert Task 3 — surfaced, not assumed silently.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc reader / incident responder -> repo docs | A responder trusts SECURITY.md to name the real sensitive artifacts during an incident |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | SECURITY.md Best-Practices bullet | medium | mitigate | Task 2 replaces the nonexistent audit-log pointer with the real `state-NN.json` + `events.jsonl` files so incident response is not misdirected to a phantom evidence file |
| T-15-01b | Tampering | README/DEPENDENCIES command claims | low | mitigate | Every command/flag/version claim is traced to `main.rs`/`Cargo.toml`; the `--help` snapshot guard blocks silent CLI-vs-docs drift |
</threat_model>

<verification>
- `cargo test -p devflow-cli --test help_snapshot` green (CLI surface matches docs)
- All three files' negative/positive grep assertions above pass
- Manual read-through of each edited section against its named source file
</verification>

<success_criteria>
README documents gate + logs and per-phase state and links OPERATIONS.md; SECURITY.md names
only real state/event files; DEPENDENCIES.md doctor sample matches the real version and file
model. No CLI source changed. Help-snapshot guard green.
</success_criteria>

<output>
Create `.planning/phases/15-oss-readiness/15-01-SUMMARY.md` when done
</output>
