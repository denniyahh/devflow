---
phase: 15-oss-readiness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ARCHITECTURE.md
  - docs/guides/quickstart.md
  - docs/guides/configuration.md
autonomous: true
requirements: [15b]

must_haves:
  truths:
    - "ARCHITECTURE.md documents the real 5-stage Stage enum (Define -> Plan -> Code -> Validate -> Ship), not the dead 8-step machine"
    - "ARCHITECTURE.md describes hooks, per-phase state + two-level locking, events.jsonl, gate protocol, and monitor ownership from source"
    - "ARCHITECTURE.md's Configuration section documents the no-config-file model (CLI flags only), not `.devflow.yaml`/`devflow init`"
    - "ARCHITECTURE.md's Git/ship model is gate-driven (`devflow gate approve --stage ship`), with no `confirm`/`rejectpr` commands"
    - "docs/guides/quickstart.md and configuration.md describe the real v2 entry flow and no-config-file reality"
  artifacts:
    - ARCHITECTURE.md
    - docs/guides/quickstart.md
    - docs/guides/configuration.md
  key_links:
    - "ARCHITECTURE state machine <-> crates/devflow-core/src/stage.rs `enum Stage`"
    - "ARCHITECTURE subsystems <-> hooks.rs, workflow.rs, lock.rs, events.rs, gates.rs, monitor.rs"
    - "ARCHITECTURE extension-points checklist <-> CONTRIBUTING.md anchor pointer (must stay a pointer, not a duplicate)"
  prohibitions:
    - "Rewrite ARCHITECTURE.md by expanding README's compressed summary instead of deriving each fact from source files"
    - "Reconstruct the Stage machine, hooks, locking, or gate model from memory of what DevFlow used to do instead of tracing it to stage.rs/hooks.rs/lock.rs/events.rs/gates.rs/workflow.rs/monitor.rs"
    - "Duplicate the extension-points/adding-an-agent checklist into CONTRIBUTING.md — CONTRIBUTING keeps its anchor-link pointer, only ARCHITECTURE holds the authoritative copy"
    - "Leave a phantom reference (`.devflow.yaml`, `devflow init`, `confirm`, `rejectpr`, or the dead 8-step machine) in any doc touched by this plan"
    - "Edit CLI or core source code — this plan is docs-only; source files are read-only ground truth"
---

<!-- planner-discipline-allow: .devflow.yaml -->
<!-- planner-discipline-allow: devflow init -->
<!-- planner-discipline-allow: confirm -->
<!-- planner-discipline-allow: rejectpr -->

<objective>
Rewrite ARCHITECTURE.md against current source so it describes the real DevFlow v2 pipeline,
and bring the two `docs/guides/*.md` files into agreement with the no-config-file reality.
ARCHITECTURE.md is the heaviest, most-stale doc in the phase: it documents a dead 8-step state
machine, a removed `.devflow.yaml`/`devflow init` config model, and `confirm`/`rejectpr`
commands that no longer exist. The rewrite reuses the existing section skeleton but re-derives
every factual claim from the named source file.

Purpose: ARCHITECTURE.md is the authoritative design doc contributors and adapter authors read;
if it lies about the state machine or the extension model, every downstream contribution starts
from a false map.
Output: an accurate ARCHITECTURE.md plus two corrected guide files.
</objective>

<artifacts_produced>
## Artifacts this plan produces
This plan creates NO new files and NO new symbols. It edits three existing files in place:
- `ARCHITECTURE.md` (modified — full rewrite of stale sections, skeleton retained)
- `docs/guides/quickstart.md` (modified)
- `docs/guides/configuration.md` (modified — rewritten to reflect the no-config-file model)
No new CLI flags, subcommands, or test files are added.
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-oss-readiness/15-RESEARCH.md
@.planning/phases/15-oss-readiness/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARCHITECTURE.md full rewrite against source</name>
  <files>ARCHITECTURE.md</files>
  <read_first>
    - ARCHITECTURE.md (current state — state machine at line 20-38, Git/ship model at line 108-125, Configuration at line 127-158, extension points at line 216-231; note the accurate sections to preserve: Crates table 8-18, Agent model 40-56, Completion evaluation 58-73, Logging 160-215)
    - crates/devflow-core/src/stage.rs (`enum Stage` at line 16-26 and the `next()`/`is_gate_stage()`/`is_agent_stage()` methods — the real 5-stage machine and its slash-command mapping)
    - crates/devflow-core/src/hooks.rs (BranchCreate/DocsUpdate/ChangelogAppend/VersionBump/BranchCleanup and the transitions they map to)
    - crates/devflow-core/src/lock.rs (two-level locking: per-phase advance lock + project checkout lock)
    - crates/devflow-core/src/events.rs (`events.jsonl`, schema v1)
    - crates/devflow-core/src/gates.rs (`run_gate()`, `.devflow/gates/NN-<stage>.json`, `poll_response()`, `DEVFLOW_GATE_NOTIFY_CMD`)
    - crates/devflow-core/src/workflow.rs and state.rs (per-phase `state-NN.json`)
    - crates/devflow-core/src/monitor.rs (`spawn_monitor()` detached child owning the agent process)
    - crates/devflow-cli/src/main.rs (`enum Command` line 61, `enum GateCmd` line 226 — confirm no `Confirm`/`RejectPr` variant)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md (ARCHITECTURE.md section — section-by-section stale-vs-source map)
    - .planning/phases/15-oss-readiness/15-RESEARCH.md (System Architecture Diagram — the verified data-flow the rewrite must convey)
  </read_first>
  <action>
    Replace the dead 8-step state machine (Idle/Branching/Planning/Executing/Verifying/Docsing/
    Shipping/Cleaning) with the real 5-stage `Stage` enum from stage.rs: Define -> Plan -> Code
    -> Validate -> Ship, including each stage's GSD slash-command mapping and which stages are
    agent stages vs gate stages. Add coverage of the subsystems the current doc omits, each traced
    to its source file: hooks (hooks.rs, the five hook kinds and their mapped transitions),
    per-phase state + two-level locking (workflow.rs/state.rs/lock.rs), the events log
    (events.rs, `events.jsonl` schema v1), the gate protocol (gates.rs — `run_gate()`,
    `.devflow/gates/NN-<stage>.json`, `poll_response()`, `DEVFLOW_GATE_NOTIFY_CMD`), and monitor
    ownership (monitor.rs — detached child owns the agent process). Replace the Configuration
    section's removed `.devflow.yaml`/`devflow init` schema with the no-config-file model (all
    options are `devflow start` CLI flags), cross-referencing per-phase `state-NN.json`. Replace
    the Git/ship model's `confirm`/`rejectpr` commands with the gate-driven Ship model
    (`devflow gate approve <phase> --stage ship`). Keep the extension-points/adding-an-agent
    checklist authoritative HERE and only update its trailing docs-file list; do NOT copy it into
    CONTRIBUTING.md. Preserve the accurate sections (Crates table, Agent model, Completion
    evaluation, Logging) verbatim in substance. Use the RESEARCH.md System Architecture Diagram as
    the reference data-flow.
  </action>
  <acceptance_criteria>
    - `rg -n 'Define' ARCHITECTURE.md` and `rg -n 'Validate' ARCHITECTURE.md` both hit (real Stage enum present)
    - `rg -c 'Branching|Executing|Docsing|Shipping' ARCHITECTURE.md` returns 0 (dead 8-step machine gone)
    - `rg -n 'events\.jsonl' ARCHITECTURE.md` returns at least one hit
    - `rg -n 'gate' ARCHITECTURE.md` describes the gate protocol / gate-approve Ship model
    - `rg -c '\.devflow\.yaml' ARCHITECTURE.md` returns 0 and `rg -c 'devflow init' ARCHITECTURE.md` returns 0
    - `rg -c 'rejectpr' ARCHITECTURE.md` returns 0 and the Git/ship section no longer lists a `confirm` command
    - `rg -n 'state-' ARCHITECTURE.md` matches per-phase state naming
    - CONTRIBUTING.md's "Adding a New Agent" section is unchanged by this task (still an anchor pointer, not a duplicated checklist)
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c 'Branching|Executing|Docsing' ARCHITECTURE.md)" = "0" && test "$(rg -c '\.devflow\.yaml' ARCHITECTURE.md)" = "0" && test "$(rg -c 'rejectpr' ARCHITECTURE.md)" = "0" && rg -q 'events\.jsonl' ARCHITECTURE.md && rg -q 'Define' ARCHITECTURE.md</automated>
  </verify>
  <done>ARCHITECTURE.md matches the real Stage machine, subsystems, config model, and Ship model, each traced to source; extension checklist stays authoritative and un-duplicated.</done>
</task>

<task type="auto">
  <name>Task 2: docs/guides/quickstart.md accuracy pass</name>
  <files>docs/guides/quickstart.md</files>
  <read_first>
    - docs/guides/quickstart.md (current state — `devflow init` at line 19, `.devflow/state.json` + `.devflow.yaml` claim at line 22, real `devflow start` invocation at line 27)
    - crates/devflow-cli/src/main.rs (`enum Command` — confirm there is no `init` variant; `start` requires `--phase`, `--agent`, `--mode`)
    - OPERATIONS.md (the worked `devflow start --phase N --agent claude --mode auto .` example to mirror)
    - .planning/phases/15-oss-readiness/15-PATTERNS.md and 15-RESEARCH.md (State of the Art table — removed `devflow init`/`.devflow.yaml`)
  </read_first>
  <action>
    Remove the `devflow init` step (no such command exists) and the claim that it creates a
    `.devflow.yaml` config file. Correct the state-file reference from a single
    `.devflow/state.json` to per-phase `.devflow/state-NN.json`. Ensure the quickstart's primary
    entry point is the real `devflow start --phase N --agent claude --mode auto` invocation,
    consistent with OPERATIONS.md's worked example. Keep the guide's structure and tone; only
    correct the factual steps.
  </action>
  <acceptance_criteria>
    - `rg -c 'devflow init' docs/guides/quickstart.md` returns 0
    - `rg -c '\.devflow\.yaml' docs/guides/quickstart.md` returns 0
    - `rg -n 'devflow start --phase' docs/guides/quickstart.md` returns at least one hit
    - `rg -c 'state\.json' docs/guides/quickstart.md` returns 0 (single-state-file claim gone; `state-NN.json` does not match)
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c 'devflow init' docs/guides/quickstart.md)" = "0" && test "$(rg -c '\.devflow\.yaml' docs/guides/quickstart.md)" = "0" && rg -q 'devflow start --phase' docs/guides/quickstart.md</automated>
  </verify>
  <done>quickstart.md describes the real init-less entry flow and per-phase state.</done>
</task>

<task type="auto">
  <name>Task 3: docs/guides/configuration.md accuracy pass — no-config-file reality</name>
  <files>docs/guides/configuration.md</files>
  <read_first>
    - docs/guides/configuration.md (current state — the whole file documents `.devflow.yaml` at line 3 and a merged-config model at line 73)
    - crates/devflow-cli/src/main.rs (`enum Command` `Start` variant flags — `--phase`, `--agent`, `--mode`, `--no-worktree` — the real "configuration" surface)
    - crates/devflow-core/src/config.rs if present (confirm the v2.0.0 "no `.devflow.yaml`" stance)
    - OPERATIONS.md (env-var table: DEVFLOW_GATE_NOTIFY_CMD, DEVFLOW_GATE_TIMEOUT_SECS, DEVFLOW_CHECKOUT_LOCK_TIMEOUT_SECS, RUST_LOG, DEVFLOW_LOG_FORMAT)
    - .planning/phases/15-oss-readiness/15-RESEARCH.md (State of the Art — config-file model removed in v2.0.0)
  </read_first>
  <action>
    This guide currently documents a `.devflow.yaml` config file that no longer exists in v2.
    Rewrite it in place to reflect the real model: DevFlow has no config file; all workflow
    options are `devflow start` CLI flags (`--phase`, `--agent`, `--mode auto|supervise`,
    `--no-worktree`), and runtime behavior is tuned via the environment variables documented in
    OPERATIONS.md (list them: DEVFLOW_GATE_NOTIFY_CMD, DEVFLOW_GATE_TIMEOUT_SECS,
    DEVFLOW_CHECKOUT_LOCK_TIMEOUT_SECS, RUST_LOG, DEVFLOW_LOG_FORMAT). Point readers to
    OPERATIONS.md as the authoritative operator reference. Do NOT invent a new config mechanism —
    describe only what exists. (This file is flagged discretionary in RESEARCH Open Question 1;
    see flagged assumptions — default is rewrite-in-place, not deletion.)
  </action>
  <acceptance_criteria>
    - `rg -c '\.devflow\.yaml' docs/guides/configuration.md` returns 0
    - `rg -n '--mode' docs/guides/configuration.md` documents the real `auto|supervise` flag
    - `rg -n 'DEVFLOW_GATE_NOTIFY_CMD' docs/guides/configuration.md` returns at least one hit (env-var tuning documented)
    - `rg -n 'OPERATIONS\.md' docs/guides/configuration.md` returns at least one hit
  </acceptance_criteria>
  <verify>
    <automated>test "$(rg -c '\.devflow\.yaml' docs/guides/configuration.md)" = "0" && rg -q 'DEVFLOW_GATE_NOTIFY_CMD' docs/guides/configuration.md && rg -q 'OPERATIONS\.md' docs/guides/configuration.md</automated>
  </verify>
  <done>configuration.md describes the real no-config-file / CLI-flags + env-vars model and links OPERATIONS.md.</done>
</task>

</tasks>

<flagged_assumptions>
Surfaced (not silently resolved). None auto-resolve.
- **15b-2 (ARCHITECTURE rewrite) — spec-less probe returned `unclassified`/`unresolved`.** A doc rewrite has no numeric/collection/stateful edge case; correctness is proven by source-traced grep assertions and a section-by-section read against named source files, not by an edge-coverage test. Flagged for reviewer awareness.
- **RESEARCH Open Question 1 (docs/guides scope):** RESEARCH flags quickstart.md and configuration.md as discretionary (CONTEXT.md's checklist does not name them) but recommends folding them in because README links them and they carry the same stale `.devflow.yaml`/`devflow init` content as old ARCHITECTURE.md. This plan folds them in via rewrite-in-place. If the operator prefers to DELETE these guides instead of fixing them, drop Tasks 2-3 and remove the README links — surfaced, not decided silently.
- **RESEARCH Open Question 1 (Hermes SKILL.md):** `skills/hermes/devflow/SKILL.md` is deliberately OUT of scope here (Phase 16 owns the Hermes surface). Not touched by this plan.
</flagged_assumptions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contributor / adapter author -> ARCHITECTURE.md | Contributors trust the design doc as the authoritative map of the state machine and extension model |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02 | Tampering | ARCHITECTURE.md / guides factual claims | low | mitigate | Every architectural claim is re-derived from a named source file (stage.rs/hooks.rs/lock.rs/events.rs/gates.rs/workflow.rs/monitor.rs); grep gates block the dead 8-step machine and phantom `.devflow.yaml`/`confirm`/`rejectpr` from surviving |
| T-15-02b | Information Disclosure | configuration.md env-var docs | low | accept | Documenting real env-var names (no secrets) is expected operator guidance; no credential values are written into docs |
</threat_model>

<verification>
- All grep gates above pass (dead machine / phantom config / removed commands are gone; real subsystems present)
- `cargo test -p devflow-cli --test help_snapshot` green (no CLI source touched)
- Section-by-section read of ARCHITECTURE.md against each named source file
</verification>

<success_criteria>
ARCHITECTURE.md accurately describes the 5-stage machine, hooks, locking, events, gates, and
monitor from source; the extension checklist stays authoritative and un-duplicated; both guides
reflect the init-less, no-config-file v2 reality and link OPERATIONS.md. No CLI source changed.
</success_criteria>

<output>
Create `.planning/phases/15-oss-readiness/15-02-SUMMARY.md` when done
</output>
