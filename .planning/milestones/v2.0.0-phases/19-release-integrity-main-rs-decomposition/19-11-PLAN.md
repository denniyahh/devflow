---
phase: 19-release-integrity-main-rs-decomposition
plan: 11
type: execute
wave: 6
depends_on: ["19-01", "19-02", "19-03", "19-05", "19-09", "19-10"]
files_modified: []
autonomous: false
requirements: [19c, 19d, 19e, 19f]

must_haves:
  truths:
    - "All three parts of the equivalence proof hold ON A CI RUN AGAINST THE BRANCH: symbol-level per-function diff, test name-set identity, and per-target pass-count identity. D-11 makes local-green explicitly insufficient."
    - "CI-vs-LOCAL command distinction, recorded not conflated (Codex review): `.github/workflows/ci.yml:20` runs the literal `cargo test` (NOT `cargo test --workspace` and NOT `--all-targets`), while LOCAL verification in this and prior plans runs `cargo test --workspace`. The plan records both as what they are and does not present today's CI `cargo test` as evidence of a `cargo test --workspace` run. (In this virtual workspace with no `default-members`, `cargo test` does compile and run every member's tests, so member-coverage is equivalent; but the strings differ and CI passes neither `--workspace` nor `--all-targets` for the test job — that residual gap is recorded, not silently treated as satisfied.)"
    - "CONCURRENCY, adjudicated not assumed: the `ENV_MUTEX` question has an explicit recorded disposition — either 'preserved, no finding' backed by repeated green CI runs, or a written finding under D-12."
    - "The downstream-repo threat that motivated 19a is confirmed closed by a real scratch repository, not only by the automated test."
    - "Every requirement 19a, 19b, 19c–19f, 19g has a recorded outcome, including any that landed partially. `depends_on` for this final gate lists `19-01, 19-02, 19-03, 19-05, 19-09, 19-10` so the metadata matches the roll-call it performs (Codex review — the prior `[19-01, 19-03, 19-09]` omitted 19-02's 19a-WR02, 19-05's 19g dogfood, and 19-10's doc reconciliation). Note: `depends_on` is descriptive metadata; wave-gating already enforces execution order regardless."
  artifacts:
    - ".planning/phases/19-release-integrity-main-rs-decomposition/19-11-SUMMARY.md — the phase verification record, including CI run URLs, the three-part proof results, and the ENV_MUTEX disposition"
  key_links:
    - "`.github/workflows/ci.yml` triggers on `on: push` with no branch filter — pushing the phase branch automatically runs three jobs: `test` (literal `cargo test`, ci.yml:20), `clippy` (`cargo clippy --workspace --all-targets -- -D warnings`, ci.yml:31), and `fmt` (`cargo fmt --check`, ci.yml:42). No new CI config is added by this plan. The `test` job's `cargo test` is NOT the `cargo test --workspace` local verification uses — that difference is recorded, not conflated (Codex review)."
    - "`19-SPLIT-BASELINE.md` + the committed, git-tracked `19-SPLIT-BASELINE-names.txt`/`-names-full.txt` (plan 19-06) → the recorded pre-split SHA, durable name lists, and per-target pass counts every comparison here is made against; the name-set diff reads the committed `.txt`, not an ephemeral `/tmp` file (Codex + OpenCode review)"
    - "Each `19-0N-SUMMARY.md`'s recorded per-function diff results → aggregated here into one auditable claim"
  prohibitions:
    - "MUST NOT accept local-green as the phase gate. D-11 is explicit and is grounded in evidence: failure 19i hit 2/2 in CI after passing locally most of the time, because CI's shared runners widen race windows relative to this workstation."
    - "MUST NOT re-run CI until it passes and report only the passing run. Every run's result is recorded, and an intermittent failure is a finding, not noise."
    - "MUST NOT dismiss a red run as flaky without identifying the specific test, the specific env var, and the specific lock site. That identification IS the D-12 finding."
    - "MUST NOT declare the phase complete with an unresolved `ENV_MUTEX` instability. A surfaced finding with a partial split is an acceptable outcome; an unrecorded one is not."
    - "MUST NOT modify any source file to make a check pass. This plan verifies; it does not fix. A failure routes back to the owning plan."
---

<objective>
Run the phase gate.

`19-VALIDATION.md` records why a passing suite is not sufficient evidence for
this phase: the split's correctness claim is *absence of behavioral change*, and
a pure move that silently drops a test also passes. Three independent checks are
required — symbol-level diff, test name-set identity, and pass-count identity —
and per D-11 all three must hold **on a CI run against the branch**.

This is also where the phase's principal risk gets its verdict. D-12 makes a
partially-completed split with the `ENV_MUTEX` question surfaced an explicitly
acceptable outcome. What is not acceptable is finishing the phase without an
answer either way.

Purpose: 19i, GAP-2 and DEN-29 are the same failure class, and it has the worst
track record on this project precisely because it passes locally.
Output: a recorded verification with CI evidence and an explicit `ENV_MUTEX`
disposition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-CONTEXT.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-VALIDATION.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-SPLIT-BASELINE.md
</context>

## Artifacts this phase produces

**New symbols / files / modules / CLI flags / dependencies:** none. This plan
produces only the verification record in its SUMMARY. It modifies no file in the
repository outside `.planning/`.

<tasks>

<task type="auto">
  <name>Task 1: Assemble the three-part equivalence proof and push for CI</name>

  <files>(none — verification only; evidence lands in .planning/phases/19-release-integrity-main-rs-decomposition/19-11-SUMMARY.md)</files>

  <read_first>
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-VALIDATION.md` § "Equivalence-Proof Procedure (19c–19f)" — the three checks, verbatim.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-SPLIT-BASELINE.md` and the committed `19-SPLIT-BASELINE-names.txt`/`-names-full.txt` — the recorded SHA, both durable name lists, the collision-check result (which determines whether trailing names or normalised full paths are the safe comparison form), and the per-target pass counts. The name-set diff reads the committed `.txt` (durable, git-tracked), not a `/tmp` file.
    - Every `19-0N-SUMMARY.md` from plans 06 through 09 — each records its own per-function diff result, function counts, and any halt. This task aggregates them; it does not re-derive them from scratch.
    - `.github/workflows/ci.yml` — read it directly and record the LITERAL commands each job runs. Confirm it triggers on `on: push` (no branch filter) so pushing the branch is sufficient and no workflow change is needed, and confirm the `test` job runs `cargo test` (line 20) — NOT `cargo test --workspace` and NOT `--all-targets`. Do not paraphrase `cargo test` as `cargo test --workspace`.
  </read_first>

  <action>
    **Check 1 — symbol-level diff, aggregated.** Collect from the 19-06 through
    19-09 SUMMARYs the per-function diff results each recorded: how many
    functions were diffed against the baseline SHA, and how many unexplained
    hunks were found. Sum them. Cross-check the total moved-function count
    against the top-level item inventory of `main.rs` at the baseline SHA
    (`git show <baseline-sha>:crates/devflow-cli/src/main.rs | rg -c '^fn '`)
    minus what remains in `main.rs` now. If the numbers do not reconcile, some
    function was moved without being diffed — identify which, diff it now, and
    record the gap. Do not accept a summed claim that does not reconcile against
    an independent count.

    **Check 2 — test name-set identity.** Run
    `cargo test --workspace -- --list`, derive the comparison list in whichever
    form 19-06's collision check prescribed, and `diff` it against the committed,
    git-tracked `.planning/phases/19-release-integrity-main-rs-decomposition/19-SPLIT-BASELINE-names.txt`
    (or `-names-full.txt` when the collision check mandated the full form) — the
    durable baseline, not a `/tmp` file. The diff must be empty. Record both the
    command and its output. Note that module-path prefixes legitimately changed
    — that is why the comparison strips or normalises them — but a trailing name
    appearing or disappearing is a lost or invented test.

    **Check 3 — pass-count identity.** Run `cargo test --workspace` and compare
    every `test result: ok. N passed` line against the corresponding baseline
    line. The expected delta is exactly the tests this phase added, enumerated:
    plan 19-01's `ensure_devflow_dir` unit tests and its two integration tests,
    and plan 19-03's three `commit_path` tests. Any other delta must be
    explained before this task is done. A workspace total alone is not
    acceptable — a test lost in one target and gained in another cancels out.

    **Then push the branch and let CI run.** Record the CI run URL and its
    result, and record the LITERAL command each CI job runs, transcribed from
    `ci.yml` (Codex review — do not conflate CI's command with the local one):
    the `test` job runs `cargo test` (line 20), the `clippy` job runs
    `cargo clippy --workspace --all-targets -- -D warnings` (line 31), and the
    `fmt` job runs `cargo fmt --check` (line 42). **Do not assert that CI runs
    `cargo test --workspace`** — it does not; it runs plain `cargo test`. In this
    virtual workspace (no `default-members`) that still compiles and runs every
    member's tests, so member-coverage matches, but the CI test job passes
    neither `--workspace` nor `--all-targets`. Record that residual as a **known
    CI-coverage limitation for D-11**: local verification runs
    `cargo test --workspace` explicitly and is the stronger evidence, while CI
    contributes the shared-runner race-window exposure D-11 actually requires.
    Do **not** modify `ci.yml` to add `--workspace`/`--all-targets` here — that
    is a scope change belonging in its own plan; if the phase decides
    workspace-explicit CI coverage is load-bearing, record it as a follow-up
    finding rather than silently treating today's `cargo test` as equivalent.

    **Run CI at least three times against the same commit.** Push an empty
    commit or re-dispatch the workflow to obtain independent runs. One green run
    does not discharge D-11: 19i's whole lesson is that CI's shared runners
    widen race windows non-deterministically, so a single green is weaker
    evidence than three. Record every run's result, including any red.
  </action>

  <verify>
    <automated>cargo test --workspace -- --list | rg '::tests::' | sed 's/.*::tests:://' | sort > /tmp/19-final-names.txt; diff /tmp/19-final-names.txt .planning/phases/19-release-integrity-main-rs-decomposition/19-SPLIT-BASELINE-names.txt && echo NAMESET-IDENTICAL; cargo test --workspace 2>&1 | grep -E 'test result: ok\.'</automated>
  </verify>

  <acceptance_criteria>
    - Check 1: the aggregated moved-function count reconciles against the independent count derived from the baseline SHA, and the total unexplained-hunk count is **0**. Both numbers recorded in the SUMMARY.
    - Check 2: the name-set diff is empty and the verify command prints `NAMESET-IDENTICAL`. Command and output recorded.
    - Check 3: every per-target `test result: ok. N passed` line is accounted for against the baseline, with the delta explained test-by-test. `0 failed` in every target.
    - **CI evidence:** at least three CI runs against the phase branch are recorded with their URLs and results. All three green, or every red run's failing test named.
    - CI coverage recorded ACCURATELY (Codex review): the SUMMARY transcribes the literal per-job commands — `test` → `cargo test` (ci.yml:20), `clippy` → `cargo clippy --workspace --all-targets -- -D warnings` (ci.yml:31), `fmt` → `cargo fmt --check` (ci.yml:42). The SUMMARY does NOT claim CI runs `cargo test --workspace`; it records the `cargo test` vs local `cargo test --workspace` distinction and the `--workspace`/`--all-targets` residual as a known CI-coverage limitation for D-11, rather than silently treating today's `cargo test` as equivalent evidence. `rg -c 'cargo test --workspace' .github/workflows/ci.yml` returns `0` — proving the claim, if ever written, would be false.
    - `git status --porcelain -- crates/` produces no output — this task changed no source.
  </acceptance_criteria>

  <done>
    All three equivalence checks are run, aggregated against an independent
    count, and confirmed on at least three CI runs against the branch, with
    every command, output, and run URL recorded.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Adjudicate the `ENV_MUTEX` disposition and confirm the downstream-repo fix</name>

  <files>(none — verification only; evidence lands in .planning/phases/19-release-integrity-main-rs-decomposition/19-11-SUMMARY.md)</files>

  <action>
    Work through Parts A, B and C in `<how-to-verify>` in order: adjudicate the
    `ENV_MUTEX` disposition from the recorded stability and CI evidence, run the
    scratch-repository reproduction of the `17-REVIEW.md` scenario against a
    provenance-checked binary built from this branch, and complete the
    requirement roll-call. Record every command, every verbatim output, and the
    verdict for each part in the SUMMARY. Modify no file under `crates/`; a
    failed check routes back to its owning plan rather than being fixed here.
  </action>

  <read_first>
    - Task 1's recorded CI results, in full — including any red run.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-06-SUMMARY.md` through `19-09-SUMMARY.md` — every recorded `D-12 ENV_MUTEX finding` section, and every three-consecutive-run stability result.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-CONTEXT.md` § D-11, D-12 — the two decisions this checkpoint adjudicates.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-VALIDATION.md` § "Manual-Only Verifications" — the scratch-repo reproduction procedure.
  </read_first>

  <what-built>
    The phase delivered, across ten prior plans: a self-ignoring `.devflow/`
    constructor wired into all 7 production sites (19a-WR01); `exe_path`
    redaction in `events.jsonl` (19a-WR02); a genuinely idempotent `commit_path`
    (19b); the `main.rs` split into a thin crate root plus flat sibling modules
    with a single hoisted `ENV_MUTEX` (19c–19f); the AI change acceptance
    contract as this repository's first project skill (19g, dogfooded in plan
    19-05); and regenerated codebase documents (19-10).

    Task 1 assembled the three-part equivalence proof and ran it on CI.
  </what-built>

  <how-to-verify>
    **Part A — `ENV_MUTEX` disposition (D-12). This must end in one of exactly
    two recorded verdicts; "seems fine" is not one of them.**

    1. Read every three-consecutive-run stability result recorded in plans
       19-06 through 19-09, and every CI run from Task 1.
    2. If **all** were stable and identical: record the verdict
       `ENV_MUTEX preserved — no finding`, with the evidence (which runs, how
       many, which commits). State explicitly that exactly one `ENV_MUTEX`
       exists in the `devflow` crate and that all lock sites resolve to it, and
       cite the source assertion from 19-06 that proves it.
    3. If **any** run differed — a failure, a flake, a differing pass count:
       record the verdict `D-12 ENV_MUTEX finding raised`, and write up the
       finding with: the test name, the environment variable involved, the lock
       site, which module it now lives in, and what was observed. Then state
       what it means for the phase — most likely that the split is accepted as
       partial and the serialization question is promoted to its own backlog
       item. Do not attempt a fix in this plan.
    4. Either way, confirm the D-04 invariant is documented where the mutex
       lives, and that `devflow-core`'s two separate `ENV_MUTEX` statics were
       left untouched.

    **Part B — downstream-repo reproduction (19a-WR01).** Plan 19-01's automated
    test proves the `.gitignore` is written. Only a real repository proves the
    sweep stops. This is the exact `17-REVIEW.md` scenario.

    1. Create a fresh scratch git repository outside this workspace, with an
       initial commit and a root `.gitignore` containing **no** `.devflow`
       pattern of any kind.
    2. Run a DevFlow phase in it using the binary built from this branch. Confirm
       first that the binary is actually from this branch — a stale binary is a
       recorded failure mode on this project. Verify with
       `devflow --version` and the build-provenance commit.
    3. Run `git add . && git commit -m "user commit"`.
    4. Run `git log -1 --name-only`. **No path beginning `.devflow/` may
       appear.** Record the full output.
    5. Confirm `.devflow/.gitignore` exists in the scratch repo and contains `*`.
    6. **Empty/single-element edge:** confirm the same holds when `.devflow/`
       contains only the `.gitignore` and nothing else — a directory whose sole
       member is the marker must still contribute zero paths to the commit.
    7. **Boundary edge:** confirm `git add -f .devflow/some-file` still works.
       The fix must ignore, not forbid; a user who deliberately wants to commit
       something under `.devflow/` must still be able to.

    **Part C — requirement roll-call.** For each of 19a, 19b, 19c, 19d, 19e,
    19f, 19g, record: landed / partially landed / not landed, the plan that owns
    it, and the evidence. Any partial must say what is missing and where it went
    (backlog item, follow-up phase, or open finding). No requirement may be left
    without a verdict.

    **If any part fails**, do not adjust the check to make it pass. Record what
    was observed, name the owning plan, and report the checkpoint as not passed.
  </how-to-verify>

  <verify>
    <human-check>Part A ends in one of the two named verdicts with evidence; Part B's `git log -1 --name-only` output shows zero `.devflow/` paths and all three edges are confirmed; Part C gives all seven requirements a verdict. All outputs recorded verbatim in the SUMMARY.</human-check>
  </verify>

  <acceptance_criteria>
    - The SUMMARY contains a section headed `ENV_MUTEX disposition` whose first line is exactly one of `ENV_MUTEX preserved — no finding` or `D-12 ENV_MUTEX finding raised`, followed by the evidence that verdict requires.
    - If a finding was raised, it names the test, the env var, the lock site, and the module — not a general statement that something was flaky.
    - The SUMMARY records the verbatim `git log -1 --name-only` output from the scratch repository, and it contains no substring `.devflow/`.
    - The SUMMARY records the scratch repo's `.devflow/.gitignore` content as `*`.
    - The SUMMARY records the binary-provenance check (`devflow --version` plus build commit) confirming the scratch run used a binary built from this branch, not a stale one.
    - All three Part B edges — full `.devflow/`, marker-only `.devflow/`, and `git add -f` — are recorded with their observed results.
    - The SUMMARY contains a requirement roll-call table with a verdict and evidence for each of 19a, 19b, 19c, 19d, 19e, 19f, 19g. No cell reads "assumed" or is blank.
    - Every check that did not pass is recorded as not passed, naming the owning plan, rather than rationalised.
  </acceptance_criteria>

  <resume-signal>Type "approved" if the ENV_MUTEX verdict is recorded with evidence, the scratch repo commits zero `.devflow/` paths across all three edges, and all seven requirements have a verdict. Otherwise describe which part failed and what was observed.</resume-signal>

  <done>
    The `ENV_MUTEX` disposition is recorded as one of the two named verdicts
    with its required evidence; the scratch repository commits zero `.devflow/`
    paths across all three edges with binary provenance confirmed; and all seven
    requirements have a verdict with evidence and no blank or assumed cell.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local workstation → CI runners | CI's shared runners widen race windows relative to this workstation; local-green is measurably weaker evidence (19i hit 2/2 in CI after passing locally most of the time) |
| Built binary → scratch repository | The end-to-end 19a threat crosses a repository boundary, so only a real repository can confirm it is closed |
| Phase verification → project record | An unrecorded finding is indistinguishable from no finding six months later |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-34 | Repudiation | The phase is signed off on local-green, leaving the `ENV_MUTEX` failure class exactly where it was — the failure mode with the worst track record on this project | high | mitigate | Task 1 requires at least three CI runs against the branch with recorded URLs; D-11 is stated as a hard prohibition; a single green run is explicitly declared insufficient. |
| T-19-35 | Repudiation | A red CI run is dismissed as flaky and re-run until green, and only the green run is reported | high | mitigate | Every run's result must be recorded including reds; dismissing a red requires naming the test, env var, and lock site — which is itself the D-12 finding, so there is no path that discards the observation. |
| T-19-36 | Tampering | The aggregated per-function diff claim is accepted without an independent count, so a function moved without being diffed passes unnoticed | high | mitigate | Task 1 requires reconciling the summed count against an independent count derived from the baseline SHA, and requires diffing any function the reconciliation exposes. |
| T-19-01 | Information Disclosure | The 19a fix is proven only by its own unit test, and the real downstream sweep is never exercised | high | mitigate | Part B runs the exact `17-REVIEW.md` scratch-repo reproduction with a provenance-checked binary, across three edges including the marker-only directory and the `git add -f` escape hatch. |
| T-19-37 | Repudiation | A requirement is left without a verdict and the phase closes with an unstated gap | medium | mitigate | Part C requires a roll-call table with no blank or "assumed" cell. |
| T-19-38 | Tampering | A source file is edited during verification to make a check pass, so the phase verifies a tree nobody reviewed | medium | mitigate | Hard prohibition plus a `git status --porcelain -- crates/` acceptance criterion in Task 1; failures route back to the owning plan. |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new external packages in this plan or in the phase. `19-RESEARCH.md` § "Package Legitimacy Audit" records the phase as not applicable; no install task exists anywhere in the phase, so no legitimacy checkpoint is required. |

**ASVS level:** 1. **Block on:** high. Four high-severity threats, all mitigated in-plan; none accepted.
</threat_model>

<verification>
1. Three-part equivalence proof: aggregated symbol diff reconciled against an independent count with zero unexplained hunks; empty name-set diff; per-target pass-count identity with the delta explained test-by-test.
2. At least three CI runs against the branch, URLs and results recorded, with each job's LITERAL command transcribed from `ci.yml`: `test` → `cargo test` (NOT `--workspace`), `clippy` → `cargo clippy --workspace --all-targets -- -D warnings`, `fmt` → `cargo fmt --check`. The `cargo test` vs local `cargo test --workspace` distinction and the `--workspace`/`--all-targets` residual are recorded as a known CI-coverage limitation, not conflated.
3. `ENV_MUTEX` disposition recorded as one of the two named verdicts with its required evidence.
4. Scratch-repo reproduction recorded with verbatim `git log -1 --name-only` output containing no `.devflow/` path, across all three edges, with binary provenance confirmed.
5. Requirement roll-call complete for 19a, 19b, 19c, 19d, 19e, 19f, 19g.
6. `git status --porcelain -- crates/` empty throughout.
</verification>

<success_criteria>
- The equivalence proof holds on CI, not only locally.
- The `ENV_MUTEX` question has an explicit verdict — preserved, or a written finding under D-12.
- The downstream-repo threat that motivated the phase is confirmed closed in a real repository.
- Every requirement has a recorded outcome, including any that landed partially.
</success_criteria>

<output>
Create `.planning/phases/19-release-integrity-main-rs-decomposition/19-11-SUMMARY.md` when done.
</output>
