---
phase: 19-release-integrity-main-rs-decomposition
plan: 10
type: execute
wave: 6
depends_on: ["19-09"]
files_modified:
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/TESTING.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [19f]

must_haves:
  truths:
    - "`.planning/codebase/STRUCTURE.md` describes the module layout that actually exists after the split — no reference to `main.rs` as a single large file survives."
    - "`.planning/codebase/TESTING.md` cites only tests that exist at HEAD; the deleted `devflow_ignores_stray_devflow_yaml` reference is gone, replaced by a test verified present."
    - "Both documents name the shared `crate::test_support` module and the single-`ENV_MUTEX` invariant, so the next contributor adding an env-mutating test finds the rule rather than declaring a second mutex."
    - "Every file path, module name, and test name written into either document is verified to exist at HEAD before being written."
    - "ROADMAP.md's Phase 19 entry lists the plans that were actually created and reflects what actually landed, including any D-12 partial outcome."
  artifacts:
    - ".planning/codebase/STRUCTURE.md — regenerated"
    - ".planning/codebase/TESTING.md — regenerated"
    - ".planning/ROADMAP.md — Phase 19 entry reconciled"
  key_links:
    - "`19-09-SUMMARY.md`'s recorded final line counts → the figures cited in STRUCTURE.md"
    - "`19-06-SUMMARY.md`'s D-04 invariant text → the env-mutation rule stated in TESTING.md"
    - "`19-07`/`19-08`/`19-09` SUMMARYs → which clusters landed, for the ROADMAP reconciliation"
  prohibitions:
    - "MUST NOT write any file path, module name, function name, or test name into either document without first verifying it exists at HEAD. These two documents are being regenerated precisely because unverified claims rotted in them."
    - "MUST NOT touch any Rust source file. This plan is documentation only — that is what lets it run in the same wave as the verification plan."
    - "MUST NOT rewrite `.planning/codebase/CONVENTIONS.md` or `CONCERNS.md`. They were written 2026-07-17 and are not verified stale; regenerating them would be unscoped work."
    - "MUST NOT replace ROADMAP.md wholesale. Edit only the Phase 19 entry, in place, with scoped replacements."
    - "MUST NOT claim the split delivered pipeline-internal wave parallelism. `19-RESEARCH.md` Pitfall 1 verified the three pipeline files are mutually cyclic; the honest claim is that the *other* clusters gained independence."
---

<objective>
Bring the codebase map back in line with reality.

CONTEXT.md's canonical-refs section flags two documents as stale, and
`19-RESEARCH.md` open question #2 records that the backlog dossier and
CONTEXT.md's deferred list disagree on whether fixing them belongs to this
phase. **Resolved as in scope**: the dossier's framing ("not worth its own
backlog number, but do not let it slide past this phase's completion") is more
specific and post-dates the general deferred list, and the split invalidates
both documents further, so deferring means shipping a knowingly wrong map.

- `STRUCTURE.md` still calls `main.rs` a "single 1000+ line file" — it was 8,467
  lines before this phase and is now a thin crate root plus eight siblings.
- `TESTING.md` cites `devflow_ignores_stray_devflow_yaml` as its `main.rs`
  example test; that test has been deleted.

Purpose: `STRUCTURE.md` is the document a future planner reads to answer "where
does new code go." After this phase that answer changed completely, and a wrong
answer costs a whole planning cycle.
Output: two regenerated codebase documents and a reconciled ROADMAP entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-CONTEXT.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-06-SUMMARY.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-07-SUMMARY.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-08-SUMMARY.md
@.planning/phases/19-release-integrity-main-rs-decomposition/19-09-SUMMARY.md
</context>

## Artifacts this phase produces

**New symbols / files / modules / CLI flags / dependencies:** none. This plan
writes only Markdown under `.planning/`.

**Modified:** `.planning/codebase/STRUCTURE.md`, `.planning/codebase/TESTING.md`, `.planning/ROADMAP.md` (Phase 19 entry only)

<tasks>

<task type="auto">
  <name>Task 1: Regenerate `.planning/codebase/STRUCTURE.md`</name>

  <files>.planning/codebase/STRUCTURE.md</files>

  <read_first>
    - `.planning/codebase/STRUCTURE.md` in full — the current document, so the regeneration preserves its structure, headings, and voice rather than replacing it with a differently-shaped document.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-09-SUMMARY.md` — the recorded final line counts for `main.rs` and every split module. Use those recorded figures rather than re-deriving them, and cross-check one of them with `wc -l` to confirm the record is accurate.
    - The actual tree: `ls crates/devflow-cli/src/ crates/devflow-core/src/` and `rg -n '^mod |^pub mod ' crates/devflow-cli/src/main.rs crates/devflow-core/src/lib.rs`. Every module named in the document must appear in this output.
    - Each new module's doc comment: `rg -n -A6 '^//!' crates/devflow-cli/src/*.rs`. The "what this module owns" sentences written during 19-07/19-08/19-09 are the source material for the per-module descriptions — do not invent new ones that could drift.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-RESEARCH.md` § "Pattern 1", § "Pattern 2", § "Pitfall 1" — the coupling facts the "where does new code go" guidance must reflect.
  </read_first>

  <action>
    Rewrite `.planning/codebase/STRUCTURE.md` in place, preserving its existing
    section structure and heading names. Replace the module-layout content with
    what exists at HEAD.

    Describe `crates/devflow-cli/src/` as a thin `main.rs` crate root plus flat
    sibling modules, listing each with its actual line count and a one-sentence
    description sourced from that module's own doc comment:
    `main.rs`, `commands.rs`, `pipeline_launch.rs`, `pipeline_outcomes.rs`,
    `pipeline_gate.rs`, `preflight.rs`, `staleness.rs`, `parallel.rs`,
    `config_parse.rs`, `test_support.rs`. If 19-08 halted at a seam boundary
    under D-12, describe what actually exists — do not describe the intended end
    state as if it landed.

    In the "where to add new code" guidance, state three things the split does
    **not** give you, because a future planner reading only the file list would
    assume otherwise:
    - `preflight.rs` and the pipeline modules are bidirectionally coupled in
      production code (the 18-07 `run_preflight` ↔ `launch_stage_inner` pair). A
      change to either is likely to need both.
    - The three `pipeline_*.rs` files are mutually cyclic. A change to pipeline
      logic is likely to touch two or three of them together; the split buys
      `pub(crate)` boundaries and reviewability, not pipeline-internal wave
      parallelism.
    - What the split *does* buy is that the other clusters — commands,
      staleness, parallel, config_parse — are now genuinely independent of
      pipeline and of each other, which is the property Phase 20's backlog
      needs.

    State the visibility convention: `devflow-cli` is a binary crate with no
    external consumers, so cross-module items are `pub(crate)`, never `pub`.
    Note that `devflow-core` is a library crate and does use `pub`, so the two
    crates' conventions genuinely differ and that is intentional.

    Remove every remaining reference to `main.rs` as a single large file. Update
    the document's "last verified" date.

    **Verification discipline for this task**: for every path, module, or symbol
    you write, run a grep confirming it exists at HEAD before writing it. This
    document is being regenerated because unverified claims rotted in it; do not
    reintroduce the failure mode.
  </action>

  <verify>
    <automated>for m in $(rg -o '`crates/devflow-cli/src/[a-z_]+\.rs`' .planning/codebase/STRUCTURE.md | tr -d '`' | sort -u); do test -f "$m" || echo "MISSING: $m"; done; echo "path-check-done"</automated>
  </verify>

  <acceptance_criteria>
    - Every file path of the form `crates/.../*.rs` mentioned in `STRUCTURE.md` exists on disk. The verify command prints `path-check-done` with **no** `MISSING:` line.
    - Source assertion: `rg -c '1000\+ line|single .*line file' .planning/codebase/STRUCTURE.md` returns `0` — the stale claim is gone.
    - Source assertion: `rg -c 'pipeline_launch|pipeline_outcomes|pipeline_gate|test_support|config_parse' .planning/codebase/STRUCTURE.md` returns `≥ 5` — every new module is named. Adjust downward only if 19-08 halted under D-12, and record that adjustment in the SUMMARY.
    - Source assertion: `rg -c 'pub\(crate\)' .planning/codebase/STRUCTURE.md` returns `≥ 1` — the visibility convention is stated.
    - Honesty assertion: `rg -c 'cyclic|coupled|together' .planning/codebase/STRUCTURE.md` returns `≥ 2` — the two coupling caveats are present. A document that lists nine independent-looking files without them is the failure mode this criterion exists to prevent.
    - Every module line count cited in the document matches `wc -l` on that file, within zero tolerance. Spot-check at least three and record the checked values in the SUMMARY.
    - `git status --porcelain -- crates/` produces no output — zero source touched.
  </acceptance_criteria>

  <done>
    `STRUCTURE.md` describes the module layout that exists, cites verified line
    counts, states the `pub(crate)` convention, and records both coupling
    caveats so a future planner does not over-plan parallelism.
  </done>
</task>

<task type="auto">
  <name>Task 2: Regenerate `.planning/codebase/TESTING.md`</name>

  <files>.planning/codebase/TESTING.md</files>

  <read_first>
    - `.planning/codebase/TESTING.md` in full — the current document and the exact stale reference to `devflow_ignores_stray_devflow_yaml`.
    - `crates/devflow-cli/src/test_support.rs` — the hoisted `ENV_MUTEX` doc comment written in 19-06, which states the D-04 invariant. The document's env-mutation rule must match it, not paraphrase it into something weaker.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-06-SUMMARY.md` — the D-04 invariant text and the hoisted-item inventory.
    - `ls crates/devflow-cli/tests/ crates/devflow-core/tests/` — the actual integration-test inventory.
    - `.planning/phases/19-release-integrity-main-rs-decomposition/19-VALIDATION.md` — the recorded false-green traps, which belong in this document permanently rather than only in a phase artifact.
  </read_first>

  <action>
    Rewrite `.planning/codebase/TESTING.md` in place, preserving its existing
    section structure.

    Remove the `devflow_ignores_stray_devflow_yaml` reference. Replace it with a
    test that exists at HEAD, chosen from the split module that best illustrates
    the convention being described, and confirmed present with
    `cargo test -p devflow <name>` reporting `1 passed` before it is written into
    the document.

    Record the unit-test layout as it now is: each module carries its own
    `#[cfg(test)] mod tests` block at the bottom of its own file, using
    `use super::*;` for its own items and `use crate::test_support::*;` for the
    shared fixtures. Name the shared module explicitly and list what it holds
    (`ENV_MUTEX`, `init_repo` and variants, the fake adapters,
    `stub_agent_binary`, `prepend_path`, `stage_launched_count`).

    **State the env-mutation rule as a rule, not a description.** A contributor
    adding a test that touches `PATH`, `DEVFLOW_GATE_TIMEOUT_SECS`,
    `DEVFLOW_CHECKOUT_LOCK_TIMEOUT_SECS`, or `DEVFLOW_GATE_NOTIFY_CMD` must lock
    `crate::test_support::ENV_MUTEX` and must **not** declare a new mutex. State
    the D-04 invariant verbatim as the reason: every env var is guarded by
    exactly one mutex, and no var is touched under two. Note that
    `devflow-core`'s `gates.rs` and `config.rs` each still hold their own
    `ENV_MUTEX`, that they are safe only because they compile into different
    test binaries, and that nothing enforces the invariant mechanically — so it
    is a convention a reviewer must check.

    Record the two false-green traps permanently, with the reason each one
    matters:
    - `cargo test --exact` with a bare test name matches nothing and still exits
      0. Assert on the reported pass count, never on exit status alone.
    - The package is `devflow`, not `devflow-cli`. Also record that
      `cargo test --lib` does not work on this binary-only crate.

    Cross-reference `.claude/skills/ai-change-acceptance/` (created in plan
    19-04) as the acceptance contract a test must satisfy, so the testing
    conventions and the acceptance criteria are linked rather than living in two
    unaware places.

    List the integration tests that actually exist, verified by `ls`. Update the
    document's "last verified" date.
  </action>

  <verify>
    <automated>rg -c 'devflow_ignores_stray_devflow_yaml' .planning/codebase/TESTING.md; rg -c 'ENV_MUTEX' .planning/codebase/TESTING.md; rg -c 'test_support' .planning/codebase/TESTING.md</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `rg -c 'devflow_ignores_stray_devflow_yaml' .planning/codebase/TESTING.md` returns `0` — the deleted test is no longer cited.
    - Behavior assertion: the replacement example test named in the document is confirmed present by running `cargo test -p devflow <that name>` and observing `1 passed`. Record the command and its output in the SUMMARY.
    - Source assertion: `rg -c 'ENV_MUTEX' .planning/codebase/TESTING.md` returns `≥ 1` and `rg -c 'exactly one mutex' .planning/codebase/TESTING.md` returns `≥ 1` — the D-04 invariant is stated, not merely alluded to.
    - Source assertion: `rg -c 'test_support' .planning/codebase/TESTING.md` returns `≥ 1`.
    - Source assertion, false-green traps recorded: `rg -c 'exit' .planning/codebase/TESTING.md` returns `≥ 1` and `rg -c 'devflow-cli' .planning/codebase/TESTING.md` returns `≥ 1` with the surrounding text identifying it as the wrong package name to pass to `-p`.
    - Source assertion: `rg -c 'ai-change-acceptance' .planning/codebase/TESTING.md` returns `≥ 1`.
    - Every integration test file named in the document exists — verify each against `ls crates/devflow-cli/tests/ crates/devflow-core/tests/` and record the check in the SUMMARY.
    - `git status --porcelain -- crates/` produces no output.
  </acceptance_criteria>

  <done>
    `TESTING.md` cites only tests that exist, states the D-04 env-mutation rule
    as an enforceable convention with its rationale, records both false-green
    traps permanently, and links to the acceptance contract.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the ROADMAP Phase 19 entry with what actually landed</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    - `.planning/ROADMAP.md` § "### Phase 19" — the entry as it stands, including the `**Plans:** 0 plans` placeholder and the `- [ ] TBD` line.
    - Every `19-*-SUMMARY.md` in this phase directory — the record of what actually landed, including any D-12 partial outcome.
  </read_first>

  <action>
    Edit **only** the `### Phase 19` section of `.planning/ROADMAP.md`, using
    scoped replacements. Do not rewrite the file; a whole-file write would
    destroy every other phase entry.

    Replace the `**Plans:** 0 plans` line with the real count and completion
    state. Replace the `- [ ] TBD (run /gsd-plan-phase 19 to break down)` line
    with the wave-grouped plan list, matching the format used by the completed
    Phase 12 and Phase 18 entries elsewhere in the file — a `**Wave N**` heading
    followed by one checkbox line per plan with a brief objective.

    Leave the existing Goal, Targets, Depends on, Sequencing, and Principal-risk
    paragraphs unchanged. They were written during scoping and are still
    accurate; rewriting them would lose the recorded rationale.

    If any plan halted under D-12, add one sentence to the Principal-risk
    paragraph recording that the `ENV_MUTEX` question was surfaced, which seam
    it surfaced at, and where the finding is written up. Do not describe a
    partial split as a completed one.
  </action>

  <verify>
    <automated>rg -n -A3 '^\*\*Plans:\*\*' .planning/ROADMAP.md | rg -n 'Phase 19' ; rg -c '19-0[1-9]-PLAN.md|19-1[01]-PLAN.md' .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - Source assertion: `rg -c '19-01-PLAN.md' .planning/ROADMAP.md` returns `≥ 1`, and every plan file present in the phase directory appears in the ROADMAP list. Verify by comparing `ls .planning/phases/19-*/19-*-PLAN.md` against the ROADMAP entries — the two sets must match exactly.
    - Source assertion: `rg -c 'TBD \(run /gsd-plan-phase 19' .planning/ROADMAP.md` returns `0` — the placeholder is gone.
    - Diff-scope assertion: `git diff .planning/ROADMAP.md` shows hunks **only** within the `### Phase 19` section. No other phase entry, and no `## Backlog` item, appears in the diff.
    - Every other `### Phase N` heading present before this task is still present after it — verify with `rg -c '^### Phase ' .planning/ROADMAP.md` returning the same count as before the edit. Record both counts in the SUMMARY.
    - If any plan halted under D-12, the Principal-risk paragraph says so and names where the finding is written up.
    - `git status --porcelain -- crates/` produces no output.
  </acceptance_criteria>

  <done>
    The ROADMAP Phase 19 entry lists every plan that exists, grouped by wave,
    with no placeholder remaining, no collateral damage to any other entry, and
    an honest record of any partial outcome.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codebase documents → future planners | `STRUCTURE.md` is read to answer "where does new code go"; a wrong answer costs a planning cycle, which is exactly the cost this phase is paying down |
| Whole-file write → ROADMAP.md | A non-scoped write to ROADMAP.md destroys every phase entry outside the diff window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-30 | Repudiation | The regenerated documents contain unverified claims and rot again, reproducing the exact failure being fixed | medium | mitigate | Every path, module, symbol, and test name must be grep- or `ls`-verified before being written; the verify command checks every cited file path exists; the replacement example test must be run and observed passing. |
| T-19-31 | Tampering | `STRUCTURE.md` lists nine sibling files and a future planner infers nine independent wave lanes, planning colliding Phase 20 plans | medium | mitigate | Both coupling caveats (preflight↔pipeline, pipeline-internal cycle) are required content with a source assertion, and the honest claim about what the split *does* buy is stated alongside them. |
| T-19-32 | Denial of Service | A whole-file write to `ROADMAP.md` destroys unrelated phase entries and the `## Backlog` section | high | mitigate | Scoped `Edit` only is mandated; the acceptance criteria require the diff to touch only the `### Phase 19` section and require the `### Phase ` heading count to be unchanged before and after. |
| T-19-33 | Repudiation | A D-12 partial split is documented as a complete one, so the surfaced `ENV_MUTEX` finding disappears from the project record | high | mitigate | Tasks 1 and 3 both require describing what actually landed, and Task 3 requires the Principal-risk paragraph to record the finding and its write-up location. |
| T-19-SC | Tampering | npm/pip/cargo installs | low | accept | This plan writes only Markdown under `.planning/`. Zero packages, zero source, zero build changes. |

**ASVS level:** 1. **Block on:** high. T-19-32 and T-19-33 are high and both mitigated with explicit diff-scope and content assertions; no high-severity threat is accepted.
</threat_model>

<verification>
1. Every `crates/.../*.rs` path cited in `STRUCTURE.md` exists — verify command prints no `MISSING:` line.
2. `devflow_ignores_stray_devflow_yaml` no longer appears in `TESTING.md`; its replacement example was run and observed passing.
3. The D-04 invariant appears verbatim in `TESTING.md`; both false-green traps are recorded.
4. Both coupling caveats appear in `STRUCTURE.md`.
5. `git diff .planning/ROADMAP.md` is confined to the `### Phase 19` section, and the `### Phase ` heading count is unchanged.
6. `git status --porcelain -- crates/` is empty for the whole plan.
</verification>

<success_criteria>
- Both codebase documents describe HEAD, with every claim verified before writing.
- The two coupling caveats and the D-04 env-mutation rule are recorded where a contributor will find them.
- ROADMAP.md's Phase 19 entry lists every plan, grouped by wave, with no collateral edits.
- Any D-12 partial outcome is recorded honestly in both the codebase map and the ROADMAP.
</success_criteria>

<output>
Create `.planning/phases/19-release-integrity-main-rs-decomposition/19-10-SUMMARY.md` when done.
</output>
