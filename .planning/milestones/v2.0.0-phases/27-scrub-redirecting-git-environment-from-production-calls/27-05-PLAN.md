---
phase: 27-scrub-redirecting-git-environment-from-production-calls
plan: 05
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - crates/devflow-cli/src/preflight.rs
autonomous: true
requirements: [D-01, D-03]
must_haves:
  truths:
    - "D-01: all eleven production git invocations in `preflight.rs` construct through `git_command`, including the two that live inside closures and are invisible to any function-signature search."
    - "D-01: `fast_forward_base_ref`'s `git update-ref` — the only MUTATING git operation in the preflight surface — cannot write a ref into a repository the operator never named."
    - "D-03: `preflight::` unit tests reach a `test result:` line with `0 failed` under a hostile `GIT_DIR`, where they previously failed."
    - "The one site that operates on `execution_root` rather than `project_root` (`breaking_commit_subjects`) keeps its own path expression — ten sites use `project_root`, one uses `execution_root`, and the split is asserted by source count so a silent repoint is caught."
  artifacts:
    - "crates/devflow-cli/src/preflight.rs — all 11 production call sites routed through `git_command`"
  key_links:
    - "pipeline_launch::launch_stage -> preflight::run_preflight -> {phase_reachability_on_base, ensure_base_ref_current, preflight_major_bump_check} -> git_command — the gate that decides whether `devflow start` may fork a phase at all"
    - "preflight::ensure_base_ref_current -> fast_forward_base_ref -> git_command -> `git update-ref` — the one write operation on this surface"
---

<!-- planner-discipline-allow: Command::new("git") -->
<!-- planner-discipline-allow: std::process::Command::new("git") -->

<objective>
Migrate `preflight.rs`, the phase's largest single file: eleven production git
invocations, no internal wrapper, two of them buried inside closures.

Purpose: preflight is the gate `devflow start` passes through before it forks a
worktree, and it is the only migrated surface that *writes* — `fast_forward_base_ref`
runs `git update-ref` on the base branch. Under a hostile `GIT_DIR`, every
preflight question is answered by a foreign repository and that one write lands in
it. A preflight that reads a stranger's refs to decide the local run is safe is
strictly worse than no preflight at all, because it reports success.

Output: zero unscrubbed production git invocations in the file.
</objective>

## Artifacts this plan produces

No new public symbols. New test symbols only:

| Symbol | Location | Kind |
|---|---|---|
| `preflight::tests::phase_reachability_resolves_caller_root_under_a_hostile_git_dir` | `crates/devflow-cli/src/preflight.rs` | test (new) |
| `preflight::tests::fast_forward_base_ref_never_writes_into_a_hostile_git_dir` | `crates/devflow-cli/src/preflight.rs` | test (new) |

The constructor this plan consumes is created by `27-01`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-CONTEXT.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-PATTERNS.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-01-SUMMARY.md
</context>

## Shared recipe: the hostile-`GIT_DIR` harness

```
HOSTILE=$(mktemp -d) && git init -q "$HOSTILE"
GIT_DIR="$HOSTILE/.git" cargo test <scope>
```

A `test result:` line must actually print. A timeout, or a `0 passed` line
(meaning the filter matched nothing), is a FAILURE, not a pass. Never use a bare
`cargo test --workspace` under a hostile `GIT_DIR` — it does not terminate
(`27-RESEARCH.md` Pitfall 2).

## Locating the sites

There is no internal wrapper in this file — every one of the eleven sites is an
independent edit. Two of them (`base_ref_currency`'s `is_ancestor` closure and
`ensure_base_ref_current`'s `resolve` closure) live inside closure bodies and will
NOT surface in a search for functions that take `project_root`. Find every site
with a literal grep for the constructor, and reconcile the count to eleven before
you start editing. Line numbers below are as of the phase base commit and shift as
you edit.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate phase_reachability_on_base and base_ref_currency (7 sites)</name>
  <files>crates/devflow-cli/src/preflight.rs</files>

  <read_first>
    - `crates/devflow-cli/src/preflight.rs` lines 1-40 (module doc, including the deliberate `pipeline_launch` mutual-call note, and the `use devflow_core::{agents, events, version, workflow};` import block the new import should join) and lines 140-400 (`phase_reachability_on_base`, `unreachable_message`, `ensure_phase_reachable_on_base`, `base_ref_currency`, `stale_base_message`)
    - `crates/devflow-core/src/test_support.rs` — the canonical hermetic-command pattern and the module doc explaining why `current_dir` alone is insufficient
    - `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-PATTERNS.md` § "crates/devflow-cli/src/preflight.rs" — the representative soft-fail before-shape and the closure-embedded before-shape, both quoted verbatim
  </read_first>

  <behavior>
    New test `phase_reachability_resolves_caller_root_under_a_hostile_git_dir` in `preflight.rs`'s existing `#[cfg(test)] mod tests` (starts at line 968; existing tests there already build fixtures with `devflow_core::test_support::git_command(root)` — follow that fixture idiom):

    - Build a throwaway repository whose base branch does NOT contain a `.planning/phases/NN-*/` directory for the phase under test.
    - Build a second, unrelated throwaway repository whose base branch DOES contain one. This is the dangerous direction: a foreign repository vouching for a phase that does not exist locally, letting `devflow start` fork a run with nothing to work on.
    - Assert `phase_reachability_on_base` reports the real repository's answer, with the foreign `GIT_DIR` injected into the spawned child only. Do NOT mutate the process environment with `std::env::set_var` (Rust 2024 `unsafe`, unsound under threaded tests — Phase 25 D-14); reuse `27-01`'s child-scoped injection technique verbatim.
    - Watch it fail against the unmigrated code, then migrate, then watch it pass. Record both outputs in the SUMMARY.
  </behavior>

  <action>
Migrate the seven production git invocations in `phase_reachability_on_base` and
`base_ref_currency`. At the phase base commit, **all seven pass `project_root` to
`.current_dir`**:

| Enclosing item | Base-commit line | Argv | Note |
|---|---|---|---|
| `phase_reachability_on_base` | 146 | `rev-parse --verify …` | |
| `phase_reachability_on_base` | 160 | roadmap-file read | |
| `phase_reachability_on_base` | 183 | `ls-tree -r --name-only <base> -- .planning/phases/` | multi-line `.args([...])`; `.current_dir` sits 8 lines below the constructor |
| `base_ref_currency` | 332 | `fetch --quiet <ORIGIN> <base>` | soft-fail: `.map(...).unwrap_or(false)` |
| `base_ref_currency` | 345 | `rev-parse --verify --quiet …` | |
| `base_ref_currency` | 356 | `merge-base --is-ancestor <a> <d>` | **inside the `is_ancestor` closure** (base-commit 355-362), captures `project_root` by reference from the enclosing scope |
| `base_ref_currency` | 371 | `rev-list --count …` | |

At every site: replace `std::process::Command::new("git")` and its separate
`.current_dir(project_root)` with a single `git_command(project_root)`. Keep
everything else exactly as written — the multi-line `.args([...])` arrays, the
`.output()` calls, and each site's own fail-soft tail
(`.map(|out| out.status.success()).unwrap_or(false)`,
`.ok().filter(...)`, and the `match` arms that follow). Do not tighten any
fail-soft posture into a hard error: Phase 24's `check_ssh_signing_viability`
precedent and `phase_reachability_on_base`'s own doc comment both establish that
these degrade gracefully on purpose. This is an environment change only.

The closure site at base-commit line 356 migrates identically to any other — the
closure captures `project_root` by reference and `git_command(project_root)`
works unchanged inside it. It is called out separately only because it is easy to
miss: it does not appear in any search for functions taking `project_root`, only
in a literal constructor grep.

Add `use devflow_core::git::git_command;` to the file's existing
`use devflow_core::…` block. This file uses fully-qualified `std::process::Command`
throughout and has deliberately never imported `Command` — do not add such an
import while you are here.

Surgical scope: seven constructor swaps, seven `.current_dir` removals, one import
added, plus the new test. Do not touch `unreachable_message`, `stale_base_message`,
or any of the message-formatting code.
  </action>

  <verify>
    <automated>[ "$(rg -v '^\s*//' crates/devflow-cli/src/preflight.rs | rg -o 'Command::new\("git"\)' | wc -l)" -eq 4 ]</automated>
    <automated>HOSTILE=$(mktemp -d) && git init -q "$HOSTILE" && GIT_DIR="$HOSTILE/.git" cargo test -p devflow --bin devflow -- preflight:: 2>&1 | tee /tmp/27-05-pf1.txt | rg -q 'test result: ok\.' && ! rg -q 'test result: ok\. 0 passed' /tmp/27-05-pf1.txt</automated>
    <automated>HOSTILE=$(mktemp -d) && git init -q "$HOSTILE" && GIT_DIR="$HOSTILE/.git" cargo test -p devflow --bin devflow -- preflight::tests::phase_reachability_resolves_caller_root_under_a_hostile_git_dir 2>&1 | rg -q 'test result: ok\. 1 passed; 0 failed'</automated>
  </verify>

  <acceptance_criteria>
    - `[ "$(rg -v '^\s*//' crates/devflow-cli/src/preflight.rs | rg -o 'Command::new\("git"\)' | wc -l)" -eq 4 ]` — seven migrated, the four owned by Task 2 still present
    - `[ "$(rg -o 'git_command\(project_root\)' crates/devflow-cli/src/preflight.rs | wc -l)" -eq 7 ]`
    - `rg -n 'use devflow_core::git::git_command' crates/devflow-cli/src/preflight.rs` prints one line
    - `[ "$(rg -o '^use std::process::Command;' crates/devflow-cli/src/preflight.rs | wc -l)" -eq 0 ]` — the file's fully-qualified convention was respected
    - `rg -n -A4 'let is_ancestor = \|' crates/devflow-cli/src/preflight.rs` shows the closure body constructing through `git_command` — the closure site was not missed
    - `[ "$(rg -o 'unwrap_or\(false\)' crates/devflow-cli/src/preflight.rs | wc -l)" -ge 4 ]` — fail-soft tails were preserved, not tightened
    - `preflight::tests::phase_reachability_resolves_caller_root_under_a_hostile_git_dir` reports `1 passed; 0 failed` under a hostile `GIT_DIR`
    - The SUMMARY records the RED output for that test against the unmigrated code and the GREEN output after
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>

  <done>All seven sites in `phase_reachability_on_base` and `base_ref_currency` — including the `is_ancestor` closure — construct through `git_command(project_root)` with every fail-soft tail intact, and a regression test proves phase reachability is judged against the caller's repository.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate the remaining four preflight sites, including the one write and the second closure</name>
  <files>crates/devflow-cli/src/preflight.rs</files>

  <read_first>
    - `crates/devflow-cli/src/preflight.rs` lines 420-560 (`base_is_checked_out_anywhere`, `fast_forward_base_ref`, `ensure_base_ref_current` and its `resolve` closure) and lines 765-800 (`breaking_commit_subjects`)
    - `crates/devflow-cli/src/preflight.rs` as edited by Task 1 — read the current state so the already-migrated shape is the reference
    - `crates/devflow-core/src/test_support.rs` — the canonical hermetic-command pattern
    - `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-PATTERNS.md` § "crates/devflow-cli/src/preflight.rs" — the closure-embedded before-shape and the note that a second closure exists around base-commit lines 525-537
  </read_first>

  <behavior>
    New test `fast_forward_base_ref_never_writes_into_a_hostile_git_dir` in `preflight.rs`'s `#[cfg(test)] mod tests`:

    - Build a real throwaway repository with a base branch at a known commit, plus a second commit to fast-forward to.
    - Build a foreign throwaway repository with a branch of the SAME name at a DIFFERENT commit, and record that commit's sha before the call.
    - Call `fast_forward_base_ref(real_repo, base, expected_old, new)` with the foreign `GIT_DIR` injected into the spawned child only (reuse `27-01`'s child-scoped technique; no `std::env::set_var`).
    - Assert BOTH directions: the real repository's ref advanced to `new`, AND the foreign repository's same-named ref is byte-identical to the sha recorded before the call. The second assertion is the load-bearing one — this is the only migrated site that WRITES, so containment must be proven, not inferred from a read succeeding.
    - Watch it fail against the unmigrated code, then migrate, then watch it pass. Record both outputs in the SUMMARY, including the foreign repository's before/after sha.
  </behavior>

  <action>
Migrate the four production git invocations left in `preflight.rs`. At the phase
base commit:

| Enclosing item | Base-commit line | Argv | Path expression |
|---|---|---|---|
| `base_is_checked_out_anywhere` | 426 | worktree/branch check | `project_root` |
| `fast_forward_base_ref` | 457 | **`update-ref refs/heads/<base> <new> <expected_old>`** — the only WRITE on this surface | `project_root` |
| `ensure_base_ref_current` | 530 | `rev-parse --verify --quiet <ref>` — **inside the `resolve` closure** (base-commit 529-537) | `project_root` |
| `breaking_commit_subjects` | 778 | commit-subject read for the major-bump check | **`execution_root`** |

Same transformation as Task 1: replace `std::process::Command::new("git")` and its
separate `.current_dir(<expr>)` with a single `git_command(<expr>)`, keeping the
argv arrays, `.output()`, and each site's fail-soft tail unchanged.

Two site-specific cautions:

1. **`breaking_commit_subjects` uses `execution_root`, not `project_root`.** It is
   the only site in this file that does. Pass `execution_root` — substituting
   `project_root` would change which tree the major-bump check reads commit
   subjects from, in a file where the other ten sites make `project_root` the
   reflexive choice. This is the same 18c execution-root distinction that
   `staleness.rs` carries.
2. **`fast_forward_base_ref` is a write.** `git update-ref refs/heads/<base> <new> <expected_old>`
   mutates a ref. Its `expected_old` argument is a compare-and-swap guard against
   the *same* repository's current value, which under a hostile `GIT_DIR` is the
   foreign repository's value — so the guard does not protect against
   cross-repository misdirection at all, only against a concurrent local update.
   The environment scrub is what makes the target repository correct in the first
   place. Do not alter the `update-ref` argv, do not remove `expected_old`, and do
   not change the `.map(|out| out.status.success()).unwrap_or(false)` return.

`use devflow_core::git::git_command;` was already added by Task 1; do not add a
second import. If clippy reports an unused import after these edits, delete that
specific import rather than silencing the lint.

Surgical scope: four constructor swaps, four `.current_dir` removals, plus the new
test.
  </action>

  <verify>
    <automated>[ "$(rg -v '^\s*//' crates/devflow-cli/src/preflight.rs | rg -o 'Command::new\("git"\)' | wc -l)" -eq 0 ]</automated>
    <automated>[ "$(rg -o 'git_command\(project_root\)' crates/devflow-cli/src/preflight.rs | wc -l)" -eq 10 ] && [ "$(rg -o 'git_command\(execution_root\)' crates/devflow-cli/src/preflight.rs | wc -l)" -eq 1 ]</automated>
    <automated>HOSTILE=$(mktemp -d) && git init -q "$HOSTILE" && GIT_DIR="$HOSTILE/.git" cargo test -p devflow --bin devflow -- preflight:: 2>&1 | tee /tmp/27-05-pf2.txt | rg -q 'test result: ok\.' && ! rg -q 'test result: ok\. 0 passed' /tmp/27-05-pf2.txt</automated>
    <automated>HOSTILE=$(mktemp -d) && git init -q "$HOSTILE" && GIT_DIR="$HOSTILE/.git" cargo test -p devflow --bin devflow -- preflight::tests::fast_forward_base_ref_never_writes_into_a_hostile_git_dir 2>&1 | rg -q 'test result: ok\. 1 passed; 0 failed'</automated>
  </verify>

  <acceptance_criteria>
    - `[ "$(rg -v '^\s*//' crates/devflow-cli/src/preflight.rs | rg -o 'Command::new\("git"\)' | wc -l)" -eq 0 ]` — all eleven migrated
    - `[ "$(rg -o 'git_command\(project_root\)' crates/devflow-cli/src/preflight.rs | wc -l)" -eq 10 ]` and `[ "$(rg -o 'git_command\(execution_root\)' crates/devflow-cli/src/preflight.rs | wc -l)" -eq 1 ]` — the 10/1 split proves `breaking_commit_subjects` was not silently repointed at `project_root`
    - `rg -n -A4 'let resolve = \|' crates/devflow-cli/src/preflight.rs` shows the closure body constructing through `git_command` — the second closure site was not missed
    - `rg -n '"update-ref"' crates/devflow-cli/src/preflight.rs` still shows `update-ref`, the base ref, `new`, and `expected_old` in that order — the compare-and-swap argv is unchanged
    - `preflight::tests::fast_forward_base_ref_never_writes_into_a_hostile_git_dir` reports `1 passed; 0 failed` under a hostile `GIT_DIR`, and its assertions cover BOTH the real repository advancing and the foreign repository's ref being unchanged
    - The SUMMARY records the RED output for that test against the unmigrated code, the GREEN output after, and the foreign repository's before/after sha
    - Under a hostile `GIT_DIR`, `cargo test -p devflow --bin devflow -- preflight::` reaches `test result: ok.` with `0 failed` and a passed count greater than zero
    - The passed count under the hostile run equals the passed count in a normal-environment run of the same filter
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>

  <done>All eleven of `preflight.rs`'s git invocations construct through `git_command`, both closure sites were found and migrated, `breaking_commit_subjects` kept `execution_root`, and a regression test proves the one write operation cannot land in a repository the operator never named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent process env -> `devflow` process | Inherited `GIT_DIR` / `GIT_WORK_TREE` from a hook, `rebase --exec`, `bisect run`, `submodule foreach`, or a polluted shell |
| `preflight` -> spawned `git` child (read) | Every gate decision `devflow start` makes before forking a worktree |
| `preflight` -> spawned `git` child (write) | `fast_forward_base_ref`'s `git update-ref` — the only ref mutation on this surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | `preflight::{phase_reachability_on_base, base_ref_currency, base_is_checked_out_anywhere, ensure_base_ref_current, breaking_commit_subjects}` | high | mitigate | All read sites constructed via `git_command` (D-01). A preflight that answers from a foreign repository reports success and lets `devflow start` fork a run against a phase, base ref, or commit range that does not exist locally — worse than no preflight, because it is trusted. Asserted by a comment-filtered count of zero unscrubbed constructions plus a hostile-`GIT_DIR` `preflight::` run reaching `0 failed`. |
| T-27-12 | Tampering | `preflight::fast_forward_base_ref` — `git update-ref` | critical | mitigate | The only WRITE among the 41 sites. Under a hostile `GIT_DIR` this advances a branch ref inside a repository the operator never named, and its `expected_old` compare-and-swap guard offers no protection because it compares against that same foreign repository's value. Scrubbed at the constructor; containment proven in both directions by `fast_forward_base_ref_never_writes_into_a_hostile_git_dir`, which asserts the foreign repository's ref is byte-identical before and after. |
| T-27-13 | Tampering | The two closure-embedded sites (`base_ref_currency`'s `is_ancestor`, `ensure_base_ref_current`'s `resolve`) | high | mitigate | These do not surface in any function-signature search and are the most likely sites to be silently skipped by a migration that greps for enclosing functions rather than for the constructor literal. Located by literal grep, count reconciled to eleven before editing, and each asserted individually by a grep of the closure body. |
| T-27-14 | Tampering | The `execution_root` / `project_root` split at `breaking_commit_subjects` | medium | mitigate | Ten of eleven sites use `project_root`, making it the reflexive choice; substituting it at the eleventh would silently change which tree the major-bump check reads. Asserted by a 10/1 source count rather than by review. |
| T-27-05 | Tampering | `crates/devflow-cli/build.rs` | low | accept | D-02 — out of scope; this plan does not touch it. |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new crates; `27-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. `Cargo.toml` is unchanged by this plan. |
</threat_model>

<verification>
- Zero unscrubbed production git constructions remain (comment-filtered count).
- 10 `git_command(project_root)` + 1 `git_command(execution_root)` = 11, matching the site count exactly.
- Both closure bodies verified individually by grep.
- `preflight::` suite reaches `0 failed` under a hostile `GIT_DIR`, with the passed count matching a normal-environment run.
- The `update-ref` compare-and-swap argv unchanged; write containment proven in both directions.
- clippy `-D warnings` and `fmt --check` clean.
</verification>

<success_criteria>
- `preflight.rs`: 0 remaining, all 11 sites scrubbed.
- Both closure-embedded sites found and migrated.
- `breaking_commit_subjects` kept `execution_root`.
- Two new regression tests, including one proving write containment for `fast_forward_base_ref`.
</success_criteria>

<output>
Create `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-05-SUMMARY.md` when done, including RED and GREEN output for both new tests and the foreign repository's before/after sha from the write-containment test.
</output>
