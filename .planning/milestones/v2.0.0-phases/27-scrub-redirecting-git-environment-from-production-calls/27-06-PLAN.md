---
phase: 27-scrub-redirecting-git-environment-from-production-calls
plan: 06
type: execute
wave: 3
depends_on: ["27-02", "27-03", "27-04", "27-05"]
files_modified:
  - .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-VALIDATION.md
  - .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md
autonomous: true
requirements: [D-01, D-02, D-03]
must_haves:
  truths:
    - "D-01: a workspace-wide, comment-filtered sweep of `crates/*/src/` finds zero unscrubbed production git constructions — the 41-site figure is confirmed closed by measurement, not by summing per-plan claims."
    - "D-03: both hostile-`GIT_DIR` acceptance commands reach a literal `test result:` line with `0 failed`. A timeout is not a pass."
    - "D-03: the re-measured pre-migration baselines are recorded alongside the post-migration results, so the red-to-green claim is evidenced rather than asserted — and the stale `37` figure from ROADMAP.md and 27-CONTEXT.md is superseded in writing."
    - "D-02: `crates/devflow-cli/build.rs` is byte-identical across the whole phase diff, proving the indirect-path mitigation in 27-04 was implemented at DevFlow's own spawn edge and never by editing build.rs."
    - "Every production spawn edge that can indirectly reach git — not just literal git constructions — is enumerated with a file, a line, and a disposition. RESEARCH.md Assumption A2 (the exhaustiveness gap) is closed by a census or explicitly escalated, never left implicit."
  artifacts:
    - "27-SPAWN-CENSUS.md — every production `Command::new(...)` spawn edge in `crates/*/src/`, classified as direct-git / indirect-git-reaching / cannot-reach-git, with a disposition per row"
    - "27-VALIDATION.md — Per-Task Verification Map filled in, Validation Sign-Off checked, status advanced from draft"
  key_links:
    - "27-VALIDATION.md § Per-Task Verification Map -> the actual task IDs and commands from 27-01 … 27-05"
    - "27-SPAWN-CENSUS.md -> a backlog entry, if any production spawn edge outside this phase's scope is found to reach git"
---

<!-- planner-discipline-allow: Command::new("git") -->
<!-- planner-discipline-allow: Command::new("sh") -->

<objective>
Close the phase by measurement rather than by summing per-plan claims: sweep the
whole workspace for anything still unscrubbed, run both hostile-`GIT_DIR`
acceptance commands to a real `test result:` line, and record the evidence.

Purpose: every prior plan asserted a per-file count. This plan asks the different
question — is there anything *nobody planned for*. That question has a known,
already-flagged answer waiting: `27-RESEARCH.md` Assumption A2 states that the
"only indirect git-reaching site" claim was NOT exhaustively proven across the
workspace, and rates the risk MEDIUM precisely because a missed path would
undermine D-01 silently until an incident.

Output: a spawn-edge census, filled-in validation evidence, and either a clean
bill or a filed, evidenced follow-up.
</objective>

## Recorded corrections this plan must publish

The stale-figure correction and the probe-fallback skip are recorded in full in
`27-01-PLAN.md` § "Phase-level recorded decisions and corrections". This plan is
where they get published as measured evidence:

- **The acceptance figure is 98, not 37.** ROADMAP.md § "Phase 27" and
  `27-CONTEXT.md` D-03 both say 37; that predates several phases. Re-measured at
  HEAD `6350798`: 54 (core) + 44 (cli, `pipeline_gate`/`pipeline_outcomes`
  excluded) = 98. Re-measure again here against the phase base commit rather than
  trusting either number.
- **Spec-less probe fallback: skipped, visibly.** `spec-less probe fallback
  skipped: phase has no requirement IDs to probe (visible skip).` Carry this line
  into `27-VALIDATION.md` so the skip stays recorded past this phase.
- **RESEARCH Open Question #1 was decided IN SCOPE** — see `27-04-PLAN.md` for the
  full rationale. Confirm the mitigation shipped and that `build.rs` is untouched.
- **RESEARCH Open Question #2 remains open** — `pipeline_gate` /
  `pipeline_outcomes` did not terminate under a hostile `GIT_DIR` during research
  and the root cause was not diagnosed. Re-measure post-migration and record the
  result as a finding, NOT as an acceptance criterion.

## Artifacts this plan produces

| Artifact | Kind |
|---|---|
| `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md` | new evidence document |
| Updated `27-VALIDATION.md` (Per-Task Verification Map rows, Validation Sign-Off, `status`) | modified |

No source symbols. All source changes belong to `27-01` … `27-05`.

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-CONTEXT.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-RESEARCH.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-VALIDATION.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-01-SUMMARY.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-02-SUMMARY.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-03-SUMMARY.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-04-SUMMARY.md
@.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace spawn-edge census — close RESEARCH Assumption A2 or escalate it</name>
  <files>.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md</files>

  <precondition>All of `27-02`, `27-03`, `27-04`, and `27-05` are merged into this working tree. If any of their SUMMARYs is missing, STOP — a census run against a partially-migrated tree produces a number that means nothing.</precondition>

  <read_first>
    - `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-RESEARCH.md` § Assumptions Log (specifically A2, the exhaustiveness gap and its MEDIUM risk rating) and § Common Pitfalls Pitfall 1 (why a literal-git grep is a floor, not a ceiling)
    - `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-04-PLAN.md` § "Recorded decision: RESEARCH Open Question #1 — IN SCOPE" — the reasoning template for classifying an indirect spawn edge, and the D-02 boundary it preserves
    - `crates/devflow-core/src/test_support.rs` — `hermetic_command`'s doc comment, which states in the project's own words that the redirecting variables are inherited all the way down a process tree, so scrubbing only direct git calls leaves that path open. This is the census's governing principle.
    - The four production non-`commands.rs` `sh` spawn sites named in the action below, each read in context
  </read_first>

  <action>
Two sweeps and one document.

**Sweep A — direct git constructions.** Confirm by measurement that the 41-site
migration is actually complete workspace-wide, rather than trusting the sum of
five per-file claims:

Search `crates/devflow-core/src` and `crates/devflow-cli/src` for the literal git
constructor, then filter out lines whose content is a comment (`//`, `///`, `//!`)
— `hermetic_command`'s own doc comment names the literal, and an unfiltered count
would be permanently self-invalidating. The filtered count must be **zero**.
Note that `crates/devflow-cli/build.rs` lives outside `src/` and is therefore
naturally excluded from this sweep, which is correct: D-02 puts it out of scope.

**Sweep B — indirect spawn edges.** Enumerate EVERY `Command::new(...)`
construction in production code (above each file's `#[cfg(test)] mod tests`
boundary) across `crates/*/src/`, whatever program it names. For each, classify:

- `direct-git` — constructs `git` itself. Should be zero after Sweep A.
- `reaches-git` — spawns a program that can invoke `git`, directly or through a
  descendant. The redirecting variables are inherited down the whole process tree.
- `cannot-reach-git` — the spawned program has no plausible path to git.

**Sites already known to exist, which the census must include and classify by
name.** These were found during planning and are NOT in `27-CONTEXT.md`'s
seven-file scope; do not treat their absence from that list as evidence they are
safe:

| Site | Base-commit line | What it spawns | Note |
|---|---|---|---|
| `crates/devflow-cli/src/commands.rs::test_cmd` | 1956 | `sh -c "cargo …"` | Already mitigated by `27-04` via `hermetic_command`. Verify, do not re-migrate. |
| `crates/devflow-core/src/monitor.rs` | 148 | `sh -c …` | Spawns the agent monitor. **Highest-consequence candidate**: the descendant is an AI agent process that performs the phase's actual git work. |
| `crates/devflow-core/src/agent.rs` | 707, 739, 778 | `sh -c …` | Below the `#[cfg(test)]` boundary at line 532 — confirm they are test code and record that, do not assume it. |
| `crates/devflow-core/src/hooks.rs` | 222 | `sh -c …` | Runs hook commands. Note explicitly that a git hook is a context where `GIT_DIR` may be set *legitimately* by git itself — this one needs judgment, not a reflexive scrub. |
| `crates/devflow-core/src/gates.rs` | 323 | `sh -c …` | Runs gate commands. |
| `crates/devflow-core/src/verify.rs` | 106 | `sh -c …` | Runs verification commands. |

**Do NOT migrate any of the `reaches-git` sites found outside this phase's scope.**
That is a deliberate boundary, and it rests on two of the three legitimate
grounds, not on difficulty:

1. **Missing information.** No source artifact characterises these sites. Deciding
   each one requires knowing what the child legitimately needs — and `hooks.rs` is
   a real counter-example, since git itself sets `GIT_DIR` when invoking a hook.
   Scrubbing blind could break correct behaviour.
2. **Context cost.** Per-site judgment across four-plus files in a wave-3
   acceptance plan is a second migration, not a verification step.

The correct output is evidence and an escalation, not a silent deferral and not an
unplanned expansion.

**The document.** Write
`.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md`
containing: the two sweep commands verbatim with their actual output; a table with
one row per production spawn edge (file, line, program, classification,
disposition, one-line rationale); an explicit statement of whether RESEARCH
Assumption A2 is CLOSED (no `reaches-git` site remains unmitigated) or OPEN (one or
more do); and, if OPEN, a proposed backlog entry — title, evidence, severity, and
a note that it is the same threat class as 999.39 one process deeper.

If the census finds any unmitigated `reaches-git` site, say so plainly in the
SUMMARY as a finding that needs an operator decision. Do not soften it, and do not
let the phase's own green acceptance numbers imply the class is closed when the
census says it is not.
  </action>

  <verify>
    <automated>[ "$(rg --no-heading -n 'Command::new\("git"\)' crates/devflow-core/src crates/devflow-cli/src | rg -v ':\s*(//|///|//!)' | wc -l)" -eq 0 ]</automated>
    <automated>test -f .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md && rg -q 'Assumption A2' .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md</automated>
    <automated>rg -q 'monitor\.rs|hooks\.rs|gates\.rs|verify\.rs' .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md</automated>
    <automated>rg -qE 'A2 (is )?(CLOSED|OPEN)' .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-SPAWN-CENSUS.md</automated>
  </verify>

  <acceptance_criteria>
    - `[ "$(rg --no-heading -n 'Command::new\("git"\)' crates/devflow-core/src crates/devflow-cli/src | rg -v ':\s*(//|///|//!)' | wc -l)" -eq 0 ]` — zero unscrubbed direct git constructions workspace-wide, comment lines excluded so a doc comment cannot mask a real site
    - `27-SPAWN-CENSUS.md` exists and contains both sweep commands with their verbatim output
    - The census table has one row per production spawn edge, each with a file, a line number, the program spawned, a classification of `direct-git` / `reaches-git` / `cannot-reach-git`, and a disposition
    - `monitor.rs`, `hooks.rs`, `gates.rs`, and `verify.rs` each appear by name with a classification — none is omitted
    - `agent.rs`'s three sites are recorded with an explicit statement of whether they sit above or below the `#[cfg(test)]` boundary at line 532, verified rather than assumed
    - `commands.rs::test_cmd` is recorded as already mitigated by `27-04`, with the `hermetic_command` call site cited
    - The document states literally `A2 is CLOSED` or `A2 is OPEN` — no hedged third option
    - If any `reaches-git` site is unmitigated, a proposed backlog entry is present with title, evidence, and severity, and the SUMMARY names the finding without softening it
    - No source file under `crates/` was modified by this task: `git diff --stat HEAD -- crates/` prints nothing
  </acceptance_criteria>

  <done>Zero unscrubbed direct git constructions remain workspace-wide by measurement, every production spawn edge is enumerated and classified in `27-SPAWN-CENSUS.md`, and RESEARCH Assumption A2 is stated as CLOSED or OPEN with evidence either way.</done>
</task>

<task type="auto">
  <name>Task 2: Phase acceptance run and validation evidence</name>
  <files>.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-VALIDATION.md</files>

  <precondition>Task 1's Sweep A returned zero. If direct git constructions remain, the acceptance numbers below are measuring a partially-migrated tree and must not be recorded as the phase result.</precondition>

  <read_first>
    - `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-VALIDATION.md` — the whole file. Its Per-Task Verification Map, Sampling Rate, Wave 0 Requirements, and Validation Sign-Off checklist are the contract this task fills in.
    - `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-RESEARCH.md` § Validation Architecture and § Common Pitfalls Pitfall 2 (why a bare workspace run is unusable here) and Pitfall 4 (why `--features test-support` is mandatory for a `-p devflow-core` run)
    - The five SUMMARY files from `27-01` … `27-05`, for the per-task IDs and commands that populate the Per-Task Verification Map
  </read_first>

  <action>
Measure, then record. Do not record a number you did not observe in this run.

**Step 1 — re-measure the pre-migration baseline.** The `98` figure was measured at
HEAD `6350798`, before this phase's own commits. From a detached checkout or
`git stash`-equivalent of the phase base commit, re-run both scoped commands under
a hostile `GIT_DIR` and record the actual failure counts. If re-measuring against
the base commit is impractical in this working tree, say so explicitly and cite
`27-RESEARCH.md`'s measured 54 / 44 / 98 as the baseline of record, dated and
attributed — do not silently substitute the stale `37`, and do not present a
research-era number as a fresh measurement.

**Step 2 — run both acceptance commands.** Using the phase's hostile-`GIT_DIR`
harness (a throwaway `mktemp -d` repository, `GIT_DIR` set for the `cargo`
invocation only):

- `cargo test -p devflow-core --features test-support` — target: `0 failed`
- `cargo test -p devflow --bin devflow -- --skip pipeline_gate --skip pipeline_outcomes` — target: `0 failed`

Both must print a literal `test result:` line. **A timeout is not a pass** — if
either command does not terminate, record it as a FAILURE with the elapsed time,
not as an inconclusive result. `--features test-support` on the first command is
mandatory: without it, `devflow-core`'s integration test targets fail to compile
with `E0433` (RESEARCH Pitfall 4), which is a compile error, not a test result.
Never substitute a bare `cargo test --workspace` here — it did not terminate at
either 240s or 480s during research.

**Step 3 — the residual (RESEARCH Open Question #2).** Run `pipeline_gate` and
`pipeline_outcomes` under a hostile `GIT_DIR`, together, with an explicit bounded
timeout of 300 seconds. Record exactly one of three outcomes: they now complete
cleanly (a plausible bonus effect of the migration, since both call through
`git.rs`), they still do not complete, or they complete with failures. This is a
**finding, not an acceptance criterion** — the phase does not pass or fail on it.
If they still do not complete, file it as a backlog follow-up with the elapsed
time and the exact command.

**Step 4 — the ordinary-environment gates.** In a normal environment:
`cargo test --workspace` (`0 failed`), `cargo clippy --workspace --all-targets -- -D warnings`,
and `cargo fmt --check`. The workspace run is safe here precisely because the
environment is not hostile.

**Step 5 — the D-02 boundary check.** `git diff --stat <phase-base>..HEAD -- crates/devflow-cli/build.rs`
must print nothing. This is the plan-time verification `27-RESEARCH.md` § Phase
Requirement -> Test Map assigns to D-02, and it is the proof that `27-04`'s
indirect-path mitigation was implemented at DevFlow's own spawn edge rather than by
editing `build.rs`.

**Step 6 — update `27-VALIDATION.md`.** Fill in the Per-Task Verification Map: real
task IDs, plan numbers, and waves from the five SUMMARYs, replacing every `TBD`;
mark each row's status. Replace the stale baseline note with this run's measured
numbers. Add the recorded skip line verbatim: `spec-less probe fallback skipped:
phase has no requirement IDs to probe (visible skip).` Check the Validation
Sign-Off boxes that this run actually satisfies and leave unsatisfied ones
unchecked with a one-line reason. Set `nyquist_compliant: true` only if every
`<automated>` verify across all six plans genuinely ran and passed; otherwise leave
it `false` and say which one did not. Leave `status: draft` — advancing it to
`validated` belongs to `/gsd-validate-phase`, not to this plan.
  </action>

  <verify>
    <automated>HOSTILE=$(mktemp -d) && git init -q "$HOSTILE" && GIT_DIR="$HOSTILE/.git" timeout 600 cargo test -p devflow-core --features test-support 2>&1 | tee /tmp/27-06-core.txt | rg -q 'test result: ok\.' && ! rg -q 'test result: FAILED' /tmp/27-06-core.txt</automated>
    <automated>HOSTILE=$(mktemp -d) && git init -q "$HOSTILE" && GIT_DIR="$HOSTILE/.git" timeout 600 cargo test -p devflow --bin devflow -- --skip pipeline_gate --skip pipeline_outcomes 2>&1 | tee /tmp/27-06-cli.txt | rg -q 'test result: ok\.' && ! rg -q 'test result: FAILED' /tmp/27-06-cli.txt</automated>
    <automated>cargo test --workspace 2>&1 | tee /tmp/27-06-normal.txt | rg -q 'test result: ok\.' && ! rg -q 'test result: FAILED' /tmp/27-06-normal.txt</automated>
    <automated>cargo clippy --workspace --all-targets -- -D warnings && cargo fmt --check</automated>
    <automated>[ "$(git diff --stat $(git merge-base HEAD develop)..HEAD -- crates/devflow-cli/build.rs | wc -l)" -eq 0 ]</automated>
    <automated>! rg -q 'TBD' .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-VALIDATION.md</automated>
    <automated>rg -q 'spec-less probe fallback skipped' .planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-VALIDATION.md</automated>
  </verify>

  <acceptance_criteria>
    - Under a hostile `GIT_DIR`, `cargo test -p devflow-core --features test-support` prints a literal `test result:` line with `0 failed` (baseline of record: 54 failed / 352 passed)
    - Under a hostile `GIT_DIR`, `cargo test -p devflow --bin devflow -- --skip pipeline_gate --skip pipeline_outcomes` prints a literal `test result:` line with `0 failed` (baseline of record: 44 failed / 139 passed)
    - Neither acceptance command timed out. If either did, it is recorded as a FAILURE with elapsed time — not as inconclusive
    - The SUMMARY records both commands' verbatim final `test result:` lines, and the baseline they are being compared against, with its source and date
    - The SUMMARY states the corrected figures 54 / 44 / 98 and explicitly notes that ROADMAP.md § Phase 27 and `27-CONTEXT.md` D-03 both carry the stale `37`
    - `pipeline_gate` + `pipeline_outcomes` under a hostile `GIT_DIR` with a 300s bound is recorded as exactly one of: completed clean, completed with failures, or did not complete — with elapsed time. Recorded as a FINDING; the phase does not pass or fail on it
    - In a normal environment, `cargo test --workspace` reaches `test result:` with `0 failed` on every target
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0 and `cargo fmt --check` exits 0
    - `git diff --stat <phase-base>..HEAD -- crates/devflow-cli/build.rs` prints nothing (D-02)
    - `27-VALIDATION.md` contains zero remaining `TBD` strings in its Per-Task Verification Map, and every row names a real task, plan, wave, and command
    - `27-VALIDATION.md` contains the literal line `spec-less probe fallback skipped: phase has no requirement IDs to probe (visible skip).`
    - `27-VALIDATION.md`'s `status` is still `draft` — advancing it is `/gsd-validate-phase`'s job, not this plan's
    - `nyquist_compliant` is set to `true` only if every `<automated>` verify across all six plans actually ran and passed; otherwise it stays `false` with a named exception
    - No source file under `crates/` was modified by this task: `git diff --stat HEAD -- crates/` prints nothing
  </acceptance_criteria>

  <done>Both hostile-`GIT_DIR` acceptance commands reach a real `test result:` line with `0 failed`, the ordinary-environment gates are green, `build.rs` is provably untouched, the `pipeline_gate`/`pipeline_outcomes` residual is recorded as a finding with a measured outcome, and `27-VALIDATION.md` carries this run's evidence instead of `TBD` placeholders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| parent process env -> `devflow` process | The boundary the whole phase exists to defend. This plan verifies the defence holds by measurement rather than by assertion. |
| `devflow` -> any spawned child (git or otherwise) | The census's subject: the redirecting variables are inherited down the entire process tree, so the relevant surface is every spawn edge, not only the git-named ones. |
| phase claims -> recorded evidence | A softer boundary, and a real one on this project: a self-reported "tests green" has been wrong here before. Every number in the SUMMARY must come from output observed in this run. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-15 | Tampering | Any production spawn edge missed by the 41-site literal grep | high | mitigate | `27-RESEARCH.md` Assumption A2 rates this MEDIUM only because it was unmeasured; a missed indirect path leaves D-01's guarantee false while every per-file count reads green. Closed by the Sweep B census, which enumerates every production `Command::new(...)` regardless of program and forces a `direct-git` / `reaches-git` / `cannot-reach-git` verdict on each, with `monitor.rs`, `hooks.rs`, `gates.rs`, and `verify.rs` named explicitly so none can be omitted by inattention. |
| T-27-16 | Repudiation | The phase's own acceptance claim | high | mitigate | A timeout that reads as "no failures" would let the phase claim red-to-green without evidence — and `27-RESEARCH.md` Pitfall 2 proves the unscoped workspace run times out under a hostile `GIT_DIR`. Both acceptance commands must print a literal `test result:` line; a timeout is explicitly a FAILURE; baselines must be re-measured or cited with source and date rather than restated from a stale document. |
| T-27-02 | Tampering / Elevation of Privilege | The phase's contribution to a future same-repository guard | critical | mitigate | 999.25's `mutating_project_root` will be rebuilt on top of this codebase. If any site were left unscrubbed, that guard would be correct-by-construction in name only. The workspace-wide comment-filtered sweep returning zero is what makes the inheritance claim true. |
| T-27-05 | Tampering | `crates/devflow-cli/build.rs` | low | accept | D-02. Verified byte-identical across the full phase diff, which simultaneously proves `27-04`'s indirect-path mitigation respected the boundary. |
| T-27-17 | Denial of Service | `pipeline_gate` / `pipeline_outcomes` under a hostile `GIT_DIR` | medium | transfer | Reproducible non-termination, root cause undiagnosed (`27-RESEARCH.md` Open Question #2). Out of D-03's mechanism-proof scope. Measured and recorded here with a bounded 300s run; if still unresolved it transfers to a backlog follow-up rather than blocking this phase. |
| T-27-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new crates anywhere in this phase; `27-RESEARCH.md` § Package Legitimacy Audit records the gate as not applicable. This plan modifies no source and no manifest. |
</threat_model>

<verification>
- Workspace-wide comment-filtered count of direct git constructions: 0.
- `27-SPAWN-CENSUS.md` exists, classifies every production spawn edge, and states `A2 is CLOSED` or `A2 is OPEN`.
- Both hostile-`GIT_DIR` acceptance commands print `test result:` with `0 failed`; neither timed out.
- Normal-environment `cargo test --workspace`, clippy `-D warnings`, and `fmt --check` all clean.
- `crates/devflow-cli/build.rs` byte-identical across the phase diff.
- `27-VALIDATION.md` free of `TBD`, carrying this run's numbers and the recorded probe-fallback skip.
</verification>

<success_criteria>
- The 41-site migration is confirmed complete by workspace measurement, not by summing per-plan claims.
- RESEARCH Assumption A2 is resolved to CLOSED or escalated as OPEN with a proposed backlog entry.
- The D-03 red-to-green claim is evidenced with observed before/after numbers, and the stale `37` figure is superseded in writing.
- D-02's boundary is proven by diff.
- RESEARCH Open Question #2 is measured and recorded as a finding, not silently dropped and not treated as a gate.
</success_criteria>

<output>
Create `.planning/phases/27-scrub-redirecting-git-environment-from-production-calls/27-06-SUMMARY.md` when done. It must contain: both acceptance commands' verbatim final `test result:` lines; the baseline being compared against with its source and date; the `pipeline_gate`/`pipeline_outcomes` residual outcome with elapsed time; and, if `27-SPAWN-CENSUS.md` reports `A2 is OPEN`, the finding stated plainly and unsoftened as needing an operator decision.
</output>