# 14-02: Coarse checkout lock + sequentagent behind the monitor (14a/14b)

**Wave 2** — blocked on 14-01 (per-phase state + lock model).

## Tasks

1. **Coarse project lock for main-checkout mutations** (`lock.rs`, `main.rs`)
   - `lock::acquire_project(root)` on `.devflow/lock-project`, plus a
     blocking variant with bounded backoff (seconds, not gate-days) for
     callers that should wait out a sibling's short critical section.
   - Held around: `finish_workflow`'s after-ship hook batch, `transition`'s
     Validate→Ship hook batch (DocsUpdate/ChangelogAppend commit to the main
     checkout), and sequentagent's `ensure_branch`/integrate/push sections.
   - Never held across a gate wait.

2. **14b: sequentagent behind monitor-owned execution** (`monitor.rs`,
   `main.rs`, `agent.rs`)
   - `spawn_monitor` grows an on-exit choice: advance (normal pipeline) or
     no-op (sequentagent) — one script builder, the advance tail optional.
   - `run_agent_blocking` = cleanup files → spawn no-advance monitor → block
     on the exit file (error if the monitor dies without writing it) →
     `evaluate_layer1` + exit-code gate exactly as today.
   - sequentagent acquires the per-phase lock for its whole run (excludes a
     concurrent monitored run of the same phase).
   - Delete the dead sync path: `agent::launch_agent`,
     `agent::capture_agent_output`, `AgentCapture`, and the `lib.rs` export.
   - `cron-instructions.json` re-check: keyed per phase
     (`cron-instructions-{NN}.json`) with legacy-name read compat;
     `status`/`recover` handle both.

## Tests

- concurrent `finish_workflow` git sections serialize on the project lock
  (second caller blocks/retries; both version bumps land).
- sequentagent stub-agent run captures stdout/exit via the monitor files and
  still gates on a nonzero exit without a marker (existing behavior kept).
- sequentagent fails fast with Contended when the phase lock is held.
- sync-path symbols are gone (compile-time).

## Acceptance

13-DEFERRED-CR-03 scenario 2 closed: two phases finishing concurrently
serialize their main-checkout git ops; sequentagent is inside the same
locking model instead of outside it.
