# 14-03: Multi-phase status/recover + parallel acceptance test (14a closeout)

**Wave 3** — blocked on 14-01/14-02.

## Tasks

1. **`devflow status` enumerates all active phases** — one block per state
   from `workflow::list_states`, sorted by phase; idle output unchanged when
   none.
2. **`devflow recover` inspects/cleans every phase** — `recover::inspect_all`
   returns one `RecoveryStatus` per active state; `--clean` clears each
   state file (plus stale locks and cron-instructions, as today).
3. **Parallel-safety integration test** (phase7_cli-style, real git repo,
   stub agents): interleave two phases through start→advance and assert both
   stage machines advanced independently, no shared-file clobbering, and
   `status` lists both.

## Acceptance

All three 13-DEFERRED-CR-03 acceptance criteria demonstrably pass.
