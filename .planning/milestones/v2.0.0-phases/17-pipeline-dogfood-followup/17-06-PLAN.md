---
phase: 17-pipeline-dogfood-followup
plan: 06
type: execute
wave: 5
depends_on: ["17-01", "17-04", "17-05"]
files_modified:
  - crates/devflow-core/src/state.rs
  - crates/devflow-core/src/mode.rs
  - crates/devflow-cli/src/main.rs
  - .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [17b, 17c, 17d]
must_haves:
  truths:
    - "state.infra_failures resets to 0 inside transition() alongside state.consecutive_failures, so the ceiling in mode::MAX_INFRA_FAILURES bounds a stuck loop within the phase, not the phase's lifetime (CR-01 fix, closes VERIFICATION.md Truth 4)"
    - "a regression test exercises transition() with infra_failures one below the ceiling, asserts the reset (in-memory AND persisted), then drives a subsequent infra fault and asserts it starts counting from 1, not the pre-transition count"
    - "embedded_commit_is_stale returns Fresh only when embedded_commit is an EXACT match for the current HEAD (via a new git rev-parse HEAD comparison); a STRICT ancestor of HEAD (merge-base --is-ancestor exits 0 but embedded_commit != HEAD) now returns Stale (WR-01 fix, closes VERIFICATION.md Truth 10)"
    - "a regression test reproduces the verifier's exact linear two-commit, clean-tree fixture (no dirty files, so the mtime arm never runs) and asserts combined_staleness reports Stale and enforce_build_staleness hard-blocks a self-dogfood workspace in that scenario"
    - "the pre-existing embedded_commit_is_stale_maps_ancestry_exit_codes test is corrected: its `base` assertion changes from Fresh to Stale (the assertion previously encoded the WR-01 bug), and a genuine HEAD-equality Fresh case is added"
    - "17-VERIFICATION.md frontmatter records an attributed `overrides:` entry accepting the AC-4 scope narrowing, in the exact shape the verifier proposed in its Gaps Summary — Gap 1 and Gap 2's existing `status: failed` gap entries are left untouched (they close via re-verification of the code fix, not via override)"
    - "ROADMAP.md's Phase 17 Requirements line no longer implies AC-4 is unconditionally satisfied — it states the narrowed scope (plan-interactivity + Ship-scoped gh-auth only) and references the accepted override"
  artifacts:
    - crates/devflow-core/src/state.rs
    - crates/devflow-core/src/mode.rs
    - crates/devflow-cli/src/main.rs
    - .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md
    - .planning/ROADMAP.md
  key_links:
    - "transition() -> state.infra_failures = 0 (new statement, mirrors the existing state.consecutive_failures = 0 reset one line above it)"
    - "embedded_commit_is_stale's exit-0 branch -> run_git_stdout(project_root, [\"rev-parse\", \"HEAD\"]) (new equality check gating Fresh vs. Stale)"
    - "17-VERIFICATION.md frontmatter overrides: entry -> ROADMAP.md's Phase 17 Requirements line (both must describe the same narrowed AC-4 scope)"
  prohibitions:
    - statement: "MUST NOT edit Gap 1 (CR-01) or Gap 2 (WR-01)'s existing `gaps:` entries or their `status: failed` values in 17-VERIFICATION.md — those close only when a future re-verification pass confirms the code fix; this plan adds an `overrides:` entry for Gap 3 only, additive, never touching the other two gap records"
      status: recalled
      verification: flagged
    - statement: "MUST NOT implement a security-artifact preflight check or a built-in adapter reviewer-set enforcement — Gap 3's disposition is to record the accepted scope narrowing, not to build the deferred checks"
      status: recalled
      verification: flagged
---

<objective>
Close the two functional gaps (CR-01, WR-01) and the one disclosed scope-narrowing gap (AC-4) found by 17-VERIFICATION.md's initial verification pass (score 9/12, `status: gaps_found`).

Purpose: `state.infra_failures` never resets, turning a "stuck loop" ceiling into a phase-lifetime counter that falsely hard-aborts long phases with several well-spaced, cleanly-resolved infra faults (Truth 4). The self-dogfood staleness gate misses the single most common real staleness case — a clean-tree binary built from a commit that is a strict ancestor of (but not equal to) HEAD — which is the exact Phase 16 "committed, forgot to rebuild" incident this phase exists to catch (Truth 10). AC-4's security-artifact and reviewer-set sub-checks were deliberately deferred during cross-AI review but ROADMAP.md's Requirements line does not disclose the narrowing (Truth 8, partial).

Output: `transition()` resets both fault counters; `embedded_commit_is_stale` distinguishes exact-HEAD-match from strict-ancestor; two new regression tests plus one corrected pre-existing test; an attributed override recorded in 17-VERIFICATION.md; ROADMAP.md's Requirements line updated to match the narrowed AC-4 scope.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md
@.planning/phases/17-pipeline-dogfood-followup/17-01-SUMMARY.md
@.planning/phases/17-pipeline-dogfood-followup/17-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 — reset infra_failures on every successful transition</name>
  <files>crates/devflow-core/src/state.rs, crates/devflow-core/src/mode.rs, crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-cli/src/main.rs (transition() lines 1612-1635 — the fix site; handle_infra_outcome 1278-1294; gate_or_abort_infra 1296-1318; handle_rate_limited_outcome 1320-1373; existing tests infra_ceiling_aborts_instead_of_gating ~4135-4156, rate_limited_at_infra_ceiling_stops_resuming_and_aborts ~4200-4231, resource_killed_on_validate_bumps_infra_not_consecutive_failures ~4103-4133 for the test idioms this task extends; the ENV_MUTEX pattern and its unsafe set_var/remove_var usage at ~3140 and ~4897-4960 for the PATH-clearing safety mechanism this task's new test needs)
    - crates/devflow-core/src/state.rs (infra_failures field doc comment, lines 34-41)
    - crates/devflow-core/src/mode.rs (MAX_INFRA_FAILURES doc comment, lines 20-30)
  </read_first>
  <behavior>
    - transition() sets state.infra_failures = 0 in the same statement group as state.consecutive_failures = 0, before workflow::save_state persists it
    - a new test asserts: starting from infra_failures == MAX_INFRA_FAILURES - 1, a call to transition() resets infra_failures to 0 both in the returned in-memory State and in the persisted state.json (reload via workflow::load_state)
    - the same test then drives handle_infra_outcome for a fresh infra fault on the post-transition stage and asserts infra_failures == 1 (not 5), proving the fault starts counting from zero rather than resuming the pre-transition count toward a false premature abort
    - the new test clears PATH (scoped under the existing ENV_MUTEX guard, restored immediately after the transition() call) before calling transition(), because this host genuinely has `claude`/`codex`/`opencode` on PATH — without clearing it, transition()'s downstream launch_stage would try to actually spawn a real agent CLI subprocess, which this test must never do
    - state.rs's infra_failures doc comment and mode.rs's MAX_INFRA_FAILURES doc comment both state that the counter resets to 0 on every successful stage transition, so the "stuck loop, not phase lifetime" language they already contain is now accurate rather than aspirational
  </behavior>
  <action>
    In crates/devflow-cli/src/main.rs's transition() function, add a `state.infra_failures = 0;` statement immediately adjacent to the existing `state.consecutive_failures = 0;` line (both must be set before `workflow::save_state(state)?` runs) — this is the entire functional fix for CR-01. Add a short doc-comment note above transition() referencing CR-01 and explaining why the reset matters (several well-spaced, successfully-resolved infra faults across a long phase must not falsely accumulate to the ceiling). In crates/devflow-core/src/state.rs, extend the infra_failures field's doc comment with a sentence stating it is reset to 0 on every successful stage transition (name the CLI's transition() function in prose, not as an intra-doc link — it lives in a different crate) alongside consecutive_failures, referencing the CR-01 gap-closure fix. In crates/devflow-core/src/mode.rs, extend MAX_INFRA_FAILURES's doc comment similarly, noting the reset is what makes the "5 unobserved cycles" ceiling bound a stuck loop rather than a phase's lifetime. Add a new test in main.rs's tests module (near the other infra-ceiling tests) named to describe the CR-01 regression: build a State with stage Code and infra_failures = mode::MAX_INFRA_FAILURES - 1, save it, clear PATH under an ENV_MUTEX lock guard (use a freshly created empty tempdir path, not an empty string, so agent_binary_available's PATH scan has zero possible matches), call transition(root, &mut state, Stage::Validate) and ignore its Result (launch_stage will fail closed on ensure_agent_binary once PATH is cleared, but the counter reset happens earlier in the function and is unaffected by that downstream failure), restore the original PATH immediately, then assert state.infra_failures == 0 and that workflow::load_state(root, phase) also reports infra_failures == 0. Follow with a pre-seeded Abort gate response (mirroring resource_killed_on_validate_bumps_infra_not_consecutive_failures's pattern) and a call to handle_infra_outcome for the new Validate stage, asserting infra_failures == 1 afterward. Do not modify the existing infra_ceiling_aborts_instead_of_gating or rate_limited_at_infra_ceiling_stops_resuming_and_aborts tests — both remain valid as-is (they test the ceiling itself, not the reset).
  </action>
  <verify>
    <automated>cargo test -p devflow transition_resets_infra_failures && cargo test -p devflow infra_ceiling && cargo test -p devflow-core state:: && cargo clippy --workspace -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - transition() contains a `state.infra_failures = 0` statement, set before workflow::save_state persists the transition
    - a new regression test proves the reset is both in-memory and persisted, and that a subsequent infra fault after a clean transition starts counting from 1
    - the new test never spawns a real `claude`/`codex`/`opencode` subprocess (PATH is neutralized under the ENV_MUTEX guard before calling transition(), and restored immediately after)
    - state.rs's infra_failures doc comment and mode.rs's MAX_INFRA_FAILURES doc comment both describe the reset-on-transition semantics
    - infra_ceiling_aborts_instead_of_gating, rate_limited_at_infra_ceiling_stops_resuming_and_aborts, and resource_killed_on_validate_bumps_infra_not_consecutive_failures all still pass unmodified
    - `cargo clippy --workspace -- -D warnings` and `cargo fmt --check` are clean
  </acceptance_criteria>
  <done>infra_failures resets alongside consecutive_failures on every successful stage transition, proven by a regression test spanning two infra faults with a clean transition in between; the field's own doc comments now match the implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 — classify a clean-tree strict-ancestor build as Stale, not Fresh</name>
  <files>crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-cli/src/main.rs (Staleness enum + embedded_commit_is_stale, lines 841-868 — the fix site; run_git_stdout 870-883; tracked_source_newer_than_build 885-908; combined_staleness 910-927; is_self_dogfood_workspace 929-956; enforce_build_staleness 979-1027; existing tests embedded_commit_is_stale_maps_ancestry_exit_codes ~4574-4590 (must be corrected, not just extended — see behavior below), init_repo_with_diverged_commit fixture ~4525-4572, combined_staleness_mtime_arm_flags_dirty_tree_newer_than_build ~4592-4636, enforce_build_staleness_blocks_self_dogfood_and_records_event_before_erroring ~4638-4682, is_self_dogfood_workspace_matches_both_member_paths_only ~4489-4523)
    - .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md (Gap 2 / Truth 10 — the verifier's exact live-reproduction narrative: a two-commit clean-tree fixture where the embedded commit legitimately IS an ancestor of the new HEAD, so `merge-base --is-ancestor` exits 0 and the mtime arm never runs on a clean tree)
  </read_first>
  <behavior>
    - embedded_commit_is_stale returns Fresh only when embedded_commit, after trimming, exactly equals the current HEAD commit (read via a new `git rev-parse HEAD` call through the existing run_git_stdout helper)
    - embedded_commit_is_stale returns Stale when merge-base --is-ancestor exits 0 but embedded_commit != HEAD (a strict ancestor — HEAD moved forward since the build) — this is the fix
    - embedded_commit_is_stale still returns Stale on merge-base exit 1 (not an ancestor at all — diverged history), and Indeterminate on any other outcome, an empty embedded_commit, or a failed rev-parse HEAD — unchanged from before
    - the pre-existing embedded_commit_is_stale_maps_ancestry_exit_codes test's `base` assertion is corrected from Fresh to Stale (it previously encoded the WR-01 bug: `base` is an ancestor of the fixture's final HEAD but is not HEAD itself), and a new assertion against the genuine current HEAD commit proves the Fresh case is still reachable
    - a new test reproduces the verifier's exact linear two-commit, clean-tree fixture (a workspace Cargo.toml with both crate member paths committed in the first commit, an unrelated second commit on top, `git status --porcelain` empty throughout) and asserts combined_staleness reports Stale and enforce_build_staleness hard-blocks with the self_dogfood_stale_blocked event recorded
  </behavior>
  <action>
    In embedded_commit_is_stale's `Ok(Some(0))` match arm, replace the direct `Staleness::Fresh` return with a nested match on `run_git_stdout(project_root, &["rev-parse", "HEAD"])`: `Some(head)` where `head.trim() == embedded_commit.trim()` maps to Fresh, any other `Some(_)` maps to Stale, and `None` (rev-parse failed) maps to Indeterminate. Leave the `Ok(Some(1)) => Staleness::Stale` and catch-all `_ => Staleness::Indeterminate` arms untouched. Update the Staleness enum's doc comment (directly above it) to explain that exit 0 alone is not sufficient for Fresh — merge-base --is-ancestor also exits 0 for a strict ancestor of HEAD, which is the "committed new commits, forgot to rebuild" incident this fix closes (WR-01) — and that only an exact match to HEAD is genuinely Fresh. Correct the existing embedded_commit_is_stale_maps_ancestry_exit_codes test: capture the fixture's actual final HEAD commit hash (same `git rev-parse HEAD` + current_dir + output pattern already used elsewhere in the file), assert Fresh for that HEAD hash, and change the existing `base` assertion from `Staleness::Fresh` to `Staleness::Stale` with a comment explaining the corrected expectation (base is an ancestor of the fixture's trunk2 HEAD but is not HEAD itself). Leave the `side` (diverged, exit 1), empty-string, and unknown-hash assertions unchanged. Add a new test, placed near the other staleness tests, that builds a fresh git repo with a single initial commit containing a`Cargo.toml` whose `members = [...]` array already lists both `"crates/devflow-core"` and `"crates/devflow-cli"` plus one other tracked file, captures that commit's hash as `embedded_commit`, adds a second commit on top with an unrelated new file (no modifications to already-committed files, so the tree stays clean), asserts `git status --porcelain` is empty at that point (documenting the fixture's clean-tree property in a comment), then asserts `embedded_commit_is_stale(root, &embedded_commit) == Staleness::Stale`, `combined_staleness(root, &embedded_commit, 0) == Staleness::Stale`, `is_self_dogfood_workspace(root)` is true, and that `enforce_build_staleness(root, &state, &embedded_commit, 0)` (state.stage = Stage::Code) returns an Err whose message contains "self-dogfood stale build blocked", with `devflow_core::events::last_event_for_phase(root, phase)` reporting the `self_dogfood_stale_blocked` event — mirroring enforce_build_staleness_blocks_self_dogfood_and_records_event_before_erroring's assertions exactly.
  </action>
  <verify>
    <automated>cargo test -p devflow embedded_commit_is_stale && cargo test -p devflow combined_staleness && cargo test -p devflow enforce_build_staleness && cargo test -p devflow is_self_dogfood_workspace && cargo clippy --workspace -- -D warnings && cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - embedded_commit_is_stale distinguishes an exact HEAD match (Fresh) from a strict ancestor of HEAD (Stale) via a new git rev-parse HEAD comparison, gated only inside the merge-base exit-0 branch
    - embedded_commit_is_stale_maps_ancestry_exit_codes now asserts `base` -> Stale and a freshly-captured HEAD hash -> Fresh (not `base` -> Fresh as before the fix)
    - a new test reproduces a linear, clean-tree, two-commit fixture and proves combined_staleness is Stale and enforce_build_staleness hard-blocks a self-dogfood workspace in that scenario, with the self_dogfood_stale_blocked event recorded before the error
    - combined_staleness_mtime_arm_flags_dirty_tree_newer_than_build, enforce_build_staleness_blocks_self_dogfood_and_records_event_before_erroring, enforce_build_staleness_warns_for_ordinary_project_with_stale_commit, enforce_build_staleness_never_blocks_on_indeterminate, and is_self_dogfood_workspace_matches_both_member_paths_only all still pass unmodified
    - `cargo clippy --workspace -- -D warnings` and `cargo fmt --check` are clean
  </acceptance_criteria>
  <done>A clean-tree binary built from a commit that is a strict ancestor of HEAD is now classified Stale, closing the WR-01 false-negative that missed the exact "committed, forgot to rebuild" incident class 17d exists to catch.</done>
</task>

<task type="auto">
  <name>Task 3: Record the AC-4 scope-narrowing override and update ROADMAP.md's Requirements line</name>
  <files>.planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md (top YAML frontmatter lines 1-87, especially the `gaps:` list's third entry / Gap 3 at lines 63-86; the "This looks intentional (Gap 3 only)" override proposal in the Gaps Summary section near the end of the file)
    - .planning/ROADMAP.md (Phase 17 section, lines 175-190 — specifically the `**Requirements:**` line)
  </read_first>
  <action>
    Using the Edit tool (never Write) on .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md, add a new top-level `overrides:` key to the YAML frontmatter (after the existing `gaps:` list, still inside the frontmatter block delimited by the `---` markers) containing exactly one entry in the shape the verifier proposed: `must_have` set to the AC-4 truth text verbatim ("A non-interactive plan, unavailable reviewer, missing security artifact, or invalid required credential is reported before the stage launch (retrospective AC-4)"), `reason` set to the verifier's proposed rationale ("D-14(b) named no concrete security-artifact path/key at planning time; cross-AI review consensus #6 deferred it and reviewer-set enforcement to Phase 18's Hermes adapter, which is the first adapter with real reviewer storage"), `accepted_by` set to "Dennis Kim (gap-closure disposition, /gsd-plan-phase --gaps, phase 17)", and `accepted_at` set to an ISO-8601 UTC timestamp for this gap-closure session (use the actual commit time, not a placeholder). Also update the frontmatter's `overrides_applied: 0` to `overrides_applied: 1` so the summary count matches the new list. Do NOT touch the `gaps:` list's first two entries (CR-01, WR-01) or their `status: failed` values — those close only via a future re-verification pass confirming Tasks 1 and 2's code fixes, not via this override. Then, using Edit (never Write) on .planning/ROADMAP.md, extend Phase 17's `**Requirements:**` line (currently ending "...criterion 1 is already covered by Phase 16's regression test — verify against final HEAD rather than re-plan)") with an additional sentence stating that AC-4 is narrowed to the plan-interactivity and Ship-scoped `gh auth` checks only, that the security-artifact and reviewer-set sub-checks are deferred to Phase 18's Hermes adapter, and that this is an accepted override recorded in 17-VERIFICATION.md (cite the file name so a future reader can find the attribution). Do not alter any other part of either file — this task is a scoped, additive documentation change, not a rewrite.
  </action>
  <verify>
    <automated>grep -c "^overrides:" .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md && grep -c "accepted_by:" .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md && grep -c "overrides_applied: 1" .planning/phases/17-pipeline-dogfood-followup/17-VERIFICATION.md && grep -c "17-VERIFICATION.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - 17-VERIFICATION.md frontmatter contains a top-level `overrides:` list with exactly one entry: `must_have`, `reason`, `accepted_by`, `accepted_at` for the AC-4 truth, in the shape the verifier proposed
    - `overrides_applied: 1` in the same frontmatter (was 0)
    - the `gaps:` list's Gap 1 (CR-01) and Gap 2 (WR-01) entries and their `status: failed` values are byte-for-byte unchanged
    - ROADMAP.md's Phase 17 `**Requirements:**` line documents the narrowed AC-4 scope and cites 17-VERIFICATION.md as the source of the accepted override
    - both edits were made with the Edit tool, not Write
  </acceptance_criteria>
  <done>The disclosed AC-4 scope narrowing is formally recorded as an attributed override, and ROADMAP.md's Requirements line no longer implies AC-4 is unconditionally satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| infra fault chain -> state.infra_failures -> mode::MAX_INFRA_FAILURES ceiling | an under-reset counter can turn a resilience mechanism into a false denial-of-service against long-running phases |
| embedded build commit -> self-dogfood staleness decision | a stale binary is untrusted evidence about the state of the source it is driving; a false Fresh classification lets stale behavior masquerade as current |
| 17-VERIFICATION.md override attribution -> ROADMAP.md Requirements line | an unattributed or silent scope narrowing would misrepresent what the phase actually delivers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-16 | Denial of Service | `state.infra_failures` / `transition()` | high | mitigate | CR-01 fix: `transition()` resets `infra_failures` to 0 alongside `consecutive_failures`, so well-spaced, successfully-resolved infra faults across a phase's lifetime never falsely accumulate to a premature hard-abort |
| T-17-17 | Spoofing | `embedded_commit_is_stale` self-dogfood staleness detection | high | mitigate | WR-01 fix: an exact `git rev-parse HEAD` equality check now gates the merge-base exit-0 branch, so a clean-tree binary built from a strict ancestor of HEAD (the Phase 16 "forgot to rebuild" incident) is classified Stale instead of Fresh |
| T-17-18 | Repudiation | AC-4 scope-narrowing disclosure | low | accept | The narrowed AC-4 scope (security-artifact + reviewer-set checks deferred to Phase 18's Hermes adapter) is recorded as an explicit, attributed `overrides:` entry in 17-VERIFICATION.md and echoed in ROADMAP.md's Requirements line, rather than left as a silent gap |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new dependencies or package installs in this gap-closure plan — Task 1/2 add tests and doc comments only, Task 3 edits Markdown/YAML frontmatter |

</threat_model>

<verification>
- `cargo test -p devflow` full CLI suite green, including both new regression tests and the corrected `embedded_commit_is_stale_maps_ancestry_exit_codes`
- `cargo test -p devflow-core state::` green (infra_failures serde/doc tests unaffected)
- `cargo clippy --workspace -- -D warnings` and `cargo fmt --check` clean
- `grep` checks confirm the `overrides:` frontmatter entry and the ROADMAP.md Requirements-line update exist
- Manual reasoning: neither Task 1's nor Task 2's new tests spawn a real `claude`/`codex`/`opencode` subprocess or leave the process-global `PATH` mutated after the test returns
</verification>

<success_criteria>
- `transition()` resets `infra_failures` alongside `consecutive_failures` — Truth 4 closed
- `embedded_commit_is_stale` classifies a clean-tree strict-ancestor build as Stale — Truth 10 closed
- AC-4's scope narrowing is a recorded, attributed override, not a silent gap — Truth 8 disposition closed
- Full workspace test suite green, clippy/fmt clean
</success_criteria>

<output>
Create `.planning/phases/17-pipeline-dogfood-followup/17-06-SUMMARY.md` when done
</output>