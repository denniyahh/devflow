---
phase: 21-operator-usability-release-execution
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - crates/devflow-cli/src/commands.rs
autonomous: true
requirements: [21b, D-04, D-05]
# No REQ-IDs on this project. `requirements` = unit 21b + its CONTEXT decisions
# D-04 (detection-only; flag drift, never auto-correct prose) and D-05 (integrate
# as a new check in the existing doctor path; do not fork a second reporter).
# depends_on [21-02] because both plans edit commands.rs — same-wave zero-file-
# overlap forces 21b into the wave after 21a.

must_haves:
  truths:
    - "`devflow doctor` (text + `--json`) flags a ROADMAP.md/STATE.md version claim whose git tag does not exist or is not reachable from `main`."
    - "The check compares each parseable single-semver version cell against the corresponding git tag, normalizing the `v` prefix (cells are bare `1.7.0`; tags are `v1.7.0`)."
    - "`doctor --json` remains a SINGLE JSON object; the finding set is a THIRD top-level key (e.g. `planning_doc_staleness`), never a second concatenated array (WR-01 discipline)."
    - "The check is detection-only: it has NO write path to ROADMAP.md/STATE.md and offers no auto-repair (repair: None) (D-04)."
    - "The v1.5.0 legacy-noise cutoff is applied by NUMERIC `(major, minor, patch)` comparison, not string/lexicographic compare — so a post-cutoff `v1.10.0` is correctly treated as ≥ v1.5.0 (a lexicographic compare would wrongly sort `\"1.10.0\" < \"1.5.0\"` and misclassify it as legacy)."
  artifacts:
    - "crates/devflow-cli/src/commands.rs — a pure parse fn extracting (phase-label, version) rows from ROADMAP.md `## Shipped` and STATE.md `## Completed`, skipping ranges/em-dashes/non-semver cells"
    - "crates/devflow-cli/src/commands.rs — a git-tag existence+reachability helper (argv `git`), and a pure fn producing PlanningDocFinding(s)"
    - "crates/devflow-cli/src/commands.rs — doctor() text + doctor_json_body() extended with the third key"
  key_links:
    - "doctor() → collect planning-doc rows → tag_exists_and_reachable(v-normalized) → PlanningDocFinding → doctor_json_body third key + render text"
  prohibitions:
    - "MUST NOT auto-edit ROADMAP.md/STATE.md prose — no write path whatsoever (D-04)."
    - "MUST NOT fork a second doctor reporter or emit a second top-level JSON array — extend doctor_json_body's single object (D-05, WR-01)."
    - "MUST NOT flood on legacy history: version ranges (`0.1.0–0.6.0`), em-dashes (`—`), and pre-v1.5.0 mismatches must NOT produce Problem-severity noise (RESEARCH Pitfall #2). Downgrade pre-v1.5.0 mismatches to Warn/informational; skip unparseable cells."
    - "MUST NOT panic on a malformed table row — skip it (degrade, don't die)."
    - "MUST NOT build git commands from a shell string — argv array only."
---

<objective>
Deliver unit 21b (D-04, D-05): add a detection-only `doctor` check that compares the version/outcome claims in `.planning/ROADMAP.md` (`## Shipped`) and `.planning/STATE.md` (`## Completed`) against the repo's git tags, and flags drift — the exact class of bug this session hit (STATE/ROADMAP claiming Phase 18 unreleased after v1.5.0 shipped). It never rewrites prose (D-04); it integrates as a new finding in the existing `doctor` path so human and `--json` output stay consistent (D-05).

The load-bearing risk is noise: `ROADMAP.md`'s `## Shipped` and `STATE.md`'s `## Completed` carry version ranges (`0.1.0–0.6.0`), em-dash placeholders (`—`), and duplicate/legacy versions (Phase 9 and Phase 11 both claim `1.2.0`; Phases 6/7 claim `1.0.0` but no `v1.0.0` tag exists — tags start at `v1.0.1`). A naive per-row check floods `doctor` with Problem-severity findings on every run — the exact alert-fatigue failure 999.14 exists to prevent. Scope the check to cells matching `^v?\d+\.\d+\.\d+$`, skip the rest, and downgrade pre-v1.5.0 mismatches to Warn/informational.

Assumptions surfaced: (1) finding type is a NEW sibling `PlanningDocFinding` (not `PhaseFinding`), because most claims are about already-shipped phases with no active `state-NN.json`/`PhaseFacts` (RESEARCH Open Q1); (2) the noise cutoff is a fixed `v1.5.0` boundary (first consistently-tagged release) rather than a config knob. This plan is Wave 3; it depends on 21-02 (shared `commands.rs`).

Purpose: make devflow catch its own planning-doc drift instead of an operator finding it weeks later.
Output: a third, detection-only `doctor` finding set, in both text and `--json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-operator-usability-release-execution/21-CONTEXT.md
@.planning/phases/21-operator-usability-release-execution/21-RESEARCH.md
@.planning/phases/21-operator-usability-release-execution/21-PATTERNS.md
@crates/devflow-cli/src/commands.rs
</context>

<artifacts_produced>
- `struct PlanningDocFinding { source: String, claim: String, severity: Severity, detail: String, repair: Option<String> }` (commands.rs) — sibling of `PhaseFinding`; `repair: None` (detection-only).
- A pure `fn parse_planning_doc_versions(text: &str, source: &str) -> Vec<(String /*label*/, String /*version*/)>` scanning `## Shipped`/`## Completed` rows, keeping only cells matching `^v?\d+\.\d+\.\d+$`.
- `fn tag_exists_and_reachable(project_root: &Path, tag: &str, base_branch: &str) -> bool` (argv git: `rev-parse --verify refs/tags/<tag>` + `merge-base --is-ancestor <tag> <base>`).
- A pure `fn parse_semver(cell) -> Option<(u32, u32, u32)>` and a pure `fn reconcile_planning_docs(rows, tag_lookup) -> Vec<PlanningDocFinding>` whose v1.5.0 severity cutoff compares parsed `(major, minor, patch)` tuples NUMERICALLY (never lexicographically — `parse_semver("1.10.0") >= Some((1,5,0))`).
- `fn render_planning_doc_findings_json(&[PlanningDocFinding]) -> serde_json::Value` and text rendering, wired into `doctor()` and `doctor_json_body()` as the third key `planning_doc_staleness`.
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: detection core — parse version rows, tag lookup, produce findings (end-to-end)</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs — `PhaseFinding`/`Severity` (the finding shape + `Severity::{Ok,Warn,Problem}` and `.label()`); `check_gate_pending_without_gate` (the pure `Option<PhaseFinding>`, `repair`-when-fixable idiom to mirror — 21b uses `repair: None`); `run_git_stdout` usage nearby / staleness.rs's `run_git_stdout` (argv `Command::new("git").args([...])` idiom); `is_self_dogfood_workspace` (the codebase's "hand-scan structured text, no parser crate" convention to follow for the Markdown tables).
    - .planning/ROADMAP.md `## Shipped` table and .planning/STATE.md `## Completed` table — the exact row shapes to parse (columns, `—` placeholders, `0.1.0–0.6.0` ranges, duplicate versions). Real tag inventory: `v1.0.1, v1.2.0, v1.3.0, v1.3.69, v1.4.0, v1.5.0, v1.6.0, v1.7.0` (no `v1.0.0`).
    - 21-RESEARCH.md Common Pitfall #2 (legacy-row noise + the `^v?\d+\.\d+\.\d+$` scope + `v`-prefix normalization), "Code Examples" (tag existence/reachability helper), and Open Q1 (new sibling finding type). 21-PATTERNS.md §21b.
  </read_first>
  <behavior>
    - parse: given a small `## Shipped`-shaped table string with a semver cell, a `—` cell, a `0.1.0–0.6.0` range cell, and a duplicate version → `parse_planning_doc_versions` returns only the rows whose cell matches `^v?\d+\.\d+\.\d+$` (range and em-dash skipped).
    - reconcile (Problem): a row claiming a version whose tag is absent/unreachable AND is ≥ v1.5.0 → a `PlanningDocFinding` at `Severity::Problem` with `repair: None`.
    - reconcile (no noise): a pre-v1.5.0 mismatch (e.g. `1.0.0`, no tag) → at most `Severity::Warn`, never `Problem`; em-dash/range rows → no finding at all.
    - numeric-semver cutoff (NOT lexicographic): the ≥ v1.5.0 boundary compares parsed `(major, minor, patch)` tuples, so a future unreachable `1.10.0` is treated as POST-cutoff (10 > 5 at the minor position) → `Severity::Problem`, while an unreachable `1.4.0` → `Severity::Warn` (pre-cutoff). A lexicographic compare would wrongly sort `"1.10.0" < "1.5.0"` and downgrade `1.10.0` to Warn — this test fails under a string compare and passes only with tuple comparison (Codex MEDIUM).
    - tag normalization: a bare cell `1.7.0` is looked up as `v1.7.0`.
  </behavior>
  <action>
    Add a `PlanningDocFinding` struct (sibling to `PhaseFinding`: `source`, `claim`, `severity: Severity`, `detail: String`, `repair: Option<String>`), keeping the T-18-01 discipline (no filesystem paths/usernames in `detail`). Add a pure `parse_planning_doc_versions(text, source) -> Vec<(String, String)>` that scans lines of a `## Shipped`/`## Completed` markdown table, extracts the phase-label and version cells by splitting on `|`, and keeps only version cells matching `^v?\d+\.\d+\.\d+$` (skip `—`, ranges containing `–`/`-`/`,`, and any non-semver cell). Do NOT add a markdown-parser crate — hand-scan, per the `is_self_dogfood_workspace` convention. Add `tag_exists_and_reachable(project_root, tag, base_branch) -> bool` using the argv-array `Command::new("git").args([...])` idiom: `git rev-parse --verify refs/tags/<tag>` then `git merge-base --is-ancestor <tag> <base>`; never a shell string. Add a pure `fn parse_semver(cell: &str) -> Option<(u32, u32, u32)>` that strips an optional leading `v` and parses the three dot-separated numeric components (returns `None` for any non-semver cell). Add a pure `reconcile_planning_docs(rows, &mut impl FnMut(&str)->bool)` (or accept a lookup closure so it is testable without a real repo) that, for each row, normalizes the cell to `v{cell}` if it lacks the `v` prefix, calls the lookup, and on a miss emits a `PlanningDocFinding`: `Severity::Problem` when the claimed version is ≥ the `v1.5.0` cutoff **compared as a numeric `(major, minor, patch)` tuple** — `parse_semver(cell) >= Some((1, 5, 0))`, NEVER a string/lexicographic compare (a lexicographic `>=` wrongly sorts `"1.10.0"` below `"1.5.0"` and would misclassify a real future release as legacy) — else `Severity::Warn` (the legacy cutoff), always `repair: None`. Parse any malformed row defensively — skip, never panic. Write the three `<behavior>` tests (RED first where they can be) proving parse-skips, the Problem case, and the no-noise legacy case.
  </action>
  <verify>
    <automated>cargo test --workspace commands::tests::planning_doc 2>&1 | rg "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn parse_planning_doc_versions|fn tag_exists_and_reachable|fn reconcile_planning_docs|fn parse_semver|struct PlanningDocFinding" crates/devflow-cli/src/commands.rs` shows all five.
    - Unit tests prove: range/em-dash cells are skipped; a ≥v1.5.0 unreachable claim → `Severity::Problem`, `repair: None`; a pre-v1.5.0 mismatch → `Severity::Warn` (not Problem); the em-dash/range rows produce no finding (`test result: ok. N passed`).
    - The v1.5.0 cutoff uses NUMERIC tuple comparison (`parse_semver(...) >= Some((1, 5, 0))`), and a unit test proves an unreachable `1.10.0` → `Severity::Problem` (post-cutoff) while an unreachable `1.4.0` → `Severity::Warn` — a lexicographic/string compare would fail this test (`test result: ok. N passed`). `rg -n "fn parse_semver" crates/devflow-cli/src/commands.rs` returns one hit.
    - No markdown/table-parsing crate added (`git diff crates/devflow-cli/Cargo.toml crates/devflow-core/Cargo.toml` empty); `tag_exists_and_reachable` uses `.args([...])`, no `sh -c`.
    - No write path to ROADMAP.md/STATE.md exists in any new function (`rg -n "OpenOptions|fs::write|File::create" ` in the new functions returns nothing).
    - Every new `pub`/`pub(crate)` item (`PlanningDocFinding`, `reconcile_planning_docs`, and the helper fns) carries a `///` doc comment (CLAUDE.md: document public items).
  </acceptance_criteria>
  <done>The detection core parses only well-formed semver rows, checks each against a v-normalized git tag, and produces detection-only findings with the legacy-noise cutoff — proven by unit tests, no parser crate, no write path.</done>
</task>

<task type="auto">
  <name>Task 2: wire into doctor() text + doctor_json_body() third key</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs — `doctor` (its `if json { … doctor_json_body … } else { … render_reconciliation_text … }` split); `doctor_json_body` (the single-object `{"environment", "reconciliation"}` composed value — WR-01 comment above it explains why it must stay one object); `render_reconciliation_json`/`checks_json_value` (array-building idiom); `render_reconciliation_text` (the text-section idiom to mirror).
    - 21-RESEARCH.md "doctor --json extension shape" and Pattern 2 (single-document JSON; third key). 21-PATTERNS.md §21b (`doctor_json_body` extension; no second array).
  </read_first>
  <action>
    In `doctor`, read `.planning/ROADMAP.md` and `.planning/STATE.md` (best-effort; a MISSING file yields no rows, never an error — do not fabricate a Problem from an absent doc), run `parse_planning_doc_versions` on each, then `reconcile_planning_docs` with a real lookup closure calling `tag_exists_and_reachable(project_root, tag, "main")`. The base branch is `"main"` — VERIFIED a LOCAL branch in this repo (`git branch --list main` lists it; `git merge-base --is-ancestor v1.7.0 main` returns 0 OFFLINE). A cross-AI reviewer's concern that `main` might be origin-only (making every `doctor` run spuriously flag tags unreachable) was checked against source and REJECTED — do NOT "fix" this to `origin/main` (needless network dependency, contradicts `doctor`'s no-fetch design) or to `develop` (wrong base). Add `render_planning_doc_findings_json(&[PlanningDocFinding]) -> serde_json::Value` (an array, mirroring `render_reconciliation_json`) and extend `doctor_json_body` to take the findings and emit a THIRD top-level key `"planning_doc_staleness"` — do NOT print a second top-level array (WR-01). For text output, add a `render_planning_doc_text(&[PlanningDocFinding]) -> String` section (mirroring `render_reconciliation_text`) printed after the reconciliation section, listing each finding's `source`/`claim`/`detail`; when there are no findings, print a single `planning docs: consistent with git tags` line. Keep `doctor` read-only.
  </action>
  <verify>
    <automated>cargo test --workspace commands::tests::doctor_json 2>&1 | rg "test result: ok" ; cargo clippy -p devflow --all-targets -- -D warnings ; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `doctor --json` output parses as a SINGLE JSON object with exactly three top-level keys: `environment`, `reconciliation`, `planning_doc_staleness` (a test asserts `serde_json::from_str::<serde_json::Value>` succeeds and the object has the three keys — proving no second concatenated array).
    - `rg -n "planning_doc_staleness" crates/devflow-cli/src/commands.rs` appears only inside `doctor_json_body` (one nested key), never as a separate `println!`/`to_string` of its own top-level array.
    - Text `doctor` prints a planning-docs section after reconciliation; with a consistent fixture it prints the `consistent with git tags` line.
    - A test with a MISSING `.planning/ROADMAP.md` (and/or `STATE.md`) asserts `doctor` does not error and `planning_doc_staleness` is an empty array / the text prints `consistent with git tags` — best-effort missing-file handling proven, not just stated (Codex/OpenCode LOW).
    - `doctor` performs no writes to ROADMAP.md/STATE.md (read-only); `cargo clippy -p devflow --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`doctor` and `doctor --json` surface planning-doc staleness as a third, detection-only key in the single existing document — no fork, no second array, no prose edits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ROADMAP.md/STATE.md text → doctor | operator-authored markdown tables parsed into version claims |
| git subprocess → doctor | `git rev-parse`/`merge-base` output determines tag existence/reachability |
| doctor finding → operator trust | a finding must not silently mutate the docs it reports on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21b-01 | Tampering (of trust) | planning-doc check | medium | mitigate | Detection-only by construction (D-04): the new functions have NO write path to ROADMAP.md/STATE.md; `repair: None`. Acceptance criteria grep-assert no `fs::write`/`File::create`/`OpenOptions` in the new code. |
| T-21b-02 | Tampering | git-tag shelling | low | mitigate | `tag_exists_and_reachable` uses `Command::new("git").args([...])` argv array; tag strings are built from validated `^v?\d+\.\d+\.\d+$` cells, never free-form; no `sh -c`. |
| T-21b-03 | Denial of Service (local) | markdown row parsing | low | mitigate | Parse defensively — any row not matching the expected shape is skipped, never a panic (mirrors `Gates::list_open`'s degrade-don't-die discipline). |
| T-21b-04 | Information Disclosure | finding `detail` strings | low | mitigate | `detail` carries only phase labels, version strings, and tag names — no filesystem paths or usernames (T-18-01); no new `events.jsonl` writes. |
| T-21b-05 | Repudiation (alert fatigue) | legacy-row noise | medium | mitigate | Scope to `^v?\d+\.\d+\.\d+$` cells and downgrade pre-v1.5.0 mismatches to Warn — prevents the noise that would erode trust in `doctor` (RESEARCH Pitfall #2), the exact failure 999.14 exists to prevent. |
| T-21b-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency changes this plan/phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test --workspace commands::` — planning-doc parse/reconcile/doctor_json tests green, 0 failed.
- `doctor --json` is valid single-document JSON with three top-level keys (parser-asserted in test).
- `cargo clippy -p devflow --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
</verification>

<success_criteria>
- `doctor` flags a version claim with a missing/unreachable tag (≥v1.5.0 → Problem; legacy → Warn).
- Ranges, em-dashes, and pre-v1.5.0 history do not create Problem-severity noise.
- `--json` stays one object with a third `planning_doc_staleness` key; zero prose auto-edits.
</success_criteria>

## Review Incorporation

Targeted edits from the Phase-21 cross-AI review (`21-REVIEWS.md`, 3-reviewer Consensus). The two-reviewer pass rated 21-03 the phase's highest risk; the third lane + source verification lowered that — the scariest concern was **refuted**, leaving one one-line fix plus nice-to-haves.

- **INCORPORATED — numeric semver cutoff (Codex MEDIUM, STANDS).** The v1.5.0 cutoff now compares parsed `(major, minor, patch)` tuples, not strings (a lexicographic `>=` sorts `"1.10.0"` below `"1.5.0"` and would misclassify a real future release as legacy). Added `parse_semver` + a numeric-cutoff test (`1.10.0` unreachable → Problem, `1.4.0` unreachable → Warn). See Task 1 `<behavior>`/`<action>`/`<acceptance_criteria>`, `artifacts_produced`, and the new `must_haves.truths` bullet.
- **REJECTED (recorded so no one "re-fixes" it) — `main` reachability is origin-only (OpenCode MEDIUM, REFUTED).** This was the phase's highest-rated concern (a spurious-flood on every `doctor` run). Verified against source: `main` IS a local branch (`git branch --list main`), and `git merge-base --is-ancestor v1.7.0 main` succeeds offline. Hermes independently confirmed. Task 2 `<action>` now records that `tag_exists_and_reachable(.., "main")` is correct and must NOT be "fixed" to `origin/main`/`develop`.
- **INCORPORATED — missing-doc handling test (Codex/OpenCode LOW).** Added a Task 2 acceptance criterion: a missing `.planning/ROADMAP.md`/`STATE.md` yields an empty `planning_doc_staleness` / `consistent with git tags`, never an error (the plan already stated this; now it is tested).
- **DEFERRED (documented) — cross-table coverage: also read the ROADMAP milestone table (Codex HIGH → downgraded to breadth enhancement).** The detector reads `## Shipped` + `## Completed` only, so drift that appears ONLY in the ROADMAP milestone table would be missed. Deferred with rationale: (1) the consensus reclassified this from a live bug to a coverage-breadth improvement — there is **no current drift** in the milestone table; (2) all three reviewers praised the tight scope, and the `--reviews` contract forbids scope expansion; (3) it is cheap to reverse — `parse_planning_doc_versions` is table-shape-agnostic, so pointing it at the milestone table later is a one-line addition in Task 2, not a redesign. If milestone-table drift is observed, add it then.
- **N/A — Hermes rated 21-03 LOW / well-scoped**; no further change requested. Frontmatter, waves, and threat_model unchanged.

<output>
Create `.planning/phases/21-operator-usability-release-execution/21-03-SUMMARY.md` when done.
</output>