# Phase 37.1: Pi Subagent-Extension Spike (research) - Pattern Map

**Mapped:** 2026-08-17
**Files analyzed:** 0
**Analogs found:** n/a (no files to map)

## No Files To Classify

This phase has no CONTEXT.md (research-only spike, no `/gsd-discuss-phase` decisions to extract
file lists from). RESEARCH.md is explicit and repeated on this point, not merely silent:

- Summary/Architectural Responsibility Map: *"Note: this phase produces no code, so this map is
  informational for the planner deciding **where** Phase 39-follow-on work would live, not a map
  of code this phase itself touches."* (37.1-RESEARCH.md:64-65)
- Package Legitimacy Audit: *"This phase does not install any package as part of its own
  deliverable (it is research-only)."* (37.1-RESEARCH.md:69)
- Validation Architecture: *"This phase produces a research document (comparison + recommendation
  + decision gate), not executable code, so there is no test framework, no Req ID → Test map, and
  no Wave 0 gap list in the conventional sense."* (37.1-RESEARCH.md:368-370)
- Wave 0 Gaps: *"None — no code, no test infrastructure required by this phase."*
  (37.1-RESEARCH.md:386)
- Environment Availability: *"this is a research-only phase with no build/test requirement."*
  (37.1-RESEARCH.md:363-364)

The phase's actual deliverable is 37.1-RESEARCH.md itself (already written, 436 lines,
comparison + architectural recommendation + decision-gate verdict) plus 37.1-VALIDATION.md. There
is no source code — Rust, TypeScript, or otherwise — that this phase creates or modifies.

**Consequence for the planner:** the phase's PLAN.md (if one is generated) should task out
research refinement / decision-gate write-up / roadmap-update work referencing this research
(e.g., surfacing the two Open Questions as explicit decision points for Phase 39's
discuss-phase, per RESEARCH.md's own recommendation at line 335-337), not `src/`-style
implementation tasks. There is nothing in this phase to pattern-map against existing controller/
service/component/model code, because the phase produces no such files.

## Deferred Implementation Surface (informational only — not this phase's scope)

RESEARCH.md's own Architectural Responsibility Map and "Don't Hand-Roll" section name concrete
future integration points, in case a later phase (the independently-scoped "Pi full-dispatch"
follow-on RESEARCH.md recommends, not Phase 39 itself) needs a starting pattern map. Recorded here
for continuity, not as this phase's deliverable:

| Future file (not created by this phase) | Would-be role | Would-be analog | Analog quality |
|---|---|---|---|
| `crates/devflow-core/src/agents/pi.rs` — a future `parse_pi_result` | completion parser | `parse_codex_event_result` in `crates/devflow-core/src/agent_result.rs` (per RESEARCH.md:60, cited as the Layer-1 precedent; already Phase 39 D-02 scope, not this phase) | role-match, out of this phase's scope |
| A future Pi-specific `CloseRule` drain predicate | drain-gate matcher | `crates/devflow-core/src/monitor.rs:582-609` (Claude's `task_started`/`task_progress`/`background_tasks_changed` subtype scan) | role-match only — RESEARCH.md is explicit this predicate does not exist yet and needs new, Pi-specific design (name-string matching on a `subagent` tool call, not a task-subtype scan) |
| A vendored `~/.pi/agent/extensions/` dispatch extension | extension/config, user-scope install | `~/.pi/agent/extensions/gsd.js` (DevFlow's own existing GSD extension, cited at RESEARCH.md:94-96 as the precedent installer pattern) | exact pattern-match for "how DevFlow vendors a Pi extension," but this phase does not install or vendor anything |

None of the above are files this phase's plan should create. They are recorded only so a future
planner does not re-derive this mapping from scratch when the deferred follow-on phase is
scoped (per RESEARCH.md Open Question 1, this scheduling is an explicit roadmap decision still
pending, not settled by this research).

## Metadata

**Analog search scope:** n/a — no codebase search performed, since no files require pattern
mapping.
**Files scanned:** 0
**Pattern extraction date:** 2026-08-17
