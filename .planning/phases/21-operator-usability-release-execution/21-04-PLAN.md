---
phase: 21-operator-usability-release-execution
plan: 04
type: execute
wave: 4
depends_on: ["21-03"]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-cli/src/parallel.rs
  - crates/devflow-cli/src/commands.rs
autonomous: true
requirements: [21c, D-06]
# No REQ-IDs on this project. `requirements` = unit 21c + its CONTEXT decision
# D-06 (re-scope to sequentagent's second agent only; define what "tracked"
# means for a non-stage-machine handoff as the first plan step; reversible).
# depends_on [21-03] because the status surfacing edits commands.rs, which
# 21-02 and 21-03 also edit — same-wave zero-file-overlap forces 21c last.

must_haves:
  truths:
    - "sequentagent writes a lightweight per-phase record naming the currently-running agent slot (A/B) and its AgentKind, following agent_result.rs's `xxx_path(project_root, phase)` convention — NOT routed through State/save_state (Phase 19 D-14)."
    - "While agent B runs, `devflow status` surfaces a distinct line identifying it (e.g. `sequentagent: agent B (codex) running (pid P)`), cross-referencing pid liveness from the existing agent-pid file — so the second agent is no longer invisible."
    - "The record is overwritten at each agent's launch (A then B) and cleared on EVERY sequentagent exit path — success AND all five error-exits — via a `Drop` cleanup guard; a stale record with a dead pid is surfaced as not-running (or `starting` before its pid file appears), never as a live agent."
  artifacts:
    - "crates/devflow-core/src/agent_result.rs — sequentagent_slot_path(project_root, phase) -> PathBuf + write/read/clear helpers + a SequentagentSlot record type"
    - "crates/devflow-cli/src/parallel.rs — run_agent_blocking gains a slot label param; sequentagent writes slot A / slot B and clears on completion"
    - "crates/devflow-cli/src/commands.rs — status() surfaces live sequentagent slots via a pure render helper"
  key_links:
    - "sequentagent → run_agent_blocking(slot) → write_sequentagent_slot → .devflow/phase-NN-sequentagent → status() read + agent_running(pid)"
  prohibitions:
    - "MUST NOT route the record through State/save_state — sequentagent's state is synthetic/never-persisted; forcing persistence corrupts status/doctor's single-state-per-phase assumption (Phase 19 D-14; RESEARCH Pitfall #3)."
    - "MUST NOT force sequentagent into the stage machine — only give its second process a record (CONTEXT code_context)."
    - "MUST NOT write a filesystem path or OS username into the record or any new event (WR-02); slot/kind/pid only."
    - "MUST NOT change sequentagent's execution/integration/rebase behavior — this is additive bookkeeping + display."
---

<objective>
Deliver unit 21c (D-06): give `sequentagent`'s second agent a tracked record so it is observable while it runs. Today one `phase-N-agent-pid` file is written per phase, keyed only by `(project_root, phase)` with no agent-slot dimension; `sequentagent` calls `run_agent_blocking` twice (agent A, then agent B) writing the same paths, and because it never persists a `State`, `devflow status` shows nothing for either agent. The monitor half of the original item shipped in v1.5.0 (18b); this is narrowed to the sequentagent second-process gap.

Re-scope finding (D-06's mandated first step, stated not assumed): status/doctor derive phase visibility from `workflow::list_states()`, and sequentagent persists no state, so BOTH agent A and agent B are currently invisible. This plan takes the **narrow** reading D-06's text calls for — give the second process a distinctly-labeled record (slot A vs B + AgentKind) and surface it in `status` — while explicitly recording that the record labels whichever agent is currently running (so agent A is covered too, incidentally). Surfacing is scoped to `status` (the live-observability command); `doctor` integration is deliberately NOT included here — doctor reconciles persisted `State`, and adding a text-only line without a matching `--json` key would reintroduce the human/json inconsistency 18-WR01 fixed. This is an explicit planner decision (assumption-delta), reversible.

This plan is Wave 4; it shares `commands.rs` with 21-02/21-03, hence last.

Purpose: make the sequentagent handoff legible instead of a black box.
Output: a slot record + a `status` section that names the running agent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-operator-usability-release-execution/21-CONTEXT.md
@.planning/phases/21-operator-usability-release-execution/21-RESEARCH.md
@.planning/phases/21-operator-usability-release-execution/21-PATTERNS.md
@crates/devflow-cli/src/parallel.rs
@crates/devflow-core/src/agent_result.rs
@crates/devflow-cli/src/commands.rs
</context>

<artifacts_produced>
- `pub fn sequentagent_slot_path(project_root: &Path, phase: u32) -> PathBuf` → `.devflow/phase-NN-sequentagent` (agent_result.rs, mirroring `agent_pid_path`).
- `pub enum SequentagentSlotKind { A, B }` (agent_result.rs) with `fn as_str(&self) -> &'static str` → `"A"`/`"B"` — the slot param is a TYPED enum, not a stringly-typed `&str`, so the two call sites cannot typo the slot (cross-AI review LOW).
- `pub struct SequentagentSlot { pub slot: String, pub agent: String }` + `pub fn write_sequentagent_slot(project_root, phase, slot: SequentagentSlotKind, agent: AgentKind)`, `pub fn read_sequentagent_slot(project_root, phase) -> Option<SequentagentSlot>`, `pub fn clear_sequentagent_slot(project_root, phase)` (agent_result.rs). `read_` keeps `slot: String` (defensive — a hand-edited file still parses). The writer creates `.devflow/` via `crate::workflow::ensure_devflow_dir` (`.gitignore` hygiene, mirroring `history_dir`'s writer) — NOT the private path-only `devflow_dir` (cross-AI review LOW). Content is path-free (slot letter + agent kind only).
- `run_agent_blocking(project_root, phase, agent, workdir, slot: SequentagentSlotKind)` — new TYPED `slot` param; writes the slot record right after the monitor spawns.
- `sequentagent` passes `SequentagentSlotKind::A`/`::B`. A RAII `SequentagentSlotGuard` (`Drop`-based cleanup guard, bound before agent A runs) clears the slot record on EVERY exit path — all five error-exits AND success — replacing the single success-path clear (cross-AI review MUST-FIX).
- A pure `fn render_sequentagent_status(project_root) -> Option<String>` in commands.rs, surfaced in `status`, reading the slot record + `agent_running(pid)` from the existing agent-pid file; renders `starting` (not `not running`) when the slot record exists but the agent-pid file has not appeared yet (the monitor writes it asynchronously — cross-AI review LOW pid-race).
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: sequentagent slot record — data model + write/clear wiring (end-to-end)</name>
  <files>crates/devflow-core/src/agent_result.rs, crates/devflow-cli/src/parallel.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs — `stdout_path`/`stderr_path`/`exit_code_path`/`agent_pid_path` (the `fn xxx_path(project_root: &Path, phase: u32) -> PathBuf` naming convention to extend — all `devflow_dir(project_root).join(format!("phase-{:02}-…", phase))`); `devflow_dir` (private, path-only — does NOT create the dir); `history_dir` + its `crate::workflow::ensure_devflow_dir(&history_dir)?` writer (the precedent the slot writer must follow for `.gitignore` hygiene, rather than assuming `.devflow/` exists); `archive_phase_files` (what sweeps the live phase files — the new record is a sibling, keep it out of the archive sweep or accept that it's overwritten each launch).
    - crates/devflow-cli/src/parallel.rs — `run_agent_blocking` (called twice; the `spawn_monitor_no_advance` returns `monitor_pid` — write the slot record right after; the "Synthetic, never-persisted state" comment at the top is the D-14 constraint); `sequentagent` and its FIVE error-exit paths that must ALSO clear the slot (agent-A `Failed` → `return Err`; agent-A `RateLimited` zero-commit → `return Ok(())`; rebase-B-onto-base fails → `?`; agent-B `Failed`/`RateLimited` → `return Err`; agent-B integrate fails → `?`), plus the success path's final `delete_cron_instructions`; `split_two_agents`; `AgentKind` (`use devflow_core::state::{AgentKind, State}` — has Display via `agent.to_string()` and FromStr).
    - crates/devflow-core/src/agent_result.rs security note: WR-02 — `.devflow/` files must be path/username-free. 21-PATTERNS.md §21c (path-naming convention; DO NOT route through State), 21-RESEARCH.md Pitfall #3/#4.
  </read_first>
  <behavior>
    - round-trip: `write_sequentagent_slot(root, 7, SequentagentSlotKind::B, AgentKind::Codex)` then `read_sequentagent_slot(root, 7)` returns `Some(SequentagentSlot { slot: "B", agent: "codex" })`; `clear_sequentagent_slot(root, 7)` then read returns `None`.
    - path-free: the written file at `sequentagent_slot_path` contains only the slot letter and agent kind — no absolute path, no username.
    - the record file lives at `.devflow/phase-07-sequentagent` (naming consistent with `phase-07-agent-pid`); the writer creates `.devflow/` via `workflow::ensure_devflow_dir`, so a fresh project root with NO pre-existing `.devflow/` still writes successfully and the dir carries the `*` `.gitignore` (cross-AI review LOW).
    - guard clears on drop: a `SequentagentSlotGuard` for phase 7 dropped after a write leaves `read_sequentagent_slot(root, 7) == None` — proving the record is cleared even when the drop is reached via an early error return, not just the success path (3/3-review MUST-FIX).
  </behavior>
  <action>
    In agent_result.rs, add `pub enum SequentagentSlotKind { A, B }` with `fn as_str(&self) -> &'static str` (`"A"`/`"B"`) so the slot is TYPED, not a stringly-typed `&str` (cross-AI review LOW — typo-proofs the two call sites). Add `sequentagent_slot_path(project_root, phase)` → `devflow_dir(project_root).join(format!("phase-{:02}-sequentagent", phase))`, mirroring `agent_pid_path`. Add `pub struct SequentagentSlot { pub slot: String, pub agent: String }` and three helpers: `write_sequentagent_slot(project_root, phase, slot: SequentagentSlotKind, agent: AgentKind)` — FIRST call `crate::workflow::ensure_devflow_dir(&devflow_dir(project_root))` (mirroring `history_dir`'s writer; do NOT assume `.devflow/` exists — this also seeds the `*` `.gitignore`), then serialize a tiny path-free record (a two-line `slot\nagent` text file, or `serde_json` of `{slot, agent}`; slot stored via `slot.as_str()`, agent via `agent.to_string()`); `read_sequentagent_slot(project_root, phase) -> Option<SequentagentSlot>` (keep `slot: String` and parse defensively, `None` on any error); and `clear_sequentagent_slot(project_root, phase)` (best-effort remove, ignore missing). In parallel.rs, add a `slot: SequentagentSlotKind` parameter to `run_agent_blocking` and, immediately after `spawn_monitor_no_advance` returns `monitor_pid`, call `agent_result::write_sequentagent_slot(project_root, phase, slot, agent)`. Update the two `sequentagent` call sites to pass `SequentagentSlotKind::A` and `::B`. To clear the record on EVERY terminal path — not only success — install a RAII cleanup guard: define a small `struct SequentagentSlotGuard<'a> { project_root: &'a Path, phase: u32 }` whose `Drop` calls `agent_result::clear_sequentagent_slot(self.project_root, self.phase)`, and bind `let _slot_guard = SequentagentSlotGuard { .. }` in `sequentagent` BEFORE the agent-A run block so it fires on all five error-exit returns (agent-A fail; agent-A rate-limit/zero-commit `Ok(())`; rebase-B `?`; agent-B fail/rate-limit; integrate-B `?`) AND on success. Remove the now-redundant standalone success-path `clear_sequentagent_slot` — the guard is the single clear point. NOTE (per the 3/3 review): stale records are HARMLESS — `render_sequentagent_status` (Task 2) probes `agent_running(pid)`, so a leaked record with a dead pid renders "not running", never a false live agent; the guard fixes `.devflow/` CLUTTER accumulation, not a correctness bug. Do NOT persist phase state through the workflow persistence path — the synthetic, never-persisted monitor state stays exactly as it is (Phase 19 D-14). Do NOT change the rebase/integrate/monitor flow. Add the round-trip + path-free + `ensure_devflow_dir`-creates-dir + guard-clears-on-drop unit tests in agent_result.rs/parallel.rs.
  </action>
  <verify>
    <automated>cargo test --workspace agent_result::tests::sequentagent_slot 2>&1 | rg "test result: ok" ; cargo test --workspace parallel:: 2>&1 | rg "test result: ok"</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn sequentagent_slot_path|struct SequentagentSlot|enum SequentagentSlotKind|fn write_sequentagent_slot|fn read_sequentagent_slot|fn clear_sequentagent_slot" crates/devflow-core/src/agent_result.rs` shows all six.
    - Round-trip test: write slot `SequentagentSlotKind::B`/Codex → read returns `slot: "B"`, `agent: "codex"`; clear → read returns None (`test result: ok. N passed`).
    - The record file is path-free: a test reads its raw contents and asserts they do NOT contain the temp dir path or a `/` home path (WR-02).
    - `write_sequentagent_slot` calls `workflow::ensure_devflow_dir` before writing: `rg -n "ensure_devflow_dir" crates/devflow-core/src/agent_result.rs` includes the slot writer, and a test writing into a root with NO pre-existing `.devflow/` succeeds and produces a `.devflow/.gitignore` of `*`.
    - `run_agent_blocking`'s signature now takes `slot: SequentagentSlotKind` (NOT `&str`); both `sequentagent` call sites pass `SequentagentSlotKind::A`/`::B` (`rg -n "SequentagentSlotKind::(A|B)" crates/devflow-cli/src/parallel.rs` shows both).
    - The slot is cleared on ALL exit paths via a `Drop` guard: `rg -n "impl Drop for SequentagentSlotGuard|struct SequentagentSlotGuard" crates/devflow-cli/src/parallel.rs` shows the guard, and NO standalone success-only `clear_sequentagent_slot` call remains after the final integrate (the guard is the single clear point). A unit test proves the guard clears the record on drop even when drop is reached via an early return (not the success path).
    - `rg -n "save_state" crates/devflow-cli/src/parallel.rs | rg -v '//'` returns no matches — the only `save_state` mention stays the pre-existing D-14 doc comment (line 6); the slot record adds no actual state-persistence call.
    - `cargo test --workspace parallel::` green (existing sequentagent tests still pass after the signature change).
    - Every new `pub` item in `agent_result.rs` (`SequentagentSlot`, `SequentagentSlotKind`, `sequentagent_slot_path`, `write_/read_/clear_sequentagent_slot`) carries a `///` doc comment — this is `devflow-core` public API (CLAUDE.md: document all public items). Check: `rg -B1 "pub fn sequentagent_slot_path|pub struct SequentagentSlot|pub enum SequentagentSlotKind" crates/devflow-core/src/agent_result.rs | rg "///"`.
  </acceptance_criteria>
  <done>The slot record round-trips, is path-free, is written A-then-B at launch via a typed `SequentagentSlotKind` param (writer creates `.devflow/` via `ensure_devflow_dir`), and is cleared on EVERY exit path (all five error-exits + success) by a `Drop` guard — never touching State/save_state — proven by unit + parallel tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: surface the live sequentagent slot in `status`</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs — `status` (where to add the section — after the per-state loop, near `print_worktrees`/`cron_instruction_hints`, since a sequentagent phase has no entry in `workflow::list_states`); `agent_pid_from_file` (reads `agent_result::agent_pid_path` → the pid to check liveness on); `agent::agent_running` (liveness predicate); `render_pending_gate_banner` (the pure-String-then-println idiom to mirror for testability).
    - crates/devflow-core/src/agent_result.rs — the new `read_sequentagent_slot` + `sequentagent_slot_path` from Task 1, and how to enumerate `.devflow/phase-*-sequentagent` files (mirror `default_logs_phase`'s `read_dir` + `strip_prefix("phase-")`/`strip_suffix` scan in commands.rs).
    - 21-PATTERNS.md §21c (`agent_pid_from_file`-style read helper) and 21-RESEARCH.md Open Q2 (narrow-vs-broad; this plan takes narrow + status-only).
  </read_first>
  <behavior>
    - `render_sequentagent_status` given a project with a slot record for phase 7 (slot "B", codex) whose recorded/agent pid is alive → returns `Some(String)` containing `sequentagent`, `agent B`, `codex`, and `running`.
    - a slot record whose agent pid is dead → the rendered line marks it not-running (a stale record never claims a live agent).
    - a slot record present but NO agent-pid file yet (the monitor writes it asynchronously — the launch/pid-write race) → the rendered line marks it `starting`, NOT `not running` — an honest transient state rather than a misleading "dead" (cross-AI review LOW pid-race).
    - no slot records → returns `None` (status prints nothing extra).
  </behavior>
  <action>
    Add a pure `fn render_sequentagent_status(project_root: &Path) -> Option<String>` that enumerates `.devflow/phase-*-sequentagent` records (a `read_dir` scan mirroring `default_logs_phase`; O(1) files in practice — one live sequentagent at a time), reads each via `agent_result::read_sequentagent_slot`, cross-references liveness via `agent_pid_from_file(project_root, phase)` + `agent::agent_running(pid)`, and builds a section like `sequentagent phase {phase}: agent {slot} ({agent}) {running|starting|not running} (pid {pid})`. Distinguish THREE liveness states: `running` when the pid file exists AND `agent_running(pid)`; `starting` when the slot record exists but `agent_pid_from_file` returns `None` (the monitor writes the pid file asynchronously, so a just-launched agent must read as `starting`, not `not running` — resolving the cross-AI pid-race LOW WITHOUT a blocking `wait_for_agent_pid` in the launch path); `not running` when the pid file exists but the process is dead. Return `None` when there are no records so `status` output is unchanged for the common case. Call it from `status` (print the returned block if `Some`). Keep it read-only. Add unit tests for the four `<behavior>` cases (running / not-running / starting / none) using a tempdir fixture. Note (documented in a code comment): `doctor` surfacing is intentionally out of scope for this plan (status is the live command; doctor reconciles persisted State) — surfacing there without a matching `--json` key would reintroduce the WR-01 human/json split.
  </action>
  <verify>
    <automated>cargo test --workspace commands::tests::sequentagent_status 2>&1 | rg "test result: ok" ; cargo clippy -p devflow --all-targets -- -D warnings ; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "fn render_sequentagent_status" crates/devflow-cli/src/commands.rs` returns one hit; `status` calls it.
    - Unit tests prove: a live slot renders `agent B (codex) running`; a dead-pid slot renders `not running`; a slot with no agent-pid file yet renders `starting`; no records → `None` (`test result: ok. N passed`).
    - `render_sequentagent_status` performs no writes and does not call `save_state`/`transition` (read-only display).
    - `cargo clippy -p devflow --all-targets -- -D warnings` clean; `cargo fmt --check` clean; full workspace suite green.
  </acceptance_criteria>
  <done>`devflow status` names the running sequentagent slot (A/B + kind + liveness), making the second agent observable — read-only, status-only, no State involvement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sequentagent → .devflow/phase-NN-sequentagent | a per-phase record persisted to the shared `.devflow/` dir |
| slot record → status display | status reads the record + probes pid liveness |
| pid → OS | `agent_running(pid)` probes an OS process |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21c-01 | Information Disclosure | slot record contents | medium | mitigate | The record stores only the slot letter and agent kind (path/username-free), per WR-02 — a test asserts no path/home string is present. `.devflow/` is advertised as safe to tail. |
| T-21c-02 | Spoofing (stale pid) | status liveness display | low | mitigate | Liveness is derived live via `agent::agent_running(pid)` against the current agent-pid file; a stale record with a dead pid renders "not running", never a false live agent. |
| T-21c-03 | Tampering (state corruption) | avoiding State/save_state | medium | mitigate | The record is a standalone sibling file; it is NOT routed through `State`/`save_state`, preserving status/doctor's single-state-per-phase assumption (Phase 19 D-14; RESEARCH Pitfall #3). Acceptance criteria grep-assert no new `save_state`. |
| T-21c-04 | Denial of Service (local) | record parsing | low | mitigate | `read_sequentagent_slot` parses defensively and returns `None` on any malformed content — status degrades, never panics. |
| T-21c-SC | Tampering | npm/pip/cargo installs | low | accept | No dependency changes this plan/phase (RESEARCH Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cargo test --workspace agent_result::tests::sequentagent_slot` + `parallel::` + `commands::tests::sequentagent_status` — all green, 0 failed.
- `cargo clippy -p devflow --all-targets -- -D warnings` clean; `cargo fmt --check` clean; full `cargo test --workspace` green.
- Manual reasoning: `git diff` shows sequentagent's rebase/integrate/monitor flow unchanged; only additive record writes + a status read.
</verification>

<success_criteria>
- sequentagent's second agent has a distinct, path-free record; `status` names the running slot (A/B + kind + liveness).
- No State/save_state involvement; sequentagent behavior unchanged.
- Record cleared on completion; stale records never render a false live agent.
</success_criteria>

## Review Incorporation

Targeted edits from the Phase-21 cross-AI review (`21-REVIEWS.md`, 3-reviewer Consensus). 21-04 carried the phase's second **unanimous (3/3) MUST-FIX**.

- **INCORPORATED — clear the slot record on all error-exit paths (3/3 MUST-FIX, LOW–MEDIUM).** All three reviewers flagged that the single success-path clear leaves a stale record on `sequentagent`'s five error-exits (Hermes enumerated all five). Resolved by the reviewers' preferred approach (a `Drop`/cleanup guard): Task 1 now installs a `SequentagentSlotGuard` bound before agent A runs so the record clears on every terminal path, and removes the standalone success clear. **Severity clarification recorded:** the reviewers verified `render_sequentagent_status` already probes `agent_running(pid)`, so stale records are HARMLESS clutter (a dead pid renders "not running"), NOT false-live — the guard fixes `.devflow/` clutter accumulation, not a correctness bug. Captured in Task 1 `<action>`/`<behavior>`/`<acceptance_criteria>`/`<done>` and the `must_haves.truths` bullet.
- **INCORPORATED — type `slot` as an enum, not `&str` (3/3 LOW).** `run_agent_blocking` and the writer now take `SequentagentSlotKind { A, B }`; the two call sites pass `::A`/`::B`, typo-proofing them.
- **INCORPORATED — use `workflow::ensure_devflow_dir` in the writer (Codex LOW).** `write_sequentagent_slot` creates `.devflow/` via `ensure_devflow_dir` (mirroring `history_dir`'s writer) for `.gitignore` hygiene, rather than the private path-only `devflow_dir`. Tested against a root with no pre-existing `.devflow/`.
- **INCORPORATED (light) — pid-race `starting` state (Codex/OpenCode MEDIUM→LOW).** `render_sequentagent_status` renders `starting` (not `not running`) when the slot record exists but the async-written agent-pid file has not appeared yet — an honest transient. Chosen over adding a blocking `monitor::wait_for_agent_pid` to the launch path (which would add latency); the non-blocking render resolves the legibility concern without changing execution timing.
- **N/A — `doctor` symmetry (`"sequentagent_status"` --json key).** Reviewers noted `status` knows more than `doctor` about a running sequentagent. Left as the plan's existing explicit decision (documented in a code comment): `doctor` reconciles persisted State, and a text-only line without a matching `--json` key would reintroduce the WR-01 human/json split. Unchanged; frontmatter, waves, and threat_model intact.

<output>
Create `.planning/phases/21-operator-usability-release-execution/21-04-SUMMARY.md` when done.
</output>