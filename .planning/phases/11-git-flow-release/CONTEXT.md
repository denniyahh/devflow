# Phase 11 — Git-Flow Release Completion + Merge Guard Rails

**Status:** Requirements defined | **Priority:** HIGH | **Target:** TBD

## Goal

Complete DevFlow's git-flow implementation by wiring up dead code paths (`feature_finish`, `release_finish`), adding a separate `devflow release` command for production releases, and adding merge-topology guard rails. Codify: feature → develop only, develop → main only via release.

---

## 11a — Wire `feature_finish` → `devflow finish`

`feature_finish()` exists in `git.rs` but is never called by the CLI. Wire it up as a new command.

- [ ] New command: `devflow finish [--phase N] [--push]`
- [ ] Guard rail: reject if not on a `feature/*` branch (matches `git_flow.feature_prefix`)
- [ ] Action: merge `feature/phase-NN` → `develop` with `--no-ff`, delete local feature branch
- [ ] `--push`: push develop to origin, delete remote feature branch
- [ ] Integration: called automatically by state machine after `Executing` step completes (when agent succeeds)
- [ ] Test: `devflow finish` merges feature into develop, deletes branch, develop has the commits

---

## 11b — New `devflow release` Command

`release_finish()` exists in `git.rs` but is never called by the CLI. Add a separate command for production releases — decoupled from merging to develop.

- [ ] New command: `devflow release [--push]`
- [ ] Guard rails:
  - Must have a valid last-ship record (`.devflow/last-ship.json`)
  - Must be on `develop` or the release branch
  - Reject if no release branch exists
- [ ] Action (calls `release_finish(version)`):
  1. Checkout `main`
  2. Merge `release/X.Y.Z` into `main` with `--no-ff`
  3. Tag `vX.Y.Z` on main
  4. Checkout `develop`
  5. Merge `release/X.Y.Z` into `develop` with `--no-ff` (brings version bump + any final fixes back)
  6. Delete local `release/X.Y.Z` branch
- [ ] `--push`: push main + develop + tags to origin, delete remote release branch
- [ ] Test: `devflow release` merges release → main, tags correctly, merges → develop, deletes branch

---

## 11c — Merge Guard Rails (`git_flow.enforce`)

New config option to validate merge topology before any git-flow operation.

- [ ] New config field: `git_flow.enforce` (bool, default `true`)
- [ ] Guard checks:
  - `devflow finish` validates source branch matches `feature_prefix`
  - `devflow ship` validates current branch is a feature branch (not main/develop directly)
  - `devflow release` validates last-ship record exists, release branch exists, target topology is correct
- [ ] When `enforce: false`, all guard rails are skipped (opt-out for non-standard workflows)
- [ ] Config schema: add `enforce` field to `GitFlowConfig`, `parse_config()`, `default_config_yaml()`, `to_yaml()`
- [ ] Test: guard rails reject wrong-source merges; `enforce: false` allows them

---

## 11d — Updated Documentation

- [ ] Update `ARCHITECTURE.md`: Git and ship model section — add `finish` + `release` commands, guard rail model
- [ ] Update `README.md`: command table — add `finish`, `release`; update shipping section
- [ ] Update `CONTRIBUTING.md`: development workflow section reflecting new command flow
- [ ] Update CHANGELOG.md entries (auto-generated by `devflow confirm`)

---

## Updated Ship Flow

```
devflow start    → feature/phase-NN (from develop) → agent works
devflow finish   → feature/phase-NN merged into develop, branch deleted
devflow ship     → release/X.Y.Z (from feature HEAD), version bump, PR to develop
 [PR review + merge into develop]
devflow release  → merge release → main, tag vX.Y.Z, merge → develop, cleanup
devflow confirm  → CHANGELOG + ROADMAP update, clears ship record
```

---

## Success Criteria

| # | Criterion |
|---|-----------|
| 1 | `devflow finish` merges feature→develop, deletes branch |
| 2 | `devflow release` merges release→main, tags, merges→develop, deletes branch |
| 3 | Guard rails reject `devflow finish` from main/develop |
| 4 | Guard rails reject `devflow ship` from main/develop |
| 5 | Guard rails reject `devflow release` without last-ship record |
| 6 | `enforce: false` disables all guard rails |
| 7 | All existing tests still pass |
| 8 | New integration tests cover finish + release + guard rails |

---

## Deferred to Phase 12

- GitHub branch protection API integration
- Pre-push hook validation
- Merge conflict auto-detection for stale branches
