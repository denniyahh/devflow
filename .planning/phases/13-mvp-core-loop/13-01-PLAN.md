---
phase: 13-mvp-core-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/src/main.rs
  - crates/devflow-core/src/gates.rs
autonomous: true
requirements: [13a, 13c, WR-11]
must_haves:
  truths:
    - "A non-Validate stage failure (Define/Plan/Code/Ship) writes a gate file and fires the notify hook — the loop is never silent."
    - "A Ship-stage agent failure is distinguished from a Ship-stage review failure: AgentFailed gates+notifies, ReviewFailed loops back to Code."
    - "The notify hook never blocks or fails the pipeline (fail-soft)."
    - "The gate poll timeout is configurable via an environment variable, defaulting to 7 days."
    - "When the operator approves a retry (GateAction::Advance) of a failed stage, the stale gate/response/ack files are cleaned up FIRST so the retry cannot silently consume the prior response (CR-01 regression closed)."
    - "A gate that fires on a stage the active mode would not normally gate (e.g. Define/Plan/Code failure in Auto mode) is marked as a never-silent/unexpected gate in its context text, its notify payload sets DEVFLOW_NON_SILENT_GATE=1, and an info! log records it."
    - "A Ship ReviewFailed loop-back re-runs Code with the /gsd-audit-fix prompt (FixType::AuditFix), not the validation-gaps prompt."
  artifacts:
    - "crates/devflow-core/src/gates.rs (fire_gate_notify + run_notify_command helpers)"
    - "crates/devflow-cli/src/main.rs (handle_stage_failure, handle_ship_failure, gate_timeout_secs, parse_gate_timeout, loop_back_to_code fix-type param)"
  key_links:
    - "advance() failure arm → handle_stage_failure / handle_ship_failure → run_gate → fire_gate_notify"
    - "run_gate → gates::fire_gate_notify (every gate write notifies; unexpected gates carry DEVFLOW_NON_SILENT_GATE=1)"
    - "handle_stage_failure GateAction::Advance → Gates::cleanup(phase, stage) → launch_stage (no stale-response reuse)"
    - "handle_ship_failure `review:` prefix → loop_back_to_code(FixType::AuditFix)"
---

<objective>
Close WR-11's silent-halt bug and give the Ship stage a real failure branch. Today
`advance()`'s catch-all `_ => Err(...)` arm returns an error but writes no gate and sends
no notification for any non-Validate stage failure (Define/Plan/Code/Ship), leaving state
dirty with `gate_pending: false` — nothing ever surfaces the halt. This plan routes every
non-Validate failure through a gate + a new pluggable notify hook, distinguishes Ship
`AgentFailed` (gate+notify) from `ReviewFailed` (loop back to Code), and makes the gate
poll timeout env-configurable.

Purpose: unattended runs must always surface a halt or a gate — silence is the failure
mode that makes dogfooding (13e) impossible. This is the architecturally riskiest chunk
and 13e's dogfood acceptance depends on it, so it is front-loaded into Wave 1.
Output: `fire_gate_notify` in gates.rs; `handle_stage_failure`, `handle_ship_failure`,
and `gate_timeout_secs()` in main.rs; notify wired into `run_gate`; failing-then-passing
unit tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mvp-core-loop/CONTEXT.md
@.planning/phases/13-mvp-core-loop/13-RESEARCH.md
@.planning/phases/13-mvp-core-loop/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan (exclude from drift/source-grounding review):
- `fire_gate_notify(phase, stage, context, unexpected)` — new pub fn in `crates/devflow-core/src/gates.rs`
- `run_notify_command(cmd, phase, stage, context, unexpected)` — new fn in `crates/devflow-core/src/gates.rs` (pure shell-out runner, testable without env)
- `handle_stage_failure(project_root, state, stage, reason)` — new fn in `crates/devflow-cli/src/main.rs`
- `handle_ship_failure(project_root, state, reason)` — new fn in `crates/devflow-cli/src/main.rs`
- `gate_timeout_secs()` + `parse_gate_timeout(raw: Option<String>)` — new fns in `crates/devflow-cli/src/main.rs` (replaces `const GATE_TIMEOUT_SECS`; parse split out as a pure fn so tests need no env mutation)
- `loop_back_to_code` gains a `FixType` parameter (or a `loop_back_to_code_with_fix` sibling) so Ship ReviewFailed can loop back with `FixType::AuditFix`
- Env vars produced/consumed: `DEVFLOW_GATE_NOTIFY_CMD`, `DEVFLOW_GATE_TIMEOUT_SECS`, `DEVFLOW_GATE_PHASE`, `DEVFLOW_GATE_STAGE`, `DEVFLOW_GATE_CONTEXT`, `DEVFLOW_NON_SILENT_GATE`
- `review:` reason-prefix convention marking a Ship-stage ReviewFailed result (matched via `reason.trim().to_lowercase().starts_with("review:")`)
- New tests: `notify_hook_failure_is_fail_soft`, `notify_hook_runs_configured_command`, `notify_hook_unset_is_noop` (core); `ship_agent_failed_fires_gate`, `ship_review_failed_loops_to_code`, `ship_review_failed_uses_audit_fix`, `non_validate_failure_fires_gate_and_hook`, `stage_failure_retry_cleans_stale_response`, `parse_gate_timeout_env_override` (cli)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fail-soft gate notify hook to gates.rs</name>
  <files>crates/devflow-core/src/gates.rs</files>
  <read_first>
    - crates/devflow-core/src/gates.rs (current write_gate/poll_response/ack/cleanup API and module doc — the notify helper is a sibling of write_gate)
    - crates/devflow-core/src/hooks.rs lines 117-137 (docs_update — the exact fail-soft `Command::new("sh").arg("-c")` idiom to copy: log warn! on failure, always return Ok)
    - crates/devflow-core/src/monitor.rs (shell_escape / argv-based command construction — the WR-01 precedent for NOT string-interpolating untrusted context)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "Security Domain" (shell-injection mitigation: pass gate context via env vars, never interpolate into the sh -c string)
  </read_first>
  <behavior>
    - Test `notify_hook_runs_configured_command`: `run_notify_command` invoked with a command that touches a sentinel file inside a tempdir creates the sentinel — no env mutation needed (the command string is passed as an argument, so this test is env-free and race-free).
    - Test `notify_hook_failure_is_fail_soft`: `run_notify_command` invoked with a command that exits non-zero returns normally (no panic, no Err) — fail-soft.
    - Test `notify_hook_unset_is_noop`: with DEVFLOW_GATE_NOTIFY_CMD unset, `fire_gate_notify` does nothing and returns normally. This test touches the env, so it acquires the shared `ENV_MUTEX` (a module-level `static ENV_MUTEX: Mutex<()>`) so it does not race with other env-mutating tests — NO new dependency (do not add `serial_test`; the workspace has no config-crate policy and RESEARCH.md forbids new deps).
    - Test `notify_hook_sets_non_silent_flag`: when `run_notify_command` is called with `unexpected = true`, the child command observes `DEVFLOW_NON_SILENT_GATE=1` in its environment (assert via a command that writes `$DEVFLOW_NON_SILENT_GATE` to a sentinel file); with `unexpected = false` the flag is absent/`0`.
  </behavior>
  <action>
    Split the notify hook into a pure, env-free runner and a thin env-reading wrapper so the behavior is unit-testable WITHOUT mutating process-global env (addresses the Rust 2024 `set_var`-is-unsafe / racy-test review concern — consensus #8):

    1. Add `run_notify_command(cmd: &str, phase: u32, stage: Stage, context: &str, unexpected: bool)` to gates.rs, modeled exactly on `hooks.rs::docs_update`'s fail-soft shell-out idiom. Run the command via `Command::new("sh").arg("-c").arg(cmd)`. CRITICAL SECURITY (V5 / WR-01 precedent): do NOT interpolate `phase`, `stage`, or `context` into the command string. Pass them to the child as environment variables via `.env("DEVFLOW_GATE_PHASE", phase.to_string())`, `.env("DEVFLOW_GATE_STAGE", stage.to_string())`, `.env("DEVFLOW_GATE_CONTEXT", context)`, and `.env("DEVFLOW_NON_SILENT_GATE", if unexpected { "1" } else { "0" })` (so an operator's handler can route never-silent/unexpected gates with higher urgency — OpenCode suggestion 5). `warn!` on non-zero exit or spawn error; always return `()` (never propagate an error that could abort run_gate).

    2. Add `pub fn fire_gate_notify(phase: u32, stage: Stage, context: &str, unexpected: bool)` that reads `std::env::var("DEVFLOW_GATE_NOTIFY_CMD")`; if unset or empty, return immediately (silent no-op); otherwise delegate to `run_notify_command(&cmd, phase, stage, context, unexpected)`. Do NOT take `project_root` (it is unused — prefer omitting the parameter to satisfy clippy). Add `///` doc comments per repo convention.
  </action>
  <verify>
    <automated>cargo test -p devflow-core notify_hook -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - `gates.rs` contains `pub fn fire_gate_notify(` and `fn run_notify_command(`
    - `gates.rs` reads `DEVFLOW_GATE_NOTIFY_CMD` and passes context via `.env("DEVFLOW_GATE_CONTEXT"` (source assertion: no `format!`/string concatenation of `context` into the `sh -c` argument)
    - `gates.rs` sets `DEVFLOW_NON_SILENT_GATE` via `.env(` on the child (source assertion)
    - `cargo test -p devflow-core notify_hook` exits 0
    - `cargo clippy -p devflow-core -- -D warnings` exits 0
  </acceptance_criteria>
  <done>fire_gate_notify (env reader) + run_notify_command (pure runner) exist, are fail-soft, pass context + the DEVFLOW_NON_SILENT_GATE flag via env vars (no shell interpolation), and the four unit tests pass with env-touching tests serialized under ENV_MUTEX (no new dependency).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add non-Validate + Ship failure branches and env-configurable gate timeout to advance()</name>
  <files>crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-cli/src/main.rs lines 336-437 (advance(), the `failed` match at 359-375, handle_validate_outcome, handle_ship_outcome — the exact arms to extend)
    - crates/devflow-cli/src/main.rs lines 503-560 (run_gate, abort — where the notify call and timeout read go)
    - crates/devflow-cli/src/main.rs line 16 (GATE_TIMEOUT_SECS const to replace) and lines 178-185 pattern reference (DEVFLOW_LOG_FORMAT env-var-with-fallback idiom)
    - crates/devflow-core/src/mode.rs lines 37-52 (should_gate — confirms non-Validate stages are NOT gated by mode; the never-silent path must gate unconditionally, independent of mode)
    - crates/devflow-core/src/gates.rs (GateAction variants Advance/LoopBack/Abort; fire_gate_notify from Task 1)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "Common Pitfalls" Pitfall 3 (keep the new failure path structurally SEPARATE from handle_validate_outcome so Validate's consecutive_failures/auto-loop semantics do not leak)
  </read_first>
  <action>
    Make the following changes in main.rs. NOTE the CLI crate's Cargo package is named `devflow` (not `devflow-cli`) — all `cargo … -p devflow` (consensus/divergent: package-name normalization).

    1. GATE TIMEOUT (pure + env split, so the parse is testable without env mutation). Replace `const GATE_TIMEOUT_SECS: u64 = 7 * 24 * 60 * 60;` (line 16) with two functions: `fn parse_gate_timeout(raw: Option<String>) -> u64` that does `raw.and_then(|s| s.parse().ok()).unwrap_or(7 * 24 * 60 * 60)`, and `fn gate_timeout_secs() -> u64 { parse_gate_timeout(std::env::var("DEVFLOW_GATE_TIMEOUT_SECS").ok()) }`. Call `gate_timeout_secs()` at the `Gates::poll_response(..., GATE_TIMEOUT_SECS)` site (currently line 518). The pure `parse_gate_timeout` is what the unit test drives — no env write.

    2. NOTIFY + UNEXPECTED-GATE MARKER in `run_gate` (lines 505-529). Immediately after `Gates::write_gate(project_root, state.phase, stage, context)?` compute `let unexpected = !state.mode.should_gate(stage, state.consecutive_failures);` — this is `true` exactly for gates the active mode would NOT normally fire (Define/Plan/Code failures in Auto). When `unexpected` is true, emit `info!("never-silent gate: {stage} failed in {:?} mode — surfacing an unattended gate this mode would not normally fire", state.mode)` (addresses OpenCode HIGH #1 / suggestion 6 — the Auto-mode behavior change is intentional but must be observable). Then call `devflow_core::gates::fire_gate_notify(state.phase, stage, context, unexpected)` (fail-soft — every gate write now notifies; unexpected gates carry `DEVFLOW_NON_SILENT_GATE=1`).

    3. STAGE-FAILURE HANDLER with CR-01 stale-gate cleanup. Add `handle_stage_failure(project_root: &Path, state: &mut State, stage: Stage, reason: Option<String>) -> Result<(), CliError>` that UNCONDITIONALLY calls `run_gate(project_root, state, stage, &context)` (ignore `Mode::should_gate` — WR-11's whole point is these must never be silent; run_gate now marks it as unexpected and notifies) with a context naming the failed stage (mark it clearly, e.g. `format!("[never-silent] stage {stage} failed: {} — human review needed (retry, loop-to-code, or abort)", reason.unwrap_or_else(|| "no details available".into()))`), then maps the returned GateAction. CRITICAL (CR-01, consensus #2): before re-launching on `Advance` OR `LoopBack`, call `let _ = Gates::cleanup(project_root, state.phase, stage);` so the stale response/ack cannot be consumed by the retry's `poll_response` (mirror the cleanup already done in `loop_back_to_code`/`finish_workflow`/`abort`). Map: `Advance` → cleanup, clear `gate_pending`, then `launch_stage(state, None)` (retry the SAME failed stage); `LoopBack(_)` → cleanup, then re-launch the SAME failed stage too — do NOT jump to Code for a Define/Plan failure (Codex 13-01 MEDIUM: Code is not a valid recovery target before planning exists); `Abort(reason)` → `abort` (abort already cleans up). Do NOT touch `consecutive_failures` and do NOT auto-loop — this path is Validate-independent.

    4. SHIP-FAILURE HANDLER. Add `handle_ship_failure(project_root: &Path, state: &mut State, reason: Option<String>) -> Result<(), CliError>` that inspects `reason`: if `reason.as_deref().map(|r| r.trim().to_ascii_lowercase().starts_with("review:")).unwrap_or(false)` (exact matching rule — trim then case-fold; document that this string convention is an inherent limitation of the agent-reported contract, verified live in 13-06 — OpenCode HIGH #2), treat it as ReviewFailed: loop back to Code with the AUDIT-FIX prompt, not the gaps prompt (consensus #7) — call `loop_back_to_code(project_root, state, FixType::AuditFix)` (see change 5). Otherwise treat it as AgentFailed and call `handle_stage_failure(project_root, state, Stage::Ship, reason)`.

    5. FIXTYPE PARAM on loop-back. `loop_back_to_code` currently hardcodes `Some(prompt::fix_prompt(FixType::GapsOnly, state.phase))` (line 478). Add a `fix: FixType` parameter to `loop_back_to_code` (or add a `loop_back_to_code_with_fix` sibling) so the Validate path passes `FixType::GapsOnly` (unchanged behavior) and the Ship ReviewFailed path passes `FixType::AuditFix` (`/gsd-audit-fix`, the review-remediation command). Update the existing `loop_back_to_code` call sites in `handle_validate_outcome`/`handle_ship_outcome` to pass `FixType::GapsOnly` so behavior is unchanged there.

    6. Finally, replace the `failed` match arms (currently `Stage::Validate => handle_validate_outcome(...false)`, `_ => Err(...)`) with: `Stage::Validate => handle_validate_outcome(project_root, &mut state, false)`, `Stage::Ship => handle_ship_failure(project_root, &mut state, result.reason)`, `_ => handle_stage_failure(project_root, &mut state, stage, result.reason)`.
  </action>
  <verify>
    <automated>cargo build -p devflow && cargo clippy -p devflow -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `main.rs` no longer contains `const GATE_TIMEOUT_SECS` and does contain `fn parse_gate_timeout(` and `fn gate_timeout_secs(`
    - `main.rs` contains `fn handle_stage_failure(` and `fn handle_ship_failure(`
    - `handle_stage_failure` calls `Gates::cleanup(` before re-launching on Advance/LoopBack (source assertion: `Gates::cleanup` appears inside handle_stage_failure)
    - `run_gate` computes `state.mode.should_gate(` for the unexpected flag and calls `fire_gate_notify(` after write_gate (source assertions)
    - `handle_ship_failure` uses `FixType::AuditFix` on the `review:` path (source assertion: `FixType::AuditFix` present in handle_ship_failure)
    - The `_ =>` catch-all arm in advance()'s `failed` block no longer returns a bare `CliError::Message` (source assertion: `_ => handle_stage_failure(` present)
    - `cargo clippy -p devflow -- -D warnings` exits 0
  </acceptance_criteria>
  <done>advance() routes Ship AgentFailed and every non-Validate failure through gate+notify; stale gate files are cleaned up before any retry (CR-01 closed); unexpected gates are logged + flagged; ReviewFailed loops to Code with AuditFix; gate timeout is env-configurable via a pure parse fn; crate builds clippy-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for Ship/stage failure gating and env timeout</name>
  <files>crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-cli/src/main.rs lines 1527-1640 (existing `#[cfg(test)]` harness: advance_ship_success_runs_finish_workflow, abort_cleans_up_gate_files... — copy the init_repo/state/gate-response setup idiom, especially how tests write a gate response and drive advance to a terminal path without spawning a monitor)
    - crates/devflow-cli/tests/phase7_cli.rs lines 1-70 (init_repo / git helper idioms if a shared helper is reused)
    - .planning/phases/13-mvp-core-loop/13-VALIDATION.md (Per-Task Verification Map — the illustrative test names to realize)
  </read_first>
  <behavior>
    - `ship_agent_failed_fires_gate`: a Ship-stage AgentFailed result (status Failed, reason without `review:` prefix) writes `.devflow/gates/NN-ship.json` (gate surfaced, not a silent Err).
    - `ship_review_failed_loops_to_code`: a Ship-stage result with reason starting `review:` sets state.stage back to Code (loop-back), does NOT finish the workflow.
    - `ship_review_failed_uses_audit_fix`: the ReviewFailed loop-back prompt is the `/gsd-audit-fix` prompt (FixType::AuditFix), NOT the `--gaps-only` prompt (assert the launched prompt/override contains `/gsd-audit-fix`).
    - `non_validate_failure_fires_gate_and_hook`: a Code-stage failure writes a gate file AND, with the notify command set to touch a sentinel, the sentinel is created; the child's `DEVFLOW_NON_SILENT_GATE` env observed as `1` (unexpected gate in Auto).
    - `stage_failure_retry_cleans_stale_response`: a stage fails, the operator's `Advance` response is written and consumed; a SECOND failure of the same stage does NOT auto-approve from the first response — the stale `.response.json`/ack is cleaned up before the retry (CR-01 regression test, modeled on `abort_cleans_up_gate_files_so_a_later_gate_does_not_reuse_stale_response`).
    - `parse_gate_timeout_env_override`: `parse_gate_timeout(Some("42".into()))` == 42, `parse_gate_timeout(Some("bad".into()))` == 604800, `parse_gate_timeout(None)` == 604800 — a PURE test with no env mutation.
  </behavior>
  <action>
    Add the tests above to main.rs's `#[cfg(test)]` module. Reuse the existing harness's repo/state setup and the "write a gate response file then call the outcome handler" pattern from `abort_cleans_up_gate_files_so_a_later_gate_does_not_reuse_stale_response` so tests terminate without spawning a real monitor (drive the failure handler and, where a terminal path is needed, pre-write an abort response so run_gate returns immediately).

    Env discipline (consensus #8 — `std::env::set_var` is `unsafe`/process-global under Rust 2024): prefer env-FREE tests. `parse_gate_timeout_env_override` calls the pure `parse_gate_timeout` directly — no env. `ship_review_failed_uses_audit_fix`, `ship_agent_failed_fires_gate`, `ship_review_failed_loops_to_code`, and `stage_failure_retry_cleans_stale_response` need no env. Only `non_validate_failure_fires_gate_and_hook` sets `DEVFLOW_GATE_NOTIFY_CMD`; wrap its env set/remove in a lock on a module-level `static ENV_MUTEX: Mutex<()>` (add the static once, reuse across any env-touching test) so parallel `cargo test` runs do not race. Do NOT add `serial_test` or any new dependency (RESEARCH.md forbids new deps). For the stale-response test, pre-write an `Advance` response, drive the first failure through `handle_stage_failure`, then drive a second failure of the same stage and assert `poll_response` does not immediately return the old response (i.e. the retry blocked/cleaned it) — assert on the absence of the stale `Gates::response_path`/`ack_path` after the first retry.
  </action>
  <verify>
    <automated>cargo test -p devflow ship_agent_failed_fires_gate ship_review_failed_loops_to_code ship_review_failed_uses_audit_fix non_validate_failure_fires_gate_and_hook stage_failure_retry_cleans_stale_response parse_gate_timeout_env_override</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow ship_agent_failed_fires_gate ship_review_failed_loops_to_code ship_review_failed_uses_audit_fix non_validate_failure_fires_gate_and_hook stage_failure_retry_cleans_stale_response parse_gate_timeout_env_override` exits 0
    - `cargo test -p devflow` (full crate) exits 0 — no existing test regressed (esp. advance_ship_success_runs_finish_workflow, validate_failure_threshold_forces_gate_then_aborts, abort_cleans_up_gate_files_so_a_later_gate_does_not_reuse_stale_response)
  </acceptance_criteria>
  <done>All six new tests pass (env-touching ones serialized under ENV_MUTEX, the timeout test is pure), the CR-01 stale-response regression is covered, and the full devflow crate suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → notify command | DEVFLOW_GATE_NOTIFY_CMD is operator-configured (trusted); gate context passed to it may contain agent-generated text (untrusted) |
| agent stdout → advance() reason | The `reason` string driving ReviewFailed/AgentFailed originates from agent output (untrusted) but is only string-matched, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-01 | Tampering | fire_gate_notify shell-out | high | mitigate | Never interpolate phase/stage/context into the `sh -c` string; pass via `.env()` vars (DEVFLOW_GATE_PHASE/STAGE/CONTEXT), following the WR-01 argv-not-shell precedent. Test asserts no string concatenation of context into the command. |
| T-13-02 | Denial of Service | notify hook blocking the loop | medium | mitigate | Fail-soft: fire_gate_notify warns and returns on non-zero exit or spawn failure, never propagates an error into run_gate. |
| T-13-03 | Denial of Service | env-configurable gate timeout set absurdly low/high | low | accept | Operator-controlled single-user tool; a bad DEVFLOW_GATE_TIMEOUT_SECS only affects the operator's own run. Falls back to 7 days on parse failure. |
| T-13-04 | Spoofing | agent emits `review:`-prefixed reason to force loop-back | low | accept | Local single-operator trust boundary; the agent is already fully trusted to run under `--dangerously-skip-permissions`. Loop-back to Code (with AuditFix) is a safe action (no merge/ship occurs). The `review:` prefix is an inherent string-convention limitation of the agent-reported DEVFLOW_RESULT contract (matched via `reason.trim().to_lowercase().starts_with("review:")`); dogfood (13-06) verifies real agent compliance. |
| T-13-18 | Tampering | stale gate `.response.json`/ack consumed by an Advance-retry of the same failed stage (CR-01 recurrence) | high | mitigate | `handle_stage_failure` calls `Gates::cleanup(phase, stage)` before any Advance/LoopBack re-launch, mirroring `loop_back_to_code`/`abort`; regression test `stage_failure_retry_cleans_stale_response` asserts no reuse. |

No new external packages are added by this plan (no npm/pip/cargo install) — package-legitimacy checkpoint not applicable.
</threat_model>

<review_findings>
Cross-AI review (13-REVIEWS.md) findings addressed by this plan:

| Finding | Severity | Reviewers | Disposition |
|---------|----------|-----------|-------------|
| CR-01: Advance-retry reuses stale gate response (no `Gates::cleanup`) | HIGH | Codex, Cursor (consensus #2) | INCORPORATED — Task 2 change 3 cleans up before every Advance/LoopBack re-launch; Task 3 `stage_failure_retry_cleans_stale_response` regression test. |
| Package name is `devflow`, not `devflow-cli`, in verify commands | HIGH (factual, confirmed) | Codex (divergent, orchestrator-confirmed) | INCORPORATED — all Task 2/3 `cargo … -p devflow-cli` normalized to `-p devflow`. |
| ReviewFailed loop-back uses GapsOnly; should use AuditFix (`/gsd-audit-fix`) | MEDIUM | OpenCode, Cursor (consensus #7) | INCORPORATED — Task 2 change 4/5 adds a FixType param; `review:` path passes `FixType::AuditFix`; `ship_review_failed_uses_audit_fix` test. |
| Env-mutating unit tests racy under Rust 2024 (`set_var` unsafe) | MEDIUM | Codex, Cursor (consensus #8) | INCORPORATED — `parse_gate_timeout` made pure (env-free test); only one env test remains, serialized under a module `ENV_MUTEX` static; no new dependency. |
| Unconditional gate in Auto mode is a behavior change; make it observable | HIGH (OpenCode) / intended (others) | OpenCode #1 (divergent) | INCORPORATED — run_gate computes `unexpected = !should_gate(...)`, emits `info!`, marks context `[never-silent]`, and sets `DEVFLOW_NON_SILENT_GATE=1` on the notify child (OpenCode suggestions 5/6). Behavior kept per WR-11. |
| Define/Plan failure LoopBack→Code is an invalid recovery target | MEDIUM | Codex 13-01 | INCORPORATED — generic `handle_stage_failure` retries the SAME failed stage on Advance/LoopBack; only Abort halts. Code jump is Validate-specific and unchanged. |
| `review:` string prefix is fragile with LLM agents | HIGH (inherent) | OpenCode #2 | ACKNOWLEDGED — exact match rule specified (trim + case-fold); documented in threat T-13-04 as an inherent DEVFLOW_RESULT limitation verified live in 13-06 (not fixable in Rust). |
| Retry loop has no circuit breaker (infinite approve→fail→approve) | MEDIUM | OpenCode #4 | DEFERRED — every retry cycle fires a gate + notification (never silent), and the operator can Abort at any gate, so there is no silent unbounded loop; a hard auto-abort counter is an observability enhancement routed to Phase 14. Rationale recorded here per the reviews contract. |
| RateLimited gated like AgentFailed loses the cron-resume path | MEDIUM | Cursor 13-01 (divergent) | DEFERRED (deliberate) — for MVP a never-silent gate+notify strictly beats a silent halt; the operator is notified and can retry/reschedule. Cron-resume (`sequentagent`) stays a separate scheduled-run mechanism; re-evaluate in Phase 14. |
</review_findings>

<verification>
- `cargo test --workspace` exits 0
- `cargo clippy --workspace -- -D warnings` exits 0
- `cargo fmt --check` exits 0
- Manual grep: `rg -n "fire_gate_notify" crates/devflow-cli/src/main.rs` shows the call inside run_gate
</verification>

<success_criteria>
- Every non-Validate stage failure surfaces a gate + notify (WR-11 closed)
- Ship AgentFailed gates+notifies; Ship ReviewFailed loops back to Code (13a failure path)
- Notify hook is fail-soft and injection-safe (13c)
- Gate timeout is env-configurable with a 7-day fallback (13c)
</success_criteria>

<output>
Create `.planning/phases/13-mvp-core-loop/13-01-SUMMARY.md` when done
</output>
