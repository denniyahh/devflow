---
phase: 13-mvp-core-loop
plan: 06
type: execute
wave: 4
depends_on: [13-01, 13-02, 13-03, 13-04, 13-05]
files_modified: []
autonomous: false
requirements: [13a, 13b, 13c, 13d, 13e]
user_setup:
  - service: notify-hook
    why: "Answer gates via ntfy/desktop notification during the unattended dogfood run (13c)"
    env_vars:
      - name: DEVFLOW_GATE_NOTIFY_CMD
        source: "Operator-chosen shell command (e.g. an ntfy publish or `notify-send`) that reads DEVFLOW_GATE_PHASE / DEVFLOW_GATE_STAGE / DEVFLOW_GATE_CONTEXT"
  - service: claude-cli
    why: "Full-loop dogfood run, Claude leg (13e)"
    env_vars: []
  - service: codex-cli
    why: "Codex leg through Code→Validate; confirm envelope parsing against the real --json stream (13b/13e)"
    env_vars: []
must_haves:
  truths:
    - "The full Define→Plan→Code→Validate→Ship loop runs end-to-end on a real external project with the Claude adapter, gates answered via the notify hook."
    - "The Ship stage completes the previously-BLOCKED 12-12 Full-Ship verification (a real PR flow) — recorded as PASS."
    - "The same loop is exercised with the Codex adapter at least through Code→Validate, confirming the JSONL envelope parser against the real --json stream."
  artifacts:
    - ".planning/phases/13-mvp-core-loop/13-06-SUMMARY.md recording dogfood evidence (commands, gate/notify round-trip, PR link, Codex --json capture)"
  key_links:
    - "13a Ship failure path + 13b envelope/verdict + 13c notify + 13d worktree → a real unattended loop"
---

<objective>
The phase's exit criterion is not "tasks complete" but "the loop ran." Exercise the full
DevFlow core loop on a real external project: a Claude full-loop run (Define→Ship) with gates
answered via the notify hook, the re-run of the Full-Ship verification recorded as BLOCKED in
`12-12-SUMMARY.md` (now unblocked by the 13a ship.rs rewrite), and a Codex leg through at
least Code→Validate to confirm the new JSONL envelope parser against a real `--json` stream.
Any failure surfaced during dogfooding is in-scope — the loop working is the deliverable.

Purpose: 13e MVP acceptance. This is the final wave; it depends on all of 13a–13d landing
and being green. These are human-in-the-loop verifications (live agents, real credentials,
real PR flow) that no automated test can stand in for.
Output: `13-06-SUMMARY.md` capturing the evidence and any follow-up gaps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mvp-core-loop/CONTEXT.md
@.planning/phases/13-mvp-core-loop/13-RESEARCH.md
@.planning/phases/12-bootstrap-housekeeping/12-12-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan produces no source symbols — it is a manual acceptance run. It produces
`13-06-SUMMARY.md` with dogfood evidence. No new files in the codebase.
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: pre-flight — workspace green, doctor clean, notify hook fires, Ship prompt headless-safe</name>
  <action>Human runs the steps in how-to-verify and reports the outcome; nothing is auto-executed. This gates the live dogfood run on 13a–13d being merged and green.</action>
  <what-built>
    Pre-flight: 13a–13d have all landed and the full workspace is green
    (`cargo test --workspace && cargo clippy --workspace -- -D warnings && cargo fmt --check`).
    The Ship-stage prompt now sequences `/gsd-code-review` before `/gsd-ship` (headless-safe,
    Plan 02), the notify hook + configurable gate timeout are wired (Plan 01), worktree is
    the default (Plan 04), and native envelope + verdict parsing are in place (Plans 03/05).
  </what-built>
  <how-to-verify>
    1. From the devflow repo root, run `cargo test --workspace` — confirm exit 0.
    2. Run `cargo clippy --workspace -- -D warnings` and `cargo fmt --check` — confirm both exit 0.
    3. Confirm `devflow doctor` reports `claude`, `codex`, `git`, and `gh` present/authenticated.
    4. Export a notify command, e.g. set `DEVFLOW_GATE_NOTIFY_CMD` to a command that reads
       `$DEVFLOW_GATE_PHASE` / `$DEVFLOW_GATE_STAGE` / `$DEVFLOW_GATE_CONTEXT` and pushes an
       ntfy/desktop notification. Verify it fires by writing a throwaway gate (or trust the
       unit coverage from Plan 01) and observing the notification.
    5. RESEARCH Pitfall 2 pre-flight: confirm the Ship-stage prompt no longer depends on an
       interactive AskUserQuestion — inspect `prompt::stage_prompt(Stage::Ship, N)` output
       (or a dry-run) to confirm `/gsd-code-review` is sequenced before `/gsd-ship`.
  </how-to-verify>
  <resume-signal>Type "approved" when the workspace is green, doctor is clean, and the notify hook fires; or describe what failed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Claude full-loop dogfood run + Full-Ship re-verification (unblocks 12-12)</name>
  <action>Human runs the steps in how-to-verify on a real external throwaway project with the Claude adapter and reports the outcome (including the PR URL); nothing is auto-executed.</action>
  <what-built>
    Claude full-loop dogfood run (13e, Claude leg) + Full-Ship re-verification (13a) — the
    item left BLOCKED in 12-12-SUMMARY.md, now unblocked by the ship.rs rewrite.
  </what-built>
  <how-to-verify>
    1. Pick a real external throwaway/side project (NOT the devflow repo) with a git remote and
       `gh` configured.
    2. Run `devflow start --phase <N> --agent claude --mode auto` (worktree is now the default;
       do NOT pass `--no-worktree`). Confirm the run creates `.worktrees/phase-NN/`.
    3. Let the loop advance Define→Plan→Code→Validate. When a gate fires, confirm the notify
       hook delivered a notification carrying the phase/stage/context, and answer the gate via
       the gate-response file (or Hermes poller) the notification points you to.
    4. At Ship: confirm `/gsd-code-review` runs, then `/gsd-ship` pushes and opens a real PR via
       `gh` (this is the Full-Ship verification that was BLOCKED in 12-12). Confirm the
       "Ship complete — approve merge?" gate fires and a notification is delivered.
    5. Deliberately (optional) exercise a failure: cause a non-Validate stage or Ship stage to
       fail and confirm it fires a gate + notification (never silent — WR-11/13a), rather than
       exiting with a dirty state.
    6. Record in 13-06-SUMMARY.md: the exact commands, the PR URL, the gate/notify round-trip,
       and whether the Ship AgentFailed / ReviewFailed paths behaved as designed.
  </how-to-verify>
  <resume-signal>Type "approved" with the PR URL and a note that the Full-Ship verification passed; or describe the failure (which is in-scope for this phase).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Verify: Codex leg through Code→Validate + real --json envelope parsing</name>
  <action>Human runs the steps in how-to-verify with the Codex adapter on a real external project and reports whether the JSONL parser matched real output; nothing is auto-executed.</action>
  <what-built>
    Codex adapter leg (13e / 13b) — exercise the loop with Codex at least through Code→Validate
    and confirm the new JSONL event-stream parser (Plan 03) matches the installed Codex CLI's
    real `--json` output.
  </what-built>
  <how-to-verify>
    1. On a real external project, run `devflow start --phase <N> --agent codex --mode auto`.
    2. Let it run at least through Code→Validate. Confirm DevFlow correctly classifies the Codex
       run (success vs. failure) from the `--json` stream — inspect
       `.devflow/phase-NN-stdout` and confirm the last `turn.completed` / `turn.failed` event was
       parsed as the RESEARCH Pitfall 4 note predicts.
    3. If the installed Codex CLI's `--json` shape differs from the schema Plan 03 was written
       against, capture the real output and record the delta in 13-06-SUMMARY.md as a follow-up
       gap (this is the empirically-safe practice 12-12-SUMMARY.md established for Claude).
    4. Confirm the Validate verdict split (Plan 05) behaves: if Codex validation finds gaps, the
       loop does NOT advance to Ship.
  </how-to-verify>
  <resume-signal>Type "approved" with a note confirming the Codex --json parser matched real output (or the recorded delta); or describe what diverged.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator notify command → external notification service | DEVFLOW_GATE_NOTIFY_CMD is operator-configured; context is passed via env vars (Plan 01), not interpolated |
| live agent (`--dangerously-skip-permissions`) → worktree | The dogfood run executes a permission-bypassed agent; the worktree default (Plan 04) is the isolation boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-15 | Elevation of Privilege | permission-bypassed agent during a real dogfood run | high | mitigate | Run defaults to worktree isolation (Plan 04); pre-flight checkpoint confirms `.worktrees/phase-NN/` is created. Use a throwaway external project, not a production repo. |
| T-13-16 | Denial of Service | Ship-stage interactive-review hang in headless mode (RESEARCH Pitfall 2) | high | mitigate | Plan 02 sequences non-interactive `/gsd-code-review` before `/gsd-ship`; pre-flight checkpoint verifies the prompt before the live run. |
| T-13-17 | Information Disclosure | notify command leaking gate context to an external service | low | accept | Operator explicitly configures the notify command and chooses the destination; single-operator local tool. |

No new external packages are added by this plan (no npm/pip/cargo install) — package-legitimacy checkpoint not applicable.
</threat_model>

<verification>
- Full workspace green before the run (cargo test/clippy/fmt)
- A real PR opened by the Claude Ship stage (Full-Ship verification unblocked)
- Codex `--json` stream correctly classified by the new parser
- 13-06-SUMMARY.md records commands, PR URL, gate/notify round-trip, and any follow-up gaps
</verification>

<success_criteria>
- Claude full-loop dogfood run completes with gates answered via the notify hook (13e)
- The 12-12 BLOCKED Full-Ship verification is re-run and passes (13a/13e)
- Codex leg confirms envelope parsing against the real --json stream (13b/13e)
</success_criteria>

<output>
Create `.planning/phases/13-mvp-core-loop/13-06-SUMMARY.md` when done
</output>
