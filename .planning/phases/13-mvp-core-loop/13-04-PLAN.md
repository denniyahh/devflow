---
phase: 13-mvp-core-loop
plan: 04
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - crates/devflow-cli/src/main.rs
  - crates/devflow-cli/tests/phase7_cli.rs
autonomous: true
requirements: [13d]
must_haves:
  truths:
    - "`devflow start` with no worktree flag creates an isolated git worktree by default."
    - "An explicit `--no-worktree` opt-out runs the agent on a feature branch in the primary checkout (the old default)."
    - "`parallel` and `sequentagent` are unaffected by the default flip (they call start() with an explicit bool / create their own worktrees)."
  artifacts:
    - "crates/devflow-cli/src/main.rs (Start.no_worktree opt-out flag + call-site inversion + help text)"
    - "crates/devflow-cli/tests/phase7_cli.rs (worktree-by-default + opt-out assertions)"
  key_links:
    - "Command::Start.no_worktree → start(worktree: !no_worktree) → ensure_phase_worktree"
---

<objective>
Flip the `devflow start` default so it runs the agent inside an isolated git worktree at
`.worktrees/phase-NN/` unless an explicit `--no-worktree` opt-out is passed. Today the
default runs a `--dangerously-skip-permissions` agent directly in the primary checkout —
the worktree is the isolation boundary that makes unattended runs safe. Update the CLI help
text for the new default and confirm the `parallel` / `sequentagent` commands (which pass an
explicit worktree bool / create their own worktrees) are unaffected.

Purpose: 13d — unattended-safety default. This is a CLI-layer change; `worktree::add`,
`ensure_phase_worktree`, and `recover.rs`'s stale-state detection are already
worktree-path-aware. Runs in Wave 2 because it edits `crates/devflow-cli/src/main.rs`,
which Wave 1 Plan 01 also edits (different functions, same file — sequential to avoid a
same-wave file conflict).
Output: `--no-worktree` opt-out; inverted default; updated help; regression tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mvp-core-loop/CONTEXT.md
@.planning/phases/13-mvp-core-loop/13-RESEARCH.md
@.planning/phases/13-mvp-core-loop/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan (exclude from drift/source-grounding review):
- `Command::Start.no_worktree: bool` — renamed/inverted CLI arg (replaces the `worktree: bool` opt-in field) in `crates/devflow-cli/src/main.rs`
- New `--no-worktree` CLI flag; the `--worktree` flag is removed (worktree is now default)
- New tests in `crates/devflow-cli/tests/phase7_cli.rs`: `start_defaults_to_worktree`, `start_no_worktree_uses_feature_branch`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Invert the Start worktree default to opt-out</name>
  <files>crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-cli/src/main.rs lines 29-54 (Command::Start struct — the `worktree: bool` #[arg(long)] field and the `agent` field's default_value idiom)
    - crates/devflow-cli/src/main.rs lines 195-213 (the Start match arm that destructures `worktree` and calls `start(...)`)
    - crates/devflow-cli/src/main.rs lines 251-315 (start() signature and its `if worktree { ensure_phase_worktree } else { feature_start }` body — the bool sense to preserve)
    - crates/devflow-cli/src/main.rs lines 649-660, 738-775 (parallel() calls start(..., true, false); sequentagent creates its own worktrees — confirm neither reads the CLI default)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "13d" and Open Question #3 (parallel/sequentagent pass explicit bools, so the flip must not change them); Anti-Pattern (do NOT remove --dangerously-skip-permissions — the worktree is the isolation boundary, not the flag)
  </read_first>
  <action>
    Rename the `Start` struct's `worktree: bool` field to `no_worktree: bool` with `#[arg(long)]` and a doc comment describing it as the opt-out ("Run the agent directly in the primary checkout instead of an isolated worktree (not recommended for unattended runs)"). Update the Start match arm to destructure `no_worktree` and pass `worktree: !no_worktree` into the existing `start(...)` call — leaving `start()`'s internal `worktree: bool` parameter and body unchanged so `parallel()`/`sequentagent`'s explicit-bool call sites are untouched. Update the doc comment on the removed `--worktree` semantics and the `Start` subcommand help so `devflow start --help` documents that a worktree is created by default and `--no-worktree` opts out. Do NOT remove `--dangerously-skip-permissions` from the Claude adapter — the worktree is the safety boundary. Confirm no other module references the old `--worktree` CLI flag name.
  </action>
  <verify>
    <automated>cargo build -p devflow-cli && cargo clippy -p devflow-cli -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - `main.rs` Start struct contains `no_worktree: bool` and no longer contains a `worktree: bool` arg field (source assertion)
    - The Start call site passes `!no_worktree` to start() (source assertion: `!no_worktree` present)
    - `parallel` still calls `start(..., true, false)` unchanged (source assertion)
    - `devflow start --help` output mentions `--no-worktree` (CLI assertion: `cargo run -p devflow-cli -- start --help` contains `--no-worktree`)
    - `cargo clippy -p devflow-cli -- -D warnings` exits 0
  </acceptance_criteria>
  <done>`devflow start` defaults to a worktree; `--no-worktree` opts out; parallel/sequentagent unchanged; help text updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for worktree-by-default and opt-out</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    - crates/devflow-cli/tests/phase7_cli.rs lines 1-70 (init_repo / devflow_bin / Command helpers)
    - crates/devflow-cli/tests/phase7_cli.rs lines 336-392 (reference_and_cleanup_worktree_cli_flow — currently invokes `start ... --worktree`; this MUST be updated since `--worktree` is removed, and the default now creates a worktree)
    - crates/devflow-cli/tests/phase7_cli.rs lines 125-150 (parallel_creates_two_worktrees_and_spawns_two_monitors — the fake-bin monitor-avoidance pattern to reuse so `start` does not launch a real agent)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "Security Domain" (add a test asserting the DEFAULT no-flag path creates a worktree — the EoP mitigation)
  </read_first>
  <behavior>
    - `start_defaults_to_worktree`: running `devflow start --phase N --mode auto` with NO worktree flag creates `.worktrees/phase-NN/` (default is worktree).
    - `start_no_worktree_uses_feature_branch`: running `devflow start --phase N --mode auto --no-worktree` does NOT create `.worktrees/phase-NN/` and instead creates the feature branch in the primary checkout.
    - The existing `reference_and_cleanup_worktree_cli_flow` test is updated to the new flag surface and still passes.
  </behavior>
  <action>
    Update `reference_and_cleanup_worktree_cli_flow` to remove the now-invalid `--worktree` argument (worktree is the default). Add `start_defaults_to_worktree` asserting the default (no flag) path creates `.worktrees/phase-NN/`, and `start_no_worktree_uses_feature_branch` asserting `--no-worktree` skips worktree creation. Reuse the existing fake-bin / PATH-shim harness (as parallel_creates_two_worktrees... does) so `start` spawns a stub agent rather than a real Claude/Codex process. Follow the existing tempdir git-repo setup helpers; do not add new test infrastructure.
  </action>
  <verify>
    <automated>cargo test -p devflow-cli --test phase7_cli start_defaults_to_worktree start_no_worktree_uses_feature_branch reference_and_cleanup_worktree_cli_flow</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow-cli --test phase7_cli start_defaults_to_worktree start_no_worktree_uses_feature_branch` exits 0
    - `cargo test -p devflow-cli --test phase7_cli` (full integration file) exits 0 — reference_and_cleanup_worktree_cli_flow updated and green
    - `rg -c '"--worktree"' crates/devflow-cli/tests/phase7_cli.rs` returns 0 (removed flag no longer referenced)
  </acceptance_criteria>
  <done>Default-worktree and --no-worktree paths are asserted; the old --worktree CLI test is migrated; integration suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → agent execution locus | The default determines whether a permission-bypassed agent runs in the primary checkout (dangerous) or an isolated worktree (safe) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Elevation of Privilege | `--dangerously-skip-permissions` agent running in the primary checkout by default | high | mitigate | Flip the default to worktree-isolated; add an explicit test asserting the no-flag default creates a worktree (not just that flags work). Opt-out is explicit and documented. |
| T-13-12 | Tampering | default flip silently changes parallel/sequentagent behavior | medium | mitigate | Those commands call start() with explicit bools / create their own worktrees; acceptance criteria assert `start(..., true, false)` unchanged and their tests stay green. |

No new external packages are added by this plan (no npm/pip/cargo install) — package-legitimacy checkpoint not applicable.
</threat_model>

<verification>
- `cargo test --workspace` exits 0
- `cargo clippy --workspace -- -D warnings` exits 0
- `cargo fmt --check` exits 0
- `cargo run -p devflow-cli -- start --help` output contains `--no-worktree`
</verification>

<success_criteria>
- Worktree is the default; `--no-worktree` opts out (13d)
- parallel/sequentagent unaffected; --dangerously-skip-permissions retained (13d)
</success_criteria>

<output>
Create `.planning/phases/13-mvp-core-loop/13-04-SUMMARY.md` when done
</output>
