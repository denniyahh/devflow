---
phase: 13-mvp-core-loop
plan: 05
type: execute
wave: 3
depends_on: [13-01, 13-02, 13-03, 13-04]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-core/src/prompt.rs
  - crates/devflow-cli/src/main.rs
autonomous: true
requirements: [13b]
must_haves:
  truths:
    - "A Validate agent that completes its task but finds gaps reports `verdict: gaps`, and DevFlow does NOT advance it to Ship — it loops back to Code (or gates)."
    - "At the Validate stage, ONLY an explicit `verdict: pass` advances to Ship. A missing verdict OR `verdict: gaps` is treated as not-passed (gate/loop) — because the Validate prompt now requires the verdict, its absence is a fail-safe, NOT a silent pass. This closes the Plan 03↔05 composition bug where a marker-less/verdict-less Validate could reach Ship."
    - "Non-Validate stages do not consult verdict at all, so their markers are unaffected by this change."
    - "A malformed / unknown / miscased verdict string (e.g. `\"wat\"`, `\"Pass\"`) deserializes to `None` via a lenient case-insensitive deserializer and NEVER breaks the whole DEVFLOW_RESULT marker parse (no fallthrough to Layer 2 from a bad verdict)."
    - "Every `AgentResult { .. }` struct literal across the workspace — including `run_agent_blocking` in main.rs — sets the new `verdict` field so the workspace compiles."
  artifacts:
    - "crates/devflow-core/src/agent_result.rs (Verdict enum + AgentResult.verdict + lenient deserializer)"
    - "crates/devflow-core/src/prompt.rs (Validate prompt requires a verdict)"
    - "crates/devflow-cli/src/main.rs (advance() Validate arm consults verdict; run_agent_blocking literal updated)"
  key_links:
    - "Validate prompt verdict instruction → parse_devflow_result lenient verdict field → advance() Validate arm (Some(Pass) advances; None/Gaps gate) → handle_validate_outcome(passed)"
---

<objective>
Split "the agent ran validation" from "validation passed." Today `advance()` treats a
Validate stage's `DEVFLOW_RESULT: success` as passed → Ship. But an agent that runs
`/gsd-validate-phase` and *finds gaps* succeeded at its task while validation failed. This
plan adds a distinct `verdict` field (`pass` | `gaps`) to the DEVFLOW_RESULT contract, parses
it into `AgentResult`, requires the Validate prompt to emit it, and makes `advance()`'s
Validate arm treat `verdict: gaps` as a validation failure (loop back to Code / gate) rather
than advancing to Ship.

Purpose: 13b verdict-vs-ran correctness — the loop's success signal must mean "the work
passed," not "the agent exited." Runs in Wave 3: it edits agent_result.rs (Plan 03,
Wave 1), prompt.rs (Plan 02, Wave 1), and main.rs advance() (Plans 01/04, Waves 1/2), so it
sequences after all of them.
Output: a `Verdict` enum, verdict parsing, a verdict-requiring Validate prompt, and a
verdict-aware advance() Validate arm.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-mvp-core-loop/CONTEXT.md
@.planning/phases/13-mvp-core-loop/13-RESEARCH.md
@.planning/phases/13-mvp-core-loop/13-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols created or changed by THIS plan (exclude from drift/source-grounding review):
- `Verdict` enum (`Pass`, `Gaps`) with `#[serde(rename_all = "lowercase")]` in `crates/devflow-core/src/agent_result.rs`
- `AgentResult.verdict: Option<Verdict>` field (with `#[serde(default, deserialize_with = "deserialize_verdict_lenient")]`)
- `deserialize_verdict_lenient` — a custom case-insensitive deserializer mapping `"pass"`→Pass, `"gaps"`→Gaps, anything else (incl. absent/miscased/unknown) → None, so a bad verdict never fails the whole `AgentResult` parse
- Verdict parsing wired through `parse_devflow_result` / `parse_marker_lines`
- ALL `AgentResult { .. }` construction sites updated with `verdict: None` — `agent_result.rs:221`, `agent_result.rs:275`, `agent_result.rs:325`, AND `crates/devflow-cli/src/main.rs:711` (run_agent_blocking)
- Validate-stage prompt now requires a `verdict` in DEVFLOW_RESULT (`crates/devflow-core/src/prompt.rs`)
- `advance()` Validate SUCCESS arm consults verdict in `crates/devflow-cli/src/main.rs` (only Some(Pass) advances)
- New tests: `parse_devflow_result_reads_verdict`, `parse_devflow_result_reads_verdict_pass`, `parse_devflow_result_verdict_absent_is_none`, `parse_devflow_result_malformed_verdict_is_none_not_parse_error`, `validate_gaps_does_not_advance_to_ship`, `validate_missing_verdict_does_not_advance`, `validate_pass_advances`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a Verdict field to the DEVFLOW_RESULT contract</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs lines 13-35 (AgentResult struct + AgentStatus enum with `#[serde(rename_all = "lowercase")]` — the serde convention to mirror for Verdict)
    - crates/devflow-core/src/agent_result.rs lines 52-215 (parse_devflow_result / parse_marker_lines — where the DEVFLOW_RESULT JSON object is decoded; the verdict key is read here)
    - crates/devflow-core/src/agent_result.rs lines 460-680 (existing marker-parsing tests — the pattern for a new verdict-parse test)
    - .planning/phases/13-mvp-core-loop/13-RESEARCH.md § "Open Questions" #1 (prefer a distinct field; verdict is `pass`|`gaps`) and CONTEXT.md § 13b ("Add a distinct verdict field (e.g. `verdict: pass|gaps`)")
  </read_first>
  <behavior>
    - `parse_devflow_result_reads_verdict`: `DEVFLOW_RESULT: {"status":"success","verdict":"gaps"}` parses to AgentResult with status Success and verdict Some(Verdict::Gaps).
    - `parse_devflow_result_reads_verdict_pass`: `"verdict":"pass"` parses to Some(Verdict::Pass).
    - `parse_devflow_result_verdict_absent_is_none`: a marker with no verdict key parses with verdict None.
    - `parse_devflow_result_malformed_verdict_is_none_not_parse_error`: `{"status":"success","verdict":"wat"}` AND `{"status":"success","verdict":"Pass"}` (capitalized) BOTH parse successfully to status Success with verdict None — the whole AgentResult marker is NOT dropped to Layer 2 (consensus #5). This is the critical regression the lenient deserializer exists for.
  </behavior>
  <action>
    Add a `Verdict` enum with variants `Pass` and `Gaps`, deriving Debug/Clone/Copy/PartialEq/Eq/Serialize/Deserialize and `#[serde(rename_all = "lowercase")]` to mirror AgentStatus.

    Add `verdict: Option<Verdict>` to `AgentResult`, but do NOT rely on the derived `Option<Verdict>` deserialize — with derived serde a bad value like `"wat"` or the mis-cased `"Pass"` makes `serde_json::from_str::<AgentResult>(json_str)` fail for the ENTIRE marker (confirmed: `parse_marker_lines` uses `from_str::<AgentResult>` at agent_result.rs:209), silently dropping a valid `status` to Layer 2 (consensus #5). Instead annotate the field `#[serde(default, deserialize_with = "deserialize_verdict_lenient")]` and implement `fn deserialize_verdict_lenient<'de, D>(d: D) -> Result<Option<Verdict>, D::Error>` that deserializes an `Option<String>`, lowercases it, and maps `"pass"`→`Some(Verdict::Pass)`, `"gaps"`→`Some(Verdict::Gaps)`, and ANYTHING else (unknown, mis-cased, absent) → `Ok(None)` — never an error. This makes a malformed verdict a controlled `None`, not a parse failure of the whole marker.

    Extend the decode path so the verdict key is read through this lenient path when the marker JSON is parsed into `AgentResult`. Add `///` doc comments.

    Update EVERY `AgentResult { .. }` struct literal across the workspace to set `verdict: None` — not only the ones in this file. Confirmed sites (run `rg -n "AgentResult \{"` to re-verify before editing): `agent_result.rs:221` (rate-limit), `agent_result.rs:275` (layer2), `agent_result.rs:325` (layer3), AND `crates/devflow-cli/src/main.rs:711` (`run_agent_blocking`, in the CLI crate — package `devflow`). Missing the main.rs literal breaks the workspace build (consensus #4).
  </action>
  <verify>
    <automated>cargo build --workspace && cargo test -p devflow-core parse_devflow_result_reads_verdict parse_devflow_result_verdict_absent_is_none parse_devflow_result_malformed_verdict_is_none_not_parse_error</automated>
  </verify>
  <acceptance_criteria>
    - `agent_result.rs` contains `enum Verdict` with `Pass` and `Gaps`
    - `AgentResult` has `verdict: Option<Verdict>` with `deserialize_with = "deserialize_verdict_lenient"` (source assertion)
    - `rg -c "verdict: None" crates/devflow-cli/src/main.rs` returns ≥ 1 (run_agent_blocking literal updated)
    - `cargo build --workspace` exits 0 (all AgentResult literals updated — no missing-field error)
    - `cargo test -p devflow-core parse_devflow_result_reads_verdict parse_devflow_result_verdict_absent_is_none parse_devflow_result_malformed_verdict_is_none_not_parse_error` exits 0
    - `cargo test -p devflow-core` full crate exits 0 (no serde/marker regression)
  </acceptance_criteria>
  <done>Verdict enum + lenient deserializer parse from the marker; absent/malformed/miscased verdict is a controlled None (never a whole-marker parse failure); every AgentResult literal incl. main.rs:711 sets verdict; workspace builds; all core tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require a Validate verdict in the prompt and consult it in advance()</name>
  <files>crates/devflow-core/src/prompt.rs, crates/devflow-cli/src/main.rs</files>
  <read_first>
    - crates/devflow-core/src/prompt.rs (COMPLETION_PROTOCOL, stage_prompt — the Validate stage prompt to extend with the verdict requirement; note Plan 02 already special-cased Stage::Ship here)
    - crates/devflow-cli/src/main.rs lines 336-423 (advance() — the `Stage::Validate => handle_validate_outcome(project_root, &mut state, true)` success arm at ~line 382, and handle_validate_outcome which takes `passed: bool`)
    - crates/devflow-core/src/agent_result.rs (Verdict enum + AgentResult.verdict from Task 1)
    - .planning/phases/13-mvp-core-loop/CONTEXT.md § 13b (Validate-with-gaps must NOT advance to Ship)
  </read_first>
  <behavior>
    - `validate_gaps_does_not_advance_to_ship`: an advance() with a Validate-stage AgentResult of status Success + verdict Gaps drives handle_validate_outcome with passed=false (loops back to Code / gates), NOT a transition to Ship.
    - `validate_missing_verdict_does_not_advance`: an advance() with a Validate-stage AgentResult of status Success + verdict None drives handle_validate_outcome with passed=false — because the Validate prompt now REQUIRES a verdict, its absence is a fail-safe (gate/loop), NOT a silent pass (consensus #1). Does NOT transition to Ship.
    - `validate_pass_advances`: a Validate result with status Success + verdict Pass advances to Ship.
    - Validate-stage prompt contains a verdict instruction (behavior assertion): the agent must report `verdict: "pass"` or `verdict: "gaps"` (exact lowercase strings).
  </behavior>
  <action>
    In prompt.rs, extend the Validate-stage prompt (special-case Stage::Validate, or extend the shared COMPLETION_PROTOCOL text used for Validate) to instruct the agent that after running `/gsd-validate-phase {N}` it MUST include a `verdict` field in its DEVFLOW_RESULT: exactly `"verdict": "pass"` (lowercase) if validation found no gaps, or `"verdict": "gaps"` (lowercase) if it found gaps — distinct from `status`, which reports only whether the validation task itself completed. State the exact lowercase spelling because the wire format is `#[serde(rename_all = "lowercase")]`.

    In main.rs advance(), change the Validate SUCCESS arm so that instead of always calling `handle_validate_outcome(..., true)`, it computes `passed` from the verdict as `let passed = matches!(result.verdict, Some(Verdict::Pass));` — i.e. ONLY an explicit `verdict: pass` advances; `Some(Gaps)` OR `None` → `passed = false` (gate/loop). This is the deliberate composition fix (consensus #1): now that the prompt requires the verdict, a missing verdict at Validate must be treated as gaps, not backward-compat pass — otherwise a marker-less/verdict-less Validate could reach Ship (Plan 03's turn.completed and Layer-2 changes already prevent a bare success at Validate, and this arm is the final gate). Then call `handle_validate_outcome(project_root, &mut state, passed)`. Import `Verdict` from devflow_core::agent_result. Do NOT consult verdict for any non-Validate stage. Do not change the Validate FAILURE arm (handled in Plan 01). Add the three cli tests and the prompt test.

    NOTE (accepted for MVP — OpenCode #6): `handle_validate_outcome` still increments `consecutive_failures` for a `passed=false` result, so a legitimately gaps-finding agent accrues failures toward the forced-gate threshold the same as a crashing agent. This conflation is acceptable for MVP (both correctly surface a gate); documented in review_findings, not changed here.
  </action>
  <verify>
    <automated>cargo test -p devflow validate_gaps_does_not_advance_to_ship validate_missing_verdict_does_not_advance validate_pass_advances && cargo test -p devflow-core prompt</automated>
  </verify>
  <acceptance_criteria>
    - `main.rs` advance() Validate success arm computes `passed` as `matches!(result.verdict, Some(Verdict::Pass))` (source assertion: `Some(Verdict::Pass)` referenced in advance())
    - Validate-stage prompt output contains a `verdict` instruction naming the exact lowercase `pass`/`gaps` strings (behavior assertion in a prompt test)
    - `cargo test -p devflow validate_gaps_does_not_advance_to_ship validate_missing_verdict_does_not_advance validate_pass_advances` exits 0
    - `cargo test --workspace` exits 0
  </acceptance_criteria>
  <done>Validate advances to Ship ONLY on explicit verdict:pass; gaps AND missing-verdict gate/loop (no false-advance); the Validate prompt requires the exact lowercase verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent verdict → advance() gating | The `verdict` value is agent-reported (untrusted self-report); it only gates advancement, never authorizes a merge directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | Spoofing | agent reports `verdict: pass` despite real gaps | medium | accept | Single-operator local trust boundary; the Ship stage still runs code-review (Plan 02) and the 13e dogfood run manually verifies the real Validate→Ship gate. Verdict is a correctness improvement over the current status-only signal, not a security control. |
| T-13-14 | Tampering | malformed/miscased verdict value breaks the WHOLE marker deserialization, silently dropping a valid status to Layer 2 | medium | mitigate | `#[serde(default, deserialize_with = "deserialize_verdict_lenient")]` maps any unknown/mis-cased/absent value to a controlled `None` WITHOUT failing `from_str::<AgentResult>`; `parse_devflow_result_malformed_verdict_is_none_not_parse_error` asserts the whole marker still parses. Downstream, a `None` verdict at Validate is treated as not-passed (fail-safe), so a mangled verdict cannot cause a false advance either. |

No new external packages are added by this plan (no npm/pip/cargo install) — package-legitimacy checkpoint not applicable.
</threat_model>

<review_findings>
Cross-AI review (13-REVIEWS.md) findings addressed by this plan:

| Finding | Severity | Reviewers | Disposition |
|---------|----------|-----------|-------------|
| Missing verdict defaults to pass → composes with Plan 03 to false-advance a marker-less Validate to Ship | HIGH | Codex, OpenCode, Cursor (consensus #1) | INCORPORATED — Task 2 advance() Validate arm advances ONLY on `Some(Verdict::Pass)`; `None`/`Gaps` gate. `validate_missing_verdict_does_not_advance` test. Coordinated with Plan 03. |
| Unknown/miscased verdict breaks the whole `from_str::<AgentResult>` parse (derived serde does not ignore it) | HIGH/MEDIUM | Codex, OpenCode, Cursor (consensus #5) | INCORPORATED — Task 1 adds `deserialize_verdict_lenient` (case-insensitive, unknown→None, never errors) + `parse_devflow_result_malformed_verdict_is_none_not_parse_error` test. |
| `AgentResult` literal in `run_agent_blocking` (main.rs:711) must gain `verdict` or the workspace won't compile | HIGH | Codex, OpenCode, Cursor (consensus #4) | INCORPORATED — Task 1 enumerates ALL literals (agent_result.rs:221/275/325 + main.rs:711) and the acceptance builds `--workspace`. |
| Package name is `devflow`, not `devflow-cli` | HIGH (factual) | Codex (confirmed) | INCORPORATED — Task 2 test commands normalized to `-p devflow`. |
| Validate prompt must be special-cased carefully (Plan 02 already special-cased Ship) | MEDIUM | Codex 13-05 | INCORPORATED — Task 2 special-cases Stage::Validate; generic template preserved for other stages; prompt tests guard no regression. |
| `consecutive_failures` conflates a gaps-finding agent with a crashing agent | MEDIUM | OpenCode #6 | ACCEPTED (MVP) — documented in Task 2 note + T-13-13; both correctly surface a gate, so behavior is safe; refine in a later phase if noise is observed. |
| Agent self-report verdict is spoofable; a secondary validation-artifact read is not planned | MEDIUM | Cursor 13-05 | ACCEPTED — trust boundary T-13-13; artifact-based verdict is a CONTEXT.md alternative deferred beyond MVP; dogfood (13-06) verifies the real gate. |
</review_findings>

<verification>
- `cargo test --workspace` exits 0
- `cargo clippy --workspace -- -D warnings` exits 0
- `cargo fmt --check` exits 0
</verification>

<success_criteria>
- Validate-with-gaps does not advance to Ship (13b verdict-vs-ran)
- Verdict pass/absent preserves the advance-to-Ship path (13b)
</success_criteria>

<output>
Create `.planning/phases/13-mvp-core-loop/13-05-SUMMARY.md` when done
</output>
