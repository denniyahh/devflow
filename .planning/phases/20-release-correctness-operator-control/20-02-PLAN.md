---
phase: 20-release-correctness-operator-control
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/tests/phase7_cli.rs
autonomous: true
requirements: [20b]

must_haves:
  truths:
    - "devflow cleanup --force refuses (hard, no override) to remove a worktree whenever the recorded agent pid is ALIVE (regardless of monitor liveness — including Unknown/monitor_pid=None and Stuck/dead-monitor) OR the monitor is alive (Healthy/BetweenStages), and names devflow resume as the unblocking action (D-06, review: fail-closed on live agent)."
    - "cleanup proceeds to remove a worktree ONLY when the agent is dead AND the monitor is not active; for that genuinely-dead phase it retries git worktree remove --force with bounded backoff to absorb the transient Directory-not-empty race, and if retries exhaust it prints a descriptive warning that the operator must manually clear the worktree directory (D-06, review: warn-not-silent)."
    - "cleanup run twice succeeds: the second run finds the worktree already gone and does not error (edge-probe 20b/idempotency)."
    - "A live agent with no recorded monitor (monitor_pid=None ⇒ Unknown liveness) is REFUSED, and a live agent under a dead monitor (Stuck liveness) is REFUSED — the guard keys on agent liveness, not only on Healthy/BetweenStages (review: Codex HIGH)."
    - "cleanup run while a live monitor is writing into the worktree does not race the removal — the liveness guard refuses before git worktree remove is attempted (edge-probe 20b/concurrency)."
    - "The two flaky fixtures reference_and_cleanup_worktree_cli_flow (phase7_cli.rs:534) and start_worktree_mode_ignores_main_checkout_divergence (phase7_cli.rs:236) no longer flake under CI concurrency — instance 2 fixed fixture-side only (fsync durability / shrunk loop) per D-05."
    - "git worktree prune is NOT used as the primary recovery — prune only removes metadata for already-absent directories and would orphan leftover files on disk (Pitfall 3)."
  artifacts:
    - crates/devflow-cli/src/commands.rs
    - crates/devflow-cli/tests/phase7_cli.rs
  key_links:
    - "cleanup (commands.rs:292) → liveness() (commands.rs:371) + Liveness enum (commands.rs:341) → refuse-or-retry decision → worktree::remove (worktree.rs:105)."
---

<objective>
20b — `phase7_cli.rs` git fixtures are unreliable under CI, and the underlying
`cleanup --force` worktree-removal race is product-reachable.

Two distinct CI flakes surfaced on release-path PRs in one day: a
`git worktree remove` "Directory not empty" race
(`reference_and_cleanup_worktree_cli_flow`, phase7_cli.rs:534) and a git
object-store "invalid object" corruption mid-way through a 60-commit loop
(`start_worktree_mode_ignores_main_checkout_divergence`, phase7_cli.rs:236).

Research CONFIRMED instance 1 is a real product defect (resolving D-04):
`commands::cleanup` (commands.rs:292-335) removes every worktree
unconditionally — it never loads `workflow::list_states`, never checks
`monitor_pid`, never calls the existing `liveness()` predicate built in 18b for
exactly this. A real `devflow cleanup --force` run against a phase whose monitor
is still alive races the agent process's writes inside the worktree against
`git worktree remove`'s clean-directory scan.

Purpose: fix the product defect (liveness-gated removal + bounded retry) so the
fixture goes green as a consequence, and harden the fixtures' durability so
instance 2 stops flaking. Instance 2 is fixed FIXTURE-SIDE ONLY per D-08; the
`devflow parallel` object-store analog is deferred to backlog (see below).

Output: a liveness guard + bounded-backoff retry in `cleanup`; fixture
durability knobs in `phase7_cli.rs`; regression + idempotency tests.
</objective>

<review_incorporation>
## Review Incorporation (Phase 20 cross-AI review)

Findings from `20-REVIEWS.md` touching this plan (full text in that file):

- **[HIGH — Codex + Hermes MEDIUM] Guard must FAIL-CLOSED on a live agent pid, not only Healthy/BetweenStages** → INCORPORATED. `liveness()` (commands.rs:371) returns `Unknown` when `monitor_pid` is `None` — which includes an agent live with no recorded monitor (pre-18b state, or a monitor never spawned) — and `Stuck` for dead-monitor + live-agent (matrix `(false, _) => Stuck`, commands.rs:377). A guard that proceeds on Stuck/Unknown could STILL delete a worktree out from under a LIVE agent. The refuse predicate is TIGHTENED (no new flag — stays within locked D-06): refuse removal whenever the recorded agent pid is alive (`agent_alive`, regardless of monitor liveness) OR the monitor is alive (`Healthy`/`BetweenStages`); proceed only when the agent is dead AND the monitor is not active. The `Unknown` case is now EXPLICIT — it is not a blanket "proceed." Two required tests added: (a) live agent pid with `monitor_pid = None` (Unknown liveness) — Task 1; (b) dead-monitor / live-agent (Stuck liveness) — Task 2. must_haves truths #1/#2 rewritten to the tightened predicate.
- **[MEDIUM — Codex] Define the worktree→phase join** → INCORPORATED. Task 2 matches each `git worktree list` entry to its owning `State` by `State.worktree_path == wt.path` FIRST (persisted at commands.rs:175, read at :423), with a branch/path fallback only when no `worktree_path` match exists.
- **[LOW — Antigravity] Retry-failure must warn, not fail silently** → INCORPORATED. Task 2/Task 3: if bounded-backoff retry ultimately fails on a genuinely-dead phase, print a descriptive warning naming the worktree directory the operator must clear manually (not a silent failure).
- **[LOW — Hermes] Surface "CI-on-branch, not local-green" sign-off as an explicit acceptance criterion** → INCORPORATED. Added as a Task 3 acceptance criterion (previously only in the verification section).
- **D-06 consistency:** the fix TIGHTENS the refuse predicate; it adds NO override flag, keeps bounded-backoff retry for genuinely-dead phases, and never uses `git worktree prune` as primary recovery — exactly as D-06 locks.
- **DEN-51 deferral preserved:** the `devflow parallel` object-store race stays deferred to backlog (D-08) — see `<output>`.
</review_incorporation>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-correctness-operator-control/CONTEXT.md
@.planning/phases/20-release-correctness-operator-control/20-RESEARCH.md
@.planning/phases/20-release-correctness-operator-control/20-PATTERNS.md
</context>

<artifacts_produced>
New symbols this plan creates (source-grounding drift pass MUST exclude these):
- A liveness-guard + bounded-backoff-retry block inside
  `commands::cleanup` (no new public fn required; reuses `liveness()` /
  `Liveness`). A small private retry helper is at executor discretion.
- New `#[test]` in `crates/devflow-cli/tests/phase7_cli.rs`:
  `cleanup_force_refuses_on_live_agent_unknown_monitor` (case a: live agent, monitor_pid=None),
  `cleanup_force_refuses_on_dead_monitor_live_agent` (case b: Stuck liveness, live agent),
  `cleanup_is_idempotent_when_worktree_already_removed`.
- A test-repo durability helper (e.g. `harden_fixture_git_durability`) applying
  `core.fsyncObjectFiles`/`core.fsync` in the fixture init path — name at
  executor discretion.
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED — cleanup --force removes a live agent's worktree (Unknown monitor)</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    - crates/devflow-cli/tests/phase7_cli.rs:534 (`reference_and_cleanup_worktree_cli_flow`) — the CLI-invocation harness style (assert_cmd / spawned `devflow` binary) to mirror for the new test.
    - crates/devflow-cli/src/commands.rs:292-335 (`cleanup`) — the unguarded removal loop; note there is NO liveness check before `worktree::remove` at :308 today.
    - crates/devflow-cli/src/commands.rs:341-380 (`Liveness` enum + `liveness()`) — the predicate the guard will consult; note `liveness(None, _, _) => Unknown` (:373) and `(false, _) => Stuck` (:377): an Unknown or Stuck classification can STILL sit over a LIVE agent, so the guard must ALSO key on agent liveness (review: Codex HIGH).
    - crates/devflow-cli/src/commands.rs:742-746 (`agent_pid_from_file`) + :446 (`agent_alive = agent_pid.is_some_and(agent::agent_running)`) — the agent pid comes from `agent_result::agent_pid_path`, NOT from `State`; write that file with `std::process::id()` for a deterministically-alive agent.
    - crates/devflow-core/src/state.rs:118-133 (`State::new`) + :72 (`monitor_pid`) — construct + persist a State; for case (a) leave `monitor_pid = None` (Unknown liveness).
    - PATTERNS.md § phase7_cli.rs — write a live agent pid file + a State with `monitor_pid = None` before invoking `cleanup --force`; assert removal is refused with a `devflow resume` message.
  </read_first>
  <behavior>
    - Given a phase whose agent pid file holds a still-running pid (`std::process::id()`, trivially alive) and whose persisted State has `monitor_pid = None` (so `liveness()` classifies it `Unknown`, NOT Healthy), `devflow cleanup --force` must REFUSE to remove that phase's worktree and exit non-zero with a message naming `devflow resume`.
    - Against current HEAD this test FAILS (cleanup removes unconditionally, exits 0) — and it is the exact case a naive "proceed on Unknown/Stuck" predicate would ALSO wrongly remove.
  </behavior>
  <action>Add `cleanup_force_refuses_on_live_agent_unknown_monitor` to `phase7_cli.rs`. Set up a worktree for a phase (reuse the existing worktree-setup helper), write the agent pid file for that phase (at `agent_result::agent_pid_path`) with `std::process::id()`, and persist a `State` with `monitor_pid = None` (Unknown liveness) plus `worktree_path` set to the created worktree via `workflow::save_state`. Invoke the `devflow` binary with `["cleanup", "--force"]`. Assert the command exits non-zero and its stderr/stdout names `devflow resume`. Confirm RED against unmodified `cleanup`.</action>
  <verify>
    <automated>cargo test -p devflow --test phase7_cli cleanup_force_refuses_on_live_agent_unknown_monitor 2>&1 | grep -q "1 failed" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --test phase7_cli cleanup_force_refuses_on_live_agent_unknown_monitor` reports `1 failed` (RED) against unmodified `cleanup`.
    - The test uses a genuinely-alive pid (`std::process::id()`) in the AGENT pid file with `monitor_pid = None`, so the liveness classification is `Unknown` yet the agent is provably alive — the fail-closed case the guard must catch.
    - Assertion is on exit code + the literal `devflow resume` recovery verb, not subjective language.
  </acceptance_criteria>
  <done>A committed failing test pinning the product race: `cleanup --force` deletes a LIVE agent's worktree today even when liveness is Unknown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Liveness-gated removal + bounded-backoff retry in cleanup</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs:292-335 (`cleanup`) — insert the liveness check before the `worktree::remove(project_root, &wt.path, force)` call at :308; do NOT overload `force` (its existing meaning at :304 is "also remove the reference worktree").
    - crates/devflow-cli/src/commands.rs:371-380 (`liveness()`) — reuse verbatim, but the REFUSE decision keys on BOTH agent liveness and monitor liveness (see below), not on Healthy/BetweenStages alone (review: Codex HIGH — Unknown/Stuck can sit over a live agent).
    - crates/devflow-cli/src/commands.rs:446-448 + :742-746 + :1374-1377 — the exact `agent_pid_from_file` + `agent_alive`/`monitor_alive` probe idiom `status`/`collect_phase_facts` already use; reuse it to compute `agent_alive` for each phase.
    - crates/devflow-cli/src/commands.rs:175,423 (`state.worktree_path`) — the persisted worktree path used to JOIN a `git worktree list` entry to its owning `State` (worktree→phase join, review: Codex MEDIUM).
    - crates/devflow-cli/src/commands.rs:1247-1261 (`check_dead_agent`) — the `PhaseFinding.repair = "devflow resume --phase {}"` error-shape to mirror for the refuse message.
    - crates/devflow-core/src/worktree.rs:105-118 (`remove`, `prune`) — `remove` is the retry target; `prune` is metadata-only and MUST NOT be the primary recovery (Pitfall 3).
    - crates/devflow-core/src/workflow.rs (`list_states`/`load_state`) — how to obtain each phase's persisted `State` (its `monitor_pid` + `worktree_path`).
    - PATTERNS.md § cleanup — hard-refuse (D-06, no override flag); bounded-backoff retry (~3 attempts, short sleeps) around `worktree::remove` only for a genuinely-dead phase.
  </read_first>
  <behavior>
    - Worktree→phase join: for each `git worktree list` entry under `.worktrees/`, find the owning `State` by `state.worktree_path == wt.path` first; fall back to branch/path matching only if no `worktree_path` match exists.
    - Refuse predicate (fail-closed on a live agent): compute `agent_alive` from `agent_pid_from_file` + `agent::agent_running`, and `monitor_alive` from `state.monitor_pid`. Refuse removal when `agent_alive` is true (live agent — ANY monitor state, including Unknown/monitor_pid=None and Stuck/dead-monitor) OR when `liveness()` is `Healthy`/`BetweenStages`. The message names `devflow resume`. Test 1 (Task 1, Unknown+live agent) and new Test 2 (Stuck+live agent) are now GREEN/refused.
    - Proceed only when the agent is dead AND the monitor is not active; then remove with bounded-backoff retry, and if retries exhaust, print a descriptive warning naming the worktree directory to clear manually.
  </behavior>
  <action>In `cleanup`, before each `worktree::remove` call: (1) JOIN the worktree entry to its owning phase `State` by `state.worktree_path == wt.path` (via `workflow::list_states`), branch/path fallback only if absent; (2) compute `agent_alive` (via `agent_pid_from_file` + `agent::agent_running`) and `monitor_alive` (via `state.monitor_pid`), and `liveness(state.monitor_pid, monitor_alive, agent_alive)`; (3) REFUSE with a `CliError::Message` naming `devflow resume --phase {phase}` (do NOT add a new override flag) whenever `agent_alive` is true OR liveness is `Healthy`/`BetweenStages` — this is the fail-closed-on-live-agent rule; the `Unknown` and `Stuck` classifications are handled explicitly by the `agent_alive` term rather than a blanket proceed; (4) otherwise (agent dead AND monitor not active) proceed, wrapping `worktree::remove` in a small bounded-backoff retry (a few attempts, short sleeps) so a transient `Directory not empty` race is absorbed; if the retry loop still fails, print a descriptive warning that the operator must manually clear the worktree directory (name the path) rather than failing silently. Do NOT call `git worktree prune` as the recovery path. Add `cleanup_force_refuses_on_dead_monitor_live_agent` (monitor_pid = a dead pid ⇒ Stuck, agent pid file = `std::process::id()` ⇒ live agent) asserting refusal. Make Task 1's RED test GREEN.</action>
  <verify>
    <automated>cargo test -p devflow --test phase7_cli cleanup 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --test phase7_cli cleanup` → `test result: ok`, including `cleanup_force_refuses_on_live_agent_unknown_monitor` (case a) and `cleanup_force_refuses_on_dead_monitor_live_agent` (case b).
    - Both refusal tests key on a LIVE agent pid — proving the guard is not satisfied by Healthy/BetweenStages alone (review: Codex HIGH fail-closed).
    - The diff shows the worktree→phase join uses `state.worktree_path == wt.path` first (review: Codex MEDIUM).
    - Diff shows `liveness()` reused (no second liveness predicate defined); `git worktree prune` is not introduced as a fallback.
    - The refuse path returns a non-zero `CliError` naming `devflow resume`; the retry-exhaustion path prints a warning naming the worktree directory.
    - `cargo clippy -p devflow --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`cleanup` refuses removal whenever a live agent is present (any monitor state) or the monitor is active, joins worktrees to phases by `worktree_path`, and retries dead-phase removal with bounded backoff + a manual-clear warning on exhaustion — the product race is closed.</done>
</task>

<task type="auto">
  <name>Task 3: Fixture durability + idempotency for both flaky tests</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    - crates/devflow-cli/tests/phase7_cli.rs:236 (`start_worktree_mode_ignores_main_checkout_divergence`) and its 60-commit loop at :246 — the loop exists only to cross the `behind > 50` hard-fail threshold; the window can shrink to just over 50.
    - crates/devflow-cli/tests/phase7_cli.rs:534 (`reference_and_cleanup_worktree_cli_flow`) — instance 1's fixture; now backed by the Task 2 product fix.
    - PATTERNS.md § phase7_cli.rs — apply `core.fsyncObjectFiles=true`/`core.fsync=all` via `git config` in the fixture's repo-init helper so a loose object write is visible to the following index read; shrink the loop window where it isn't needed for the `>50` threshold.
    - D-08 (CONTEXT.md) — instance 2 is fixed FIXTURE-SIDE ONLY; do NOT add a `devflow parallel` object-store fix (deferred backlog).
  </read_first>
  <behavior>
    - Idempotency (probe 20b/idempotency): `cleanup` run twice succeeds — the second run finds the worktree already gone and does not error.
    - Both previously-flaky fixtures pass deterministically under repeated local runs; the object-store fixture no longer depends on cross-runner fsync-visibility luck.
  </behavior>
  <action>Add a fixture git-durability helper that sets `core.fsyncObjectFiles=true` and `core.fsync=all` (via `git config` on the fixture repo) in the repo-init path used by both flaky tests, so an object write is durable before the next index read. Reduce the 60-commit loop at :246 to the smallest count that still crosses `behind > 50` (e.g. 51-52), narrowing the corruption window. Add `cleanup_is_idempotent_when_worktree_already_removed`: run `cleanup --force` on a Stuck/Unknown phase twice, assert the second invocation exits 0 (worktree already gone is not an error). Do NOT introduce any product change for the `devflow parallel` object-store concern — it is deferred to backlog per D-08.</action>
  <verify>
    <automated>cargo test -p devflow --test phase7_cli 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --test phase7_cli` → `test result: ok` (whole file green).
    - `start_worktree_mode_ignores_main_checkout_divergence` and `reference_and_cleanup_worktree_cli_flow` pass on 5 consecutive local runs (`for i in (seq 5); cargo test -p devflow --test phase7_cli ...; end` all green).
    - The 60-commit loop count is reduced but still `> 50`.
    - No `devflow parallel` object-store code change appears in the diff (D-08 scope boundary).
    - **CI-on-branch sign-off (review: Hermes LOW — surfaced, not buried):** local 5x green is NECESSARY but NOT SUFFICIENT for 20b. Sign-off REQUIRES the full workspace suite green on a pushed CI run (the flakes are CI-concurrency-dependent per the Phase 19 `ENV_MUTEX` precedent).
    - `cargo test --workspace` → 0 failed.
  </acceptance_criteria>
  <done>Both fixtures are durable and non-flaky; cleanup is idempotent; instance 2 stays fixture-side per D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → another operator's in-progress worktree | `cleanup --force` can force-remove a worktree an alive monitor is still writing into (shared/multi-phase checkout). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-02 | Tampering | `cleanup --force` worktree removal | high | mitigate | The liveness gate (Pattern 2) IS the mitigation — added as code, not merely documented: `cleanup` hard-refuses removal whenever a LIVE agent is present (any monitor state, including Unknown/Stuck) OR the monitor is active. Fail-closed-on-live-agent closes the residual hole a Healthy/BetweenStages-only guard left (review: Codex HIGH). Regression tests `cleanup_force_refuses_on_live_agent_unknown_monitor` (case a) + `cleanup_force_refuses_on_dead_monitor_live_agent` (case b). |
| T-20-02b | Denial of Service | transient `Directory not empty` race on dead-phase removal | medium | mitigate | Bounded-backoff retry around `worktree::remove` for Stuck/Unknown phases absorbs the transient race instead of surfacing an opaque error. |
| T-20-SC | Tampering | package installs | low | accept | Zero new external dependencies (RESEARCH § Package Legitimacy Audit). No install task in this plan. |

ASVS L1 non-applicable: V2/V3 (CLI, no auth/session), V5 (no new parsing surface — `cleanup` takes no free-form input), V6 (no crypto).
</threat_model>

<verification>
## Edge-probe reconciliation (no silent drop)
- 20b/idempotency → truth: `cleanup` run twice succeeds (Task 3 test).
- 20b/concurrency → truth: the race is the liveness guard's target — refusal happens before `git worktree remove` runs for a live phase (Task 1/Task 2). Full concurrent-load reproduction is CI-dependent (see below).

## 20b sign-off is CI-on-branch, not local-green
Per VALIDATION.md and the Phase 19 `ENV_MUTEX` precedent: 20b's flakiness is
CI-concurrency-dependent and does not reproduce reliably locally. Local green
(5x per-file loop) is necessary but NOT sufficient — sign-off requires the full
suite green on a **pushed CI run**.

## Deferred (do NOT plan here)
- `devflow parallel` concurrent-worktree object-store race (D-08 / RESEARCH
  Assumption A1) — file a new `999.N` backlog item, mirrored to Linear. Out of
  scope for Phase 20; instance 2 is fixed fixture-side only.

## Phase checks
- `cargo test -p devflow --test phase7_cli` → 0 failed, 5x locally.
- `cargo test --workspace` → 0 failed.
- Full suite green on a pushed CI run (20b sign-off gate).
</verification>

<success_criteria>
- `cleanup --force` refuses live phases and retries dead ones — the product race is closed, not papered over test-side.
- Both flaky fixtures are durable; `cleanup` is idempotent.
- No scope drift into a `devflow parallel` product fix (D-08).
</success_criteria>

<output>
Create `.planning/phases/20-release-correctness-operator-control/20-02-SUMMARY.md` when done. Record that the deferred `devflow parallel` object-store race stays out of scope (D-08) — its backlog item is DEN-51 (`999.26`, already filed per STATE.md); confirm it remains tracked, do NOT implement it here.
</output>
