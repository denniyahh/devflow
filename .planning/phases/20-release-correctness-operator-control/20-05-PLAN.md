---
phase: 20-release-correctness-operator-control
plan: 05
type: execute
wave: 3
depends_on: [20-01, 20-04]
files_modified:
  - crates/devflow-cli/src/main.rs
  - crates/devflow-cli/src/pipeline_gate.rs
  - crates/devflow-cli/tests/snapshots/devflow-help.txt
  - OPERATIONS.md
autonomous: true
requirements: [20e]

must_haves:
  truths:
    - "devflow ship --phase N reads the already-written Ship gate-response record and, on GateAction::Advance, calls the SAME pipeline_gate::finish_workflow the live poll loop would have called — not a reimplementation of the after-ship hook batch (D-01)."
    - "The command requires state.stage == Stage::Ship; any earlier stage returns an error directing the operator to resolve that stage first — --force NEVER skips Validate (D-02, ASVS V4)."
    - "--force still errors on ANY non-Ship stage, with or without --force — regression-tested to prevent the flag silently widening scope over time (security EoP)."
    - "When no Ship response file was ever written (dead monitor never wrote the gate) or the phase is not at Ship, the command refuses with a clear fail-closed error (edge-probe 20e/empty)."
    - "A failed Merge in the after-ship batch still stops the batch, preserves state, and refuses to emit workflow_finished — the Phase 16 fail-closed invariant is inherited unchanged because finish_workflow is reused verbatim."
    - "LoopBack/Abort responses are handled the same way run_gate's live callers already handle them — no special-casing of the second entry point."
  artifacts:
    - crates/devflow-cli/src/pipeline_gate.rs
    - crates/devflow-cli/src/main.rs
  key_links:
    - "main.rs Command::Ship{phase,force} → pipeline_gate::ship_override → workflow::load_state (stage==Ship gate) → Gates::response_path exists? → GateAction::from_response → Advance ⇒ finish_workflow(project_root, &mut state) (SAME terminal effect as run_gate)."
---

<objective>
20e — Manual ship override (`devflow ship --phase N [--force]`).

`devflow gate approve` assumes a healthy pipeline: `respond()` refuses when no
gate is open (`gates.rs:186` `NoOpenGate`), and approving only WRITES a response
file — a live monitor polling that path is what actually advances the workflow.
If the monitor is dead, the approval sits unconsumed forever. This is the
recovery gap when that assumption fails, which on this project's dogfood history
is not rare.

Research (Pattern 5) independently re-derived and CONFIRMED CONTEXT.md's
D-01/D-02 design correct against source: keep ONE on-disk gate-response record,
add a SECOND out-of-process consumer. `devflow ship --phase N` reads the
already-written Ship response directly and, on `GateAction::Advance`, calls the
same `pipeline_gate::finish_workflow` the live poll loop would have called
(`pipeline_gate.rs:130`) — `finish_workflow` already owns the fail-closed
retry-gate-reopen and terminal `workflow_finished` emission; nothing about it
needs reimplementing for a second caller.

**Sequenced last** and **depends on 20a (20-01):** a manual ship path runs
`VersionBump`, so it inherits 20a's self-pin fix for free — another reason 20a
ships first. Also depends on 20-04 (shares `main.rs`/CLI dispatch surface with
20d; sequenced to keep the conflict surface in one review).

Output: a `devflow ship` subcommand + a `ship_override` handler reusing
`finish_workflow`; D-02 scope enforcement + EoP regression test; regenerated
help snapshot + OPERATIONS.md.
</objective>

<assumption_delta_decision>
**Noun now primary:** the on-disk Ship gate-response record
(`NN-ship.response.json`).
**Transition:** singular → plural on "who consumes a gate adjudication." Before
20e, 18f's live in-process poll (`pipeline_gate::run_gate`) was the ONLY
consumer; `devflow ship` adds a second, out-of-process one.
**Decision:** `add-alongside` — the live poll stays exactly as-is; the new
command is a second, independent trigger of the SAME `GateResponse` record and
the SAME `finish_workflow` terminal effect. No promotion/identity-model change
is needed because there is exactly ONE record and ONE terminal effect, just two
triggers (D-01).
**Accepted-debt note:** none — both consumers route through the same terminal
function (`finish_workflow`); there is no divergent second code path to keep in
sync.
</assumption_delta_decision>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-release-correctness-operator-control/CONTEXT.md
@.planning/phases/20-release-correctness-operator-control/20-RESEARCH.md
@.planning/phases/20-release-correctness-operator-control/20-PATTERNS.md
@crates/devflow-core/src/gates.rs
@crates/devflow-cli/src/pipeline_gate.rs
</context>

<artifacts_produced>
New symbols this plan creates (source-grounding drift pass MUST exclude these):
- New `Command::Ship { phase: u32, force: bool, project: PathBuf }` variant +
  dispatch (`main.rs`).
- New `pipeline_gate::ship_override` handler (exact name at executor discretion).
- New `#[test]`s in `pipeline_gate.rs`'s test module:
  `ship_override_advances_via_written_response`,
  `ship_override_refuses_when_not_at_ship_stage`,
  `ship_override_refuses_when_no_response_written`.
</artifacts_produced>

<tasks>

<task type="tracer" tdd="true" reversibility="costly">
  <name>Task 1: End-to-end ship override — second consumer of the Ship response</name>
  <files>crates/devflow-cli/src/main.rs, crates/devflow-cli/src/pipeline_gate.rs</files>
  <read_first>
    - crates/devflow-cli/src/main.rs:238-259 (`GateCmd::Approve` shape) + :361 (`GateCmd::Approve => {...}` dispatch) — the subcommand + dispatch pattern to mirror; the new `Ship` is simpler (no legacy positional baggage).
    - crates/devflow-cli/src/pipeline_gate.rs:130-164 (`finish_workflow`) — D-01's exact reuse target; already owns the hook batch, fail-closed retry-gate-reopen, and terminal emission. Call it verbatim; reimplement none of it.
    - crates/devflow-core/src/gates.rs:57-92 (`GateAction`, `from_response`) + :127 (`response_path`) + :179-198 (`respond`) — read `response_path`, parse `GateResponse`, convert via `GateAction::from_response`.
    - crates/devflow-cli/src/pipeline_outcomes.rs:275-282 (`handle_ship_outcome`) — shows the live `Advance` branch calls the same `finish_workflow`; 20e reaches the same terminal state via a second entry point.
    - crates/devflow-cli/src/pipeline_gate.rs:309 (`advance_ship_success_runs_finish_workflow`) — the test fixture to mirror (temp repo, feature branch, `State` at `Stage::Ship`, saved) but drive via `Gates::respond` + the new command handler.
    - PATTERNS.md § pipeline_gate.rs (20e core) — the 5-step handler contract.
  </read_first>
  <behavior>
    - Given a phase whose `State` is at `Stage::Ship` with a Ship gate written and a `GateResponse` (Advance) written via `Gates::respond` — but NO live process polling — `devflow ship --phase N` reads that response and drives the phase through `finish_workflow`, emitting `workflow_finished` and clearing state, reaching the SAME terminal state the live monitor would have.
    - The handler calls `finish_workflow` (not a reimplemented batch); the diff shows no duplicated hook-batch logic.
  </behavior>
  <action>Add `Command::Ship { #[arg(long)] phase: u32, #[arg(long)] force: bool, #[arg(default_value=".")] project: PathBuf }` to `main.rs`, dispatched to a new `pipeline_gate::ship_override(project_root, phase, force)`. Implement `ship_override` per the 5-step contract: (1) `workflow::load_state`; (2) require `state.stage == Stage::Ship` else error (see Task 2); (3) check `Gates::response_path(.., Stage::Ship).exists()` else fail-closed error; (4) parse `GateResponse`, `GateAction::from_response`; (5) on `Advance` call `finish_workflow(project_root, &mut state)` verbatim; on `LoopBack`/`Abort` route to the same `loop_back_to_code`/`abort` the live callers use. Add `ship_override_advances_via_written_response` to `pipeline_gate.rs`'s test module, mirroring `advance_ship_success_runs_finish_workflow` but writing the response via `Gates::respond` and driving through `ship_override`; assert `workflow_finished` emitted and state cleared. **Reversibility: costly** — this couples a second command to `finish_workflow`, a path Phase 16/17 hardened; inverting later means untangling that coupling, not a one-line revert. Flagged, not gated.</action>
  <verify>
    <automated>cargo test -p devflow ship_override_advances_via_written_response 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow ship_override_advances_via_written_response` → `test result: ok`.
    - The diff shows `ship_override` calling `finish_workflow` directly — no re-implemented Merge/VersionBump/ChangelogAppend/BranchCleanup batch (D-01).
    - The test drives the response through `Gates::respond` (the real on-disk record), not by calling `finish_workflow` directly.
    - `cargo clippy -p devflow --all-targets -- -D warnings` clean; `cargo fmt --check` clean.
  </acceptance_criteria>
  <done>`devflow ship --phase N` advances a Ship-staged phase through the same terminal effect as the live monitor, from a dead-monitor state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-02 scope enforcement — --force never skips Validate (EoP guard)</name>
  <files>crates/devflow-cli/src/pipeline_gate.rs</files>
  <read_first>
    - RESEARCH § Pattern 5 (D-02) + § Anti-Patterns — the command must check `state.stage == Stage::Ship` BEFORE doing anything; `--force` only means "skip re-verifying the Ship gate's own preconditions," NEVER "advance past Validate."
    - RESEARCH § Security Domain — a `--force` flag silently widening scope over time (EoP) is exactly the class Phase 16/17 hardened for the terminal Ship invariant; the mitigation is a regression test.
    - crates/devflow-core/src/stage.rs — `Stage` variants to iterate for the non-Ship-stage assertion.
    - crates/devflow-cli/src/pipeline_gate.rs test module — existing fail-closed assertion style to mirror.
  </read_first>
  <behavior>
    - `ship_override` on a `State` whose stage is any non-Ship value (Define/Plan/Code/Validate) returns an error naming the stage to resolve first — with `--force` true AND false. It never calls `finish_workflow`.
    - When `state.stage == Stage::Ship` but no response file exists, it fail-closes with a clear "no Ship gate response written" error (dead monitor never wrote the gate).
  </behavior>
  <action>Enforce D-02 in `ship_override`: return a `CliError::Message` if `state.stage != Stage::Ship`, naming the current stage and directing the operator to resolve it first; `--force` does NOT bypass this. Add `ship_override_refuses_when_not_at_ship_stage` asserting the error (and that `finish_workflow` is not reached) for every non-Ship stage, with `--force` both true and false. Add `ship_override_refuses_when_no_response_written` asserting the fail-closed error when `state.stage == Ship` but `response_path` is absent.</action>
  <verify>
    <automated>cargo test -p devflow ship_override_refuses 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow ship_override_refuses` → `test result: ok` (both refusal tests).
    - `ship_override_refuses_when_not_at_ship_stage` asserts refusal for every non-Ship `Stage`, with `--force` true and false — the EoP scope-creep regression guard.
    - The no-response case fail-closes with a clear error (edge-probe 20e/empty).
    - No path lets `--force` reach `finish_workflow` from a non-Ship stage.
  </acceptance_criteria>
  <done>`--force` is provably scoped to `state.stage == Stage::Ship`; it can never skip Validate — the terminal Ship invariant is preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate help snapshot + OPERATIONS.md; LoopBack/Abort symmetry</name>
  <files>crates/devflow-cli/tests/snapshots/devflow-help.txt, OPERATIONS.md, crates/devflow-cli/src/pipeline_gate.rs</files>
  <read_first>
    - crates/devflow-cli/tests/help_snapshot.rs + tests/snapshots/devflow-help.txt — regenerate via `cargo run -q -p devflow -- --help > crates/devflow-cli/tests/snapshots/devflow-help.txt` (Pitfall 5); the new `Ship` subcommand changes `--help`.
    - OPERATIONS.md command table + gate/recovery section — add `devflow ship --phase N [--force]` with its "dead monitor recovery" purpose.
    - crates/devflow-cli/src/pipeline_gate.rs — the existing `loop_back_to_code(project_root, state, FixType::AuditFix)` / `abort(project_root, state, &reason)` callers to route LoopBack/Abort through (do not special-case Ship's LoopBack/Abort differently from the live path).
  </read_first>
  <behavior>
    - `devflow --help` matches the regenerated committed snapshot (new `Ship` subcommand present).
    - A `LoopBack`/`Abort` Ship response drives `ship_override` down the same `loop_back_to_code`/`abort` path the live monitor uses — verified at least for the Abort case.
  </behavior>
  <action>Regenerate `crates/devflow-cli/tests/snapshots/devflow-help.txt` from the built binary and add the `devflow ship` row to OPERATIONS.md's command/recovery table. Ensure `ship_override`'s `LoopBack`/`Abort` arms call the same `loop_back_to_code`/`abort` helpers the live callers use; add a focused test (e.g. `ship_override_abort_routes_through_abort`) proving the Abort response reaches the shared abort path rather than a special-cased branch.</action>
  <verify>
    <automated>cargo test -p devflow --test help_snapshot 2>&1 | grep -qE "test result: ok" && cargo test -p devflow ship_override 2>&1 | grep -qE "test result: ok" && echo GREEN</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --test help_snapshot` → `test result: ok` (snapshot regenerated + committed, `Ship` present).
    - OPERATIONS.md lists `devflow ship --phase N [--force]` with its recovery purpose.
    - The LoopBack/Abort arms reuse the existing shared helpers (grep the diff: `loop_back_to_code`/`abort` calls, no duplicated logic).
    - `cargo test --workspace` → 0 failed; clippy + fmt clean.
  </acceptance_criteria>
  <done>The new subcommand is documented and snapshot-tested; LoopBack/Abort reuse the live path — no second divergent code path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI → terminal Ship transition | `devflow ship --force` can drive a phase through the terminal, irreversible after-ship hook batch (Merge/VersionBump/tag/publish-adjacent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-05 | Elevation of Privilege | `--force` scope | high | mitigate | `--force` is scoped to `state.stage == Stage::Ship` ONLY (D-02); regression test `ship_override_refuses_when_not_at_ship_stage` asserts it still errors on every non-Ship stage with `--force` true/false — blocks the flag silently widening past Validate. |
| T-20-05b | Tampering | after-ship batch integrity | high | mitigate | `finish_workflow` is reused verbatim, inheriting the Phase 16 fail-closed contract (failed Merge stops the batch, preserves state, refuses `workflow_finished`) — no reimplementation to drift. |
| T-20-05c | Repudiation | terminal effect not recorded | low | accept | `finish_workflow` already emits `workflow_finished`; the second trigger reuses it, so the audit event is unchanged. |
| T-20-SC | Tampering | package installs | low | accept | Zero new external dependencies (RESEARCH § Package Legitimacy Audit). No install task. |

ASVS L1 non-applicable: V2/V3 (CLI, no auth/session), V5 (`--phase`/`--force` are typed flags, no free-form parsing surface), V6 (no crypto implemented — signing is 20d's read-only concern).
</threat_model>

<verification>
## Edge-probe reconciliation (no silent drop)
- 20e/empty → truth: no Ship response written, or phase not at Ship ⇒ fail-closed clear error (Task 2).
- 20e/adjacency → truth mapping: `state.stage` must be EXACTLY `Ship`; the adjacent earlier stage (Validate) is rejected, never "just touched" into advancing (Task 2, D-02).
- 20e/ordering → **flagged assumption, N/A**: `ship_override` triggers a single terminal effect, not an ordered collection output; the generic ordering template does not target a recovery command.

## Phase checks
- `cargo test -p devflow ship_override` → 0 failed.
- `cargo test -p devflow --test help_snapshot` → 0 failed.
- `cargo test --workspace` → 0 failed; clippy + fmt clean.
</verification>

<success_criteria>
- `devflow ship --phase N` recovers a Ship-staged phase from a dead-monitor state via the SAME `finish_workflow` effect (D-01), no reimplementation.
- `--force` provably cannot skip Validate (D-02, EoP-regression-tested).
- Fail-closed on missing response / wrong stage; LoopBack/Abort reuse the live path.
</success_criteria>

<output>
Create `.planning/phases/20-release-correctness-operator-control/20-05-SUMMARY.md` when done.
</output>
