---
phase: 12-bootstrap-housekeeping
plan: 10
type: execute
wave: 2
depends_on: [12-02]
files_modified:
  - crates/devflow-core/src/ship.rs
autonomous: true
requirements: [WR-05, WR-08, 12f-rfc3339-negative-offset]
must_haves:
  truths:
    - "parse_rfc3339ish's second-restoration is documented as timezone-safe"
    - "shell_quote's safe-unquoted character set no longer needlessly single-quotes common safe path characters (false-negative reduced), and unsafe input is still quoted"
    - "A test proves parse_rfc3339ish handles negative UTC offsets and normalizes to UTC correctly"
  artifacts:
    - path: "crates/devflow-core/src/ship.rs"
      provides: "documented parse_rfc3339ish; widened shell_quote safe set; a negative-offset cron test"
  key_links:
    - from: "cron_schedule_from_retry_after(\"…-05:00\")"
      to: "UTC-normalized cron fields"
      via: "parse_rfc3339ish applies the offset before rounding to a cron minute"
      pattern: "cron_schedule_from_retry_after"
---

<objective>
Finish the low-priority ship.rs review items plus the RFC3339 negative-offset coverage gap:

- WR-05 (`ship.rs:380-396`): the `parse_rfc3339ish` second-restoration is confusing but reviewer-confirmed correct. Documentation-only clarification of WHY restoring `second` after UTC-minute normalization is timezone-safe.
- WR-08 (`ship.rs:451-460`): `shell_quote`'s "safe unquoted" set misses `~ : @ + = %` etc. False-negative only (it over-quotes), low priority. Widen the safe set (or document the conservative choice), keeping all genuinely-unsafe input single-quoted.
- 12f: `parse_rfc3339ish` is only tested for `Z`/UTC; add coverage for negative UTC offsets.

Depends on 12-02 (which also edits ship.rs, in the cron functions) to avoid a same-file wave collision.

Purpose: Make the timestamp/quoting helpers legible and prove negative-offset handling.
Output: a WR-05 doc comment, a widened WR-08 safe set + test, and a negative-offset cron test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-bootstrap-housekeeping/CONTEXT.md
@.planning/phases/12-bootstrap-housekeeping/12-PATTERNS.md
@crates/devflow-core/src/ship.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: WR-05 — document why parse_rfc3339ish second-restoration is timezone-safe</name>
  <files>crates/devflow-core/src/ship.rs</files>
  <read_first>
    - crates/devflow-core/src/ship.rs (parse_rfc3339ish at 359-396, especially the utc_minutes normalization + `normalized.second = second` at 392-394; split_time_and_offset at 398-417; RetryTimestamp::to_epoch_minutes/from_epoch_minutes at 329-346)
  </read_first>
  <action>
    Add a clarifying comment in `parse_rfc3339ish` explaining the second-restoration: the UTC-offset normalization operates on whole minutes (`to_epoch_minutes`/`from_epoch_minutes`, which zero the seconds), and since a timezone offset never carries a sub-minute (seconds) component in practice here, the original `second` value is timezone-invariant and is restored verbatim after minute-level normalization. Comment only — do not change the arithmetic or any behavior.
  </action>
  <acceptance_criteria>
    - `parse_rfc3339ish` carries a comment explaining the timezone-safe second-restoration.
    - No behavior change; `cargo test -p devflow-core ship::` green; `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: WR-08 — widen shell_quote's safe-unquoted character set</name>
  <files>crates/devflow-core/src/ship.rs</files>
  <read_first>
    - crates/devflow-core/src/ship.rs (shell_quote at 451-460 — current safe set is ascii-alphanumeric plus `/ . _ -`)
  </read_first>
  <action>
    Reduce the false-negative over-quoting in `shell_quote` by adding shell-safe characters that never need quoting to the unquoted allowlist — include `~ : @ + = %` (and any others that are unambiguously safe unquoted in a POSIX shell word). Keep the fallback: anything not in the safe set is still wrapped in single quotes with embedded single quotes escaped. Do not remove any character currently treated as safe. Add a test `shell_quote_leaves_common_safe_chars_unquoted` asserting a value like `user@host:1.2.3+build` stays unquoted, and keep/verify a case proving a value with spaces or `'` is single-quoted.
  </action>
  <acceptance_criteria>
    - New test passes: a value containing only the widened safe set is returned unquoted; a value with a space or single quote is single-quoted.
    - `cargo test -p devflow-core ship::` green; `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 12f — parse_rfc3339ish negative UTC offset coverage</name>
  <files>crates/devflow-core/src/ship.rs</files>
  <read_first>
    - crates/devflow-core/src/ship.rs (parse_rfc3339ish at 359-396 and split_time_and_offset at 398-425 — negative offset handling exists but is untested; cron_schedule_from_retry_after at 202-208; existing cron_schedule_rounds_up_to_nearest_minute test at 579-589 for the assertion style)
  </read_first>
  <action>
    Because `parse_rfc3339ish` is private, drive the negative-offset case through the public `cron_schedule_from_retry_after`. Add a test asserting a negative-offset timestamp is normalized to UTC before the cron minute is computed: for `"2026-06-18T15:45:30-05:00"` (local 15:45:30 at UTC-5 → 20:45:30 UTC → round up to 20:46), assert the resulting cron is `"46 20 18 6 *"`. Add at least one more negative-offset case with a non-zero offset-minutes component if convenient (e.g. `-05:30`). Mirror the existing `Z`-suffix test's assertion style. (If 12-02 changed `cron_schedule_from_retry_after` to return `Option<String>`, unwrap/assert accordingly.)
  </action>
  <acceptance_criteria>
    - New test passes: a negative-offset timestamp yields the correct UTC-normalized cron schedule.
    - `cargo test -p devflow-core ship::` green; `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| retry timestamp string → cron schedule | Timezone parsing feeds a scheduled command; a mis-normalized offset would schedule at the wrong time. |
| project path → shell command string (shell_quote) | Paths embedded in a generated shell command must remain correctly quoted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-10-T | Tampering (shell word splitting) | `ship::shell_quote` | low | mitigate | Widening only adds provably-safe unquoted characters; all unsafe input still falls through to single-quote wrapping (a test asserts the quoted case), so the change cannot introduce an injection. |
| T-12-10-I | Information Disclosure (wrong-time schedule) | `parse_rfc3339ish` offset handling | low | mitigate | Negative-offset test proves correct UTC normalization, preventing a resume cron scheduled at the wrong absolute time. |
</threat_model>

## Artifacts this phase produces

New symbols introduced by this plan (exclude from source-drift verification):
- New test fns in `crates/devflow-core/src/ship.rs`: `shell_quote_leaves_common_safe_chars_unquoted` and a negative-offset cron test (name at executor's discretion, e.g. `cron_schedule_normalizes_negative_offset`).
- Widened safe-character set inside `shell_quote` (no new public symbol).

<verification>
- `cargo test -p devflow-core ship::` green (existing + new tests).
- `cargo clippy -p devflow-core -- -D warnings` and `cargo fmt --check` clean.
</verification>

<success_criteria>
- WR-05: second-restoration documented as timezone-safe (no behavior change).
- WR-08: safe set widened; unsafe input still quoted (tested).
- 12f: negative-offset normalization proven by a cron test.
</success_criteria>

<output>
Create `.planning/phases/12-bootstrap-housekeeping/12-10-SUMMARY.md` when done.
</output>
