---
phase: 34-stream-json-coverage-and-the-validate-trust-boundary-999-73-
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-core/src/config.rs
  - crates/devflow-core/src/agent_result.rs
autonomous: true
requirements: [DOGFOOD-03]
user_setup: []

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`canary_gate_only_applies_to_the_stream_launch_path` proves its discrimination on the legacy opt-out, which `claude_stream_launch_enabled` respects regardless of `STREAM_JSON_STAGES`'s contents — so the test survives full five-stage widening instead of becoming unconstructible (ROADMAP criterion 7)"
    - "A phase run producing exactly `capture_retention` archive events retains every one of them; the next archive event evicts only the oldest, and a capture already copied out of `.devflow/` into the phase evidence directory survives that eviction because it no longer lives under a pruned path"
    - { statement: "`STREAM_JSON_STAGES` membership is tested with `slice::contains`, so no launch behaviour depends on the constant's element order; the doc comment lists stages in `Stage`-enum declaration order for readability only", verification: backstop }
    - "`DEFAULT_CAPTURE_RETENTION` is large enough that a five-stage phase taking up to three Validate→Code loop-backs cannot evict an unread stage capture, and the value's arithmetic is recorded in its own doc comment — an inspectable source artifact, not a run-local env var (ROADMAP criterion 7)"
    - "`STREAM_JSON_STAGES`'s doc comment records in source that `ClaudeAgent::exec_command` ignores its `_phase`, `_prompt` and `_extra_writable_roots` arguments, so the launch argv is byte-identical across all five stages and a per-stage capture is evidence about agent behaviour, never about the transport (ROADMAP criterion 1)"
    - "The phase evidence directory layout exists on disk before any capture run, so the copy-at-landing step has a landing spot rather than creating one under time pressure"
  prohibitions:
    - "A check that cannot produce the opposite result must not be reported as a demonstration — the rebuilt canary test must keep an assertion that the launch predicate still returns true somewhere, or its false-branch reading is a constant rather than a discrimination"
  artifacts:
    - path: "crates/devflow-cli/src/pipeline_launch.rs"
      provides: "canary_gate_only_applies_to_the_stream_launch_path rebuilt on the legacy opt-out discriminator"
      contains: "canary_gate_only_applies_to_the_stream_launch_path"
    - path: "crates/devflow-cli/src/pipeline_launch.rs"
      provides: "STREAM_JSON_STAGES doc comment carrying criterion 1's stage-blind-argv record"
      contains: "exec_command"
    - path: "crates/devflow-core/src/config.rs"
      provides: "DEFAULT_CAPTURE_RETENTION raised with its arithmetic recorded in the doc comment"
      contains: "DEFAULT_CAPTURE_RETENTION"
    - path: "crates/devflow-core/src/agent_result.rs"
      provides: "prune_history_retains_a_full_five_stage_run_with_loop_backs — the retention regression test"
      contains: "prune_history_retains_a_full_five_stage_run_with_loop_backs"
    - path: ".planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/README.md"
      provides: "the per-stage evidence directory layout, stubbed before the capture run"
      contains: "raw_output.jsonl"
  key_links:
    - from: "crates/devflow-cli/src/pipeline_launch.rs"
      to: "crates/devflow-cli/src/pipeline_launch.rs"
      via: "the rebuilt canary test drives the real claude_stream_launch_enabled predicate rather than a hardcoded boolean, so it tracks the rollout instead of a copy of it"
      pattern: "claude_stream_launch_enabled"
    - from: "crates/devflow-core/src/agent_result.rs"
      to: "crates/devflow-core/src/config.rs"
      via: "archive_phase_files_with_stamp calls prune_history with the value capture_retention resolves from DEFAULT_CAPTURE_RETENTION"
      pattern: "capture_retention"
---

<objective>
Land criterion 7's collateral and the capture campaign's infrastructure **before** anything widens
`STREAM_JSON_STAGES`, so the widening in plan 34-05 lands against a test suite that can survive it
and an evidence directory that already exists.

Purpose: three separate hazards, all mechanical and all recorded in `34-REVIEW.md` § "Traps recorded
for the planner". (a) `canary_gate_only_applies_to_the_stream_launch_path` asserts `Stage::Plan`
resolves to the legacy path — with all five stages widened, no Claude stage yields `false` and the
test is unconstructible as written. (b) `DEFAULT_CAPTURE_RETENTION = 5` evicts an earlier stage's
capture on any Validate→Code loop-back; a six-transition run loses Define's. (c) `.devflow/.gitignore`
is literally `*`, so a capture is invisible to `git add` until it is copied out — and there is
nowhere to copy it to yet.

Output: a canary test rebuilt on a discriminator that survives widening, a retention constant that
cannot evict an unread capture with its arithmetic recorded in source, the criterion-1 stage-blindness
record in `STREAM_JSON_STAGES`'s own doc comment, and the Phase-30-shaped evidence directory layout
stubbed on disk.

**Why this is wave 2 and not wave 1**, despite having no content dependency on plan 34-01: task 2
adds a test to `crates/devflow-core/src/agent_result.rs`, which 34-01 also modifies. Same-wave plans
must have zero `files_modified` overlap, so the shared file is itself the dependency edge and
`depends_on: ["34-01"]` records it. Nothing in this plan reads 34-01's behavioural change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-CONTEXT.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-RESEARCH.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-PATTERNS.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-REVIEW.md
</context>

<coverage_fallback>
This plan owns two of the four spec-less probe fallback edge rows (full accounting in plan 34-01):

- **Row 1 — DOGFOOD-03 / adjacency**, resolved **covered**. The generic "exactly equal or just
  touch" probe maps onto the capture-retention boundary: the archive event whose index exactly
  equals `capture_retention` retains everything; the next one evicts the oldest. Authored as a plain
  `must_haves.truths` string and pinned by the retention regression test in task 2.
- **Row 3 — DOGFOOD-03 / ordering**, resolved **backstop**. `STREAM_JSON_STAGES` is consulted with
  `slice::contains`, so element order is semantically inert; there is no output ordering to specify.
  Authored as a structured `{ statement, verification: backstop }` marker, so a verifier that cannot
  confirm it with explicit evidence abstains to `human_needed` rather than passing silently.
</coverage_fallback>

<artifacts_this_phase_produces>
Symbols this plan creates (new; exclude from drift verification):

| Kind | Name | File |
|---|---|---|
| test fn | `prune_history_retains_a_full_five_stage_run_with_loop_backs` | `crates/devflow-core/src/agent_result.rs` |
| test fn | `canary_gate_still_fires_for_a_widened_stage_without_the_opt_out` | `crates/devflow-cli/src/pipeline_launch.rs` |
| new path | `.planning/phases/34-…/34-evidence/README.md` | evidence directory index |
| new path | `.planning/phases/34-…/34-evidence/{define,plan,code,validate,ship}/` | per-stage capture landing spots |

Modified (pre-existing): `canary_gate_only_applies_to_the_stream_launch_path`,
`DEFAULT_CAPTURE_RETENTION`, `STREAM_JSON_STAGES`'s doc comment.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebuild the canary test on a discriminator that survives full widening</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_launch.rs:439-480` — `STREAM_JSON_STAGES` and `claude_stream_launch_enabled`, whose `!legacy_opt_out && agent == AgentKind::Claude && STREAM_JSON_STAGES.contains(&stage)` shape is what makes the opt-out a surviving discriminator.
    - `crates/devflow-cli/src/pipeline_launch.rs:1751-1805` — `canary_gate_only_applies_to_the_stream_launch_path` in full, including the stage-membership premise assigned at `:1763`, the first assertion at `:1771-1774`, and the negative control at `:1777-1780`.
    - `crates/devflow-cli/src/pipeline_launch.rs:2309-2334` — `legacy_launch_flag_forces_the_single_document_path`. RESEARCH.md confirmed this one needs **no** change; read it to see the `legacy_state` helper and `state.legacy_claude_launch` field the rebuild reuses.
    - `crates/devflow-cli/src/pipeline_launch.rs:333-354` — `canary_gate`, so the rebuilt test's `stream_launch` argument and `state.canary` assertions still line up with what the gate actually does.
    - `.planning/phases/34-.../34-RESEARCH.md` § "Criterion 7 Collateral — the Canary Test and the Legacy-Flag Test", which evaluates both candidate discriminators and recommends the opt-out over a non-Claude agent, with the reason.
  </read_first>
  <behavior>
    - With `legacy_claude_launch = true` and `state.stage = Stage::Code` (a stage that is on the stream path today and stays on it), `claude_stream_launch_enabled` returns `false`, `canary_gate` is not invoked, and `state.canary` stays `None`.
    - Negative control, in the same test: with `legacy_claude_launch = false` and the same stage, the predicate returns `true`. If both readings agreed the first would be a constant, not a discrimination.
    - Both readings hold regardless of how many stages `STREAM_JSON_STAGES` names — including all five — because the opt-out is a separate `&&` term the predicate always respects.
  </behavior>
  <action>
Rewrite `canary_gate_only_applies_to_the_stream_launch_path` at
`crates/devflow-cli/src/pipeline_launch.rs:1754` so its false-branch premise comes from the legacy
opt-out rather than from `Stage::Plan`'s absence from `STREAM_JSON_STAGES`. Concretely: keep
`canary_state(root, phase)`, the `ENV_MUTEX` guard, `init_repo`, the counting-canary harness, the
`canary_gate(...).unwrap()` call and both tail assertions (`calls.get() == 0` and
`state.canary == None`) exactly as they are. Retarget the state's stage from Plan to `Stage::Code`,
set `state.legacy_claude_launch = true`, and keep driving `stream_launch` from the real
`claude_stream_launch_enabled(state.agent, state.stage, state.legacy_claude_launch)` call rather
than a hardcoded `false` — the existing comment explaining why that matters stays.

Replace the existing negative control at `:1777-1780` with one that is not `STREAM_JSON_STAGES`-
membership-dependent: assert `claude_stream_launch_enabled(AgentKind::Claude, state.stage, false)`
is `true` — same agent, same stage, opt-out cleared. That is the paired opposite-result case and it
is what proves the first reading is a discrimination. Update the assertion messages so they name the
opt-out as the variable under test, and update the test's doc comment to state that the discriminator
was moved off stage membership deliberately (ROADMAP criterion 7) because full widening makes no
Claude stage yield `false`.

Add a companion test `canary_gate_still_fires_for_a_widened_stage_without_the_opt_out` that runs the
same fixture with `legacy_claude_launch = false` and asserts `calls.get() == 1` and that
`state.canary` is recorded — so the pair covers both directions of the gate, not only the refusal.
Use `canary::CanaryOutcome::Present` (or whichever variant the counting-canary harness accepts
without erroring — read `counting_canary`'s signature and `canary_gate`'s error path before
choosing; the existing test deliberately uses `Absent` because an invoked canary would make its
`unwrap` fail).

Do NOT touch `legacy_launch_flag_forces_the_single_document_path` — RESEARCH.md verified it uses
`Stage::Code` throughout and remains valid after widening.
  </action>
  <verify>
    <automated>cargo test -p devflow --lib pipeline_launch::tests::canary_gate_only_applies_to_the_stream_launch_path -- --exact 2>&1 | tee /tmp/34-02-t1.txt; cargo test -p devflow --lib pipeline_launch::tests::canary_gate_still_fires_for_a_widened_stage_without_the_opt_out -- --exact 2>&1 | tee -a /tmp/34-02-t1.txt; cargo test -p devflow --lib pipeline_launch::tests::legacy_launch_flag_forces_the_single_document_path -- --exact 2>&1 | tee -a /tmp/34-02-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/34-02-t1.txt` contains three separate `1 passed` lines, each with a non-zero `filtered out` count. The package is `devflow`, not `devflow-cli` — a wrong `-p` value plus a name that matches nothing exits 0 and proves nothing.
    - `rg -n 'fn canary_gate_only_applies_to_the_stream_launch_path' -A 40 crates/devflow-cli/src/pipeline_launch.rs` shows `legacy_claude_launch` set to `true` on the state under test and shows a second `claude_stream_launch_enabled` call with the opt-out argument `false` asserted `true`.
    - `rg -n 'fn canary_gate_only_applies_to_the_stream_launch_path' -A 40 crates/devflow-cli/src/pipeline_launch.rs | rg -c 'Stage::Plan'` returns 0 — the stage-membership premise is gone.
    - Temporarily editing `STREAM_JSON_STAGES` to `&[Stage::Code, Stage::Plan, Stage::Define, Stage::Validate, Stage::Ship]` in the working tree and re-running the first two selectors still yields `1 passed` each. Revert the temporary edit afterwards and state in the SUMMARY that it was made and reverted — this is the control proving the rebuild actually survives widening rather than merely looking like it should.
    - `cargo clippy -p devflow --all-targets -- -D warnings` exits 0.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only change in one file; the production predicate is untouched.</reversibility>
  <done>Both canary tests and the legacy-flag test pass, and the pair still passes under a temporarily fully-widened constant.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise capture retention so an unread stage capture cannot be evicted</name>
  <files>crates/devflow-core/src/config.rs, crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    - `crates/devflow-core/src/config.rs:11-12` — `DEFAULT_CAPTURE_RETENTION`'s current value and doc comment.
    - `crates/devflow-core/src/config.rs:136-149` — the `capture_retention(project_root)` accessor and its `DEVFLOW_CAPTURE_RETENTION` env override, plus the `devflow.toml` fallback.
    - `crates/devflow-core/src/agent_result.rs:2402-2424` — `history_dir` and `archive_stamp`.
    - `crates/devflow-core/src/agent_result.rs:2435-2547` — `archive_phase_files` and `archive_phase_files_with_stamp`, including the `prune_history(&history_dir, retain)` call at the tail.
    - `crates/devflow-core/src/agent_result.rs:2615` — `prune_history`'s body, so the new test asserts against what it actually does rather than what it is assumed to do.
    - `.planning/phases/34-.../34-RESEARCH.md` § "`DEFAULT_CAPTURE_RETENTION = 5` — the eviction mechanism, verified concretely" — the six-archive-event arithmetic for a phase with one loop-back.
  </read_first>
  <behavior>
    - With `retain` set to the new default, a history directory holding 12 stamps keeps all 12 after `prune_history`.
    - With 13 stamps, exactly one is removed and it is the oldest by stamp ordering — the opposite-result case that proves the retention test is measuring pruning rather than a no-op.
    - `capture_retention(project_root)` still returns the `DEVFLOW_CAPTURE_RETENTION` env value when set and the `devflow.toml` value when present — the constant change must not disturb the precedence chain.
  </behavior>
  <action>
Change `DEFAULT_CAPTURE_RETENTION` in `crates/devflow-core/src/config.rs:12` from `5` to `12`, and
replace its one-line doc comment with the arithmetic that justifies the number: `archive_phase_files`
runs once per launch and archives the previous stage's files, so a clean five-stage
Define→Plan→Code→Validate→Ship run produces 4 archive events; each Validate→Code loop-back adds 2;
12 therefore survives four loop-backs with headroom, where 5 lost Define's capture on the first one.
Cite ROADMAP criterion 7 and state explicitly that this is the "changing the constant" branch of the
criterion's two sanctioned mitigations, chosen over a run-local `DEVFLOW_CAPTURE_RETENTION` export
because the criterion requires the mitigation leave an inspectable artifact — a committed source
constant is greppable and survives the run; an exported env var does not.

Do not change `capture_retention`'s precedence order and do not remove the env override — it stays
available, it is simply no longer the phase's mitigation.

Add `prune_history_retains_a_full_five_stage_run_with_loop_backs` to `agent_result.rs`'s test module,
following the file's small-descriptively-named-test convention: build a tempdir, create
`history_dir(root, phase)`, write 12 files whose names follow `archive_stamp`'s `{nanos}-{seq}`
shape with strictly increasing stamps, call `prune_history` with `DEFAULT_CAPTURE_RETENTION`, and
assert all 12 survive. In the same test, add a 13th stamp, re-run `prune_history`, and assert
exactly 12 remain and that the removed entry is the lowest-stamped one. Name the second half in an
inline assertion message as the opposite-result control: if both halves retained everything, the
test would be measuring a directory listing, not pruning. Its doc comment cites ROADMAP criterion 7
and states what a regression costs (a stage capture evicted before the phase reads it).
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib agent_result::tests::prune_history_retains_a_full_five_stage_run_with_loop_backs -- --exact 2>&1 | tee /tmp/34-02-t2.txt; cargo test -p devflow-core --lib config:: 2>&1 | tee -a /tmp/34-02-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/34-02-t2.txt` contains `1 passed` with a non-zero `filtered out` count for the new test.
    - `/tmp/34-02-t2.txt` shows the `config::` module selector reporting `0 failed` with a non-zero `filtered out` count — the precedence chain is undisturbed.
    - `rg -n 'DEFAULT_CAPTURE_RETENTION: usize = 12' crates/devflow-core/src/config.rs` returns exactly one match.
    - `rg -B 12 -n 'DEFAULT_CAPTURE_RETENTION: usize' crates/devflow-core/src/config.rs` shows a doc comment containing the transition arithmetic and the words `criterion 7`.
    - `rg -n 'fn prune_history_retains_a_full_five_stage_run_with_loop_backs' -A 40 crates/devflow-core/src/agent_result.rs` shows both the 12-stamp retain assertion and the 13-stamp evict-one assertion.
    - The SUMMARY reports both halves' results together; reporting only the retain half does not satisfy this criterion.
  </acceptance_criteria>
  <reversibility rating="reversible">One constant plus a test; the env and TOML overrides are unchanged, so any operator who needs the old value can set it.</reversibility>
  <done>The default retention is 12 with its arithmetic recorded in source, and a named test proves both that 12 survive and that the 13th evicts exactly one — the oldest.</done>
</task>

<task type="auto">
  <name>Task 3: Record the stage-blind argv in source and stub the evidence directory layout</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs, .planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/README.md</files>
  <read_first>
    - `crates/devflow-core/src/agents/claude.rs:46-61` — `ClaudeAgent::exec_command`, whose `_phase`, `_prompt` and `_extra_writable_roots` parameters are all unused and which returns a fixed argv. Read it and confirm all three are still ignored at HEAD before writing the claim into a doc comment; if any is now used, the recorded fact must match source, not this plan.
    - `crates/devflow-cli/src/pipeline_launch.rs:439-446` — `STREAM_JSON_STAGES`'s existing doc comment, which is the established shape for recording why a stage is on the list.
    - `crates/devflow-cli/src/pipeline_launch.rs:158-176` — `resolve_launch_shape`, so the recorded note names the right function as the thing membership actually selects.
    - `.planning/phases/34-.../34-PATTERNS.md` § "Evidence directory layout for per-stage captures (999.73 criteria 1/2)" — the concrete file inventory from Phase 30's four directories, and the correction that raw/scrubbed/operator are **three separate runs, not one file redacted three ways**.
    - `.planning/milestones/v2.3.0-phases/30-keep-the-session-alive-past-turn-end/30c-evidence/run.log` — the `run.log` format to reproduce.
  </read_first>
  <action>
Extend `STREAM_JSON_STAGES`'s doc comment at `crates/devflow-cli/src/pipeline_launch.rs:439` with a
new paragraph recording criterion 1's in-source fact: `ClaudeAgent::exec_command`
(`crates/devflow-core/src/agents/claude.rs:46`) ignores its `_phase`, `_prompt` and
`_extra_writable_roots` arguments and returns a byte-identical argv for every stage, so membership in
this constant selects `resolve_launch_shape`'s pipe-owning branch and nothing else about the
transport varies per stage. State the consequence explicitly: a per-stage capture is evidence about
**agent behaviour under that stage's prompt** — whether it backgrounds work, whether
`background_tasks_changed` appears, whether it drains — and is never evidence about the transport.
Cite `ROADMAP criterion 1` and `34-REVIEW.md R-02`. Leave the existing `Stage::Code` / D-10
paragraph intact; this is an addition, not a replacement, and plan 34-05 will add the per-stage
evidence entries beneath it.

Create the evidence directory layout under
`.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/`,
following Phase 30's precedent as PATTERNS.md inventoried it, with one subdirectory per stage:
`define/`, `plan/`, `code/`, `validate/`, `ship/`. Write `34-evidence/README.md` stating: the file
inventory each stage directory will hold (`raw_output.jsonl` copied verbatim out of
`.devflow/`, `run.log` in the Phase-30 format carrying command invoked, stage, `claude` CLI version,
`devflow` build commit, the git commit of `STREAM_JSON_STAGES` at capture time, and outcome); that
`.devflow/.gitignore` is literally `*` so a capture is committed by **copying out**, never by
`git add -f` on the original path; that the three PII fields named in Phase 30's redaction table —
`home_path`, `os_username`, `session_identifier` — are placeholder-scrubbed before commit; and the
PATTERNS.md correction that Phase 30's raw / scrubbed / operator split was **three separate runs,
not one file redacted three ways**, so this phase's n=1-per-stage captures reproduce only the raw
variant plus scrubbing, not a three-way split. Add a `.gitkeep` in each stage subdirectory so the
empty layout is committable.

Do not create or copy any capture in this task — there is nothing to capture until plan 34-05
widens the constant and rebuilds the binary. This task creates the landing spot only.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/README.md && for s in define plan code validate ship; do test -d ".planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/$s" || exit 1; done && echo LAYOUT_OK; cargo test -p devflow --lib pipeline_launch:: 2>&1 | tee /tmp/34-02-t3.txt</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `LAYOUT_OK` and all five stage subdirectories exist.
    - `/tmp/34-02-t3.txt` shows the `pipeline_launch::` module selector reporting `0 failed` with a non-zero `filtered out` count.
    - `rg -c 'exec_command' crates/devflow-cli/src/pipeline_launch.rs` returns at least 1 and the match is inside `STREAM_JSON_STAGES`'s doc comment block (confirm with `rg -n -B 20 'const STREAM_JSON_STAGES'`).
    - `rg -n '_extra_writable_roots' crates/devflow-core/src/agents/claude.rs` returns a match on a parameter that is still unused at HEAD — confirm by reading the function body, not by the underscore prefix alone, and record the confirmation in the SUMMARY.
    - `rg -c 'home_path|os_username|session_identifier' .planning/phases/34-*/34-evidence/README.md` returns 3.
    - `rg -c 'three separate runs' .planning/phases/34-*/34-evidence/README.md` returns at least 1 — the PATTERNS.md correction is recorded where the capture operator will read it.
    - `git status --porcelain .planning/phases/34-*/34-evidence/` lists the README and the five `.gitkeep` files as untracked or staged; if it lists nothing, the directory is being swallowed by an ignore rule and the layout is not committable.
  </acceptance_criteria>
  <reversibility rating="reversible">A doc comment and empty directories.</reversibility>
  <done>The stage-blind-argv fact is recorded in source beside the constant it qualifies, and the five-stage evidence layout exists and is committable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow orchestrator → local filesystem (`.devflow/`) | Agent stdout captures are written here and pruned here. Everything under it is gitignored by a literal `*`. |
| phase evidence directory → public git history | Anything copied out of `.devflow/` into `.planning/` becomes a permanent public artifact on push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-02-01 | Denial of Service | `prune_history` / `DEFAULT_CAPTURE_RETENTION` | medium | mitigate | Retention eviction silently destroys the evidence a criterion depends on, with no error and no log. Task 2 raises the default to 12 with recorded arithmetic and pins both the retain and the evict-one behaviour with a named test. |
| T-34-02-02 | Repudiation | `canary_gate_only_applies_to_the_stream_launch_path` | medium | mitigate | A test whose premise silently becomes vacuous after widening reads as a passing guard while guarding nothing. Task 1 moves the discriminator onto the opt-out and keeps a paired opposite-result assertion, verified under a temporarily fully-widened constant. |
| T-34-02-03 | Information Disclosure | the evidence directory | high | mitigate | The layout created here is where an unscrubbed capture would land. `README.md` names the three PII fields to placeholder-scrub before commit and states that `git add -f` on a `.devflow/` path is the wrong route. The blocking control is in plan 34-05's checkpoint; this task makes the requirement visible at the landing spot. |
| T-34-02-04 | Tampering | `DEFAULT_CAPTURE_RETENTION` raise | low | accept | Raising the default increases local disk use in every user's `.devflow/history/`. Phase 30's captures were 55–65 KB, so twelve generations per phase is on the order of 1 MB. Accepted; the env and TOML overrides remain for anyone who needs less. |
| T-34-02-SC | Tampering | package installs | low | accept | No packages installed; `34-RESEARCH.md` § "Package Legitimacy Audit" records the phase adds no dependency. Re-run the Package Legitimacy Gate protocol if that changes. |
</threat_model>

<verification>
- `cargo test -p devflow --lib pipeline_launch::` — `0 failed`, non-zero `filtered out`.
- `cargo test -p devflow-core --lib agent_result::` and `config::` — `0 failed` each.
- `scripts/check.sh all` exits 0 (check `check.sh`'s own exit code, not a pipeline's).
- The temporarily-widened-constant control in task 1 was run and reverted, and the SUMMARY says so.
</verification>

<success_criteria>
- ROADMAP criterion 7's canary-test and retention halves are closed, both with paired opposite-result cases.
- ROADMAP criterion 1's in-source stage-blindness record exists beside `STREAM_JSON_STAGES`.
- The evidence directory layout exists, is committable, and carries the PII-scrub and copy-out instructions the capture operator needs.
- Nothing in this plan widens `STREAM_JSON_STAGES` — that is plan 34-05's deliverable and it must not be pre-empted here.
</success_criteria>

<output>
Create `.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-02-SUMMARY.md` when done.
</output>
