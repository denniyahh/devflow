---
phase: 34-stream-json-coverage-and-the-validate-trust-boundary-999-73-
plan: 03
type: tdd
wave: 2
depends_on: ["34-01"]
files_modified:
  - crates/devflow-cli/src/pipeline_outcomes.rs
autonomous: true
requirements: [DOGFOOD-04]
user_setup: []

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`classify_validate_outcome`'s `Passed` arm is gated on the derived `AgentStatus` structurally: every one of the seven `AgentStatus` variants appears by name in the match's status position, and adding an eighth variant is a compile error (E0004) rather than a silent join (D-06, ROADMAP criterion 3)"
    - "The match tuple is `(layer0, status, verdict)` where `layer0` is the layer-only normalisation `result.decided_by_layer == Some(0)` — never the composite `external` predicate, which folds an `AgentStatus` equality test back in and conflates Layer-1-Success with Layer-0-Failed (D-06's named trap)"
    - "`(true, Success, Some(Gaps))` and `(true, Success, None)` still classify as `Ambiguous`, and `(false, Success, Some(Gaps) | None)` still classifies as `Failed` — the ordinary Validate auto-loop Phase 33 repaired is preserved, not converted into an immediate gate (D-05's 'what this does NOT cover')"
    - "`RateLimited` and `AgentUnavailable` — the two variants the superseded criterion omitted — have a decided, defended destination (`Failed`, matching the destination today's `_` arm already gives them), with an inline comment naming why the cell is unreachable at the classifier and why it must not contradict `decide_action`'s live `RateLimited => AutoResume` routing"
    - "A 42-cell sweep exercises `layer0` in BOTH states across all 7 statuses and all 3 verdict states, so a regression deleting both `Ambiguous` arms is red rather than green"
    - "The post-graft-fix Validate shape `(layer0 = true, Success, verdict None)` routes to `ValidateOutcome::Ambiguous` and thence to an immediate gate, not to `Passed`/Ship — the downstream half of ROADMAP criterion 4's demonstration"
  prohibitions:
    - "A check that cannot produce the opposite result must not be reported as a demonstration — the exhaustiveness claim is reported only alongside the result of deliberately deleting a status arm and of adding an eighth variant, both of which must produce E0004"
    - "The Validate trust boundary must not be reported as closed by this plan alone — this closes criterion 3; criterion 4's graft is plan 34-01's and neither alone closes the pair"
  artifacts:
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "classify_validate_outcome rewritten as a wildcard-free-in-the-status-position match over (layer0, status, verdict)"
      contains: "fn classify_validate_outcome"
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "classify_validate_outcome_sweeps_all_forty_two_cells — the D-08 full matrix sweep"
      contains: "classify_validate_outcome_sweeps_all_forty_two_cells"
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "NC-1/NC-2/NC-3 named controls: external_verify_gaps_is_ambiguous_only_when_layer0_decided and external_verify_absent_verdict_is_ambiguous_only_when_layer0_decided"
      contains: "only_when_layer0_decided"
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "grafted_failure_shape_gates_instead_of_shipping — criterion 4's downstream routing demonstration"
      contains: "grafted_failure_shape_gates_instead_of_shipping"
  key_links:
    - from: "crates/devflow-cli/src/pipeline_outcomes.rs"
      to: "crates/devflow-core/src/agent_result.rs"
      via: "the rewritten match reads AgentResult::decided_by_layer and AgentResult::status as separate dimensions rather than through the collapsed `external` boolean"
      pattern: "decided_by_layer"
    - from: "crates/devflow-cli/src/pipeline_outcomes.rs"
      to: "crates/devflow-core/src/outcome_policy.rs"
      via: "the RateLimited arm's inline comment cites decide_action's AutoResume routing as the reason the cell is unreachable and must not be given divergent semantics"
      pattern: "AutoResume"
---

<objective>
Close ROADMAP criterion 3 (DOGFOOD-04's classifier half): replace `classify_validate_outcome`'s
`match (external, result.verdict)` — whose first arm `(_, Some(Verdict::Pass))` discards the derived
status entirely — with an exhaustive match on `(layer0, status, verdict)` in which every
`AgentStatus` variant appears by name, so a future variant is a compile error rather than a silent
join in either direction.

Purpose: the function's own doc comment already admits the current equality test was "audited by
hand" precisely because an equality test compiles untouched against a new variant, and `IdleTimeout`
arriving during Phase 31 is proof the variant set grows. This repo consistently prefers structural
guards over hand audits — `decide_action`'s wildcard-free match, `ParsedCapture` making dropped lines
representable — and D-06 continues that line.

This plan does **not** close criterion 4. The graft (plan 34-01) launders the status upstream, so by
the time the classifier runs the derived status genuinely is `Success` and this fix passes cleanly
over the exploit. Criterion 3 and criterion 4 are separate deliverables; that separation is itself
part of the deliverable and is recorded in the rewritten doc comment.

Output: the rewritten match, a 42-cell sweep exercising `layer0` in both states, four named controls
(NC-1 through NC-4), and the downstream half of criterion 4's demonstration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-CONTEXT.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-RESEARCH.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-PATTERNS.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-VALIDATION.md
@.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-01-SUMMARY.md
</context>

<flagged_assumptions>
**Spec-less probe fallback, row 4 — DOGFOOD-04 / `unclassified`, status `unresolved`.**

The edge probe returned one row for DOGFOOD-04 with category `unclassified` and the literal probe
text *"unclassified — review manually"*. Per the fallback protocol an `unclassified` row is **never**
auto-backstopped and stays `unresolved`, so it is surfaced here as an explicit flagged assumption
rather than silently converted into a truth or dropped.

- **What the probe could not classify:** DOGFOOD-04 is a trust-boundary guarantee ("a Validate
  stage's reported outcome reflects its actually-derived status"), not a data-structure operation.
  The probe's generic categories (adjacency / empty / ordering) have no purchase on it, which is why
  it fell through to `unclassified` rather than producing a wrong category.
- **What this plan asserts anyway, not as a resolution of the row but as its own coverage:** the
  42-cell sweep plus NC-4's two E0004 controls. Reviewed manually, that is the strongest edge
  criterion available for this requirement — every input combination has a decided destination and
  the enumeration is compiler-enforced.
- **What remains genuinely unresolved and is NOT claimed closed:** whether a real agent ever emits a
  self-contradictory `DEVFLOW_RESULT` marker in practice. No parser cross-checks `status` against
  `verdict`, no archived capture contains such a line, and absence here is weak evidence rather than
  a bound (`34-REVIEW.md` R-01 "What this does not establish", S-01 "Still unestablished"). The
  phase's fixes make the shape harmless; they do not measure its frequency.

An operator or verifier reading this must treat row 4 as open, not as covered by the bullets above.
</flagged_assumptions>

<artifacts_this_phase_produces>
Symbols this plan creates (new; exclude from drift verification):

| Kind | Name | File |
|---|---|---|
| test fn | `classify_validate_outcome_sweeps_all_forty_two_cells` | `crates/devflow-cli/src/pipeline_outcomes.rs` |
| test fn | `external_verify_gaps_is_ambiguous_only_when_layer0_decided` | `crates/devflow-cli/src/pipeline_outcomes.rs` |
| test fn | `external_verify_absent_verdict_is_ambiguous_only_when_layer0_decided` | `crates/devflow-cli/src/pipeline_outcomes.rs` |
| test fn | `verdict_pass_classifies_as_passed_regardless_of_layer` | `crates/devflow-cli/src/pipeline_outcomes.rs` |
| test fn | `grafted_failure_shape_gates_instead_of_shipping` | `crates/devflow-cli/src/pipeline_outcomes.rs` |
| test helper fn | `classifier_fixture` (builds an `AgentResult` from `(layer0, status, verdict)`) | `crates/devflow-cli/src/pipeline_outcomes.rs` |

Modified (pre-existing): `classify_validate_outcome` and its doc comment.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite classify_validate_outcome as an exhaustive (layer0, status, verdict) match</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_outcomes.rs:149-215` — `ValidateOutcome`'s three variants and `classify_validate_outcome` in full, including the `31-02 audit` paragraph at `:184-188` (which admits the hand audit) and the `ADJACENT, PRE-EXISTING, NOT INTRODUCED HERE` paragraph at `:195-202` (which asserts the superseded reachability story and must be rewritten).
    - `crates/devflow-core/src/outcome_policy.rs:1-67` — the module doc comment stating the no-wildcard mechanism, and `decide_action`'s body, which is this repo's precedent for naming every variant even when two share a destination and for the inline comment that explains a shared destination.
    - `crates/devflow-core/src/agent_result.rs:20-42` — `AgentResult`'s fields and `decided_by_layer`'s doc comment, which reserves `None` for test-only fixture literals.
    - `crates/devflow-core/src/agent_result.rs:47-81` — the seven `AgentStatus` variants, verbatim.
    - `crates/devflow-core/src/agent_result.rs:107-113` — `Verdict`'s two variants; the field is `Option<Verdict>`, giving three observable states.
    - `.planning/phases/34-.../34-CONTEXT.md` D-06 in full, especially the **"Shape, specified"** paragraph, which names the composite-`external` normaliser as the trap and states that the wildcard ban is positional.
    - `.planning/phases/34-.../34-01-SUMMARY.md` — the graft fix's landed state and the criterion-5 written finding, which this task's doc-comment rewrite must reproduce consistently.
  </read_first>
  <behavior>
    Given `layer0 = result.decided_by_layer == Some(0)`:
    - `(_, Success, Some(Pass))` → `Passed`
    - `(true, Success, Some(Gaps))` → `Ambiguous("external verification passed but the agent reported gaps")`
    - `(true, Success, None)` → `Ambiguous("external verification passed but no agent verdict arrived")`
    - `(false, Success, Some(Gaps))` → `Failed`
    - `(false, Success, None)` → `Failed`
    - `(_, Failed | Unknown | ResourceKilled | IdleTimeout | RateLimited | AgentUnavailable, _)` → `Failed`, with each of those six variants named in the status position
    - Deleting any status arm produces E0004. Adding an eighth `AgentStatus` variant produces E0004.
  </behavior>
  <action>
Rewrite `classify_validate_outcome`'s body at `crates/devflow-cli/src/pipeline_outcomes.rs:203-215`.
Keep the match **inline** in the function rather than extracting a helper — the function is 13 lines
today, has no other callers of its internal shape, and the sweep test constructs full `AgentResult`
literals, so a helper earns nothing (RESEARCH.md § "Where it physically lands").

Delete the composite boolean binding at `pipeline_outcomes.rs:204` — the one whose right-hand side
conjoins a `decided_by_layer` test with an `AgentStatus` equality test — and introduce in its place a
**layer-only** boolean named `layer0`, bound to the equality between `result.decided_by_layer` and
`Some(0)` and nothing else. Do not carry the status conjunct over into it, and do not keep the old
binding under any name. This is D-06's named trap: the composite folds an `AgentStatus` equality test
back into the normaliser — the exact hand-audited construct the rewrite exists to eliminate — and its
false value then conflates "Layer 1, Success" with "Layer 0, Failed", so the arms can no longer tell
provenance from status.

Match on `(layer0, result.status, result.verdict)`. The wildcard ban is **positional**: `_` in the
`layer0` or `verdict` position is permitted; the `AgentStatus` position must enumerate every variant
by name. A wildcard arm whose destination is `Failed` is forbidden just as one whose destination is
`Passed` is — the original D-06 wording banned only the `Passed` direction, which still permits a
catch-all routed to `Failed` and preserves the same compiles-untouched-against-a-new-variant
weakness in the other direction. Combine
variants that share a destination with `|` (the six non-`Success` statuses all go to `Failed`) rather
than writing 42 separate arms; the requirement is that the position is exhaustive, not that every
combination is written out.

Add an inline comment on the six-variant `Failed` arm, in `decide_action`'s style, recording: that
`RateLimited` and `AgentUnavailable` are the two variants the superseded criterion 3 omitted; that
all six are unreachable here because `classify_validate_outcome`'s sole production call site is
inside `Action::Advance` and `decide_action` maps only `Success` there; that `Failed` is chosen as
the destination because it is what today's `_` arm already gives them, so the rewrite has zero
runtime delta; and that a divergent destination for `RateLimited` specifically would contradict
`outcome_policy.rs`'s live, defended `AgentStatus::RateLimited => Action::AutoResume` routing if the
cell ever became reachable.

Rewrite the function's doc comment. Delete the claim that `Some(Verdict::Pass)` "is matched FIRST
and wins regardless of which layer decided the result" — it is no longer true. Delete the
`31-02 audit (non-exhaustive equality site 3 of 3)` paragraph, whose hand-audit rationale this
rewrite retires. Replace the `ADJACENT, PRE-EXISTING, NOT INTRODUCED HERE` paragraph with the
corrected record: the trust inversion IS reachable, by `reconcile_layer0_verdict`'s graft rather
than by this match's old wildcard; this match's own inputs genuinely are always `Success` because
the status is laundered upstream; that graft is fixed in plan 34-01 and **criterion 3's fix does not
close it**; and the `Ambiguous` arms' safety continues to depend on a routing decision in another
crate which `decide_action`'s own comment marks revisitable. Keep and restate the paragraph
explaining that a monitor-produced idle timeout carries `decided_by_layer: Some(1)` and
`verdict: None`, so it lands on the `Failed` destination — that routing is unchanged and intended.
Cite `D-06`, `D-05`, `D-15` and `ROADMAP criteria 3 and 5` in the rewritten comment.
  </action>
  <verify>
    <automated>cargo build -p devflow 2>&1 | tee /tmp/34-03-t1.txt; cargo test -p devflow --lib pipeline_outcomes:: 2>&1 | tee -a /tmp/34-03-t1.txt; cargo clippy -p devflow --all-targets -- -D warnings 2>&1 | tail -5 | tee -a /tmp/34-03-t1.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/34-03-t1.txt` shows the `pipeline_outcomes::` module selector reporting `0 failed` with a non-zero `filtered out` count. The package is `devflow`, not `devflow-cli`.
    - `rg -n 'fn classify_validate_outcome' -A 40 crates/devflow-cli/src/pipeline_outcomes.rs | rg -c 'let external'` returns 0 — the composite normaliser is gone.
    - `rg -n 'fn classify_validate_outcome' -A 40 crates/devflow-cli/src/pipeline_outcomes.rs | rg -c 'let layer0'` returns 1.
    - Each of the seven strings `AgentStatus::Success`, `AgentStatus::Failed`, `AgentStatus::Unknown`, `AgentStatus::RateLimited`, `AgentStatus::ResourceKilled`, `AgentStatus::AgentUnavailable`, `AgentStatus::IdleTimeout` appears at least once inside the rewritten match body (check with `rg -n 'fn classify_validate_outcome' -A 45`). A variant reachable only through a wildcard fails this criterion.
    - `rg -n 'fn classify_validate_outcome' -A 45 crates/devflow-cli/src/pipeline_outcomes.rs | rg -v '^\s*//' | rg -c '_ =>'` returns 0 — no wildcard arm at all in the match, in either destination direction. The comment filter is deliberate: an explanatory comment mentioning the old arm must not make this gate self-invalidating.
    - `cargo clippy -p devflow --all-targets -- -D warnings` exits 0.
    - The three pre-existing `external_verify_*` tests at `pipeline_outcomes.rs:1009-1130` still pass unmodified — run them by name and record the result. If any needed editing to pass, the rewrite changed reachable behaviour and that must be reported, not absorbed.
  </acceptance_criteria>
  <reversibility rating="reversible">A pure function over `&AgentResult` with no I/O, directly unit-testable, and no caller depends on the match's internal shape (CONTEXT.md D-06's own rating, restated).</reversibility>
  <done>The match enumerates all seven statuses by name with no wildcard arm, `layer0` is the layer-only normalisation, and the pre-existing external-verify tests pass untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The 42-cell sweep and NC-1/NC-2/NC-3's paired mirrors</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_outcomes.rs:1009-1130` — the three back-to-back `external_verify_*` tests. These are the naming convention, the `AgentResult` struct-literal style, and — critically — the proof that every existing literal sets `decided_by_layer: Some(0)` **explicitly**, never omitted.
    - `crates/devflow-core/src/agent_result.rs:38-42` — `decided_by_layer`'s `#[serde(default)]` attribute and the doc comment reserving `None` for fixtures that do not route through the real cascade. This is why an omitted field silently collapses half the sweep.
    - `.planning/phases/34-.../34-VALIDATION.md` § "Mandatory Negative Controls", rows NC-1, NC-2, NC-3, and the paragraph "Why NC-2/NC-3 pairing is load-bearing, not decorative".
    - `.planning/phases/34-.../34-CONTEXT.md` D-08 in full, including its amendment raising the sweep from 21 to 42 cells and naming the required controls.
    - `.planning/phases/34-.../34-PATTERNS.md` § "Descriptively-named small tests, never table-driven macros" and § "`AgentResult` struct literals in tests always set `decided_by_layer` explicitly".
  </read_first>
  <behavior>
    - The sweep visits all 2 × 7 × 3 = 42 `(layer0, status, verdict)` combinations and asserts each maps to its expected `ValidateOutcome`. It builds `layer0 = true` cells with `decided_by_layer: Some(0)` and `layer0 = false` cells with `Some(1)` — explicitly, in both cases.
    - NC-1: `(true, Success, Some(Pass))` and `(false, Success, Some(Pass))` both → `Passed`. The positive control is layer-independent.
    - NC-2: `(true, Success, Some(Gaps))` → `Ambiguous`; its mirror `(false, Success, Some(Gaps))` → `Failed`.
    - NC-3: `(true, Success, None)` → `Ambiguous`; its mirror `(false, Success, None)` → `Failed`.
    - A regression that deleted both `Ambiguous` arms turns NC-2 and NC-3 red. A sweep that omitted `decided_by_layer` would leave both green, which is the failure this pairing exists to prevent.
  </behavior>
  <action>
Add a private test helper `classifier_fixture(layer0: bool, status: AgentStatus, verdict:
Option<Verdict>) -> agent_result::AgentResult` to `pipeline_outcomes.rs`'s test module. It constructs
the struct literal in the existing style — `exit_code: None`, `reason: None`, `commits: None`,
`summary: None` — and sets `decided_by_layer` to `Some(0)` when `layer0` is true and `Some(1)` when
false. Never `None`: that value is reserved for fixtures that do not route through the real cascade,
and using it here would make the `layer0 = false` half indistinguishable from an omission bug. Give
the helper a doc comment stating exactly that.

Add `classify_validate_outcome_sweeps_all_forty_two_cells`. Iterate the seven `AgentStatus` variants
and the three `Option<Verdict>` states against both `layer0` values, computing the expected
`ValidateOutcome` from the decision table in this task's `<behavior>` block and asserting equality
per cell with a failure message naming the cell's three coordinates. Assert at the end that exactly
42 cells were visited — a counter compared to a literal, so a truncated iterator cannot pass
quietly. Do not introduce a table-driven macro, `rstest`, or any parameterised-test crate; none
exists in this codebase and RESEARCH.md's Open Question 2 explicitly recommends against introducing
one.

Add the three named controls as their own `#[test]` functions beside the sweep, each with a doc
comment naming its NC number and what the mirror proves:
`verdict_pass_classifies_as_passed_regardless_of_layer` (NC-1, both layer values asserted `Passed`
in one test, with an inline message stating that the layer-independence is the point);
`external_verify_gaps_is_ambiguous_only_when_layer0_decided` (NC-2, both halves in one test — the
`true` half `Ambiguous`, the `false` half `Failed` — so the pair cannot be split and half-deleted);
`external_verify_absent_verdict_is_ambiguous_only_when_layer0_decided` (NC-3, same shape). Each
mirror assertion carries an inline message stating that if both halves returned the same outcome the
`layer0` dimension would be decorative rather than load-bearing.

Leave the three pre-existing `external_verify_*` tests at `:1009-1130` unmodified. They are the
narrow behavioural pins for D-18e and they must keep passing against the rewritten match without
edits.
  </action>
  <verify>
    <automated>cargo test -p devflow --lib pipeline_outcomes::tests::classify_validate_outcome_sweeps_all_forty_two_cells -- --exact 2>&1 | tee /tmp/34-03-t2.txt; cargo test -p devflow --lib pipeline_outcomes::tests::external_verify_ 2>&1 | tee -a /tmp/34-03-t2.txt; cargo test -p devflow --lib pipeline_outcomes::tests::verdict_pass_classifies_as_passed_regardless_of_layer -- --exact 2>&1 | tee -a /tmp/34-03-t2.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/34-03-t2.txt` shows `1 passed` with a non-zero `filtered out` count for the sweep.
    - `/tmp/34-03-t2.txt` shows the `external_verify_` selector reporting at least `6 passed` and `0 failed`, with a non-zero `filtered out` count. Four tests pre-exist under that prefix at HEAD — `external_verify_agreement_advances_to_ship` (`:1010`), `external_verify_disagreement_gates_immediately` (`:1064`), `external_verify_no_verdict_gates_immediately` (`:1108`) and `external_verify_cycles_reach_ceiling_without_unbounded_loop` (`:1967`) — plus NC-2 and NC-3. Re-derive the pre-task count with `rg -c '^\s*fn external_verify_' crates/devflow-cli/src/pipeline_outcomes.rs` before trusting this number; if it is not 4 at execution time, an earlier wave added or removed one and the expected total must be recomputed rather than assumed.
    - `rg -n 'fn classifier_fixture' -A 15 crates/devflow-cli/src/pipeline_outcomes.rs` shows `Some(0)` and `Some(1)` and does not show `decided_by_layer: None`.
    - `rg -n 'fn classify_validate_outcome_sweeps_all_forty_two_cells' -A 40 crates/devflow-cli/src/pipeline_outcomes.rs` shows an assertion comparing a visited-cell counter to the literal `42`.
    - **Mutation control, run and reverted:** temporarily delete the `(true, Success, Some(Gaps))` arm from the rewritten match (letting that cell fall to `Failed`) and confirm `external_verify_gaps_is_ambiguous_only_when_layer0_decided` and the sweep both go red. Then temporarily change `classifier_fixture` to set `decided_by_layer: None` for the `layer0 = true` case and confirm NC-2 and NC-3 go red. Revert both. Report both mutation results in the SUMMARY — a sweep reported green with no mutation control is exactly the measurement this repo's discipline forbids.
  </acceptance_criteria>
  <reversibility rating="reversible">Test-only additions.</reversibility>
  <done>All 42 cells are asserted, the visited count is pinned to 42, NC-1/NC-2/NC-3 pass with their mirrors in the same tests, and both mutation controls were observed red and reverted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: NC-4's exhaustiveness controls and criterion 4's downstream routing demonstration</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_outcomes.rs` — the rewritten `classify_validate_outcome` from task 1.
    - `crates/devflow-cli/src/pipeline_outcomes.rs:217-230` — `ValidateResult`'s deliberately two-variant shape and the WR-03/18-fix reasoning for it.
    - `crates/devflow-cli/src/pipeline_outcomes.rs:298-400` — `handle_validate_outcome`, specifically the `Ambiguous` early-return gate path and the `ValidateResult::Passed` → `transition(.., Stage::Ship)` at `:395`. This is the routing criterion 4 must be established against.
    - `crates/devflow-core/src/agent_result.rs` — `reconcile_layer0_verdict` as fixed in plan 34-01, to confirm the exact post-fix triple this task asserts on.
    - `.planning/phases/34-.../34-VALIDATION.md` § "Mandatory Negative Controls" row NC-4.
    - `.planning/phases/34-.../34-REVIEW.md` § "S-03", the paragraph recording that two independent lanes compiled the wildcard-free match in 10 arms with deleting-an-arm and adding-an-eighth-variant both producing E0004.
  </read_first>
  <behavior>
    - NC-4a: deleting any single `AgentStatus` arm from the rewritten match makes `cargo check -p devflow` fail with error code E0004.
    - NC-4b: adding an eighth variant to `AgentStatus` in `devflow-core` makes `cargo check -p devflow` fail with E0004 at `classify_validate_outcome`.
    - Criterion 4 downstream: `classify_validate_outcome` applied to the POST-graft-fix Validate shape — `decided_by_layer: Some(0)`, `status: Success`, `verdict: None` — returns `Ambiguous`, which `handle_validate_outcome` routes to an immediate gate.
    - Criterion 4 pre-fix contrast: the same function applied to the PRE-fix laundered shape — `decided_by_layer: Some(0)`, `status: Success`, `verdict: Some(Pass)` — returns `Passed`, which routes to Ship. This is the downstream half of the exploit and it is asserted as such, not as a defect in the classifier.
  </behavior>
  <action>
Run NC-4 as a **recorded compile experiment**, not a committed test — Rust cannot assert a compile
error from inside the same crate's test suite without a `trybuild`-style harness, and this codebase
has none (do not introduce one; `34-RESEARCH.md` § "Package Legitimacy Audit" records the phase adds
no dependency, and introducing one would require re-running the Package Legitimacy Gate protocol).
For NC-4a: delete one named `AgentStatus` arm from the rewritten match, run `cargo check -p devflow`,
capture the diagnostic, confirm the error code is `E0004`, revert. For NC-4b: add a temporary eighth
variant to `AgentStatus` in `crates/devflow-core/src/agent_result.rs`, run `cargo check -p devflow`,
confirm `E0004` is raised at `classify_validate_outcome`'s match, revert. Paste both diagnostics
verbatim into the plan SUMMARY under a heading naming NC-4, and record the exact `git status`
cleanliness check confirming both reverts landed. Do **not** leave either mutation in the tree; run
`git diff --stat` after reverting and record that it shows no change to `agent_result.rs`.

Add `grafted_failure_shape_gates_instead_of_shipping` as a committed test. Using `classifier_fixture`
from task 2, assert that the POST-fix shape `(layer0 = true, AgentStatus::Success, None)` classifies
as `ValidateOutcome::Ambiguous` and that its payload string names the missing verdict, and assert in
the same test that the PRE-fix laundered shape `(layer0 = true, AgentStatus::Success,
Some(Verdict::Pass))` classifies as `ValidateOutcome::Passed`. The doc comment states the pairing
explicitly: this is the downstream half of ROADMAP criterion 4's demonstration; the upstream half —
that the graft no longer produces the second shape from a `{"status":"failed","verdict":"pass"}`
marker — is pinned in `devflow-core` by
`layer0_verdict_graft_declines_when_layer1_status_is_not_success` (plan 34-01), and the two crates'
tests together cover the route because `classify_validate_outcome` is `pub(crate)` to `devflow-cli`
and cannot be called from `devflow-core`'s test module. Name that visibility constraint in the doc
comment so a future reader does not "consolidate" the pair into one crate and break it.

Additionally assert, in the same test, that `handle_validate_outcome`'s two destinations are
distinguishable in the direction this demonstration claims — either by calling it against a tempdir
fixture in the style of the pre-existing `external_verify_agreement_advances_to_ship` test at
`pipeline_outcomes.rs:1009`, or, if that test's fixture cannot be reused without an `ENV_MUTEX`
deadlock, by asserting on `ValidateOutcome`'s discriminant and citing
`external_verify_disagreement_gates_immediately` as the pre-existing pin for the routing half. State
in the SUMMARY which of the two routes was taken and why.
  </action>
  <verify>
    <automated>cargo test -p devflow --lib pipeline_outcomes::tests::grafted_failure_shape_gates_instead_of_shipping -- --exact 2>&1 | tee /tmp/34-03-t3.txt; cargo test -p devflow --lib pipeline_outcomes:: 2>&1 | tee -a /tmp/34-03-t3.txt; git diff --stat crates/devflow-core/src/agent_result.rs 2>&1 | tee -a /tmp/34-03-t3.txt</automated>
  </verify>
  <acceptance_criteria>
    - `/tmp/34-03-t3.txt` shows `1 passed` with a non-zero `filtered out` count for `grafted_failure_shape_gates_instead_of_shipping`.
    - `/tmp/34-03-t3.txt` shows the `pipeline_outcomes::` module selector reporting `0 failed` and a `passed` count no lower than task 2's.
    - `/tmp/34-03-t3.txt`'s `git diff --stat` line for `crates/devflow-core/src/agent_result.rs` is empty — NC-4b's temporary eighth variant was reverted.
    - The SUMMARY contains both NC-4 diagnostics verbatim, each showing `E0004`, under a heading naming NC-4. A prose claim that exhaustiveness holds, without the two pasted diagnostics, does not satisfy this criterion.
    - The SUMMARY states which of the two routes task 3 took for the `handle_validate_outcome` assertion, and why.
    - `rg -n 'pub\(crate\)' crates/devflow-cli/src/pipeline_outcomes.rs | rg -c 'fn classify_validate_outcome'` returns 1 — the visibility that forces the two-crate test split is unchanged.
  </acceptance_criteria>
  <reversibility rating="reversible">One committed test plus two mutations that are made, measured and reverted within the task.</reversibility>
  <done>Both E0004 diagnostics are recorded and reverted, and the downstream routing pair (`Ambiguous` post-fix, `Passed` for the laundered shape) is pinned by a named committed test that documents its cross-crate partner.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written `DEVFLOW_RESULT` envelope → Validate classification | `status` and `verdict` are both self-reported fields deserialized independently with no cross-check; the classifier is where they are arbitrated against provenance. |
| classifier → pipeline state machine | `ValidateOutcome::Passed` is the only value that reaches `transition(.., Stage::Ship)` without a gate in `Mode::Auto`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-34-03-01 | Spoofing | `classify_validate_outcome`'s old `(_, Some(Pass))` arm | high | mitigate | An agent-controlled `verdict` field currently outranks the independently-derived status structurally. Task 1 enumerates the status position so no status can be discarded by a wildcard; task 3's NC-4 controls prove the enumeration is compiler-enforced rather than hand-audited. |
| T-34-03-02 | Denial of Service | the `(false, Success, Gaps \| None)` cells | high | mitigate | Collapsing these to `Ambiguous` would convert the ordinary "validation found gaps" auto-loop into an immediate gate on cycle one — the loop 999.65/999.66 and Phase 33 just repaired, i.e. a self-inflicted denial of unattended operation. Pinned by NC-2 and NC-3's `layer0 = false` mirrors, both of which must assert `Failed`. |
| T-34-03-03 | Tampering | the sweep fixture's `decided_by_layer` | high | mitigate | `decided_by_layer` is `#[serde(default)]`, so an omitted field silently yields `None`, collapses `layer0` to false in all 42 cells, and leaves both `Ambiguous` arms unexercised — a regression deleting them both would be green. Mitigated by `classifier_fixture` setting the field explicitly in both directions and by task 2's second mutation control, which deliberately reintroduces the omission and requires the tests to go red. |
| T-34-03-04 | Repudiation | the rewritten doc comment | medium | mitigate | The existing comment asserts the superseded reachability story; leaving it would have a future reader reconstruct the wrong route into 999.74. Task 1 replaces it with the corrected record and names criterion 3 and criterion 4 as separate deliverables. |
| T-34-03-05 | Elevation of Privilege | `RateLimited`'s new explicit destination | low | accept | Giving `RateLimited` a destination inside the classifier could, if the cell ever became reachable, contradict `decide_action`'s deliberate `AutoResume` routing. Accepted rather than mitigated: the cell is unreachable today, `Failed` is what the current `_` arm already yields so the delta is zero, and the tension is recorded in an inline comment so a future change that makes it reachable is confronted with it. |
| T-34-03-SC | Tampering | package installs | low | accept | No packages installed. Task 3 explicitly declines to add a `trybuild`-class dependency for NC-4 and runs it as a recorded compile experiment instead. Re-run the Package Legitimacy Gate protocol if any dependency is introduced. |
</threat_model>

<verification>
- `cargo test -p devflow --lib pipeline_outcomes::` — `0 failed`, non-zero `filtered out`, and the three pre-existing `external_verify_*` tests passing unmodified.
- `cargo clippy -p devflow --all-targets -- -D warnings` exits 0.
- `scripts/check.sh all` exits 0 (its own exit code, not a pipeline's).
- Both NC-4 diagnostics recorded verbatim in the SUMMARY, both showing `E0004`, both reverted with `git diff --stat` confirming it.
- Both task-2 mutation controls observed red and reverted.

</verification>

<success_criteria>
- ROADMAP criterion 3 is satisfied: the `Passed` arm is gated on the derived status structurally, with a decided and defended destination for all seven `AgentStatus` variants including `RateLimited` and `AgentUnavailable`.
- NC-1 through NC-4 are each discharged by a named artifact — three committed tests and one recorded, reverted compile experiment.
- The downstream half of criterion 4 is pinned and its cross-crate partner is named in source.
- The plan does NOT claim criterion 4 is closed; the graft fix (plan 34-01) is the other half.
- Spec-less probe fallback row 4 remains flagged as unresolved and is not silently marked covered.
</success_criteria>

<output>
Create `.planning/phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-03-SUMMARY.md` when done.
</output>
