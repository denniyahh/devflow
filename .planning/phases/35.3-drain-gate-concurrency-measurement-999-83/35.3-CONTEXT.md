# Phase 35.3: Drain Gate Concurrency Measurement — Context

**Gathered:** 2026-08-12
**Status:** Ready for planning

<domain>
## Phase Boundary

Get a real, evidence-based answer to whether the drain gate — the v2.4.0 stream-json safety net —
actually observes concurrent child work. This is investigation-shaped: design and run the right
experiment, then record what it found, rather than apply a predetermined fix.

The honest result may be negative ("the gate does not see concurrency"), and a negative is a valid
deliverable — it must be recorded as-is, not assumed away.

**In scope:**
- A measurement that establishes which event families the Claude CLI actually emits for each kind
  of concurrent child work — sub-agent dispatch and backgrounded shell — from live production
  evidence, n>1 where feasible
- An honest, version-pinned record of the answer, including negative/inconclusive results
- The per-phase Validate-failure count observation (the same ask 35.1's criterion 6 made)

**Out of scope:**
- Widening `CloseRule::observe` itself — that widening ALREADY LANDED in commit `47ea511`
  ("fix(monitor): stop the idle timeout killing stages that background work", merged in 35.1).
  `CloseRule` already tracks `open_tasks` from the real per-task vocabulary
  (`task_started`/`task_progress` open, `task_notification`/`task_updated` close). What remains is
  measuring whether it works — not writing more of it.
- A CLI version matrix. Pin to the current CLI version and record it; do not chase a range.
</domain>

<decisions>
## Implementation Decisions

### Evidence-gathering method

- **D-01:** Use a drill-style disposable fixture (the 35.1 `unattended-drill.sh` pattern), not a
  live dogfood run. A drill controls the workload shape — it can force both the sub-agent path and
  the backgrounded-shell path deterministically — and it is repeatable to n>1, where Phase 34's
  live-run approach produced exactly one observation.

  The drill inherits 35.1's precedent: same throwaway-fixture scaffolding, same
  refuse-to-scaffold-inside-this-checkout destination guard (T-23-01), same capture-to-evidence-file
  flow. It is smaller than 35.1's: scaffold a throwaway repo, launch `claude -p` against a prompt
  that deliberately dispatches sub-agents AND backgrounds a shell, capture the raw stream, and count
  the `background_tasks_changed` / `task_started` / `task_progress` / `task_notification` events.

  **The synthetic-prompt caveat is load-bearing.** A drill prompt is synthetic, and Phase 34's whole
  refutation is that the synthetic fixture does not match what production emits. The drill must
  therefore be paired with at least one real capture (a real GSD `execute-phase` workflow) to
  validate that the synthetic prompt triggers the same event families the real one does. The
  synthetic prompt establishes the event-family mapping; the real capture confirms it is the right
  mapping to have established.

### Event-family coverage

- **D-02:** Cover BOTH concurrent-work mechanisms, because they are mechanistically different and
  one is untested:
  1. **Sub-agent dispatch** (`Agent` tool → `task_started`/`task_progress`/`task_notification`) —
     Phase 34 refuted the *old* single-key gate (8 dispatches, zero `background_tasks_changed`),
     and `47ea511` already responded by adding `open_tasks` tracking on exactly this vocabulary.
     The goal here is to *verify the widened gate observes this path* at n>1, not re-refute the
     old one.
  2. **Backgrounded shell** (`Bash` with `run_in_background: true`) — never exercised by Phase 34
     or by `47ea511` (whose commit message says "Proven in tests, untested live"). The open
     question is whether backgrounded shells emit `task_started`/`task_progress` too, or something
     the widened gate still cannot see.

  The gate now keys on TWO signals ANDed: the synthetic `background_tasks_changed` drain arm, and
  the real per-task `open_tasks` vocabulary. If backgrounded shells emit neither, the widened gate
  is still blind to that path. Covering only the sub-agent path would leave the most interesting
  unknown unanswered.

### On confirmation of refutation

- **D-03:** The widening of `CloseRule::observe` ALREADY LANDED in commit `47ea511` (merged in
  35.1) — it was done as part of `999.99`'s idle-timeout fix, not as a separate backlog item.
  `CloseRule` already tracks `open_tasks` from the real per-task vocabulary
  (`task_started`/`task_progress` open, `task_notification`/`task_updated` close), ANDed with the
  synthetic `background_tasks_changed` drain arm.

  **Consequence for this phase:** the "widen or document" fork criterion 3 offers is already
  resolved — it was widened. What remains is to measure whether the widening actually observes
  production concurrency. If the measurement finds a residual gap (notably the backgrounded-shell
  path, which `47ea511` never exercised — its commit message says "Proven in tests, untested
  live"), THEN file that specific gap as a `999.x` backlog item. Do not re-widen what is already
  widened, and do not file a widening item for the sub-agent path unless the measurement shows the
  existing `open_tasks` tracking is itself wrong.

### CLI version

- **D-04:** Pin to the current CLI version and record it in the evidence. Do not build a version
  matrix. A version-pinned negative ("on CLI X, sub-agent dispatch emits no
  `background_tasks_changed`") is actionable — it says *your current CLI* does not feed the gate.
  A matrix would only matter to prove a historical claim, and nothing here needs one.

### Claude's Discretion

- The exact fixture prompt text and the number of repetitions (n) — the plan should aim for n≥2 per
  path where the run cost permits, and record the actual n honestly.
- Whether the real capture rides on an existing dogfood run or a dedicated minimal
  `devflow start --no-worktree` probe (Phase 34's `34-evidence/` README records the layout and PII
  scrub conventions to reuse).
</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase scope and the prior refutation
- `.planning/ROADMAP.md` § "Phase 35.3: Drain Gate Concurrency Measurement (999.83)" — the five success criteria
- `.planning/milestones/v2.4.0-phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/DRAIN-ANALYSIS.md` — Phase 34's refutation of the sub-agent path, and its explicit "what this does not establish" (backgrounded-shell path, single CLI version, n=1)
- `.planning/milestones/v2.4.0-phases/34-stream-json-coverage-and-the-validate-trust-boundary-999-73-/34-evidence/README.md` — the capture layout, PII scrub, and `run.log` provenance format to reuse

### The gate under test
- `crates/devflow-core/src/monitor.rs` § `CloseRule` / `BackgroundTaskState` (around line 502-650) — the two-armed close rule: the synthetic `background_tasks_changed` drain arm AND the real per-task `open_tasks` vocabulary, `TERMINAL_TASK_STATUSES`, and `observe_task_event`
- commit `47ea511` (`git show 47ea511`) — the widening that already landed in 35.1; its commit message records the production capture ("task_started x1, task_progress x61 ... background_tasks_changed x0") and the "Proven in tests, untested live" caveat the measurement exists to close
- `crates/devflow-core/src/monitor.rs` around line 1164 — the synthetic fixture that manufactures a `background_tasks_changed` announcement

### Drill precedent
- `scripts/unattended-drill.sh` (35.1-04) — the fixture-scaffolding, destination-guard, and capture-to-evidence precedent this phase's drill reuses

### The observation to carry
- `.planning/ROADMAP.md` § "Phase 35.3" criterion 5 — the per-phase Validate-failure count observation (same as 35.1 criterion 6)
</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- **`scripts/unattended-drill.sh`** — the 35.1 drill: throwaway fixture scaffolding, T-23-01 destination guard, two-arm structure, evidence-file writing. This phase's drill is a smaller single-path variant of it.
- **`34-evidence/` layout** — `raw_output.jsonl` + `run.log` + `stderr.log` per stage, with the PII scrub (`home_path`/`os_username`/`session_identifier`) and the required `run.log` fields (argv, stage, CLI version, build commit, outcome).
- **`BackgroundTaskState` / `CloseRule::observe`** — the code the measurement is *about*. The event-key shape to count (`type: "system", subtype: "background_tasks_changed"`) and the synthetic fixture's `task_type: "local_agent"` value.

### Established Patterns
- **Synthetic-vs-production validation** — a synthetic fixture must be validated against at least one real capture, or its measurement is a proxy for the fixture, not for production (the exact lesson Phase 34's DRAIN-ANALYSIS records).
- **Negative results are results** — Phase 34 recorded the refutation instead of assuming the answer resolved toward "the gate works." This phase inherits that posture.
- **PII scrub before commit** — anything copied into `.planning/` is a permanent public artifact on push; the three-field scrub (`home_path`, `os_username`, `session_identifier`) is mandatory.

### Integration Points
- The drill produces capture files that land in the phase's own evidence directory (following `34-evidence/`), not in `.devflow/` — copy out, never `git add -f` the live capture.
- If the measurement finds a residual gap the widened gate (`47ea511`) does not cover — notably the backgrounded-shell path — file that specific gap as a `999.x` backlog entry. Do not re-widen what is already widened; do not file a widening item for the sub-agent path unless the measurement shows `open_tasks` tracking is itself wrong.
</code_context>

<specifics>
## Specific Ideas

No specific references beyond what's captured above — the measurement design follows the decisions directly.
</specifics>

<deferred>
## Deferred Ideas

- **Residual widening gaps, IF the measurement finds them** — e.g. the backgrounded-shell path
  not emitting the per-task vocabulary, or `open_tasks` tracking being wrong in a way the
  measurement exposes. File as a `999.x` backlog item on measurement evidence, not in advance.
- **A CLI version matrix** — deferred. Nothing here needs to prove a historical claim; a
  version-pinned result is sufficient.

</deferred>

---

*Phase: 35.3-drain-gate-concurrency-measurement*
*Context gathered: 2026-08-12*
