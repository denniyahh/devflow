---
phase: 42-hermes-driver
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/agents/hermes.rs
  - crates/devflow-core/src/state.rs
  - crates/devflow-core/src/agents/mod.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/tests/phase7_cli.rs
autonomous: true
requirements: [HRMS-01, HRMS-02, HRMS-03]
estimate:
  tokens: 65000
  raw_tokens: 35000
  tasks: 4
  confidence: high

must_haves:
  truths:
    - "`--agent hermes` resolves through `AgentKind`/`driver_for`/`agent_program`, and `devflow doctor` reports it installed (HRMS-01, D-05, D-06)"
    - "`HermesDriver::build_command` returns `(\"hermes\", vec![\"-z\".into(), prompt.to_string(), \"--yolo\".into(), \"--accept-hooks\".into()])` and `environment()` sets `HERMES_ACCEPT_HOOKS=1` (HRMS-02, D-01)"
    - "`HermesDriver::render_prompt` delegates to `crate::prompt::render_claude_style(intent)` (D-02)"
    - "`HermesDriver::capabilities` probes `hermes tools list` for enabled delegation via `hermes_subagent_dispatch_available()` (D-04)"
    - "HermesDriver is enrolled in the shared conformance suite (5 → 6 drivers), proven by the `hermes_conformance_enrollment` unit test (D-06)"
    - "Completion is process-exit + `DEVFLOW_RESULT` prompt contract; a stubbed `hermes` binary with exit 0 and no marker does NOT advance a commit-gated stage (HRMS-03, D-03)"
    - "Non-zero exit codes and hung `hermes` processes are handled cleanly without advancing stages or leaking processes (HRMS-03)"
  artifacts:
    - `crates/devflow-core/src/agents/hermes.rs` — HermesDriver implementation, environment, subagent capability probe, and unit tests
    - `crates/devflow-core/src/state.rs` — `AgentKind::Hermes` variant, `Display`, `FromStr`, and serde serialization
    - `crates/devflow-core/src/agents/mod.rs` — `driver_for` match arm, module export, and 6-driver conformance suite enrollment
    - `crates/devflow-cli/src/commands.rs` — `doctor_checks()` hermes presence check
    - `crates/devflow-cli/tests/phase7_cli.rs` — hermes stub fixture, marker-less regression, non-zero exit regression, and hung process detection
---

<objective>
Implement the modular Hermes agent driver (`AgentKind::Hermes`), register it across `devflow-core` and `devflow-cli`, enroll it in the shared conformance test suite, add presence-only health checks to `devflow doctor`, and verify process-exit failure modes (marker-less, non-zero exit, hung process) via integration tests.

Purpose: Deliver full Hermes agent support in DevFlow (HRMS-01, HRMS-02, HRMS-03) matching the modular driver contract.
Output: Working `HermesDriver` module, updated `state.rs`, `agents/mod.rs`, `commands.rs`, and green unit/integration tests.
</objective>

<execution_context>
@$HOME/.config/opencode/gsd-core/workflows/execute-plan.md
@$HOME/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-hermes-driver/42-CONTEXT.md
@.planning/phases/42-hermes-driver/42-RESEARCH.md
@.planning/phases/42-hermes-driver/42-PATTERNS.md
@crates/devflow-core/src/agents/pi.rs
@crates/devflow-core/src/agents/antigravity.rs
@crates/devflow-core/src/agents/mod.rs
@crates/devflow-core/src/state.rs
@crates/devflow-cli/src/commands.rs
@crates/devflow-cli/tests/phase7_cli.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HermesDriver in crates/devflow-core/src/agents/hermes.rs</name>
  <files>crates/devflow-core/src/agents/hermes.rs</files>
  <read_first>
    crates/devflow-core/src/agents/pi.rs
    crates/devflow-core/src/agents/antigravity.rs
    crates/devflow-core/src/agents/mod.rs
    crates/devflow-core/src/prompt.rs
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `crates/devflow-core/src/agents/hermes.rs` exists and defines `pub struct HermesDriver` implementing `AgentDriver`
    - `HermesDriver::name()` returns `"Hermes"`
    - `HermesDriver::render_prompt()` delegates to `crate::prompt::render_claude_style(intent)` (D-02)
    - `HermesDriver::build_command()` returns `("hermes", vec!["-z".into(), prompt.to_string(), "--yolo".into(), "--accept-hooks".into()])` (D-01)
    - `HermesDriver::environment()` returns `vec![("HERMES_ACCEPT_HOOKS".into(), "1".into())]` (D-01)
    - `HermesDriver::capabilities()` returns `DriverCapabilities { subagent_dispatch: hermes_subagent_dispatch_available() }` (D-04)
    - `hermes_subagent_dispatch_available()` runs `hermes tools list` and checks for `enabled` and `delegation`
    - Unit tests in `hermes.rs` verify `exec_command_shape`, `environment_contains_accept_hooks`, `render_prompt_claude_style`, and `capabilities_detect_delegation`
    - `cargo test -p devflow-core --lib hermes_driver` exits 0
  </acceptance_criteria>
  <action>
    Create `crates/devflow-core/src/agents/hermes.rs` implementing `AgentDriver` for `HermesDriver`.
    Implement `name`, `render_prompt` (reusing `render_claude_style` per D-02), `build_command` (`-z`, prompt, `--yolo`, `--accept-hooks` per D-01), `environment` (`HERMES_ACCEPT_HOOKS=1` per D-01), `capabilities` (using `hermes_subagent_dispatch_available` per D-04), and `health` (presence-only).
    Include unit tests with stubbed PATH tests verifying command shape, environment, and dynamic delegation detection.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib hermes_driver -- --nocapture</automated>
  </verify>
  <done>HermesDriver is implemented with unit tests and verified green.</done>
</task>

<task type="auto">
  <name>Task 2: Register AgentKind::Hermes and wire driver dispatch and conformance suite</name>
  <files>crates/devflow-core/src/state.rs, crates/devflow-core/src/agents/mod.rs</files>
  <read_first>
    crates/devflow-core/src/state.rs
    crates/devflow-core/src/agents/mod.rs
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `crates/devflow-core/src/state.rs` enum `AgentKind` includes `Hermes` variant (D-05)
    - `AgentKind::Hermes.to_string()` returns `"hermes"`
    - `"hermes".parse::<AgentKind>()` returns `Ok(AgentKind::Hermes)`
    - `crates/devflow-core/src/agents/mod.rs` exposes `pub mod hermes;` and `pub use hermes::HermesDriver;`
    - `driver_for(AgentKind::Hermes)` returns `Box::new(HermesDriver)`
    - `every_driver_passes_the_conformance_suite` hardcoded array size updated from 5 to 6 drivers (D-06)
    - Test `hermes_conformance_enrollment` in `agents/mod.rs` tests `HermesDriver` against all 7 contract checks
    - `cargo test -p devflow-core --lib agent_kind_hermes` exits 0
    - `cargo test -p devflow-core --lib hermes_conformance_enrollment` exits 0
  </acceptance_criteria>
  <action>
    1. In `crates/devflow-core/src/state.rs`: Add `Hermes` variant to `AgentKind`, update `Display`, `FromStr`, `AgentParseError` message, and add unit tests for `AgentKind::Hermes` serialization and parsing.
    2. In `crates/devflow-core/src/agents/mod.rs`: Add `pub mod hermes;`, `pub use hermes::HermesDriver;`, wire `driver_for(AgentKind::Hermes) => Box::new(HermesDriver)`, update `every_driver_passes_the_conformance_suite` to include `Box::new(HermesDriver)` in its 6-element array, and add `hermes_conformance_enrollment` test.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib agent_kind_hermes -- --nocapture && cargo test -p devflow-core --lib hermes_conformance_enrollment -- --nocapture</automated>
  </verify>
  <done>AgentKind::Hermes is registered, driver_for is wired, and conformance enrollment is verified.</done>
</task>

<task type="auto">
  <name>Task 3: Add Hermes presence probe to devflow doctor</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    crates/devflow-cli/src/commands.rs
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `doctor_checks()` in `crates/devflow-cli/src/commands.rs` includes a `cmd_check` for `"hermes"` checking `hermes --version` (D-06)
    - Unit test `doctor_includes_hermes` in `commands.rs` verifies that the `doctor_checks()` list contains the `"hermes"` check
    - `cargo test -p devflow --bin devflow doctor_includes_hermes` exits 0
  </acceptance_criteria>
  <action>
    Add `"hermes"` `cmd_check` entry to `doctor_checks()` in `crates/devflow-cli/src/commands.rs`.
    Add unit test `doctor_includes_hermes` verifying the check is present in `doctor_checks()`.
  </action>
  <verify>
    <automated>cargo test -p devflow --bin devflow doctor_includes_hermes -- --nocapture</automated>
  </verify>
  <done>devflow doctor includes Hermes presence check, verified by unit test.</done>
</task>

<task type="auto">
  <name>Task 4: Add Hermes integration regression tests in phase7_cli.rs</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    crates/devflow-cli/tests/phase7_cli.rs
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `crates/devflow-cli/tests/phase7_cli.rs` defines `hermes_stub(launch: &str)`
    - Test `hermes_marker_less_run_does_not_advance` proves exit 0 without DEVFLOW_RESULT does not advance commit-gated stage (HRMS-03, D-03)
    - Test `hermes_nonzero_exit_does_not_advance` proves exit 1 halts/gates the pipeline (HRMS-03)
    - Test `hermes_hung_process_is_detected_not_left_running` proves a hung hermes process is detected and reaped (HRMS-03)
    - All tests bind `MonitorReapGuard` and clean up processes cleanly
    - `cargo test -p devflow --test phase7_cli hermes` exits 0
  </acceptance_criteria>
  <action>
    In `crates/devflow-cli/tests/phase7_cli.rs`:
    1. Define `hermes_stub(launch: &str) -> String` that handles `tools list` (`✓ enabled delegation 👥 Task Delegation`) and `launch` for stage runs.
    2. Add integration tests:
       - `hermes_marker_less_run_does_not_advance()`: stubbed hermes exits 0 with no marker; asserts stage does not advance past Plan.
       - `hermes_nonzero_exit_does_not_advance()`: stubbed hermes exits 1; asserts stage does not advance.
       - `hermes_hung_process_is_detected_not_left_running()`: stubbed hermes sleeps; asserts liveness detection and process cleanup.
  </action>
  <verify>
    <automated>cargo test -p devflow --test phase7_cli hermes -- --nocapture</automated>
  </verify>
  <done>Hermes transport integration tests are implemented and verified green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow process monitor → Hermes CLI | Spawning `hermes -z` with `--yolo` and `--accept-hooks` executes commands non-interactively. |
| Hermes process stdout → `parse_marker_lines` | Stdout parsing must strictly validate DEVFLOW_RESULT JSON format before advancing commit-gated stages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-01 | Elevation of Privilege | CLI spawn argv | high | mitigate | Pass `--accept-hooks` and `HERMES_ACCEPT_HOOKS=1` to prevent interactive prompt hang; sanitize prompt argument. |
| T-42-02 | Tampering | Completion detection | high | mitigate | Process-exit parser requires explicit DEVFLOW_RESULT marker on commit-gated stages; marker-less runs fail closed. |
| T-42-03 | Denial of Service | Hung process handling | medium | mitigate | Monitor tracks agent PID and liveness; kills hung child processes on timeout. |
| T-42-04 | Repudiation | Conformance suite | low | mitigate | Enroll HermesDriver in conformance suite to verify 7 trait contract checks. |
| T-42-05 | Information Disclosure | Doctor presence check | low | mitigate | Doctor probe checks version string and existence without executing user prompts. |
| T-42-06 | Denial of Service | Subagent probe failure | low | mitigate | `hermes_subagent_dispatch_available` fails closed to `false` on any probe failure without halting launch. |
</threat_model>

<verification>
Full test suite passes:
1. `cargo test -p devflow-core --lib hermes_driver`
2. `cargo test -p devflow-core --lib agent_kind_hermes`
3. `cargo test -p devflow-core --lib hermes_conformance_enrollment`
4. `cargo test -p devflow --bin devflow doctor_includes_hermes`
5. `cargo test -p devflow --test phase7_cli hermes`
</verification>

<success_criteria>
`AgentKind::Hermes` resolves end-to-end; `HermesDriver` implements the modular driver contract; conformance suite passes with 6 drivers; `devflow doctor` checks Hermes; marker-less, error exit, and hung process regressions pass in `phase7_cli.rs`.
</success_criteria>

<output>
Create `.planning/phases/42-hermes-driver/42-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- `HermesDriver` (struct in `crates/devflow-core/src/agents/hermes.rs`)
- `AgentKind::Hermes` (enum variant in `crates/devflow-core/src/state.rs`)
- `hermes_subagent_dispatch_available` (function in `crates/devflow-core/src/agents/hermes.rs`)
- `hermes_conformance_enrollment` (test function in `crates/devflow-core/src/agents/mod.rs`)
- `doctor_includes_hermes` (test function in `crates/devflow-cli/src/commands.rs`)
- `hermes_marker_less_run_does_not_advance` (test function in `crates/devflow-cli/tests/phase7_cli.rs`)
- `hermes_nonzero_exit_does_not_advance` (test function in `crates/devflow-cli/tests/phase7_cli.rs`)
- `hermes_hung_process_is_detected_not_left_running` (test function in `crates/devflow-cli/tests/phase7_cli.rs`)
