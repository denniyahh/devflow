---
phase: 42-hermes-driver
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - crates/devflow-cli/src/preflight.rs
  - .planning/phases/42-hermes-driver/42-VERIFICATION.md
  - .planning/phases/42-hermes-driver/42-UAT.md
autonomous: false
requirements: [ANTG-04]
estimate:
  tokens: 50000
  raw_tokens: 25000
  tasks: 3
  confidence: high

must_haves:
  truths:
    - "Phase 42 is executed through a real supervised Antigravity dogfood run (`devflow start --agent antigravity --phase 42 --mode supervise`) (ANTG-04, D-07)"
    - "Quiet-gap event cadence distribution is measured and compared against the 120s idle timeout floor (`DEVFLOW_ANTIGRAVITY_IDLE_TIMEOUT_SECS`) (ANTG-04, D-07)"
    - "The `--print-timeout 60m` quiet-gap override survives long operations without dying, closing the 41-UAT deferred negative control (ANTG-04, D-07)"
    - "Completing the supervised dogfood run unlocks `--mode auto` for Antigravity in `crates/devflow-cli/src/preflight.rs` (C2 preflight gate) (ANTG-04, D-07)"
    - "At least one live gate fires at Validate and the operator's decision is honored without silent advance"
  artifacts:
    - `crates/devflow-cli/src/preflight.rs` — updated `unattended_launch_shape_condition` allowing Antigravity in unattended mode
    - `.planning/phases/42-hermes-driver/42-VERIFICATION.md` — dogfood execution evidence and cadence measurement log
    - `.planning/phases/42-hermes-driver/42-UAT.md` — UAT sign-off including cadence verification and 60m print-timeout confirmation
---

<objective>
Execute the supervised Antigravity dogfood run (`devflow start --agent antigravity --phase 42 --mode supervise`), measure real event cadence against the 120s idle timeout floor, verify that `--print-timeout 60m` holds end-to-end, unlock `--mode auto` for Antigravity in `preflight.rs` (C2 gate), and document all evidence in `42-VERIFICATION.md` and `42-UAT.md`.

Purpose: Fulfill requirement ANTG-04 and decision D-07 to graduate Antigravity to fully supported unattended operation.
Output: Dogfood run execution, updated `preflight.rs`, and complete verification evidence.
</objective>

<execution_context>
@$HOME/.config/opencode/gsd-core/workflows/execute-plan.md
@$HOME/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/42-hermes-driver/42-CONTEXT.md
@.planning/phases/42-hermes-driver/42-RESEARCH.md
@crates/devflow-cli/src/preflight.rs
@crates/devflow-core/src/monitor.rs
@.planning/phases/40-pi-dogfood/40-02-PLAN.md
@.planning/phases/41-antigravity-driver/41-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dogfood preconditions</name>
  <files>.planning/phases/42-hermes-driver/42-CONTEXT.md</files>
  <read_first>
    crates/devflow-cli/src/preflight.rs
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `agy --version` exits 0 and reports Antigravity CLI
    - `hermes --version` exits 0 and reports Hermes Agent
    - `devflow doctor` reports both Antigravity and Hermes present
    - `cargo test -p devflow-core --lib hermes` exits 0
  </acceptance_criteria>
  <action>
    Verify all preconditions for launching the Phase 42 supervised Antigravity dogfood run:
    1. Verify `agy` binary is on PATH and functional.
    2. Verify `hermes` binary is on PATH.
    3. Run `devflow doctor` and confirm agent entries are green.
    4. Confirm 42-01 implementation is complete.
  </action>
  <verify>
    <automated>agy --version && hermes --version && echo "preconditions verified"</automated>
  </verify>
  <done>All dogfood launch preconditions are verified and satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Launch supervised Antigravity dogfood run and answer Validate gate</name>
  <files>.planning/phases/42-hermes-driver/42-VERIFICATION.md, .planning/phases/42-hermes-driver/42-UAT.md</files>
  <read_first>
    crates/devflow-core/src/monitor.rs
    .planning/phases/41-antigravity-driver/41-UAT.md
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - Supervised run `devflow start --agent antigravity --phase 42 --mode supervise` executes Define→Plan→Code→Validate
    - Event timestamps are captured and quiet-gap cadence is evaluated against the 120s idle timeout floor (`DEVFLOW_ANTIGRAVITY_IDLE_TIMEOUT_SECS`) (ANTG-04)
    - `--print-timeout 60m` quiet-gap override confirmed functional during long operations (closing 41-UAT test 12 deferred control)
    - Live Validate gate is answered by operator; pipeline advances only upon explicit gate decision
    - Evidence recorded in `.planning/phases/42-hermes-driver/42-VERIFICATION.md` and `42-UAT.md`
  </acceptance_criteria>
  <action>
    Execute `devflow start --agent antigravity --phase 42 --mode supervise` from the worktree.
    Monitor the run:
    1. Record quiet-gap distribution between stream events.
    2. Verify no false-alarm idle timeouts occurred.
    3. Answer the live Validate gate when prompted.
    4. Record execution evidence, timestamps, and cadence findings in `42-VERIFICATION.md` and `42-UAT.md`.
  </action>
  <verify>
    <automated>devflow status --phase 42 2>&1 | grep -qE "Validate|complete" && echo "dogfood reached Validate"</automated>
  </verify>
  <done>Supervised Antigravity run completed with cadence measured and gate honored.</done>
</task>

<task type="auto">
  <name>Task 3: Unlock unattended mode for Antigravity in preflight.rs</name>
  <files>crates/devflow-cli/src/preflight.rs</files>
  <read_first>
    crates/devflow-cli/src/preflight.rs
    .planning/phases/42-hermes-driver/42-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `unattended_launch_shape_condition` in `crates/devflow-cli/src/preflight.rs` permits `AgentKind::Antigravity` in unattended mode (`--mode auto`) following successful dogfooding (ANTG-04, D-07)
    - Unit tests in `preflight.rs` updated: `unattended_launch_shape_condition_antigravity_allowed` asserts `ConditionState::Holds` for Antigravity
    - `cargo test -p devflow --bin devflow unattended_launch_shape_condition_antigravity` exits 0
  </acceptance_criteria>
  <action>
    In `crates/devflow-cli/src/preflight.rs`:
    1. Update `unattended_launch_shape_condition(state: &State)` to include `AgentKind::Antigravity` alongside `AgentKind::Claude` when `stream_launch_enabled` holds and `!state.legacy_claude_launch`.
    2. Update unit tests to verify Antigravity is now accepted for unattended mode.
  </action>
  <verify>
    <automated>cargo test -p devflow --bin devflow unattended_launch_shape_condition_antigravity -- --nocapture</automated>
  </verify>
  <done>Antigravity unattended mode is unlocked in preflight and verified with unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Preflight C2 Gate → Headless Launch | Unlocking `--mode auto` allows unattended runs for Antigravity; requires proof of reliable event stream handling. |
| Operator → Validate Gate | Supervise mode ensures human verification before committing phase completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-42-07 | Denial of Service | Idle timeout misconfiguration | medium | mitigate | Measure quiet-gap distribution during supervised run; ensure 120s floor is sufficient before unlocking auto mode. |
| T-42-08 | Elevation of Privilege | Premature auto-mode unlock | high | mitigate | Gate unattended mode behind successful completion of the supervised dogfood run (ANTG-04). |
</threat_model>

<verification>
1. `devflow status --phase 42` shows successful completion through Validate.
2. `cargo test -p devflow --bin devflow unattended_launch_shape_condition_antigravity` passes.
3. `42-VERIFICATION.md` and `42-UAT.md` contain cadence measurements and operator gate attestation.
</verification>

<success_criteria>
Antigravity dogfood run succeeds in supervise mode; cadence distribution and 60m print-timeout verified; `--mode auto` unlocked for Antigravity in preflight.rs; evidence documented.
</success_criteria>

<output>
Create `.planning/phases/42-hermes-driver/42-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces
- `unattended_launch_shape_condition_antigravity_allowed` (test function in `crates/devflow-cli/src/preflight.rs`)
- `42-VERIFICATION.md` (evidence artifact in `.planning/phases/42-hermes-driver/42-VERIFICATION.md`)
- `42-UAT.md` (UAT artifact in `.planning/phases/42-hermes-driver/42-UAT.md`)
