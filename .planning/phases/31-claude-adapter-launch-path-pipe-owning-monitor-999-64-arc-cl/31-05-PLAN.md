---
phase: 31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl
plan: 05
type: execute
wave: 4
depends_on: ["31-04"]
files_modified:
  - .planning/phases/97-acceptance-two-plan-wave/97-01-PLAN.md
  - .planning/phases/97-acceptance-two-plan-wave/97-02-PLAN.md
  - .planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md
autonomous: false
requirements:
  - D-16
  - D-17
  - D-18
  - D-19
  - Constraint-7
estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The acceptance workload is a purpose-built two-plan wave, not a Phase 29 reconstruction"
    - "The two plans are both wave 1 with empty depends_on and disjoint files_modified, so the Code stage dispatches them concurrently"
    - "The orchestrator runs no git operation of any kind while the executor holds the working tree"
    - "The evidence file records what the run does NOT establish alongside what it does"
    - statement: "Both dispatched plans produce a SUMMARY.md and both merge — the exact inverse of the 999.64 failure, where both executors made real commits on orphaned branches, neither wrote a summary, and nothing merged"
      verification: backstop
    - statement: "The pass criterion is NOT 'the stage reports Success' — the completion oracle already scored the orphaned Phase 29 stage as Success — and NOT 'both completions observed in the stream', because constraint 7 makes an observed count the very signal that can undercount"
      verification: backstop
    - statement: "The declared-token startup canary confirms against the real Claude CLI, not an injected launcher"
      verification: backstop
  artifacts:
    - path: ".planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md"
      provides: "The live run's evidence: route taken, timings, per-plan SUMMARY and merge status, canary outcome, and what the run does not establish"
      contains: "Pass criterion"
  key_links:
    - from: ".planning/phases/97-acceptance-two-plan-wave/97-01-PLAN.md"
      to: ".planning/phases/97-acceptance-two-plan-wave/97-02-PLAN.md"
      via: "both at wave 1 with empty depends_on and disjoint files_modified, forcing concurrent dispatch"
      pattern: "wave: 1"
---

<objective>
Run the phase's acceptance criterion for real: a DevFlow-driven Code stage dispatches two plans in
one wave through the pipe-owning monitor, and both come back with a summary and a merge.

Purpose: this phase's automated tests prove plumbing. Review constraint H4 states plainly that the
acceptance criterion is **not substitutable by integration tests** — a mocked CLI validates
plumbing, not the delivery premise. This project has already shipped green tests over a broken
feature more than once, and the completion oracle already scored the orphaned Phase 29 stage as
Success. The live run is the gate that catches that class, and per D-19 a failing run means the
phase does not close whatever the unit tests say.

Output: a purpose-built two-plan wave, one live run on the main checkout, and an evidence file
that states what the run establishes and what it does not.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-CONTEXT.md
@.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-01-SUMMARY.md
@.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-02-SUMMARY.md
@.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-03-SUMMARY.md
@.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-04-SUMMARY.md
@CLAUDE.md

All four prior summaries are referenced because this plan verifies their combined behaviour in
production and its evidence file must name what each contributed.
`.planning/ROADMAP.md` and `.planning/STATE.md` are deliberately not referenced — see the note in
`31-01-PLAN.md`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the two-plan wave and take the baseline</name>
  <precondition>The `devflow` binary that will be launched was built from this branch's current HEAD — `cargo build --release` (or the build this project's launcher uses) has been run since the last commit on `feature/phase-31`. A stale binary silently exercises the pre-31 launch path and the whole run measures nothing.</precondition>
  <files>.planning/phases/97-acceptance-two-plan-wave/97-01-PLAN.md, .planning/phases/97-acceptance-two-plan-wave/97-02-PLAN.md</files>
  <read_first>
    - `.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-01-PLAN.md` — this file's own frontmatter, as the shape a PLAN.md must have for `/gsd-execute-phase` to consume it.
    - `CLAUDE.md` — the whole file. Two rules bind this plan directly: never run git operations while an executor holds the working tree, and `cargo test --exact <name>` exits 0 when the name matches nothing.
    - `crates/devflow-cli/src/main.rs` lines 46-140 — `Start` and `Resume`'s real argument sets, so the run's invocation is written against what the binary actually accepts rather than from memory.
    - `crates/devflow-cli/src/commands.rs` — `start`'s branch and worktree handling, to determine which route (below) leaves the current branch intact.
  </read_first>
  <action>
Create `.planning/phases/97-acceptance-two-plan-wave/` containing exactly two plan files,
`97-01-PLAN.md` and `97-02-PLAN.md`.

The workload is D-16's "cheapest workload that still crosses every seam under test", not a Phase
29 reconstruction — that is expensive and its plans no longer exist in runnable form. Every seam
under test must be crossed: two plans dispatched concurrently from one wave, real agents, real
background completion, real drain. Nothing more.

Both plans must carry `wave: 1`, `depends_on: []`, `autonomous: true`, and **disjoint**
`files_modified`. The disjointness is what makes `/gsd-execute-phase` dispatch them concurrently
rather than serially; a file overlap would silently serialize the run and the acceptance criterion
would measure nothing. Each plan has exactly one task: create one distinct file under a scratch
directory with a fixed, plan-specific string as its content, and commit it. Nothing that reads the
codebase, nothing that runs a build, nothing that could fail for a reason unrelated to delivery.

Give each plan's single task a `<verify><automated>` that checks its own file exists with its own
content, and a `<done>` naming its own SUMMARY.md. Each must produce
`.planning/phases/97-acceptance-two-plan-wave/97-01-SUMMARY.md` and `97-02-SUMMARY.md`
respectively — those two files are half of D-18's pass criterion and must be named explicitly in
the plans rather than left to convention.

Take the baseline **before** anything is launched, and record all of it in the scratch notes that
task 2 will fold into the evidence file:
  - `git rev-parse HEAD` and `git rev-parse --abbrev-ref HEAD`
  - `git status --porcelain` (must be clean; a dirty tree makes the merge evidence unreadable)
  - `sha256sum .planning/STATE.md .planning/ROADMAP.md` — the acceptance run drives a real GSD
    skill and may write to either; the run must be able to prove afterwards whether it did
  - `claude --version` — the arc's premise was witnessed on `2.1.220`; if this differs, the run's
    result is still valid but the evidence file must say which version it was witnessed on
  - the `devflow` binary's own path and `devflow --version`, plus the mtime of the built binary
    against the timestamp of `git log -1 --format=%cI`, as the check that the binary is not stale

Decide and record the run route. Two are acceptable; the choice changes what the evidence means,
so it must be recorded, not left implicit.

  **Route A (preferred) — seed and resume.** Write `.devflow/state-97.json` with `stage: "Code"`,
  `phase: 97`, `agent: "claude"`, `mode: "auto"`, the project root, and the same field set
  `State` serializes, using an atomic temp-plus-rename write. Then `devflow resume --phase 97`.
  `resume` explicitly does not create a new branch or worktree, so the current branch survives and
  the two purpose-built plans stay visible in the tree. **Known hazard, verify it:** `devflow
  resume` clears `stop_until` unconditionally, so an unfired `--until` cap is silently lost. Do
  not rely on a cap to stop this run; rely on `yes_ship` being false and on stopping the run
  yourself once the Code stage's evidence is captured.

  **Route B (fallback) — start.** `devflow start --phase 97 --agent claude --mode auto
  --no-worktree --until code`. `--no-worktree` satisfies D-17 (a worktree adds an uncontrolled
  variable to the process behaviour being measured, and a worktree's `.git` file points at a host
  path the pre-push container cannot see, which already breaks the gate here). **Known hazard,
  verify before choosing this route:** `start` creates and checks out `feature/phase-97`. Confirm
  what it branches from; if it branches from `develop`, the two purpose-built plans committed on
  this branch will not be present in the working tree and the run will have nothing to dispatch.
  Only take route B after establishing that the workload is visible on the branch the run ends up
  on.

Record which route was chosen and the concrete reason. If route A is blocked, say what blocked it.
  </action>
  <verify>
    <automated>test -f .planning/phases/97-acceptance-two-plan-wave/97-01-PLAN.md && test -f .planning/phases/97-acceptance-two-plan-wave/97-02-PLAN.md && echo BOTH_PLANS_PRESENT</automated>
    <automated>node $HOME/.claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/97-acceptance-two-plan-wave/97-01-PLAN.md; node $HOME/.claude/gsd-core/bin/gsd-tools.cjs query verify.plan-structure .planning/phases/97-acceptance-two-plan-wave/97-02-PLAN.md</automated>
  </verify>
  <acceptance_criteria>
    - Both plan files exist and `verify.plan-structure` reports `valid: true` for each.
    - Both carry `wave: 1` and `depends_on: []`: `rg -c 'wave: 1' .planning/phases/97-acceptance-two-plan-wave/97-01-PLAN.md` returns 1, and the same for `97-02`.
    - The two `files_modified` lists share no entry. Compute the intersection explicitly and record it as empty in the notes — do not assert it by eye.
    - `git status --porcelain` produced no output at baseline. Record the exact command output (empty) rather than a claim.
    - The `devflow` binary's mtime is later than `git log -1 --format=%cI` on the current branch. Record both values. A stale binary is the documented way this project has previously "verified" a fix that was never running.
    - `claude --version` output is recorded verbatim.
    - The chosen route (A or B) and its reason are recorded.
  </acceptance_criteria>
  <done>Two purpose-built plans exist that `/gsd-execute-phase` will dispatch concurrently, the pre-run baseline is captured with real command output rather than assertions, and the run route is chosen and justified.</done>
</task>

<task type="auto">
  <name>Task 2: Run it live, hands off git, and write down what happened</name>
  <precondition>Task 1's baseline recorded `git status --porcelain` as empty and confirmed the `devflow` binary is newer than the branch HEAD commit.</precondition>
  <files>.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md</files>
  <read_first>
    - `CLAUDE.md` § "Never run git operations while an executor holds the working tree" — the whole section, including both 2026-08-02 failures it records. This rule binds this task more directly than any other in the phase.
    - The task-1 baseline notes.
    - `crates/devflow-core/src/events.rs` — `emit`'s output shape, so `.devflow/events.jsonl` can be read as evidence.
  </read_first>
  <action>
Launch the run using the route task 1 chose, then **touch no git at all** until the executor
reports. No `add`, no `commit`, no `push`, no `checkout`, no branch or tag operation, no
`git status` that could race a mid-flight tree — nothing. `CLAUDE.md` records two separate
failures from ignoring this on 2026-08-02: a mid-run `git push` failed the pre-push gate because
`build_provenance` copies every tracked file and the executor had momentarily deleted one, and a
blanket `git add -A` swept an executor's in-progress evidence into an unrelated commit. Neither was
a real defect. Worktree-isolated executors do not have this problem; this run is deliberately on
the main checkout (D-17), which is exactly the case the rule covers.

While the run is in flight, observe it only through read-only channels that do not touch the git
index: tail `.devflow/events.jsonl`, tail `.devflow/phase-97-stdout`, tail
`.devflow/phase-97-monitor.log`, and poll for the two SUMMARY files. Do not run `devflow advance`
to "check on it" — `devflow advance` blocks on a pending gate and leaks a process pair.

Capture, into `31-ACCEPTANCE.md`:

  - **Route, invocation, and timings.** The exact command run, wall-clock start and end, and the
    interval between the last `background_tasks_changed` drain and the final top-level `result`
    event as read from the capture. Report the interval that actually varied; if any duration is
    dominated by a fixed timeout, say so and report the timeout instead of pretending the number
    measured something else.
  - **The canary's real-CLI outcome.** Which `claude_delivery_canary_*` event appears in
    `.devflow/events.jsonl` and the CLI version recorded alongside it. This is the first time D-13's guard runs against the real CLI rather
    than an injected launcher.
  - **D-18's pass criterion, both halves, per plan.** For each of the two plans: does its
    SUMMARY.md exist, and are its commits reachable from the branch the run ended on? Use
    `test -f` for the first and `git merge-base --is-ancestor <plan commit> <branch>` or
    `git log --oneline <branch> | rg '<the plan-specific marker string>'` for the second — after
    the executor has finished, never during.
  - **What the run does NOT establish.** Explicitly: it does not establish that the stage reported
    Success (that is not the criterion — the completion oracle already scored the orphaned Phase 29
    stage as Success), it does not establish a count of observed completions (constraint 7 makes an
    observed count the very signal that can undercount, and a coalesced pair is superficially
    indistinguishable from one delivered and one lost), and one passing run is a weak bound on
    reliability rather than a guarantee. State roughly what a single success does support and what
    more would be needed for a reliability claim.
  - **Whether `.planning/STATE.md` or `.planning/ROADMAP.md` changed.** Re-run the `sha256sum` from
    task 1's baseline and compare. If either changed, restore it from git — *after* the executor
    has reported, never during — and record what changed and that it was restored.

Then clean up, still only after the executor has reported: remove
`.planning/phases/97-acceptance-two-plan-wave/` and any scratch files the two plans created, and
remove `.devflow/state-97.json` and the `phase-97-*` capture files. `31-ACCEPTANCE.md` is the
surviving record; it must reproduce enough of the removed workload (both plans' frontmatter and
their single task each) that a reader can tell what was actually run without the directory.

If the run fails, do not retry blind. D-19 makes a failing run mean the phase does not close.
Diagnose from the capture and the event ledger, record the diagnosis in `31-ACCEPTANCE.md`, and
re-run only when the diagnosis names a specific cause and a specific change. Record every attempt,
not only the last one — a run that passed on the third try is different evidence from one that
passed on the first, and the difference is the operator's to weigh.
  </action>
  <verify>
    <automated>test -f .planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md && rg -c 'Pass criterion' .planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md</automated>
    <automated>rg -c 'does not establish|What this does not' .planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md</automated>
  </verify>
  <acceptance_criteria>
    - `31-ACCEPTANCE.md` exists and contains a per-plan table with two rows, each recording SUMMARY-exists (yes/no) and merged (yes/no) with the command output that established it.
    - The file contains a section naming what the run does not establish, covering all three items in the action: not stage-status, not an observed completion count, and the weakness of a single-run bound.
    - The file records which `claude_delivery_canary_*` event appeared, quoting the line from `.devflow/events.jsonl`.
    - The file records every attempt, with the diagnosis for any that failed.
    - `git status --porcelain` after cleanup shows only the intended additions to the Phase 31 directory — the scratch phase directory and the `phase-97-*` capture files are gone. Record the output.
    - The `sha256sum` comparison for `.planning/STATE.md` and `.planning/ROADMAP.md` is recorded, with either "unchanged" or "changed and restored, here is what changed".
    - No git command was run between launch and the executor's report. State this explicitly in the summary, and if it was violated, say so — a violated rule that is recorded is recoverable; one that is not is the failure `CLAUDE.md` documents.
  </acceptance_criteria>
  <done>The live two-plan wave has run on the main checkout with the orchestrator touching no git during it, and `31-ACCEPTANCE.md` records the route, the timings, the canary's real-CLI outcome, both halves of D-18's criterion per plan, every attempt, and what the run does not establish.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Adjudicate the 999.64 arc gate</name>
  <what-built>
Phase 31 replaced the detached `sh` monitor with a pipe-owning Rust monitor for the Claude
adapter, flipped the adapter to always-on `stream-json` with the prompt delivered over stdin,
added a first-class idle-timeout verdict that cannot be shadowed by a stale stream `result`, added
a once-per-run declared-token delivery canary that refuses to run when the premise is gone, and
stopped a stream-derived success from outranking a contradicting exit code.

Task 2 then ran the acceptance criterion live: a purpose-built two-plan wave dispatched
concurrently from the Code stage on the main checkout, through the new monitor, against the real
Claude CLI. Its evidence is in `31-ACCEPTANCE.md`.
  </what-built>
  <how-to-verify>
1. Read `.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-ACCEPTANCE.md`
   end to end, starting with its "what this does not establish" section rather than its result.
2. Confirm D-18's criterion on its own terms: **both** plans produced a SUMMARY.md **and** both
   merged. Do not accept "the stage reported Success" as a substitute — the completion oracle
   already scored the orphaned Phase 29 stage as Success. Do not accept "both completions were
   observed in the stream" either — constraint 7 makes an observed count the very signal that can
   undercount.
3. Check the attempt log. A pass on the first attempt and a pass on the third are different
   evidence; decide which you were shown.
4. Check the recorded `claude --version` against `2.1.220`, the version the arc's delivery premise
   was witnessed on, and the canary event that was emitted.
5. Decide whether the phase closes. D-19: if the acceptance run failed, the phase does not close —
   diagnose and re-run rather than closing PARTIAL.

Two open follow-ons are surfaced here rather than decided:
  - `STREAM_JSON_STAGES` still lists only `Stage::Code` (D-09/D-10 sequencing). Widening it to the
    other stages is a deliberate later change gated on real production captures. It is not done.
  - Per-child declared tokens (D-14) remain deferred on size. They would defeat constraint 7's
    coalescing undercount by construction instead of leaning on the drain gate.
  </how-to-verify>
  <resume-signal>Reply "closes" to close the phase, "does not close" with what you saw, or name what you want re-run.</resume-signal>
</task>

</tasks>

<artifacts_this_phase_produces>
Introduced by this plan (31-05):

| Symbol / path | Kind | Location |
|---|---|---|
| `31-ACCEPTANCE.md` | evidence file (surviving) | `.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/` |
| `.planning/phases/97-acceptance-two-plan-wave/` | scratch workload (removed after the run) | `.planning/phases/` |

This plan produces no source symbols. The full inventory of what Phase 31 adds to the codebase:
`MonitorLaunch`, `run_pipe_owning_monitor`, `user_turn_line`, `CloseRule`,
`spawn_monitor`'s new `launch` parameter, `ClaudeAgent::exec_command_single_document`,
`prompt_path` (`.devflow/phase-NN-prompt`), `monitor_log_path` (`.devflow/phase-NN-monitor.log`),
`token_reported_in_capture`, `devflow __monitor`, `claude_stream_launch_enabled`,
`STREAM_JSON_STAGES` — 31-01; `AgentStatus::IdleTimeout` (wire form `idle_timeout`),
`idle_timeout_path` (`.devflow/phase-NN-idle-timeout`), `parse_idle_timeout_side_channel`,
`DEVFLOW_CLAUDE_IDLE_TIMEOUT_SECS`, `IDLE_TIMEOUT_FLOOR_SECS`, `parse_idle_timeout_secs`,
`idle_timeout_setting`, `IdleTimeoutSetting` — 31-02; `crates/devflow-core/src/canary.rs`,
`declare_token`, `CanaryOutcome`, `CanaryLauncher`, `ClaudeCanaryLauncher`, `run_delivery_canary`,
`State::canary`, the `claude_delivery_canary_*` events — 31-03;
`reconcile_stream_success_against_exit_code`, `--legacy-claude-launch`,
`DEVFLOW_CLAUDE_LEGACY_LAUNCH`, `State::legacy_claude_launch`, the
`claude_legacy_launch_forced` event — 31-04.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Acceptance-run agent → the main checkout's working tree | A real agent writes and commits in the same tree the orchestrator occupies |
| Acceptance-run GSD skill → `.planning/STATE.md` and `.planning/ROADMAP.md` | A scratch run can mutate the project's real planning state |
| Run evidence → the phase-close decision | A single run's report is what the operator's close decision rests on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-31-19 | Repudiation | the pass criterion | high | mitigate | The criterion is SUMMARY-exists plus merged, per plan, evidenced by real command output. Accepting "the stage reported Success" would repeat the exact 999.64 miss, in which the oracle scored an orphaned stage as Success while both executors' work sat on branches nobody merged. Accepting an observed completion count would use the signal constraint 7 shows the CLI destroys by coalescing. |
| T-31-20 | Tampering | main-checkout git during the run | high | mitigate | The orchestrator runs no git operation between launch and the executor's report. `CLAUDE.md` records two real 2026-08-02 failures from breaking this: a mid-run push that failed the pre-push gate on a momentarily-absent tracked file, and a blanket `git add -A` that swept an executor's in-progress evidence into an unrelated commit. |
| T-31-21 | Tampering | scratch phase 97 vs. real planning state | medium | mitigate | `.planning/STATE.md` and `.planning/ROADMAP.md` are hashed before the run and re-hashed after; any change is restored from git after the executor reports, and the change is recorded rather than silently reverted. The scratch phase directory and `phase-97-*` captures are removed, with `31-ACCEPTANCE.md` reproducing enough of the workload to stay readable. |
| T-31-22 | Spoofing | a stale `devflow` binary | high | mitigate | A binary built before this phase's commits silently exercises the pre-31 launch path, so the run measures nothing while reporting a pass. The task-1 precondition and acceptance criteria compare the binary's mtime against the branch HEAD commit timestamp and record both. This project has previously "verified" a fix that was never running, for exactly this reason. |
| T-31-23 | Repudiation | single-run evidence | medium | accept | One passing run is a weak reliability bound. Accepted deliberately, and mitigated by disclosure rather than by more runs: `31-ACCEPTANCE.md` must state what a single success supports and what more would be needed, and the attempt log distinguishes a first-try pass from a third-try one. |
| T-31-SC | Tampering | package-manager installs | low | accept | No packages are installed by this plan. `31-RESEARCH.md` § Package Legitimacy Audit records zero new dependencies for the phase. |
</threat_model>

<verification>
1. `31-ACCEPTANCE.md` exists, carries the per-plan pass table, the attempt log, the canary event
   line, and the "what this does not establish" section.
2. The scratch phase directory and `phase-97-*` capture files are gone; `git status --porcelain`
   output is recorded.
3. `.planning/STATE.md` and `.planning/ROADMAP.md` hashes are compared against the baseline and
   the outcome is recorded either way.
4. `cargo test --workspace` and `scripts/check.sh all` are still green after the run — capture
   each command's own exit code, not a pipeline's.
5. The human checkpoint is answered.
</verification>

<success_criteria>
- A live two-plan wave ran on the main checkout through the pipe-owning monitor against the real
  Claude CLI.
- Both plans produced a SUMMARY.md and both merged, evidenced by real command output.
- The orchestrator touched no git while the executor held the tree.
- The evidence file states what the run does not establish as prominently as what it does.
- The operator has adjudicated whether the phase closes, per D-19.
</success_criteria>

<output>
Create `.planning/phases/31-claude-adapter-launch-path-pipe-owning-monitor-999-64-arc-cl/31-05-SUMMARY.md` when done.
</output>
