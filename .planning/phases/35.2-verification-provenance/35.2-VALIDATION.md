---
phase: 35.2
slug: verification-provenance
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-12
---

# Phase 35.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust) |
| **Config file** | none — existing workspace `Cargo.toml` covers both crates |
| **Quick run command** | `cargo test -p devflow --bin devflow verification_authored_this_run` (or the specific new test name once authored) |
| **Full suite command** | `cargo test -p devflow --bin devflow` (the `devflow` package is binary-only — no `--lib` target; `-p devflow-core --lib` for `State`/`phase_id` changes) |
| **Estimated runtime** | ~30-60 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p devflow --bin devflow <narrow filter>` (or `-p devflow-core --lib <filter>` for State/phase_id-only changes)
- **After every plan wave:** Run the full suite commands above for both crates
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 35.2-01-01 | 01 | 1 | HARDEN-03 | — | N/A | unit | `cargo test -p devflow-core --lib state::` | ✅ | ⬜ pending |
| 35.2-01-02 | 01 | 1 | HARDEN-03 | — | N/A | unit | `cargo test -p devflow --bin devflow verification_authored_this_run` | ✅ | ⬜ pending |
| 35.2-01-03 | 01 | 1 | HARDEN-03 | — | N/A | unit | `cargo test -p devflow-core --lib phase_id::` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Existing infrastructure covers all phase requirements — `cargo test` is already installed and the target functions (`verification_authored_this_run`, `select_loop_back_fix`, `State`, `PhaseId`) already have passing regression tests to extend.*

---

## Manual-Only Verifications

*If none: "All phase behaviors have automated verification."*

All phase behaviors have automated verification — this phase is a pure backend/state-logic change with no UI or manual-only surface.

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
