---
phase: 30-withdraw-devflow-from-the-release-business
plan: 04
type: execute
wave: 4
depends_on: ["30-01", "30-02", "30-03"]
files_modified:
  - ARCHITECTURE.md
  - CONTRIBUTING.md
  - OPERATIONS.md
  - README.md
  - docs/index.md
  - docs/architecture/git-ship.md
  - docs/diagrams/ship-flow.md
  - docs/guides/configuration.md
autonomous: true
requirements: ["30c"]

must_haves:
  truths:
    - "CONTRIBUTING.md is the sole release authority, and says so."
    - "No document in the repository — including the published docs site under `docs/` — describes DevFlow as merging, tagging, bumping a version, appending a changelog, or deleting a branch."
    - "The sweep that proves it searches behavioural PHRASES as well as symbol spellings, and covers Rust doc comments rather than excluding them."
    - "ARCHITECTURE.md's Ship model matches the code: gate-driven, terminating at an open PR, with no post-approval hook batch."
    - "CHANGELOG.md maintenance is documented as the operator's, with no dangling reference to an automated appender."
  artifacts:
    - "ARCHITECTURE.md — hook table, Ship lifecycle and git-module descriptions matching the post-Phase-30 code"
    - "CONTRIBUTING.md — an explicit statement of where DevFlow's mandate ends and the operator's release begins"
  key_links:
    - "CONTRIBUTING.md § Cutting a Release -> the only place release steps are described"
    - "ARCHITECTURE.md § Git and ship model -> crates/devflow-core/src/git.rs's actual surviving surface"
    - "OPERATIONS.md § devflow release --check -> commands.rs::release_check's actual three checks"
---

<objective>
Make the documentation say what the code now does, and establish CONTRIBUTING.md as the
single place the release is described.

This is unit 30c. Its non-regression case is already verified by the adversarial review:
zero hook-authored changelog commits exist in all of this repository's history — the
entries were stranded on an unpushable local `develop` — and CONTRIBUTING step 1 already
has the operator hand-writing `CHANGELOG.md`. Removing the automated appender therefore
regresses nothing real. The documentation work is to stop describing a capability that
never actually functioned and no longer exists.

Purpose: stale documentation is how a withdrawn capability gets rebuilt. ARCHITECTURE.md
currently describes a hook table with five entries, a Validate → Ship batch that appends
the changelog, and library primitives that cut release branches — three claims that were
already partly stale before this phase and are now entirely false.

Output: four documents that match the code, and one sweep proving nothing was missed.

**Why this plan is in wave 4 — a content dependency, not a file conflict.** Every other
adjacent pair in this phase is serialised by shared file ownership. This plan is not:
it touches only markdown, and plan 30-03 touches only Rust. It is sequenced last because
its entire job is to describe the code's FINAL state, and that state does not exist until
30-01, 30-02 and 30-03 have all landed — including the planner-identified extensions each
of them is required to flag in its SUMMARY, which this plan reads rather than predicts.
Running it earlier would document an intermediate state and produce a sweep that passed
against code still holding references. Do not "optimise" it into an earlier wave on the
grounds that the file sets are disjoint; they are, and it is still wrong.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-withdraw-devflow-from-the-release-business/30-01-SUMMARY.md
@.planning/phases/30-withdraw-devflow-from-the-release-business/30-02-SUMMARY.md
@.planning/phases/30-withdraw-devflow-from-the-release-business/30-03-SUMMARY.md

Read all three prior SUMMARYs first — they are the authoritative record of exactly what
was removed, including the planner-identified extensions each one flagged. Write the
documentation from the code's current state, not from this plan's expectations.

Document ranges to read:

- `ARCHITECTURE.md` — the Hooks section :55-86 (the five-row hook table is :62-68; the
  transition-batch paragraph :70-78 already misdescribes the current code; the
  branch-creation-variant note :80-86), the Git and ship model section :262-300 (the `GitFlow`
  primitive list :266-273 and the numbered Ship lifecycle :283-300), and the logging
  conventions entries for `git.rs` and `hooks.rs` :345-360.
- `CONTRIBUTING.md` — § Cutting a Release :236-322 (already operator-driven and correct;
  step 1 :243-248 already has the operator hand-writing the changelog), § Release
  signing :52-88, § Repository git policy :39-51.
- `OPERATIONS.md` — the `devflow release --check` row :48 and the `devflow evidence` row
  :50 (the evidence row was already rewritten in 30-01; verify it, do not redo it).
- `README.md` — check for any claim about what DevFlow does at or after Ship.
- The **published docs site** under `docs/` (mkdocs, `docs/mkdocs.yml`). Four pages teach
  the withdrawn model today and are the most publicly visible stale surface in the repo:
  `docs/index.md:13` (a flowchart whose Ship branch points at a two-release-action node),
  `docs/architecture/git-ship.md:15-26` (a lifecycle block listing merge, compute version
  and tag, and branch cleanup), `docs/diagrams/ship-flow.md:16-28` (a sequence diagram
  with the same three steps), and `docs/guides/configuration.md:37` (an environment-
  variable table row for the foreground gate-timeout knob deleted in 30-01). The other
  eight pages under `docs/` were scanned and are clean — verify that yourself rather than
  taking it on trust, since 30-01 through 30-03 may have moved the line.
- `site/` is generated mkdocs output and is gitignored — do not edit it, and do not let
  it appear in the sweep.

Note: `crates/devflow-core/src/doc_check.rs` enforces docs-to-source parity for CLI flags
against `doc-check-allowlist.toml`. No CLI flag is removed by this phase, so no allowlist
entry should be needed — but run the workspace suite and treat a doc-check failure as a
real finding, not a formality.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ARCHITECTURE.md to the code that now exists</name>
  <files>ARCHITECTURE.md</files>
  <action>
Rewrite the three sections that describe removed capability. Verify each claim against
the current source before writing it — several statements in this file were already stale
before Phase 30 and must not be carried forward in edited form.

Hooks section :55-86:
- Reduce the hook table to the variants that survive in `crates/devflow-core/src/hooks.rs`.
- Rewrite the paragraph at :70-78. It describes a Validate → Ship batch of two hooks and
  a post-approval batch, both wrong now. State the actual mapping and state plainly that
  there is no post-Ship batch. Keep the two properties that are still true and load-
  bearing: both hook dispatch paths run under the project-wide checkout lock, and hook
  failures are fail-soft.
- Delete the note at :80-86 about the branch-creation hook variant. That variant is gone
  from the enum along with the rest of the class; branch and worktree
  creation for a phase happens directly in `start()`, which the surrounding text already
  covers.
- Add one sentence stating the rule the hook system now obeys: hooks produce content, and
  no hook mutates git history beyond committing on the branch it is already on.

Git and ship model :262-300:
- Rewrite the `GitFlow` primitive list :266-273 from the type's actual surviving methods.
  Delete the sentence about release-branch primitives existing but being uncalled — that
  sentence is now a description of something that was deleted precisely because it said
  that about itself.
- Rewrite the numbered Ship lifecycle :283-300. Step 1's hook batch is wrong. Step 2's
  quoted gate text changed in 30-01 — quote the new literal, verified against the source,
  not from memory. Step 3's description of what approval runs is wrong: approval now
  performs gate cleanup, state clearing, registry deregistration and the two event
  emissions, and nothing else. Steps 4 and the never-silent framing are unchanged.
- Add an explicit statement of the mandate boundary at the head of this section: DevFlow
  drives Define through Ship, and its mandate ends when a completed phase run exists
  through commit with a pull request open. Merging, versioning, tagging, changelog
  authorship and publishing are the operator's, and are described in CONTRIBUTING.md.

Logging conventions :345-360:
- The `git.rs` entry names removed operations. The `hooks.rs` entry gives a fail-soft
  example that no longer exists. Correct both against the current source.

Do not editorialise about Phases 26 and 29 here. ARCHITECTURE.md documents the system as
it is; the history and the reasoning live in the ROADMAP entry.
  </action>
  <verify>
    <automated>cd /var/home/denniyahh/Github/devflow; cargo test --workspace > /tmp/t30-ws.log 2>&amp;1; w=$?; tail -30 /tmp/t30-ws.log; test $w -eq 0</automated>
  </verify>
  <done>
Every hook name, `GitFlow` method name and quoted operator-facing string in
ARCHITECTURE.md resolves to a symbol or literal that exists in the current source. The
Ship lifecycle's step 3 describes gate cleanup, state clearing, deregistration and the
two event emissions. The mandate-boundary statement is present. The workspace suite,
including the doc-check parity tests, is green.

The verify captures cargo's exit status before `tail`. Do not write it as
`cargo test --workspace | tail -N`: that returns tail's status, so the gate would pass
unconditionally — which for a docs task with no other automated check would mean the task
had no gate at all.
  </done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Make CONTRIBUTING.md the sole release authority, and correct the published docs site</name>
  <files>CONTRIBUTING.md, OPERATIONS.md, README.md, docs/index.md, docs/architecture/git-ship.md, docs/diagrams/ship-flow.md, docs/guides/configuration.md</files>
  <action>
CONTRIBUTING.md § Cutting a Release is already accurate, complete and operator-driven —
seven steps through a protected-branch PR flow, with the version bumped in two places by
hand in step 1 and the changelog written by hand alongside it. Do not rewrite working
instructions. What is missing is the statement that this is the ONLY such description.

- Add a short lead paragraph to § Cutting a Release establishing the boundary in both
  directions: DevFlow drives a phase to a reviewable pull request and performs no git
  mutation after Ship; every step in this section is performed by a human. Reference
  Phase 30 by name once so a reader tracing the change has an anchor, and say plainly
  that any tooling proposing to automate a step here is reopening a question this project
  has already answered twice.
- Re-read all seven steps against the current code before touching them. Step 1's
  reference to `crates/devflow-cli/tests/workspace_version_pin.rs` is still correct — that
  guard survives, with framing rewritten in 30-03. Verify the reference still matches the
  test's new prose. Change nothing else in steps 1-7 unless it is factually wrong now.
- § Release signing and § Repository git policy are unaffected by this phase and stay as
  they are.
- Check § Development and § Testing notes for any instruction that depends on something
  deleted in 30-01 through 30-03.

In OPERATIONS.md: the `devflow release --check` row at :48 describes four checks
including signature-viability prediction, and points a bare `devflow release` at a
deferred executor that would perform the release cut. Rewrite it to the three surviving
checks and to the limits statement 30-03 added, and remove the deferred-executor framing
entirely — the deferral became a withdrawal. Verify the `devflow evidence` row at :50 already carries 30-01's rewrite; if
it does not, that is a gap in 30-01 and must be reported, not silently patched over.

In README.md: read it for any claim about what DevFlow does at or after Ship and correct
what is now false. If nothing is false, say so in the SUMMARY rather than leaving a
reader to wonder whether it was checked.

**In the published docs site under `docs/` (H6) — the most publicly visible stale
surface, and the one no automated gate in this repository covers.** These four pages
teach the withdrawn model to anyone reading the site:

- `docs/index.md:13` — the landing flowchart's `approved` branch from Ship points at a
  node naming two release actions DevFlow no longer performs. Retarget it at what
  actually happens: an open PR the operator reviews. This is the first diagram a new
  reader sees.
- `docs/architecture/git-ship.md:15-26` — the Feature Lifecycle block lists Ship approval
  as merging into develop with `--no-ff`, deriving a version and creating the release
  ref, then a best-effort branch deletion. Rewrite it to the surviving sequence and add
  the mandate boundary, matching ARCHITECTURE.md's wording from Task 1 so the two cannot
  drift.
- `docs/diagrams/ship-flow.md:16-28` — the Mermaid sequence diagram sends those same
  three actions as messages to the git actor. Remove all three. Check the diagram's
  "Terminal Guarantees" section immediately below for claims that depended on them.
- `docs/guides/configuration.md:37` — an environment-variable table row documenting the
  foreground gate-timeout knob deleted in 30-01. Delete the row. An operator setting a
  knob that no longer exists gets silence, not an error.

Then read the remaining eight pages under `docs/` and confirm they are clean. Record in
the SUMMARY which pages you changed and that you checked the rest — "the four named
pages" is this plan's expectation, not a finding.

Do not touch `site/`: it is generated mkdocs output and gitignored.
  </action>
  <verify>
    <automated>cd /var/home/denniyahh/Github/devflow; cargo test --workspace > /tmp/t30-ws.log 2>&amp;1; w=$?; cargo run -q -p devflow -- release --check . > /tmp/t30-rcout.log 2>&amp;1; tail -30 /tmp/t30-ws.log; cat /tmp/t30-rcout.log; test $w -eq 0 &amp;&amp; rg -q 'CONTRIBUTING' /tmp/t30-rcout.log</automated>
  </verify>
  <done>
§ Cutting a Release opens with the mandate-boundary statement and its seven steps are
verified accurate. OPERATIONS.md's `release --check` row names three checks and no
deferred executor, and its wording matches what the command actually prints. README.md is
either corrected or confirmed clean, with the outcome recorded either way. All four named
docs-site pages are corrected and the remaining eight are confirmed clean, with both
recorded.

As in 30-03 Task 3, the live `release --check` run is asserted on its output and not its
exit status: run from a phase feature branch it legitimately exits non-zero on the
divergence check. Cargo's own status IS gated, captured before `tail`.
  </done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Sweep the repository for dangling references and close the phase</name>
  <files>ARCHITECTURE.md, OPERATIONS.md, README.md, CONTRIBUTING.md</files>
  <action>
Prove nothing was missed, rather than asserting it.

There are TWO sweep commands in this task's verify block, and they catch different things.
An earlier draft had only the first, restricted in two ways that made it unable to find
most of what this phase actually left behind (H6).

**Sweep A — symbol spellings, across every tracked file.** Excludes only `CHANGELOG.md`
(a historical record, which must NOT be rewritten), `.planning/` (planning artifacts,
which record the removal deliberately), `target/` and the gitignored `site/`. Note what
it no longer excludes: `*.rs`. The earlier draft skipped all Rust files, which meant a
stale rustdoc block naming a deleted symbol — of which this phase creates several — was
invisible to it. After this phase every one of these symbols should be absent from Rust
source too, so including `.rs` makes the sweep strictly stronger and costs nothing.

**Sweep B — behavioural phrases, across prose.** Symbol names are the easy half. The
docs site never named a symbol; it described the actions in plain English, which no
symbol grep can reach. The exact phrase list lives in Sweep B's own command below — read
it there rather than duplicating it here. Each phrase was checked against the tree while
this plan was written: every current hit is in a file this phase now owns, and none is
legitimate surviving text, so zero hits is the correct gate rather than an aspiration.

For each hit outside the exclusions, decide and act:
- a description of current behaviour → fix it;
- a historical statement clearly scoped to the past → leave it and note it;
- a `.planning/` or `CHANGELOG.md` hit → leave it, always.

Two specific things to confirm rather than assume:

1. CHANGELOG.md maintenance must not be silently regressed. Confirm that no document,
   prompt or script instructs a reader or an agent to expect the changelog to be updated
   automatically, and that CONTRIBUTING step 1 remains the single instruction for writing
   it. The review recorded that the automated path never actually worked — the entries
   were stranded on an unpushable local `develop` — so this is a documentation-coherence
   check, not a functional one. Record the finding either way.
2. The operator-facing removals recorded in the 30-01 SUMMARY — the deleted
   foreground-gate-timeout environment knob and the changed Ship gate text — must not
   still be documented anywhere as available or as previously worded. Search for the knob
   by name and for the old gate question text.

Finally, add the phase's closing entry to `CHANGELOG.md`'s unreleased section only if this
repository's convention is to accumulate entries there between releases; check how the
top of the file is currently structured and follow it. If the convention is that the
operator writes the section at release time (CONTRIBUTING step 1), do NOT add one — say so
in the SUMMARY instead. Do not guess: read the file.
  </action>
  <verify>
    <automated>cd /var/home/denniyahh/Github/devflow; rg -n --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!target/**' --glob '!site/**' -e 'hooks_after_ship' -e 'VersionBump' -e 'ChangelogAppend' -e 'BranchCleanup' -e 'BranchCreate' -e 'compute_version' -e 'write_version' -e 'prepend_changelog' -e 'release_start' -e 'release_finish' -e 'feature_finish' -e 'merge_feature_into_develop' -e 'check_signing_viability' -e 'SigningViability' -e 'classify_ssh_add_status' -e 'DEVFLOW_FOREGROUND_GATE_TIMEOUT_SECS' ; test $? -eq 1</automated>
    <automated>cd /var/home/denniyahh/Github/devflow; rg -ni --glob '!.planning/**' --glob '!CHANGELOG.md' --glob '!target/**' --glob '!site/**' -e 'merge and tag' -e 'write version and tag' -e 'compute version and tag' -e 'tag-signing' -e 'release-cut executor' -e 'branch cleanup' -e 'merge/tag/sync/publish' -e 'DEN-50' ; test $? -eq 1</automated>
    <automated>cd /var/home/denniyahh/Github/devflow; cargo clippy --workspace --all-targets -- -D warnings > /tmp/t30-clippy.log 2>&amp;1; c=$?; cargo test --workspace > /tmp/t30-ws.log 2>&amp;1; w=$?; cargo test -p devflow --test ship_no_git_mutation > /tmp/t30-tracer.log 2>&amp;1; t=$?; tail -20 /tmp/t30-clippy.log; tail -30 /tmp/t30-ws.log; tail -20 /tmp/t30-tracer.log; test $c -eq 0 &amp;&amp; test $w -eq 0 &amp;&amp; test $t -eq 0 &amp;&amp; rg -q '1 passed' /tmp/t30-tracer.log</automated>
  </verify>
  <done>
BOTH sweeps return no hits. Sweep A covers Rust source as well as prose; Sweep B covers
behavioural phrasing that names no symbol. The changelog-coherence check and the
operator-facing-removals check are both recorded with their findings. Clippy is clean,
the workspace suite is green, and the tracer fixture from 30-01 still reports `1 passed`
after every deletion in the phase.

If Sweep B fires on text that is legitimately historical — a `docs/` page deliberately
describing what DevFlow USED to do, say — do not weaken the sweep to accommodate it.
Rephrase the prose so the past tense is unambiguous and the phrase no longer appears, or
move the statement into `CHANGELOG.md` where the history belongs. A gate loosened once to
let a real hit through stops being a gate.
  </done>
  <reversibility rating="reversible">Documentation only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → a future contributor or agent | A document describing a removed capability is an instruction to rebuild it; this project has rebuilt release automation twice. |
| Documentation → the operator's release procedure | If two documents describe the release, one of them will drift and be followed anyway (the 25f failure mode). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-12 | Tampering | stale ARCHITECTURE.md hook and git-model sections | high | mitigate | Rewrite from the current source, and prove completeness with a repo-wide sweep whose exclusions are limited to the historical record and planning artifacts. |
| T-30-13 | Repudiation | two competing release descriptions | medium | mitigate | Establish CONTRIBUTING.md as the sole authority in an explicit lead paragraph; strip release-step description from every other document. |
| T-30-14 | Information Disclosure | documented but deleted environment knob | low | mitigate | Search for the removed knob by name; an operator setting a knob that no longer exists gets silence, not an error. |
| T-30-SC | Tampering | npm/pip/cargo installs | low | accept | Documentation-only plan; no packages installed. |
</threat_model>

<verification>
1. The dangling-reference sweep in Task 3 returns no hits outside `CHANGELOG.md` and
   `.planning/`.
2. `cargo test --workspace` is green, including the doc-check parity tests.
3. `cargo test -p devflow --test ship_no_git_mutation` reports `1 passed` — the phase's
   load-bearing property holds at the phase's final commit.
4. `devflow release --check .` output matches OPERATIONS.md's description of it,
   character for character on the check names.
5. Tests-pass is NOT this phase's acceptance (ROADMAP criterion 4). Phase acceptance is
   the recorded red-then-green in 30-01's SUMMARY plus the end-state assertions the
   tracer fixture makes on every run.
</verification>

<success_criteria>
- CONTRIBUTING.md states that it is the sole release authority and that DevFlow performs
  no git mutation after Ship.
- ARCHITECTURE.md's hook table, `GitFlow` primitive list, Ship lifecycle and logging
  conventions all resolve to symbols and literals present in the current source.
- OPERATIONS.md describes three release checks and no deferred release-cut executor.
- No prose anywhere in the repository, outside the historical `CHANGELOG.md` and the
  `.planning/` record, refers to a capability removed by this phase.
- Changelog authorship is documented as the operator's, with the finding recorded.
</success_criteria>

<output>
Create `.planning/phases/30-withdraw-devflow-from-the-release-business/30-04-SUMMARY.md` when done.

It MUST record: (a) the sweep command's final output; (b) the changelog-coherence
finding; (c) whether README.md needed correction or was confirmed clean; and (d) whether
a CHANGELOG.md unreleased entry was added, with the reasoning either way.
</output>
