---
phase: 45-unattended-auto-mode-hardening-999-110-999-109-999-94
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/config.rs
  - crates/devflow-core/src/git.rs
  - crates/devflow-core/src/hooks.rs
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-core/src/ship_evidence.rs
  - crates/devflow-cli/src/parallel.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - OPERATIONS.md
  - docs/guides/unattended-mode.md
autonomous: true
requirements: [AUTO-01]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`devflow start --phase N` creates `.worktrees/phase-NN` forked from the configured base branch, not from `develop`, when `base_branch` is set in `devflow.toml` or `DEVFLOW_BASE_BRANCH` is exported (D-01 / AUTO-01)."
    - "A phase worktree forked from a configured base branch that carries `.planning/config.json` contains that file, so `preflight_unattended_launch_check`'s `unattended_config_condition` passes without operator intervention (AUTO-01)."
    - "With no `base_branch` key, no `DEVFLOW_BASE_BRANCH`, and no `devflow.toml` at all, every resolved value is byte-identical to today's `develop` behaviour — zero regression for existing projects."
    - "When the configured base branch has no `origin/<base>` tracking ref, `ensure_base_ref_current` returns `Ok(())` after printing a warning containing the words `fail-open`, rather than refusing the launch (REVIEWS risk 1.2, verified fail-open contract at preflight.rs:540-547)."
    - "A resolved base branch equal to the value of `config::MAIN` is refused before any git mutation, with an operator-facing message that names the branch and contains no absolute filesystem path."
    - "The branch a phase worktree forks FROM and the branch `merge_feature_into_develop` merges INTO are the same resolved value — `GitFlow`'s develop-consuming methods read the project-resolved config, not `GitFlowConfig::default()` (REVIEWS finding 1 substance)."
    - "`HookContext.git_flow` is no longer discarded: `merge_feature`, `branch_create` and `branch_cleanup` construct their `GitFlow` from `ctx.git_flow` rather than re-defaulting it."
    - "`devflow start --no-worktree` creates its feature branch from the resolved base too, not from `develop` — the configured base governs BOTH fork paths, so no supported path validates the configured base and then forks from a different one (review round 2, F3)."
    - "A resolved base that is not an existing LOCAL BRANCH is refused: the production branch named indirectly through a remote-tracking spelling, a fully-qualified `refs/heads/` path, `HEAD`, or a bare SHA does not satisfy the guard, because `git rev-parse --verify` accepts all four and `worktree::add` forwards the raw value (review round 2, base-validation finding)."
    - "An explicitly configured base that fails validation is a hard error naming the value and its source, NOT a silent fallback to `develop` — otherwise the `main` refusal above is unobservable for the most direct way to configure it (review round 2, resolver-fail-soft finding)."
    - "The phase-artifact presence probe reads the resolved base on all three of its call sites, so a planning branch carrying CONTEXT.md/PLAN.md is visible to preflight and the operator messages name the branch actually probed (review round 2, F4)."
    - "`enumerate_phase_commits` resolves its git-flow config from the canonical project root rather than from the phase worktree it runs `git log` in, so idle-timeout commit evidence cannot silently fall back to the default trunk (review round 2, monitor-root finding)."
  artifacts:
    - crates/devflow-core/src/config.rs
    - crates/devflow-core/src/git.rs
    - crates/devflow-cli/src/parallel.rs
    - crates/devflow-cli/src/commands.rs
    - crates/devflow-cli/src/preflight.rs
    - crates/devflow-core/src/hooks.rs
    - OPERATIONS.md
    - docs/guides/unattended-mode.md
  key_links:
    - "`config::base_branch(project_root)` -> `config::git_flow_for_project(project_root)` -> `GitFlow::for_project(project_root)`: one resolver feeds both the worktree start point and the git-flow integration target, so they can never disagree."
    - "`commands::start` -> `ensure_base_ref_current(root, &base)` / `ensure_phase_reachable_on_base(root, phase, &base)` -> `ensure_phase_worktree(root, phase, force, &base)` -> `worktree::add(.., start_point = &base)`: the branch the guards inspect is the branch the run forks from."
    - "`pipeline_outcomes.rs:1056-1064` -> `HookContext.git_flow` -> `hooks::merge_feature` -> `GitFlow::with_config`: the only production `HookContext` construction site is the single point where the resolved config reaches every checkout hook."
    - "`env_value(\"DEVFLOW_BASE_BRANCH\")` written as a string literal -> `doc_check::source_read_env_vars` -> `OPERATIONS.md` env table: a const-mediated read would be invisible to the scanner and pass green while undocumented."
  prohibitions:
    - statement: "The base-branch knob must never become a path by which an unattended run forks from or merges into the production branch. A resolved base equal to `config::MAIN` is refused, and no ambient environment value may silently redirect a phase's integration target onto `main`."
    - statement: "A configured base branch name supplied by the operator may be echoed back to the operator, but DevFlow must never derive, infer, or synthesize a username or an absolute host path into any preflight message or persisted `events.jsonl` payload as a consequence of this change (WR-02 / 999.10 class)."
---

<objective>
Make the branch a phase worktree forks from — and merges back into — a resolved
configuration value rather than the hardcoded `develop` constant, so a project whose
`.planning/` lives on a personal tracking branch (e.g. `workspace/denniyahh`) can launch
`--mode auto` out of the box with `.planning/config.json` present in the worktree.

Purpose: `preflight_unattended_launch_check`'s `unattended_config_condition` reads
`.planning/config.json` from the WORKTREE (`preflight.rs:1116`, 999.76). A worktree forked
from `develop` does not carry a `.planning/` that only exists on the planning branch, so the
unattended check refuses every launch. That is AUTO-01 / 999.110.

Output: a `base_branch` config field + `DEVFLOW_BASE_BRANCH` resolver, a config-taking
`GitFlow` constructor, threading through `commands::start` / `preflight` / `parallel`, the
production call-site audit that keeps fork-point and merge-target identical, and the
same-plan operator-docs edit the `doc_check` env-var test requires.

This implements D-01 as locked in 45-CONTEXT.md ("configurable base branch resolution in
DevFlow config ... with `develop` as the default ... used when creating a phase worktree or
checking base ref reachability/currency"), at D-01's own **costly** reversibility rating.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-CONTEXT.md
@.planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-PATTERNS.md
@.planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-REVIEWS.md
</context>

## Artifacts this phase produces

New symbols and keys introduced by THIS plan (excluded from drift verification — they do
not exist at HEAD):

| Kind | Name | Location |
|---|---|---|
| struct field | `DevflowConfig.base_branch: Option<String>` | `crates/devflow-core/src/config.rs` |
| function | `config::base_branch(project_root: &Path) -> Result<ResolvedBaseBranch, String>` | `crates/devflow-core/src/config.rs` |
| struct | `config::ResolvedBaseBranch { value: String, source: BaseBranchSource }` | `crates/devflow-core/src/config.rs` |
| enum | `config::BaseBranchSource` (`Env` / `ConfigFile` / `Default`) | `crates/devflow-core/src/config.rs` |
| function | `commands::ensure_base_is_a_local_branch(&Path, &str) -> Result<(), CliError>` | `crates/devflow-cli/src/commands.rs` |
| function | `preflight::undeterminable_currency_warning(base: &str) -> String` | `crates/devflow-cli/src/preflight.rs` |
| function | `config::git_flow_for_project(project_root: &Path) -> GitFlowConfig` | `crates/devflow-core/src/config.rs` |
| function | `config::validate_base_branch(value: &str) -> Result<(), String>` | `crates/devflow-core/src/config.rs` |
| constructor | `GitFlow::with_config(root, config: GitFlowConfig) -> Self` | `crates/devflow-core/src/git.rs` |
| constructor | `GitFlow::for_project(root) -> Self` | `crates/devflow-core/src/git.rs` |
| env var | `DEVFLOW_BASE_BRANCH` | `crates/devflow-core/src/config.rs`, `OPERATIONS.md` |
| config key | `base_branch` (top-level key in `devflow.toml`) | `OPERATIONS.md`, `docs/guides/unattended-mode.md` |
| fn parameter | `base: &str` added to `parallel::ensure_phase_worktree` | `crates/devflow-cli/src/parallel.rs` |
| rename + fn parameter | `phase_artifact_on_develop` -> `phase_artifact_on_base`, gaining `base: &str` | `crates/devflow-cli/src/commands.rs` |
| fn parameter | `project_root: &Path` added to `monitor::enumerate_phase_commits` (distinct from its existing `workdir`) | `crates/devflow-core/src/monitor.rs` |

## Review incorporation (REVIEWS.md finding 1)

**Incorporated, with a correction to the finding's premise.**

- **Premise checked and found FALSE.** REVIEWS claims "the downstream GitFlow lifecycle
  ... remains hardcoded to `develop`". It is not. All eight develop-consuming `GitFlow`
  methods already read `self.config.develop` (`git.rs:133, 142, 161, 177, 200, 209, 234,
  317-318, 397`); production code in `git.rs` contains no `develop` string literal (every
  literal is at line >= 817, inside the test module). The hardcoding lives in exactly one
  place: `GitFlow::new`'s body (`git.rs:119-124`), which sets `GitFlowConfig::default()`.
  There is no `with_config` constructor at HEAD — verified absent. So the work is **one new
  constructor plus a call-site audit**, not the lifecycle rewrite the finding assumed.
- **Substance incorporated (finding 1, Planner Action bullets 1-2).** The finding's real
  question — is `base_branch` a worktree start point only, or the whole trunk? — is answered
  explicitly in Task 1: it is the **whole trunk for this project's DevFlow phase lifecycle**.
  Fork point and merge target resolve from the same value. The alternative (a separate
  `worktree_start_ref` with `develop` still the merge target) is REJECTED here because it
  produces exactly finding 1's risk 1.1: a feature branch forked from a personal branch,
  merged into `develop`, dragging unrelated history in. Task 3 does the call-site audit that
  makes the single-value design real.
- **Risk 1.2 (no `origin/` ref) incorporated as a REGRESSION TEST, not new handling.**
  Verified at source: `base_ref_currency` returns `Undeterminable` when `origin/{base}` fails
  `rev-parse --verify` (`preflight.rs:383-390`), and `ensure_base_ref_current` maps
  `Undeterminable` to `Ok(())` with a printed `fail-open` warning (`preflight.rs:540-547`).
  The behaviour the finding asks for already exists; Task 2 pins it so a future change cannot
  silently convert a local-only planning branch into a hard refusal.
- **`main` guard added beyond the finding.** Making the trunk configurable creates a new way
  to point a phase run at `main`. Task 1 refuses that outright.

## Review round 2 incorporation (45-REVIEWS.md — BOTH lanes returned REJECT)

Every round-2 finding scoped to this plan, with its disposition. Each was re-verified against
live source during this revision rather than accepted on the reviewer's word; where the reviewer
was wrong that is recorded too.

| # | Finding | Verified? | Disposition |
|---|---|---|---|
| F1 | Clippy check is a true constant-pass | YES — measured: the old pipeline exits 0 for a clean AND a failing clippy | FIXED in Task 3's `<verify>`: clippy's own exit status is captured and asserted alongside an `^error` count. Both directions re-measured against fixtures. |
| F2 | Green full-suite check exits non-zero | YES — measured: green prints `0` exit 1; red prints `1` exit 0 | FIXED in Task 3's `<verify>`: `; true` appended so the exit status cannot kill a green run, with the assertion still on printed output. The plan's own warning against the Phase-44 `rg -c` form had been followed by the same class of error with `grep -c`; that is now closed rather than repeated a third time. |
| F3 | `--no-worktree` still forks from the default trunk | YES — `commands.rs:343` calls `GitFlow::new`, whose sole constructor hardcodes the defaults (`git.rs:119-124`) | FIXED in Task 1: the `else` arm converts to `GitFlow::for_project`. A new `must_haves` truth and a two-direction test cover it. |
| F4 | `phase_artifact_on_develop` hardcodes the trunk | YES — literal branch name in the `ls-tree` argument array; three call sites; twelve total occurrences of the identifier | FIXED in Task 2: renamed, parameterised, all call sites and operator strings retargeted, the existing test renamed rather than deleted. |
| F5 | `commands::reference` ignores the configured base | YES — `commands.rs:552` defaults to the constant | FIXED in Task 3, with its own grep gate because the call-site audit gate cannot see it (no `GitFlow` construction there). Found by only one lane; this is the two-lane payoff and the reason a single reviewer is not sufficient. |
| F6 | Plan directed an edit to a nonexistent call site | YES — `evaluate_agent_result` appears in `pipeline_outcomes.rs` only at `:3277`/`:3344`, both test function names, both inside the `#[cfg(test)]` module starting at `:1097` | FIXED in Task 3: the phantom directive is removed and replaced with an exhaustive, re-derived 18-site inventory, plus an explicit "do not go looking for it" note and an acceptance criterion. |
| F7 | The fail-open warning text is not pinned | YES — the warning is a bare `println!` with no injectable writer, so deleting it leaves the disposition test green | FIXED in Task 2: the message moves into a pure helper and gets its own test with a negative control against the `Diverged` wording. |
| — | Base validation permits commit-ish aliases and production refs | YES — `git rev-parse --verify` accepts `HEAD`, a remote-tracking name, a `refs/heads/` path and a bare SHA; `worktree.rs:74-79` forwards the raw value | FIXED in Task 1 via `ensure_base_is_a_local_branch`, anchored on `refs/heads/{base}` and scoped to an explicitly configured base so the default path's existing fall-open is untouched. |
| — | The `main` refusal is bypassed by the resolver's fail-soft fallback | YES — plan-internal: a warn-and-fall-through resolver returns the default trunk, so `start` never sees the offending value and never emits the refusal this plan's `must_haves` promises | FIXED in Task 1: the resolver becomes fallible and carries provenance. This is a deliberate divergence from the `yes_ship` sibling's shape and the reason is recorded in the action and in the function's required doc comment. |
| — | Operator output still says branches are behind the default trunk | YES — `commands.rs:2083` | FIXED in Task 3, named explicitly rather than left to a generic sweep, because a generic sweep instruction is what missed it the first time. Backed by its own grep gate. |
| — | [SUSPECTED] Monitor commit enumeration resolves config from the wrong root | YES, and the suspicion is correct — `enumerate_phase_commits` takes only `workdir`, while its sole caller `fire_idle_timeout` has `project_root` in scope and already uses it four lines later | FIXED in Task 3: `project_root` is threaded and the config resolves from it; `workdir` stays as the git working directory. Covered by a test whose negative control swaps the two roots. |

Nothing from round 2 is deferred or rejected. `agy`'s Section A clean bill on verify-command
executability is NOT folded in as reassurance — it was disproven empirically by F1, and per this
project's review protocol a low-citation pass is a claim about the reviewer, not the artifact.

**What this revision does NOT establish.** Every fix above is a change to plan text. None of it
has been executed; the `<verify>` commands were validated against synthetic fixtures and against
HEAD's source, not against the post-change code, which does not exist yet. Specifically: the four
grep and audit gates were each run at HEAD and each fired its negative control (18 residual sites;
12 occurrences of the old probe name; 1 hardcoded trunk string; 4 constant uses), which proves
they can FAIL — it does not prove they will PASS after correct work, because correct work has not
happened. Treat a green run of these gates as necessary, not sufficient.

## Flagged assumption (spec-less probe fallback, §C)

This phase has no SPEC.md, so the deterministic edge probe ran against REQUIREMENTS.md text.
AUTO-01's row came back `{"category":"unclassified","status":"unresolved"}`. Per the
protocol an `unclassified` row is NEVER auto-resolved with a backstop and NEVER silently
dropped — it is surfaced here as a flagged assumption:

> **FLAGGED (AUTO-01, unclassified/unresolved):** the probe could not classify what edge
> category AUTO-01 belongs to, so no probe-derived acceptance criterion was authored into
> `must_haves.truths` from it. The `truths` in this plan were derived goal-backward from
> the ROADMAP success criterion and from 45-PATTERNS.md's verified source reading, not from
> the probe. If AUTO-01 has an edge the probe would have found under a proper
> classification, this plan does not cover it. **Reviewer: check this before accepting.**

No-silent-drop accounting for this plan: 1 probe-surfaced item = 0 authored into `truths`
from the probe + 1 surfaced as a flagged assumption.

**Canon-referral breadcrumb (§B):** "a base branch name shaped like a git flag (leading `-`)
reaching an argv position" was recalled and is canon argument-injection, so it is DROPPED
from `must_haves.prohibitions` per the canon-referral rule. The validation is still
implemented (Task 1) as an ordinary acceptance criterion.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end — a configured base branch reaches worktree::add</name>

  <read_first>
    - crates/devflow-core/src/config.rs (whole file — the `yes_ship` field at :74-98, the resolver at :210-229, `load_config` at :138-158, `env_value` at :265-267, the `ENV_MUTEX`/`EnvOverride` test guard at :272-292, and the `claude_legacy_launch` doc comment at :246-250 explaining why the env-var name MUST be a string literal)
    - crates/devflow-core/src/git.rs lines 95-135 (the `GitFlow` struct, its `config` field, and `new`'s body + doc comment)
    - crates/devflow-cli/src/parallel.rs lines 1-45 (`ensure_phase_worktree` in full)
    - crates/devflow-cli/src/commands.rs lines 198-350 (the `start` entry point: the `yes_ship` config-combining idiom at :215-232, `ensure_base_ref_current` at :270, `ensure_phase_reachable_on_base` at :280, the `divergence_from_develop` arm at :321-332, `ensure_phase_worktree` at :335)
    - crates/devflow-core/src/worktree.rs lines 60-83 (`add`, already parameterised on `start_point`)
    - crates/devflow-core/src/doc_check.rs lines 8-13, 55-67, 210-228, 407-428 (SCOPED_DOCS, scoped_doc_paths incl. `docs/guides/*.md`, `source_read_env_vars`, and the test that will go red without the docs edit)
    - OPERATIONS.md lines 108-125 (the existing `DEVFLOW_*` env-var table — `DEVFLOW_YES_SHIP` at :117 is the row to copy)
    - .planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-PATTERNS.md (sections D-01a through D-01e)
  </read_first>

  <files>
    crates/devflow-core/src/config.rs,
    crates/devflow-core/src/git.rs,
    crates/devflow-cli/src/parallel.rs,
    crates/devflow-cli/src/commands.rs,
    OPERATIONS.md,
    docs/guides/unattended-mode.md
  </files>

  <behavior>
    Write these tests FIRST and watch them fail before implementing.

    In `crates/devflow-core/src/config.rs` tests (serialize on the existing `ENV_MUTEX`,
    use the existing `EnvOverride` RAII guard for every env-mutating case):
    - `base_branch_defaults_to_develop_with_no_config`: an empty tempdir (no `devflow.toml`)
      resolves `Ok` with `value` equal to `DEVELOP` and `source` equal to `Default`. NEGATIVE
      CONTROL for the whole change: if this breaks, every existing project silently changed trunk.
    - `base_branch_reads_devflow_toml`: a `devflow.toml` containing a `base_branch` key set
      to `workspace/example` resolves `Ok` with that value and `source` equal to `ConfigFile`.
    - `base_branch_env_beats_file`: with the same file present AND `DEVFLOW_BASE_BRANCH` set
      to `other/branch`, resolves `Ok` with `other/branch` and `source` equal to `Env`.
    - `base_branch_empty_env_falls_through_to_file`: `DEVFLOW_BASE_BRANCH` set to the empty
      string is ignored (this is `env_value`'s documented empty-string filter), so the file
      value wins and `source` is `ConfigFile`.
    - `base_branch_errors_on_an_explicitly_configured_production_branch`: a `devflow.toml` whose
      `base_branch` holds the value of `MAIN` returns `Err`, and the error string contains that
      value and identifies the config file as the source. Repeat the same assertion with the
      value supplied through `DEVFLOW_BASE_BRANCH` instead, and require the error to identify the
      environment variable as the source.
      NEGATIVE CONTROL, same test: with neither source set, the resolver returns `Ok(DEVELOP)`.
      THIS TEST IS THE POINT OF THE FALLIBLE RESOLVER. Review round 2 established that a
      warn-and-fall-through resolver makes this plan's `main`-refusal truth unobservable for the
      most direct configuration — the operator gets a silent fallback to the default trunk and
      never sees a refusal. If this test passes only because `start` refuses later, it is testing
      the wrong layer: assert on `config::base_branch`'s own return value.
    - `base_branch_errors_on_an_explicitly_configured_blank_or_flag_shaped_value`: a
      `devflow.toml` key or an environment value that is whitespace-only, or that begins with
      `-`, returns `Err` rather than falling back.
    - `git_flow_for_project_replaces_develop_only`: `git_flow_for_project` on a tempdir whose
      `devflow.toml` sets `base_branch` to `workspace/example` returns a `GitFlowConfig` whose
      `develop` is `workspace/example`, whose `main` still equals `MAIN`, and whose
      `feature_prefix` still equals `FEATURE_PREFIX`. NEGATIVE CONTROL: `main` and
      `feature_prefix` must NOT move.
    - `validate_base_branch_refuses_main`: passing the value of `MAIN` returns `Err`, and the
      error string contains neither a `/` followed by `home` nor the current user's name.
    - `validate_base_branch_refuses_flag_shaped_and_blank`: a value beginning with `-` returns
      `Err`; a value that is empty or all whitespace returns `Err`; `workspace/example`
      returns `Ok`. NEGATIVE CONTROL: the `Ok` case is what proves the validator is not
      rejecting everything.

    In `crates/devflow-core/src/git.rs` tests (copy the temp-repo fixture shape used by the
    `cleanup_merged` tests at :1119-1249):
    - `with_config_uses_the_supplied_develop_not_the_default`: build a repo with `main`,
      `develop` and a third branch; construct via `with_config` with `develop` set to the
      third branch; assert a develop-relative operation resolves against the third branch.
      NEGATIVE CONTROL: the same operation via `GitFlow::new` on the same repo must resolve
      against `develop`, proving the constructor is what changed the answer.

    In `crates/devflow-cli/src/parallel.rs` tests:
    - `ensure_phase_worktree_forks_from_the_supplied_base`: a repo with `develop` and a
      `workspace/example` branch carrying a file that is absent on `develop`; calling
      `ensure_phase_worktree` with `workspace/example` produces a worktree containing that
      file. NEGATIVE CONTROL: the same call with `develop` produces a worktree where the file
      is ABSENT. Both directions must be asserted in the same test or the test proves nothing.

    In `crates/devflow-cli/src/commands.rs` tests (git fixtures, per CLAUDE.md hermeticity rules):
    - `ensure_base_is_a_local_branch_rejects_commit_ish_that_is_not_a_local_branch`: in a repo
      that has a local `develop`, a local `workspace/example`, a remote-tracking `origin/main`
      and at least one commit, assert `Ok` for `workspace/example` and `Err` for EACH of: the
      remote-tracking spelling of the production branch, its fully-qualified `refs/heads/` path,
      the literal `HEAD`, and the resolved SHA of `HEAD`.
      This is the review-round-2 bypass, and each of the four spellings is a distinct escape —
      verified at source that `git rev-parse --verify <base>` (the probe this plan otherwise
      relies on) accepts every one of them, and that `worktree::add` forwards the raw value to
      `git worktree add` as a start point.
      NEGATIVE CONTROL, same test: the `Ok` case above is what proves the helper is not simply
      rejecting everything. A test containing only the four `Err` assertions would pass against a
      helper that returns `Err` unconditionally.
    - `no_worktree_start_forks_the_feature_branch_from_the_configured_base`: with a configured
      base carrying a file absent on `develop`, drive the `--no-worktree` path far enough to
      create the feature branch, and assert the new branch's tip contains that file.
      NEGATIVE CONTROL, same test: assert the new branch's tip is NOT an ancestor-equal of
      `develop`'s tip. This is the direct guard for the review-round-2 finding that this path
      still forked from the default trunk while every preceding check reported success; an
      assertion that only "a branch was created" passes against exactly the broken behaviour.
      If driving the full `start` entry point is impractical in a unit test, assert instead on
      `GitFlow::for_project(root).feature_start(phase)` — the call this task substitutes in —
      and record in the SUMMARY that the assertion is one level below the CLI entry point, rather
      than claiming end-to-end coverage that was not obtained.
  </behavior>

  <action>
    Per D-01, add `base_branch: Option<String>` to `DevflowConfig`, defaulting to `None` in the
    `Default` impl. Document on the field that `None` means the built-in `DEVELOP` constant and that this
    key is the project's DevFlow integration trunk, not merely a worktree start point.

    Add `config::base_branch(project_root: &Path) -> Result<ResolvedBaseBranch, String>`, where
    `ResolvedBaseBranch { value: String, source: BaseBranchSource }` and `BaseBranchSource` is
    `Env | ConfigFile | Default`. Resolution order matches the existing `yes_ship` resolver:
    `env_value("DEVFLOW_BASE_BRANCH")` with the variable name written as a **string literal** (a
    const-mediated read is invisible to `doc_check::source_read_env_vars` and would pass green
    while undocumented — the failure recorded in `claude_legacy_launch`'s doc comment), then
    `load_config(project_root).base_branch`, then `DEVELOP`.

    **This resolver is deliberately FAIL-HARD on an explicitly supplied value, unlike its
    `yes_ship` sibling — do not "fix" it to match.** When the environment variable or the
    `devflow.toml` key supplies a value that fails `validate_base_branch`, return `Err` naming
    both the offending value and which source supplied it. Do NOT warn-and-fall-through. The
    divergence from the sibling resolver is justified and must be recorded in the function's doc
    comment: a bad `yes_ship` bool falls back to `false`, which is the SAFER direction, whereas a
    bad base branch falling back to `DEVELOP` silently redirects the trunk to a value the
    operator did not ask for. Review round 2 established that fail-soft here makes this plan's
    own `main`-refusal truth unobservable: an operator writing the production branch name into
    `devflow.toml` would get a `tracing::warn!` nobody reads and a silent fallback, and the
    refusal that names the offending branch would never be emitted for the single most direct
    way to configure it. Only the `Default` arm — no env var, no key — is infallible, and it
    always yields `DEVELOP`.

    The `source` field is not decoration: `commands::start` needs it for the operator-visible
    note (which must say WHERE the value came from), and the local-branch check below is scoped
    on it.

    Add `config::validate_base_branch(value: &str) -> Result<(), String>` rejecting: a value
    equal to `MAIN`; a value that is empty or entirely whitespace; a value whose first byte is
    `-`. The `MAIN` rejection message must name the offending branch and the reason (forking and
    merging phase work on the production branch bypasses the release path), and must contain no
    absolute filesystem path and no host username, per the WR-02 / 999.10 convention documented
    at `preflight.rs:218-224`.

    Add `config::git_flow_for_project(project_root: &Path) -> GitFlowConfig` returning a
    `GitFlowConfig` whose `main` is `MAIN`, `feature_prefix` is `FEATURE_PREFIX`, and `develop`
    is the resolved base. This is the single place the trunk substitution happens. It returns a
    plain `GitFlowConfig` rather than a `Result`, because it is called from many non-CLI sites
    that have no error channel; on a resolver `Err` it logs and returns `GitFlowConfig::default()`.
    That fallback is acceptable ONLY because `commands::start` refuses on the same `Err` before
    any git mutation, so no run reaches those sites with an invalid configuration. State that
    dependency in the doc comment — it is the reason the fallback is not a hole.

    Add `commands::ensure_base_is_a_local_branch(project_root: &Path, base: &str) ->
    Result<(), CliError>` beside the other private `commands.rs` git helpers, verifying with
    `git rev-parse --verify --quiet refs/heads/{base}` that the resolved base names an existing
    LOCAL BRANCH, and returning a `CliError::Message` naming the value when it does not.

    This closes a bypass that the pure `validate_base_branch` string check cannot: verified at
    source this session, `preflight::phase_reachability_on_base` probes the base with
    `git rev-parse --verify <base>`, which accepts any commit-ish, and `worktree::add` passes the
    value through to `git worktree add` as a raw `<start_point>` (`worktree.rs:74-79`). A value
    naming the production branch indirectly — through a remote-tracking name, a fully-qualified
    ref path, a reflog alias, or a bare SHA — therefore satisfies every check this plan otherwise
    performs, and the "never fork from production" guard is bypassable by spelling. Anchoring on
    `refs/heads/{base}` rejects all four shapes at once: none of them exists as a local branch
    under that spelling. It is also the correct semantic requirement independently, because this
    phase's whole design makes the base a MERGE TARGET as well as a fork point, and a merge target
    must be a branch.

    **Scope this check to `source != BaseBranchSource::Default`, and say why in a comment.** A
    fresh clone can legitimately have `develop` only as `origin/develop` with no local branch; at
    HEAD that case falls open through `phase_reachability_on_base`'s `Undeterminable` arm. Making
    the check unconditional would convert that fall-open into a hard refusal and break the
    zero-regression truth in this plan's `must_haves`. Applying it only to an explicitly
    configured base closes the bypass for the knob this phase adds without touching the default
    path at all.

    In `git.rs`, add `pub fn with_config(root: impl AsRef<Path>, config: GitFlowConfig) -> Self`
    and `pub fn for_project(root: impl AsRef<Path>) -> Self` (the latter delegating to
    `with_config` with `config::git_flow_for_project`). Leave `new` behaviourally unchanged so
    library callers with no project context keep the defaults, and update `new`'s doc comment,
    which currently asserts it uses the hardcoded git-flow constants without mentioning that a
    project-resolved sibling now exists.

    In `parallel.rs`, add a `base: &str` parameter to `ensure_phase_worktree` and pass it as
    `worktree::add`'s `start_point` in place of the imported `DEVELOP` constant. Switch the
    `delete_branch` call in the `force` arm to `GitFlow::for_project(project_root)` so the
    branch-protection list it consults matches the resolved trunk. Remove the now-unused
    `DEVELOP` import if nothing else in the file uses it.

    In `commands.rs::start`, resolve the base once near where `config::yes_ship` is already read
    (around line 215) and return a `CliError::Message` on the resolver's `Err`. When the source is
    not `Default`, call `ensure_base_is_a_local_branch` and propagate its error too. Both refusals
    must land BEFORE any git mutation — this is the "refused before any git mutation" clause of
    this plan's `must_haves`, and the ordering is the whole of it.

    Then substitute the resolved value for the `DEVELOP` argument at both `ensure_base_ref_current`
    and `ensure_phase_reachable_on_base`, and pass it into `ensure_phase_worktree`. When the
    resolved value differs from `DEVELOP`, print an operator-visible note in the same shape as the
    existing `yes_ship` note at :215-232, naming the resolved branch and its `BaseBranchSource`.

    Switch the `divergence_from_develop` construction at :321 to `GitFlow::for_project`, and
    rewrite the two operator strings in that arm so they name the resolved branch via
    interpolation instead of the literal trunk name.

    **Convert the `--no-worktree` branch-creation path at `commands.rs:343` as well.** Review
    round 2 found this omission and it was re-verified at source during this revision: the
    `else` arm of the `if worktree` block constructs `GitFlow::new(project_root)` and calls
    `feature_start` / `feature_start_force` on it. `GitFlow::new` is the sole constructor at HEAD
    and hardcodes `GitFlowConfig::default()` (`git.rs:119-124`), so on this path `devflow start`
    validates the configured base, checks its currency and its reachability, prints a note naming
    it — and then forks the feature branch from the default trunk anyway. That is AUTO-01's core
    promise failing on a supported path while every check reports success, which is a worse
    outcome than the bug this phase fixes, because it is silent. Use `GitFlow::for_project` here
    so the branch this arm creates forks from the same resolved value the worktree arm uses.

    Documentation, in this SAME task because `doc_check`'s
    `source_devflow_env_vars_and_subcommands_are_documented` test goes red the moment the
    `env_value` literal lands: add a `DEVFLOW_BASE_BRANCH` row to the OPERATIONS.md env-var
    table (same columns as the `DEVFLOW_YES_SHIP` row: name, default, description naming the
    `base_branch` key it overrides), and add a short subsection to
    `docs/guides/unattended-mode.md` stating that a project whose planning artifacts live on a
    branch other than the default trunk must set `base_branch`, that the value is the branch
    phase worktrees fork from AND merge back into, and that `main` is refused.
  </action>

  <verify>
    <automated>cargo test -p devflow-core --lib config::tests::base_branch 2>&1 | rg 'test result: ok. [1-9][0-9]* passed'</automated>
    <fails_when>non-zero exit, or no line matching "test result: ok. &lt;n&gt; passed" with n >= 1 (rg prints nothing and exits 1). A filter that matches no test prints "0 passed" and is therefore also a failure — this is the CLAUDE.md false-green trap.</fails_when>

    <automated>cargo test -p devflow-core --lib config::tests::validate_base_branch 2>&1 | rg 'test result: ok. [1-9][0-9]* passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line, or no "test result: ok." line at all</fails_when>

    <automated>cargo test -p devflow-core --lib config::tests::git_flow_for_project 2>&1 | rg 'test result: ok. [1-9][0-9]* passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>cargo test -p devflow-core --lib with_config_uses_the_supplied_develop_not_the_default 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or the summary reads "0 passed" (the name matched nothing — `--exact`-style filters exit 0 on a no-match, per CLAUDE.md)</fails_when>

    <automated>cargo test -p devflow --bin devflow ensure_phase_worktree_forks_from_the_supplied_base 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed", or stderr contains "no library targets found" (which would mean `--lib` was used against the binary-only `devflow` package — CLAUDE.md)</fails_when>

    <automated>cargo test -p devflow-core --lib base_branch_errors_on_an_explicitly_configured 2>&1 | rg 'test result: ok. 2 passed'</automated>
    <fails_when>non-zero exit, or a passed count other than 2 — fewer means one of the two fail-hard resolver tests is missing, and "0 passed" exits 0 and is the CLAUDE.md false-green trap</fails_when>

    <automated>cargo test -p devflow --bin devflow ensure_base_is_a_local_branch_rejects_commit_ish_that_is_not_a_local_branch 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>cargo test -p devflow --bin devflow no_worktree_start_forks_the_feature_branch_from_the_configured_base 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>cargo test -p devflow-core --lib doc_check::tests::source_devflow_env_vars_and_subcommands_are_documented 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or output contains "source-read environment variable `DEVFLOW_BASE_BRANCH` is missing from scoped operator docs", or "0 passed"</fails_when>
  </verify>

  <acceptance_criteria>
    - `crates/devflow-core/src/config.rs` contains a `base_branch` field on `DevflowConfig` whose `Default` value is `None`.
    - `crates/devflow-core/src/config.rs` contains the exact substring `env_value("DEVFLOW_BASE_BRANCH")` written as a string literal (not via a const), so `doc_check::source_read_env_vars` sees it.
    - `OPERATIONS.md` contains the token `DEVFLOW_BASE_BRANCH`.
    - `docs/guides/unattended-mode.md` contains the token `base_branch`.
    - `crates/devflow-core/src/git.rs` declares both `pub fn with_config` and `pub fn for_project`.
    - `crates/devflow-cli/src/parallel.rs`'s `ensure_phase_worktree` signature has four parameters, the last being `base: &str`, and `worktree::add`'s fifth argument at that call site is that parameter.
    - `config::base_branch` on a tempdir with no `devflow.toml` and no `DEVFLOW_BASE_BRANCH` returns `Ok` whose `value` equals `DEVELOP` and whose `source` is `Default` (regression guard: existing projects unchanged).
    - `config::base_branch` returns `Err` — not a fallback to `DEVELOP` — when an explicitly supplied `devflow.toml` key or `DEVFLOW_BASE_BRANCH` value fails validation, and the `Err` string names both the offending value and its source. A resolver that warns and falls through does NOT satisfy this criterion, however green the rest of the suite is.
    - `config::git_flow_for_project` on a project configured with a non-default base returns `main == MAIN` and `feature_prefix == FEATURE_PREFIX` unchanged, and only `develop` moved.
    - `config::validate_base_branch(MAIN)` returns `Err`; `validate_base_branch("")`, `validate_base_branch("   ")` and `validate_base_branch("--upload-pack=x")` return `Err`; `validate_base_branch("workspace/example")` returns `Ok`.
    - `commands::ensure_base_is_a_local_branch` returns `Err` for all four non-local-branch spellings of the production branch enumerated in `behavior`, and `Ok` for a real local branch. Anchoring the probe on anything other than `refs/heads/{base}` does not satisfy this criterion — a bare `rev-parse --verify` accepts every one of the four.
    - `commands.rs`'s `--no-worktree` arm constructs its `GitFlow` from `GitFlow::for_project`, and the string `GitFlow::new(project_root)` no longer appears in that arm.
    - The base refusal for an invalid or production-named base occurs before `ensure_phase_worktree`, `feature_start` and `feature_start_force` are reachable — no git mutation precedes it.
    - `ensure_phase_worktree` called with a base branch carrying a file absent on `develop` produces a worktree containing that file, AND the same call with `develop` produces a worktree without it (both directions asserted).
    - No operator-facing string added by this task contains a `/home/` or `/Users/` prefix or an OS username.
  </acceptance_criteria>

  <done>
    A `devflow.toml` with `base_branch = "workspace/example"` (or `DEVFLOW_BASE_BRANCH` exported)
    causes `devflow start` to fork `.worktrees/phase-NN` from that branch; with neither set,
    behaviour is byte-identical to today; `main` as a base is refused; and the workspace
    doc-check test is green because the env var is documented in the same commit.
  </done>

  <reversibility rating="costly">
    Adds a public config surface and two public constructors that downstream code will start
    depending on; removing the key later means an operator's committed `devflow.toml` silently
    changes meaning. Matches the operator's own D-01 rating in 45-CONTEXT.md. Not `one-way`,
    so no `checkpoint:decision` is inserted — deliberately, because this phase exists to make
    UNATTENDED runs work and a blocking-human checkpoint cannot receive an answer under
    DevFlow's one-shot launch model (999.57 / DEN-82).
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the fail-open contract, and retarget the artifact-presence probe off the default trunk</name>

  <read_first>
    - crates/devflow-cli/src/preflight.rs lines 130-160 (`phase_reachability_on_base` and its stale NOTE at :142-145 claiming `base` is always `DEVELOP`)
    - crates/devflow-cli/src/preflight.rs lines 318-400 (`ORIGIN`, the `BaseRefCurrency` enum, and `base_ref_currency`'s `ref_exists` branch that returns `Undeterminable` at :383-390)
    - crates/devflow-cli/src/preflight.rs lines 520-575 (`ensure_base_ref_current`, including the `Undeterminable` fail-open arm at :540-547 and the `Diverged` refusal)
    - crates/devflow-cli/src/preflight.rs around line 2760 (the existing `currency_fixture` helper) and around line 2917 (`currency_behind_refuses_when_base_is_checked_out_in_another_worktree`, the closest existing test shape)
    - crates/devflow-cli/src/commands.rs lines 85-105 (`phase_artifact_on_develop` in full — note the branch name is a literal element of the `ls-tree` argument array, not a parameter)
    - crates/devflow-cli/src/commands.rs lines 285-312 (both call sites and their two operator strings)
    - crates/devflow-cli/src/preflight.rs lines 605-630 (the third call site inside `preflight_interactivity_check`, and its operator string)
    - .planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-REVIEWS.md (finding 1 risk 1.2; findings F4 and F7)
  </read_first>

  <files>crates/devflow-cli/src/preflight.rs, crates/devflow-cli/src/commands.rs</files>

  <behavior>
    Write these tests FIRST.

    **How to produce the RED half — read before attempting it.** Do NOT try to delete the
    `Undeterminable` arm. Verified at source this session: `BaseRefCurrency` has five variants
    (`Current`, `Ahead`, `Behind`, `Diverged`, `Undeterminable`) and `ensure_base_ref_current`
    matches all five with no `_` wildcard, so deleting the arm yields a `non-exhaustive patterns`
    COMPILE error and the test never runs. Per CLAUDE.md a compile error is not evidence that a
    test catches a regression, exactly as a hang is not. Instead, temporarily replace the arm's
    BODY — swap `Ok(())` for an `Err(CliError::Message(...))` — which keeps the match exhaustive,
    keeps the workspace building, and produces a real `test result: FAILED`. Restore the body
    afterwards and quote the observed failure output in the SUMMARY.
    Use the same body-substitution technique for the warning-text control below.

    - `base_ref_currency_is_undeterminable_when_the_remote_ref_is_absent`: build a repo with a
      local branch that has no `origin/<name>` ref and no remote configured at all; assert
      `base_ref_currency` returns `BaseRefCurrency::Undeterminable`.
      NEGATIVE CONTROL, same test: a second branch that DOES have a matching `origin/` ref (set
      up via the existing `currency_fixture` shape) must return `Current`, not `Undeterminable`.
      Without that arm the test cannot distinguish "correctly detected absence" from "always
      returns Undeterminable".
    - `ensure_base_ref_current_fails_open_for_a_local_only_planning_branch`: the same
      remote-less branch passed to `ensure_base_ref_current` returns `Ok(())`.
      NEGATIVE CONTROL, same test: a genuinely diverged base still returns `Err`, proving the
      function did not become unconditionally permissive.

    - `undeterminable_currency_warning_names_the_branch_and_its_disposition`: assert directly on
      the extracted message helper (see `action`) that its output for a given base contains that
      base's name AND the disposition wording this plan's `must_haves` requires.
      This test exists because the disposition assertion above canNOT reach the message. Verified
      at source: `ensure_base_ref_current` emits the warning through a bare `println!`
      (`preflight.rs:540-547`) with no injectable writer anywhere in its call path, so a test that
      asserts only `Ok(())` stays green after the warning is deleted or reworded. Review round 2
      found precisely that gap between this plan's stated warning contract and the control offered
      for it. Extracting the message into a pure helper is what makes the contract assertable at
      all — without the extraction there is no honest way to satisfy the `must_haves` truth, and
      the truth would have to be struck instead.
      NEGATIVE CONTROL, same test: assert the helper's output does NOT contain the wording used
      by the `Diverged` refusal, so the assertion is discriminating between the two dispositions
      rather than matching any message the module produces.

    - `phase_artifact_probe_reads_the_supplied_base_not_the_default_trunk`: a repo whose
      configured base branch carries a `.planning/phases/{NN}-.../{NN}-CONTEXT.md` that is ABSENT
      on `develop`. Assert the probe returns `true` when passed the configured base.
      NEGATIVE CONTROL, same test: the identical repo and phase, probed with `develop`, returns
      `false`. Both halves are required — the probe fails OPEN on git errors (returning `true`),
      so a test asserting only `true` cannot distinguish "found the artifact on the right branch"
      from "the git invocation failed and it fell open", which is the same answer for the wrong
      reason.
  </behavior>

  <action>
    Add the two regression tests described in `behavior` to `preflight.rs`'s test module, reusing
    the `currency_fixture` and `run_git` helpers already in that module rather than writing a new
    git fixture. Follow CLAUDE.md's git-hermeticity rules for any new repo fixture: configure
    `user.email`, `user.name`, `commit.gpgsign=false` and `core.hooksPath=/dev/null` on the temp
    repo, and never mutate process-global environment.

    Rewrite the stale doc comment at `preflight.rs:142-145`. It currently asserts that `base` is
    always the `DEVELOP` constant at the one call site — after Task 1 that claim is false, and a
    stale doc claim is precisely the drift this repo's post-commit hook exists to catch. Replace
    it with a statement that `base` is the project-resolved value from `config::base_branch`, and
    a pointer to the `ORIGIN` constant's own note that the remote name remains non-configurable.

    Add a doc comment on `ensure_base_ref_current`'s `Undeterminable` arm recording, in one line,
    that this arm is what allows a local-only planning branch to launch, and naming the tests
    above as its guards. State the fail direction and the reason, per this codebase's convention
    of commenting fail direction explicitly.

    **Extract the warning message into a pure helper.** Add a private
    `undeterminable_currency_warning(base: &str) -> String` to `preflight.rs` holding the text the
    `Undeterminable` arm currently formats inline, and have the arm `println!` the helper's return
    value. Keep the wording's meaning intact — the message must still name the base, name the
    remote ref it could not compare against, and state the disposition in the terms this plan's
    `must_haves` requires. The extraction is the ONLY reason the message contract becomes
    testable: a `println!` in a function with no injectable writer is unobservable from a unit
    test, so today the message could be deleted outright with every test still green.

    **Retarget the artifact-presence probe.** <!-- planner-discipline-allow: phase_artifact_on_develop -->
    Rename `commands.rs`'s `phase_artifact_on_develop` to `phase_artifact_on_base` and give it a
    `base: &str` parameter, replacing the branch name that is currently a literal element of its
    `git ls-tree` argument array.

    **Do not leave the old identifier behind in a comment.** The acceptance gate for this rename
    negative-greps both files for the old name and deliberately does NOT strip comment lines,
    because two of the twelve occurrences at HEAD ARE comments and stale doc prose is exactly the
    drift this repo's post-commit hook exists to catch. A "renamed from ..." breadcrumb comment
    would therefore fail the gate. Put the rationale in the SUMMARY, not in the source. Update all three
    call sites — the two in `commands.rs::start` and the one in
    `preflight::preflight_interactivity_check` — to pass the project-resolved base
    (`config::base_branch(project_root)`, falling back to `DEVELOP` on the resolver's `Err`
    rather than propagating, because this probe is a fail-open advisory check and `start` already
    refuses on that same `Err` before reaching it).

    The rename has five further mechanical consequences, enumerated here because the gate that
    checks it greps both files for the old identifier and will fail if any is missed. Verified by
    running that gate at HEAD during this revision: the old name occurs twelve times — the
    definition (`commands.rs:90`), the two `commands.rs` call sites (`:293`, `:303`), the
    `preflight.rs` import (`:21`), two `preflight.rs` doc/inline comments that cite it by name
    (`:139`, `:184`), the `preflight.rs` call site (`:618`), and an EXISTING test in
    `commands.rs` — `phase_artifact_on_develop_detects_context_and_fails_open` at `:5223` with
    four assertions at `:5244-5263`. Rename that test alongside the function and update its
    assertions to pass the base explicitly; do not delete it, and do not weaken its fail-open
    assertions, which cover behaviour this change must preserve.

    Rewrite the probe's doc comment and all three call sites' operator strings so they name the
    branch actually probed instead of the default trunk. Verified at source this session that
    each of those three messages currently asserts the artifact is missing from the default trunk;
    after this change that sentence would be a false statement to the operator about a branch the
    probe did not look at. This is the review-round-2 F4 finding, and its impact is not cosmetic:
    a configured planning branch holding `CONTEXT.md`/`PLAN.md` is invisible to the probe, so a
    `RequiresExistingArtifact` driver is refused at Define for an artifact that exists — the same
    class of false refusal AUTO-01 exists to remove.
  </action>

  <verify>
    <automated>cargo test -p devflow --bin devflow base_ref_currency_is_undeterminable_when_the_remote_ref_is_absent 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary (the filter matched no test), or stderr contains "no library targets found"</fails_when>

    <automated>cargo test -p devflow --bin devflow ensure_base_ref_current_fails_open_for_a_local_only_planning_branch 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary</fails_when>

    <automated>rg -c 'always `devflow_core::config::DEVELOP`' crates/devflow-cli/src/preflight.rs; echo "rg_exit=$?"</automated>
    <fails_when>a printed count other than 0, or the absence of the line `rg_exit=1` (rg exits 1 when it finds nothing, which is the passing state here; exit 0 means the stale claim survives)</fails_when>

    <automated>cargo test -p devflow --bin devflow undeterminable_currency_warning_names_the_branch_and_its_disposition 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>cargo test -p devflow --bin devflow phase_artifact_probe_reads_the_supplied_base_not_the_default_trunk 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>rg -n 'phase_artifact_on_develop' crates/devflow-cli/src/commands.rs crates/devflow-cli/src/preflight.rs; echo "rg_exit=$?"</automated>
    <fails_when>the absence of the line `rg_exit=1`. rg exits 1 when it finds nothing, which is the passing state: the old name must be gone from BOTH files. Any printed match line, or `rg_exit=0`, means a call site or the definition still carries the default trunk in its name. Negative control for this gate: it prints three match lines and `rg_exit=0` against HEAD, so it is not a constant-pass.</fails_when>
  </verify>

  <acceptance_criteria>
    - Both named tests exist in `crates/devflow-cli/src/preflight.rs` and each reports `1 passed`.
    - Each of the two tests contains BOTH a positive case and its stated negative control in the same test body — a test asserting only `Undeterminable`, or only `Ok(())`, does not satisfy this criterion.
    - Replacing the `BaseRefCurrency::Undeterminable` arm's BODY with an `Err(..)` causes `ensure_base_ref_current_fails_open_for_a_local_only_planning_branch` to print a real `test result: FAILED`. Deleting the arm entirely does NOT satisfy this criterion: the enum is matched exhaustively with no wildcard, so deletion is a compile error and the test never runs — and per CLAUDE.md a compile error, like a hang, is not evidence that a test catches a regression. Record the observed failure output in the SUMMARY.
    - Removing the `fail-open` wording from `undeterminable_currency_warning`'s returned string causes `undeterminable_currency_warning_names_the_branch_and_its_disposition` to print a real `test result: FAILED`. This is the control that distinguishes the message contract from the return-value contract; without it, the warning can be deleted with the suite still green.
    - `undeterminable_currency_warning` exists as a pure function returning `String`, and `ensure_base_ref_current`'s `Undeterminable` arm prints its return value rather than an inline `format!`.
    - The identifier `phase_artifact_on_develop` appears in neither `commands.rs` nor `preflight.rs`; the renamed function takes a `base: &str` parameter and its `git ls-tree` argument array contains no branch-name literal.
    - All three call sites pass a project-resolved base, and none of the three operator strings names the default trunk as the branch probed.
    - `phase_artifact_probe_reads_the_supplied_base_not_the_default_trunk` asserts BOTH the `true` (configured base) and the `false` (default trunk) outcome in the same test body. Because the probe fails open by returning `true`, a single-direction assertion is indistinguishable from a broken git invocation.
    - The phrase asserting that `base` is always the develop constant no longer appears in `preflight.rs`.
    - No new git fixture in this task mutates process-global environment variables.
  </acceptance_criteria>

  <done>
    A base branch with no `origin/` tracking ref classifies as `Undeterminable` and launches with
    a warning rather than a refusal; both the disposition AND the warning's text are pinned by
    tests whose failing direction was actually observed; the artifact-presence probe reads the
    configured base on all three call sites and says so to the operator; and `preflight.rs` no
    longer claims the base is always `develop`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Call-site audit — fork point and merge target resolve to one value</name>

  <read_first>
    - crates/devflow-core/src/hooks.rs lines 36-50 (`HookContext`, including the `git_flow: GitFlowConfig` field that is currently read only for `feature_prefix`)
    - crates/devflow-core/src/hooks.rs lines 118-145 (`branch_create`, `branch_cleanup`) and lines 175-300 (`merge_feature` and the remaining `GitFlow::new` sites at :239, :278, :289)
    - crates/devflow-cli/src/pipeline_outcomes.rs lines 1050-1075 (the ONLY production `HookContext` construction, with `GitFlowConfig::default()` at :1056 feeding it at :1064)
    - crates/devflow-cli/src/pipeline_outcomes.rs line 599 (`phase_commit_count` with a defaulted config)
    - crates/devflow-cli/src/pipeline_launch.rs line 1471 (`GitFlowConfig::default()` feeding `evaluate_agent_result`)
    - crates/devflow-core/src/monitor.rs lines 1243-1258 (`enumerate_phase_commits`, which builds a `develop..feature` range from a defaulted config)
    - crates/devflow-core/src/ship_evidence.rs lines 155-165 (`GitFlow::new` at :158 and `GitFlowConfig::default().feature_prefix` at :161)
    - crates/devflow-cli/src/commands.rs lines 660-680 and 2040-2080 (the remaining `GitFlow::new` sites at :672, :2052, :2072)
    - crates/devflow-core/src/gsd_config.rs line 320 (`GitFlow::new` used only for `commit_path` — trunk-irrelevant, expected to stay as-is)
    - .planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-PATTERNS.md ("Every production site that must be re-pointed" table and the `HookContext` latent-inconsistency note)
  </read_first>

  <files>
    crates/devflow-core/src/hooks.rs,
    crates/devflow-core/src/monitor.rs,
    crates/devflow-core/src/ship_evidence.rs,
    crates/devflow-cli/src/commands.rs,
    crates/devflow-cli/src/pipeline_launch.rs,
    crates/devflow-cli/src/pipeline_outcomes.rs
  </files>

  <behavior>
    Write these tests FIRST.

    - `merge_feature_targets_the_configured_base_not_the_default`: a temp repo with `develop`, a
      `workspace/example` branch, and a `feature/phase-NN` branch forked from `workspace/example`
      with one commit; a `HookContext` whose `git_flow.develop` is `workspace/example`; running
      the merge hook lands the commit on `workspace/example`.
      NEGATIVE CONTROL, same test: `develop` must NOT contain that commit afterwards. Asserting
      only "the merge succeeded" would pass against today's code merging into `develop`, and is
      therefore worthless as a regression guard.
    - `hook_context_git_flow_is_not_discarded`: with a `HookContext` whose `git_flow.develop` is
      a branch that does not exist in the repo, the merge hook returns `Err` naming that branch.
      This is the direct proof the field is read rather than re-defaulted — under today's code
      the hook would silently succeed against the real `develop`.
    - `enumerate_phase_commits_ranges_from_the_configured_base`: commits reachable from the
      feature branch but not from the configured base are enumerated.
      NEGATIVE CONTROL, same test: a commit present on the configured base is NOT listed.
    - `enumerate_phase_commits_resolves_config_from_the_project_root`: call it with a
      `project_root` carrying a `devflow.toml` that configures a non-default base, and a
      `workdir` that carries NO `devflow.toml` — the shape of a phase worktree whose base branch
      does not track the operator's local config. Assert the enumerated range is the configured
      one.
      NEGATIVE CONTROL, same test: swap the two arguments so the config lookup would hit the
      worktree, and assert the result differs. Without the swap the test cannot distinguish
      "resolved from the right root" from "both roots happened to give the same answer", which is
      the whole of the review-round-2 concern.
  </behavior>

  <action>
    Convert every production site that consumes the trunk from a defaulted config to the
    project-resolved one, so that the branch a worktree forks from in Task 1 is the same branch
    the lifecycle merges into. Named sites, each a substitution rather than a redesign:

    In `hooks.rs`: `branch_create` (:125), `branch_cleanup` (:132), `merge_feature` (:181), and
    the sites at :239, :278 and :289 construct their `GitFlow` from `ctx.git_flow` via
    `GitFlow::with_config(&ctx.project_root, ctx.git_flow.clone())` instead of `GitFlow::new`.
    This is the fix for the latent inconsistency 45-PATTERNS.md flagged: `HookContext.git_flow`
    exists and is currently read only for `feature_prefix`, so every git operation inside a hook
    silently re-defaults the trunk. Threading config into `HookContext` alone would be a no-op
    without this half.

    **The complete inventory below was re-derived from live source during this revision** by
    listing every `GitFlow::new(` and `GitFlowConfig::default(` occurrence in each file and
    discarding those at or after the file's `#[cfg(test)]` line. It is exhaustive at HEAD: 18
    production sites across seven files, plus two deliberate exclusions. Work from this table, not
    from memory.

    | File | Production sites | Disposition |
    |---|---|---|
    | `hooks.rs` | 125, 132, 181, 239, 278, 289 | `GitFlow::with_config(&ctx.project_root, ctx.git_flow.clone())` |
    | `ship_evidence.rs` | 158, 161 | `GitFlow::for_project` / `config::git_flow_for_project` |
    | `monitor.rs` | 1249 | project-resolved — see the root note below |
    | `commands.rs` | 321, 343 | **already converted by Task 1** — listed so the audit gate's arithmetic is complete, not to be done twice |
    | `commands.rs` | 672, 2052, 2072 | `GitFlow::for_project` |
    | `parallel.rs` | 28 | **already converted by Task 1** (the `delete_branch` call in the `force` arm) |
    | `pipeline_launch.rs` | 1471 | `config::git_flow_for_project(project_root)` — `project_root` is in scope at that line |
    | `pipeline_outcomes.rs` | 599, 1056 | `config::git_flow_for_project(project_root)` |

    Deliberate exclusions, both to be restated in the SUMMARY with their reasons so a reader does
    not wonder whether they were missed: `git.rs:122` is `GitFlow::new`'s own body, which stays
    defaulted by design so library callers with no project context keep today's behaviour; and
    `gsd_config.rs:320` uses only `commit_path`, which never consults the trunk.

    In `pipeline_outcomes.rs`: `:1056` is the config that feeds the `HookContext` at `:1064` — the
    single production `HookContext` construction, and therefore the one point where the resolved
    value reaches every checkout hook. `:599` is a separate defaulted config passed to
    `phase_commit_count`. **There is no third site.** A previous revision of this plan directed an
    additional edit to ":1056's sibling use in `evaluate_agent_result`"; that call site does not
    exist. Verified during this revision: `evaluate_agent_result` appears in `pipeline_outcomes.rs`
    only at lines 3277 and 3344, both of which are TEST FUNCTION NAMES inside the `#[cfg(test)]`
    module that begins at line 1097. The real production call lives in `pipeline_launch.rs:1472`
    and is fed by the `:1471` site already listed above. Do not go looking for the phantom.

    In `monitor.rs`, the conversion needs one extra step beyond substituting the constructor.
    `enumerate_phase_commits` (`:1245`) takes only `workdir` and builds a two-dot trunk-to-phase
    range from a defaulted config; against a mismatched trunk that range either over- or
    under-reports the phase's commits, which is exactly the false-evidence shape the never-silent
    commit gate depends on. **Resolve the config from the canonical project root, not from
    `workdir`.** Add a `project_root: &Path` parameter and pass it from the sole caller,
    `fire_idle_timeout` (`:1190`), which already has both roots in scope — verified during this
    revision: `project_root` is `fire_idle_timeout`'s first parameter and is already used four
    lines below the call. Keep `workdir` as the git invocation's working directory; only the
    CONFIG lookup moves. Resolving config from `workdir` would read a phase worktree, which need
    not carry a `devflow.toml` that exists only in the main checkout, and would silently fall back
    to the default trunk — the failure being fixed, reintroduced one layer down.

    Convert `commands::reference` (`:552`) as well. It defaults its optional `--branch` argument
    to the `DEVELOP` constant, so `devflow reference` with no argument snapshots the default trunk
    even on a project whose trunk is configured elsewhere — and then prints a message naming the
    branch it snapshotted, so the operator is told the wrong thing confidently. This site was
    found by only one of the two review lanes and carries no `GitFlow` construction, so the
    audit gate below will not catch it; it needs its own conversion and its own grep.

    After the conversions, fix the operator-facing strings that name the trunk as a literal word.
    One is named explicitly because a generic sweep instruction already failed to catch it once:
    **`commands.rs:2083`**, inside `print_open_branches`, formats a per-branch suffix asserting how
    many commits behind the default trunk each open branch is — while the comparison it reports was
    computed against whatever trunk `list_feature_branches` actually used. Interpolate the resolved
    branch. Then sweep the remaining touched files for the same shape in strings and doc comments.
    A message that says the run is behind a branch it did not compare against is a false statement
    to the operator, and it is the kind that reads as authoritative.
  </action>

  <verify>
    <automated>cargo test -p devflow-core --lib merge_feature_targets_the_configured_base_not_the_default 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line (the test name matched nothing)</fails_when>

    <automated>cargo test -p devflow-core --lib hook_context_git_flow_is_not_discarded 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>cargo test -p devflow-core --lib enumerate_phase_commits_ranges_from_the_configured_base 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>cargo test -p devflow-core --lib hooks:: 2>&1 | rg 'test result: ok. [1-9][0-9]* passed'</automated>
    <fails_when>non-zero exit, or "0 passed", or any line matching "^test result: FAILED"</fails_when>

    <automated>cargo test -p devflow-core --lib enumerate_phase_commits_resolves_config_from_the_project_root 2>&1 | rg 'test result: ok. 1 passed'</automated>
    <fails_when>non-zero exit, or "0 passed" in the summary line</fails_when>

    <automated>bash -c 'set +e; : > target/45-01-audit.txt; for f in crates/devflow-core/src/hooks.rs crates/devflow-core/src/ship_evidence.rs crates/devflow-core/src/monitor.rs crates/devflow-cli/src/commands.rs crates/devflow-cli/src/parallel.rs crates/devflow-cli/src/pipeline_launch.rs crates/devflow-cli/src/pipeline_outcomes.rs; do cfg=$(rg -n "^#\[cfg\(test\)\]" "$f" | head -1 | cut -d: -f1); [ -z "$cfg" ] && cfg=999999; rg -n "GitFlow::new\(|GitFlowConfig::default\(" "$f" | rg -v ":[[:space:]]*//" | awk -F: -v c="$cfg" -v f="$f" "\$1+0 < c+0 {print f\":\"\$1}" >> target/45-01-audit.txt; done; echo "residual_prod_default_sites=$(wc -l < target/45-01-audit.txt)"; cat target/45-01-audit.txt'</automated>
    <fails_when>the line `residual_prod_default_sites=0` is absent, or any file:line follows it. Each printed line is a production site still building its git-flow config from the defaults instead of the project. NEGATIVE CONTROL, measured against HEAD during this revision: this exact command prints `residual_prod_default_sites=18` and the eighteen sites enumerated in the action's table — so it is not a constant-pass, and a run printing 0 at HEAD would mean the gate itself is broken rather than the code being clean. The `bash -c` wrapper is REQUIRED and not stylistic: this repo's interactive shell is fish, where `for f in $VAR` does not word-split, and an earlier draft of this gate silently printed `0` while rg errored on a single concatenated filename — a false green produced by the shell dialect alone. The `rg -v` step strips commented-out matches, per CLAUDE.md's lesson that a grep over source counts comment prose.</fails_when>

    <automated>rg -n 'DEVELOP\b' crates/devflow-cli/src/commands.rs | rg -v '^[0-9]+:\s*//' ; echo "rg_exit=$?"</automated>
    <fails_when>any printed line whose content is a use of the constant as a VALUE rather than an import or a comparison against the resolved base — in particular a `unwrap_or_else`/`unwrap_or` default (the `commands::reference` shape) or a positional argument to a probe. Read the printed lines and judge each; this gate SURFACES sites, it does not adjudicate them, and the SUMMARY must list each surviving line with the reason it is legitimate. Negative control: at HEAD this prints the `reference` default at :552 among its matches, which is the site review round 2 found and a purely mechanical count would have passed over.</fails_when>

    <automated>rg -c 'behind develop' crates/devflow-cli/src/commands.rs; echo "rg_exit=$?"</automated>
    <fails_when>the absence of the line `rg_exit=1`. rg exits 1 on no match, which is the passing state: the hardcoded trunk name must be gone from that operator string. `rg_exit=0` plus a printed count means it survived. Negative control, measured at HEAD during this revision: this prints `1` and `rg_exit=0`.</fails_when>

    <automated>cargo test --workspace > target/45-01-suite.txt 2>&1; echo "cargo_exit=$?"; grep -c '^test result: FAILED' target/45-01-suite.txt; true</automated>
    <fails_when>the line `cargo_exit=0` is absent, OR the grep does not print a line reading exactly `0`. Both directions were measured against fixtures this session: a green suite prints `0`, a red suite prints `1` — the two directions differ in stdout, which is what makes this a real check. The trailing `; true` is load-bearing and NOT cosmetic: `grep -c` exits **1** when the count is zero, so without it the GREEN case exits non-zero and any harness that aborts on exit status kills a passing run before reading stdout (measured). A missing count line means the redirect file was never written — also a failure. Do NOT substitute the Phase-44 form `| rg -c '^test result: FAILED' | rg '^0$'`: `rg -c` prints nothing and exits 1 on zero matches, so that pipeline can never pass on a green suite.</fails_when>

    <automated>cargo clippy --workspace --all-targets -- -D warnings > target/45-01-clippy.txt 2>&1; echo "clippy_exit=$?"; grep -c '^error' target/45-01-clippy.txt; true</automated>
    <fails_when>the line `clippy_exit=0` is absent (clippy's OWN exit status is the primary signal), OR the grep does not print a line reading exactly `0`, OR no count line is printed at all. The previous form of this check — piping clippy's output into a `Finished|error` alternation — was a TRUE CONSTANT-PASS and was measured as such this session: it exits 0 for a clean clippy AND for a failing clippy, because the alternation matches the literal failure text. Never reintroduce it. Negative control for the replacement, also measured: a clean fixture yields `clippy_exit=0` then `0`; a failing fixture yields a non-zero `clippy_exit` then a non-zero count — the two directions differ on BOTH signals.</fails_when>
  </verify>

  <acceptance_criteria>
    - `crates/devflow-core/src/hooks.rs` contains `GitFlow::with_config(&ctx.project_root, ctx.git_flow.clone())` at each of the six sites previously using `GitFlow::new(&ctx.project_root)` (lines 125, 132, 181, 239, 278, 289 at HEAD).
    - `crates/devflow-cli/src/pipeline_outcomes.rs`'s production `HookContext` construction populates `git_flow` from `config::git_flow_for_project`, not `GitFlowConfig::default()`.
    - `crates/devflow-core/src/monitor.rs`'s `enumerate_phase_commits` and `crates/devflow-cli/src/pipeline_launch.rs:1471` both resolve their `GitFlowConfig` from the project, not from `default()`.
    - `enumerate_phase_commits` takes a `project_root` parameter distinct from `workdir`, resolves its config from `project_root`, and still runs its `git log` in `workdir`. Resolving from `workdir` does not satisfy this criterion: a phase worktree need not carry a `devflow.toml` that exists only in the main checkout, and the fallback is silent.
    - The residual-production-site audit prints `residual_prod_default_sites=0` and no file:line rows. The same command prints `18` against HEAD; if it prints `0` before any conversion work, the gate is broken and must be fixed before it is trusted.
    - `commands::reference`'s branch default resolves from the project rather than from the `DEVELOP` constant, and the message it prints names the branch it actually snapshotted.
    - The string `behind develop` does not appear in `crates/devflow-cli/src/commands.rs`; the branch name in that operator string is interpolated from the resolved base.
    - No edit in this task targets an `evaluate_agent_result` call site in `pipeline_outcomes.rs`. That call site does not exist — the identifier appears there only as two test function names inside the `#[cfg(test)]` module. If the executor spent time searching for it, say so in the SUMMARY.
    - `crates/devflow-core/src/gsd_config.rs:320` still uses `GitFlow::new`, and the SUMMARY states why (uses only `commit_path`; trunk-irrelevant).
    - `merge_feature_targets_the_configured_base_not_the_default` asserts BOTH that the configured base received the commit AND that `develop` did not. A single-direction assertion does not satisfy this criterion.
    - `hook_context_git_flow_is_not_discarded` fails against HEAD's code (where `ctx.git_flow` is re-defaulted). Record the observed pre-fix failure output in the SUMMARY as the RED half of the cycle.
    - `cargo test --workspace` reports `cargo_exit=0` and a `^test result: FAILED` count of 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` reports `clippy_exit=0` and a `^error` count of 0. Asserting on a `Finished` line instead is forbidden — that form was measured this session to pass in both directions.
  </acceptance_criteria>

  <done>
    Every production consumer of the git-flow trunk resolves it from the project configuration,
    `HookContext.git_flow` is genuinely read rather than re-defaulted, the merge target provably
    equals the fork point, and the full workspace suite plus clippy are clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ambient environment → DevFlow config resolver | `DEVFLOW_BASE_BRANCH` is read from the process environment, which a CI runner, a parent shell, or a `.envrc` can set without the operator noticing |
| `devflow.toml` on disk → DevFlow config resolver | a committed config file changes trunk for every clone of the repository |
| resolved branch name → `git` argv | the base value is passed to `git worktree add` and `git merge-base` as a positional argument |
| resolved branch name → operator output + `.devflow/events.jsonl` | a personal branch name can carry a username into persisted artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-45-01 | Elevation of Privilege | `config::base_branch` → `worktree::add` / `merge_feature_into_develop` | high | mitigate | `validate_base_branch` refuses a value equal to `config::MAIN`, so the knob cannot be used to fork from or merge into the production branch (Task 1) |
| T-45-02 | Tampering | `env_value("DEVFLOW_BASE_BRANCH")` | medium | mitigate | Task 1 prints an operator-visible note whenever the resolved base differs from the default, naming the branch and its source (env vs file), so an ambient redirect is never silent |
| T-45-03 | Tampering | resolved branch name → `git` argv | medium | mitigate | `validate_base_branch` rejects a value whose first byte is `-`, closing the argv-flag-injection shape before the value reaches any `git` invocation (Task 1) |
| T-45-04 | Information Disclosure | preflight messages, `.devflow/events.jsonl` | medium | mitigate | Task 1 acceptance criteria forbid absolute paths and derived usernames in any new operator string; DevFlow echoes only the operator-supplied branch name and never derives one from the host (WR-02 / 999.10) |
| T-45-05 | Spoofing | `base_branch` naming a branch that does not exist | low | mitigate | `ensure_phase_reachable_on_base` already refuses before any git mutation; Task 1 routes the resolved value into that existing guard rather than bypassing it |
| T-45-06 | Repudiation | merge target diverging from fork point | high | mitigate | Task 3's call-site audit makes both resolve from `config::git_flow_for_project`, and `merge_feature_targets_the_configured_base_not_the_default` asserts both directions |
| T-45-07 | Denial of Service | a local-only planning branch failing the currency probe | medium | accept | The existing `Undeterminable` fail-open arm is the accepted disposition; Task 2 pins it with a test rather than adding new handling. Accepted because the alternative — refusing every launch from an unpushed branch — is the AUTO-01 defect itself |
| T-45-08 | Tampering | npm/pip/cargo installs | high | accept | This plan adds no new dependency to any manifest; no package-legitimacy gate is required. If an executor finds itself adding a crate, stop and escalate — the phase has no RESEARCH.md and therefore no Package Legitimacy Audit table |
</threat_model>

<verification>
- `cargo test --workspace` reports `cargo_exit=0` and zero `^test result: FAILED` lines.
- `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` are clean.
- With no `devflow.toml` and no `DEVFLOW_BASE_BRANCH`, `config::base_branch` returns the value of `DEVELOP` — the zero-regression guard for every existing project.
- `doc_check::source_devflow_env_vars_and_subcommands_are_documented` passes, proving `DEVFLOW_BASE_BRANCH` is documented in a scoped operator doc.
- The RED half of Task 2's and Task 3's TDD cycles is recorded in the SUMMARY as observed failure output, not asserted from memory.
</verification>

<success_criteria>
AUTO-01 is satisfied: a project whose `.planning/` lives on a branch other than `develop`
can set `base_branch` (or export `DEVFLOW_BASE_BRANCH`), and `devflow start --phase N`
creates a worktree that contains `.planning/config.json`, so
`preflight_unattended_launch_check` passes without operator intervention. The branch forked
from and the branch merged into are the same resolved value. Default behaviour is unchanged.
</success_criteria>

<output>
Create `.planning/phases/45-unattended-auto-mode-hardening-999-110-999-109-999-94/45-01-SUMMARY.md` when done.
</output>
