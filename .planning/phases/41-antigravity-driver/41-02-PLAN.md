---
phase: 41-antigravity-driver
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - crates/devflow-cli/tests/phase7_cli.rs
  - scripts/check-in-container.sh
autonomous: true
requirements: [HYG-01, HYG-02]
estimate:
  tokens: 30000
  raw_tokens: 15000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Every test in phase7_cli.rs that spawns `devflow start` reaps ITS OWN monitor by PID — unwind-safe guard keyed to `state.monitor_pid`, TERM→KILL via `devflow_core::agent::terminate_and_verify`, death VERIFIED (the `ReapMonitorOnDrop` pattern, test_support.rs:573; `MonitorReapGuard` is DEFINED in 41-01 Task 8, antigravity notice (a))"
    - "A SUITE-LEVEL registered-PID audit proves the leak gate can fail: `run_devflow` auto-registers the monitor PID in a suite registry; the audit test asserts the registry drains (every guard ran); a testable intentional opt-out demonstrates a deliberately leaked PID is DETECTED (codex-4 — per-test Drop guards alone cannot detect an unguarded test)"
    - "No `ps aux | grep devflow` counting anywhere — racy/self-matching and reaps nothing (round-1 finding 8)"
    - "`check-in-container.sh` works from a git WORKTREE AND the main checkout — the failure is the worktree `.git`-file mount, NOT uid 0, NOT '3 git-env tests under root' (D-06, round-1 finding 4); re-derived from real container runs"
    - "No test file gains `skip_if_root()` or `#[ignore]` for uid 0 — CI runs root over a normal checkout where these tests guard real regressions (CR-01/WR-07, protected-branch hook)"
  artifacts:
    - `crates/devflow-cli/tests/phase7_cli.rs` — MonitorReapGuard (defined in 41-01 Task 8) bound in every monitor-spawning test + suite registry/audit + intentional opt-out control
    - `scripts/check-in-container.sh` — worktree-aware container mount, validated by real container runs
  key_links:
    - "`state.monitor_pid` ↔ MonitorReapGuard ↔ `terminate_and_verify` (bounded TERM→KILL, verified death)"
    - "`run_devflow` registry ↔ suite audit test ↔ intentional opt-out — the suite-level leak gate (codex-4)"
    - "worktree `.git` file ↔ `check-in-container.sh` REPO_ROOT mount ↔ in-container `fatal: not a git repository`"
    - "main checkout under uid 0 (passes) ↔ worktree under uid 0 (fails) — the negative control isolating the mount (D-06)"
assumption_delta_decision: "round-3 refinement — 41-01 Task 8 now defines MonitorReapGuard (forward-ref resolved, antigravity notice (a)); Task 1 gains the suite-level registered-PID audit + intentional opt-out per codex-4. HYG-02 unchanged from round 2 (D-06)"
---

<objective>
Close the two dogfood-hygiene defects surfaced by Phase 40's real supervised run: leaked test
monitor processes (HYG-01) and in-container git failures (HYG-02). Round-3 refinement: 41-01 Task 8
defines `MonitorReapGuard` (the forward-reference from round-2 docs is resolved); this plan does the
systematic pass across ALL pre-existing phase7_cli tests and adds the SUITE-LEVEL registered-PID
audit that round-2 codex-4 required (per-test Drop guards cannot detect an unguarded test). HYG-02
stays the re-derived worktree-mount fix (D-06) — never the fabricated "3 git-env tests under root".

Purpose: the full `cargo test -p devflow --test phase7_cli` run leaves 0 suite-spawned monitors,
provably (audit), and `check-in-container.sh` passes from both a worktree and the main checkout
without disabling any CI guard.

Output:
- `MonitorReapGuard` bound in every monitor-spawning test (systematic pass) + suite registry/audit +
  intentional opt-out control (HYG-01)
- `check-in-container.sh` fixed for the worktree case, re-derived from real container runs (HYG-02)
</objective>

<execution_context>
@.planning/CONTEXT.md (D-06: HYG-02 re-derived; the reap-model citations)
@.planning/reviews/phase-41/review-2/SUMMARY.md + codex.md (codex-4: suite-level audit) + antigravity.md (notice (a): MonitorReapGuard defined in 41-01 Task 8)
@CLAUDE.md (verification habits: real `1 passed` + non-zero `filtered out`; devflow binary-only → `--test phase7_cli`)
@crates/devflow-cli/src/test_support.rs (ReapMonitorOnDrop:573 / reap_monitor_pid:509 — the per-PID model; public devflow-core APIs only — no lib.rs)
@crates/devflow-cli/tests/phase7_cli.rs (the suite to harden; MonitorReapGuard defined in 41-01 Task 8)
@scripts/check-in-container.sh (REPO_ROOT:17; single mount:69)
@.planning/phases/41-antigravity-driver/41-01-PLAN.md (Task 8 — MonitorReapGuard definition; do not redefine)
</execution_context>

<context>
@crates/devflow-cli/tests/gitignore_coverage.rs / ci_parity_guards.rs / pre_commit_branch_guard.rs (UNTOUCHED — no `git config --global`, no git in ci_parity_guards, guards CR-01/WR-07 under CI root)
@crates/devflow-core/src/agent.rs (terminate_and_verify, agent_running, discover_stray_devflow_processes)
@crates/devflow-core/src/state.rs (state.monitor_pid)
@.planning/ROADMAP.md § Phase 41 criteria 5-6
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Systematic MonitorReapGuard pass + suite-level registered-PID audit (HYG-01, codex-4)</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    crates/devflow-cli/tests/phase7_cli.rs (entire file) — every test that spawns `devflow start` via run_devflow; helpers
    crates/devflow-cli/src/test_support.rs (495-630) — ReapMonitorOnDrop / reap_monitor_pid (the model; its own negative control at 779-800)
    crates/devflow-cli/tests/reap_strays_e2e.rs (1-60) — how this repo tests reaping WITHOUT acting on unrelated processes (the "never act on anything it did not spawn" rule)
    41-01-PLAN.md Task 8 — MonitorReapGuard (already defined; reuse, do not redefine)
  </read_first>
  <behavior>
    - Test 1 (systematic pass): EVERY pre-existing test in phase7_cli.rs that spawns `devflow start` binds `MonitorReapGuard::after_launch(&state)` strictly after its final `&mut state` use — including the tests added in 41-01 Task 8 (which already bind it)
    - Test 2 (RED then GREEN, codex-4 — suite-level audit): `run_devflow` (and the 41-01 helper) auto-REGISTERS each spawned monitor's PID in a suite-level registry (`static REGISTRY: OnceLock<Mutex<HashSet<u32>>>`); a suite audit test `suite_reap_audit` polls until the registry drains (bounded) and asserts it is EMPTY — proving every registered monitor was reaped. The audit LOGIC is a pure helper (`assert_no_registered_monitors_alive`) unit-tested to FAIL against a deliberately-alive registered PID — the gate can fail
    - Test 3 (intentional opt-out, codex-4): a test opts OUT of auto-reap/registration (an explicit `reap_disabled` flag on the run helper), demonstrates the monitor stays alive, then cleans up — proving an unguarded test is DETECTABLE at the suite level, not just that a monitor can outlive a launch (round-2's control was weaker)
    - Test 4: no `ps aux | grep devflow` counting anywhere; all reaping is per-PID with verified death (TERM→KILL escalation — 999.44 measured 15/15 bare-SIGTERM survivors)
    - Test 5: a full default-parallel `cargo test -p devflow --test phase7_cli` run ends with the registry empty and 0 suite-spawned monitors alive
  </behavior>
  <action>
    In `crates/devflow-cli/tests/phase7_cli.rs`:
    1. Reuse `MonitorReapGuard` from 41-01 Task 8 — no redefinition. If 41-01 Task 8 named it differently or bound it inline, standardise on the shared helper.
    2. Add the suite registry: `static REGISTRY: std::sync::OnceLock<std::sync::Mutex<std::collections::HashSet<u32>>>`; `run_devflow_inner` (and any direct `Command::new(devflow_bin())` spawn that launches a monitor) registers the PID read from `load_state` after launch — unless the test passed the explicit `reap_disabled` opt-out (Test 3 only). Registration is best-effort (a test that fails before launch must not poison the registry).
    3. Extend `MonitorReapGuard` (or add a suite guard) so drop also DEREGISTERS the PID from the registry after the verified reap — the audit's empty-registry assertion then means "every guard ran".
    4. Add `suite_reap_audit` — polls `REGISTRY` until empty (bounded, e.g. 20s) then asserts empty; if any PID survives, panic naming the PID. The audit's detection helper is unit-tested: feed it a live registered PID → assert failure (RED), then reap → green.
    5. Add the intentional opt-out control test (Test 3) — bound, asserted, cleaned up; it is the suite-level analogue of the binary's own control (test_support.rs:779-800).
    6. Remove any round-2 `unreaped_monitor_is_detected_by_the_guard`-style control that only proved "a monitor can outlive a launch" (codex-4) — replace it with the suite-level detection proof.
    7. Do NOT add `ps aux | grep devflow` counting.
  </verify>
  <automated>cargo test -p devflow --test phase7_cli -- --nocapture</automated>
  <done>
    - Every monitor-spawning test binds MonitorReapGuard (systematic pass incl. 41-01 Task 8 tests)
    - Suite registry + `suite_reap_audit` (empty-registry assertion) + detection helper with RED/GREEN proof
    - Intentional opt-out control proves an unguarded test is detectable at suite level
    - Full default-parallel `--test phase7_cli` passes with registry empty; 0 suite-spawned monitors alive; no ps-count
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-derive and fix check-in-container.sh for the worktree case (HYG-02) — NOT the three test files, NOT skip_if_root</name>
  <files>scripts/check-in-container.sh</files>
  <read_first>
    scripts/check-in-container.sh (lines 14-22) — REPO_ROOT = `git rev-parse --show-toplevel`
    scripts/check-in-container.sh (lines 60-78) — the docker run + `-v "$REPO_ROOT":/workspace` mount
    .planning/reviews/phase-41/claude.md (round-1 finding 4) — container negative controls: worktree → `fatal: not a git repository`; main checkout as uid 0 → PASS
    crates/devflow-cli/tests/gitignore_coverage.rs / ci_parity_guards.rs / pre_commit_branch_guard.rs (verify for yourself: NO `git config --global`; ci_parity_guards makes no git calls)
  </read_first>
  <behavior>
    - Test 1 (re-derivation, do first): pinned image (`mcr.microsoft.com/devcontainers/rust:2.0.13-1-bookworm`), run from the WORKTREE → reproduce `fatal: not a git repository` on the git-dependent tests
    - Test 2 (negative control): same image, MAIN checkout as uid 0 → all pass (cause is the mount, not root)
    - Test 3 (RED then GREEN): after the script fix, worktree run passes AND main-checkout run still passes
    - Test 4: none of the three test files changed; no `skip_if_root()` / `#[ignore]` anywhere for uid 0
  </behavior>
  <action>
    **Step 1 — re-derive (round-1 finding 4, unchanged in round 2/3):**
    1. Worktree: `docker run --rm -v $(git rev-parse --show-toplevel):/workspace -w /workspace <IMAGE> git -C /workspace status` → `fatal: not a git repository`. Cause: the worktree `.git` is a FILE (`gitdir: <main>/.git/worktrees/<N>`); the mount exposes only the worktree; the gitdir target is outside the container.
    2. Main checkout: same command → passes. Record both observations in the script doc comment + commit message.
    **Step 2 — fix the script (Option A preferred):**
    - **Option A:** when `REPO_ROOT` is a worktree (`git rev-parse --git-dir` resolves OUTSIDE `REPO_ROOT`), bind-mount the main repo's `.git` at its absolute host path inside the container (extra `-v <main>/.git:<main>/.git`). Verify worktree AND main-checkout runs pass.
    - **Option B:** resolve the container workspace to the main checkout when invoked from a worktree (CI-parity-equivalent; document the caveat).
    - **Option C:** refuse worktrees with a clear error (only if A/B infeasible; record why).
    **Step 3 — explicitly do NOT:**
    - No changes to `gitignore_coverage.rs` / `ci_parity_guards.rs` / `pre_commit_branch_guard.rs` (they guard CR-01/WR-07 and the protected-branch hook; GitHub Actions runs root over a normal checkout where they pass — a root skip would silently disable them).
    - No `skip_if_root()`; no `git config --global → --local` rewrites (no such usage exists).
    **Step 4 — verify with real container runs:**
    1. Worktree: `bash scripts/check-in-container.sh all` → exit 0.
    2. Main checkout: `bash scripts/check-in-container.sh all` → exit 0.
    3. Local (uid 1000): full suite unchanged and green.
  </verify>
  <automated>bash scripts/check-in-container.sh all</automated>
  <done>
    - Failure reproduced from the WORKTREE and absent from the MAIN checkout (re-derivation recorded)
    - Script fixed per Option A/B; worktree and main-checkout container runs both exit 0
    - Zero changes to the three test files; zero `skip_if_root()`/`#[ignore]` additions
    - Local full suite green; commit message cites the review evidence
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → spawned monitor | Test spawns `devflow start` (detached `__monitor` wrapper); the test owns reaping its own monitor PID |
| Suite registry | Cross-test record of spawned monitor PIDs; the audit asserts it drains |
| Test environment → container mount | `check-in-container.sh` mount must include every path git resolves (worktree gitdir included) |
| CI (uid 0) → repo checkout | GitHub Actions runs root over a NORMAL checkout; tests must keep guarding real regressions there |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-HYG-01 | Denial | Orphaned monitors accumulate during the test suite (Phase 40 leaked 43) | medium | mitigate | Per-test `MonitorReapGuard` (TERM→KILL, verified death) + SUITE-LEVEL registered-PID audit (codex-4): run_devflow auto-registers; `suite_reap_audit` asserts the registry drains; intentional opt-out proves the gate can fail |
| T-41-HYG-02 | Availability | `check-in-container.sh` fails from a git worktree (gitdir outside the mount) | medium | mitigate | Worktree-aware mount (Option A/B) re-derived from real container runs; main-checkout negative control stays green |
| T-41-HYG-03 | Availability | Root-skip guard silently disables working CI checks | high | mitigate | Explicit prohibition: no `skip_if_root()`/`#[ignore]` for uid 0; the three test files are untouched |
| T-41-HYG-04 | Availability | Racy/self-matching process counting reports a false clean or false failure | medium | mitigate | No `ps aux | grep devflow` counting anywhere; per-PID + registry verification only |

**Disposition rationale:**
- **T-41-HYG-01:** per-test guards are necessary but not sufficient (codex-4) — the suite registry + audit makes an unguarded test detectable, and the intentional opt-out control proves the gate can fail.
- **T-41-HYG-02/03/04:** unchanged from rounds 1-2 (D-06); the re-derived mount fix, the root-skip prohibition, and the per-PID-only verification.

**Related ASVS controls:** None directly (test infrastructure and operational concerns).
</threat_model>

## Artifacts this phase produces

### Code Artifacts

1. `crates/devflow-cli/tests/phase7_cli.rs` — `MonitorReapGuard` (defined in 41-01 Task 8, reused here) bound in every monitor-spawning test; suite registry; `suite_reap_audit`; intentional opt-out control; detection-helper RED/GREEN test.
2. `scripts/check-in-container.sh` — worktree-aware container mount with the re-derivation recorded in a doc comment.

**Unchanged (deliberately):** `gitignore_coverage.rs`, `ci_parity_guards.rs`, `pre_commit_branch_guard.rs`.

### Semantic Artifacts

- **Process hygiene contract:** every phase-7 monitor reaped by PID with verified death; the suite audit PROVES no unguarded test leaked (HYG-01)
- **Container parity contract:** `check-in-container.sh` passes from both a worktree and the main checkout under the pinned image (HYG-02), without weakening any test

### Non-Code Artifacts

- Re-derivation record (worktree fails / main checkout passes) in the script doc comment + commit message
