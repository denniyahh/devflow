---
phase: 41-antigravity-driver
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - crates/devflow-cli/tests/phase7_cli.rs
  - scripts/check-in-container.sh
autonomous: true
requirements: [HYG-01, HYG-02]
estimate:
  tokens: 28000
  raw_tokens: 14000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Every test in phase7_cli.rs that spawns `devflow start` reaps ITS OWN monitor by PID — bound an unwind-safe reap guard keyed to `state.monitor_pid` (the `ReapMonitorOnDrop` pattern, test_support.rs:573) that escalates TERM→KILL via `devflow_core::agent::terminate_and_verify` and VERIFIES death — NOT a global `ps aux | grep devflow` count (racy, self-matching, and reaps nothing; review findings 8/codex-6)"
    - "The reap assertion can fail: a negative control test deliberately skips reaping and is DETECTED (the gate is real, not vacuous)"
    - "A full default-parallel `cargo test -p devflow --test phase7_cli` run leaves 0 suite-spawned monitors — per-PID verified, not process-count inferred"
    - "`check-in-container.sh` works when invoked from a git WORKTREE (the phase harness context that surfaced the bug) AND from the main checkout — the failure is a worktree `.git`-file mount problem, NOT uid 0, and NOT '3 git-env tests that fail as root' (fabricated premise; D-06)"
    - "No test file gains `skip_if_root()` or `#[ignore]` for uid 0 — GitHub Actions runs root over a normal checkout where these tests pass and guard real regressions (CR-01/WR-07, protected-branch hook); a root skip would silently disable them"
  artifacts:
    - `crates/devflow-cli/tests/phase7_cli.rs` — per-test PID reap guard (ReapMonitorOnDrop pattern) + negative control test
    - `scripts/check-in-container.sh` — worktree-aware container mount (gitdir resolution) OR documented main-checkout requirement, validated by real container runs
  key_links:
    - "`state.monitor_pid` ↔ per-test reap guard ↔ `devflow_core::agent::terminate_and_verify` (bounded TERM→KILL, verified death)"
    - "worktree `.git` file (`gitdir: <main>/.git/worktrees/<N>`) ↔ `check-in-container.sh` `REPO_ROOT` mount ↔ in-container `fatal: not a git repository`"
    - "main checkout under uid 0 (passes) ↔ worktree under uid 0 (fails) — the negative control that isolates the mount as the cause (D-06)"
assumption_delta_decision: "rework — the pre-review 41-02 was based on two falsified premises (HYG-01: ps-count reaping; HYG-02: '3 git-env tests fail under root'). Both re-derived from review evidence; see 41-CONTEXT.md D-06"
---

<objective>
Close the two dogfood-hygiene defects surfaced by Phase 40's real supervised run: leaked test
monitor processes (HYG-01) and in-container git failures (HYG-02). This plan is the 2026-08-20
REWORK — the pre-review version was blocked by unanimous adversarial review (claude/codex/
antigravity, live-verified). HYG-01's `ps aux | grep devflow` count is racy, self-matching
(every cargo test binary path contains `devflow`), and reaps nothing; HYG-02's "3 git-env tests
fail under root via `git config --global`" premise is fabricated — no such usage exists in any of
the three named files, and the real cause is the worktree `.git`-file mount. Re-derived fixes:
per-PID monitor reaping (the repo's own `ReapMonitorOnDrop` pattern) and a `check-in-container.sh`
mount fix validated against both the worktree and the main checkout.

Purpose: Ensure the full `cargo test` suite runs with zero leaked monitors, and
`check-in-container.sh` passes whether invoked from a worktree or the main checkout — without
disabling any CI guard.

Output:
- Phase-7 tests reap their spawned monitors by PID, unwind-safe, with a negative control proving
  the gate fires (HYG-01)
- `check-in-container.sh` fixed for the worktree case; re-derived from real container runs against
  the main checkout and the worktree (HYG-02)
</objective>

<execution_context>
@.planning/CONTEXT.md (D-06: HYG-02 re-derived — worktree mount, NOT uid 0, NOT the three test files)
@.planning/reviews/phase-41/SUMMARY.md (findings 4, 7, 8 + codex findings 6-7 — the falsified premises and the evidence)
@CLAUDE.md (verification habits: assert a real `1 passed` with non-zero `filtered out`; devflow is binary-only so integration tests need `--test phase7_cli`; `--bin devflow` never reaches `tests/`)
@crates/devflow-cli/src/test_support.rs (ReapMonitorOnDrop, test_support.rs:573 — the per-PID, unwind-safe reap model; terminate_and_verify: bounded TERM→KILL, 999.44 measured 15/15 bare-SIGTERM survivors)
@scripts/check-in-container.sh (REPO_ROOT from `git rev-parse --show-toplevel` at :17; single `-v "$REPO_ROOT":/workspace` mount at :69)
@crates/devflow-cli/tests/phase7_cli.rs (the suite to harden; helpers: fake_bin_dir, run_devflow, load_state, wait_for_gate)
@.planning/RESEARCH.md + @.planning/PATTERNS.md (SUPERSEDED on HYG-01/HYG-02 claims — follow 41-CONTEXT.md instead)
</execution_context>

<context>
@crates/devflow-cli/tests/gitignore_coverage.rs (runs ONLY `git check-ignore` — no `git config --global`; passes as current user)
@crates/devflow-cli/tests/ci_parity_guards.rs (no git calls at all — static string assertions on workflow files)
@crates/devflow-cli/tests/pre_commit_branch_guard.rs (reads scripts/hooks/pre-commit + `git symbolic-ref --short HEAD` — no config writes)
@crates/devflow-core/src/agent.rs (terminate_and_verify, agent_running, discover_stray_devflow_processes — public primitives integration tests CAN use; test_support.rs itself is binary-internal, no lib.rs)
@crates/devflow-core/src/state.rs (state.monitor_pid — the per-test reap handle)
@.planning/ROADMAP.md § Phase 41 criteria 5-6 (corrected 2026-08-20)
@.planning/REQUIREMENTS.md § HYG-01, HYG-02 (HYG-02 wording "3 git-env tests that fail as root" superseded by D-06)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Per-test PID monitor reaping in phase7_cli.rs (HYG-01) — ReapMonitorOnDrop pattern + negative control</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <read_first>
    crates/devflow-cli/src/test_support.rs (lines 495-635) — reap_monitor_pid + ReapMonitorOnDrop (the exact model: PID capture BY VALUE, unwind-safe Drop, TERM→KILL escalation, verified death)
    crates/devflow-cli/tests/phase7_cli.rs (entire file) — every test that spawns `devflow start` via run_devflow; helpers (fake_bin_dir, run_devflow, load_state, wait_for_gate)
    crates/devflow-core/src/state.rs (state.monitor_pid — where the detached monitor's PID is persisted)
  </read_first>
  <behavior>
    - Test 1 (RED then GREEN): every `run_devflow()` test binds a reap guard immediately after launch (before the final `&mut state` use) keyed to `state.monitor_pid`; the guard reaps on drop even when a later assertion panics (unwind-safe)
    - Test 2 (negative control): a test that spawns a monitor and does NOT bind the guard is DETECTED — the control proves the assertion can fail, so a guard that silently never matches is not a false green (review finding 8: nothing in the pre-review plan demonstrated the gate could fail)
    - Test 3: after a full default-parallel `cargo test -p devflow --test phase7_cli` run, zero suite-spawned monitor PIDs remain alive (per-PID verified via `agent_running`)
    - Test 4: the reap uses TERM→KILL escalation (bare SIGTERM survives 15/15 against this process shape, 999.44) and verifies death — never an unverified assumption
  </behavior>
  <action>
    In `crates/devflow-cli/tests/phase7_cli.rs`:
    1. Add a local `MonitorReapGuard` mirroring `ReapMonitorOnDrop` (test_support.rs:573). Integration tests cannot link the binary's `test_support` module (devflow-cli has no lib.rs — CLAUDE.md) so the guard uses the PUBLIC devflow-core primitives: capture `state.monitor_pid` by value (`Option<u32>`), and on `Drop` call `devflow_core::agent::terminate_and_verify(pid, …)` then assert `!devflow_core::agent::agent_running(pid)` — panicking on the assertion is safe in the guard ONLY if it cannot run during an unwind (mirror the doc-comment reasoning at test_support.rs:504-508; if the guard may drop during unwind, keep the verify result and log rather than panic — match the binary's behavior exactly).
    2. Bind `let _reap = MonitorReapGuard::after_launch(&state);` in EVERY test that spawns `devflow start` (all `run_devflow` callers, including the two new antigravity tests from 41-01 Task 6), strictly AFTER the test's final `&mut state` use. The constructor name `after_launch` carries the ordering requirement (bound before the launch call returns, it captures `None` and silently does nothing — same trap as the binary's version).
    3. Remove/do not add any `ps aux | grep devflow` counting helper — it is racy under default-parallel cargo test (sibling test processes match `grep devflow` via their own argv), self-matching, and reaps nothing.
    4. Add the negative control test `unreaped_monitor_is_detected_by_the_guard` modeled on the binary's own control (`test_support.rs:779-800`): spawn via run_devflow, deliberately bind NO guard, assert the monitor is still running, then reap it with the guard to clean up — proving the guard's absence is detectable.
    5. Where a test's assertions run before the launch returns (e.g. `wait_for_gate`), bind the guard as soon as the first `State` with a `monitor_pid` is available.
  </verify>
  <automated>cargo test -p devflow --test phase7_cli -- --nocapture</automated>
  <done>
    - `MonitorReapGuard` exists in phase7_cli.rs; bound in every monitor-spawning test (incl. 41-01's antigravity tests)
    - Negative control test proves an unreaped monitor is detected (the gate can fail)
    - No `ps aux | grep devflow` counting anywhere in the suite
    - Full `cargo test -p devflow --test phase7_cli` (default parallel) passes with 0 suite-spawned monitors left alive (per-PID verified)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-derive and fix check-in-container.sh for the worktree case (HYG-02) — NOT the three test files, NOT skip_if_root</name>
  <files>scripts/check-in-container.sh</files>
  <read_first>
    scripts/check-in-container.sh (lines 14-22) — REPO_ROOT = `git rev-parse --show-toplevel`
    scripts/check-in-container.sh (lines 60-78) — the docker run + `-v "$REPO_ROOT":/workspace` mount
    .planning/reviews/phase-41/claude.md (finding 4) — the container negative controls: worktree mount → `fatal: not a git repository` exit 128; MAIN checkout as uid 0 → PASS
    crates/devflow-cli/tests/gitignore_coverage.rs + ci_parity_guards.rs + pre_commit_branch_guard.rs (the "3 git-env tests" — verify for yourself they contain NO `git config --global` and pass as the current user)
  </read_first>
  <behavior>
    - Test 1 (re-derivation, do first): in the pinned image (`mcr.microsoft.com/devcontainers/rust:2.0.13-1-bookworm`), run `check-in-container.sh` from the WORKTREE → reproduce the failure (`fatal: not a git repository` on the git-dependent tests)
    - Test 2 (negative control): same image, from the MAIN checkout as uid 0 → all pass (proves the cause is the mount, not root — the pre-review "fix root" premise is falsified)
    - Test 3 (RED then GREEN): after the script fix, running from the WORKTREE passes; running from the MAIN checkout still passes (no regression)
    - Test 4: none of the three test files changed; no `skip_if_root()` / `#[ignore]` added anywhere for uid 0
  </behavior>
  <action>
    **Step 1 — re-derive, don't assume (review finding 4):**
    1. Run the pinned image against the WORKTREE: `docker run --rm -v $(git rev-parse --show-toplevel):/workspace -w /workspace <IMAGE> git -C /workspace status` → observe `fatal: not a git repository`. Confirm the cause: the worktree's `.git` is a FILE (`gitdir: <main>/.git/worktrees/<N>`), and the mount exposes only the worktree — the gitdir target is outside the container.
    2. Run the same against the MAIN checkout → passes. Record both observations in the commit message / script doc comment.
    **Step 2 — fix the script (choose one, prefer A):**
    - **Option A (recommended): make the container mount the gitdir too.** When `REPO_ROOT` is a worktree (`git rev-parse --git-dir` resolves OUTSIDE `REPO_ROOT`), bind-mount the main repo's `.git` directory at its absolute host path inside the container (e.g. an extra `-v <main>/.git:<main>/.git`) so git operations resolve. Verify both the worktree and main-checkout runs pass.
    - **Option B: resolve the container workspace to the main checkout when invoked from a worktree** (document the caveat that the in-container tree is then the main checkout, not the worktree — the run's purpose is CI parity, and CI runs on a normal checkout, so this is behaviorally equivalent for the tests).
    - **Option C: refuse worktrees with a clear error** (only if A/B prove infeasible) — record why in the script comment.
    **Step 3 — explicitly do NOT:**
    - Do not modify `gitignore_coverage.rs` / `ci_parity_guards.rs` / `pre_commit_branch_guard.rs` (they are correct as written; they guard CR-01/WR-07 and the protected-branch hook, and GitHub Actions runs root over a normal checkout where they pass — a root skip or `#[ignore]` would silently disable them).
    - Do not add `skip_if_root()` anywhere (review finding 4: "actively harmful").
    - Do not "fix" the three files with `git config --global → --local` (no such usage exists — Approach A of the pre-review plan is inapplicable).
    **Step 4 — verify with real container runs:**
    1. Worktree: `bash scripts/check-in-container.sh all` → exit 0, no git-env failures.
    2. Main checkout: `bash scripts/check-in-container.sh all` → exit 0 (negative control stays green).
    3. Local (uid 1000, no container): full suite unchanged and green.
  </verify>
  <automated>bash scripts/check-in-container.sh all</automated>
  <done>
    - Container failure reproduced from the WORKTREE and shown absent from the MAIN checkout (re-derivation recorded)
    - `check-in-container.sh` fixed per Option A/B; both worktree and main-checkout container runs exit 0
    - Zero changes to the three test files; zero `skip_if_root()`/`#[ignore]` additions
    - Local full suite green; commit message cites the review evidence for the re-derivation
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → spawned monitor | Test spawns `devflow start` (detached `__monitor` wrapper); the test owns reaping its own monitor PID |
| Test environment → container mount | `check-in-container.sh` mounts the repo into the pinned image; the mount must include every path git resolves (worktree gitdir included) |
| CI (uid 0) → repo checkout | GitHub Actions runs root over a NORMAL checkout; tests must keep guarding real regressions there |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-41-HYG-01 | Denial | Orphaned monitor processes accumulate during the test suite (Phase 40 leaked 43) | medium | mitigate | Per-test PID reap guard (`state.monitor_pid` + `terminate_and_verify`, TERM→KILL escalation, verified death); negative control proves the gate can fail; per-PID verification after a full default-parallel run — no global ps-count |
| T-41-HYG-02 | Availability | `check-in-container.sh` fails from a git worktree (gitdir outside the mount) | medium | mitigate | Worktree-aware mount (Option A/B) re-derived from real container runs; main-checkout negative control stays green |
| T-41-HYG-03 | Availability | Root-skip guard silently disables working CI checks | high | mitigate | Explicit prohibition: no `skip_if_root()`/`#[ignore]` for uid 0; the three test files are untouched; the fix lives in the container script |
| T-41-HYG-04 | Availability | Racy/self-matching process counting reports a false clean or false failure | medium | mitigate | No `ps aux | grep devflow` counting anywhere; reap and verify by PID only |

**Disposition rationale:**
- **T-41-HYG-01:** The repo already owns the mechanism (ReapMonitorOnDrop, test_support.rs:573) — this plan adapts it to the integration suite with the same TERM→KILL escalation 999.44 proved necessary (15/15 bare-SIGTERM survivors) and the same unwind-safety guarantee.
- **T-41-HYG-02:** Re-derived per D-06: the failure is the worktree `.git` file outside the container mount, not uid 0. The main-checkout run under root is the negative control that isolates the cause.
- **T-41-HYG-03:** The pre-review plan's `skip_if_root()` is the highest-severity hazard of the rework — it would disable working CI guards (CR-01/WR-07, protected-branch hook) on the only environment that matters.
- **T-41-HYG-04:** Follows review finding 8/codex-6: process counts under default-parallel cargo test are nondeterministic and match sibling test binaries; per-PID verification is the only sound measure.

**Related ASVS controls:** None directly (test infrastructure and operational concerns, not application code security).
</threat_model>

## Artifacts this phase produces

### Code Artifacts

**New (in-test) helper:**
1. `crates/devflow-cli/tests/phase7_cli.rs` — `MonitorReapGuard` (ReapMonitorOnDrop pattern via public devflow-core primitives) + negative control test; guard bound in every monitor-spawning test, including 41-01 Task 6's antigravity tests.

**Modified files:**
2. `scripts/check-in-container.sh` — worktree-aware container mount (Option A: bind the main repo's `.git` gitdir; or Option B: resolve to the main checkout), with the re-derivation recorded in a doc comment.

**Unchanged (deliberately):**
3. `crates/devflow-cli/tests/gitignore_coverage.rs`, `ci_parity_guards.rs`, `pre_commit_branch_guard.rs` — no changes; no `skip_if_root()`/`#[ignore]`; they keep guarding CR-01/WR-07 and the protected-branch hook under CI root.

### Semantic Artifacts

- **Process hygiene contract:** every phase-7 monitor is reaped by PID with verified death — the full suite leaves 0 suite-spawned monitors (HYG-01 regression gate)
- **Container parity contract:** `check-in-container.sh` passes from both a worktree and the main checkout under the pinned image (HYG-02), without weakening any test

### Non-Code Artifacts

- Re-derivation record (worktree fails / main checkout passes) in the script doc comment + commit message
