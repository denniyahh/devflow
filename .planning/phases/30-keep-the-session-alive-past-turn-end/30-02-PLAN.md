---
phase: 30-keep-the-session-alive-past-turn-end
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-keep-the-session-alive-past-turn-end/30c-monitor-env-harness.py
  - .planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/
  - .planning/phases/30-keep-the-session-alive-past-turn-end/30c-VERDICT.md
autonomous: false
requirements: ["30c", "constraint-6"]
tags: ["experiment", "harness", "python", "claude-cli"]

must_haves:
  truths:
    - "The question 'does task-notification delivery survive DevFlow's real launch environment (detached, env-scrubbed, no TTY, separated stderr)?' has a recorded binary answer on disk, not an assumption."
    - "The harness replicates every spawn_monitor process characteristic it can replicate, and deviates deliberately in exactly ONE: it holds its own stdin pipe open. Residual environment inheritance it cannot remove (Claude-session marker variables, process ancestry) is enumerated BY NAME in the evidence and read against the operator-launched DevFlow environment, rather than asserted away."
    - "crates/devflow-core/src/monitor.rs is byte-identical to its pre-phase state — 30c reads it, never edits it."
    - "No raw, unsanitised CLI output is ever written into a committed path: raw stdout and stderr land in a run-scoped directory outside .planning/, and only validated, redacted, secret-scanned copies are published into 30c-evidence/."
    - "The archived evidence contains no absolute home paths, OS usernames, session identifiers, CLI task output-file paths, or credential-shaped tokens."
    - "delivery: confirmed requires BOTH delegated children to have been observably delivered — one delivered child and one lost child is not confirmation."
    - "If delivery is refuted, that outcome is recorded as a first-class result with a rejected-options table — not treated as an experiment failure to be retried until it succeeds."
  artifacts:
    - ".planning/phases/30-keep-the-session-alive-past-turn-end/30c-monitor-env-harness.py"
    - ".planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/raw_output.jsonl"
    - ".planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/stderr.log"
    - ".planning/phases/30-keep-the-session-alive-past-turn-end/30c-VERDICT.md"
  key_links:
    - "30c-VERDICT.md's `delivery:` field is the gate on Phase 31 ever being planned — if it reads `refuted`, Phase 31 is cancelled before planning per the locked ROADMAP decision."
    - "The harness reads the env-scrub list out of crates/devflow-core/src/git.rs at runtime rather than hardcoding a copy — a stale copy would silently test the wrong environment."
---

<objective>
Run the v3 background-task experiment once inside a faithful replica of
`spawn_monitor`'s production process environment, and record a binary answer to
the one question that gates Phase 31: **does the Claude CLI still deliver
`task-notification`-origin `result` events when it is not running inside an
interactive Claude Code session?**

Purpose: all three archived 30a captures ran inside an interactive session —
inherited environment, merged stderr, no worktree. Review finding M4 flags that
as the deciding untested variable. If delivery does not survive the production
environment, Phase 31's monitor rewrite rests on a premise that does not hold,
and the locked decision is that Phase 31 is **cancelled before it is planned**
rather than paid for. This experiment is deliberately sequenced ahead of the
expensive work.

Output: a standalone harness, its raw evidence, and `30c-VERDICT.md` carrying a
machine-readable `delivery: confirmed | refuted` field.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/30-keep-the-session-alive-past-turn-end/30-RESEARCH.md
@.planning/phases/30-keep-the-session-alive-past-turn-end/30-PATTERNS.md
@.planning/phases/30-keep-the-session-alive-past-turn-end/30a-evidence/README.md
</context>

<scope_fence>
**30c does NOT modify `spawn_monitor`.** This is a locked decision, stated twice
in the ROADMAP and once in RESEARCH.md. `crates/devflow-core/src/monitor.rs`,
`crates/devflow-core/src/git.rs`, and every other file under `crates/` are
**read-only references** for this plan. `files_modified` contains no Rust file.
The harness must not import or invoke DevFlow's binary either — it replicates
the environment, it does not drive DevFlow.

Note the stale attribution in `30a-evidence/README.md`: it says the production
test is "assigned to 30b's monitor plan". That predates the operator's phase
split. It is 30c, this plan, and there is no monitor plan in this phase at all.
</scope_fence>

<why_a_replica_and_not_spawn_monitor_itself>
The ROADMAP phrases this unit as "run the v3 harness once *through `spawn_monitor`
itself*". Taken literally that is impossible, and the reason is worth stating so
nobody re-litigates it mid-execution: `spawn_monitor` configures the child with
`.stdin(Stdio::null())` (`monitor.rs:171`). The entire experiment depends on
holding stdin OPEN past the first `result` event — that is what keeps the
session alive. A harness run through an unmodified `spawn_monitor` would have
its stdin closed at launch and could not perform the measurement at all, and
modifying `spawn_monitor` to allow it is explicitly forbidden.

So the experiment isolates the ENVIRONMENT variable instead: replicate every
process characteristic `spawn_monitor` imposes except the one under test. That
is a stronger experiment than the literal reading anyway — it changes one
variable rather than conflating environment with pipe ownership, which is
Phase 31's separate question.
</why_a_replica_and_not_spawn_monitor_itself>

<review_incorporation>
Cross-AI review (`30-REVIEWS.md`, codex/gpt-5.6-sol, 2026-08-02) raised four
findings against this plan. All four were re-verified against live source and
the archived evidence during replanning; all four HOLD and all four are
incorporated.

**HIGH — redaction cannot occur before the proposed evidence write. ACCEPTED,
fixed in Task 1.** The original action directed child stdout straight into
`30c-evidence/raw_output.jsonl` while a later paragraph required redaction
before anything reached that directory. OS-level redirection cannot be
intercepted by a redactor, so the two instructions were unsatisfiable together
and the executor would have resolved them by writing raw. The leak is not
hypothetical: `30a-evidence/raw_output_v3.jsonl` line 5's `init` event carries
`"cwd":"/tmp/claude-1000/-var-home-denniyahh-Github-devflow/…"` — the operator's
OS username, verbatim, in an already-committed artifact — and every event line
carries a live `session_id`, and `task_notification` events at lines 31 and 46
carry absolute `output_file` paths. Fix: stage raw output outside `.planning/`,
publish only sanitised copies.

**HIGH — "at least one" is too weak a confirmation gate for a two-child
experiment. ACCEPTED, fixed in Task 2.** Confirmed against the baseline: v3
produced THREE `result` events (lines 19, 37, 54), of which the latter two carry
`origin.kind == "task-notification"` — one per delegated child — plus a
`background_tasks_changed` drain to `tasks: []` at line 44. Phase 29's wave 2
lost BOTH executors; a gate satisfied by one delivered child and one lost child
would green-light Phase 31 on exactly the shape that has never worked.

**MEDIUM — "exactly one controlled variable" is unproven. ACCEPTED, truth
softened and an evidence requirement added in place of the assertion.** The
harness scrubs the two Git const lists only (`git.rs:27` and `git.rs:55`), and
`start_new_session=True` detaches a process without sanitising inherited
environment or erasing ancestry. The claim was stronger than the harness. It is
now stated as what is actually true and the residue is recorded by name.

**MEDIUM — credential validation narrower than the stated contract. ACCEPTED,
secret scan added.** The acceptance criteria checked home paths and username
while the must-have promised no credentials.

**Cross-plan item 5 — conditional stop.** Task 3's checkpoint now also records
an operator decision on whether the 30b expansion plans (30-03, 30-05) land if
delivery is refuted. Grounding: the shipped adapter still emits
`--output-format json` (`crates/devflow-core/src/agents/claude.rs`,
`exec_command`), so nothing in production produces the stream shape those plans
parse. That decision belongs to the operator, not to this plan's author, and the
prior wording ("Plans 30-01, 30-03 and 30-05 still land") pre-empted it.
</review_incorporation>

<tasks>

<task type="auto">
  <name>Task 1: Build the environment-replication harness</name>

  <files>.planning/phases/30-keep-the-session-alive-past-turn-end/30c-monitor-env-harness.py</files>

  <precondition>The `claude` CLI is on PATH and `claude --version` reports `2.1.220` — the version recorded in every archived capture's `init` event. A different version invalidates comparison against 30a's evidence; halt and report rather than silently measuring a different CLI.</precondition>

  <read_first>
    - `.planning/phases/30-keep-the-session-alive-past-turn-end/30a-evidence/run_experiment_v3.py` — the direct ancestor. It already implements stdin-held-open, `select()`-based non-blocking JSONL reads, and the two-concurrent-children prompt. Reuse its experiment core; this task changes only how the child is launched.
    - `crates/devflow-core/src/monitor.rs` lines 140-180 — the production launch: `hermetic_command("sh", workdir_path)`, `.arg("-c")`, the script string, and `.stdin/.stdout/.stderr(Stdio::null())` at lines 171-173. READ ONLY.
    - `crates/devflow-core/src/git.rs` lines 27-95 — `REPO_LOCAL_GIT_VARS` (const at line 27), `ALSO_REDIRECTING_GIT_VARS` (const at line 55), and `hermetic_command` (line 87), whose `env_remove` loop at line 90 chains both lists. READ ONLY.
  </read_first>

  <action>
    Write `30c-monitor-env-harness.py`, taking `run_experiment_v3.py`'s
    experiment core — the prompt that dispatches two concurrent background
    children at roughly 10s and 22s, the `select()`-based read loop, the signal
    file checks — and replacing its launch mechanics with a replica of
    `spawn_monitor`'s.

    Replicate these five characteristics, each a real difference between an
    interactive session and production:

    1. **Launch via `sh -c`**, not a direct `claude` exec — production builds
       `hermetic_command("sh", workdir)` then `.arg("-c").arg(&script)`.
    2. **Scrubbed environment.** Parse the two const string arrays out of
       `crates/devflow-core/src/git.rs` at harness runtime (read the file,
       extract the string literals declared under `REPO_LOCAL_GIT_VARS` and
       under `ALSO_REDIRECTING_GIT_VARS`) and remove every named variable from
       the child's environment. Parse them; do not transcribe them. RESEARCH.md's
       Pattern 3 warns explicitly against a hardcoded copy — a stale list means
       the harness silently tests an environment production does not impose. If
       parsing yields zero variables from either const, abort with a clear error
       rather than proceeding: an empty scrub is the failure mode that looks
       like success.
    3. **Detached process** — `start_new_session=True`, so the child sits in its
       own process group with no controlling terminal.
    4. **No TTY** on any stream.
    5. **stderr redirected to its own separate file**, never merged into stdout.
       Production separates them via the `sh` script's own redirection, and
       merged stderr is one of the three environment caveats
       `30a-evidence/README.md` names as untested.

    The single deliberate deviation, which must be commented as such in the
    source: the harness owns and holds open the child's **stdin** pipe. That is
    the variable under test.

    **Structure the launch as a reusable function, not inline script body.**
    Expose a module-level function that takes the workdir and the prompt payload
    and returns the launched process plus the paths it is writing to, so plan
    30-04's harness can import and reuse this exact launcher rather than
    reimplementing it against `run_experiment_v3.py`'s direct-exec shape. The
    module must remain safe to import — no experiment run at import time; put
    any driving code behind the standard `__main__` guard.

    **Raw output is staged OUTSIDE `.planning/`, never redirected into a
    committed path.** Create a run-scoped temporary directory (`tempfile.mkdtemp`
    is sufficient; record its path in the run log) and point the child's stdout
    and stderr redirection there. Tail the staged stdout file to read the JSONL,
    so the capture path still matches production — the `sh` script's redirection
    writes a file and the reader reads it back — while nothing unsanitised ever
    lands under `.planning/`.

    **Then sanitise and publish, in this order, as a distinct step after the run
    completes:**

    1. **Validate.** Every staged stdout line must parse as JSON. Record the
       count of lines that do not parse; a nonzero count is a finding for the
       verdict body, not something to silently drop.
    2. **Redact structurally, on the parsed objects, not with a regex over the
       raw text.** Replace the operator's home directory with `~` and the OS
       username with a placeholder wherever either appears in any string value;
       replace every `session_id` value with a stable pseudonym (identical input
       value maps to an identical placeholder, so the "all three `init` events
       share one session" fact stays checkable without publishing the real id);
       replace `output_file` path values and the `cwd` value with placeholders
       preserving only the final path component. Re-serialise the redacted
       objects to the published file.
    3. **Secret-scan the redacted output before publishing it.** Fail the
       publish — do not publish and do not proceed — if the sanitised text still
       contains the operator's home path, the OS username, any raw UUID matching
       a `session_id` seen in the staged capture, or a credential-shaped token
       (at minimum: `sk-` prefixed strings, `ghp_`/`gho_`/`ghs_` prefixed
       strings, `Bearer ` followed by a token, and any assignment whose key name
       contains `KEY`, `TOKEN`, `SECRET` or `PASSWORD`). Report which pattern
       matched, never the matched value itself.
    4. **Publish atomically** into `30c-evidence/raw_output.jsonl` and
       `30c-evidence/stderr.log` — write to a sibling temporary file in the same
       directory and `os.replace` it into position, so an interrupted publish
       cannot leave a half-sanitised file committed.

    This ordering is the whole point: the redactor cannot intercept an OS-level
    redirection, so the only way redaction can precede a committed write is for
    the committed write to be a separate, later step. This repository has a
    standing leak class — backlog 999.10, and finding WR-02 in the Phase 18
    review, both about absolute home paths and usernames reaching committed
    artifacts — and `30a-evidence/raw_output_v3.jsonl` is a live instance of it.

    Emit a run log — itself sanitised through the same pipeline before
    publishing — recording at minimum: the resolved `claude --version`; the
    NAMES of the variables actually removed from the child environment; the
    NAMES (never values) of any residual Claude-session marker variables still
    present in the child environment after the scrub, plus the parent process
    name and whether the harness itself was launched from an interactive agent
    session; wall-clock timestamps for launch / first `result` event / each later
    `result` event / stdin close / process exit; and the `origin` field of every
    `result` event observed. The residual-environment record is what turns
    "exactly one controlled variable" from an assertion into something a reader
    can check — see the `<review_incorporation>` block above.

    Do not have the harness print its own verdict. 30a's README records that the
    harness-printed verdict was wrong in v1 and unreliable in v2, and that the
    raw JSONL is the evidence of record. The harness reports observations;
    Task 2 interprets them.
  </action>

  <verify>
    <automated>cd /var/home/denniyahh/Github/devflow && python3 -c "import ast; ast.parse(open('.planning/phases/30-keep-the-session-alive-past-turn-end/30c-monitor-env-harness.py').read()); print('parsed ok')"</automated>
  </verify>

  <acceptance_criteria>
    - `30c-monitor-env-harness.py` parses under `python3 -c "import ast; ast.parse(...)"` and the command prints `parsed ok`.
    - The module is importable without side effects: `python3 -c "import importlib.util,sys; …"` loading it as a module runs no experiment and launches no process (driving code sits behind a `__main__` guard).
    - The module exposes a launcher function that plan 30-04 can import, taking a workdir and prompt payload and returning the launched process plus its staged output paths.
    - The harness sources its scrub list by reading `crates/devflow-core/src/git.rs` at runtime: running it with that file temporarily unreadable produces the explicit abort error, not a silent run with an empty scrub.
    - The harness launches its child through `sh -c` with `start_new_session=True`, and routes child stderr to a path distinct from the stdout path.
    - Neither the child's stdout nor its stderr is redirected to any path under `.planning/`. The staged directory is created via a temp-directory API and its path is recorded in the run log. This is checkable by inspecting the redirection targets: they resolve outside the repository working tree.
    - The publish step is a separate function that runs after the child exits and performs validate → structurally redact → secret-scan → atomic `os.replace` into `30c-evidence/`, in that order, and raises rather than publishing when the secret scan matches.
    - Feeding the publish step a synthetic staged file containing the operator home path, an OS username, a UUID `session_id`, and an `sk-`-prefixed token produces either a sanitised output containing none of them or a raised failure — verified by running that fixture through the function, not by reading the source.
    - `git status --porcelain crates/` reports no modifications — no file under `crates/` was edited by this task.
    - A dry inspection of the harness source shows the stdin pipe is held open and is annotated as the single deliberate deviation from `spawn_monitor`'s shape.
  </acceptance_criteria>

  <reversibility rating="reversible">A new standalone script under `.planning/`; touches no shipped code.</reversibility>

  <done>The harness exists, parses, is importable without side effects, replicates the five named environment characteristics, derives its env-scrub list from live source rather than a transcribed copy, stages raw output outside `.planning/`, and publishes into `30c-evidence/` only through a validate → redact → secret-scan → atomic-replace pipeline proven against a synthetic leaky fixture.</done>
</task>

<task type="auto">
  <name>Task 2: Run the experiment and record a binary verdict</name>

  <files>.planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/, .planning/phases/30-keep-the-session-alive-past-turn-end/30c-VERDICT.md</files>

  <precondition>Task 1's harness exists and parses, and no other live `claude` CLI experiment is running concurrently — a second concurrent CLI session confounds background-task delivery timing and can consume rate limit shared with this measurement.</precondition>

  <read_first>
    - `.planning/phases/30-keep-the-session-alive-past-turn-end/30a-evidence/raw_output_v3.jsonl` — the interactive-session baseline this run is compared against. The facts to compare are: three `result` events (lines 19, 37, 54); the first carries NO `origin` key, the second and third carry `origin.kind == "task-notification"`; all three `init` events (lines 5, 32, 47) share one `session_id`; `background_tasks_changed` drains to an empty array at line 44.
    - `.planning/phases/30-keep-the-session-alive-past-turn-end/30a-evidence/README.md` — the three environment caveats this run exists to close, and the standing rule that harness-printed verdicts are not evidence.
  </read_first>

  <action>
    Run the harness once. Publish the sanitised JSONL, the separated stderr log,
    and the run log into `30c-evidence/` through Task 1's publish pipeline —
    never by copying the staged files directly.

    Then read the PUBLISHED JSONL — not the harness's console output — and
    answer the confirmation question. **The bar is both delegated children
    delivered, not one.** Record `delivery: confirmed` only when ALL of the
    following hold:

    - both background children ran to completion, evidenced by their signal
      files existing on disk at their expected contents; AND
    - after the first `result` event fired, either **two** further top-level
      `result` events arrived carrying `origin.kind == "task-notification"`,
      each correlatable to a distinct child (by `task_id` through the preceding
      `task_notification` event, or by the content of the result text naming a
      distinct child), **or** one such notification-origin result arrived
      together with an observed `background_tasks_changed` event whose `tasks`
      array is empty, proving the task set drained rather than one child being
      silently dropped.

    Anything short of that bar — including the specific one-delivered/one-lost
    shape — is `refuted`, because Phase 31's premise is delivery for a
    multi-plan wave, and a wave with one lost executor is exactly the Phase 29
    failure this arc exists to fix. Record the nuance in a companion frontmatter
    field rather than by hedging the binary one.

    Baseline for comparison, confirmed at planning time by re-parsing
    `raw_output_v3.jsonl`: three `result` events (19, 37, 54), the first with no
    `origin` key and the latter two both `origin.kind == "task-notification"`;
    `task_notification` events at 26, 31, 38 and 46; `background_tasks_changed`
    at 8 and 13 (filling), 29 (one drained), 44 (empty). The interactive
    baseline therefore clears the strengthened bar on both arms.

    Write `30c-VERDICT.md` with YAML frontmatter carrying these fields:
    `unit: 30c`, `delivery:` set to exactly `confirmed` or `refuted`,
    `claude_code_version:` as resolved at run time, `result_events:` the observed
    count, `task_notification_origin_results:` the observed count,
    `children_completed:` the count of children whose signal files were verified
    on disk, `task_set_drained:` `true` or `false` for whether an empty
    `background_tasks_changed.tasks` array was observed, `partial_delivery:`
    `true` when at least one but not all children were delivered (this is the
    field that records a one-of-two outcome without softening `delivery`),
    `unparseable_lines:` the count from the publish step's validation, and
    `evidence:` the relative paths of the published files.

    The body must contain: a per-`result`-event table (index, wall-clock offset
    from launch, `origin` field verbatim or `absent`, redacted `session_id`
    pseudonym); a per-child correlation table (child, `task_id`, notification
    observed, signal file verified, which `result` event it triggered); an
    explicit side-by-side comparison against the v3 interactive baseline named in
    `read_first`; a `## Residual environment` section listing by NAME the
    Claude-session marker variables and ancestry facts the scrub could not
    remove, and reading them against what the operator-launched DevFlow
    environment would carry; and a `## Limits of this evidence` section stating
    what a single trial cannot establish — the 30a captures were each single
    trials too, and n=1 is honest here only if labelled.

    **If `delivery: refuted`**, do not retry the experiment hoping for a
    different answer and do not soften the wording. Add a `## Rejected options`
    table to the verdict recording what the refutation eliminates: the
    pipe-owning monitor (Phase 31's constraint 4 close-rule has no signal to
    gate on), the always-on adapter switch (constraint 1 buys nothing without
    delivery), and whatever residual value the stream parser retains for the
    single-document and no-background cases. A refutation is a successful
    experiment — it saves an entire M-sized phase. The ROADMAP's locked decision
    is that this outcome cancels Phase 31 before it is planned and re-scopes
    999.64 around exactly this table.

    If the run is inconclusive for a mechanical reason — the CLI errored, the
    children never started, the harness aborted on the scrub check — that is
    neither `confirmed` nor `refuted`. Fix the mechanical fault and re-run.
    Record the aborted attempt in the verdict's body so the record shows what
    was tried.
  </action>

  <verify>
    <automated>cd /var/home/denniyahh/Github/devflow && test -s .planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/raw_output.jsonl && rg -q '^delivery: (confirmed|refuted)$' .planning/phases/30-keep-the-session-alive-past-turn-end/30c-VERDICT.md && echo "verdict recorded"</automated>
  </verify>

  <acceptance_criteria>
    - `30c-evidence/raw_output.jsonl` exists, is non-empty, and every line parses as JSON.
    - `30c-VERDICT.md` frontmatter contains a `delivery:` field whose value is exactly `confirmed` or `refuted` — the verify command's regex must match, and it does not match `unknown`, `partial`, or any hedge.
    - Frontmatter also carries `children_completed`, `task_set_drained`, `partial_delivery` and `unparseable_lines`.
    - `delivery: confirmed` appears ONLY when `children_completed` equals the number of children the harness dispatched AND either `task_notification_origin_results >= 2` with a per-child correlation table showing two distinct children, or `task_notification_origin_results >= 1` together with `task_set_drained: true`. A frontmatter recording one delivered child and one lost child while claiming `confirmed` fails this criterion.
    - The observed `result` event count and `task-notification`-origin count in the frontmatter match a fresh independent count taken from the published JSONL, not from the harness's console output.
    - `30c-VERDICT.md` contains a `## Limits of this evidence` section and a `## Residual environment` section.
    - If `delivery: refuted`, `30c-VERDICT.md` contains a `## Rejected options` table.
    - No absolute home directory path, OS username, raw session UUID, CLI `output_file` path, or credential-shaped token (`sk-`, `ghp_`/`gho_`/`ghs_`, `Bearer <token>`, or a `KEY`/`TOKEN`/`SECRET`/`PASSWORD` assignment) appears anywhere under `30c-evidence/` or in `30c-VERDICT.md`. Prove it by running the publish step's own secret scanner over the published tree and quoting its clean result in the SUMMARY.
    - No file under `.planning/` outside `30c-evidence/` and `30c-VERDICT.md` was written by the run, and no staged raw file was copied into a committed path without passing through the publish pipeline.
    - `git status --porcelain crates/` reports no modifications.
  </acceptance_criteria>

  <reversibility rating="costly">The verdict is the input to a cancel-or-proceed decision on an M-sized phase. A misread `refuted` wastes the Phase 31 opportunity; a misread `confirmed` spends a full phase on a false premise. The experiment itself is trivially re-runnable — what is costly is acting on a wrong reading, which is why Task 3 puts a human between the reading and the decision.</reversibility>

  <done>Sanitised evidence is published and `30c-VERDICT.md` records a binary `delivery:` value derived from the published JSONL itself under the both-children bar, with per-child correlation, residual environment, limits stated, and — if refuted — a rejected-options table.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator confirms the verdict reading before Phase 31 is planned</name>

  <what-built>
    A replica of `spawn_monitor`'s process environment was built and run once
    against the live `claude` CLI. The raw JSONL, separated stderr, and run log
    are archived under `30c-evidence/`, and `30c-VERDICT.md` records a binary
    `delivery: confirmed | refuted` derived from re-reading the raw capture.
  </what-built>

  <how-to-verify>
    1. Read `.planning/phases/30-keep-the-session-alive-past-turn-end/30c-VERDICT.md`
       — the frontmatter `delivery:` value, the per-`result`-event table, the
       per-child correlation table, and the `## Residual environment` and
       `## Limits of this evidence` sections.
    2. Independently count the `task-notification`-origin result events in the
       published capture rather than trusting the table:
       `python3 -c "import json;[print(i,json.loads(l).get('origin')) for i,l in enumerate(open('.planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/raw_output.jsonl'),1) if json.loads(l).get('type')=='result']"`
    3. Independently confirm the task set drained, since a single notification
       plus a drain is one of the two ways `confirmed` can be earned:
       `python3 -c "import json;[print(i,json.loads(l).get('tasks')) for i,l in enumerate(open('.planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/raw_output.jsonl'),1) if json.loads(l).get('subtype')=='background_tasks_changed']"`
    4. Confirm both counts match the frontmatter, and that the verdict follows
       from them under the both-children bar — one delivered child and one lost
       child is `refuted`, not `confirmed`.
    5. Spot-check the published evidence for leaks: confirm no absolute home
       path, OS username, or raw session UUID survives in
       `30c-evidence/raw_output.jsonl`.
    6. If `refuted`, read the `## Rejected options` table and confirm you accept
       cancelling Phase 31 before it is planned, per the ROADMAP's locked decision.
    7. If `confirmed`, confirm you accept that a single trial in one environment
       is the basis for planning Phase 31 — the same evidential standard the
       three 30a captures were each held to.
    8. **If `refuted`, decide explicitly whether plans 30-03 and 30-05 should
       still land.** They add stream-parsing capability — rate limit, envelope
       failure, `session_id`, checkpoint scoping — to a codebase whose shipped
       Claude adapter still launches with `--output-format json`
       (`crates/devflow-core/src/agents/claude.rs`, `exec_command`), so under a
       refuted premise nothing in production emits the shape they parse until
       some future work changes the adapter. The cross-AI review flagged landing
       them automatically as a defect in this plan set. The two live options,
       stated without a recommendation embedded in the plan: **(a) land them
       anyway** — the parser is the prerequisite for any alternative approach
       that still reads Claude stream output, including review constraint 5's
       idle-timeout heartbeat, which needs per-event stream signal regardless of
       whether background-task notifications are delivered; or **(b) stop after
       30-04** and re-scope 999.64 around the rejected-options table before
       spending further parser work. Answer (a) or (b) explicitly; plans 30-03
       and 30-05 each halt on this answer.
  </how-to-verify>

  <resume-signal>Type "approved" to accept the verdict as the Phase 31 gate. If the verdict is `refuted`, also state "land 30-03/30-05" or "stop after 30-04". Otherwise describe what does not follow from the evidence.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → committed repository | The harness writes CLI output and environment detail into `30c-evidence/`, which is committed and published with this repo. |
| harness → live `claude` CLI | The harness launches a real CLI process with a scrubbed but otherwise inherited environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-30-06 | Information Disclosure | `30c-evidence/` published artifacts | high | mitigate | Raw stdout/stderr are redirected into a run-scoped directory OUTSIDE `.planning/`; a separate post-run step validates, structurally redacts (home path, username, `session_id`, `output_file`, `cwd`), secret-scans, and only then atomically publishes into `30c-evidence/`. Ordering is the mitigation — an OS-level redirection cannot be intercepted by a redactor, which is why the original single-step design could not satisfy this threat. Recurring leak class in this repository (backlog 999.10; Phase 18 review WR-02), with a live instance in `30a-evidence/raw_output_v3.jsonl` line 5. |
| T-30-07 | Information Disclosure | child process environment | medium | mitigate | The harness records only the NAMES of scrubbed variables and of residual Claude-session marker variables, never their values, and never dumps the full child environment into the run log. Credentials reachable via inherited env (API keys, tokens) must not be enumerated into a committed artifact. |
| T-30-26 | Information Disclosure | credential-shaped tokens in CLI output | high | mitigate | The publish step runs an explicit secret scan over the redacted text before writing it, covering `sk-` prefixes, `ghp_`/`gho_`/`ghs_` prefixes, `Bearer ` tokens, and `KEY`/`TOKEN`/`SECRET`/`PASSWORD` assignments, and raises rather than publishing on a match — reporting the matched pattern name, never the matched value. Closes the gap between the must-have promising "no credentials" and acceptance criteria that previously checked only paths and usernames. Proven by running a synthetic leaky fixture through the function. |
| T-30-27 | Repudiation | unproven environment equivalence | medium | mitigate | The claim "differs in exactly one variable" is downgraded to what the harness can actually deliver, and the verdict carries a `## Residual environment` section naming the inherited Claude-session markers and ancestry facts the scrub cannot remove, read against the operator-launched DevFlow environment. A reader can now check the claim instead of taking it. |
| T-30-08 | Tampering | env-scrub list transcription | medium | mitigate | The scrub list is parsed from `crates/devflow-core/src/git.rs` at runtime and the harness aborts if either const yields zero entries. A hardcoded or silently-empty list would test the wrong environment and produce a confident but invalid verdict. |
| T-30-09 | Repudiation | verdict provenance | medium | mitigate | The verdict is derived by re-reading the archived raw JSONL, never from harness console output — 30a's README records that harness-printed verdicts were wrong in v1 and unreliable in v2. Task 3's checkpoint has the operator independently recount from the raw file. |
| T-30-10 | Denial of Service | live CLI usage | low | accept | One experiment run consumes CLI quota. Bounded, deliberate, and far cheaper than the M-sized phase it gates. Task 2's precondition serializes it against other live experiments. |

**Package legitimacy:** not applicable — this plan installs no packages. The harness uses only the Python standard library (`json`, `os`, `select`, `subprocess`, `time`), matching `run_experiment_v3.py`'s existing import set.
</threat_model>

<verification>
1. `30c-evidence/raw_output.jsonl` exists, is non-empty, and every line parses as JSON.
2. `30c-VERDICT.md` frontmatter has `delivery:` equal to exactly `confirmed` or `refuted`, plus `children_completed`, `task_set_drained`, `partial_delivery` and `unparseable_lines`.
3. An independent recount of `task-notification`-origin result events AND of `background_tasks_changed.tasks` arrays from the published JSONL matches the frontmatter counts, and `confirmed` is only present when the both-children bar is met.
4. The publish step's own secret scanner, re-run over `30c-evidence/` and `30c-VERDICT.md`, reports no match for home path, OS username, raw session UUID, `output_file` path, or credential-shaped token.
5. The child's stdout and stderr redirection targets resolved outside the repository working tree; the staged directory path is recorded in the published run log.
6. `git status --porcelain crates/` is empty — `monitor.rs` and `git.rs` were read, never written.
7. Operator sign-off recorded in the plan SUMMARY, verbatim — including, when the verdict is `refuted`, the explicit "land 30-03/30-05" or "stop after 30-04" answer.
</verification>

<success_criteria>
The production-environment question has a recorded, human-confirmed binary
answer derived from raw evidence, with limits stated; DevFlow's source is
unmodified; and the archived artifacts leak no paths or usernames.
</success_criteria>

<conditional_next_step>
This plan is a decision gate, and the branch is explicit rather than assumed:

- **`delivery: confirmed`** — Phase 31 may be planned. Its acceptance criterion
  remains the live Phase 29 wave-2 re-run, which review constraint H4 states is
  not substitutable by integration tests.
- **`delivery: refuted`** — **Phase 31 is cancelled before it is planned**, per
  the ROADMAP's locked decision. 999.64 re-scopes around `30c-VERDICT.md`'s
  rejected-options table. Plan 30-01 has already landed by then and stays
  landed. **Whether plans 30-03 and 30-05 still land is the operator's call,
  taken at Task 3 step 8, not this plan's to pre-decide.** The prior wording
  here asserted they land regardless; the cross-AI review correctly flagged that
  as pre-empting a decision, since the shipped adapter emits
  `--output-format json` and a refuted premise leaves the expansion parsers with
  no production producer. Both plans read the recorded answer and halt if it is
  "stop after 30-04".

Recorded as an explicit conditional next-step rather than a `checkpoint:decision`
task. The ROADMAP's locked decision sanctions either form, and this project has
an open defect — backlog 999.57 part B, deferred out of Phase 28 — where a
multi-option `checkpoint:decision` cannot reliably receive an operator answer
under a one-shot launch. Task 3 uses `checkpoint:human-verify`, whose answer
return path Phase 28 closed and shipped.
</conditional_next_step>

<artifacts_this_phase_produces>
Files created by this plan (none exist today):

- `.planning/phases/30-keep-the-session-alive-past-turn-end/30c-monitor-env-harness.py`
- `.planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/raw_output.jsonl`
- `.planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/stderr.log`
- `.planning/phases/30-keep-the-session-alive-past-turn-end/30c-evidence/run.log`
- `.planning/phases/30-keep-the-session-alive-past-turn-end/30c-VERDICT.md`

Python symbols created inside `30c-monitor-env-harness.py`, referenced by plan
30-04 and therefore part of this phase's produced surface:

- a module-level launcher function (workdir + prompt payload → launched process
  and staged output paths), imported and reused by 30-04's harness
- a module-level publish function (validate → structurally redact →
  secret-scan → atomic replace), imported and reused by 30-04's harness
- a module-level secret-scan function, re-run standalone during verification

Exact names are the executor's choice; plan 30-04's `read_first` requires it to
read this file and use whatever names exist rather than reimplementing them.

No Rust symbols are created or modified by this plan.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/30-keep-the-session-alive-past-turn-end/30-02-SUMMARY.md` when done.
Record the operator's Task 3 response verbatim, and quote the independent
recount command's output alongside the verdict frontmatter values.
</output>
