---
phase: 40-pi-dogfood
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - .planning/phases/40-pi-dogfood/40-VERIFICATION.md
  - .planning/phases/40-pi-dogfood/40-UAT.md
autonomous: false
requirements: [PIDG-01, MAINT-01]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - A real supervised run completes Define→Plan→Code→Validate through `--agent pi`.
    - The live gates are honored — Pi's Define gate (35.1-03 unattended-launch check, Legacy can't bind the chain flag) and/or the Validate gate (supervise mode) — no lost gate, no silent advance.
    - A subagent is actually dispatched during the Code stage (dispatch path exercised, not just available).
    - The two 999.85 comments are rewritten to cite the structural defenses (enumerated status position; graft's status filter), keeping `verdict: None` intact.
  artifacts:
    - crates/devflow-core/src/agent_result.rs
    - .planning/phases/40-pi-dogfood/40-VERIFICATION.md
    - .planning/phases/40-pi-dogfood/40-UAT.md
  key_links:
    - The Code-stage Pi agent's DEVFLOW_RESULT marker → `parse_devflow_result` (completion honesty).
    - The Validate gate → the operator's file-based response (no lost gate decision).
    - The Pi agent's `subagent` tool → `@bacnh85/pi-subagent` (dispatch exercised).
---

<objective>
Drive one real phase through `devflow start --agent pi` in supervise mode, ending at Validate. The
Pi agent rewrites the 999.85 comments (D-01; D-02 — MAINT-01 satisfied here, not Phase 45) during
the Code stage, dispatches a subagent to independently verify the comment claims (exercising the
dispatch path), and the operator answers the live Validate gate. This is the "real" half of the
hardening bar (D-05).

Purpose: prove the shipped Pi driver survives a real Define→Validate run with a real gate and a real
subagent dispatch — the thing baseline unit tests cannot show.
Output: the 999.85 comment rewrite (committed) + 40-VERIFICATION.md + 40-UAT.md evidence.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-pi-dogfood/40-CONTEXT.md
@crates/devflow-core/src/agent_result.rs   # the two 999.85 comment sites (:1746-1750, :6412-6417)
@.planning/milestones/v2.7.0-phases/39-pi-end-to-end/39-CONTEXT.md  # Pi driver prior decisions D-01..D-04
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify dogfood preconditions</name>
  <files>.planning/phases/40-pi-dogfood/40-CONTEXT.md</files>
  <action>Confirm every precondition before launch, and halt if any fails: (1) the `pi` binary is on
  PATH and `pi auth check` reports the `litellm` provider ready (D-03 from Phase 39); (2)
  `@bacnh85/pi-subagent` is installed — `pi list --no-approve` lists it, so capability detection
  reports `subagent_dispatch: true`; (3) this phase's CONTEXT.md (D-01..D-05) is present. Record the
  confirmation in the run notes.</action>
  <verify>
    <automated>pi list --no-approve 2>&1 | grep -q "@bacnh85/pi-subagent" && echo "preconditions OK"</automated>
  </verify>
  <done>All preconditions verified and recorded; the run may launch.</done>
</task>

<task type="auto">
  <name>Task 2: Launch the supervised Pi dogfood run and answer the Validate gate</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <action>Launch `devflow start --agent pi --phase 40 --mode supervise` from the phase worktree. The
  run first gates at **Define** (35.1-03 unattended-launch check: Legacy launch cannot bind the chain
  flag — expected; answer it), then the Pi agent drives Define (no-op, D-04) → Plan (warn-only) →
  Code. During Code, the agent MUST (a) rewrite the two 999.85 comments at agent_result.rs:1746-1750
  and :6412-6417 to cite the two structural defenses that now carry the `verdict: None` invariant —
  the enumerated status position (34-03) and the graft's status filter (34-01) — keeping the
  `verdict: None` instruction intact and unweakened (D-01), and (b) dispatch a `reviewer` subagent to
  independently verify the rewritten comments' claims against the actual classifier code before
  finalizing. At Validate, a supervise gate fires; the operator answers it (file-based response). Do
  NOT run in auto mode — the live gates are the point (D-05). Do NOT touch any code beyond the two
  comment sites.</action>
  <verify>
    <automated>devflow status --phase 40 2>&1 | grep -qE "Validate|complete" && echo "run reached Validate"</automated>
  </verify>
  <done>The run completes Define→Plan→Code→Validate; one live gate was answered; the comments are rewritten; a subagent was dispatched.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the 999.85 rewrite and capture dogfood evidence</name>
  <files>crates/devflow-core/src/agent_result.rs, .planning/phases/40-pi-dogfood/40-VERIFICATION.md, .planning/phases/40-pi-dogfood/40-UAT.md</files>
  <action>Verify the rewrite against the 999.85 spec: both comments now cite the enumerated status
  position and the graft's status filter; the `verdict: None` instruction is intact. Run the full
  test suite (assert on real "test result: N passed", not the pipeline exit code) to confirm no
  test referenced the old comment strings. Write 40-VERIFICATION.md (what was proven, incl. the gate
  honored and the subagent dispatch witnessed) and 40-UAT.md (operator attestation of the live gate).
  Do NOT do the DEN-95/DEN-98 status sweep — out of scope (Deferred Ideas).</action>
  <verify>
    <automated>cargo test -p devflow-core --lib agent_result 2>&1 | grep -E "test result:.*passed" && test -f .planning/phases/40-pi-dogfood/40-VERIFICATION.md && echo "evidence written"</automated>
  </verify>
  <done>Comments verified correct; tests green; 40-VERIFICATION.md + 40-UAT.md exist and record the run, the gate, and the dispatch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → Validate gate | A human decision crosses into the pipeline via a file-based response; it must not be lost or auto-answered. |
| Pi agent → agent_result.rs | An agent-authored edit to a correctness-critical comment; the comment must stay accurate to its invariant. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-03 | Repudiation | 999.85 comment rewrite | medium | mitigate | Subagent re-verifies the claims against the classifier; operator reviews the diff; the `verdict: None` instruction must remain intact. |
| T-40-04 | Tampering | Validate gate response | medium | mitigate | Supervise mode guarantees the gate is not auto-advanced; the operator's response is the single source of truth. |
| T-40-05 | Information Disclosure | subagent dispatch | low | accept | `@bacnh85/pi-subagent` is user-scope and fails closed headlessly (Phase 37.1); no project agents are involved. |
</threat_model>

<verification>
The phase's success is the dogfood evidence itself: a real supervised run through Pi, a witnessed
subagent dispatch, a live gate honored, and the 999.85 comments correctly rewritten with the test
suite green.
</verification>

<success_criteria>
Define→Plan→Code→Validate completed through `--agent pi`; one live gate answered; a subagent
dispatched; the two 999.85 comments correctly rewritten and committed; evidence artifacts written.
</success_criteria>

<output>
Create `.planning/phases/40-pi-dogfood/40-02-SUMMARY.md` when done
</output>
