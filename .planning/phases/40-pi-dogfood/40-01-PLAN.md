---
phase: 40-pi-dogfood
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/tests/phase7_cli.rs
autonomous: true
requirements: [PIDG-01]

estimate:
  tokens: 45000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - A Pi run that exits 0 without emitting DEVFLOW_RESULT does not advance its stage.
    - A Pi run that exits non-zero does not advance its stage.
    - A hung Pi process is reported as such (monitor liveness), not silently left running.
  artifacts:
    - crates/devflow-cli/tests/phase7_cli.rs
  key_links:
    - The stubbed `pi` binary on PATH must be picked up by the same launch path the real `pi -p` uses (Legacy).
---

<objective>
Add regression tests proving Pi's `-p`/Legacy transport feeds DevFlow's generic completion, exit,
and liveness machinery correctly. This is the "simulate separately" half of the hardening bar (D-05):
the failure modes a real run cannot guarantee, proven with a stubbed `pi` binary.

Purpose: Pi's happy path is already shipped and tested; these tests close the transport-specific
failure-mode delta so "Pi is hardened" does not rest on the happy path alone.
Output: stub-pi fixtures + three regression tests in `crates/devflow-cli/tests/phase7_cli.rs`.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/40-pi-dogfood/40-CONTEXT.md
@crates/devflow-core/src/agent_result.rs   # parse_devflow_result / evaluate_layer1 / evaluate_layer3
@crates/devflow-core/src/agents/pi.rs      # PiDriver `-p --no-approve`, Legacy launch
@crates/devflow-cli/tests/phase7_cli.rs    # existing stub-PATH + ENV_MUTEX integration pattern
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stub `pi` and prove marker-less and non-zero-exit runs do not advance</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <behavior>
    - A `pi` stub that exits 0 and prints no DEVFLOW_RESULT → the stage result is NOT Success (no advance).
    - A `pi` stub that exits non-zero → the stage result is NOT Success (no advance).
  </behavior>
  <action>Add a stubbed `pi` executable (a shell script or compiled helper) to the test's tempdir PATH,
  following the existing stub-PATH + ENV_MUTEX pattern in phase7_cli.rs. One stub exits 0 with no
  DEVFLOW_RESULT marker; another exits non-zero. Drive each through the same launch path the real
  `pi -p --no-approve` uses (Legacy), and assert the stage does not advance. Do NOT re-test the
  generic marker/exit logic — only the Pi-transport delta (that Pi's `-p` output feeds
  parse_devflow_result / evaluate_layer3). Cite D-05 in the action.</action>
  <verify>
    <automated>cargo test -p devflow --test phase7_cli -- marker-less / non-zero 2>&1 | grep -E "test result:.*(1|2) passed"</automated>
  </verify>
  <done>Both stubbed-pi runs are proven to not advance their stage; the tests are committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove a hung Pi is detected, not silently left running</name>
  <files>crates/devflow-cli/tests/phase7_cli.rs</files>
  <behavior>
    - A `pi` stub that never exits → monitor liveness reports the process as not-completed (no false "Success").
  </behavior>
  <action>Add a `pi` stub that sleeps indefinitely, and assert DevFlow's liveness path (monitor liveness,
  Phase 18) reports it correctly — the run is not declared complete and the process is surfaced as
  alive/hung, never silently advanced. Use the existing stub-PATH + ENV_MUTEX pattern; bound any
  real wait under ENV_MUTEX so the test cannot wedge (the Phase 17-09 lesson). Cite D-05.</action>
  <verify>
    <automated>cargo test -p devflow --test phase7_cli -- hung 2>&1 | grep -E "test result:.*1 passed"</automated>
  </verify>
  <done>The hung-pi test passes deterministically (no unbounded wait); the test is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → stubbed `pi` | The stub is a test-only fake binary on a tempdir PATH; it never crosses a production boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-40-01 | Tampering | stubbed `pi` fixture | low | accept | Test-only; scoped to a tempdir PATH, no production code touched. |
| T-40-02 | Denial of Service | hung-pi test | low | mitigate | Bound the wait under ENV_MUTEX so the test cannot wedge CI (Phase 17-09 precedent). |
</threat_model>

<verification>
`cargo test -p devflow --test phase7_cli` — assert on real "test result: N passed" counts, not the
pipeline exit code (CLAUDE.md). `cargo clippy --workspace --all-targets -- -D warnings` and
`cargo fmt --check` clean.
</verification>

<success_criteria>
The three failure-mode tests pass deterministically and are committed; no production code changed.
</success_criteria>

<output>
Create `.planning/phases/40-pi-dogfood/40-01-SUMMARY.md` when done
</output>
