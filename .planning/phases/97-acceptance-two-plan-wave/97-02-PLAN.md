---
phase: 97-acceptance-two-plan-wave
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt
autonomous: true
estimate:
  tokens: 2000
  raw_tokens: 2000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "The file .planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt exists and its only line is ACCEPTANCE-97-02-BETA"
    - "The marker file is committed, and this plan writes 97-02-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt"
      provides: "Plan 02's delivery marker — the file whose existence plus merge is half of the acceptance criterion"
      contains: "ACCEPTANCE-97-02-BETA"
---

<objective>
Write one file with one fixed string, and commit it.

Purpose: this plan is a **delivery probe**, not a feature. It is one half of a two-plan wave whose
only job is to be dispatched concurrently with its sibling (`97-01`) so that Phase 31's pipe-owning
monitor has to keep a `stream-json` session alive past turn end and deliver a task-notification when
this executor finishes. Whether this plan's SUMMARY.md exists and whether its commit merges is the
evidence; the file's contents are arbitrary and deliberately worthless.

Output: `.planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt`, committed, plus
`97-02-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
No project context is referenced on purpose. This plan must not be able to fail for any reason
other than a delivery failure, so it reads nothing, builds nothing, and depends on nothing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write plan 02's delivery marker</name>
  <files>.planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt</files>
  <action>
Create `.planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt` whose entire content
is exactly one line:

    ACCEPTANCE-97-02-BETA

Then commit it.

Do nothing else. Do not read the codebase. Do not run a build, a test suite, or a linter. Do not
touch any file outside your own `files_modified` list and your own SUMMARY.md. Your sibling plan
`97-01` is running concurrently and owns `marker-alpha.txt` — do not create, read, or reference it.

If you find yourself investigating anything, stop: there is nothing here to investigate.
  </action>
  <verify>
    <automated>test -f .planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt &amp;&amp; rg -qx 'ACCEPTANCE-97-02-BETA' .planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt &amp;&amp; echo MARKER_BETA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `marker-beta.txt` exists and its only line is `ACCEPTANCE-97-02-BETA`.
    - The file is committed (not left as a working-tree edit).
  </acceptance_criteria>
  <done>`marker-beta.txt` is committed with the exact marker string, and `.planning/phases/97-acceptance-two-plan-wave/97-02-SUMMARY.md` has been written.</done>
</task>

</tasks>

<verification>
1. `rg -qx 'ACCEPTANCE-97-02-BETA' .planning/phases/97-acceptance-two-plan-wave/scratch/marker-beta.txt`
2. `.planning/phases/97-acceptance-two-plan-wave/97-02-SUMMARY.md` exists.
</verification>

<success_criteria>
- `marker-beta.txt` is committed with its exact marker string.
- `97-02-SUMMARY.md` exists.
</success_criteria>

<output>
Create `.planning/phases/97-acceptance-two-plan-wave/97-02-SUMMARY.md` when done.
</output>
