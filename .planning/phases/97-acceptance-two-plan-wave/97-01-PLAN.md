---
phase: 97-acceptance-two-plan-wave
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt
autonomous: true
estimate:
  tokens: 2000
  raw_tokens: 2000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "The file .planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt exists and its only line is ACCEPTANCE-97-01-ALPHA"
    - "The marker file is committed, and this plan writes 97-01-SUMMARY.md"
  artifacts:
    - path: ".planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt"
      provides: "Plan 01's delivery marker — the file whose existence plus merge is half of the acceptance criterion"
      contains: "ACCEPTANCE-97-01-ALPHA"
---

<objective>
Write one file with one fixed string, and commit it.

Purpose: this plan is a **delivery probe**, not a feature. It is one half of a two-plan wave whose
only job is to be dispatched concurrently with its sibling (`97-02`) so that Phase 31's pipe-owning
monitor has to keep a `stream-json` session alive past turn end and deliver a task-notification when
this executor finishes. Whether this plan's SUMMARY.md exists and whether its commit merges is the
evidence; the file's contents are arbitrary and deliberately worthless.

Output: `.planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt`, committed, plus
`97-01-SUMMARY.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
No project context is referenced on purpose. This plan must not be able to fail for any reason
other than a delivery failure, so it reads nothing, builds nothing, and depends on nothing.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write plan 01's delivery marker</name>
  <files>.planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt</files>
  <action>
Create `.planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt` whose entire content
is exactly one line:

    ACCEPTANCE-97-01-ALPHA

Then commit it.

Do nothing else. Do not read the codebase. Do not run a build, a test suite, or a linter. Do not
touch any file outside your own `files_modified` list and your own SUMMARY.md. Your sibling plan
`97-02` is running concurrently and owns `marker-beta.txt` — do not create, read, or reference it.

If you find yourself investigating anything, stop: there is nothing here to investigate.
  </action>
  <verify>
    <automated>test -f .planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt &amp;&amp; rg -qx 'ACCEPTANCE-97-01-ALPHA' .planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt &amp;&amp; echo MARKER_ALPHA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `marker-alpha.txt` exists and its only line is `ACCEPTANCE-97-01-ALPHA`.
    - The file is committed (not left as a working-tree edit).
  </acceptance_criteria>
  <done>`marker-alpha.txt` is committed with the exact marker string, and `.planning/phases/97-acceptance-two-plan-wave/97-01-SUMMARY.md` has been written.</done>
</task>

</tasks>

<verification>
1. `rg -qx 'ACCEPTANCE-97-01-ALPHA' .planning/phases/97-acceptance-two-plan-wave/scratch/marker-alpha.txt`
2. `.planning/phases/97-acceptance-two-plan-wave/97-01-SUMMARY.md` exists.
</verification>

<success_criteria>
- `marker-alpha.txt` is committed with its exact marker string.
- `97-01-SUMMARY.md` exists.
</success_criteria>

<output>
Create `.planning/phases/97-acceptance-two-plan-wave/97-01-SUMMARY.md` when done.
</output>
