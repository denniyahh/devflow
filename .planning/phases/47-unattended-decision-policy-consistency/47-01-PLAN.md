---
phase: 47-unattended-decision-policy-consistency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - Cargo.lock
  - crates/devflow-core/Cargo.toml
  - crates/devflow-cli/Cargo.toml
  - crates/devflow-core/src/prompt.rs
  - crates/devflow-core/src/snapshots
  - scripts/check.sh
  - .gitignore
  - .planning/user/DEV-SETUP-CHECKLIST.md
  - crates/devflow-cli/tests/ci_parity_guards.rs
  - crates/devflow-cli/tests/gitignore_coverage.rs
autonomous: true
requirements: [DECN-02, DECN-03]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A wording change to a policy-carrying prompt fails `scripts/check.sh test` instead of passing silently (D-14, D-16)."
    - "`INSTA_FORCE_UPDATE=1` cannot re-bless a drifted snapshot green — the guard holds against a polluted environment (D-16 negative control, RESEARCH A-3)."
    - "A clean tree still passes `scripts/check.sh test` — the guard discriminates rather than constant-failing."
    - "A `.snap.new` file is git-ignored while a `.snap` file is not — the baseline stays reviewable in the PR diff (D-14)."
  artifacts:
    - Cargo.toml
    - crates/devflow-core/Cargo.toml
    - crates/devflow-cli/Cargo.toml
    - crates/devflow-core/src/snapshots
    - scripts/check.sh
    - .gitignore
    - .planning/user/DEV-SETUP-CHECKLIST.md
    - crates/devflow-cli/tests/ci_parity_guards.rs
    - crates/devflow-cli/tests/gitignore_coverage.rs
  key_links:
    - "`run_test` in `scripts/check.sh` ← `.github/workflows/ci.yml:63` required Test job. The guard must live in `run_test`; putting it in the `all` target leaves the required job unguarded (D-16 Placement)."
    - "`insta::assert_snapshot!` written fully qualified with no `use` ← keeps `cargo machete` green with no ignore entry (RESEARCH A-5)."
    - "`env -u INSTA_FORCE_UPDATE` in `run_test` ← the only thing standing between a drifted baseline and a green build (RESEARCH A-3)."
---

<objective>
Stand up the snapshot drift guard that the rest of Phase 47 lands behind, and prove it fires
before any baseline is blessed.

Purpose: D-14/D-15/D-16 exist so a wording change in a policy-carrying prompt surfaces as a review
diff rather than drifting unnoticed — which is the failure that produced this phase. The operator
sequenced this plan FIRST, ahead of every snapshot baseline, because a baseline landed behind an
unproven guard is worth nothing.

Output: `insta` wired at workspace level, `run_test` hardened with
`env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no`, one committed baseline, two durable Rust guards over
the wiring, and a recorded three-case demonstration (drift / force-update / clean).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md
@.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md
@.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md
</context>

<!-- Comment-text discipline: literals below are flagged by the coarse `<action>` heuristic but
     are safe here. Each line records why the gate cannot be self-invalidated by the plan's own text. -->
<!-- planner-discipline-allow: env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no cargo test -->  <!-- the exact wiring the task installs; the only negative grep in this plan that touches source is the cargo-machete one, and it strips comment lines -->
<!-- planner-discipline-allow: snap.new -->  <!-- the ignore pattern being added; no acceptance criterion negative-greps a source file for it -->
<!-- planner-discipline-allow: INSTA_UPDATE=always -->  <!-- the sanctioned baseline-creation recipe, which must be quoted exactly or a contributor cannot reproduce it -->
<!-- planner-discipline-allow: insta -->  <!-- the dependency being added; the only negative grep for it targets cargo machete's own output log, not any file an executor writes -->
<!-- planner-discipline-allow: cargo-machete -->  <!-- named as a prohibition (do not add an ignore entry); the gate counts it over a comment-stripped concatenation of both manifests, so restating the prohibition in a manifest comment cannot trip it -->
<!-- planner-discipline-allow: ## Unattended decision checkpoints -->  <!-- the policy heading whose ABSENCE this plan's baseline records; the gate counts it over generated .snap files, never over plan or comment prose -->
<!-- planner-discipline-allow: /gsd-execute-phase -->  <!-- gated POSITIVELY (the baseline must contain the fix command), also over generated .snap files -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire insta end to end — dependency, one snapshot, hardened run_test, ignore rule, checklist</name>
  <files>Cargo.toml, Cargo.lock, crates/devflow-core/Cargo.toml, crates/devflow-cli/Cargo.toml, crates/devflow-core/src/prompt.rs, crates/devflow-core/src/snapshots/, scripts/check.sh, .gitignore, .planning/user/DEV-SETUP-CHECKLIST.md</files>
  <read_first>
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § A-0, § A-5, § A-6, § A-7 — the exact `run_test` body, the snapshot file locations, and why the fully-qualified macro path keeps `cargo machete` green
    - `.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md` § `scripts/check.sh`, § `.gitignore`, § `Cargo.toml` / crate manifests, § `Planning-record edits`
    - `scripts/check.sh` (whole file; `set -euo pipefail` at :9, `run_clippy` at :34-38 is the sibling shape, `run_test` at :41-49 is the body being replaced)
    - `Cargo.toml` `[workspace.dependencies]` (:19-32) — the `serde_json` entry is the idiom for a comment explaining a load-bearing choice
    - `crates/devflow-core/Cargo.toml` (`[features]` at :16-20, `[dev-dependencies]` at :32-33) and `crates/devflow-cli/Cargo.toml` (`[dev-dependencies]` at :24-27)
    - `crates/devflow-core/src/prompt.rs` — `render_claude_style` (:391), `fix_prompt` (:567), `mod tests` (:583 onward)
    - `.gitignore` :63-72 (the commented-block idiom)
    - `.planning/user/DEV-SETUP-CHECKLIST.md` § 8 (:421-430)
  </read_first>
  <behavior>
    - Snapshot test `claude_style_full_execute_fix_prompt_snapshot` renders
      `render_claude_style` for `StageIntent::Code { phase: PhaseId::new(47), fix: Some(FixType::FullExecute) }`
      and compares it against a committed `.snap` baseline.
    - The committed baseline records the PRE-FIX text: it contains the `/gsd-execute-phase` fix
      command and does NOT contain the `## Unattended decision checkpoints` heading. That absence
      IS the DECN-02 defect, captured as a reviewed artifact. Plan 47-02 drifts it deliberately and
      re-blesses it with an asserted content change — that drift is the guard's first real proof.
    - `scripts/check.sh test` passes on a matching baseline (the guard does not over-block).
  </behavior>
  <action>
    Add `insta = "1"` to `[workspace.dependencies]` in `Cargo.toml`, keeping default features
    (`colors`; 3 net-new lockfile crates — `insta`, `console`, `similar` — per RESEARCH A-6). Carry a
    comment in the `serde_json` idiom naming D-14/D-15 and the `env -u INSTA_FORCE_UPDATE`
    obligation this creates in `scripts/check.sh`.

    Add `insta = { workspace = true }` to `[dev-dependencies]` of BOTH `crates/devflow-core` and
    `crates/devflow-cli`, each with a one-line comment naming D-14/D-15 in the existing in-file
    idiom. Do NOT put `insta` behind a Cargo feature — `crates/devflow-core/Cargo.toml` already has
    a `[features]` block and adding it there would break the plain-dev-dependency assumption
    RESEARCH A-5 verified.

    Add ONE snapshot unit test to `crates/devflow-core/src/prompt.rs`'s `mod tests`, named
    `claude_style_full_execute_fix_prompt_snapshot`. It renders `render_claude_style` for
    `StageIntent::Code { phase: PhaseId::new(47), fix: Some(FixType::FullExecute) }` and asserts via
    `insta::assert_snapshot!` written FULLY QUALIFIED with no `use insta` import — that path form is
    what keeps `cargo machete` green with no ignore entry (RESEARCH A-5). Give it a doc comment in
    the house `doc-comment-as-rationale` shape naming D-14/D-15 and stating that the committed
    baseline is deliberately the pre-fix capture that DECN-02's fix must drift.

    Create the baseline ONCE, deliberately, with `INSTA_UPDATE=always cargo test -p devflow-core
    --lib claude_style_full_execute_fix_prompt_snapshot`, then READ the generated `.snap` file
    before staging it. This is the only sanctioned use of `INSTA_UPDATE=always` in this repo.

    Replace the body of `run_test` in `scripts/check.sh` with the RESEARCH A-0 form:
    `env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no cargo test --workspace --no-fail-fast`. Preserve the
    existing `--no-fail-fast` rationale comment verbatim and ADD the A-0 comment explaining that
    both `INSTA_UPDATE=no` and `env -u INSTA_FORCE_UPDATE` are load-bearing and why dropping either
    reopens a green-over-unread guard. Keep the `echo "==> ..."` banner and make it match the
    command actually run — `ci_parity_guards.rs` parses this file's non-comment lines. Do not touch
    `set -euo pipefail` at :9 and do not add the guard to the `all` target instead (D-16 Placement:
    CI's required Test job runs `scripts/check.sh test`).

    Add a commented block to `.gitignore` in the `:63-72` idiom that ignores `*.snap.new` and states
    explicitly that `*.snap` stays TRACKED because the baseline is the reviewable artifact.

    Correct `.planning/user/DEV-SETUP-CHECKLIST.md` § 8. The existing line "zero network-dependency
    policy (serde, clap, thiserror, tracing only)" (:423-424) is falsified for dev-dependencies by
    this change — rewrite that claim rather than appending a bullet beside it. Add the
    baseline-creation recipe from RESEARCH A-7 (`INSTA_UPDATE=always cargo test -p <crate>
    <testname>`, run once, read the file before committing) and note that `cargo-insta` is NOT
    installed and `check.sh` pins `INSTA_UPDATE=no`, so this recipe is the only way to create a
    baseline. This edit ships in the SAME commit as the dependency and CI change (repo rule,
    D-16 closing note).
  </action>
  <acceptance_criteria>
    - `Cargo.toml` `[workspace.dependencies]` contains an `insta` entry; both crate manifests
      contain `insta = { workspace = true }` under `[dev-dependencies]`.
    - Exactly one line in `scripts/check.sh` runs cargo test, and it contains both
      `env -u INSTA_FORCE_UPDATE` and `INSTA_UPDATE=no`.
    - At least one `.snap` file exists under `crates/devflow-core/src/snapshots/` and is git-tracked.
    - That baseline contains zero occurrences of `## Unattended decision checkpoints` (the recorded
      pre-fix state) and at least one occurrence of `/gsd-execute-phase`.
    - `.gitignore` contains a `*.snap.new` pattern and no bare `*.snap` pattern.
    - `.planning/user/DEV-SETUP-CHECKLIST.md` no longer claims "serde, clap, thiserror, tracing
      only" without qualification, and contains the string `INSTA_UPDATE=always`.
    - `scripts/check.sh test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; scripts/check.sh test > /tmp/47-01-t1.log 2>&1; e=$?; echo "check_test_exit=$e"; test "$e" -eq 0 || rc=1; h=$(grep -c "env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no cargo test" scripts/check.sh || true); echo "hardened_run_test_lines=$h"; test "$h" -eq 1 || rc=1; s=$(git ls-files -- "crates/devflow-core/src/snapshots" | wc -l); echo "tracked_snapshots=$s"; test "$s" -ge 1 || rc=1; p=$(cat crates/devflow-core/src/snapshots/*.snap | grep -c "## Unattended decision checkpoints" || true); echo "policy_heading_in_baseline=$p"; test "$p" -eq 0 || rc=1; c=$(cat crates/devflow-core/src/snapshots/*.snap | grep -c "/gsd-execute-phase" || true); echo "fix_command_in_baseline=$c"; test "$c" -ge 1 || rc=1; g=$(grep -c "snap.new" .gitignore || true); echo "gitignore_snapnew_lines=$g"; test "$g" -ge 1 || rc=1; k=$(grep -c "INSTA_UPDATE=always" .planning/user/DEV-SETUP-CHECKLIST.md || true); echo "checklist_recipe_lines=$k"; test "$k" -ge 1 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically any of `check_test_exit` non-zero, `hardened_run_test_lines` not equal to 1, `tracked_snapshots=0`, `policy_heading_in_baseline` greater than 0, `fix_command_in_baseline=0`, `gitignore_snapnew_lines=0`, or `checklist_recipe_lines=0`.</fails_when>
  </verify>
  <done>
    `insta` is wired at workspace level and in both crates; one reviewed `.snap` baseline is
    committed recording the pre-fix prompt; `run_test` runs cargo test under
    `env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no`; `.gitignore` ignores `*.snap.new` only; the
    DEV-SETUP-CHECKLIST § 8 correction and baseline recipe ship in the same commit.
  </done>
  <reversibility rating="costly">A new dev-dependency plus a CI wiring change — undoing means
  removing three lockfile crates, reverting `run_test`, and deleting every baseline the phase
  commits (D-14).</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Demonstrate the guard failing-then-fixed, then pin the wiring with two source guards</name>
  <files>crates/devflow-cli/tests/ci_parity_guards.rs, crates/devflow-cli/tests/gitignore_coverage.rs</files>
  <read_first>
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § A-1, § A-2, § A-3 (the three-case control and the exact env combinations), § C-1 (the D-16 control block)
    - `.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md` § `Shared Patterns` → "Asserting on a shell script's source" and "Negative controls are structural, not optional"
    - `crates/devflow-cli/tests/ci_parity_guards.rs` — `check_script_fails_fast_before_any_cargo_invocation` (:94-135) is the exact analog, including the `.filter(|l| !l.starts_with('#'))` comment-stripping and the `repo_root()` helper (:37-41)
    - `crates/devflow-cli/tests/gitignore_coverage.rs` — `gitignore_covers_devflow_runtime_state_paths` (:44-50) and its one-path-per-invocation discipline
    - `crates/devflow-cli/tests/plan_bashism_scanner.rs` :9-16 — the header explaining when source-asserting is correct and when script-executing is
    - `scripts/check.sh` as written by Task 1
  </read_first>
  <behavior>
    - `check_script_pins_snapshots_against_env_override`: reading `scripts/check.sh` with comment
      lines stripped, the single line that invokes cargo test contains BOTH `env -u
      INSTA_FORCE_UPDATE` and `INSTA_UPDATE=no`. Fails if either is removed.
    - `gitignore_ignores_snapshot_scratch_but_not_the_baseline`: `git check-ignore` reports a
      `.snap.new` path as ignored AND reports a `.snap` path as NOT ignored — one path per
      invocation. The second half is the negative control; without it a `.gitignore` that ignored
      everything would pass.
  </behavior>
  <action>
    FIRST produce the failing direction, because a guard that has only ever been observed passing
    has not been observed at all. Temporarily restore the un-hardened `run_test` body (plain
    `cargo test --workspace --no-fail-fast`), append a drift line to the committed `.snap`, run
    `INSTA_FORCE_UPDATE=1 scripts/check.sh test`, and record that it exits 0 AND rewrote the
    baseline — that is the RESEARCH A-3 defect, observed on this repository rather than inferred
    from the scratch crate. Restore the hardened body and `git checkout --` the baseline before
    continuing.

    THEN run the RESEARCH C-1 three-case control against the hardened `run_test`, restoring the
    baseline between cases: (1) drifted baseline, plain `scripts/check.sh test` — expect non-zero;
    (2) drifted baseline, `INSTA_FORCE_UPDATE=1 scripts/check.sh test` — expect non-zero, and this
    is the case that would have been 0 without `env -u`; (3) clean baseline — expect 0, the control
    proving the guard is not a constant-fail. Record all three exit codes and the pre/post baseline
    bytes in the plan SUMMARY. Note explicitly in the SUMMARY that case (1) alone would have gone
    red with or without the env var and therefore certifies `insta`, not the wiring (RESEARCH A-1,
    Pitfall 2) — case (2) is the one that measures the thing being built.

    THEN add `check_script_pins_snapshots_against_env_override` to
    `crates/devflow-cli/tests/ci_parity_guards.rs`, mirroring
    `check_script_fails_fast_before_any_cargo_invocation` including its comment-stripping filter, so
    the assertion reads code and not comment prose. Carry an inline comment stating why the
    source-asserting idiom is used rather than the script-executing idiom of
    `plan_bashism_scanner.rs`: running `scripts/check.sh test` from inside a test would recurse the
    entire workspace suite.

    THEN add `gitignore_ignores_snapshot_scratch_but_not_the_baseline` to
    `crates/devflow-cli/tests/gitignore_coverage.rs`, following that file's one-path-per-invocation
    rule and asserting BOTH directions.

    Confirm at the end that `git diff --name-only -- scripts/check.sh crates/devflow-core/src/snapshots`
    is empty relative to Task 1's commit — every temporary mutation above must be fully reverted.
  </action>
  <acceptance_criteria>
    - The SUMMARY records four exit codes: the un-hardened `INSTA_FORCE_UPDATE=1` run (0, the
      defect), and the hardened drift / force-update / clean runs (non-zero, non-zero, 0).
    - `check_script_pins_snapshots_against_env_override` exists in `ci_parity_guards.rs` and passes
      with `1 passed` and a non-zero `filtered out` count.
    - `gitignore_ignores_snapshot_scratch_but_not_the_baseline` exists in `gitignore_coverage.rs`,
      asserts both directions, and passes with `1 passed` and a non-zero `filtered out` count.
    - No residual modification to `scripts/check.sh` or any `.snap` file after the demonstration.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; cargo test -p devflow --test ci_parity_guards check_script_pins_snapshots_against_env_override -- --exact 2>&1 | tee /tmp/47-01-t2a.log; echo "cargo_exit_a=${PIPESTATUS[0]}"; a=$(grep -c "test result: ok. 1 passed" /tmp/47-01-t2a.log || true); echo "guard_a_one_passed=$a"; test "$a" -eq 1 || rc=1; fa=$(grep -oE "[0-9]+ filtered out" /tmp/47-01-t2a.log | head -1); echo "guard_a_filtered=$fa"; case "$fa" in ""|"0 filtered out") rc=1;; esac; cargo test -p devflow --test gitignore_coverage gitignore_ignores_snapshot_scratch_but_not_the_baseline -- --exact 2>&1 | tee /tmp/47-01-t2b.log; echo "cargo_exit_b=${PIPESTATUS[0]}"; b=$(grep -c "test result: ok. 1 passed" /tmp/47-01-t2b.log || true); echo "guard_b_one_passed=$b"; test "$b" -eq 1 || rc=1; fb=$(grep -oE "[0-9]+ filtered out" /tmp/47-01-t2b.log | head -1); echo "guard_b_filtered=$fb"; case "$fb" in ""|"0 filtered out") rc=1;; esac; d=$(git diff --name-only -- scripts/check.sh crates/devflow-core/src/snapshots | wc -l); echo "residual_dirty_paths=$d"; test "$d" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `guard_a_one_passed` or `guard_b_one_passed` not equal to 1 (a bare `--exact` miss prints `0 passed` and still exits 0), either `filtered` value empty or the literal `0 filtered out`, or `residual_dirty_paths` greater than 0.</fails_when>
  </verify>
  <done>
    The guard has been observed failing in the direction it claims to catch, on this repository,
    with the `INSTA_FORCE_UPDATE` case distinguished from the plain-drift case; and two Rust guards
    now fail if either the `env -u` wiring or the `.snap`/`.snap.new` ignore split is removed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Confirm cargo deny and cargo machete on the REAL workspace, with a discriminating control</name>
  <files>None modified — this task asserts over Cargo.toml, crates/devflow-core/Cargo.toml, crates/devflow-cli/Cargo.toml and deny.toml, and must leave all four byte-identical to their Task 1 state</files>
  <precondition>Network access to crates.io is available and `cargo deny` 0.20.2 and `cargo machete` 0.9.2 are on PATH (RESEARCH § Environment Availability).</precondition>
  <read_first>
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § A-4, § A-5, § A-8 — every result there comes from a scratch crate at `/tmp`, and A-8 names this confirming run as a required plan task rather than an assumption
    - `deny.toml` (the licence allowlist at :89-94)
    - `crates/devflow-core/Cargo.toml` as written by Task 1
  </read_first>
  <action>
    Run `cargo deny check` and `cargo machete` against this workspace now that `insta` is present,
    and record both exit codes and their relevant output lines in the plan SUMMARY.

    Then produce the negative control RESEARCH A-4 used, because a clean `cargo deny` result and a
    broken `cargo deny` invocation look identical: temporarily move `insta` from
    `[dev-dependencies]` to `[dependencies]` in `crates/devflow-core/Cargo.toml`, run `cargo deny
    list`, and confirm the `insta` subtree (`insta`, `similar`, `console`) NOW appears where it did
    not before. Then revert the manifest and confirm `git diff --name-only` over the three manifests
    is empty. If the control does not change the output, the measurement is broken and the
    dev-dependency-exclusion finding must be reported as unconfirmed rather than restated.

    Do NOT add a `deny.toml` entry or a `[package.metadata.cargo-machete] ignored` entry unless this
    run actually demands one — RESEARCH A-4/A-5 predict neither is needed, and adding an unnecessary
    suppression would mask a future real finding.
  </action>
  <acceptance_criteria>
    - `cargo deny check` exits 0 on the workspace with `insta` as a dev-dependency.
    - `cargo machete` exits 0 and its output names no `insta`-related unused dependency.
    - The SUMMARY records the negative control: `cargo deny list` output with `insta` as a regular
      dependency lists the `insta` subtree, and with it as a dev-dependency does not.
    - `Cargo.toml`, `crates/devflow-core/Cargo.toml` and `crates/devflow-cli/Cargo.toml` are
      byte-identical to their Task 1 committed state.
    - No `deny.toml` change and no `cargo-machete` ignore entry was added.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; cargo deny check > /tmp/47-01-deny.log 2>&1; e=$?; echo "deny_check_exit=$e"; test "$e" -eq 0 || rc=1; cargo machete > /tmp/47-01-machete.log 2>&1; m=$?; echo "machete_exit=$m"; test "$m" -eq 0 || rc=1; u=$(grep -c "insta" /tmp/47-01-machete.log || true); echo "machete_insta_mentions=$u"; test "$u" -eq 0 || rc=1; d=$(git diff --name-only -- Cargo.toml crates/devflow-core/Cargo.toml crates/devflow-cli/Cargo.toml deny.toml | wc -l); echo "manifest_diff_paths=$d"; test "$d" -eq 0 || rc=1; ig=$(cat crates/devflow-core/Cargo.toml crates/devflow-cli/Cargo.toml | grep -v "^[[:space:]]*#" | grep -c "cargo-machete" || true); echo "machete_ignore_entries=$ig"; test "$ig" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `deny_check_exit` or `machete_exit` non-zero, `machete_insta_mentions` greater than 0 (machete flagged `insta` as unused, meaning the fully-qualified-macro assumption in RESEARCH A-5 does not hold here), `manifest_diff_paths` greater than 0 (the control was not reverted), or `machete_ignore_entries` greater than 0 (an unnecessary suppression was added — counted over a comment-stripped concatenation of both manifests, so a `#` comment saying no suppression was needed does not read as a suppression).</fails_when>
  </verify>
  <done>
    The RESEARCH A-4/A-5 findings are confirmed on this workspace rather than transferred from a
    scratch crate, with a control proving the measurement discriminates; no suppression entry was
    needed or added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer/CI shell environment → `scripts/check.sh` | Exported env vars cross into the build and can change what "green" means |
| crates.io → workspace lockfile | Three net-new dev-dependency crates enter the build graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-01 | Tampering | `run_test` in `scripts/check.sh` | high | mitigate | `env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no` — `INSTA_FORCE_UPDATE=1` otherwise overrides `INSTA_UPDATE=no`, rewrites the baseline and exits 0 (RESEARCH A-3). Pinned durably by `check_script_pins_snapshots_against_env_override` (Task 2) |
| T-47-02 | Repudiation | committed `.snap` baselines | high | mitigate | `.gitignore` ignores `*.snap.new` only, so every baseline change appears in the PR diff; `gitignore_ignores_snapshot_scratch_but_not_the_baseline` asserts both directions (Task 2) |
| T-47-03 | Tampering | supply chain — `insta`, `similar`, `console` | medium | mitigate | RESEARCH Package Legitimacy Audit: all three OK on crates.io, all MIT/Apache-2.0, all satisfied by the existing `deny.toml` allowlist; confirmed on the real workspace by Task 3 with a discriminating control |
| T-47-04 | Elevation of Privilege | `INSTA_UPDATE=always` in the baseline recipe | low | accept | The recipe is documented in DEV-SETUP-CHECKLIST as a deliberate one-shot; `check.sh` pins `INSTA_UPDATE=no` so it cannot leak into a build verdict |
| T-47-SC | Tampering | cargo installs | high | mitigate | Package Legitimacy Audit present in `47-RESEARCH.md`; zero `[ASSUMED]`/`[SUS]`/`[SLOP]` verdicts on load-bearing grounds — `insta` was operator-named in D-14 and was built, fetched and executed during research, so no blocking human checkpoint is required |
</threat_model>

<verification>
- `scripts/check.sh test` exits 0 on a clean tree and non-zero on a drifted baseline, including
  under `INSTA_FORCE_UPDATE=1`.
- The two new Rust guards fail if the `env -u` wiring or the `.snap` ignore split is removed.
- `cargo deny check` and `cargo machete` are green on the real workspace, with the A-4 control
  reproduced.
</verification>

<success_criteria>
- One reviewed `.snap` baseline is committed and its pre-fix content is asserted, not assumed.
- The guard has been observed failing in the `INSTA_FORCE_UPDATE` direction on this repository.
- No baseline anywhere in Phase 47 lands before this plan's demonstration is on record.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

| Kind | Symbol / path |
|---|---|
| manifest entry | `insta` in `[workspace.dependencies]` (`Cargo.toml`) |
| manifest entry | `insta = { workspace = true }` in `[dev-dependencies]` of both crates |
| test | `claude_style_full_execute_fix_prompt_snapshot` (`crates/devflow-core/src/prompt.rs`) |
| directory + golden | `crates/devflow-core/src/snapshots/` and its first `.snap` baseline |
| shell function body | hardened `run_test` in `scripts/check.sh` (`env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no`) |
| ignore rule | `*.snap.new` in `.gitignore` |
| test | `check_script_pins_snapshots_against_env_override` (`crates/devflow-cli/tests/ci_parity_guards.rs`) |
| test | `gitignore_ignores_snapshot_scratch_but_not_the_baseline` (`crates/devflow-cli/tests/gitignore_coverage.rs`) |
| doc section | corrected § 8 in `.planning/user/DEV-SETUP-CHECKLIST.md` + the baseline-creation recipe |

<output>
Create `.planning/phases/47-unattended-decision-policy-consistency/47-01-SUMMARY.md` when done.
It MUST record the four exit codes from Task 2 (un-hardened force-update run, then hardened
drift / force-update / clean) and the Task 3 `cargo deny list` control in both directions.
</output>
