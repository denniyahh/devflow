---
phase: 47-unattended-decision-policy-consistency
plan: 05
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - docs/guides/unattended-mode.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/47-unattended-decision-policy-consistency/47-PHASE49-OBSERVATION.md
autonomous: true
requirements: [DECN-02, DECN-03]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`docs/guides/unattended-mode.md` no longer claims a preflight refusal is final — an operator approving the parked gate calls `launch_stage_inner` directly and skips the just-adjudicated check (D-13)."
    - "The `blocking-human` never-auto-approved statement is QUALIFIED with the resumed-to-resolve exception and the human approval that precedes it, not inverted (D-13)."
    - "A future reader of the ROADMAP and REQUIREMENTS cannot conclude that hermes and antigravity were audited and excluded — the four/two adapter split is named in the written record (D-06)."
    - "Phase 49 carries a one-line pointer to `47-PHASE49-OBSERVATION.md` and NOTHING else changes in its criteria — D-12 is rescinded (D-09, D-12)."
    - "The behavioural question DECN-03 does not answer has a written evidence standard that distinguishes followed / not followed / void (D-10)."
    - "The two latent defects the D-13 research uncovered are on the backlog rather than only in a phase artifact (D-13)."
  artifacts:
    - docs/guides/unattended-mode.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/47-unattended-decision-policy-consistency/47-PHASE49-OBSERVATION.md
  key_links:
    - "`47-PHASE49-OBSERVATION.md` ← a one-line pointer inside Phase 49's existing criterion 4; the full evidence definition stays with the phase that found it (D-09)."
    - "the `unattended-mode.md` rewrite ← `preflight.rs:1368-1376`, the `GateAction::Advance` call site whose own comment says approval skips the just-adjudicated check."
---

<objective>
Correct the written record so it matches what the code now does, and hand DECN-03's unanswered
behavioural question to Phase 49 with a named evidence standard.

Purpose: this phase changed prompt text, widened an agent's authority under one narrow condition,
and uncovered two latent defects along the way. None of that is finished until the operator-facing
guide stops asserting something false, the requirement text stops implying two adapters were
audited and excluded, and the question this phase deliberately did not answer is written down as a
question rather than left as silence.

Output: a corrected `unattended-mode.md`, amended ROADMAP and REQUIREMENTS entries, a
`47-PHASE49-OBSERVATION.md` observation item, and two backlog entries.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md
@.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md
@.planning/phases/47-unattended-decision-policy-consistency/47-03-SUMMARY.md
</context>

<!-- Comment-text discipline: literals below are flagged by the coarse `<action>` heuristic but
     are safe here. Each line records why the gate cannot be self-invalidated by the plan's own text. -->
<!-- planner-discipline-allow: A refusal is final -->  <!-- the false heading being removed; the gate is anchored to ^# so the rewrite may legitimately quote the old claim in body prose while correcting it -->
<!-- planner-discipline-allow: Advance -->  <!-- gated POSITIVELY — the rewrite must name the override mechanism -->
<!-- planner-discipline-allow: never auto-approved by any mode -->  <!-- gated in BOTH directions: it must still be present (D-13 says qualify, not invert) and must carry a qualifier within four lines -->
<!-- planner-discipline-allow: pipeline_launch.rs:1569 -->  <!-- gated POSITIVELY — the claude-only citation the validation file must carry -->
<!-- planner-discipline-allow: 47-PHASE49-OBSERVATION.md -->  <!-- gated at exactly one occurrence in the Phase 49 section, not at zero -->
<!-- planner-discipline-allow: % -->  <!-- negative gate (percent_sign_lines == 0) over 47-PHASE49-OBSERVATION.md only; this plan's own prose names the character and is never read by it -->
<!-- Filename note (2026-09-10): D-09 names `47-VALIDATION.md` for this memo, but GSD's plan-phase
     workflow reserves that exact path for the Nyquist validation file generated from
     ~/.claude/gsd-core/templates/VALIDATION.md whenever RESEARCH.md carries a `## Validation
     Architecture` section — and 47-RESEARCH.md:633 does. A /gsd-validate-phase run reading that
     path would find a behavioural evidence standard where it expects a test-coverage map. D-09's
     substance is "the full evidence definition lives with the phase, plus a one-line ROADMAP
     pointer"; the filename is incidental to that intent, so the memo moves and the pointer follows
     it. The Nyquist file keeps 47-VALIDATION.md. -->

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the false "a refusal is final" claim and qualify the never-auto-approved statement</name>
  <files>docs/guides/unattended-mode.md</files>
  <read_first>
    - `docs/guides/unattended-mode.md` — read `:25-35` and `:68-120` as ONE pass before editing anything. The paragraph immediately after `:88` opens "Both external review lanes objected to that, on record, and the decision stands" and is anchored to the very claim being rewritten; editing `:85-88` without reading through `:110` orphans it.
    - `crates/devflow-cli/src/preflight.rs` — `:971-973` (the check applies at Define and Code only), `:1027-1030` (auto admits only claude and antigravity), `:1067-1083` (the declared-checkpoint scan), and `:1368-1376` (`GateAction::Advance` calling `launch_stage_inner` with the comment that approval skips the just-adjudicated check)
    - `crates/devflow-core/src/verify.rs` — `:131-137` (whole-file `blocking-human` substring, arms the resume) versus `:207-220` (line-anchored human-only match, used by preflight), and the `:191-196` comment acknowledging the pair is deliberate with opposite failure directions
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-13 — which line is false, which line is merely imprecise, and why
    - `.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md` § `docs/guides/unattended-mode.md` — the surrounding prose pattern: state the condition, then name the recovery on its own line
  </read_first>
  <action>
    Rewrite the `### A refusal is final` section (`:85-88`). The claim is false: `GateAction::Advance`
    is exactly the override it denies exists, and it is reachable from the parked gate a refusal
    creates. Describe the actual mechanism — a refusal parks at a gate; an operator approving that
    gate calls `launch_stage_inner` directly and skips the just-adjudicated check; the run's mode is
    unchanged, so it continues unattended afterwards and can reach the resume injection. Then name
    what that costs the operator, in the surrounding prose's shape (condition, then recovery on its
    own line). Replace the section heading with one that is true.

    Then deal with the paragraph that follows it. "Both external review lanes objected to that, on
    record, and the decision stands" is anchored to the claim being removed. Do not leave it
    dangling and do not delete the reviewers' objection silently — rework it so it still records
    that the objection was heard, against whatever the accurate statement now is. Say in the plan
    SUMMARY which of those two you did and why.

    Then qualify — do not invert — the statement at `:29-31` that `blocking-human` checkpoints and
    `checkpoint:human-action` tasks are never auto-approved by any mode. It is very nearly accurate
    for the ordinary path and should stay. Add the exception: an agent DevFlow resumed specifically
    to resolve such a gate may do so, and reaching that state requires a human to have approved the
    parked gate first. Keep it short; the full mechanism belongs in the rewritten section, not here.

    Do NOT document mechanisms 2 and 3 from D-13 (the preflight/resume predicate mismatch and the
    TOCTOU window on agent-writable plans) as behaviour in this guide. They are latent defects, not
    features, and Task 3 files them to the backlog. Documenting a defect as documented behaviour is
    how it stops being a defect on paper while still being one in the product.
  </action>
  <acceptance_criteria>
    - `docs/guides/unattended-mode.md` contains no HEADING line reading `A refusal is final`. The
      gate is anchored to `^#` deliberately: the rewrite may legitimately quote the old claim in
      body prose while correcting it, and an unanchored count would fail the plan for doing exactly
      what it was asked to do.
    - It contains zero occurrences of the phrase `no .--force-unattended. flag` (the backtick
      positions matched loosely).
    - It names `Advance` — the actual override — at least once.
    - Within four lines of `never auto-approved by any mode` it contains at least one of `unless`,
      `except` or `resumed`, so the statement is qualified rather than left absolute.
    - It contains no mention of a predicate mismatch or a time-of-check window.
    - The reviewers'-objection paragraph is either reworked against the new claim or deliberately
      removed, with the choice stated in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; f=docs/guides/unattended-mode.md; h=$(grep -c "^#.*A refusal is final" "$f" || true); echo "false_heading_sites=$h"; test "$h" -eq 0 || rc=1; n=$(grep -c "no .--force-unattended. flag" "$f" || true); echo "false_no_override_claim_sites=$n"; test "$n" -eq 0 || rc=1; a=$(grep -c "Advance" "$f" || true); echo "override_named_sites=$a"; test "$a" -ge 1 || rc=1; q=$(grep -A 4 "never auto-approved by any mode" "$f" | grep -c -E "unless|except|resumed" || true); echo "qualifier_near_absolute_claim=$q"; test "$q" -ge 1 || rc=1; b=$(grep -c "never auto-approved by any mode" "$f" || true); echo "absolute_claim_still_present=$b"; test "$b" -ge 1 || rc=1; l=$(grep -c -E "predicate mismatch|time-of-check" "$f" || true); echo "latent_defect_leak_sites=$l"; test "$l" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `false_heading_sites` (heading lines only) or `false_no_override_claim_sites` greater than 0 (the false claim survived), `override_named_sites=0` (the rewrite did not name the actual mechanism), `qualifier_near_absolute_claim=0` (the `:29-31` statement was left absolute), `absolute_claim_still_present=0` (the statement was INVERTED or deleted rather than qualified — D-13 says qualify; this is the control in the opposite direction), or `latent_defect_leak_sites` greater than 0.</fails_when>
  </verify>
  <done>
    The guide describes the operator-approval override that actually exists, the never-auto-approved
    statement carries its exception, and the reviewers' objection is not orphaned.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write 47-PHASE49-OBSERVATION.md and add the single-line Phase 49 pointer</name>
  <files>.planning/phases/47-unattended-decision-policy-consistency/47-PHASE49-OBSERVATION.md, .planning/ROADMAP.md</files>
  <read_first>
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-09, D-10 (the corrected evidence standard), D-11 (the claude-only limit and its three independent constraints), D-12 (RESCINDED), and § Specific Ideas (recency, not primacy — and the instruction that the uncited percentages must NOT become an acceptance threshold)
    - `.planning/ROADMAP.md` § `### Phase 49:` — the five existing Success Criteria, criterion 4 being the one that already carries DECN-03's behavioural question
    - `crates/devflow-cli/src/pipeline_launch.rs` `:1102-1108` — the `checkpoint_auto_decided` emission fires BEFORE the resumed agent is spawned, which is why the event alone proves injection and not decision
    - `.planning/phases/47-unattended-decision-policy-consistency/47-03-SUMMARY.md` — what actually landed, so the observation item describes the shipped text
    - a prior phase's `NN-VALIDATION.md` for the file's shape
  </read_first>
  <action>
    Write `.planning/phases/47-unattended-decision-policy-consistency/47-PHASE49-OBSERVATION.md` carrying the
    full evidence definition for DECN-03's behavioural arm, so Phase 49 can act on it without
    re-deriving it:

    Followed = the `checkpoint_auto_decided` event fires AND the resumed session's capture shows the
    concrete resolution AND the final message carries the comparison reasoning the policy demands
    AND the run progresses past that gate. Not followed = the agent reported and halted instead,
    with the session having actually run. Void = the spawn failed and only the event is present;
    neither reading applies. State plainly why the event alone is insufficient: it is emitted before
    the spawn, deliberately, so that a spawn failure still leaves the decision attempt on record — a
    failed spawn would satisfy a naive "followed" test.

    Record the scope limit: DECN-03 closes for claude alone. `pipeline_launch.rs:1569` gates the
    resume route on `AgentKind::Claude`, `exec_resume_command` exists only on `ClaudeDriver`, and
    `--mode auto` preflight admits only claude and antigravity. Three of the four claude-style
    adapters can never reach the co-resident prompt and two cannot start an unattended run at all.
    The model-specificity concern is real and is preserved as a stated limit on what closure means,
    not as a test.

    Record the position-effect framing: the resume delivers `checkpoint_auto_decide_prompt` as the
    LATEST turn of a multi-turn session while `CODE_STAGE_POLICY` sits in turn 1, so the governing
    effect is recency, not intra-document primacy. Phase 49 should record which turn carried which
    rule alongside the outcome. Do NOT restate the uncited compliance percentages from the first
    context draft, do not turn any number into an acceptance threshold, and do not use the `%`
    character anywhere in this file: the gate refuses a `%` outright rather than trying to judge
    whether a figure is being presented as a threshold.

    Then add ONE line to Phase 49's existing criterion 4 in `.planning/ROADMAP.md` pointing at this
    file. Add nothing else to Phase 49. D-12 is rescinded and re-confirmed by the operator: Phase 49
    stays a single live claude run. Do not add a criterion, do not change the criteria count, and do
    not introduce a four-adapter requirement.
  </action>
  <acceptance_criteria>
    - `47-PHASE49-OBSERVATION.md` exists and contains all three verdict labels: `Followed`, `Not followed`,
      `Void`.
    - It states that the `checkpoint_auto_decided` event is emitted before the spawn.
    - It names the claude-only limit and cites `pipeline_launch.rs:1569`.
    - It contains no `%` character (so no percentage can be presented as a threshold).
    - Phase 49's ROADMAP section references `47-PHASE49-OBSERVATION.md` exactly once.
    - Phase 49's ROADMAP section still has exactly five numbered success criteria.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; v=.planning/phases/47-unattended-decision-policy-consistency/47-PHASE49-OBSERVATION.md; test -f "$v"; e=$?; echo "validation_file_exists_exit=$e"; test "$e" -eq 0 || rc=1; for w in Followed Void; do c=$(grep -c "$w" "$v" 2>/dev/null); c=${c:-0}; echo "verdict_${w}_sites=$c"; test "$c" -ge 1 || rc=1; done; nf=$(grep -c -i "not followed" "$v" 2>/dev/null); nf=${nf:-0}; echo "verdict_not_followed_sites=$nf"; test "$nf" -ge 1 || rc=1; g=$(grep -c "pipeline_launch.rs:1569" "$v" 2>/dev/null); g=${g:-0}; echo "claude_only_citation_sites=$g"; test "$g" -ge 1 || rc=1; bs=$(grep -c -i "before[^.]*spawn" "$v" 2>/dev/null); bs=${bs:-0}; echo "event_before_spawn_sites=$bs"; test "$bs" -ge 1 || rc=1; pc=$(grep -c "%" "$v" 2>/dev/null); pc=${pc:-0}; echo "percent_sign_lines=$pc"; test "$pc" -eq 0 || rc=1; sec=/tmp/47-05-p49.txt; sed -n "/^### Phase 49:/,/^### Phase 50:/p" .planning/ROADMAP.md > "$sec"; sl=$(wc -l < "$sec"); echo "phase49_section_lines=$sl"; test "$sl" -ge 20 || rc=1; p=$(grep -c "47-PHASE49-OBSERVATION.md" "$sec" || true); echo "phase49_pointer_sites=$p"; test "$p" -eq 1 || rc=1; k=$(grep -c -E "^  [0-9]+\." "$sec" || true); echo "phase49_criteria_count=$k"; test "$k" -eq 5 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `validation_file_exists_exit` non-zero, any verdict-label count 0, `claude_only_citation_sites=0`, `event_before_spawn_sites=0` (the memo never says the audit event fires before the spawn — the one fact that makes the event insufficient evidence on its own), `percent_sign_lines` greater than 0 (a percentage appears anywhere in the memo), `phase49_section_lines` below 20 (the section extraction returned nothing, so every count over it would read as a clean zero — this is the control on the extraction itself), `phase49_pointer_sites` not exactly 1, or `phase49_criteria_count` not exactly 5 (a criterion was added or removed, which D-12's rescission forbids). Both memo additions were verified 2026-09-11 against fixture memos: a compliant memo passes; one missing the before-spawn sentence, one containing a `%`, and a missing file each fail.</fails_when>
  </verify>
  <done>
    Phase 49 is structurally reminded by one line, the evidence standard that distinguishes a real
    decision from a failed spawn lives with the phase that derived it, and Phase 49's scope is
    untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: Amend the ROADMAP goal and DECN-02 for the real adapter split, and file the two latent defects</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § `### Phase 47:` (the goal at :96-100 names only Claude/OpenCode) and the `999.12x` backlog entries around `:568` for the entry shape — Found / prose / Fix shape / Acceptance
    - `.planning/REQUIREMENTS.md` — DECN-02 (:18-26), DECN-03 (:28-34), and the DECN-03 note (:185-187)
    - `crates/devflow-core/src/agents/mod.rs` :173-197 — the dispatch proving the four/two split
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-06, D-13 (both latent-defect mechanisms with their `file:line` evidence), and § Deferred Ideas — the operator decided on 2026-09-10 NOT to file the adapter-independent-resume follow-on as a numbered entry
    - `CLAUDE.md` § "Prefer GSD commands over doing it by hand" and § "Keep the active milestone's phase headings inside its own window"
  </read_first>
  <action>
    Amend the ROADMAP Phase 47 goal and REQUIREMENTS DECN-02 to name hermes and antigravity
    alongside claude and opencode. The real blast radius is every adapter routed through
    `render_claude_style` — four of six — and codex and pi are correct because they use
    `render_workflow_style`. Say the split explicitly, so a future reader cannot conclude hermes and
    antigravity were audited and excluded. Also record in DECN-03's REQUIREMENTS text that its
    closure is claude-only and why, citing `pipeline_launch.rs:1569` (the `AgentKind::Claude` gate on
    the resume route) — the gate below checks the DECN-03 entry for that citation.

    Use a GSD command where one covers the edit (`/gsd:phase` for the phase entry) rather than
    hand-editing, per the repo rule. Where you must hand-edit, use a scoped replacement — never a
    whole-file write of `ROADMAP.md` — and say in the SUMMARY which edits were hand-made and why.
    Note the known defect that `phase.add` files new entries at the document's last `---`, which on
    this roadmap lands them in archived prose; if that happens, move the entry and record it.

    Then file the two latent defects from D-13 as SEPARATE backlog entries, `999.125` and `999.126`,
    beside the existing `999.12x` cluster. They are separate because their fixes are separate — one
    unifies two predicates, the other closes a re-scan window — and a single entry would be closed
    when half of it was done. Entry one: `phase_has_human_only_checkpoint` is line-anchored
    (`verify.rs:207-220`) while `phase_has_blocking_human_checkpoint` is a whole-file substring match
    (`verify.rs:131-137`), so a plan carrying `gate="blocking-human"` anywhere other than a
    task-opening line passes preflight and still arms the resume; the `verify.rs:191-196` comment
    acknowledges the pair is deliberate with opposite failure directions, which is precisely the
    wedge. Entry two: the preflight check applies only at Define and Code (`preflight.rs:971-973`)
    while plan files are agent-writable during Code (`verify.rs:118-119`), so a gate declared after
    Code's preflight ran is never re-scanned. Give each a Fix shape and an Acceptance line in the
    house entry shape.

    Do NOT file the adapter-independent-resume follow-on. The operator explicitly declined a
    numbered entry for it on 2026-09-10; it stays a note in `47-CONTEXT.md` § Deferred Ideas.

    Finish by spot-checking the milestone window: run `roadmap.analyze` and confirm a non-zero
    `phase_count` and a real `next_phase`. New `999.x` entries must land in the archived-backlog
    region, not inside the active milestone's heading-to-next-heading window, or the
    milestone-scoped parsers misfire silently.
  </action>
  <acceptance_criteria>
    - The ROADMAP Phase 47 section names both `hermes` and `antigravity`.
    - The REQUIREMENTS DECN-02 entry names both `hermes` and `antigravity`.
    - The REQUIREMENTS DECN-03 entry states the claude-only closure limit.
    - `### Phase 999.125:` and `### Phase 999.126:` both exist in `.planning/ROADMAP.md`, each with
      a `**Fix shape:**` and an `**Acceptance:**` line.
    - No backlog entry mentions adapter-independent resume or per-driver resume.
    - `roadmap.analyze` reports a non-zero `phase_count` and a non-empty `next_phase`.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; sec=/tmp/47-05-p47.txt; sed -n "/^### Phase 47:/,/^### Phase 48:/p" .planning/ROADMAP.md > "$sec"; sl=$(wc -l < "$sec"); echo "phase47_section_lines=$sl"; test "$sl" -ge 20 || rc=1; for w in hermes antigravity; do c=$(grep -ci "$w" "$sec" || true); echo "roadmap47_${w}_sites=$c"; test "$c" -ge 1 || rc=1; done; req=/tmp/47-05-decn02.txt; sed -n "/DECN-02/,/DECN-03/p" .planning/REQUIREMENTS.md > "$req"; rl=$(wc -l < "$req"); echo "decn02_block_lines=$rl"; test "$rl" -ge 5 || rc=1; for w in hermes antigravity; do c=$(grep -ci "$w" "$req" || true); echo "decn02_${w}_sites=$c"; test "$c" -ge 1 || rc=1; done; req3=/tmp/47-05-decn03.txt; sed -n "/^- \[.\] \*\*DECN-03\*\*/,/^### /p" .planning/REQUIREMENTS.md > "$req3"; r3=$(wc -l < "$req3"); echo "decn03_block_lines=$r3"; test "$r3" -ge 5 || rc=1; c3=$(grep -ci "claude" "$req3" || true); echo "decn03_claude_sites=$c3"; test "$c3" -ge 1 || rc=1; g3=$(grep -c "pipeline_launch.rs:1569" "$req3" || true); echo "decn03_gate_citation_sites=$g3"; test "$g3" -ge 1 || rc=1; for e in 999.125 999.126; do c=$(grep -c "### Phase $e:" .planning/ROADMAP.md || true); echo "backlog_${e}_headings=$c"; test "$c" -eq 1 || rc=1; done; for e in 999.125 999.126; do w=/tmp/47-05-w-$e.txt; sed -n "/^### Phase $e:/,/^### Phase /{/^### Phase $e:/!{/^### Phase /q};p}" .planning/ROADMAP.md > "$w"; wl=$(wc -l < "$w"); echo "backlog_${e}_window_lines=$wl"; test "$wl" -ge 5 || rc=1; fx=$(grep -c "Fix shape:" "$w" || true); echo "backlog_${e}_fix_shape=$fx"; test "$fx" -ge 1 || rc=1; ac=$(grep -c "Acceptance:" "$w" || true); echo "backlog_${e}_acceptance=$ac"; test "$ac" -ge 1 || rc=1; done; ai=$(grep -ci -E "adapter-independent|per-driver resume" .planning/ROADMAP.md || true); echo "declined_followon_sites=$ai"; test "$ai" -eq 0 || rc=1; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query roadmap.analyze > /tmp/47-05-analyze.json 2>&1; ae=$?; echo "roadmap_analyze_exit=$ae"; test "$ae" -eq 0 || rc=1; pc=$(grep -c -E "\"phase_count\": *[1-9]" /tmp/47-05-analyze.json || true); echo "nonzero_phase_count=$pc"; test "$pc" -ge 1 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `phase47_section_lines` below 20 or `decn02_block_lines` below 5 (the section extraction returned nothing, so the adapter-name counts over it would read as clean zeros — the control on the extraction), any `hermes`/`antigravity` count 0, `decn03_block_lines` below 5 (the DECN-03 extraction returned nothing), `decn03_claude_sites` or `decn03_gate_citation_sites` equal to 0 (DECN-03's entry does not state the claude-only limit, or does not cite the gate that causes it), either `backlog_*_headings` not exactly 1, any `backlog_*_window_lines` below 5 (that entry's heading-to-next-heading window came back empty, so the two counts over it would read as clean zeros — the per-entry emptiness control), any `backlog_*_fix_shape` or `backlog_*_acceptance` below 1, `declined_followon_sites` greater than 0 (an entry the operator declined was filed anyway), `roadmap_analyze_exit` non-zero, or `nonzero_phase_count=0` (the new entries landed inside the active milestone's window and broke the milestone-scoped parsers).

**Why these counts are scoped per entry rather than file-wide.** The earlier form counted `Fix shape:` across the WHOLE of ROADMAP.md and required at least 2. Measured 2026-09-10: the file already contains **3** such lines from unrelated entries, so that gate was pre-satisfied and could not distinguish the two entries this task creates — it would have passed had the task written nothing at all. The `**Acceptance:**` line the acceptance criterion demands had no gate whatsoever. Both now count inside each entry's own `### Phase NNN:`-to-next-`### Phase ` window, and each window carries its own non-empty control (verified against the existing 999.105 and 999.104 entries, whose windows extract at 90 and 15 lines).

**DECN-03 check.** Extracted from the DECN-03 bullet to the next `### ` heading, so the traceability-table row and the note further down cannot leak in. Verified 2026-09-11: today's REQUIREMENTS.md reads 10 lines with 0 and 0 (red); a copy with the claude-only sentence added reads 11 lines with 1 and 1 (green).</fails_when>
  </verify>
  <done>
    The written record names the real four/two adapter split, DECN-03's closure carries its stated
    limit, both latent defects are on the backlog as separately-closable entries, the declined
    follow-on was not filed, and the milestone window still parses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator reading `unattended-mode.md` → operating decisions | A false statement in the guide is acted on as though it were a product guarantee |
| phase artifact → project backlog | A defect recorded only inside a phase directory is invisible to every later phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-16 | Repudiation | `unattended-mode.md` asserting a refusal cannot be overridden | high | mitigate | Task 1 rewrites `:85-88` to describe `GateAction::Advance` and what approving the parked gate costs; the gate asserts the false claim is gone AND that the `:29-31` statement was qualified rather than inverted |
| T-47-17 | Elevation of Privilege | the preflight/resume predicate mismatch and the TOCTOU window | high | transfer | Out of scope to fix here; filed as `999.125` and `999.126` with `file:line` evidence and a Fix shape each. Leaving them unrecorded is what this gate prevents |
| T-47-18 | Repudiation | DECN-03 read as fully closed when it closes for claude only | medium | mitigate | The claude-only limit is recorded in both `47-PHASE49-OBSERVATION.md` and the REQUIREMENTS DECN-03 entry, with the three constraints that make it structural |
| T-47-19 | Tampering | a whole-file write to `ROADMAP.md` destroying entries outside the diff window | high | mitigate | Task 3 mandates scoped edits and a GSD command where one exists; `roadmap.analyze` is run afterwards as the structural check |
</threat_model>

<verification>
- `unattended-mode.md` carries no false claim, and the qualified statement is still present rather
  than inverted.
- Phase 49's criteria count is unchanged at five with exactly one pointer added.
- `roadmap.analyze` returns a non-zero `phase_count` after the backlog entries land.
</verification>

<success_criteria>
- ROADMAP criterion 4: the behavioural question is recorded as an explicit observation item for
  Phase 49, naming in advance what would count as evidence either way — not marked settled on a
  source read.
- D-06's written-record correction is in place for both the ROADMAP and REQUIREMENTS.
</success_criteria>

## Artifacts this phase produces

Created or changed by this plan:

| Kind | Symbol / path |
|---|---|
| doc section | rewritten override section replacing `### A refusal is final` (`docs/guides/unattended-mode.md`) |
| doc qualification | the `blocking-human` never-auto-approved statement, qualified (`docs/guides/unattended-mode.md:29-31`) |
| planning record | `.planning/phases/47-unattended-decision-policy-consistency/47-PHASE49-OBSERVATION.md` |
| roadmap amendment | Phase 47 goal naming hermes and antigravity; one-line `47-PHASE49-OBSERVATION.md` pointer in Phase 49 criterion 4 |
| requirements amendment | DECN-02 adapter split; DECN-03 claude-only closure limit |
| backlog entry | `### Phase 999.125:` — preflight/resume checkpoint-predicate mismatch |
| backlog entry | `### Phase 999.126:` — TOCTOU window on agent-writable plans after Code preflight |

<output>
Create `.planning/phases/47-unattended-decision-policy-consistency/47-05-SUMMARY.md` when done.
It MUST state what was done with the reviewers'-objection paragraph, which ROADMAP edits were made
by hand rather than by a GSD command and why, and confirm the declined adapter-independent-resume
follow-on was NOT filed.
</output>
