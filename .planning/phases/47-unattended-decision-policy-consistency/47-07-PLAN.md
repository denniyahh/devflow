---
phase: 47-unattended-decision-policy-consistency
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - scripts/test-phase-worktree-guard.sh
  - .planning/user/DEV-SETUP-CHECKLIST.md
autonomous: true
requirements: [WR-01]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "scripts/test-phase-worktree-guard.sh runs all of its cases on a host whose global git config enables SSH commit signing and a hooks directory, including when no ssh-agent holds the signing key (WR-01; operator decision 2026-09-11 A2)."
    - "The script's scratch repository is isolated from inherited git hooks AND commit signing through its own local config, set before its first commit."
    - "The guard under test is still invoked directly — six `\"$GUARD\" --staged` calls, unchanged — so the isolation does not bypass the behaviour being tested (operator decision A2)."
    - "Removing the isolation makes the script print `FAIL 7c.` on every host, not only on a host with signing enabled, because case 7 re-runs the bootstrap under a deliberately hostile global config and shows that config is armed."
    - "DEV-SETUP-CHECKLIST.md states the script's case count and its isolation."
  artifacts:
    - path: scripts/test-phase-worktree-guard.sh
      provides: "hermetic fixture bootstrap (init_fixture_repo) and case 7 (7a, 7b, 7c)"
      contains: "config commit.gpgsign false"
    - path: .planning/user/DEV-SETUP-CHECKLIST.md
      provides: "case count and isolation note for the worktree guard test"
      contains: "isolated from inherited hooks and commit signing"
  key_links:
    - "init_fixture_repo -> fixture-local core.hooksPath (empty dir under $TMP) and commit.gpgsign false -> first commit, and git worktree add (post-checkout)."
    - "case 7c -> init_fixture_repo under GIT_CONFIG_GLOBAL pointing at a hostile config; 7a and 7b show that config's hook and signing are each armed."
  prohibitions:
    - statement: "MUST NOT bypass, stub or wrap the guard under test to make the fixture pass — isolation applies to the scratch repository's git config only (operator decision A2)."
      category: integrity
      requirement_id: WR-01
      status: resolved
      verification: test
    - statement: "MUST NOT read, load or prompt for the operator's real signing key, or modify the operator's global git config or hooks, while testing — hostile configs are scratch files with a nonexistent key path."
      category: safety
      requirement_id: WR-01
      status: resolved
      verification: judgment
---

<objective>
Close WR-01 from 47-REVIEW.md. The worktree-guard test's scratch repository inherits the operator's
global git setup, so its bootstrap commit can abort before any case runs.

**In scope by operator decision 2026-09-11 (A2).** The verifier ruled WR-01 not a Phase 47 must-have,
and it maps to neither DECN-02 nor DECN-03. `requirements` therefore names the review finding rather
than claiming a DECN link that does not exist.

**The review's diagnosis is incomplete. Verified at planning time, with controls:**
- **Host config.** The global config sets `core.hooksPath` (a pre-commit that runs gitleaks),
  `commit.gpgsign true` and `gpg.format ssh`.
- **Signing alone aborts the commit.** In a scratch repository with the ssh-agent removed and hooks
  disabled, the commit still fails: exit 128, "Enter passphrase … failed to write commit object".
- **The hook alone does not.** With signing disabled and the inherited hook active, gitleaks runs,
  finds no leaks, and the commit exits 0.
- **Both disabled** exits 0.
- **The real script** under the same agent-less condition exits 128 before any case.

So disabling hooks alone — the reviewer's suggested fix, and the operator's "must not run inherited git
hooks" — would not fix the observed failure. This plan isolates the fixture from both inherited hooks
and commit signing. That satisfies the operator's intent (the bootstrap must not be aborted by inherited
global state) and does not contradict it.

Output: a hermetic bootstrap, a permanent case 7 that fails on any host if the isolation is removed, and
an updated checklist line.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/47-unattended-decision-policy-consistency/47-REVIEW.md

Worktree: every command runs from `/var/home/denniyahh/Github/devflow/.worktrees/phase-47` on branch
`feature/phase-47`. Re-assert `git rev-parse --abbrev-ref HEAD` before every commit.

`scripts/test-phase-worktree-guard.sh` is not on `scripts/hooks/post-commit`'s DEV-SETUP-CHECKLIST watch
list. The checklist is still updated in the same commit as the case-count change, because the checklist
states the count (CLAUDE.md "Keep DEV-SETUP-CHECKLIST.md in sync").

The script is not run by CI or by `scripts/check.sh`. A regression in it shows only when someone runs it;
this plan does not change that.
</context>

<!-- planner-discipline-allow: isolated from inherited hooks and commit signing -->

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — isolate the existing fixture bootstrap from inherited hooks and signing</name>
  <files>scripts/test-phase-worktree-guard.sh</files>
  <read_first>
    - scripts/test-phase-worktree-guard.sh — whole file (99 lines at b3b47ea): header rationale, the hermetic `unset` at :13, the mode checks at :26-33 (run under `set -o pipefail`), the bootstrap at :47-55, and every `"$GUARD" --staged` call
    - scripts/lint-phase-worktree.sh — the guard under test; read only, do not modify
    - scripts/hooks/pre-commit:95-108 — how the guard is wired, and why this test exists
  </read_first>
  <behavior>
    - RED (current script): the first `<automated>` gate prints `gate_rc=1`, with `hostile_summary=no_summary_line` (a scratch global config with a failing hook and an unusable signing key aborts the bootstrap) and, on this host, `host_no_agent_exit=128` (SSH signing with no agent).
    - GREEN: the same gate prints `passed=10 failed=0` for the hostile, hermetic and host-without-agent runs, keeps six direct guard invocations, and shows one hooks-isolation line and one signing-isolation line.
  </behavior>
  <action>
    **RED.** Run this task's `<automated>` gate on the unmodified script and paste its output line into
    SUMMARY. It must print `gate_rc=1`. This task has no committed test; the harness is the red test.
    Task 2 commits the permanent regression case.

    **GREEN (per operator decision A2, extended by the verified signing cause).** In
    `scripts/test-phase-worktree-guard.sh`, add three things immediately after the line
    `git init -q -b workspace/tester .` and before `git config user.email`:
    - `mkdir -p "$TMP/no-hooks"`
    - `git config core.hooksPath "$TMP/no-hooks"`
    - `git config commit.gpgsign false`

    Add a short comment above them. It must say that both the inherited hooks and inherited SSH commit
    signing come from global config, that signing with no ssh-agent was the observed abort (exit 128),
    and that these are fixture-local settings, so the guard's own environment is untouched.

    Do not export `GIT_CONFIG_GLOBAL` or `GIT_CONFIG_NOSYSTEM` in the script, and do not wrap or change
    any `"$GUARD" --staged` invocation. Either would change the environment of the guard under test.

    Run the gate: `gate_rc=0`. Commit as `fix(47-07): ...`.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; S=$(mktemp -d); trap "rm -rf \"$S\"" EXIT; mkdir -p "$S/hooks"; for h in pre-commit post-checkout; do printf "#!/bin/sh\nexit 1\n" > "$S/hooks/$h"; chmod +x "$S/hooks/$h"; done; printf "[core]\n\thooksPath = %s/hooks\n[commit]\n\tgpgsign = true\n[gpg]\n\tformat = ssh\n[user]\n\tsigningkey = %s/no-such-key.pub\n\tname = Hostile\n\temail = h@example.com\n" "$S" "$S" > "$S/hostile.gitconfig"; t=scripts/test-phase-worktree-guard.sh; GIT_CONFIG_GLOBAL="$S/hostile.gitconfig" GIT_CONFIG_NOSYSTEM=1 timeout 300 bash "$t" > "$S/hostile.log" 2>&1; he=$?; GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 timeout 300 bash "$t" > "$S/hermetic.log" 2>&1; me=$?; env -u SSH_AUTH_SOCK -u SSH_AGENT_PID -u DISPLAY -u WAYLAND_DISPLAY -u SSH_ASKPASS SSH_ASKPASS_REQUIRE=never timeout 300 setsid -w bash "$t" < /dev/null > "$S/host.log" 2>&1; oe=$?; echo "hostile_exit=$he hermetic_exit=$me host_no_agent_exit=$oe"; if [ "$he" -ne 0 ] || [ "$me" -ne 0 ] || [ "$oe" -ne 0 ]; then rc=1; fi; for r in hostile hermetic host; do s=$(grep "^passed=" "$S/$r.log" || echo no_summary_line); p=$(printf "%s" "$s" | sed -nE "s/^passed=([0-9]+) failed=0$/\1/p"); echo "${r}_summary=$s"; if [ -z "$p" ] || [ "$p" -lt 10 ]; then rc=1; fi; done; g=$(grep -c "\"\$GUARD\" --staged" "$t" || true); echo "guard_invocations=$g"; if [ "$g" -ne 6 ]; then rc=1; fi; h=$(grep -c "config core.hooksPath" "$t" || true); k=$(grep -c "config commit.gpgsign false" "$t" || true); echo "hooks_isolation_lines=$h signing_isolation_lines=$k"; if [ "$h" -lt 1 ] || [ "$k" -lt 1 ]; then rc=1; fi; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit. Specifically:
    - Any of `hostile_exit`, `hermetic_exit`, `host_no_agent_exit` is non-zero.
    - Any `*_summary` is `no_summary_line` (the script aborted before its summary), or reports fewer than 10 passed, or a non-zero `failed=`.
    - `guard_invocations` is not 6.
    - `hooks_isolation_lines` or `signing_isolation_lines` is 0.

    Tested at planning time against git-repository copies: it failed on the current script (`hostile_summary=no_summary_line`, `host_no_agent_exit=128`), passed with the three isolation lines (`passed=10 failed=0` in all three runs), and still passes after Task 2 (`passed=13 failed=0`). The host run only discriminates on a host whose global config enables commit signing; elsewhere it passes trivially.</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY carries the RED gate output line (`gate_rc=1`, with `hostile_summary=no_summary_line`).
    - `scripts/test-phase-worktree-guard.sh` contains `git config core.hooksPath "$TMP/no-hooks"` and `git config commit.gpgsign false`, both after `git init -q -b workspace/tester .` and before the first `git commit`.
    - `git ls-files --stage -- scripts/test-phase-worktree-guard.sh` still reports mode `100755`.
  </acceptance_criteria>
  <done>
    The script runs every case on this host under a hostile global config and under the reviewer's
    failing condition (signing with no agent), with the guard still invoked directly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the isolation a permanent, host-independent regression case</name>
  <files>scripts/test-phase-worktree-guard.sh, .planning/user/DEV-SETUP-CHECKLIST.md</files>
  <read_first>
    - scripts/test-phase-worktree-guard.sh — as left by Task 1, including the `check` helper's refuse/allow contract
    - .planning/user/DEV-SETUP-CHECKLIST.md:84-99 — the worktree-guard bullet that states the script's case count
  </read_first>
  <behavior>
    - The script prints `passed=13 failed=0` under a hostile global config, hermetically, and on this host without an ssh-agent.
    - The same script with its two isolation lines removed prints `FAIL 7c.` and `passed=12 failed=1` in a hermetic run — a host with no signing and no hooks configured — so the regression shows on any host.
    - 7a and 7b pass in both versions, which shows the hostile config's hook and signing are each armed.
  </behavior>
  <action>
    In `scripts/test-phase-worktree-guard.sh`:

    1. **Fold the bootstrap into a function** `init_fixture_repo <dir>`, defined after `check()` and
       before first use.
       - It creates the directory and `$TMP/no-hooks`.
       - It runs `git -C "$dir" init -q -b workspace/tester .`.
       - Then, each on its own line: `git -C "$dir" config core.hooksPath "$TMP/no-hooks"` and
         `git -C "$dir" config commit.gpgsign false`.
       - Then the existing `user.email`/`user.name` config, the three fixture files at paths under
         `"$dir"`, `git -C "$dir" add -A`, and `git -C "$dir" commit -qm init`.

       Replace the inline bootstrap with `REPO="$TMP/repo"`, then `init_fixture_repo "$REPO"`, then
       `cd "$REPO"`. Cases 1-6 and 3b stay byte-identical.
    2. **Hostile config.** Add a section headed `== fixture isolation from inherited git config ==`
       after the degenerate-inputs section.
       - Create `$TMP/hostile/hooks/pre-commit` and `$TMP/hostile/hooks/post-checkout`, each an
         executable `/bin/sh` script that touches `$TMP/hostile/fired` and exits 1.
       - Create `$TMP/empty-hooks`.
       - Write `$TMP/hostile/gitconfig` with `core.hooksPath` pointing at those hooks,
         `commit.gpgsign = true`, `gpg.format = ssh`, `user.signingkey` pointing at a nonexistent
         `$TMP/hostile/no-such-key.pub`, and a user name and email.
    3. **Helper** `hostile_commit <dir> <git -c args...>`. In a subshell that exports
       `GIT_CONFIG_GLOBAL="$TMP/hostile/gitconfig"` and `GIT_CONFIG_NOSYSTEM=1`, it runs `git init`,
       adds one file, and runs `git "$@" commit`. It deliberately applies NO fixture isolation.
    4. **The three checks,** each with `rm -f "$TMP/hostile/fired"` before it and a `set +e` / `set -e`
       bracket, using exactly these names:
       - `7a. inherited hook armed: unisolated commit -> refuse (control)`: call
         `hostile_commit … -c commit.gpgsign=false`. Expected `refuse`, but count it as allow if the
         fired marker is absent, since the refusal must come from the hook.
       - `7b. inherited signing armed: unisolated commit -> refuse (control)`: call
         `hostile_commit … -c core.hooksPath="$TMP/empty-hooks"`. Expected `refuse`, but count it as
         allow if the fired marker exists.
       - `7c. fixture bootstrap under hostile config -> allow`: run `init_fixture_repo` in a subshell
         exporting the same two variables. Expected `allow`, but count it as refuse if the fired marker
         exists.

       Write the 7a/7b `-c` arguments exactly as shown (`=` form, no `config ` prefix), so they cannot
       be confused with the isolation lines the gate's mutant strips.

    In `.planning/user/DEV-SETUP-CHECKLIST.md`, in the worktree-guard bullet, change the script's case
    count from 7 cases to 8 cases. After that bullet's closing "discriminate)." add the sentence
    `Its scratch repository is isolated from inherited hooks and commit signing; case 7 proves it under a hostile global git config.`

    Run the gate and commit both files together as `test(47-07): ...`.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; S=$(mktemp -d); M=scripts/.mutant-guard-test.sh; trap "rm -rf \"$S\"; rm -f \"$M\"" EXIT; mkdir -p "$S/hooks"; for h in pre-commit post-checkout; do printf "#!/bin/sh\nexit 1\n" > "$S/hooks/$h"; chmod +x "$S/hooks/$h"; done; printf "[core]\n\thooksPath = %s/hooks\n[commit]\n\tgpgsign = true\n[gpg]\n\tformat = ssh\n[user]\n\tsigningkey = %s/no-such-key.pub\n\tname = Hostile\n\temail = h@example.com\n" "$S" "$S" > "$S/hostile.gitconfig"; t=scripts/test-phase-worktree-guard.sh; GIT_CONFIG_GLOBAL="$S/hostile.gitconfig" GIT_CONFIG_NOSYSTEM=1 timeout 300 bash "$t" > "$S/hostile.log" 2>&1; he=$?; GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 timeout 300 bash "$t" > "$S/hermetic.log" 2>&1; me=$?; env -u SSH_AUTH_SOCK -u SSH_AGENT_PID -u DISPLAY -u WAYLAND_DISPLAY -u SSH_ASKPASS SSH_ASKPASS_REQUIRE=never timeout 300 setsid -w bash "$t" < /dev/null > "$S/host.log" 2>&1; oe=$?; echo "hostile_exit=$he hermetic_exit=$me host_no_agent_exit=$oe"; if [ "$he" -ne 0 ] || [ "$me" -ne 0 ] || [ "$oe" -ne 0 ]; then rc=1; fi; for r in hostile hermetic host; do s=$(grep "^passed=" "$S/$r.log" || echo no_summary_line); echo "${r}_summary=$s"; if [ "$s" != "passed=13 failed=0" ]; then rc=1; fi; done; c7=$(grep -cE "^  ok   7[abc]\. " "$S/hermetic.log" || true); echo "case7_ok_lines=$c7"; if [ "$c7" -ne 3 ]; then rc=1; fi; g=$(grep -c "\"\$GUARD\" --staged" "$t" || true); echo "guard_invocations=$g"; if [ "$g" -ne 6 ]; then rc=1; fi; grep -v -e "config core.hooksPath" -e "config commit.gpgsign false" "$t" > "$M"; chmod +x "$M"; GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_NOSYSTEM=1 timeout 300 bash "$M" > "$S/mutant.log" 2>&1; xe=$?; rm -f "$M"; ms=$(grep "^passed=" "$S/mutant.log" || echo no_summary_line); mf=$(grep -cE "^  FAIL 7c\. " "$S/mutant.log" || true); echo "mutant_exit=$xe mutant_summary=$ms mutant_fail_7c=$mf"; if [ "$xe" -eq 0 ] || [ "$mf" -ne 1 ] || [ "$ms" != "passed=12 failed=1" ]; then rc=1; fi; cl=.planning/user/DEV-SETUP-CHECKLIST.md; c8=$(grep -c "test-phase-worktree-guard.sh. (8 cases" "$cl" || true); co=$(grep -c "test-phase-worktree-guard.sh. (7 cases" "$cl" || true); ci=$(grep -c "isolated from inherited hooks and commit signing" "$cl" || true); echo "checklist_8_cases=$c8 checklist_7_cases=$co checklist_isolation_sentence=$ci"; if [ "$c8" -ne 1 ] || [ "$co" -ne 0 ] || [ "$ci" -ne 1 ]; then rc=1; fi; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit. Specifically:
    - **Summaries:** any run exits non-zero, or any `*_summary` is not exactly `passed=13 failed=0`.
    - **Case 7:** `case7_ok_lines` is not 3.
    - **Guard:** `guard_invocations` is not 6.
    - **Mutant** (a temporary `scripts/.mutant-guard-test.sh` copy with both isolation lines stripped; removed by `trap`): `mutant_exit=0`, `mutant_fail_7c` not 1, or `mutant_summary` not `passed=12 failed=1`. Any of these means case 7 no longer catches the regression.
    - **Checklist:** `checklist_8_cases` is not 1, `checklist_7_cases` is not 0, or `checklist_isolation_sentence` is not 1.

    These exact bytes were run at planning time against git-repository copies of the scripts directory. They failed on the Task 1 state (`case7_ok_lines=0`, `mutant_summary=passed=10 failed=0`). They failed on a finished script with the old checklist (`checklist_7_cases=1`). They passed on a finished script with the edited checklist, where the mutant printed `FAIL 7c.` and `passed=12 failed=1`.</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/test-phase-worktree-guard.sh` defines `init_fixture_repo` and `hostile_commit` and prints check names beginning `7a.`, `7b.` and `7c.`.
    - The gate's mutant run shows `  FAIL 7c. ` — the failing direction printed, not merely a non-pass.
    - `git ls-files --stage -- scripts/test-phase-worktree-guard.sh` still reports mode `100755`.
    - `.planning/user/DEV-SETUP-CHECKLIST.md` contains `isolated from inherited hooks and commit signing`, and its worktree-guard bullet states 8 cases. Both files are in the same commit.
  </acceptance_criteria>
  <done>
    Removing the fixture isolation now fails the script on any host with a named `FAIL 7c.`, and the
    checklist describes the script as it is.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host global git config and ssh-agent -> scratch fixture repository | Inherited hooks and signing settings cross into a repository the test owns |
| fixture isolation -> guard under test | Isolation must not change what the guard sees or how it is invoked |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-15 | Denial of Service | test-phase-worktree-guard.sh bootstrap | medium | mitigate | Fixture-local `core.hooksPath` set to an empty dir and `commit.gpgsign false`, before the first commit. Case 7c proves it under a hostile config; 7a and 7b prove that config is armed |
| T-47-16 | Tampering | guard under test (lint-phase-worktree.sh) | medium | mitigate | Six direct `"$GUARD" --staged` calls pinned by the gate. Isolation is repository config only, never exported environment. Controls 2, 5 and 6 still pass and case 1 still refuses |
| T-47-17 | Information Disclosure | operator's SSH signing key | low | mitigate | Hostile configs use a nonexistent key path under a temp dir. The host run removes the agent and askpass, so no key prompt can appear. The isolated fixture never attempts signing |
</threat_model>

<verification>
Both task gates exit 0. Each was run at planning time on git-repository copies, in both directions (see
each `<fails_when>`).

**Not established:**
- Isolation from hooks delivered other than through git config: `GIT_CONFIG_PARAMETERS`, a system
  config the fixture does not override, or `core.fsmonitor`.
- Behaviour on a host without signing configured. There, the host run passes trivially and only the
  hostile run and case 7 discriminate.
- Anything about CI. The script still runs only when invoked by hand.
</verification>

## Flagged assumptions (edge probe, unresolved)

- **DECN-02 / DECN-03 — unresolved, and not touched by this plan.** WR-01 maps to neither requirement;
  it is test-harness reliability for the phase-worktree guard.

## Artifacts this phase produces

- Shell functions `init_fixture_repo` and `hostile_commit` in `scripts/test-phase-worktree-guard.sh`.
- Check cases `7a.`, `7b.` and `7c.` in the same script; the script's summary becomes `passed=13 failed=0`.
- Temp paths the script creates under its own `$TMP`: `no-hooks/`, `empty-hooks/`, `hostile/`.
- No new tracked file.

<success_criteria>
- The worktree-guard test runs every case on this host whatever the global git hooks and signing
  settings are, and whether or not an ssh-agent is running.
- Removing the fixture isolation is caught on any host by a named `FAIL 7c.`
- The guard under test is invoked exactly as before.
</success_criteria>

<output>
Create `.planning/phases/47-unattended-decision-policy-consistency/47-07-SUMMARY.md` when done. It must
include:
- the Task 1 RED gate output line;
- both GREEN gate output lines;
- the Task 2 mutant result.
</output>
