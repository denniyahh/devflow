---
phase: 47-unattended-decision-policy-consistency
plan: 04
type: execute
wave: 4
depends_on: ["47-03"]
files_modified:
  - crates/devflow-core/src/prompt.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-core/src/snapshots
  - crates/devflow-cli/src/snapshots
autonomous: true
requirements: [DECN-02, DECN-03]

estimate:
  tokens: 28000
  raw_tokens: 55000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Every policy-carrying prompt this phase changed has a committed, reviewed `.snap` baseline, so a future wording edit surfaces as a review diff (D-14, D-15)."
    - "The two delivered turns are snapshotted SEPARATELY — no joined string anywhere in the suite (D-15, RESEARCH B-3)."
    - "The suite is scoped to policy-carrying prompts, so no large unreviewed baseline is blessed by default (D-15)."
  artifacts:
    - crates/devflow-core/src/snapshots
    - crates/devflow-cli/src/snapshots
    - crates/devflow-core/src/prompt.rs
    - crates/devflow-cli/src/pipeline_launch.rs
  key_links:
    - "each `.snap` ← `scripts/check.sh` `run_test` under `env -u INSTA_FORCE_UPDATE INSTA_UPDATE=no` (plan 47-01) — without that wiring these baselines are decoration."
    - "the two-turn pair snapshots ← `resolve_launch_shape` and `resume_launch_shape`, the same production constructors the delivery test drives (D-03, RESEARCH B-3)."
---

<objective>
Complete D-15's snapshot suite against the FINAL text, now that both DECN-02 and DECN-03 have
landed.

Purpose: D-14 exists so a wording change in a policy-carrying prompt surfaces as a review diff.
The suite is deliberately created after the fixes rather than before, so the reviewed baseline is
the corrected text — blessing pre-fix text as golden is the hazard D-15 names.

Output: named baselines for the six policy-carrying prompts and for the two delivered turns,
captured separately.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md
@.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md
@.planning/phases/47-unattended-decision-policy-consistency/47-01-SUMMARY.md
@.planning/phases/47-unattended-decision-policy-consistency/47-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Named baselines for the six policy-carrying prompts</name>
  <files>crates/devflow-core/src/prompt.rs, crates/devflow-core/src/snapshots/</files>
  <read_first>
    - `crates/devflow-core/src/prompt.rs` as written by plan 47-03 — `GATE_RESOLUTION_RULE`, `CODE_STAGE_POLICY`, `code_policy_applies_to_fix_arm`, `fix_prompt`, `workflow_code_prompt`, and the `claude_style_full_execute_fix_prompt_snapshot` test added by plan 47-01
    - `crates/devflow-core/src/agents/mod.rs` :174-182 — `driver_for`
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § A-6 (snapshot file locations and the `INSTA_UPDATE=always` baseline recipe) and § A-7
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-15 — the scope constraint
    - `.planning/phases/47-unattended-decision-policy-consistency/47-01-SUMMARY.md` — the baseline-creation procedure that was demonstrated
  </read_first>
  <action>
    Add one test to `crates/devflow-core/src/prompt.rs`'s `mod tests` that emits SIX named snapshots
    for the policy-carrying prompts D-15 scopes: the `FullExecute` fix prompt as each of the four
    `render_claude_style` adapters delivers it (claude, opencode, hermes, antigravity, driven through
    `driver_for(kind).render_prompt`), and the two `render_workflow_style` ones (codex, pi). Use
    explicit snapshot names via `insta::assert_snapshot!` with a name argument, written fully
    qualified with no `use insta`, so `cargo machete` stays green.

    The four claude-style baselines will be byte-identical to each other, and that redundancy is
    deliberate rather than an oversight: it is what turns an adapter silently switching render style
    into a visible snapshot diff. State that in the test's doc comment so a reviewer does not
    "clean it up".

    Leave the `claude_style_full_execute_fix_prompt_snapshot` test and its baseline from plan 47-01
    in place. It is not redundant bookkeeping: its two re-blessings — once in 47-02 when the policy
    arrived, once in 47-03 when the carve-out landed — are this phase's live evidence that the guard
    catches a real wording change on a real target, and its git history is the record of that.

    Do NOT extend the suite to the `GapsOnly`/`AuditFix` arms. D-15 scopes it to policy-carrying
    prompts, and those arms are already covered by
    `claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it`, whose failing direction
    was demonstrated in plan 47-02 Task 3 — a snapshot would add nothing that test does not already
    discriminate.

    Create the baselines with the sanctioned recipe (`INSTA_UPDATE=always cargo test -p devflow-core
    --lib <testname>`), then READ every generated `.snap` before staging. Confirm by eye that each
    carries the corrected carve-out text from 47-03 and not a stale variant.
  </action>
  <acceptance_criteria>
    - `crates/devflow-core/src/snapshots/` holds at least seven git-tracked `.snap` files (the
      47-01 baseline plus six named ones).
    - Every `.snap` file in that directory contains `## Unattended decision checkpoints` — the suite
      is policy-carrying prompts only, so a file without it means a wrong prompt was captured.
    - At least two of them contain the workflow-style phrase `Read and follow the GSD workflow file`.
    - At least five of them contain `/gsd-execute-phase`.
    - `scripts/check.sh test` exits 0 with all baselines committed.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; d=crates/devflow-core/src/snapshots; n=$(git ls-files -- "$d" | wc -l); echo "tracked_core_snapshots=$n"; test "$n" -ge 7 || rc=1; p=$(grep -l "## Unattended decision checkpoints" "$d"/*.snap | wc -l); echo "policy_carrying_snapshots=$p"; test "$p" -eq "$n" || rc=1; w=$(grep -l "Read and follow the GSD workflow file" "$d"/*.snap | wc -l); echo "workflow_style_snapshots=$w"; test "$w" -ge 2 || rc=1; c=$(grep -l "/gsd-execute-phase" "$d"/*.snap | wc -l); echo "claude_style_snapshots=$c"; test "$c" -ge 5 || rc=1; scripts/check.sh test > /tmp/47-04-t1.log 2>&1; e=$?; echo "check_test_exit=$e"; test "$e" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `tracked_core_snapshots` below 7, `policy_carrying_snapshots` not equal to `tracked_core_snapshots` (a captured prompt that does not carry the policy is out of D-15 scope and means the wrong prompt was snapshotted), `workflow_style_snapshots` below 2 or `claude_style_snapshots` below 5 (both render styles must be represented — if either count collapses, the suite is snapshotting one style twice rather than both once), or `check_test_exit` non-zero.</fails_when>
  </verify>
  <done>
    Six named baselines for the policy-carrying prompts are committed and read, both render styles
    are represented, and the whole suite passes under the hardened `run_test`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Snapshot the two delivered turns separately, and prove they are distinct artifacts</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs, crates/devflow-cli/src/snapshots/</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_launch.rs` as written by plan 47-03 — `resume_launch_shape`, `resolve_launch_shape` (:195), and `the_gate_rule_holds_in_both_delivered_turns`
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § B-3 — snapshot the two artifacts separately; joining them in a snapshot reintroduces exactly the composition D-03 rejects
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § A-6 — snapshots from a unit test in `pipeline_launch.rs` land at `crates/devflow-cli/src/snapshots/`
    - `crates/devflow-cli/Cargo.toml` — the `insta` dev-dependency added by plan 47-01
  </read_first>
  <action>
    Add named snapshots for the two delivered turns to `crates/devflow-cli/src/pipeline_launch.rs`.
    Capture each from the same production constructor the delivery test uses — turn 1 out of
    `MonitorLaunch::PipeOwning { prompt }` via `resolve_launch_shape`, turn 2 out of `argv[1]` via
    `resume_launch_shape` — and emit them as two separately-named snapshots, named exactly
    `turn_one_code_prompt` and `turn_two_resume_prompt` (`insta::assert_snapshot!("turn_one_code_prompt", …)`
    and `insta::assert_snapshot!("turn_two_resume_prompt", …)`). insta writes those as
    `devflow__pipeline_launch__tests__turn_one_code_prompt.snap` and
    `devflow__pipeline_launch__tests__turn_two_resume_prompt.snap` (naming verified 2026-09-11 in a
    scratch binary crate named `devflow` on insta 1.48.0), and the gate selects the pair by those
    names, never by directory order.

    Do NOT snapshot a joined string. Two independently reviewable `.snap` files is the D-15
    property, and a joined snapshot would reintroduce the concatenation D-03 rejected as a proxy for
    a composition production never performs.

    Whether this lives inside `the_gate_rule_holds_in_both_delivered_turns` or in a sibling test is
    yours; if it is a sibling, it must capture the turns the same way rather than re-typing them.

    Create the baselines with `INSTA_UPDATE=always cargo test -p devflow --bin devflow <testname>`
    and read both `.snap` files before staging. Confirm the turn-1 baseline carries the policy and
    the carve-out and the turn-2 baseline names `blocking-human`, and confirm they are not the same
    file content — if they are, one capture is not reaching the constructor it claims to.

    Finish with a full `scripts/check.sh all` so fmt, clippy and the whole suite are green together
    before this plan closes.
  </action>
  <acceptance_criteria>
    - `crates/devflow-cli/src/snapshots/` holds exactly one git-tracked file matching
      `*__turn_one_code_prompt.snap` and exactly one matching `*__turn_two_resume_prompt.snap`.
    - The turn-one file contains `## Unattended decision checkpoints`; the turn-two file does not,
      and contains `blocking-human`.
    - No `.snap` file in that directory contains both the turn-1 policy heading and the turn-2
      resume opening sentence — that would mean a joined capture.
    - The two named files are not byte-identical.
    - `scripts/check.sh all` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; d=crates/devflow-cli/src/snapshots; n=$(git ls-files -- "$d" | wc -l); echo "tracked_cli_snapshots=$n"; test "$n" -ge 2 || rc=1; t1=$(grep -l "## Unattended decision checkpoints" "$d"/*.snap | wc -l); echo "turn1_snapshots=$t1"; test "$t1" -ge 1 || rc=1; t2=$(grep -l "blocking-human" "$d"/*.snap | wc -l); echo "turn2_snapshots=$t2"; test "$t2" -ge 1 || rc=1; j=$(grep -l "## Unattended decision checkpoints" "$d"/*.snap | xargs grep -l "headless DevFlow run" 2>/dev/null | wc -l); echo "joined_capture_files=$j"; test "$j" -eq 0 || rc=1; n1=$(ls "$d"/*__turn_one_code_prompt.snap 2>/dev/null | wc -l); echo "turn_one_snapshot_files=$n1"; test "$n1" -eq 1 || rc=1; n2=$(ls "$d"/*__turn_two_resume_prompt.snap 2>/dev/null | wc -l); echo "turn_two_snapshot_files=$n2"; test "$n2" -eq 1 || rc=1; first=$(ls "$d"/*__turn_one_code_prompt.snap 2>/dev/null | head -1); second=$(ls "$d"/*__turn_two_resume_prompt.snap 2>/dev/null | head -1); echo "pair_first=$first"; echo "pair_second=$second"; h1=$(cat "$first" 2>/dev/null | grep -c "## Unattended decision checkpoints" || true); echo "turn_one_policy_heading=$h1"; test "$h1" -ge 1 || rc=1; h2=$(cat "$second" 2>/dev/null | grep -c "## Unattended decision checkpoints" || true); echo "turn_two_policy_heading=$h2"; test "$h2" -eq 0 || rc=1; b2=$(cat "$second" 2>/dev/null | grep -c "blocking-human" || true); echo "turn_two_blocking_human=$b2"; test "$b2" -ge 1 || rc=1; if test -n "$first" && test -n "$second"; then diff -q "$first" "$second" > /dev/null; s=$?; else s=0; fi; echo "two_snapshots_identical_exit=$s"; test "$s" -ne 0 || rc=1; scripts/check.sh all > /tmp/47-04-t2.log 2>&1; e=$?; echo "check_all_exit=$e"; test "$e" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `tracked_cli_snapshots` below 2, `turn1_snapshots` or `turn2_snapshots` equal to 0, `joined_capture_files` greater than 0 (one file carries both the turn-1 policy heading and the turn-2 resume opening, meaning the capture was concatenated), `turn_one_snapshot_files` or `turn_two_snapshot_files` not exactly 1 (a named snapshot is missing or duplicated), `turn_one_policy_heading` equal to 0 (the turn-one file does not hold the Code prompt), `turn_two_policy_heading` greater than 0 (the turn-two file holds the Code prompt, not the resume prompt), `turn_two_blocking_human` equal to 0, `two_snapshots_identical_exit` equal to 0 (the two named files are byte-identical, or one is missing so there was nothing to compare — both read as failure deliberately), or `check_all_exit` non-zero.

**Why the pair is selected by name.** The previous form compared the first two `.snap` files in `ls` order. Reproduced 2026-09-11: with two unrelated snapshots sorting ahead of the pair, it compared those instead and passed while both turn files held identical turn-one content. Selecting by name and checking each file for its own turn's marker was verified on the same fixtures: the correct pair with decoys present passes; both-turn-one, both-turn-two, swapped, and a missing turn-two file all fail; and the old form still passes the decoy case, which is the control showing the fixtures exercise the defect.</fails_when>
  </verify>
  <done>
    The two delivered turns have separate committed baselines, proven distinct, and the full
    `scripts/check.sh all` target is green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author intent → committed golden file | A blessed baseline becomes the definition of correct; an unread one launders a wrong string into the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-13 | Tampering | blessing a baseline without reading it | high | mitigate | Baselines are created only after both fixes land, with the `INSTA_UPDATE=always` recipe run once and every generated file read before staging; content assertions in both tasks pin what each baseline must contain |
| T-47-14 | Spoofing | a joined turn-1+turn-2 snapshot standing in for a composition production never performs | medium | mitigate | Task 2's `joined_capture_files` gate and the byte-identity control on the pair |
| T-47-15 | Repudiation | scope creep blessing a large unreviewed baseline | medium | mitigate | D-15 scope enforced by the `policy_carrying_snapshots` equality gate — a captured non-policy prompt fails the plan rather than quietly joining the suite |
</threat_model>

<verification>
- Seven or more core baselines, all policy-carrying, both render styles represented.
- Two CLI baselines, distinct, neither joined.
- `scripts/check.sh all` green.
</verification>

<success_criteria>
- D-15's suite exists against the final text, scoped as decided, with every baseline read before it
  was committed.
</success_criteria>

## Artifacts this phase produces

Created by this plan:

| Kind | Symbol / path |
|---|---|
| test | the six-named-snapshot policy-carrying prompt test (`crates/devflow-core/src/prompt.rs`) |
| goldens | six named `.snap` baselines under `crates/devflow-core/src/snapshots/` |
| goldens | two named `.snap` baselines under `crates/devflow-cli/src/snapshots/` (turn 1 stdin, turn 2 argv) |

<output>
Create `.planning/phases/47-unattended-decision-policy-consistency/47-04-SUMMARY.md` when done.
It MUST list every `.snap` file the phase now tracks and confirm each was read before staging.
</output>
