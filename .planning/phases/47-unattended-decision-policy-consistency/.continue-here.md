---
context: phase
phase: 47-unattended-decision-policy-consistency
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-12T23:22:31.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure this session.

- [ ] CONSTRAINT: phase work only inside `.worktrees/phase-47`. Two codex sessions ran GSD from the main checkout, and one planned against `workspace/denniyahh`'s stale `.planning/`, producing stray commit `b102898`. On 2026-09-12 an edit aimed at the main checkout's `.planning/user/DEV-SETUP-CHECKLIST.md` was refused only because that file had not been read. Assert `git rev-parse --show-toplevel` is the worktree and the branch is `feature/phase-47` before any GSD command or commit, and use the worktree's absolute path for every edit.
- [ ] CONSTRAINT: verify reviewer and agent claims against source before acting. codex implemented AND self-verified twice. Its second round was green on the full suite while its detector narrowing had left 9 regression tests unable to fail; this was caught on 2026-09-12 only by disabling the branch those tests guard, in a scratch copy.
- [ ] CONSTRAINT: a passing gate is not evidence until its printed counters are read. Three gates finished in 0 seconds and were only accepted after their logs showed `1 passed; 771 filtered out` and real region counts.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Agent session in the main checkout | codex loads `AGENTS.md` (graphify-only), never sees CLAUDE.md's worktree rule; the pre-commit guard only warns on phase docs | blocking | Pin the worktree root and branch before every GSD command and commit |
| Self-verification treated as review | codex marked REVIEW.md `resolved` and VERIFICATION `passed` without an independent reviewer | blocking | Independent review, and verify each finding against source, before UAT |
| Narrowing a matcher without re-probing its negatives | codex line-anchored `text_reports_human_gate`; the stream-scoping negatives' mid-line fixture then matched nothing, so they passed with the stream branch disabled, and 3 controls were rewritten to presence checks that hid it | blocking | Disable the guarded branch in a scratch copy; the same negatives must fail before and after the change |
| Hardening a CI-run script without CI's environment | `67cab06` nulled global git config harness-wide; it passed on the host and in the local container gate, but CI runs as root on a runner-owned checkout accepted only through a global `safe.directory`, so the harness exited 128 with no output there. The codex lane missed it for the same reason | blocking | Run any change to a script CI executes inside the pinned CI image as uid 0 on a uid-1001-owned checkout (`scratchpad/fixes/verify_fix1.sh` is the template) |
| Optional fix read as approved | "all fixes except IN-05" was taken to include IN-02's conditional detector narrowing, which codex's own summary had just listed as deferred | advisory | Present an optional or conditional fix as its own decision |
| A missing SUMMARY field read as a mismatch | 47-01..04 carry no top-level `commits:`, which verify-work treats as a legacy WARNING; an ad-hoc recount first reported the empty value as a BLOCKER | advisory | Treat an absent field as its own state, never as a failed comparison |
| `gsd_run query commit` on new files | Silently no-ops on untracked files | advisory | `git add` the explicit paths, then `git commit` |
| Heredoc inside `bash -c` | Fails to parse through the Bash tool's zsh eval wrapper | advisory | Write files with the Write tool; commit with `git commit -F <file>` |
| `state.record-session` recomputes progress | Counted phase 47 complete (2/6, 33%) before UAT on its first call; a second call left the counters alone | advisory | `git diff .planning/STATE.md` after any state verb; revert `completed_phases`, `percent` and the `Progress:` bar by hand |
| `/gsd-code-review` incremental scoping | The last commit touching REVIEW.md becomes the diff base, so resolution annotations scope a re-review to 0 files ("Skipping review") | blocking | Pass `--files=a,b,...` — the `=` spelling; `--files a,b` parses to 0 files |
| Code-review depth step on an unset overrides key | `config-get … --default '[]'` prints the quoted string `"[]"`; the resolver rejects it `not_an_array` and the workflow halts before spawning the reviewer | advisory | Resolve with a real `[]` — the config depth (`deep`) comes back |
| A red suite blamed on the change | 999.114: devflow-core has five module-local `ENV_MUTEX`es and `pi.rs`'s `PathGuard` replaces `PATH` with a stub dir, so a concurrent spawn fails; `spawn_with_timeout_kills_a_hung_child` hit `sleep: command not found` on 2026-09-12 | advisory | Read the failure message first; rerun the test alone, then one full rerun |

<current_state>
Phase 47: all seven plans executed; STATE.md `status: awaiting_uat`. Pre-UAT work is complete and UAT can start.
The first review round is closed: every `47-REVIEW.md` finding except IN-05 (GitHub #210) is fixed (`67cab06`..`9a2c7fe`, resolutions `5385022`).
The final external review (codex and agy, 2026-09-12) of the phase code `034f5b6..c4d7231` found 4 issues. All were verified here and all are fixed by operator decision: `ac3ee42` (list and blockquote gate lines), `c66cba2` (harness exit 128 in CI's container), `2790678` (harness skipped when cargo fails) and `49bb324` (literal backslash-n false positive). The record is in `47-REVIEWS.md`, section `## Final code review — phase 47 code, pre-UAT`.
Committed tree `49bb324`: full scripts/check.sh test passed first time: 31 targets ok, 774 core and 364 CLI tests (including 27 CI-parity and 3 check-script tests), harness passed=13 failed=0; HEAD and crates/scripts unchanged during the run. One host run, not CI.
The UAT commit-claim BLOCKER is a confirmed gsd-core defect, filed as open-gsd/gsd-core#4670, and the operator waived it (see decisions).
`feature/phase-47` has no upstream; nothing from this phase is pushed.
</current_state>

<completed_work>

- Replanned 47-06/47-07 from the gap-plan review round (47-REVIEWS.md final section) — committed `bf49a77`
- 47-06 executed (5 commits, `b33406c`..`1691b12`); 47-07 executed (2 commits, `61d85d8`, `b331719`)
- SUMMARYs, REVIEW resolutions and re-verification written (`6535fb7`, `95ee5a7`, `7a7770a`)
- Stray `b102898` dropped from `workspace/denniyahh` and main-checkout `47-PATTERNS.md` deleted
- Tasks 1–3: agy and gsd-code-reviewer findings verified; fixes committed `67cab06`..`9a2c7fe`, resolutions `5385022`; IN-05 filed as GitHub #210
- Commit-claim check confirmed as a gsd-core defect and filed as open-gsd/gsd-core#4670; waiver recorded `c4d7231`
- Final external review (codex, agy): both lanes completed with no writes to the worktree. The 4 findings were verified here by a CI-image reproduction with two controls, a detector probe against the pre- and post-narrowing matcher, and a real failed run. Each was fixed test-first, with the new test failing before its fix: `ac3ee42`, `c66cba2`, `2790678`, `49bb324`. For `c66cba2`, the fixed harness passes 13/0 in the CI image under CI's ownership mismatch, and both escapes `67cab06` closed stay closed. For `49bb324`, a mutation probe showed disabling the stream branch fails the same 9 tests as before, disabling JSON decoding fails 5, and restoring the old split fails only the new test
</completed_work>

<remaining_work>

- Next: `/gsd-verify-work 47`. When `find_summaries` reports `commit_claim_mismatch` for 47-05, 47-06 and 47-07, record each as waived under open-gsd/gsd-core#4670 with its bounded count: 3 to `b30adf2`, 5 to `1691b12`, 2 to `b331719`. Recount those three first; the `..HEAD` reading has grown with every commit since. 47-01..04 have no top-level `commits:` and should come back as legacy WARNINGs.
- On the eventual PR's first CI run, confirm the `check.sh test` job prints the harness's `passed=13 failed=0`. The fixed harness passed in the pinned image under CI's ownership, but only when run alone, never inside a full `check.sh test` on GitHub's runner.
- Not re-run by Claude: codex's WR-01 escape probes from the first round (the E1/E2 cases were re-run for `c66cba2`), and `cargo doc` after the IN-04 link fix.
- Not established: the line-anchored detector against a live checkpoint (Phase 49); whether any CLI test covers the stream-scoping branch (the probes ran only `agent_result::tests`); whether any relay renders checkpoints as list items; the #4670 defect end to end through a real `execute-phase` → `verify-work` run.
- Operator decision still open: how to keep agent sessions from doing phase work in the main checkout (not blocking).
- Loose ends: ROADMAP `## Progress` row still `5/5` and the phase table still says `Not started`; STATE.md `## Active Phase`, the first `## Operator Next Steps` and `## Blockers` (a phase-46 bullet, D-46-01-A, that GSD smart-entry reads as a live blocker) are stale; `47-VALIDATION.md` lacks 47-06/07 rows; `47-RESEARCH.md` Open Questions lack RESOLVED markers; VERIFICATION body still says "Re-verification: No"; placeholder-word grep hit `47-01-SUMMARY.md` and `47-VERIFICATION.md` (likely prose — not checked)
- Flakes: `spawn_with_timeout_kills_a_hung_child` belongs to 999.114 (recorded in `45-EXTERNAL-REVIEW.md`). Unrecorded: `pi_marker_less_run_does_not_advance`; `unterminated_block_is_reported_as_malformed` (fails only under a long checkout path)
</remaining_work>

<decisions_made>

- CR-01 fixed by wording only — reasoning above a last-line `DEVFLOW_RESULT`, parser untouched (operator, 2026-09-11)
- WR-01 in scope (operator, 2026-09-11)
- Finding 4 mitigated by one sentence in `checkpoint_auto_decide_prompt` only (operator, 2026-09-12)
- Independent code review and one external lane before UAT (operator, 2026-09-12)
- Stray `b102898` and untracked PATTERNS.md removed (operator, 2026-09-12)
- Fix every `47-REVIEW.md` finding except IN-05, which is backlogged to GitHub (operator, 2026-09-12, in codex's session)
- IN-02: keep the line-anchored detector narrowing and restore the stream-scoping regression tests — option A (operator, 2026-09-12)
- Codex's uncommitted fixes committed by hand rather than through a 47-08 gap plan, because they were already on disk (Claude, 2026-09-12)
- File the commit-claim defect upstream once confirmed, then waive it for 47-05/06/07; tracked as open-gsd/gsd-core#4670 (operator, 2026-09-12)
- Final adversarial code review with codex and agy (operator, 2026-09-12)
- Fix all four final-review findings, including a decode-based fix for the literal backslash-n false positive rather than backlogging it (operator, 2026-09-12)
- The final-review fixes were committed by hand rather than through `/gsd-code-review --fix`, because that command reads findings only from `47-REVIEW.md` and two fixes needed CI-container and mutation checks its fixer does not run (Claude, 2026-09-12)
</decisions_made>

<blockers>
None open. The UAT commit-claim BLOCKER is waived (see decisions); the waiver covers 47-05, 47-06 and 47-07 only, and only while their bounded counts still equal their claims.
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — worktree, commit-branch and verification-habit rules
2. `47-REVIEWS.md`, section `## Final code review — phase 47 code, pre-UAT`
3. `47-REVIEW.md` — the independent review and its `## Finding Resolutions — 2026-09-12`
4. `47-VERIFICATION.md`, section `## Re-verification — 2026-09-12`
5. `git log b3529a8..49bb324` — each fix commit's body records what it closes and what it does not establish
6. open-gsd/gsd-core#4670 — the commit-claim defect and the reproduction behind the waiver

## Critical Anti-Patterns (do NOT repeat these)
- Running GSD from the main checkout → pin `.worktrees/phase-47` and assert the branch first
- Repeating a reviewer's or agent's claim before reading the cited source → verify each against `file:line`
- Accepting a green suite after a matcher change → probe the branch its negatives guard
- Accepting a host or local-container pass for a script CI runs → reproduce CI's user and ownership

## Infrastructure State
- No codex session, reviewer agent or background job running; no executor holds either checkout
- Uncommitted in the worktree: graphify-out files only (local-only per CLAUDE.md) and untracked `.planning/user/errors/` (a stale-state detector artefact naming `6535fb7`; not reviewed)
- `feature/phase-47` has no upstream; the `workspace/denniyahh` relationship was not re-checked this session

<context>
Phase 47 is ready for operator UAT. Both review rounds are closed and committed, the four final-review findings are fixed with tests that failed before their fixes, and the one known false UAT BLOCKER is confirmed upstream and waived. Still not established: live model compliance and the narrowed detector against a live checkpoint (Phase 49), the fixed harness inside a full CI run on GitHub, and suite stability while 999.114 is open.
</context>

<next_action>
Start with: `/gsd-verify-work 47`, recording the 47-05/06/07 `commit_claim_mismatch` as waived under open-gsd/gsd-core#4670 after recounting their bounded counts.
</next_action>
