---
context: phase
phase: 47-unattended-decision-policy-consistency
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-12T17:24:42.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> Each constraint below was discovered through failure this session.

- [ ] CONSTRAINT: phase work only inside `.worktrees/phase-47` — two codex sessions ran GSD from the main checkout; one planned against `workspace/denniyahh`'s stale `.planning/` and produced stray commit `b102898` — assert `git rev-parse --show-toplevel` is the worktree and the branch is `feature/phase-47` before any GSD command or commit.
- [ ] CONSTRAINT: verify reviewer and agent claims against source before acting — codex implemented AND self-verified; agy's five findings (Task 1) and the independent review's seven findings (Task 2) were checked against source on 2026-09-12; an earlier ROADMAP "misplacement" alarm was false because a +/- filtered diff hid context lines.
- [ ] CONSTRAINT: a passing gate is not evidence until its printed counters are read — three gates finished in 0 seconds and were only accepted after their logs showed `1 passed; 771 filtered out` and real region counts.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Agent session in the main checkout | codex loads `AGENTS.md` (graphify-only), never sees CLAUDE.md's worktree rule; the pre-commit guard only warns on phase docs | blocking | Pin the worktree root and branch before every GSD command and commit |
| Self-verification treated as review | codex marked REVIEW.md `resolved` and VERIFICATION `passed` without an independent reviewer | blocking | Run `/gsd-code-review 47` and verify findings before UAT |
| `gsd_run query commit` on new files | Silently no-ops on untracked files | advisory | `git add` the explicit paths, then `git commit` |
| Heredoc inside `bash -c` | Fails to parse through the Bash tool's zsh eval wrapper | advisory | Write files with the Write tool or `printf` |
| `state.record-session` recomputes progress | Counted phase 47 complete (2/6, 33%) before UAT on its first call; a second call left the counters alone | advisory | `git diff .planning/STATE.md` after any state verb; revert `completed_phases`, `percent` and the `Progress:` bar by hand |
| `/gsd-code-review` incremental scoping | The last commit touching REVIEW.md becomes the diff base, so codex's resolution annotations scoped a re-review to 0 files ("Skipping review") | blocking | Pass `--files=a,b,...` — the `=` spelling; `--files a,b` parses to 0 files |
| Code-review depth step on an unset overrides key | `config-get … --default '[]'` prints the quoted string `"[]"`; the resolver rejects it `not_an_array` and the workflow halts before spawning the reviewer | advisory | Resolve with a real `[]` — the config depth (`deep`) comes back |

<current_state>
Phase 47: all seven plans executed; code unchanged since `7a7770a` on `feature/phase-47` (later commits are planning docs only); STATE.md `status: awaiting_uat`.
Gap closure (47-06 CR-01, 47-07 WR-01) was implemented by codex on 2026-09-12 after the Claude session hit its usage limit.
Independent re-run at `7a7770a`: all six plan gates `gate_rc=0`, `scripts/check.sh test` passed first attempt, fmt clean, clippy 0 warnings, tree unchanged.
External agy review of `ead8443..7a7770a`: 0 BLOCKER/HIGH, 1 MEDIUM, 4 LOW — all five checked against source 2026-09-12 and all five hold; none is a production-path defect (Task 1 below).
Independent deep review of `ead8443..7a7770a` (gsd-code-reviewer) committed as `aba3cee`: 0 critical, 2 warning, 5 info, each checked against source (Task 2 below). CR-01 holds at wording and parser level; WR-01 holds for hooks and signing only. Codex's session is closed.
</current_state>

<completed_work>

- Replanned 47-06/47-07 from the gap-plan review round (47-REVIEWS.md final section) — committed `bf49a77`
- 47-06 executed (5 commits, `b33406c`..`1691b12`); 47-07 executed (2 commits, `61d85d8`, `b331719`)
- SUMMARYs, REVIEW resolutions and re-verification written (`6535fb7`, `95ee5a7`, `7a7770a`)
- Stray `b102898` dropped from `workspace/denniyahh` and main-checkout `47-PATTERNS.md` deleted; `git merge-tree` of `feature/phase-47` into `workspace/denniyahh` now clean
- Independent gate / suite / fmt / clippy re-run and agy external review of the diff
- Task 1: agy's five findings checked against source (a `cargo doc` run and guard-harness mutation probes in scratch copies; repo tree unchanged)
- Task 2: independent deep review committed (`aba3cee`); its findings checked against source (scratch-copy harness probes with controls, snapshot wording counts, parser call sites, commit history)
</completed_work>

<remaining_work>

- Task 1 — DONE 2026-09-12. All five findings hold; none is a production-path defect.
  - MEDIUM, confirmed and wider than reported: `cargo doc -p devflow-core --no-deps` (rustc 1.97.1, the pinned toolchain) reports BOTH links 47-06 added to the `checkpoint_auto_decide_prompt` doc comment as unresolved — `prompt.rs:601` (`text_reports_human_gate` is a module-private fn; agy called this one merely private) and `prompt.rs:603` (`tests` exists only under `cfg(test)`). The sibling `GATE_RESOLUTION_RULE` link in the same comment resolves. Nothing in `scripts/`, `.github/` or the manifests runs `cargo doc`. Crate total: 42 warnings, 3 unresolved; the third (`phase_id.rs:10`, `Display`) is in a file 47-06 did not touch.
  - LOW, confirmed as wording: `47-06-SUMMARY.md:45` calls the `short_after` case (5 characters of trailing text) a "tail-budget boundary". The test does what `47-06-PLAN.md` specified, and the plan uses the same word. With the long control it bounds `TAIL_BUDGET_CHARS` only between 6 characters and roughly the test record's length (asserted >5,000) — read from `parse_marker_lines`, not mutation-tested.
  - LOW, confirmed as doc imprecision: `ARCHITECTURE.md:111-115` now describes every prompt's contract as "last line, reasoning above it". `VALIDATE_VERDICT_CONTRACT` (`prompt.rs:297`) still says "FINAL message must be exactly one of", and the Plan/Ship fast-exit branches (`prompt.rs:272,352,480,564`) still say "exactly". A marker-only message satisfies both forms and none of those branches carries a decision, so this is wording only; the "every prompt" generalization predates 47-06.
  - LOW, confirmed by mutation: control 7b asserts only a non-zero exit. Signing disarmed alone makes 7b FAIL (it discriminates). Signing disarmed plus an empty `user.name` makes the commit die with `Author identity unknown` (exit 128); 7b then reports ok and the harness prints `passed=13 failed=0`. Misleading it takes a compound fault.
  - LOW, confirmed as a status overclaim (agy cited `:144`; the row is `47-VERIFICATION.md:193`): re-verification marks the copied-declaration risk ✓ VERIFIED, but `text_reports_human_gate` is unchanged by 47-06 and the test's own positive control (`agent_result.rs:3733-3735`) shows a copied declaration still trips it. The mitigation is prompt wording only, per the operator's 2026-09-12 decision; the accurate status is "mitigated by wording, detector unchanged, compliance unobservable before Phase 49".
- Task 2 — DONE 2026-09-12. `47-REVIEW.md` (`aba3cee`) replaces the earlier report; the original CR-01/WR-01 text is at `git show ead8443:.planning/phases/47-unattended-decision-policy-consistency/47-REVIEW.md`. Scope was passed with `--files=` because the workflow's own scoping would have skipped the review. Verdicts:
  - CR-01 closure holds at wording and parser level: the new "LAST line" wording is in all 9 in-scope snapshots and the old or fast-exit "exactly" wording in none; the reviewer's six marker call sites (`agent_result.rs:192,196,801,959,1710,1919`) all reach `parse_marker_lines` — 1710 and 1919 pass it as a function reference. Live compliance is Phase 49's. Reviewer caveat, not checked: if the Claude CLI's final `result` omits assistant text written before a tool call, reasoning placed there is absent from the capture.
  - WR-01 (WARNING), confirmed by scratch-copy probes: a global `core.excludesFile` ignoring `.planning/` stops the harness after 6 of 13 checks, exit 1, no `passed=` line (control with a non-matching pattern: 13/0). An exported `GIT_OBJECT_DIRECTORY` passes 13/0 but leaves 17 objects outside the fixture. `git rev-parse --local-env-vars` lists 15 names; line 15 clears 8. Neither bites on this host today (no global excludes; the default XDG ignore does not match `.planning`). The reviewer's two-line fix was tested by the reviewer, not by me.
  - WR-02 (WARNING), confirmed: nothing in `scripts/check.sh`, CI, hooks or crates runs the harness, by name or by glob. `47-07-PLAN.md:109,332` already scoped CI out, so this is a declared limitation; the old REVIEW resolution's "permanent 7c control" overstated what gates.
  - IN-01 — same as Task 1's ARCHITECTURE/Validate finding.
  - IN-02 — body confirmed, title overstated: the test did fail at its own commit `98f7e96` (it asserts a phrase the prompt only gained in `509d4fb`); only its detector assertion would have passed before 47-06. The resume capture is a single-document envelope with the prompt as a positional argument (`pipeline_launch.rs:1148-1160`). Read, not run: a reasoning line such as "Resolved gate: blocking-human" matches the unchanged detector, which re-reads the capture only for Claude on a phase whose plans declare a blocking-human checkpoint, bounded by `MAX_CHECKPOINT_RESUMES` (`pipeline_launch.rs:1594-1598`).
  - IN-03 — confirmed by reading: `after_stream` turns 1-2 carry `MARKER_FAILED`, so `assert_ne!(…, Some(Success))` would also pass if the stream parser regressed to scanning every turn.
  - IN-04 — same as Task 1's doc-link finding. The report says 43 warnings; rustdoc's own total is 42 (the 43rd `warning:` line is its summary).
  - IN-05 — 7b is Task 1's finding. The hostile `post-checkout` hook never fires: 7a-7c run no checkout (read, not run).
  - Cross-lane: the reviewer independently re-found agy's doc links, ARCHITECTURE wording and 7b cause, and the substance of the copied-declaration overclaim; it did not raise the SUMMARY "boundary" wording. New from it: WR-01's two escapes, WR-02, IN-03, the dead hook, the detector reachability from reasoning text.
- Task 3: decide fixes (operator decision pending); then `/gsd-verify-work 47` with the commit-claim waiver
  - Recommendation (Claude's, NOT an operator decision): fix WR-01, IN-03, IN-04 (plus a residual-risk sentence in the same doc comment), narrow the ARCHITECTURE.md sentence, and correct the overclaims in `47-06-SUMMARY.md` and `47-VERIFICATION.md`, as gap plan 47-08; backlog WR-02 (changes CI; untested in the container), narrowing the detector (its doc comment requires re-running a live probe) and IN-05.
- Loose ends: ROADMAP `## Progress` row still `5/5` and the phase table still says `Not started`; STATE.md `## Active Phase`, the first `## Operator Next Steps` and `## Blockers` (a phase-46 bullet, D-46-01-A, that GSD smart-entry reads as a live blocker) are stale; `47-VALIDATION.md` lacks 47-06/07 rows; `47-RESEARCH.md` Open Questions lack RESOLVED markers; VERIFICATION body still says "Re-verification: No"; placeholder-word grep hit `47-01-SUMMARY.md` and `47-VERIFICATION.md` (likely prose — not checked)
- Unrecorded flakes: `pi_marker_less_run_does_not_advance`; `unterminated_block_is_reported_as_malformed` (fails only under a long checkout path)
</remaining_work>

<decisions_made>

- CR-01 fixed by wording only — reasoning above a last-line `DEVFLOW_RESULT`, parser untouched (operator, 2026-09-11)
- WR-01 in scope (operator, 2026-09-11)
- Finding 4 mitigated by one sentence in `checkpoint_auto_decide_prompt` only (operator, 2026-09-12)
- Independent code review and one external lane before UAT (operator, 2026-09-12)
- Stray `b102898` and untracked PATTERNS.md removed (operator, 2026-09-12)
</decisions_made>

<blockers>
- UAT commit-claim check (`gsd-core verify-work.md:181`) counts `plan_head_before..HEAD`, so 47-05/06/07 read 20/10/5 against claimed 3/5/2 — false BLOCKER; counts match when measured to each plan's last commit. Needs an operator waiver; file against @opengsd/gsd-core.
</blockers>

## Required Reading (in order)
1. `CLAUDE.md` — worktree, commit-branch and verification-habit rules
2. `47-REVIEWS.md`, final section `## Gap-plan review — 47-06, 47-07` — the findings the gap plans answered
3. `47-06-SUMMARY.md`, `47-07-SUMMARY.md` — what codex claims
4. `47-VERIFICATION.md`, section `## Re-verification — 2026-09-12` — codex's self-verification
5. `47-REVIEW.md` (`aba3cee`) — the independent review; verdicts on each finding are under Task 2 above
6. agy review: `/tmp/claude-1000/-var-home-denniyahh-Github-devflow/51c107f3-4031-4088-a4d4-f63e748cfbe1/scratchpad/codex-review/review-agy.md` (scratchpad; may be gone — its findings and their verdicts are recorded under Task 1 above)

## Critical Anti-Patterns (do NOT repeat these)
- Running GSD from the main checkout → pin `.worktrees/phase-47` and assert the branch first
- Repeating a reviewer's finding before reading the cited source → verify each against `file:line`

## Infrastructure State
- No codex session or reviewer agent running; no async jobs; no executor holds either checkout
- Uncommitted in the worktree: graphify-out files only (local-only per CLAUDE.md) and untracked `.planning/user/errors/`
- `workspace/denniyahh` is 7 commits ahead of origin, 0 behind (all ancestors of `feature/phase-47`)

<context>
The code has now been independently reviewed. CR-01's fix holds at wording and parser level, WR-01's holds for hooks and signing only, and there is no Critical finding. Still not established: live model compliance (Phase 49), and the harness's isolation from inherited git config beyond hooks and signing. The harness itself runs only by hand.
</context>

<next_action>
Start with: get the operator's decision on which Task 2 findings to fix (the recommendation under Task 3 is Claude's, not decided), apply it through a GSD route, then `/gsd-verify-work 47` with the commit-claim waiver.
</next_action>
