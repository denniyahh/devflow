---
phase: 47-unattended-decision-policy-consistency
plan: 02
type: tdd
wave: 2
depends_on: ["47-01"]
files_modified:
  - crates/devflow-core/src/prompt.rs
  - crates/devflow-core/src/snapshots
autonomous: true
requirements: [DECN-02]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A Validate loop-back that dispatches `Code { fix: Some(FullExecute) }` delivers `CODE_STAGE_POLICY` on claude, opencode, hermes and antigravity — not only on the first Code pass (DECN-02, D-06)."
    - "codex and pi still deliver the policy on that arm, as passing controls proving the test discriminates rather than asserting a tautology (D-07)."
    - "`GapsOnly` and `AuditFix` still omit the policy on the claude-style path as well as the workflow-style path (D-08)."
    - "`workflow_code_prompt`'s existing tests pass UNMODIFIED — the shared-helper extraction changed no observable behaviour (D-05, ROADMAP criterion 1)."
  artifacts:
    - crates/devflow-core/src/prompt.rs
    - crates/devflow-core/src/snapshots
  key_links:
    - "`fix_prompt` (`prompt.rs:567`) ← its single production call site `render_claude_style` (`prompt.rs:399`) — the only place this fix changes runtime behaviour."
    - "the shared arm helper ← extracted FROM `workflow_code_prompt`'s already-correct `Some(FullExecute) | None` arm (`prompt.rs:454-474`), not invented (D-05)."
    - "`code_policy_is_absent_from_prompts_that_must_not_carry_it` (`prompt.rs:794-822`) ← must pass unmodified; it is workflow-style only, which is why the claude-style twin is mandatory (D-08)."
---

<objective>
Close DECN-02: make the Validate loop-back Code prompt carry `CODE_STAGE_POLICY` for the
`FullExecute` arm on all four `render_claude_style` adapters, without changing any other arm and
without changing `workflow_code_prompt`'s observable behaviour.

Purpose: this is the load-bearing half of Phase 47. It needs no human in the loop, affects four
adapters, and is reachable on every Validate loop-back — unlike DECN-03, which is claude-only and
human-initiated.

Output: a shared arm helper consulted by both renderers, `fix_prompt`'s `FullExecute` arm routed
through it, a six-adapter presence test with codex/pi controls, and a claude-style omission
negative control.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md
@.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md
@.planning/phases/47-unattended-decision-policy-consistency/47-01-SUMMARY.md
</context>

<!-- Comment-text discipline: literals below are flagged by the coarse `<action>` heuristic but
     are safe here. Each line records why the gate cannot be self-invalidated by the plan's own text. -->
<!-- planner-discipline-allow: test result: FAILED -->  <!-- cargo's own output string; every gate counting it reads a cargo log, never a file an executor writes -->
<!-- planner-discipline-allow: code_policy_applies_to_fix_arm -->  <!-- the symbol being created; its gate is a positive count with comment lines stripped -->
<!-- planner-discipline-allow: ## Unattended decision checkpoints -->  <!-- the policy section heading under test; the gates counting it read generated .snap files and rendered prompt strings, not plan or comment prose -->

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Land the six-adapter presence test and the claude-style omission control — presence test RED</name>
  <files>crates/devflow-core/src/prompt.rs</files>
  <read_first>
    - `crates/devflow-core/src/prompt.rs` — `CODE_STAGE_POLICY` (:62-89), `render_claude_style` (:391-417), `render_workflow_style` (:418), `workflow_code_prompt` (:454-474), `fix_prompt` (:567-581), and the test module from :583
    - `crates/devflow-core/src/prompt.rs` existing tests that are the analogs: `code_policy_is_identical_across_both_renderers` (:777-792) and `code_policy_is_absent_from_prompts_that_must_not_carry_it` (:794-822)
    - `crates/devflow-core/src/agents/mod.rs` — `driver_for` (:174-182), the `AgentDriver::render_prompt` contract, and `drivers_reproduce_legacy_adapter_behavior` (:218-242)
    - `crates/devflow-core/src/agents/codex.rs` (:22-25) and `crates/devflow-core/src/agents/claude.rs` (:21-23) — the two render-style delegations
    - `.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md` § "D-07's six-adapter presence test" — including the warning that no `AgentKind::ALL` exists
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § B-2 (staged assertions) and § C-1 (the red-run verify template)
  </read_first>
  <behavior>
    - `code_policy_reaches_the_full_execute_fix_arm_on_every_adapter`: for every `AgentKind`
      variant, `driver_for(kind).render_prompt(&StageIntent::Code { phase, fix: Some(FixType::FullExecute) })`
      contains the `## Unattended decision checkpoints` section. PRE-FIX this fails for Claude,
      OpenCode, Hermes and Antigravity and passes for Codex and Pi.
    - Codex and Pi are the passing controls. If they ever go red the test is measuring something
      other than the claude-style gap.
    - `claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it`: `render_claude_style`
      for `Code { fix: Some(GapsOnly) }` and `Code { fix: Some(AuditFix) }` contains no
      `CODE_STAGE_POLICY`. Passes pre-fix and post-fix; its discriminating power is demonstrated in
      Task 3.
  </behavior>
  <action>
    Add both tests to `crates/devflow-core/src/prompt.rs`'s `mod tests`.

    For the presence test, drive `crate::agents::driver_for(kind).render_prompt(...)` and enumerate
    the kinds with an EXHAUSTIVE `match` over `AgentKind` inside the test body — mapping each
    variant to its expected verdict — rather than an array literal. PATTERNS records that this
    workspace has no `AgentKind::ALL` and that every existing multi-agent test hardcodes an array,
    which means a seventh variant would be silently uncovered. The exhaustive `match` makes a new
    variant a compile error, mirroring `driver_for`'s own shape. State that choice in a comment on
    the test so a reader does not assume an array loop would have been equivalent.

    Assert on the STRING LITERAL `## Unattended decision checkpoints`, not on the
    `CODE_STAGE_POLICY` constant and not on any constant this phase has not created yet. RESEARCH
    Pitfall 4: a test naming a not-yet-existing constant is a compile error, and a compile error is
    not a `test result: FAILED`. The red run must print a real failure.

    Give the presence test the house-style precondition comment (`pipeline_launch.rs:3944-3948`
    shape) naming what it would fail to discriminate without the codex/pi controls.

    For the omission control, mirror `code_policy_is_absent_from_prompts_that_must_not_carry_it`
    (:794-822) exactly, swapping `render_workflow_style(..., "/workflows")` for
    `render_claude_style(...)`. Do NOT modify the existing workflow-style control — ROADMAP
    criterion 1 requires it pass unmodified.

    Run the presence test and CAPTURE the `test result: FAILED` output. That capture is DECN-02's
    criterion-2 evidence and belongs in the plan SUMMARY. The search range is proven non-empty by
    `COMPLETION_PROTOCOL` at `prompt.rs:578`, so an absent match here means absence, not a
    mis-scoped search.
  </action>
  <acceptance_criteria>
    - `code_policy_reaches_the_full_execute_fix_arm_on_every_adapter` compiles and FAILS, printing
      `test result: FAILED`, against the tree as it stands before Task 2.
    - The failure message names at least one of Claude, OpenCode, Hermes or Antigravity.
    - `claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it` compiles and PASSES.
    - The presence test's kind enumeration is an exhaustive `match` over `AgentKind`, not an array
      literal.
    - `crates/devflow-core/src/prompt.rs` lines matching `fn code_policy_is_absent_from_prompts_that_must_not_carry_it`
      and the 29 lines after it are byte-identical to the phase fork point.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; cargo test -p devflow-core --lib code_policy_reaches_the_full_execute_fix_arm_on_every_adapter 2>&1 | tee /tmp/47-02-red.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/47-02-red.log; n=$(grep -c "test result: FAILED" /tmp/47-02-red.log || true); echo "failed_binaries=$n"; test "$n" -ge 1 || rc=1; r=$(grep -c "0 passed" /tmp/47-02-red.log || true); echo "zero_passed_lines=$r"; test "$r" -ge 1 || rc=1; ra=$(grep -c "running 1 test" /tmp/47-02-red.log || true); echo "presence_test_actually_ran=$ra"; test "$ra" -ge 1 || rc=1; cargo test -p devflow-core --lib claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it 2>&1 | tee /tmp/47-02-omit.log; echo "cargo_exit_omit=${PIPESTATUS[0]}"; o=$(grep -c "test result: ok. 1 passed" /tmp/47-02-omit.log || true); echo "omission_control_one_passed=$o"; test "$o" -eq 1 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `failed_binaries=0` or `zero_passed_lines=0` (the presence test did not actually run red — a compile error prints neither, which is the RESEARCH Pitfall 4 outcome this gate exists to distinguish), `presence_test_actually_ran=0` (verified on the pre-work tree: a name filter that matches nothing prints `running 0 tests` with a large `filtered out` count and cargo still exits 0, so a `0 passed` line alone does not prove the test executed), or `omission_control_one_passed` not equal to 1.</fails_when>
  </verify>
  <done>
    Both tests are committed. The presence test is red with a printed `test result: FAILED` and a
    `0 passed` line, and the claude-style omission control is green.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Extract the shared arm helper and route fix_prompt's FullExecute arm through it — GREEN</name>
  <files>crates/devflow-core/src/prompt.rs</files>
  <read_first>
    - `crates/devflow-core/src/prompt.rs` — `workflow_code_prompt` (:454-474), `code_stage_prompt` (:372-383, the interpolation and blank-line formatting to copy), `fix_prompt` (:551-581 including its 15-line rationale doc comment)
    - `crates/devflow-core/src/prompt.rs` tests that pin the surrounding behaviour and must keep passing: `code_stage_prompt_is_unchanged_single_command_template` (:683), `code_policy_is_identical_across_both_renderers` (:777), `code_policy_is_absent_from_prompts_that_must_not_carry_it` (:794), `both_code_prompts_still_end_with_the_completion_protocol` (:825), `fix_prompts_select_the_right_command` (:913), `fix_prompts_carry_the_chain_flag_token_only_where_it_reaches_execute_phase` (:936)
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-05 (the behaviour-preserving constraint and the stated fallback) and D-06
    - `.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md` § `crates/devflow-core/src/prompt.rs` — the formatting pattern and the doc-comment-as-rationale convention
  </read_first>
  <behavior>
    - The presence test from Task 1 turns green for all six adapters.
    - `fix_prompt(FixType::FullExecute, phase)` ends with `COMPLETION_PROTOCOL`, carries the fix
      command, and carries `CODE_STAGE_POLICY` between them.
    - `fix_prompt(FixType::GapsOnly, phase)` and `fix_prompt(FixType::AuditFix, phase)` are
      byte-identical to their pre-change output.
    - Every existing `prompt.rs` test passes with no edit to its body.
  </behavior>
  <action>
    Extract the "does this arm carry the policy" decision out of `workflow_code_prompt` into a
    private helper in `prompt.rs` — name it `code_policy_applies_to_fix_arm`, taking
    `Option<FixType>` and returning `bool`, true for `Some(FixType::FullExecute) | None` and false
    for `Some(GapsOnly) | Some(AuditFix)`. Extract it FROM the existing correct logic at
    `prompt.rs:454-474`; do not re-derive the rule. `workflow_code_prompt` then consults the helper
    while producing byte-identical output for all three arms.

    Route `fix_prompt`'s `FullExecute` arm through the same helper. `fix_prompt` takes `FixType`,
    not `Option<FixType>`, so bridge by passing `Some(fix_type)` — say so in a comment, because the
    signature mismatch is the seam where a future edit could silently widen the arm.

    Interpolate `CODE_STAGE_POLICY` into the `FullExecute` output using the `code_stage_prompt`
    (:372-383) formatting shape: fix command, blank line, policy, blank line, `COMPLETION_PROTOCOL`
    LAST. `both_code_prompts_still_end_with_the_completion_protocol` (:825) pins the trailing
    position and must keep passing.

    Extend `fix_prompt`'s existing doc comment rather than replacing it, adding a paragraph naming
    D-05/D-06 and DECN-02 in the repo's doc-comment-as-rationale shape: what changed, which decision
    produced it, and that the `GapsOnly`/`AuditFix` arms are deliberately excluded rather than
    overlooked.

    D-05's constraint is hard: if any existing `workflow_code_prompt` test has to change, the
    extraction was NOT behaviour-preserving. In that case do not edit the test — fall back to
    confining the change to `fix_prompt` alone, leave the duplication in place with a comment naming
    this phase, and say so explicitly in the plan SUMMARY.
  </action>
  <acceptance_criteria>
    - `code_policy_applies_to_fix_arm` exists in `crates/devflow-core/src/prompt.rs` and is consulted
      by both `workflow_code_prompt` and `fix_prompt`.
    - `cargo test -p devflow-core --lib` is fully green with `0 failed`.
    - `code_policy_reaches_the_full_execute_fix_arm_on_every_adapter` passes with `1 passed` and a
      non-zero `filtered out` count.
    - `git diff` on `crates/devflow-core/src/prompt.rs` relative to the phase fork point contains no
      removed line inside any pre-existing `#[test]` body.
    - If the fallback was taken, the SUMMARY says so in those words and names which test would
      otherwise have had to change.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; cargo test -p devflow-core --lib 2>&1 | tee /tmp/47-02-green.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/47-02-green.log; f=$(grep -c "test result: FAILED" /tmp/47-02-green.log || true); echo "failed_binaries=$f"; test "$f" -eq 0 || rc=1; cargo test -p devflow-core --lib code_policy_reaches_the_full_execute_fix_arm_on_every_adapter -- --exact 2>&1 | tee /tmp/47-02-one.log; p=$(grep -c "test result: ok. 1 passed" /tmp/47-02-one.log || true); echo "presence_test_one_passed=$p"; test "$p" -eq 1 || rc=1; fo=$(grep -oE "[0-9]+ filtered out" /tmp/47-02-one.log | head -1); echo "presence_filtered=$fo"; case "$fo" in ""|"0 filtered out") rc=1;; esac; h=$(grep -v "^[[:space:]]*//" crates/devflow-core/src/prompt.rs | grep -c "code_policy_applies_to_fix_arm" || true); echo "helper_code_sites=$h"; test "$h" -ge 3 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `failed_binaries` greater than 0, `presence_test_one_passed` not equal to 1, `presence_filtered` empty or the literal `0 filtered out` (a bare `--exact` name miss prints `0 passed` and still exits 0), or `helper_code_sites` below 3 — the helper must appear as a definition plus a call from each renderer, counted with comment lines stripped so doc prose is not counted as a call site.</fails_when>
  </verify>
  <done>
    The four `render_claude_style` adapters deliver `CODE_STAGE_POLICY` on the `FullExecute` fix
    arm, codex and pi still do, the two omitted arms are unchanged, and no pre-existing test body
    was edited.
  </done>
</task>

<task type="auto">
  <name>Task 3: Prove the omission control discriminates, prove the unmodified-test constraint held, re-bless the baseline</name>
  <files>crates/devflow-core/src/prompt.rs, crates/devflow-core/src/snapshots/</files>
  <read_first>
    - `crates/devflow-core/src/prompt.rs` as written by Task 2
    - `crates/devflow-core/src/snapshots/` — the baseline committed by plan 47-01 Task 1
    - `.planning/phases/47-unattended-decision-policy-consistency/47-01-SUMMARY.md` — the three-case control result and the `INSTA_UPDATE=always` baseline recipe
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-08 and the ROADMAP Phase 47 criterion 1 scope constraint
    - `CLAUDE.md` § "Verification habits this repo has already paid for" — the entry on a green suite over a broken feature
  </read_first>
  <action>
    First demonstrate the D-08 control's failing direction, because a test that asserts absence
    passes trivially and has therefore never been observed discriminating. Temporarily widen
    `code_policy_applies_to_fix_arm` to return true for `Some(FixType::GapsOnly)`, run
    `claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it`, and confirm it prints a
    real `test result: FAILED`. Then revert the widening. Record both directions in the SUMMARY.
    Without this the over-broad edit D-08 exists to catch would be unguarded in exactly the arm this
    plan modified.

    Then prove the criterion-1 constraint mechanically rather than by assertion: extract the body of
    `code_policy_is_absent_from_prompts_that_must_not_carry_it` from the phase fork point and from
    the working tree and diff them. They must be identical. The fork point is
    `git merge-base HEAD workspace/denniyahh`.

    Then re-bless the 47-01 snapshot baseline. It is now red — DECN-02's fix drifted it, which is
    the guard doing its job on a real wording change, and is the first live evidence that D-14's
    mechanism works. Regenerate with `INSTA_UPDATE=always cargo test -p devflow-core --lib
    claude_style_full_execute_fix_prompt_snapshot`, then READ the resulting `.snap` diff before
    staging it and confirm the added content is the policy section and nothing else. Do not
    regenerate blind — pasting an unread actual back into the golden value is the exact hazard D-15
    is written to avoid.
  </action>
  <acceptance_criteria>
    - The SUMMARY records the D-08 control red under the temporary widening and green after revert.
    - The 29-line window starting at `fn code_policy_is_absent_from_prompts_that_must_not_carry_it`
      is byte-identical between the phase fork point and the working tree.
    - The re-blessed `.snap` baseline now contains `## Unattended decision checkpoints` at least
      once, where plan 47-01's acceptance asserted zero occurrences.
    - `scripts/check.sh test` exits 0 with the re-blessed baseline committed.
    - `crates/devflow-core/src/prompt.rs` shows no residual temporary widening.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; base=$(git merge-base HEAD workspace/denniyahh); echo "fork_point=$base"; git show "$base":crates/devflow-core/src/prompt.rs | grep -A 29 -F "fn code_policy_is_absent_from_prompts_that_must_not_carry_it" > /tmp/47-02-ctl-base.txt; grep -A 29 -F "fn code_policy_is_absent_from_prompts_that_must_not_carry_it" crates/devflow-core/src/prompt.rs > /tmp/47-02-ctl-head.txt; bl=$(wc -l < /tmp/47-02-ctl-base.txt); echo "base_window_lines=$bl"; test "$bl" -ge 20 || rc=1; diff -q /tmp/47-02-ctl-base.txt /tmp/47-02-ctl-head.txt > /dev/null; d=$?; echo "omission_control_unmodified_exit=$d"; test "$d" -eq 0 || rc=1; p=$(cat crates/devflow-core/src/snapshots/*.snap | grep -c "## Unattended decision checkpoints" || true); echo "policy_heading_in_baseline=$p"; test "$p" -ge 1 || rc=1; scripts/check.sh test > /tmp/47-02-check.log 2>&1; e=$?; echo "check_test_exit=$e"; test "$e" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `base_window_lines` below 20 (the fork-point extraction returned nothing, so the diff would compare two empty files and pass vacuously — this is the negative control on the comparison itself), `omission_control_unmodified_exit` non-zero (criterion 1's unmodified-test constraint was broken), `policy_heading_in_baseline=0` (the snapshot was not re-blessed and still records the pre-fix defect), or `check_test_exit` non-zero.</fails_when>
  </verify>
  <done>
    D-08's control has been observed failing in the over-broad-edit direction; the workflow-style
    omission test is provably unmodified; and the snapshot baseline records the fixed text with the
    diff reviewed rather than pasted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow → unattended agent (Code loop-back prompt) | The prompt is the only instruction channel; what it omits, the agent never learns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-05 | Elevation of Privilege | `code_policy_applies_to_fix_arm` widening to `GapsOnly`/`AuditFix` | high | mitigate | `claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it` (Task 1), with its failing direction demonstrated in Task 3 by temporarily widening the helper |
| T-47-06 | Repudiation | a Validate loop-back decision made without the recorded-reasoning instruction | medium | mitigate | `CODE_STAGE_POLICY` on the `FullExecute` arm carries the "record the reasoning and comparison" requirement to the loop-back path, which is the DECN-02 fix itself |
| T-47-07 | Tampering | silent wording drift in the fix-arm prompt | medium | mitigate | The 47-01 snapshot baseline, re-blessed with a read diff in Task 3 |
| T-47-08 | Spoofing | an adapter switching render style without the policy following it | low | mitigate | Exhaustive `match` over `AgentKind` in the presence test — a new variant is a compile error, not a silent gap |
</threat_model>

<verification>
- `cargo test -p devflow-core --lib` fully green.
- The presence test red before Task 2 and green after, with both captures in the SUMMARY.
- `code_policy_is_absent_from_prompts_that_must_not_carry_it` byte-identical to the fork point.
- `scripts/check.sh test` green with the re-blessed baseline.
</verification>

<success_criteria>
- ROADMAP criterion 1: `fix_prompt` renders `CODE_STAGE_POLICY` for `Code { fix: Some(FullExecute) }`
  and `GapsOnly`/`AuditFix` still omit it, with `prompt.rs:794-822` passing unmodified.
- ROADMAP criterion 2: a test asserting the policy's presence on the loop-back path failed against
  the pre-fix tree and passes after, with a printed `test result: FAILED` as the evidence.
</success_criteria>

## Artifacts this phase produces

Created or changed by this plan:

| Kind | Symbol / path |
|---|---|
| function | `code_policy_applies_to_fix_arm(Option<FixType>) -> bool` (`crates/devflow-core/src/prompt.rs`) |
| behaviour change | `fix_prompt`'s `FullExecute` arm now interpolates `CODE_STAGE_POLICY` |
| test | `code_policy_reaches_the_full_execute_fix_arm_on_every_adapter` (`crates/devflow-core/src/prompt.rs`) |
| test | `claude_style_fix_prompts_that_must_not_carry_the_policy_still_omit_it` (`crates/devflow-core/src/prompt.rs`) |
| golden update | the `claude_style_full_execute_fix_prompt_snapshot` `.snap` baseline, re-blessed |

<output>
Create `.planning/phases/47-unattended-decision-policy-consistency/47-02-SUMMARY.md` when done.
It MUST carry the pre-fix `test result: FAILED` capture (criterion 2 evidence), the D-08 control's
both-direction result, and a statement of whether D-05's behaviour-preserving extraction held or
the stated fallback was taken.
</output>
