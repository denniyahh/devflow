---
phase: 47-unattended-decision-policy-consistency
plan: 03
type: tdd
wave: 3
depends_on: ["47-02"]
files_modified:
  - crates/devflow-core/src/prompt.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-core/src/snapshots
autonomous: true
requirements: [DECN-03]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A resumed Claude session receives ONE rule about who may resolve a `blocking-human` gate: the Code-stage policy states the prohibition with the resumed-to-resolve exception, and the resume instruction states the same condition affirmatively (D-01, D-04)."
    - "The carve-out covers `blocking-human` gates ONLY — a package-verification checkpoint keeps the unconditional prohibition (D-02, settled by the operator 2026-09-10)."
    - "`checkpoint_auto_decide_prompt` names `blocking-human` explicitly, matching what `phase_has_blocking_human_checkpoint` actually detects (D-02)."
    - "Both halves of the layered test went red against the pre-fix tree with a printed `test result: FAILED`, not a compile error (D-03, RESEARCH B-2)."
    - "The two-turn test captures each turn from the production constructor that delivers it and never concatenates them (D-03, RESEARCH B-1)."
  artifacts:
    - crates/devflow-core/src/prompt.rs
    - crates/devflow-cli/src/pipeline_launch.rs
    - crates/devflow-core/src/snapshots
  key_links:
    - "`GATE_RESOLUTION_RULE` ← interpolated by `CODE_STAGE_POLICY` AND asserted by the cross-crate delivery test, which is what makes it one definition site rather than two strings that happen to agree (D-04)."
    - "`resume_launch_shape` ← `relaunch_checkpoint_session` (`pipeline_launch.rs:1095-1133`); the `checkpoint_auto_decided` emission at `:1102-1117` must stay BEFORE the spawn (D-10)."
    - "turn 1 ← `MonitorLaunch::PipeOwning { prompt }` from `resolve_launch_shape` (`pipeline_launch.rs:195`); turn 2 ← `argv[1]` of `ClaudeDriver::exec_resume_command` (`agents/claude.rs:122-135`). Co-residence is a property of the session transcript, not of any string DevFlow builds."
---

<objective>
Close DECN-03's prompt-level half: remove the contradiction between `CODE_STAGE_POLICY` and
`checkpoint_auto_decide_prompt` about who may resolve a `blocking-human` gate, with one definition
site for the rule and a layered test that proves the contradiction existed.

Purpose: an agent resumed into a session that has already seen the Code-stage policy and is then
handed the resume instruction currently receives two contradictory rules. This plan makes it one
rule with a condition.

Deliberately NOT closed here: which instruction a model actually follows. That is a question about
model instruction-priority, not answerable by reading source. Plan 47-05 records it as a Phase 49
observation item with a named evidence standard.

Output: `GATE_RESOLUTION_RULE` as a shared constant, a narrowed carve-out in `CODE_STAGE_POLICY`, a
tightened `checkpoint_auto_decide_prompt`, a `resume_launch_shape` extraction, and a two-turn
delivery test that drives production constructors on both sides.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md
@.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md
@.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md
@.planning/phases/47-unattended-decision-policy-consistency/47-02-SUMMARY.md
</context>

<!-- Comment-text discipline: literals below are flagged by the coarse `<action>` heuristic but
     are safe here. Each line records why the gate cannot be self-invalidated by the plan's own text. -->
<!-- planner-discipline-allow: resume_launch_shape -->  <!-- the symbol being created; its gate is a positive count with comment lines stripped -->
<!-- planner-discipline-allow: test result: FAILED -->  <!-- cargo's own output string; every gate counting it reads a cargo log -->
<!-- planner-discipline-allow: package-verification -->  <!-- gated POSITIVELY (it must survive the sentence split); the negative gate targets the longer conjunction and runs over a comment-stripped copy -->
<!-- planner-discipline-allow: GATE_RESOLUTION_RULE -->  <!-- the constant being created; both gates over it are positive counts with comment lines stripped -->
<!-- planner-discipline-allow: This authority never extends to a package-verification checkpoint -->  <!-- the pinned gate-rule section; gated POSITIVELY (exactly one occurrence) over the generated fix-prompt .snap, never over plan or source prose -->

<tasks>

<task type="auto">
  <name>Task 1: Extract resume_launch_shape as production code, leaving the audit emission where it is</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - `crates/devflow-cli/src/pipeline_launch.rs` — `resolve_launch_shape` and its doc comment (:186-217), the call site destructure (:155-162), and `relaunch_checkpoint_session` (:1095-1133) with the `checkpoint_auto_decided` emission at :1102-1117 and the `exec_resume_command` call at :1119
    - `crates/devflow-cli/src/pipeline_launch.rs` tests that must survive unmodified: `relaunch_checkpoint_session_emits_exactly_one_audit_event` (:2754-2795), `relaunch_checkpoint_session_increments_and_persists_counter` (:2803), `relaunch_checkpoint_session_does_not_change_stage` (:2849)
    - `crates/devflow-core/src/agents/claude.rs` — `exec_resume_command` (:122-135), the only resume command in the workspace
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § B-0 step 1 and § B-4 (open assumption A5, Medium risk)
    - `.planning/phases/47-unattended-decision-policy-consistency/47-PATTERNS.md` § `pipeline_launch.rs` — the four properties to copy and the warning that `instruction` is consumed twice
  </read_first>
  <action>
    Add a private `resume_launch_shape` to `crates/devflow-cli/src/pipeline_launch.rs`, mirroring
    `resolve_launch_shape`: a private `fn` in the same module, returning the built
    `(program, argv)` shape rather than spawning, with a doc comment in the house style naming this
    plan and stating the reason verbatim from the precedent — so the instruction a resume actually
    DELIVERS is assertable without spawning a process. It builds the instruction via
    `prompt::checkpoint_auto_decide_prompt(phase)` and returns
    `agents::ClaudeDriver::exec_resume_command(session_id, &instruction)`.

    `relaunch_checkpoint_session` then calls it. `instruction` is consumed twice — by `events::emit`
    at :1113 and by `exec_resume_command` at :1119 — so either return `instruction` as a third tuple
    element or keep the `let instruction` binding in the caller and pass it in. Either is
    acceptable. What is NOT acceptable is moving the `checkpoint_auto_decided` emission: D-10
    depends on it staying before the spawn so that a spawn failure still leaves the decision attempt
    on record, and the whole "event proves injection, not decision" finding rests on that ordering.

    This is a pure extraction. `relaunch_checkpoint_session`'s signature and observable behaviour do
    not change. RESEARCH logs "the existing tests survive this extraction" as open assumption A5 at
    Medium risk — VERIFY it by running the three named tests, do not assume it. If any of them has
    to change, say so explicitly in the plan SUMMARY rather than quietly editing it; that is D-05's
    rule and it applies to this extraction too.
  </action>
  <acceptance_criteria>
    - `resume_launch_shape` exists in `crates/devflow-cli/src/pipeline_launch.rs` and is called by
      `relaunch_checkpoint_session`.
    - The `checkpoint_auto_decided` `events::emit` call still appears BEFORE the
      `resume_launch_shape` call in source order within `relaunch_checkpoint_session`.
    - All three `relaunch_checkpoint_session_*` tests pass, and their bodies are byte-identical to
      the phase fork point.
    - `cargo test -p devflow --bin devflow` is fully green.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; base=$(git merge-base HEAD workspace/denniyahh); echo "fork_point=$base"; for t in relaunch_checkpoint_session_emits_exactly_one_audit_event relaunch_checkpoint_session_increments_and_persists_counter relaunch_checkpoint_session_does_not_change_stage; do git show "$base":crates/devflow-cli/src/pipeline_launch.rs | sed -n "/^    fn ${t}(/,/^    }/p" > /tmp/47-03-t1-base-$t.txt; sed -n "/^    fn ${t}(/,/^    }/p" crates/devflow-cli/src/pipeline_launch.rs > /tmp/47-03-t1-head-$t.txt; bl=$(wc -l < /tmp/47-03-t1-base-$t.txt); echo "${t}_base_lines=$bl"; test "$bl" -ge 30 || rc=1; fdn=$(grep -c "^    fn " /tmp/47-03-t1-base-$t.txt || true); echo "${t}_fn_decls=$fdn"; test "$fdn" -eq 1 || rc=1; cmp -s /tmp/47-03-t1-base-$t.txt /tmp/47-03-t1-head-$t.txt; d=$?; echo "${t}_unmodified_exit=$d"; test "$d" -eq 0 || rc=1; done; s=$(grep -v "^[[:space:]]*//" crates/devflow-cli/src/pipeline_launch.rs | grep -c "resume_launch_shape" || true); echo "resume_shape_code_sites=$s"; test "$s" -ge 2 || rc=1; cargo test -p devflow --bin devflow 2>&1 | tee /tmp/47-03-t1.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/47-03-t1.log; f=$(grep -c "test result: FAILED" /tmp/47-03-t1.log || true); echo "failed_binaries=$f"; test "$f" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically, for any of the three protected tests, `{test}_base_lines` below 30 (the fork-point extraction returned nothing, so the comparison would pass vacuously), `{test}_fn_decls` not exactly 1 (the extraction ran past that test's closing brace into other code), or `{test}_unmodified_exit` non-zero (that test's body changed); `resume_shape_code_sites` below 2 (definition plus caller, counted with comment lines stripped); or `failed_binaries` greater than 0. Each test is extracted from its `fn` line to the first line that is exactly a four-space-indented closing brace, not a fixed line window, so a correct change elsewhere in the file cannot move a window boundary. Verified 2026-09-11 in a scratch repo built from the real `pipeline_launch.rs`: an unrelated edit leaves all three identical (43, 41 and 39 lines, one `fn` each); mutating one line inside the second test flags only that test; the previous single-test form reported nothing for the same mutation.</fails_when>
  </verify>
  <done>
    The resume side is production code that can be asserted on without spawning a process, the
    audit emission is unmoved, and assumption A5 has been checked by running the three tests rather
    than reasoned about.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Land the layered contradiction test — constant-level and two-turn delivery, BOTH red</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs, crates/devflow-core/src/prompt.rs</files>
  <read_first>
    - `crates/devflow-core/src/prompt.rs` — `CODE_STAGE_POLICY` (:62-89, with the two-class sentence at :86-89) and `checkpoint_auto_decide_prompt` (:530-549)
    - `crates/devflow-cli/src/pipeline_launch.rs` — `resolve_launch_shape` (:195), `stream_launch_enabled` (:756), `resume_launch_shape` from Task 1, the `mod tests` header and its imports (:1632-1637), and `legacy_launch_flag_forces_the_single_document_path` (:3936-3972) as the house style to copy
    - `crates/devflow-cli/src/pipeline_gate.rs` :198-203 — the loop-back's own `render_prompt` call, which turn 1 must reproduce
    - `crates/devflow-core/src/monitor.rs` :267-282 — `MonitorLaunch::PipeOwning { prompt }` and its documented stdin-user-turn contract
    - `crates/devflow-core/src/agents/claude.rs` :24-40 — the doc comment stating the prompt is deliberately absent from argv
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § B-0 step 2, § B-1, § B-2 (the staged-assertion sequencing), § C-1
  </read_first>
  <behavior>
    - `the_gate_rule_holds_in_both_delivered_turns` (in `pipeline_launch.rs`): captures turn 1 from
      `resolve_launch_shape` as `MonitorLaunch::PipeOwning { prompt }` and turn 2 as `argv[1]` from
      `resume_launch_shape`. Asserts turn 1 no longer carries the two-class conjunction
      `gate or a package-verification checkpoint`, and that turn 2 contains the literal
      `blocking-human`. PRE-FIX both assertions fail.
    - Its negative controls: a precondition assert that `stream_launch_enabled` is true for
      `Stage::Code` (otherwise turn 1 resolves to `Legacy` and there is no stdin turn to capture, so
      the assertion would be vacuous rather than false); `assert_ne!(turn1, turn2)`; and neither
      string containing the other. Without those the pair assertion would pass if both captures
      collapsed to the same source.
    - Turn 1 must also still contain `package-verification` — the half of the sentence D-02 keeps
      unconditional. This is the control that catches a fix which deletes the sentence instead of
      splitting it.
    - `the_gate_rule_has_one_definition_site` (in `prompt.rs`): `CODE_STAGE_POLICY` does not contain
      `gate or a package-verification checkpoint`, still contains `package-verification`, and
      `checkpoint_auto_decide_prompt(phase)` contains `blocking-human`. PRE-FIX the first and third
      fail.
  </behavior>
  <action>
    Add both tests. Assert on STRING LITERALS only, never on `GATE_RESOLUTION_RULE` — that constant
    does not exist yet and referencing it produces a compile error, which is not a
    `test result: FAILED` (RESEARCH Pitfall 4, and the repo's own "a revert that hangs is not a
    revert that fails" rule). Task 3 strengthens the assertion after the constant exists.

    Build the two-turn test on production constructors on BOTH sides. Turn 1 comes from
    `driver.render_prompt` for `StageIntent::Code { phase, fix: Some(FixType::FullExecute) }` fed
    through `resolve_launch_shape` and destructured out of `MonitorLaunch::PipeOwning`; panic with a
    named message if it resolves to `Legacy`. Turn 2 comes from `resume_launch_shape`; index
    `argv[1]` rather than using `.get()`, so an absent element panics instead of quietly reading as
    `None` — the parse-boundary rule. Assert `argv[0]` equals `-p` first, so a shape change is
    reported as a shape change and not as a missing rule.

    Do NOT concatenate the two turns and do not assert on a joined string. No production code ever
    joins them: turn 1 is delivered as a stdin JSON user turn, turn 2 positionally in argv, and
    co-residence happens across two conversation turns inside the resumed Claude session. A joined
    assertion would be a proxy for a composition that does not exist.

    Resolve the `StageIntent`/`FixType` import path against `pipeline_launch.rs`'s existing usage
    (`prompt::StageIntent` is already referenced at :95 and :1156) — RESEARCH B-4 flagged this path
    as unverified, so read it rather than copying the research snippet's imports.

    Run both tests and CAPTURE their `test result: FAILED` output. That capture is DECN-03's
    criterion-3 evidence and belongs in the plan SUMMARY. Note in the SUMMARY which assertion
    carried the red on each side: after plan 47-02, turn 1 already carries the policy section, so
    the red on turn 1 comes specifically from the un-split two-class sentence, not from the policy
    being absent.
  </action>
  <acceptance_criteria>
    - `the_gate_rule_holds_in_both_delivered_turns` exists in `crates/devflow-cli/src/pipeline_launch.rs`,
      compiles, and FAILS with a printed `test result: FAILED`.
    - `the_gate_rule_has_one_definition_site` exists in `crates/devflow-core/src/prompt.rs`,
      compiles, and FAILS with a printed `test result: FAILED`.
    - Neither test body contains a string concatenation or `format!` joining turn 1 and turn 2.
    - The two-turn test contains a `stream_launch_enabled` precondition assertion and an
      `assert_ne!` between the two captures.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; cargo test -p devflow --bin devflow the_gate_rule_holds_in_both_delivered_turns 2>&1 | tee /tmp/47-03-red-cli.log; echo "cargo_exit_cli=${PIPESTATUS[0]}"; grep "test result:" /tmp/47-03-red-cli.log; a=$(grep -c "test result: FAILED" /tmp/47-03-red-cli.log || true); echo "cli_failed_binaries=$a"; test "$a" -ge 1 || rc=1; az=$(grep -c "0 passed" /tmp/47-03-red-cli.log || true); echo "cli_zero_passed_lines=$az"; test "$az" -ge 1 || rc=1; ar=$(grep -c "running 1 test" /tmp/47-03-red-cli.log || true); echo "cli_test_actually_ran=$ar"; test "$ar" -ge 1 || rc=1; cargo test -p devflow-core --lib the_gate_rule_has_one_definition_site 2>&1 | tee /tmp/47-03-red-core.log; echo "cargo_exit_core=${PIPESTATUS[0]}"; grep "test result:" /tmp/47-03-red-core.log; b=$(grep -c "test result: FAILED" /tmp/47-03-red-core.log || true); echo "core_failed_binaries=$b"; test "$b" -ge 1 || rc=1; bz=$(grep -c "0 passed" /tmp/47-03-red-core.log || true); echo "core_zero_passed_lines=$bz"; test "$bz" -ge 1 || rc=1; br=$(grep -c "running 1 test" /tmp/47-03-red-core.log || true); echo "core_test_actually_ran=$br"; test "$br" -ge 1 || rc=1; body=/tmp/47-03-t2-body.txt; grep -m1 -A 80 -E "^[[:space:]]*fn the_gate_rule_holds_in_both_delivered_turns" crates/devflow-cli/src/pipeline_launch.rs > "$body"; bw=$(wc -l < "$body"); echo "test_body_lines=$bw"; test "$bw" -ge 20 || rc=1; pc=$(grep -c "stream_launch_enabled" "$body" || true); echo "stream_precondition_in_test=$pc"; test "$pc" -ge 1 || rc=1; ne=$(grep -c "assert_ne" "$body" || true); echo "assert_ne_in_test=$ne"; test "$ne" -ge 1 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `cli_failed_binaries=0` or `core_failed_binaries=0` (a compile error prints neither a `test result:` line nor a failure count, which is exactly the outcome this gate distinguishes from a real red), either `zero_passed_lines` value 0, either `test_actually_ran` value 0 (verified on the pre-work tree: a name filter that matches nothing prints `running 0 tests` with a large `filtered out` count and cargo still exits 0, so a `0 passed` line alone does not prove the test executed), `test_body_lines` below 20 (the test-body window came back empty, so the two counts over it would read as clean zeros — the control on the extraction), `stream_precondition_in_test=0`, or `assert_ne_in_test=0`. The two control counts are scoped to the new test's own body deliberately: `stream_launch_enabled` already appears 24 times elsewhere in this file, so a file-wide count would pass without the test existing at all. The window is `-A 80`, not `-A 45`: the behaviour specified for this test (precondition, two captures, an `argv[0]` check, an `assert_ne`, two containment controls and three rule assertions) plausibly exceeds 46 lines, and a window that ends before `assert_ne!` would fail a CORRECT test. `test_body_lines >= 20` remains the emptiness control, and the extraction is anchored to a line whose first token is `fn` with `-m1` so a rationale comment naming the test cannot open a second window.</fails_when>
  </verify>
  <done>
    Both halves of D-03's layered test are committed and both printed a real `test result: FAILED`
    against the pre-fix tree, with the captures recorded.
  </done>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: One rule, one definition site — hoist GATE_RESOLUTION_RULE, split the sentence, tighten the resume prompt</name>
  <files>crates/devflow-core/src/prompt.rs, crates/devflow-cli/src/pipeline_launch.rs, crates/devflow-core/src/snapshots/</files>
  <read_first>
    - `crates/devflow-core/src/prompt.rs` — the doc comment binding the two texts (:57-61), `CODE_STAGE_POLICY` (:62-89), `checkpoint_auto_decide_prompt` and its byte-determinism doc comment (:530-549)
    - `crates/devflow-core/src/prompt.rs` tests that assert on the exact text being changed: `code_policy_excludes_blocking_human_and_package_checkpoints` (:755-765), `code_stage_prompt_is_deterministic` (:768-775), `checkpoint_auto_decide_prompt_is_deterministic` (:989), `checkpoint_auto_decide_prompt_terminates_with_completion_protocol` (:997), `checkpoint_auto_decide_prompt_states_no_operator_judgment_and_record_reasoning` (:1008)
    - `crates/devflow-core/src/verify.rs` :131-137 — `phase_has_blocking_human_checkpoint` matches the literal `gate="blocking-human"` and nothing else; this is why D-02 tightens the resume prose to name that class
    - `.planning/phases/47-unattended-decision-policy-consistency/47-CONTEXT.md` D-01, D-02 (settled narrow), D-04, and § Specific Ideas (the scoped-hierarchy phrasing, and the recency-not-primacy correction)
    - `.planning/phases/47-unattended-decision-policy-consistency/47-RESEARCH.md` § B-2 step 4 (the second negative control)
  </read_first>
  <behavior>
    - Both tests from Task 2 turn green.
    - `CODE_STAGE_POLICY` includes the rule through `gate_resolution_rule!()`, the same single
      definition `GATE_RESOLUTION_RULE` expands, rather than stating it inline.
    - `package_verification_prohibition_is_unconditional` (NEW, in `prompt.rs`'s `mod tests`) is
      green: it asserts the rendered `CODE_STAGE_POLICY` contains the pinned gate-rule section from
      `<action>` character for character, and that `GATE_RESOLUTION_RULE` equals that section's
      second sentence exactly. Its expected text is its own literal in the test, never built from
      the constants under test — otherwise widening a constant would widen the expectation with it.
    - That test fails on ANY change to the pinned section: a synonym, a restructure, a waiver added
      inside the section, or a missing space before a line continuation. It does NOT catch an
      authorization added elsewhere in the policy, outside the section (for example "An operator
      may approve any checkpoint after review."). That case is caught only by reading the snapshot
      diff when the baseline is re-blessed, and in phase review.
    - `checkpoint_auto_decide_prompt` output names `blocking-human` and remains byte-deterministic
      across two calls for the same phase.
  </behavior>
  <action>
    Hoist the gate rule into a new `pub const GATE_RESOLUTION_RULE` in
    `crates/devflow-core/src/prompt.rs`, sited immediately above `CODE_STAGE_POLICY` and following
    that constant's shape — a `const &str` with backslash continuation and a doc comment naming
    D-04, `CODE_STAGE_POLICY` and `checkpoint_auto_decide_prompt` from both ends. It is `pub`
    deliberately, so the cross-crate delivery test in `pipeline_launch.rs` asserts against the SAME
    definition rather than a re-typed copy — that is what makes D-04's "one definition site" true
    rather than aspirational. `CODE_STAGE_POLICY` itself stays private.

    Include the rule in `CODE_STAGE_POLICY` in place of the inline text (it has to go through a
    macro — see below), and
    SPLIT the two-class sentence at :86-89. The carve-out attaches to the `blocking-human` gate
    only; the package-verification checkpoint keeps the unconditional prohibition in its own
    sentence with no exception. Splitting is the mechanical move — deleting the sentence is not, and
    `code_policy_excludes_blocking_human_and_package_checkpoints` (:755-765) asserts the lowercased
    prompt still contains both `blocking-human` and `package-verification`.

    The gate-rule section is PINNED, not left to wording judgement (operator decision 2026-09-11,
    recorded under CONTEXT D-02). After Rust joins the string continuations, `CODE_STAGE_POLICY`
    must contain exactly this text as one unbroken passage:

        This authority never extends to a package-verification checkpoint: do not self-resolve or approve one under any circumstances, and report it instead. A blocking-human gate is human-only, with one exception: if DevFlow resumed you specifically to resolve that gate, resolve it yourself and record your reasoning in your final message; otherwise report it instead.

    The first sentence replaces the package-verification half of the old two-class sentence at
    :86-89. The second sentence IS `GATE_RESOLUTION_RULE` and must render to it exactly. Keep the
    existing closing sentence, "This policy must not pause execution or request human input.",
    after the section. Write blocking-human WITHOUT backticks inside the section: the gate below
    greps the rendered snapshot for this exact text, and a backtick inside a plan's `bash -c '…'`
    command becomes command substitution.

    Two Rust facts decide how to write it, both verified on 2026-09-11:
    - `concat!` accepts only literals. `concat!("…", GATE_RESOLUTION_RULE)` fails with
      `error: expected a literal`, so `CODE_STAGE_POLICY` cannot include the constant by name.
      Define the sentence once in `macro_rules! gate_resolution_rule` expanding to the literal, set
      `pub const GATE_RESOLUTION_RULE: &str = gate_resolution_rule!();`, and splice
      `gate_resolution_rule!()` into `CODE_STAGE_POLICY`'s `concat!`. That is still one definition
      site. The check confirmed it compiles, renders the pinned section, and keeps
      `code_policy_excludes_blocking_human_and_package_checkpoints` satisfied.
    - A line continuation (a `\` at the end of a line) drops the newline AND the next line's
      leading whitespace, so the space between two words must sit BEFORE the `\`. Removing one such
      space made the pinned assertion fail in the same check ("do notself-resolve").

    `package_verification_prohibition_is_unconditional` asserts the pinned section against its own
    literal — never built with `format!` or `concat!` from `GATE_RESOLUTION_RULE` or
    `CODE_STAGE_POLICY` — and asserts `GATE_RESOLUTION_RULE` equals the second sentence. Prove it
    discriminates before moving on: temporarily append "unless DevFlow resumed you" to the package
    sentence, run the test, confirm a real `test result: FAILED`, restore it, and record both runs
    in the SUMMARY.

    The pinned text already states the carve-out as a scoped hierarchy — the prohibition stands, with
    one named condition under which it does not apply — so an agent reading it sees one rule with a
    condition, not two rules in tension. Do not paraphrase it: the wording is no longer discretionary.

    Tighten `checkpoint_auto_decide_prompt` so it names `blocking-human` rather than the generic
    "human-blocking checkpoint", and so its affirmative framing states the SAME condition as
    `GATE_RESOLUTION_RULE`. Keep the affirmative mood — D-04 rejects forcing one literal string into
    two grammatical moods; what must be identical is the condition, not the prose. Do not extend the
    resume prompt's authority beyond `blocking-human`: the resume route fires only when
    `phase_has_blocking_human_checkpoint` is true, and package-verification checkpoints are never
    routed through this injection.

    Then strengthen the two-turn test's turn-1 assertion from the Task 2 string literal to
    `contains(GATE_RESOLUTION_RULE)`, and add the same to the constant-level test. This is the point
    at which the two texts are pinned to one source.

    Then run the second negative control (RESEARCH B-2 step 4): revert ONLY the
    `checkpoint_auto_decide_prompt` tightening, re-run `the_gate_rule_holds_in_both_delivered_turns`,
    confirm a real `test result: FAILED`, and restore. Task 2's red was against different assertion
    text, so this is what proves the STRENGTHENED assertion still discriminates.

    If any pre-existing test listed in `read_first` has to change, do not quietly edit it — record
    in the plan SUMMARY which test changed and why the text change made the old assertion wrong.
    Then re-bless the `.snap` baseline whose text this change drifts, reading the diff before
    staging it.
  </action>
  <acceptance_criteria>
    - `GATE_RESOLUTION_RULE` is defined once in `crates/devflow-core/src/prompt.rs` (through
      `gate_resolution_rule!()`), is `pub`, is included in `CODE_STAGE_POLICY` through that same
      macro, and is referenced by the `prompt.rs` tests and by the `pipeline_launch.rs` test.
    - The rendered `CODE_STAGE_POLICY` contains the pinned gate-rule section from `<action>`
      character for character, and `GATE_RESOLUTION_RULE` renders to exactly its second sentence.
    - The re-blessed fix-prompt snapshot contains the pinned section exactly once — checked from
      the rendered snapshot, independently of any literal the executor writes into the test.
    - The old two-class sentence is gone, asserted by `the_gate_rule_has_one_definition_site`
      rather than by a grep over source.
    - The SUMMARY records `package_verification_prohibition_is_unconditional` printing
      `test result: FAILED` under a temporary widening of the package sentence, and green after.
    - `checkpoint_auto_decide_prompt`'s rendered output contains `blocking-human`.
    - `the_gate_rule_holds_in_both_delivered_turns` and `the_gate_rule_has_one_definition_site` both
      pass with `1 passed` and a non-zero `filtered out` count.
    - The SUMMARY records the second negative control's `test result: FAILED` capture.
    - `scripts/check.sh test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; cargo test -p devflow-core --lib prompt::tests::package_verification_prohibition_is_unconditional -- --exact 2>&1 | tee /tmp/47-03-pv.log; pv=$(grep -c "test result: ok. 1 passed" /tmp/47-03-pv.log || true); echo "pv_test_one_passed=$pv"; test "$pv" -eq 1 || rc=1; pvf=$(grep -oE "[0-9]+ filtered out" /tmp/47-03-pv.log | head -1); echo "pv_filtered=$pvf"; case "$pvf" in ""|"0 filtered out") rc=1;; esac; sf=$(ls crates/devflow-core/src/snapshots/*__claude_style_full_execute_fix_prompt_snapshot.snap 2>/dev/null | wc -l); echo "fix_prompt_snapshot_files=$sf"; test "$sf" -eq 1 || rc=1; ps=$(cat crates/devflow-core/src/snapshots/*__claude_style_full_execute_fix_prompt_snapshot.snap 2>/dev/null | grep -c -F "This authority never extends to a package-verification checkpoint: do not self-resolve or approve one under any circumstances, and report it instead. A blocking-human gate is human-only, with one exception: if DevFlow resumed you specifically to resolve that gate, resolve it yourself and record your reasoning in your final message; otherwise report it instead." || true); echo "pinned_section_in_snapshot=$ps"; test "$ps" -eq 1 || rc=1; code=/tmp/47-03-code.rs; grep -v "^[[:space:]]*//" crates/devflow-core/src/prompt.rs > "$code"; g=$(grep -c "GATE_RESOLUTION_RULE" "$code" || true); echo "gate_rule_sites_in_prompt_rs=$g"; test "$g" -ge 3 || rc=1; x=$(grep -v "^[[:space:]]*//" crates/devflow-cli/src/pipeline_launch.rs | grep -c "GATE_RESOLUTION_RULE" || true); echo "gate_rule_sites_in_cli=$x"; test "$x" -ge 1 || rc=1; cargo test -p devflow --bin devflow pipeline_launch::tests::the_gate_rule_holds_in_both_delivered_turns -- --exact 2>&1 | tee /tmp/47-03-green-cli.log; c=$(grep -c "test result: ok. 1 passed" /tmp/47-03-green-cli.log || true); echo "cli_one_passed=$c"; test "$c" -eq 1 || rc=1; fc=$(grep -oE "[0-9]+ filtered out" /tmp/47-03-green-cli.log | head -1); echo "cli_filtered=$fc"; case "$fc" in ""|"0 filtered out") rc=1;; esac; cargo test -p devflow-core --lib prompt::tests::the_gate_rule_has_one_definition_site -- --exact 2>&1 | tee /tmp/47-03-green-core.log; o=$(grep -c "test result: ok. 1 passed" /tmp/47-03-green-core.log || true); echo "core_one_passed=$o"; test "$o" -eq 1 || rc=1; fo=$(grep -oE "[0-9]+ filtered out" /tmp/47-03-green-core.log | head -1); echo "core_filtered=$fo"; case "$fo" in ""|"0 filtered out") rc=1;; esac; scripts/check.sh test > /tmp/47-03-check.log 2>&1; e=$?; echo "check_test_exit=$e"; test "$e" -eq 0 || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>`gate_rc=1` or a non-zero exit; specifically `pv_test_one_passed` not equal to 1 or `pv_filtered` empty or the literal `0 filtered out` (the pin test did not run or did not pass — a bare `--exact` name miss prints `0 passed` and still exits 0), `fix_prompt_snapshot_files` not exactly 1 (no snapshot, or more than one matching file, so the next count would not describe one rendered prompt), `pinned_section_in_snapshot` not exactly 1 (the re-blessed rendered prompt does not carry the pinned text verbatim — this reads rendered output, so it cannot be satisfied by editing the test's expected literal to match a widened policy), `gate_rule_sites_in_prompt_rs` below 3 or `gate_rule_sites_in_cli` below 1 (the rule is not actually shared), either `one_passed` value not equal to 1, either `filtered` value empty or the literal `0 filtered out`, or `check_test_exit` non-zero.

**How D-02 is enforced, and what is not covered.** On 2026-09-11 the operator chose to pin the gate-rule section's exact text, after three review rounds showed every word-list design failing: a shell token blacklist evadable 15 ways; a `sed` join that bash quoting silently broke, so that gate read 0 before the fix and 0 after; and a Rust token check that correct work could not pass — the pinned sentence itself contained two of its forbidden words — which also missed a waiver placed in a neighbouring sentence. A character-for-character pin cannot be paraphrased around. The snapshot check was verified in both directions on 2026-09-11 against a real insta 1.48.0 snapshot: the pinned text is found once; a copy widened with "unless DevFlow resumed you" is not found; a missing file and a duplicate file both fail. **Not covered mechanically:** an authorization added elsewhere in the policy, outside the pinned section. That is caught only by reading the snapshot diff at re-bless time, and in phase review.</fails_when>
  </verify>
  <done>
    The gate rule has one definition site, the carve-out is scoped to `blocking-human` alone,
    package verification keeps its unconditional prohibition, both halves of the layered test are
    green against the strengthened assertion, and the strengthened assertion has been observed
    failing.
  </done>
  <reversibility rating="costly">Undoing D-04 means re-inlining the rule into both texts and
  rewriting the two tests that assert on the shared constant. The D-02 narrowing itself is two-way
  reversible — it is text only, and it deliberately does not widen authority over package
  verification.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow → resumed Claude session (turn 2, argv) | The resume instruction grants an unattended agent authority it does not otherwise have |
| operator gate approval → `launch_stage_inner` | `GateAction::Advance` skips the just-adjudicated preflight check, which is how an unattended run reaches this path at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-47-09 | Elevation of Privilege | the carve-out widening beyond `blocking-human` to package verification | high | mitigate | D-02 settled narrow and its section PINNED (operator, 2026-09-11): `package_verification_prohibition_is_unconditional` asserts the exact text in the rendered `CODE_STAGE_POLICY`, and the gate independently finds it once in the re-blessed fix-prompt snapshot, so a widened policy cannot pass by editing the test's own literal. **Not covered mechanically:** an authorization added elsewhere in the policy, outside the pinned section — caught only by snapshot-diff review at re-bless and in phase review |
| T-47-10 | Repudiation | a self-resolved gate with no recorded reasoning | medium | mitigate | `GATE_RESOLUTION_RULE` carries the recorded-reasoning requirement into both texts; `checkpoint_auto_decide_prompt` keeps its "MUST record your reasoning" clause |
| T-47-11 | Tampering | moving the `checkpoint_auto_decided` emission after the spawn | high | mitigate | Task 1 forbids it and the three `relaunch_checkpoint_session_*` tests are asserted byte-identical to the fork point; D-10's evidence standard depends on the emission proving injection only |
| T-47-12 | Spoofing | a test asserting a joined turn-1+turn-2 string, standing in for a composition production never performs | medium | mitigate | Task 2 forbids concatenation; each turn is captured from its own production constructor with `assert_ne!` and containment controls |

**Residual, accepted and recorded rather than mitigated:** which instruction a model actually
follows when both are present is not answerable by reading source (ROADMAP criterion 4). Plan 47-05
records it as a Phase 49 observation item with a named evidence standard. DECN-03 closes for claude
only — `pipeline_launch.rs:1569` gates the resume route on `AgentKind::Claude` and
`exec_resume_command` exists only on `ClaudeDriver` (D-11).
</threat_model>

<verification>
- Both halves of the layered test red before the fix and green after, with both captures recorded.
- The second negative control (revert only the resume-prompt tightening) produced a real
  `test result: FAILED`.
- `scripts/check.sh test` green.
</verification>

<success_criteria>
- ROADMAP criterion 3: a prompt rendered with both `CODE_STAGE_POLICY` and
  `checkpoint_auto_decide_prompt` states exactly one rule about who may resolve a `blocking-human`
  gate, and a test that detects the contradiction failed before the fix and passes after.
- ROADMAP criterion 4 is deliberately NOT claimed by this plan.
</success_criteria>

## Artifacts this phase produces

Created or changed by this plan:

| Kind | Symbol / path |
|---|---|
| constant | `pub const GATE_RESOLUTION_RULE` (`crates/devflow-core/src/prompt.rs`) |
| macro | `gate_resolution_rule!()` — expands to the `GATE_RESOLUTION_RULE` sentence literal so `CODE_STAGE_POLICY` can include it via `concat!`, which rejects a `const` name |
| text change | `CODE_STAGE_POLICY` interpolates `GATE_RESOLUTION_RULE`; the two-class sentence is split |
| text change | `checkpoint_auto_decide_prompt` names `blocking-human` |
| function | `resume_launch_shape(phase, session_id)` (`crates/devflow-cli/src/pipeline_launch.rs`) |
| test | `the_gate_rule_has_one_definition_site` (`crates/devflow-core/src/prompt.rs`) |
| test | `the_gate_rule_holds_in_both_delivered_turns` (`crates/devflow-cli/src/pipeline_launch.rs`) |
| golden update | the `claude_style_full_execute_fix_prompt_snapshot` `.snap` baseline, re-blessed |

<output>
Create `.planning/phases/47-unattended-decision-policy-consistency/47-03-SUMMARY.md` when done.
It MUST carry both pre-fix `test result: FAILED` captures, the second negative control's capture,
and a statement of whether any pre-existing test had to change.
</output>
