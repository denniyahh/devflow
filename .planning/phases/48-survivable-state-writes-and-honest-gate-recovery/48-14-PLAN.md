---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 14
type: tdd
wave: 9
depends_on: ["48-13"]
files_modified:
  - crates/devflow-core/src/recover.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/tests/gate_wedge_e2e.rs
autonomous: true
requirements: [SURV-02]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-05.4 and R-1, `recover --clean --phase N` deletes the phase's gate request/response/ack files (and their temps) for every stage only when `lock::holder_status` reports no live waiter (`!may_be_waiting()`: `NoHolder`, or `Recycled`, a live pid that is not the process that took the lock); with `Live` or `Unconfirmable` it keeps them and warns naming the pid"
    - "#200 criterion 4 wedge arm completes: after the killed start and the honest `gate reject`, `devflow recover --clean --phase N` leaves no state file, no gate files, no `.tmp` orphans and no `.devflow/lock-NN`"
    - "Per D-05.5 and F-6, `PhaseFacts` carries the waiter status computed in `build_phase_facts`; a new report-only finding fires for an open gate with no live waiter (`!waiter.may_be_waiting()`, so `NoHolder` or `Recycled`), and its repair is the exact string `no_waiter_repair` returns for that gate"
    - "Per D-05.5, `check_orphan_gate` no longer prescribes `devflow gate approve` for a gate with no live waiter; `check_gate_pending_without_gate` keeps `devflow resume --phase N`"
    - "Per T-18-02 (C-13), every doctor finding stays report-only; doctor performs no write"
    - "Every repair-string assertion changed by this plan (commands.rs tests F-6 lists) is enumerated in SUMMARY with old and new text (C-28)"
  prohibitions:
    - statement: "`recover --clean` never deletes gate files while `holder_status` may be a waiter (`Live` or `Unconfirmable`) (R-1)"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/recover.rs
      provides: "clean_phase gate cleanup under R-1"
      contains: "holder_status"
    - path: crates/devflow-cli/src/commands.rs
      provides: "PhaseFacts waiter field, open-gate-without-waiter finding"
      contains: "fn check_open_gate_without_waiter"
    - path: crates/devflow-cli/tests/gate_wedge_e2e.rs
      provides: "wedge arm recovery assertions"
  key_links:
    - from: crates/devflow-cli/src/commands.rs::check_open_gate_without_waiter
      to: crates/devflow-cli/src/commands.rs::no_waiter_repair
      via: "same message code as the CLI verbs (D-05.5)"
      pattern: "no_waiter_repair"
---

<objective>
SURV-02 — `recover --clean` ends a dead phase completely without stranding a live waiter (D-05.4, R-1), and `doctor`
prints the same repair lines as the verbs (D-05.5, F-6). Completes criterion 4's wedge arm.

Output: recover change, doctor finding, updated repair assertions, extended e2e. Contracts touched (P3): C-13 (T-18-02
report-only; doctor tests near commands.rs:6026+), C-19 (`remove_stale_locks` keeps a live holder), C-28 (repair strings).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-13-SUMMARY.md

PATTERNS.md § 6 and § 14; RESEARCH Finding F-6. 48-13 provides `lock::holder_status` and `commands::no_waiter_repair`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — recover --clean clears the wedged phase end to end, and keeps gate files under a live holder</name>
  <files>crates/devflow-core/src/recover.rs, crates/devflow-cli/tests/gate_wedge_e2e.rs</files>
  <read_first>
    - crates/devflow-core/src/recover.rs lines 80-150 and its tests near 200-240 (`DEAD_PID`, fixtures)
    - crates/devflow-core/src/lock.rs `remove_stale_locks` and `holder_status`; crates/devflow-core/src/stage.rs `Stage` (variants)
    - crates/devflow-cli/tests/gate_wedge_e2e.rs (48-13)
  </read_first>
  <behavior>
    - `recover::tests::clean_phase_removes_gate_files_when_no_process_holds_the_lock`: gate request/response/ack plus a planted temp for two stages, no lock → all removed, state cleared
    - `recover::tests::clean_phase_keeps_gate_files_while_a_live_process_holds_the_lock`: in-process `lock::acquire` held → gate files remain, a warning names the pid
    - `recover::tests::clean_phase_removes_gate_files_when_the_lock_pid_is_recycled`: a lock in `acquire`'s two-line format naming the test's own pid with a start time one greater than its `/proc` start time → gate files removed (a recycled pid is not a waiter), and the lock file itself remains because `remove_stale_locks` keeps any live pid (C-19; D-03 accepted gap) — assert both
    - e2e `wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly` gains its final step: `devflow recover --clean --phase N <root>` exits 0, then `.devflow/state-NN.json`, every `.devflow/gates/NN-*` file, any `.tmp` under `.devflow/`, and `.devflow/lock-NN` are absent
  </behavior>
  <action>
Per D-05.4 and R-1: tests first. In `clean_phase`, before `workflow::clear_state`, compute `lock::holder_status(project_root, phase)`. No live waiter (`!may_be_waiting()`: `NoHolder` or `Recycled`) → call `Gates::cleanup(project_root, phase, stage)` for every `Stage` variant, pushing a warning on error. `Live { pid }` / `Unconfirmable { pid }` → push `kept phase {phase}'s gate files — pid {pid} holds its per-phase lock` and skip gate cleanup. Keep the existing agent-running warning, state clear, cron cleanup and `remove_stale_locks` order.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in clean_phase_removes_gate_files_when_no_process_holds_the_lock clean_phase_keeps_gate_files_while_a_live_process_holds_the_lock clean_phase_removes_gate_files_when_the_lock_pid_is_recycled; do cargo test -p devflow-core --lib "recover::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; cargo test -p devflow --test gate_wedge_e2e > "$L/e.log" 2>&1; e=$?; eok=$(grep -cE "^test result: ok\. 2 passed; 0 failed" "$L/e.log" || true); echo "gate_wedge_e2e_exit=$e two_passed=$eok"; [ "$e" -eq 0 ] && [ "$eok" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any of the three recover tests one_passed=0 or filtered=none, or gate_wedge_e2e not reporting exactly `2 passed; 0 failed` (an arm missing, or the recovery step failing)</fails_when>
  </verify>
  <acceptance_criteria>
    - The keep-under-holder test is the control for R-1: if gate cleanup ran unconditionally it would fail
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>`recover --clean` ends a dead phase completely and never strands a live waiter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: doctor reports an open gate with no waiter using the verbs' repair line</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs lines 3160-3290 (`PhaseFacts`, `PhaseFinding`, `check_gate_pending_without_gate`, `check_orphan_gate`) and 3360-3415 (`build_phase_facts`)
    - The doctor test module (locate with `rg -n "mod doctor_reconciliation|fn agreeing_facts" crates/devflow-cli/src/commands.rs`) and each repair assertion near :5812, :5893, :5912, :6062, :6079, :6096, :6154, :6310
  </read_first>
  <behavior>
    - `reconcile_phase_flags_open_gate_with_no_waiter_with_the_cli_repair`: open Validate gate, waiter `NoHolder` → one finding; `repair` equals `no_waiter_repair(phase, Stage::Validate)`
    - `reconcile_phase_open_ship_gate_with_no_waiter_names_ship`: repair equals `devflow ship --phase N`
    - `reconcile_phase_flags_open_gate_with_a_recycled_waiter_as_no_waiter`: open Validate gate, waiter `Recycled { pid }` → the same single no-waiter finding and repair as the `NoHolder` case (fails a check that matches `NoHolder` instead of `!may_be_waiting()`)
    - Control `reconcile_phase_open_gate_with_a_live_waiter_is_not_a_no_waiter_finding`: waiter `Live` → no no-waiter finding
  </behavior>
  <action>
Per D-05.5 and F-6: tests first. Add `pub(crate) waiter: lock::HolderStatus` to `PhaseFacts` (doc comment: computed in `build_phase_facts`, all I/O there), set it in `build_phase_facts` via `lock::holder_status`, and add it to `agreeing_facts` and every struct literal (use `HolderStatus::NoHolder` where the case has no open gate). Add `fn check_open_gate_without_waiter(facts: &PhaseFacts) -> Option<PhaseFinding>` (Problem severity, report-only) that fires when `open_gate_stages` is non-empty and `!facts.waiter.may_be_waiting()` (`NoHolder` or `Recycled`), with detail naming the stage and `repair: Some(no_waiter_repair(facts.phase, stage))`, and register it in `reconcile_phase`. Make `check_orphan_gate` return `None` when `!facts.waiter.may_be_waiting()` (the new check owns that case). Update each pinned repair assertion that now changes and list old → new in SUMMARY.

Gate scope: the full `commands::tests::` module runs because every `PhaseFacts` struct literal in it gains the `waiter` field, so no named-test subset shows the literals still compile and agree; workspace clippy runs because this is the phase's final task and Task 1 changed `devflow-core` (`recover.rs`), which every `devflow-cli` target compiles against.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in reconcile_phase_flags_open_gate_with_no_waiter_with_the_cli_repair reconcile_phase_flags_open_gate_with_a_recycled_waiter_as_no_waiter reconcile_phase_open_ship_gate_with_no_waiter_names_ship reconcile_phase_open_gate_with_a_live_waiter_is_not_a_no_waiter_finding; do cargo test -p devflow --bin devflow "$t" > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; ap=$(grep -v "^[[:space:]]*//" crates/devflow-cli/src/commands.rs | grep -c "devflow gate approve {} --stage {}" || true); echo "approve_repair_format_sites=$ap"; cargo test -p devflow --bin devflow commands::tests:: > "$L/u.log" 2>&1; ue=$?; echo "commands_unit_exit=$ue"; [ "$ue" -eq 0 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any doctor test not exactly `1 passed` with a non-zero `filtered out` (these unique names are run as substring filters because the doctor module path is not pinned; exactly one match is required), the commands unit module failing, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `approve_repair_format_sites` is printed for the record (measured 1 on 2026-09-14); if still 1, SUMMARY shows `check_orphan_gate` returns `None` whenever `!may_be_waiting()` (`NoHolder` or `Recycled`), so that repair can only print for a live or unconfirmable waiter
    - SUMMARY enumerates every changed repair-string assertion, old → new
    - `scripts/check.sh all` passes after this plan (the phase's final wave)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>doctor and the verbs give one repair for a gate nobody is waiting on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator recovery verbs → a possibly live phase | `recover --clean` takes no lock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-14-01 | Denial of Service | cleanup strands a live waiter until its timeout | high | mitigate | R-1 `holder_status` guard; keep-under-holder test |
| T-48-14-02 | Repudiation | doctor prescribes a repair that does nothing | medium | mitigate | Shared `no_waiter_repair`; orphan-gate check yields to the new finding |
| T-48-14-03 | Tampering | doctor mutates state | low | mitigate | Report-only unchanged (T-18-02) |
</threat_model>

<verification>
Both gates print `gate_rc=0`; `scripts/check.sh all` green; before `/gsd-verify-work`, `scripts/check-in-container.sh` (background it; cold runs exceed default timeouts).
</verification>

<success_criteria>
A wedged phase is recoverable with the commands the CLI names, and doctor agrees with the CLI.
</success_criteria>

## Artifacts this phase produces

- `PhaseFacts.waiter: lock::HolderStatus`
- `commands::check_open_gate_without_waiter(facts: &PhaseFacts) -> Option<PhaseFinding>`
- Tests named in both `<behavior>` blocks

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-14-SUMMARY.md` when done.
</output>
