---
context: phase
phase: 48-survivable-state-writes-and-honest-gate-recovery
task: 0
total_tasks: 34
status: in_progress
last_updated: 2026-09-15T15:00:09.637Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **Work only in the phase worktree.**
  - What happened: Codex once ran `$gsd-plan-phase 47` in the main checkout, where `workspace/denniyahh` is checked out. It planned against a stale `.planning/` and left stray commit `b102898`.
  - Mitigation: `cd /var/home/denniyahh/Github/devflow/.worktrees/phase-48` and confirm `git rev-parse --abbrev-ref HEAD` prints `feature/phase-48` before running any GSD command.
- [ ] CONSTRAINT: **Do not execute the phase 48 plans before the `--reviews` replan.**
  - What happened: `48-REVIEWS.md` confirmed safety gaps in them:
    - CX-1: `recover --clean` can delete a live gate that a newly started run just wrote.
    - CX-3: a fenced `</task>` line lets a changed checkpoint skip the CHKPT-02 human gate.
  - STATE.md still says "Ready to execute"; these findings override that.
  - Mitigation: run `$gsd-plan-phase 48 --reviews` first.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Structural gates not run against the current tree | A 48-07 gate named the wrong function's signature and would have failed on unchanged code; only the third checker round caught it | advisory | Run every structural `rg`/`grep` gate against the current tree before committing a plan, and paste the exact bytes that ran |
| Apostrophes inside `bash -c '...'` | An apostrophe closed the surrounding single quote twice this session: a commit message caused a shell parse error, and a linter comment caused a gawk crash that refused every plan | advisory | Write scripts and messages with a file-writing tool or `git commit -F <file>`; never put an apostrophe inside a single-quoted `bash -c` body |
| Trusting GSD state writes | `state.planned-phase` wrote the phase slug and recomputed progress to 17%; `roadmap.update-plan-progress` left `**Plans**: TBD` above the plan list | advisory | Run `git diff` on STATE.md and ROADMAP.md after every GSD state or roadmap command; fix by hand and log the fix in `~/.config/agents/gsd-manual-fix-incidents.md` (global rule) |
| Counting a dropped review lane as a pass | antigravity (agy) timed out at 45 minutes with 0 bytes of output | advisory | Record each drop with its reason; a review needs at least 2 completed, source-grounded lanes |
| Accepting planner output without a completion marker | The planner died at a usage limit after writing 14 plans | advisory | Check requirement, decision and structure coverage before accepting; the checker then still found 1 blocker and 7 warnings |

<current_state>
- **Planned and committed:** phase 48 on `feature/phase-48` (HEAD `e62eca9`, no upstream). 14 plans in 9 waves.
- **Checker:** passed after 2 revision rounds, plus an operator-approved narrow fix to 48-07. That fix was verified mechanically; the checker was not re-run.
- **Review:** a cross-AI review then confirmed gaps, recorded in `48-REVIEWS.md` (commit `e62eca9`). Reviewers: codex and pi (DeepSeek v4 Pro); antigravity dropped.
- **Execution:** nothing has been executed.
- **Why paused:** handoff to Codex for the `--reviews` replan.
</current_state>

<completed_work>
- Research: `48-RESEARCH.md` (`71b3eb3`)
- Validation strategy: `48-VALIDATION.md` (`2be3907`)
- Pattern map: `48-PATTERNS.md` (`c0fb193`)
- 14 plans plus STATE.md, ROADMAP.md and RESEARCH.md updates (`9915c56`)
- Pre-commit plan linter: fixed a crash (apostrophes in a comment) and a false positive on `--exact --nocapture` (`b9ee940`)
- Linter regression tests: 20/20 pass. The pre-fix linter fails all 5 new tests on the crash check (`56f3466`)
- Cross-AI review, with every finding checked against source (`e62eca9`)
</completed_work>

<remaining_work>
1. **Operator decision:** what `recover --clean` may delete while a process holds, or could take, the per-phase lock. This covers CX-1 and PI-S2 together.
2. **`$gsd-plan-phase 48 --reviews`.** Incorporate each finding, or explicitly defer it in the relevant PLAN.md:
   - High: CX-1, CX-3.
   - Medium: CX-2, CX-4, PI-1.
   - Low-medium: PI-S2.
   - Low: CX-5, CX-6, PI-2, PI-3.
   - Unverified: PI-S1 needs a clippy probe for aliased imports.
   - Out of scope: CX-S1 (`48-CONTEXT.md:451`).
3. **Checks after replanning:** the plan checker runs inside that workflow. Then run a second external adversarial review of the revised set, per the operator's standing rule. On this host the working lanes are codex and pi; agy tends to drop.
4. **Execute:** commit, then `$gsd-execute-phase 48`.
</remaining_work>

<decisions_made>
- **Requirement IDs (D-11):** plan frontmatter uses SURV-01, SURV-02, CHKPT-01, CHKPT-02 and TEST-01, never GATE-01 or GATE-02. REQUIREMENTS.md already uses those for #184 and #170. The ROADMAP keeps the old aliases (GATE-01 = 999.125, GATE-02 = 999.126, 999.38 → TEST-01) until plan 48-01 executes. Pass that mapping to the planner and checker.
- **Upgraded runs:** a run whose state has no recorded checkpoint set gates at a loop-back Code evaluation (fail closed). This is derived from D-07's text, not a new decision.
- **Criterion 4:** ROADMAP criterion 4 drops the "~40s" figure. This was an operator decision on 2026-09-14, applied by plan 48-01.
- **48-07 signature gate:** it targets `preflight_unattended_launch_check`, the function that carries the fixed signature from preflight D-09.
- **Stray AGENTS.md:** the ChatGPT desktop app's Claude Code import sync was disabled (`~/.codex/config.toml`, `external-agent-import-sync-enabled = false`) after it wrote a stray `AGENTS.md`.
- **Global rule added:** manual fixes to GSD-generated artifacts are logged in `~/.config/agents/gsd-manual-fix-incidents.md`.
</decisions_made>

<blockers>
- **recover --clean deletion scope:** needs an operator decision. Ask; do not choose on the operator's behalf.
</blockers>

## Required Reading (in order)
1. `48-REVIEWS.md` — the findings the replan must resolve, with checked severities and `file:line` evidence.
2. `48-CONTEXT.md` — locked decisions D-01 to D-11 and amendments R-1 to R-8. R-1, at lines 305-310, is central to CX-1.
3. The 14 `48-NN-PLAN.md` files. Findings touch 48-02, 48-07, 48-10, 48-11, 48-13 and 48-14.
4. Repository `CLAUDE.md`. Codex does not load it; it holds the crate topology and the canonical test commands.
5. `~/.config/agents/gsd-manual-fix-incidents.md` — the incident log template, per the global rule.

## Critical Anti-Patterns (do NOT repeat these)
Project rules that Codex does not see, because they live in CLAUDE.md or Claude memory:
- **Packages:** `devflow-core` is the library. `devflow` is binary-only, so `cargo test -p devflow --lib` verifies nothing; use `--bin devflow`.
- **Exact test names:** `cargo test <name> -- --exact` needs a module-qualified name. Assert `test result: ok. 1 passed` and a non-zero `filtered out`.
- **Checks:** full suite `scripts/check.sh all`; container parity `scripts/check-in-container.sh`.
- **Commits:** `gsd-tools query commit` silently drops absolute `--files` paths, so pass repo-relative paths. Never use `--no-verify`.
- **Linter:** the pre-commit plan linter is fixed on this branch, but not yet in the main checkout.
- **Waves:** each plan's `wave` must equal its depth in the `depends_on` graph.

## Infrastructure State
- **Processes:** no background processes and no async jobs.
- **Uncommitted:** `?? .planning/research/.cache/`, a research-store cache. Leave it untracked.
- **Review artifacts (outside the repo):** `~/.cache/devflow-review/phase48-plans-9915c56/` holds the raw reviews, `prompt.md`, `run-review.sh`, and a snapshot of `9915c56`.
- **AGENTS.md:** if a root `AGENTS.md` reappears, do not commit it. The phase 47 retrospective removed it.
- **GSD:** version 1.14.0 (`@opengsd/gsd-core`). Codex sees the GSD skills under `~/.agents/skills`.

## Pre-Execution Critique Required
- **Design artifact:** the `48-NN-PLAN.md` set.
- **Critique focus:**
  - Is every destructive recovery path performed while holding the per-phase lock?
  - Is every text extraction from plans fence-aware?
  - Does `doctor` stay write-free?
  - Do temp-file writes survive pid reuse?
- **Gate:** do not begin execution until the `--reviews` replan and a second adversarial review are complete.

<context>
The findings cluster in three areas:
1. **Destructive recovery without the per-phase lock.** In 48-14, `recover --clean` checks the lock, then deletes gate files without holding it (CX-1), and it clears state unconditionally (PI-S2).
2. **Fence handling in 48-02's checkpoint parser.** Its fence closer is too loose (CX-2), and element extraction ignores fences (CX-3). One fence-tracking pass shared by declaration detection and element extraction would address both.
3. **Doctor's read-only guarantee.** `lock::holder` deletes empty lock files, and 48-13 builds `holder_status` on top of it (PI-1).

These are suggestions from the review's fix directions, not decisions.
</context>

<next_action>
Start with: confirm you are in `/var/home/denniyahh/Github/devflow/.worktrees/phase-48` on `feature/phase-48`, read `48-REVIEWS.md`, and ask the operator how `recover --clean` should behave while a process holds the per-phase lock. Then run `$gsd-plan-phase 48 --reviews`.
</next_action>
