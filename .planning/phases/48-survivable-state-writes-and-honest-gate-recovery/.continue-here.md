---
context: phase
phase: 48-survivable-state-writes-and-honest-gate-recovery
task: 38
total_tasks: 38
status: paused
last_updated: 2026-09-19T20:25:44.397Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: finding A is undecided. Fixing it reverses Phase 28 D-03 (unconditional agent auto-decide; reversibility "costly"). The operator chose "fix A" before being told that, and the re-asked question is unanswered. Do not infer an answer; ask.
- [ ] CONSTRAINT: fix classes, not instances. T-48-15-01 took three fix rounds (76867df → 25aa87c → d3beb19) because each patch closed the audited instance and missed the class, and 25aa87c regressed T-48-07-02. Before writing a fix, state the class rule it enforces. Before closing, run a fresh auditor that re-checks every previously closed threat sharing the code.
- [ ] CONSTRAINT: check recorded decisions before framing a finding as a defect. The external-review prompt described the checkpoint set as "human-approved", so both reviewers reported behaviour that D-03 mandates as a high-severity defect. Search `.planning/**/*-CONTEXT.md` for the relevant D-xx first.
- [ ] CONSTRAINT: work only in `/var/home/denniyahh/Github/devflow/.worktrees/phase-48` on `feature/phase-48`. Codex run from the main checkout once planned against a stale `.planning/` and produced stray commits.
- [ ] CONSTRAINT: executor commits fail in this linked worktree (GSD #4799). Verify output independently, then make normal hook-enabled commits. Never use `--no-verify`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Instance-patching a security class | Stage guard, then mode guard, then truncation: each missed the next path; one regressed a closed threat | blocking | Write the class rule first; test the previously closed threats' cases alongside the new one; fresh auditor plus external pass before closing |
| Unverified reviewer framing | The review prompt's own claim ("human-approved set") produced false high findings | blocking | Ground review prompts in recorded D-xx decisions, and cite them |
| False-green exact test | `cargo test NAME -- --exact` exits 0 when NAME matches nothing | blocking | Module-qualify the name and require `test result: ok. 1 passed; 0 failed` |
| Shell word-splitting in probes | zsh does not split an unquoted `"$B"`, so `--bin devflow` passed as one argument gave 6 runs with no result line; a `$src` control read 0 on both sides | advisory | Run multi-argument probes under `bash -c`; keep a count or control that must differ |
| Stale GSD state surfaces | `phase.complete` and state writers have left STATE/ROADMAP partially updated | blocking | After `phase.complete`, audit every surface; log any GSD-output repair in `~/.config/agents/gsd-manual-fix-incidents.md` |

<current_state>
Phase 48 is paused inside the security gate, after operator-directed fixes and an external adversarial review. HEAD is `4cdd72b`. Source last changed in `d3beb19`, which passed the full suite: `scripts/check.sh all` OK, 1,387 passed and 0 failed across 33 result lines. The pinned-container parity check has not run, and nothing is pushed.

`48-SECURITY.md` (frontmatter `threats_open: 2`, `status: draft`) records every round:
- Round 1: 45 of 49 closed.
- Round 2 closed T-48-16-03.
- Round 3 closed T-48-16-02, and found T-48-07-02 regressed by `25aa87c` plus the Auto truncation path.
- Round 4 on `d3beb19` did not run: the auditor died on an HTTP 429 session limit and produced no verdict. It is not a pass.

codex and agy tried to break the `Pending`-only rule and could not; both traced every Code entry point. T-48-15-01 and T-48-07-02 therefore stay **open** until the round-4 audit runs. T-48-09-02 (low) stays open and non-blocking.

The external review found six issues (A–F) outside the plan-time threat list; dispositions are in `48-SECURITY.md`:
- **A (high):** undecided (see blockers).
- **B, C, E, F:** decided fix, not yet implemented.
- **D (medium):** decided backlog, not yet filed.

The worktree is clean apart from two unrelated untracked paths; preserve both: `.planning/research/.cache/` and `.planning/user/errors/`.
</current_state>

<completed_work>

- Plans 48-01..48-17 implemented and summarized (unchanged this session).
- Security choice recorded: verify all threats.
- Security audit round 1 → `48-SECURITY.md` (`6942aca`).
- `76867df`:
  - Code-stage recording guard.
  - `stop` passes `stop_via_gate`'s stage and `HolderStatus` into `persist_stopped_state`.
  - A recycled pid still blocking the lock fails with the D-05 repair line.
- `25aa87c`: waiter claimed only when the observed `Live` pid is the one blocking the lock. The Supervise mode guard it also added was superseded.
- `d3beb19`:
  - Class rule: `record_checkpoint_set_for_code_evaluation` records only from `Pending`, at Code, in every mode.
  - Tests `approving_a_non_code_…`, `approving_a_supervise_code_…`, `approving_an_auto_code_…never_widens_a_recorded_set` and `a_supervise_code_refusal_approval_records_the_pre_agent_set_once`.
  - Every new test failed on its defect before its fix.
- External review (codex + agy, `git archive` snapshot of `d3beb19`, snapshot unmodified) and its findings recorded (`4cdd72b`).
</completed_work>

<remaining_work>

1. **Finding A:** get the operator's disposition. Options offered: backlog with D (recommended), amend D-03 for Supervise now, or accept under D-03.
   - If amended, a ready RED test is saved at `/tmp/claude-1000/-var-home-denniyahh-Github-devflow--worktrees-phase-48/217c05ac-06db-485d-ae71-e2db225480c8/scratchpad/finding-A-supervise-test.patch`.
   - That test is a `Mode::Supervise` copy of `pipeline_launch::tests::unchanged_recorded_checkpoint_still_auto_decides`. It must see no `checkpoint_auto_decided` event and a Code gate.
   - The likely fix site is the `(Some(session_id), true)` arm near `pipeline_launch.rs:1723`: add an Auto-only guard and route Supervise to the gate with an augmented reason.
2. **B, C, E, F as one class**, TDD: no recovery command states an outcome unless a confirmed live holder will act on it.
   - **B:** `answered_gate_contention_message`'s `_` arm (`commands.rs:2106`) returns `Ok`. It should return `Err`, with neutral text plus `no_waiter_repair`. Update `stop_e2e::stop_at_a_gate_with_an_unconfirmable_holder_claims_no_waiter`, `commands::tests::stop_at_a_non_ship_gate_with_an_unconfirmable_holder_claims_neither` and the unit test `answered_gate_contention_claims_a_waiter_only_for_the_observed_live_holder`.
   - **C:** in `stop_via_gate`'s `AlreadyResponded` arm (`commands.rs:1934-1941`), read the existing response. Only an Abort means "ending". For an approval or loop-back, don't report the gate as answered by `stop`; the natural route is falling through to `stop_via_lock`, and that's a design choice to make explicitly.
   - **E:** `gate_respond`'s outcome text (`commands.rs:1441-1452`) must be conditional unless the holder is `Live`.
   - **F:** `gate sweep`'s "reaped phase" line (`commands.rs:1590`) must say what was written and that nothing confirmed pickup.
3. **D → ROADMAP 999.x backlog entry:** an auth (`human-action`) gate can be auto-decided when a `blocking-human` checkpoint is also declared. The cause is the `phase_has_blocking_human_checkpoint` vs `unapproved` predicate divergence. It predates Phase 48.
4. **Round-4 security audit:** a fresh `gsd-security-auditor` in State A over `d3beb19` plus the B/C/E/F commit. Re-check T-48-15-01, T-48-07-02, T-48-07-01/03/04, T-48-15-02/03, T-48-16-02/03 and T-48-12-02/03.
5. **Focused codex + agy pass** on the new diff, using the operator's lanes and invocation (see `external-review` skill).
   - Use a slim snapshot: `crates`, the Cargo files and the Phase 48 CONTEXT/PLAN/SECURITY files; about 3.9 MB, which let agy finish in 8 minutes.
   - Verify every finding in source before relaying it.
6. **Close `48-SECURITY.md`:** statuses, `threats_open: 0`, `status: verified`, sign-off.
7. **Remaining tail gates:** `/gsd-validate-phase 48`, then the MemPalace capture, then goal verification and `phase.complete`.
</remaining_work>

<decisions_made>

- Security gate: verify all threats (operator).
- Fix the blocking threats inline, test-first (operator).
- Record checkpoint approval only from `Pending`; once recorded, only the re-scan gate widens the set (operator option 1).
- Adversarial review lanes are codex and agy, not opencode (operator).
- Fix B, C, E, F; backlog D (operator). The "fix A" part of that answer is **suspended**, because A reverses D-03.
- The TDD commit-shape override (`48-TDD-OVERRIDE.md`) is unchanged and waives nothing else.
</decisions_made>

<blockers>
- Human decision: finding A's disposition. Open, and must not be inferred.
- Technical: GSD #4799 executor commits. Workaround: the orchestrator commits.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable state.
1. This file.
1. `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-SECURITY.md` — register, audit rounds and findings A–F with their dispositions.
1. `.planning/milestones/v2.0.0-phases/28-close-the-checkpoint-answer-return-path/28-CONTEXT.md:50-64` — D-03, the constraint behind finding A.
1. `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md:110-139` — D-05, the honest gate-recovery rules behind B, C, E and F.
1. `git show 76867df 25aa87c d3beb19` — the security fixes and their tests.
1. `CLAUDE.md` (repo) — canonical test commands. The package is `devflow` (binary-only: `--bin devflow`) and `devflow-core` (`--lib`).
1. `.planning/METHODOLOGY.md` (if present).

## Infrastructure State
- Repository: `/var/home/denniyahh/Github/devflow/.worktrees/phase-48`, branch `feature/phase-48`, HEAD `4cdd72b` (after this WIP commit, one ahead).
- Background processes: none. The codex and agy review lanes finished; no subagents are running.
- Async jobs: none.
- `verify:post` hooks: MemPalace (skip on error), Nyquist (halt), security (halt), UI review (skip on error).
- Pre-push runs the pinned container gate, which takes several minutes, so push in the background.

<context>
The Phase 48 implementation is complete, but its security gate is not closed.
- **The checkpoint rule:** the recorded set is the pre-agent baseline, and only the re-scan gate (which names every unapproved plan file) may widen it. Two external reviewers could not break this. The owed round-4 audit still has to confirm it.
- **The `stop`/gate messaging work:** unfinished. One class of dishonest outcome messages remains (B, C, E, F).
- **Finding A:** a product decision (D-03), not a Phase 48 defect.

Treat every earlier "passed" claim as needing a re-run in the new session.
</context>

<next_action>
Start with: acknowledge the constraints, ask the operator for finding A's disposition, then TDD-fix B/C/E/F as one class.
</next_action>
