---
context: phase
phase: 48-survivable-state-writes-and-honest-gate-recovery
task: 0
total_tasks: 5
status: paused
last_updated: 2026-09-22T23:08:15.359Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: instance-not-class — this phase's fixes kept closing one instance of a defect class; two external review rounds each found the siblings (WR-02..04, then R-1..R-8). State the class invariant and enumerate every site before calling a fix done.
- [ ] CONSTRAINT: a reply is not evidence — a nested `claude -p` asked to report its shell answered from its system prompt without running anything. Any probe must write an artifact the orchestrator reads, with a control that must come out the other way.
- [ ] CONSTRAINT: executors cannot commit from this worktree (#4799 workaround) — the orchestrator commits, RED and GREEN separately (`test(48-NN)` then `feat(48-NN)`), because the TDD audit greps those prefixes.
- [ ] CONSTRAINT: `cargo test --exact` with a name matching nothing exits 0 — require `1 passed`.
- [ ] CONSTRAINT: this phase belongs only in `/var/home/denniyahh/Github/devflow/.worktrees/phase-48` on `feature/phase-48`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Instance-only fixing | Fixing the flagged site and missing its siblings (recurred in every review round this session) | blocking | Write the class invariant first; enumerate all writers/messages/deleters; revert-mutate each fix and require a failing test |
| Proxy evidence | Accepting output that could have been produced without the check running (fabricated nested-model reply; a Debug-string assertion) | blocking | Artifact-based probes with a discriminating negative control; direct field assertions |
| zsh parsing traps | `==word`, `"$REF:path"`, nested `bash -c` quoting broke commands | advisory | Tool shell is now bash (verify `echo $0`); put non-trivial commands in a `bash -n`-checked script |
| Stale index.lock | A 0-byte `.git/worktrees/phase-48/index.lock` blocked commits twice (15:13, 16:07), both during container-gate runs; not reproduced on the third run | advisory | Check for a holder process before removing; do not assume lazygit |

<current_state>
All 17 original plans executed; review fixes landed through a692468 (container gate green, 1437/0). Goal verification: gaps_found 6/7 (02a704f). API break declared (39017ad). Gap plans 48-18 and 48-19 are planned and checker-PASSED after two revisions, committed at 2902c62 with backlog 999.138. Nothing of 48-18/48-19 has been executed. HEAD 2902c62; tree clean except the four deliberate untracked planning paths.
</current_state>

<completed_work>
- WR-01..WR-04 fixed test-first: b1f1f3f/c5f40fc (monitor), abb35e8/4d119fb (recover); WR-05 → 999.136 (722e9ef).
- External review round (codex, agy, DeepSeek) → records 7ef2ad8; R-2/R-7/R-8 → 999.137.
- R-5, R-4, R-3, R-1, R-6 fixed by codex terra xhigh, test-first: e507306..a692468.
- Goal verification 02a704f; API break 39017ad; gap plans 2902c62.
- Outside the repo: external-review skill (both copies), hermes config, pi default, GSD reviewer-instances now use `deepseek-flash`; `CLAUDE_CODE_SHELL=/usr/bin/bash` set in ~/.claude/settings.json (verified by an artifact probe).
</completed_work>

<remaining_work>
1. Execute 48-18 (test-only, 3 tasks) then 48-19 (TDD, 2 tasks); orchestrator commits each step.
2. Full `scripts/check-in-container.sh all` at the new HEAD — capture exit code AND the `==> check.sh: all OK` line.
3. Re-run goal verification; human_verification item 1 closes from 48-19-SUMMARY's "Operator rulings (2026-09-22)".
4. Phase completion (update_roadmap / phase.complete), MemPalace capture (verify:post hook); `git diff .planning/STATE.md` after every state write — `state.planned-phase` recomputes progress to 17% (incident logged twice).
</remaining_work>

<decisions_made>
- Criterion 4: add start-driven interrupted + live arms; keep the advance arms, renamed truthfully.
- Criterion 2: `start` refuses when the recorded agent or monitor pid is alive (existing helpers); recycled-pid false refusal accepted; no-state carve-out explicitly approved by the operator; agent-exit→advance window stays 999.136; `stop` in a lock-free run is 999.138.
- 48-19 GREEN commits as `feat(48-19)` (TDD audit); 48-TDD-OVERRIDE.md unchanged.
- Next release is major (3.0.0): WorkflowError/RecoverError are #[non_exhaustive].
- No hermes gateway restart needed (config reloads on file change; verify after the Monday 2026-09-28 09:00 self-review job).
</decisions_made>

<blockers>
- None. Executor choice for 48-18/48-19 (codex vs gsd-executor) was not asked this time — confirm with the operator before executing.
</blockers>

## Required Reading (in order)
1. `48-VERIFICATION.md` — the gaps and the human_verification item.
2. `48-18-PLAN.md`, `48-19-PLAN.md` — the work.
3. `.planning/reviews/48-code-review-2026-09-22/review-fix-round/VERIFIED.md` — the finding dispositions.

## Infrastructure State
- hermes gateway (pid 172115) runs cronjobs; intentional; not restarted.
- Codex 5h window was at 72% at 21:25Z (resets_at 1790118810).

<context>
The session's rhythm that worked: RED test first (verify it fails on the intended assertion), commit; GREEN, re-verify myself, commit; revert-mutate to prove the tests bite; full container gate; external review once, then stop. The class, not the instance.
</context>

<next_action>
Start with: confirm `echo $0` shows bash; ask the operator which executor to use for 48-18/48-19; then execute 48-18.
</next_action>
