---
context: phase
phase: 48-survivable-state-writes-and-honest-gate-recovery
task: 23
total_tasks: 38
status: paused
last_updated: 2026-09-16T22:59:33.710Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These constraints were discovered through actual failures. Acknowledge each before resuming.
> Both are STILL LIVE: Plans 48-08 .. 48-17 include further `type: tdd` plans and further
> `crates/**` commits, so neither constraint was discharged by 48-07 completing.

- [ ] CONSTRAINT: **Use operator-accepted Cargo RED evidence.** GSD's `tdd-red-evidence` parser accepts Node TAP only and falsely calls Cargo's exact assertion failures `INVALID_RED/zero_tests_discovered`. Do not fabricate TAP, do not weaken the tests, and do not re-block on that parser. Record the command, exit code, target assertion and filtered count as JSON evidence files, the way 48-07 did.
- [ ] CONSTRAINT: **Commit sequential phase work through the approved orchestrator workaround.** Upstream GSD [#4799](https://github.com/open-gsd/gsd-core/issues/4799) applies an agent-worktree branch guard to `feature/phase-48` despite `workflow.use_worktrees=false`. After independent verification, the operator authorized normal-hook orchestrator commits for the exact plan scope. Hooks stay ON; never `--no-verify`.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Fabricating unsupported RED evidence | Cargo tests fail on the intended assertion, but the TDD tool only parses Node TAP. | blocking | Treat the recorded command, exit code, target assertion and filtered count as the approved RED evidence. |
| Re-blocking sequential phase commits | The executor guard rejects `feature/phase-48` solely because `.git` is a linked-worktree file. | blocking | Independently verify each scope, then use the operator-approved normal-hook orchestrator commit. |
| Trusting GSD tracking writes | State writers produce stale plan pointers and wrong status cells. | advisory | `git diff .planning/STATE.md` after every state write; repair by hand and append to `~/.config/agents/gsd-manual-fix-incidents.md` before committing. |
| Letting a test park at a real gate | A parked gate polls `gate_timeout_secs()` — the three-day production default. It HANGS rather than fails, and a hang cannot be told apart from a wedged harness. | blocking | Pre-write an abort gate response in every test that could reach a gate, including controls where it is inert. |

<current_state>
- Phase 48 is executing on `feature/phase-48`; **10/17 plans have summaries**
  (48-01 .. 48-09 and 48-15).
- **CHKPT-02 is COMPLETE** across 48-07 (record + compare + park) and 48-15 (the three gate
  responses + refusal-gate recording). Both plans' gates print `gate_rc=0`, clippy is 0, and both
  suites are fully green: `devflow-core` 792/0, `devflow` 375/0.
- Commits: `6e746b8`/`9714e79` (48-07), `c7d5971` (48-04 doc_check fix), `1b56e5f`/`5d55e57`
  (48-15), `942c85c` (48-08), `15370f7` (48-09).
- Preserve untracked `.planning/research/.cache/` and `.planning/user/errors/`.
</current_state>

<completed_work>

- 48-01 through 48-07 are complete and tracked (7/17).
- 48-07 delivered `CheckpointApproval` / `ApprovedCheckpoint` / `unapproved` on `State`,
  recording at a passing Code preflight from the execution root, and the re-scan gate at the
  resume decision before `relaunch_checkpoint_session`.
- The gate's RESPONSE behaviour is deliberately absent — plan 48-15 owns it.

</completed_work>

<remaining_work>

1. Continue the wave order with **48-10, 48-11 and 48-17** next, then 48-12 → 48-13 → 48-16 →
   48-14. 48-15 was taken out of order (its only dependency was 48-07) and is done.
2. TEST-01 is now COMPLETE for this phase's scope (48-09). That is NOT a claim that 999.38 is
   resolved: the parallel PATH race in `commands.rs` that 999.38 also describes is out of scope
   and still reproduces intermittently.

</remaining_work>

<decisions_made>

- The operator accepted truthful Cargo RED evidence; the TAP-only checker is a tooling limitation.
- The operator authorized phase-wide orchestrator-managed normal-hook commits while #4799 is open.
- `recover --clean` under a held/contended phase lock deletes neither state nor gate files; preserve
  this Plan 48-14 contract.
- 48-07: element TEXT is stored, never a hash; `Recorded(vec![])` stays distinct from `Unrecorded`.

</decisions_made>

<blockers>

- GSD's TAP-only TDD evidence parser cannot validate Rust Cargo RED output. Recorded in
  `.planning/STATE.md` under `## Blockers` and kept there deliberately: further `type: tdd` plans
  remain in this phase, so the entry is live, not stale.

</blockers>

## Known FLAKY tests — do not chase them as regressions

Both suites go green on this branch: `devflow-core` 792/0 and `devflow` 371/0, with
`cargo clippy --workspace --all-targets -- -D warnings` at 0. Two tests nonetheless fail
intermittently under parallel load, and both pass in isolation:

- `devflow-core`: `agent::tests::discover_stray_devflow_processes_rejects_the_999_47_false_positive_shape`
  — `exec visibility timed out before the fixture became discoverable`. The known `/proc`
  visibility race; 3/3 in isolation, observed failing once under the full suite.
- `devflow`: `commands::tests::stray_process_finding::doctor_finds_a_real_stray_and_never_signals_it_across_two_runs`
  — the known PATH race (999.38). Fails often under the parallel suite; its failure output shows real
  host tool versions, which is the leak signature.

RESOLVED: `doc_check::source_devflow_env_vars_and_subcommands_are_documented` was genuinely red
since `47425fa` (plan 48-04) and is now fixed — see the `fix(48-04)` commit on this branch.

## Infrastructure State

- Branch/worktree: `feature/phase-48` at `/var/home/denniyahh/Github/devflow/.worktrees/phase-48`.
- `workflow.use_worktrees=false`; execution is sequential in this phase worktree.
- No background process or async job is active.
- Nothing is pushed: `feature/phase-48` is ahead of its remote by this session's commits.

<next_action>
Execute Plans 48-10, 48-11 and 48-17 (same wave). Three constraints now enforced by the build,
all added this session:

1. `clippy.toml` DENIES `std::env::set_var`/`remove_var`. Scope it to the child `Command`
   (`cmd.env`), or add `#[expect(clippy::disallowed_methods, reason = "...")]` on the smallest
   enclosing test-only item — and keep the attribute's INNER args at or under 70 characters or
   rustfmt re-wraps it. Never `allow`.
2. Every `DEVFLOW_*` test env var is read through a `const`; inlining one into a `var_os` call
   re-breaks `doc_check`.
3. `pipeline_outcomes.rs`'s three shared fixture helpers refuse to run outside a child — convert
   any new caller along with them.
</next_action>
