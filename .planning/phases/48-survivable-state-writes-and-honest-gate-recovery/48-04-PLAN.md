---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 04
type: execute
wave: 2
depends_on: ["48-03"]
files_modified:
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/test_support.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-08, no test in `pipeline_outcomes.rs` mutates process-global `PATH`: every former `PATH`-replacing block runs in a child through `devflow_core::test_support::run_test_in_child` with `PATH` set on the child `Command` only"
    - "Per D-08, every converted child run is guarded by `assert_child_ran_exactly_one_passing_test` with the module-qualified name (`pipeline_outcomes::tests::…`)"
    - "Per D-08, the child `PATH` is a directory built from the existing agent-free helpers (a stub agent plus `git`/`sh` symlinks where needed), never the developer's `PATH` and never removed"
    - "No test is deleted: the file's `#[test]` count is at least its count at c0fb193"
    - "Contract C-7 (CR-01) still holds: `pipeline_outcomes::tests::stage_failure_retry_cleans_stale_response` passes"
    - "Per D-08, `NeutralPath` (its struct, inherent impl and `Drop` impl) is deleted from `crates/devflow-cli/src/test_support.rs` in Task 2, because this file held its only callers: `test_support.rs` is a `#[cfg(test)]` module of a binary crate, so an uncalled `pub(crate)` item fails this plan's own `clippy -D warnings` gate on `dead_code` (the module doc names the trap). Any other helper there whose only callers were in this file is deleted too; comments and docs that still name the deleted type are 48-09 Task 2's"
    - "Edge (TEST-01 ordering, explicit): child results are asserted on counts and the `test NAME ... ok` line, never on output order"
  prohibitions:
    - statement: "No production code in `pipeline_outcomes.rs` changes (D-08)"
      status: resolved
      verification: explicit
    - statement: "The 4 non-PATH environment mutations in this file are left for the lint plan (48-09), not converted here (D-09 scope)"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-cli/src/pipeline_outcomes.rs
      provides: "PATH-isolated outcome tests"
      contains: "run_test_in_child"
  key_links:
    - from: crates/devflow-cli/src/pipeline_outcomes.rs tests
      to: crates/devflow-core/src/test_support.rs::run_test_in_child
      via: "devflow-core `test-support` feature on the cli dev-dependency"
      pattern: "devflow_core::test_support"
---

<!-- plan-criteria-allow: R4 - The c0fb193 baseline object is explicitly checked before its content is read; the scanner also joins root setup to later pipelines. -->

<objective>
TEST-01 — ROADMAP alias 999.38 (999.80 folded in, D-10). Convert every process-global `PATH` block in
`crates/devflow-cli/src/pipeline_outcomes.rs` (54 set/restore lines, measured 2026-09-14) to a guarded child run
(D-08). The 13 abort-fixture-only tests in this file are 48-08's scope; a fixture test that also has a `PATH` block
is converted here.

Purpose: 999.38's original sites include `pipeline_outcomes.rs:879/1132/1246` and eight trailing-restore regions
whose mid-region panic deletes the neutral dir and poisons `ENV_MUTEX`. Output: converted tests.

Contracts touched (P3): C-7 (pinned by `stage_failure_retry_cleans_stale_response`); C-25 `env_lock` premise (the
remaining in-process users of `env_lock` in this file are recorded in SUMMARY for 48-09).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md
@.planning/codebase/TESTING.md

48-03 created `devflow_core::test_support::{run_test_in_child, in_child_test, assert_child_ran_exactly_one_passing_test, CHILD_TEST_ENV}`.
PATTERNS.md § 20 shows the before shape (`pipeline_launch.rs:3612-3628`) and the after shape (`test_support.rs:752-803`).
</context>

<!-- `NeutralPath` is a negative check on comment-stripped code, and Task 2's action must name the type it deletes; the two helper-call literals are positive counts. -->
<!-- planner-discipline-allow: NeutralPath -->
<!-- planner-discipline-allow: run_test_in_child( -->
<!-- planner-discipline-allow: assert_child_ran_exactly_one_passing_test( -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — convert the first PATH-replacing test in the file through the core helper, end to end</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - crates/devflow-cli/src/test_support.rs lines 320-530 (`NeutralPath`, `agent_free_git_only_path_dir`, `stub_agent_binary`, `agent_free_dir_with_agent_stub`, `prepend_path`) and 745-805 (parent/child test shape)
    - crates/devflow-core/src/test_support.rs (the 48-03 helper and its docs)
    - crates/devflow-cli/src/pipeline_outcomes.rs: locate blocks with `rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" crates/devflow-cli/src/pipeline_outcomes.rs`, then read the first enclosing test in full
  </read_first>
  <action>
Per D-08: before editing, run `cargo test -p devflow --bin devflow pipeline_outcomes::tests::` once and record the passed count in SUMMARY as the module baseline.

Convert the first test (file order) that replaces `PATH`: add `const NAME: &str = "pipeline_outcomes::tests::<fn name>";`. The parent half builds the fixture directory the test needs (the repo root under a `tempfile::tempdir()`, and the `PATH` directory from the existing agent-free helper that matches what the old block put on `PATH`), then calls `devflow_core::test_support::run_test_in_child(NAME, path_dir, &[(<var>, <value>)])` passing any fixture path the child needs as an extra variable (for example `DEVFLOW_TEST_FIXTURE_ROOT`), then `assert_child_ran_exactly_one_passing_test(&out, NAME)`. The child half (`if devflow_core::test_support::in_child_test(NAME)`) reads the fixture path with `std::env::var_os`, runs the original body and assertions, and returns. Remove the old `env_lock()`/`NeutralPath`/set-restore lines from that test. If the old block used `prepend_path` (developer `PATH` plus a stub), replace it with a directory containing the stub and symlinks to exactly the binaries the body spawns — never the developer's `PATH`.

Name the converted test in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; f=crates/devflow-cli/src/pipeline_outcomes.rs; pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); echo "path_mutation_lines=$pm"; [ "$pm" -le 51 ] || rc=1; rc_sites=$(grep -v "^[[:space:]]*//" "$f" | grep -c "run_test_in_child(" || true); g=$(grep -v "^[[:space:]]*//" "$f" | grep -c "assert_child_ran_exactly_one_passing_test(" || true); echo "child_runs=$rc_sites child_guards=$g"; [ "$rc_sites" -ge 1 ] && [ "$g" -ge "$rc_sites" ] || rc=1; cargo test -p devflow --bin devflow pipeline_outcomes::tests:: > "$L/m.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; path_mutation_lines above 51 (no block converted; 54 before), child_runs=0, child_guards below child_runs, module_exit non-zero, or no `ok. N passed; 0 failed` line</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14: comment-stripped `PATH` mutation lines in this file read 54 before the change, so `path_mutation_lines -le 51` discriminates a real conversion
    - The converted test's module-qualified `--exact` run prints `test result: ok. 1 passed` and a non-zero `filtered out` (record the command and output in SUMMARY)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>One outcome test runs in a guarded child with a directory-only PATH; the module stays green.</done>
</task>

<task type="auto">
  <name>Task 2: Convert every remaining PATH block in pipeline_outcomes.rs and delete the guard type they used</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs, crates/devflow-cli/src/test_support.rs</files>
  <read_first>
    - crates/devflow-cli/src/pipeline_outcomes.rs every test enclosing a line found by `rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" crates/devflow-cli/src/pipeline_outcomes.rs`
    - The Task 1 conversion (as the template)
    - crates/devflow-cli/src/test_support.rs lines 1-14 (module doc: the `dead_code` trap) and 320-360 (the guard type's struct, inherent impl and `Drop` impl)
  </read_first>
  <action>
Per D-08, apply Task 1's shape to every remaining test that sets or removes `PATH`, including the trailing-statement restore regions 999.38 names and any abort-fixture test that also has a `PATH` block (D-10). Each gets its own module-qualified `NAME` constant and guard. A test with more than one `PATH` region becomes one child run whose child half performs all regions' work in order (the child `PATH` is fixed for the whole child; if two regions need different `PATH` values, split them into two child runs with distinct child-mode guards — never mutate `PATH` inside the child). Leave the non-PATH environment mutations untouched (48-09). Remove imports your changes leave unused.

Then, per D-08, delete `NeutralPath` (its struct, inherent `impl` and `Drop` impl) from `crates/devflow-cli/src/test_support.rs`. Its only callers were the tests this plan converts: 10 install sites, all in `pipeline_outcomes.rs`, on 2026-09-14. Confirm none is left with `rg -n "NeutralPath::" crates --glob "*.rs"`. An uncalled `pub(crate)` item in that `#[cfg(test)]` module of a binary crate fails this task's `clippy -D warnings` step on `dead_code`, so the deletion belongs here rather than in 48-09. Delete any other helper in that file whose only callers were in `pipeline_outcomes.rs` and are now gone, and name each deletion in SUMMARY. Do not touch `prepend_path`: its callers are in `pipeline_launch.rs`, `staleness.rs` and `preflight.rs`, and 48-06 deletes it. Leave `env_lock`'s doc comments that mention the deleted type for 48-09 Task 2, which rewrites them.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; git cat-file -e c0fb193^{commit} || { echo base_missing; exit 1; }; f=crates/devflow-cli/src/pipeline_outcomes.rs; pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); np=$(grep -v "^[[:space:]]*//" "$f" | grep -c "NeutralPath" || true); ts=crates/devflow-cli/src/test_support.rs; [ -f "$ts" ] || { echo test_support_missing; rc=1; }; nd=$(grep -v "^[[:space:]]*//" "$ts" | grep -c "struct NeutralPath" || true); nc=$(rg -n "NeutralPath::" crates --glob "*.rs" | rg -v -e "^[^:]+:[0-9]+:\s*//" | wc -l); echo "path_mutation_lines=$pm neutral_path_uses=$np neutral_path_struct_defs=$nd neutral_path_code_calls=$nc"; [ "$pm" -eq 0 ] && [ "$np" -eq 0 ] && [ "$nd" -eq 0 ] && [ "$nc" -eq 0 ] || rc=1; bt=$(git show "c0fb193:$f" | grep -c "#\[test\]"); nt=$(grep -c "#\[test\]" "$f"); echo "base_tests=$bt now_tests=$nt"; [ "$nt" -ge "$bt" ] || rc=1; r=$(grep -v "^[[:space:]]*//" "$f" | grep -c "run_test_in_child(" || true); g=$(grep -v "^[[:space:]]*//" "$f" | grep -c "assert_child_ran_exactly_one_passing_test(" || true); echo "child_runs=$r child_guards=$g"; [ "$r" -ge 10 ] && [ "$g" -ge "$r" ] || rc=1; cargo test -p devflow --bin devflow pipeline_outcomes::tests::stage_failure_retry_cleans_stale_response -- --exact > "$L/c7.log" 2>&1; ok7=$(grep -c "test result: ok. 1 passed" "$L/c7.log" || true); fo7=$(grep -oE "[1-9][0-9]* filtered out" "$L/c7.log" | head -1); echo "cr01_one_passed=$ok7 cr01_filtered=${fo7:-none}"; [ "$ok7" -eq 1 ] && [ -n "$fo7" ] || rc=1; cargo test -p devflow --bin devflow pipeline_outcomes::tests:: > "$L/m.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; cargo clippy -p devflow --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; path_mutation_lines or neutral_path_uses above 0, neutral_path_struct_defs above 0 (read 1 before: the guard type was not deleted) or neutral_path_code_calls above 0 (read 10 before: a caller remains), test_support_missing printed, now_tests below base_tests (a test was deleted), child_runs under 10 or child_guards below child_runs, cr01_one_passed not 1 or cr01_filtered=none, module run failing, or clippy_exit non-zero (including `dead_code` on a helper this plan left uncalled)</fails_when>
  </verify>
  <acceptance_criteria>
    - The module passed count after conversion is at least the Task 1 baseline (both numbers in SUMMARY)
    - No converted child half references `env_lock`, `NeutralPath` or `set_var("PATH"`
    - SUMMARY names every `test_support.rs` helper this task deleted
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>pipeline_outcomes.rs has zero process-global PATH mutations, every converted test is vacuity-guarded, and the guard type those tests used is deleted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo test process → spawned agent CLI | A test resolving a real agent through the developer's PATH runs it with real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-04-01 | Elevation of Privilege | converted outcome tests | medium | mitigate | Child PATH is an agent-free directory; `prepend_path` of the developer PATH is replaced |
| T-48-04-02 | Denial of Service | a conversion that silently runs nothing | medium | mitigate | Guard on every child; `#[test]` count ≥ base; module passed count ≥ baseline |
</threat_model>

<verification>
Both gates print `gate_rc=0`.
</verification>

<success_criteria>
`pipeline_outcomes.rs` tests no longer race concurrent git/sh spawns through PATH.
</success_criteria>

## Artifacts this phase produces

- No new production symbols. Test constants `NAME` inside each converted `pipeline_outcomes::tests` function.
- Deleted: `NeutralPath` (struct, inherent impl, `Drop` impl) in `crates/devflow-cli/src/test_support.rs`, plus any helper SUMMARY names as left without callers by this plan

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-04-SUMMARY.md` when done.
</output>
