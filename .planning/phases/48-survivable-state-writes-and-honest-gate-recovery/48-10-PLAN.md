---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 10
type: tdd
wave: 6
depends_on: ["48-09"]
files_modified:
  - crates/devflow-core/src/workflow.rs
  - crates/devflow-core/src/gates.rs
autonomous: true
requirements: [SURV-01, SURV-02]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Per D-02.4, `write_state_atomic` writes through a unique temp created with `File::create_new` and renames it over the state file; a failed rename removes the temp"
    - "Per R-7, unique temp names follow one shape, `.{target file name}.{pid}.{seq}.tmp`, shared by state and gate writes; `list_states` and `Gates::list_open` ignore a planted temp while a real file still lists (F-11)"
    - "Per R-7, `clear_state` removes orphan temps for its own phase only (a sibling phase's temp survives), and `Gates::cleanup` removes orphan temps for its own phase and stage only"
    - "Per D-04 and R-4, `Gates::respond` publishes the response by hard-linking a fully written unique temp; a second responder that got past the existence check receives `GateError::AlreadyResponded` and the first responder's bytes survive"
    - "Per R-4, `write_gate` and `ack` keep rename-overwrite: a leftover request is overwritten (C-17)"
    - "A filesystem without hard links surfaces a `GateError::Io` naming the failure — never a silent rename-overwrite fallback (planner note on R-4)"
    - "Edge (SURV-01 concurrency, explicit): a crash between temp write and publish leaves only a collectable temp, which the next clear/cleanup sweeps"
    - "Edge (SURV-01 precision, assumption): the per-process `AtomicU64` sequence cannot wrap in practice; no test"
  prohibitions:
    - statement: "`tempfile` is not used in production code (it is a dev-dependency only)"
      status: resolved
      verification: explicit
    - statement: "A unique temp name alone is not presented as the SURV-01 fix (D-01); the writer rule lands in 48-11/48-12"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/workflow.rs
      provides: "unique_temp_path, unique-temp write_state_atomic, clear_state temp sweep"
      contains: "fn unique_temp_path"
    - path: crates/devflow-core/src/gates.rs
      provides: "exclusive respond publish, unique-temp write_atomic, cleanup temp sweep"
      contains: "hard_link"
  key_links:
    - from: crates/devflow-core/src/gates.rs
      to: crates/devflow-core/src/workflow.rs::unique_temp_path
      via: "one shared temp-name shape and sweep pattern"
      pattern: "unique_temp_path"
---

<objective>
SURV-01 (D-02.4 backstop) and SURV-02's answer primitive (D-04): unique temp names for state and gate writes,
first-answer-wins response publication, and collectable orphans (R-4, R-7).

Purpose: today every writer of a phase shares one `.tmp` path (999.118), and `Gates::respond` checks existence then
renames, so an approval can silently replace a rejection. Output: core primitives and tests. Contracts touched (P3):
C-7 (CR-01 cleanup), C-15/C-16 (reap and first-writer-wins stay benign), C-17 (overwrite kept for requests).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md

RESEARCH Patterns 1-2, Finding F-11, Pitfall 4; PATTERNS.md § 1-2 (registry.rs:214-229 analog; `lock.rs:85-90` AlreadyExists match).
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — shared unique temp name through the state write, scanners ignore it, clear_state sweeps it</name>
  <files>crates/devflow-core/src/workflow.rs</files>
  <read_first>
    - crates/devflow-core/src/workflow.rs lines 1-40, 170-280 (`write_state_atomic`, `list_states`, `clear_state`)
    - crates/devflow-core/src/registry.rs lines 210-230; crates/devflow-core/src/lock.rs lines 76-122 and 250-285
  </read_first>
  <behavior>
    - `workflow::tests::state_write_leaves_no_temp_and_list_states_ignores_a_planted_temp`: after `save_state`, no `.state-NN.json.*.tmp` remains; a planted `.state-48.json.1.0.tmp` is not returned by `list_states`; the real state is (control)
    - `workflow::tests::clear_state_sweeps_orphan_temps_for_its_phase_only`: planted temps for phases 48 and 49; `clear_state(root, 48)` removes 48's and keeps 49's
  </behavior>
  <action>
Per D-02.4 and R-7: tests first. Add `pub(crate) fn unique_temp_path(target: &Path) -> PathBuf` returning `target.with_file_name(format!(".{name}.{pid}.{seq}.tmp"))` with a module `static TEMP_SEQ: AtomicU64`. `write_state_atomic` creates the temp with `std::fs::File::create_new`, writes, drops, renames, and removes the temp if rename fails. `clear_state` also removes entries whose name starts with `.{state file name}.` and ends with `.tmp`, collecting removal errors as warnings the way `remove_stale_locks` does rather than failing the clear.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in state_write_leaves_no_temp_and_list_states_ignores_a_planted_temp clear_state_sweeps_orphan_temps_for_its_phase_only; do cargo test -p devflow-core --lib "workflow::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; w=$(sed -n "/fn write_state_atomic/,/^}/p" crates/devflow-core/src/workflow.rs); fx=$(printf "%s\n" "$w" | grep -c "with_extension(\"tmp\")" || true); cn=$(printf "%s\n" "$w" | grep -c "create_new" || true); echo "write_state_fixed_tmp=$fx write_state_create_new=$cn"; [ "$fx" -eq 0 ] && [ "$cn" -ge 1 ] || rc=1; cargo test -p devflow-core --lib workflow::tests:: > "$L/m.log" 2>&1; e=$?; echo "workflow_module_exit=$e"; [ "$e" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; either test one_passed=0 or filtered=none, write_state_fixed_tmp above 0, write_state_create_new=0, or the workflow module run failing</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14: `with_extension("tmp")` occurs inside `write_state_atomic` today, so `write_state_fixed_tmp` moves from 1 to 0
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>State writes use collectable unique temps that no scanner mistakes for state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: First answer wins — hard-link publish in Gates::respond</name>
  <files>crates/devflow-core/src/gates.rs</files>
  <read_first>
    - crates/devflow-core/src/gates.rs lines 80-100 (GateError), 140-170 (`list_open`), 180-310 (`respond`, `reap`, `write_gate`, `poll_response`, `cleanup`, `ack`), 350-370 (`write_atomic`), and `respond_refuses_to_clobber_unconsumed_response` near :552
  </read_first>
  <behavior>
    - `gates::tests::second_publisher_past_the_existence_check_gets_already_responded`: call the publish helper directly twice with `approved: false` then `approved: true` → second returns `AlreadyResponded`; the response file parses with `approved == false`; no temp remains
    - `gates::tests::single_responder_still_publishes` (control): one `respond` → response file present and parsed
    - `gates::tests::list_open_ignores_a_planted_temp`: a planted `.48-code.json.1.0.tmp` is not listed; the real request is
    - `gates::tests::write_gate_still_overwrites_a_leftover_request` (R-4, C-17)
  </behavior>
  <action>
Per D-04 and R-4: tests first. Change `write_atomic` to use `crate::workflow::unique_temp_path` (rename-overwrite unchanged, for `write_gate` and `ack`). Extract `fn publish_response_exclusive(path: &Path, contents: &str, phase: PhaseId, stage: Stage) -> Result<(), GateError>`: write the full contents to a unique temp via `File::create_new`, then `std::fs::hard_link(&tmp, path)`, remove the temp in every arm, and map `ErrorKind::AlreadyExists` to `GateError::AlreadyResponded { phase, stage }`; any other error returns `GateError::Io` (document in the doc comment that filesystems without hard links fail loudly here by design). `respond` keeps its request-exists and response-exists fast paths and calls the helper.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in second_publisher_past_the_existence_check_gets_already_responded single_responder_still_publishes list_open_ignores_a_planted_temp write_gate_still_overwrites_a_leftover_request respond_refuses_to_clobber_unconsumed_response; do cargo test -p devflow-core --lib "gates::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; fx=$(grep -v "^[[:space:]]*//" crates/devflow-core/src/gates.rs | grep -c "with_extension(\"tmp\")" || true); hl=$(grep -v "^[[:space:]]*//" crates/devflow-core/src/gates.rs | grep -c "hard_link(" || true); echo "gates_fixed_tmp=$fx gates_hard_link=$hl"; [ "$fx" -eq 0 ] && [ "$hl" -ge 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none, gates_fixed_tmp above 0 (read 1 before), or gates_hard_link=0</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY records that `second_publisher_past_the_existence_check_gets_already_responded` failed before the helper existed (a rename-based publish lets the second writer replace the first)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>A second answer can never silently replace the first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gates::cleanup sweeps its own orphan temps</name>
  <files>crates/devflow-core/src/gates.rs</files>
  <read_first>
    - crates/devflow-core/src/gates.rs `cleanup` (as left by Task 2)
  </read_first>
  <behavior>
    - `gates::tests::cleanup_removes_orphan_temps_for_its_gate_only`: temps planted for `48-code` request/response/ack and for `48-validate` → `cleanup(root, 48, Code)` removes the three `48-code` temps and keeps the `48-validate` one; request/response/ack removal unchanged; a second `cleanup` is still Ok (idempotent)
  </behavior>
  <action>
Per R-7: test first, then extend `Gates::cleanup` to remove entries in the gates directory whose name starts with `.` plus the request, response or ack file name plus `.` and ends with `.tmp`. Keep it idempotent.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; cargo test -p devflow-core --lib gates::tests::cleanup_removes_orphan_temps_for_its_gate_only -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "cleanup_one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; cargo test -p devflow-core --lib > "$L/core.log" 2>&1; ce=$?; echo "core_lib_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; cleanup test one_passed=0 or filtered=none, core_lib_exit non-zero, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Gate cleanup leaves no collectable orphans for its gate and never touches another gate's.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| concurrent CLI verbs / monitor → `.devflow/` files | Several processes write the same gate and state paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-10-01 | Tampering | approval silently replaces a rejection | high | mitigate | Hard-link exclusive publish; two-publisher test |
| T-48-10-02 | Tampering | pre-planted symlink at a predictable temp path | medium | mitigate | Unpredictable name created with `create_new` (never follows) |
| T-48-10-03 | Denial of Service | torn response skipped forever | medium | mitigate | Response becomes visible only as a complete linked file |
| T-48-10-04 | Denial of Service | hard links unsupported | low | accept | Loud `GateError::Io`; no silent fallback |
</threat_model>

<verification>
All three gates print `gate_rc=0`.
</verification>

<success_criteria>
State and gate writes cannot collide on a temp path, and gate answers are first-writer-wins atomically.
</success_criteria>

## Artifacts this phase produces

- `devflow_core::workflow::unique_temp_path(target: &Path) -> PathBuf` (`pub(crate)`) and its temp shape `.{file}.{pid}.{seq}.tmp`
- `devflow_core::gates::publish_response_exclusive` (private helper)
- Tests named in the three `<behavior>` blocks

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-10-SUMMARY.md` when done.
</output>
