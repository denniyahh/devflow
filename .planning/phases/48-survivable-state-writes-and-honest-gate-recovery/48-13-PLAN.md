---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 13
type: tdd
wave: 9
depends_on: ["48-12"]
files_modified:
  - crates/devflow-core/src/lock.rs
autonomous: true
requirements: [SURV-02]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-05, holder_status returns NoHolder for absent, corrupt, or dead locks; Live only when a running pid has the recorded start time; Recycled when the recorded and observed start times differ; and Unconfirmable only when the holder is alive but identity cannot be checked"
    - "Per PI-1, holder_status is read-only: empty or corrupt lock files remain on disk after inspection, so doctor may collect facts without mutating phase artifacts"
    - "HolderStatus::may_be_waiting is true only for Live and Unconfirmable; SURV-02 callers can distinguish a recycled pid from an unverifiable live holder"
  artifacts:
    - path: crates/devflow-core/src/lock.rs
      provides: "HolderStatus, HolderStatus::may_be_waiting, and read-only holder_status"
      contains: "pub fn holder_status"
  key_links:
    - from: crates/devflow-core/src/lock.rs
      to: crates/devflow-core/src/agent.rs
      via: "holder_status uses process_start_time equality to separate Live from Recycled"
      pattern: "process_start_time"
---

<objective>
Provide the read-only lock-holder classification that SURV-02 command messaging and doctor reporting require.

Purpose: process identity is security-sensitive and must be tested independently before CLI verbs begin to rely on it.
Output: holder-status primitive and deterministic controls.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-12-SUMMARY.md

Read lock.rs holder_identity and holder, plus agent.rs agent_running and process_start_time. Do not call holder from the
new status function: holder removes an empty lock file and is not safe for doctor.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — read-only NoHolder and Live classifications</name>
  <files>crates/devflow-core/src/lock.rs</files>
  <read_first>
    - crates/devflow-core/src/lock.rs acquire, holder_identity, holder, and existing lock-file tests
    - crates/devflow-core/src/agent.rs agent_running and process_start_time
  </read_first>
  <behavior>
    - RED: lock::tests::holder_status_reports_no_holder_and_preserves_empty_lock_file — absent, corrupt, dead-pid, and empty locks classify NoHolder; the empty file remains after inspection
    - lock::tests::holder_status_reports_live_for_an_identity_matched_lock — an in-process acquired lock classifies Live with the process pid
  </behavior>
  <action>
Per D-05 and PI-1, write the tests first. Add the public HolderStatus enum and a private read-only lock-record reader
used only by holder_status. Do not reuse holder. Classify no lock, unreadable or corrupt pid, and a pid that is not
running as NoHolder. For a two-line lock record whose pid is running and whose recorded start time equals
agent::process_start_time, return Live. Keep the file unchanged in every inspection path.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in holder_status_reports_no_holder_and_preserves_empty_lock_file holder_status_reports_live_for_an_identity_matched_lock; do cargo test -p devflow-core --lib "lock::tests::$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=$fo"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; empty_remove=$(sed -n "/pub fn holder_status/,/^}/p" crates/devflow-core/src/lock.rs | grep -c "remove_file" || true); echo "holder_status_remove_file_calls=$empty_remove"; [ "$empty_remove" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; either exact test fails or matches no test, or holder_status_remove_file_calls is above zero</fails_when>
  </verify>
  <done>Doctor-safe status inspection distinguishes a live identity-matched holder from every no-holder form.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recycled and unconfirmable identities have distinct safe classifications</name>
  <files>crates/devflow-core/src/lock.rs</files>
  <read_first>
    - Task 1 HolderStatus and lock record parser
    - stop_via_lock identity handling in crates/devflow-cli/src/commands.rs, read only
  </read_first>
  <behavior>
    - lock::tests::holder_status_distinguishes_recycled_and_unconfirmable — same live pid with a mismatched recorded start time is Recycled; legacy one-line lock and unavailable observed start time are Unconfirmable
    - lock::tests::holder_status_may_be_waiting_only_for_live_and_unconfirmable — the predicate is true for exactly those two variants
  </behavior>
  <action>
Per D-05, write the tests first. Extend holder_status so a start-time mismatch is Recycled, never Live or
Unconfirmable. A legacy one-line record, unreadable holder record after a live pid has been identified, or a running
pid whose start time is unavailable is Unconfirmable. Implement may_be_waiting as an exhaustive match that returns true
only for Live and Unconfirmable. Document why command response code can treat Recycled as no waiter while destructive
recovery still relies on lock acquisition.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in holder_status_distinguishes_recycled_and_unconfirmable holder_status_may_be_waiting_only_for_live_and_unconfirmable; do cargo test -p devflow-core --lib "lock::tests::$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=$fo"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; cargo test -p devflow-core --lib lock::tests:: > "$L/module.log" 2>&1; me=$?; mok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/module.log" || true); echo "lock_module_exit=$me module_ok_lines=$mok"; [ "$me" -eq 0 ] && [ "$mok" -eq 1 ] || rc=1; cargo clippy -p devflow-core --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ce=$?; echo "clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; either identity test fails or matches no test, the lock module fails or reports no passed tests, or clippy_exit is non-zero</fails_when>
  </verify>
  <done>Every holder-status variant has a discriminating test and the waiter predicate cannot misclassify a recycled pid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lock-file identity to command decisions | A stale or recycled pid can look alive without a start-time comparison |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-13-01 | Spoofing | recycled pid classified live | high | mitigate | Start-time mismatch test and Recycled status |
| T-48-13-02 | Tampering | doctor mutates a lock while observing it | medium | mitigate | Read-only parser and empty-lock preservation control |
| T-48-13-03 | Denial of Service | legacy waiter treated as absent | medium | mitigate | Unconfirmable remains may_be_waiting |
</threat_model>

<verification>
Both gates print gate_rc=0; their status controls establish classification behavior, not the lifetime of a holder after a later CLI response.
</verification>

<success_criteria>
The CLI can rely on a tested, read-only holder-status primitive without treating recycled pids as waiters.
</success_criteria>

<output>
Create .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-13-SUMMARY.md when done.
</output>
