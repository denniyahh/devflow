---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 13
type: tdd
wave: 8
depends_on: ["48-12"]
files_modified:
  - crates/devflow-core/src/lock.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/tests/gate_wedge_e2e.rs
autonomous: true
requirements: [SURV-02]

estimate:
  tokens: 70000
  raw_tokens: 140000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`lock::holder_status(project_root, phase)` returns `NoHolder` (no lock, an unreadable or corrupt pid, or a holder pid not running), `Live { pid }` (running, and its `/proc` start time equals the start time the lock recorded), `Recycled { pid }` (running, but its `/proc` start time differs from the recorded one: the pid now belongs to another process, the case `stop_via_lock` already refuses to signal as recycled), or `Unconfirmable { pid }` (running, but per D-05 identity cannot be checked: a legacy single-line lock with no start time, a holder that cannot be read back, or no `/proc` start time for the pid)"
    - "`HolderStatus::may_be_waiting()` is true only for `Live` and `Unconfirmable`. Every answer writer here, and 48-14's `recover --clean` and doctor finding, decide on it, so `NoHolder` and `Recycled` are both no live waiter (D-05, PATTERNS.md § B)"
    - "Per D-05.1-3 and D-05.6, `devflow gate approve` / `gate reject` with no live waiter (`NoHolder` or `Recycled`) at a non-Ship gate write no answer file, exit non-zero, say nothing is waiting, and name `devflow resume --phase N` and `devflow recover --clean --phase N`; they never prescribe `rm -f`"
    - "Per D-05.2, with no live waiter at a Ship gate the answer is written (C-6) and the message names `devflow ship --phase N`"
    - "With `Live`, the answer is written and the message names the waiting pid; with `Unconfirmable` (the pid is alive and holds the lock, so refusing could strand a legacy-lock waiter), the answer is written and the message says a process holds the lock but cannot be confirmed, claiming neither a waiter nor its absence; with `Recycled`, the no-waiter message also names the pid and says it is not the process that took the lock"
    - "Per R-6, `stop_via_gate` and `gate sweep` apply the same rule before `Gates::reap`: a no-waiter non-Ship gate (`NoHolder` or `Recycled`) is left alone with the shared message (sweep counts it `left_alone`); `Gates::reap` stays the sweep's only write path (C-15)"
    - "Per D-05 and D-05.1, `stop` claims a waiter only when the `holder_status` that `stop_via_gate` computed before answering is `Live`: `stop_via_gate` hands that status back to `stop`, and the post-answer line 48-12 added is chosen from it, never from lock contention alone. With `Unconfirmable` it says the pid holds the lock but cannot be confirmed as waiting, claiming neither a waiter nor its absence; with `NoHolder` or `Recycled` it claims no waiter"
    - "Per D-05.1, neither the CLI verbs nor `stop` print the old poller claims; every repair line comes from one function shared with doctor (48-14)"
    - "#200 criterion 4 wedge arm: a `devflow start --mode auto` parked at its Define preflight gate and then SIGKILLed leaves a gate that `devflow gate reject N <root> --note 'abort: #200 reproduction'` reports honestly, with no `NN-define.response.json` written"
    - "#200 criterion 4 self-resolving arm (negative control): the same start left running consumes the identical `gate reject` command and exits 0 with state cleared, because a note containing `abort` resolves to `GateAction::Abort` (gates.rs:70-80) while any other note loops back and re-parks the child; the observed answer-to-pickup interval is printed and recorded as a measure of poll-backoff position, not of the wedge (F-10). If both arms wedge, the harness is broken"
    - "Edge (SURV-02 empty, explicit): `gate reject` with no open gate still returns the existing no-open-gate error"
  prohibitions:
    - statement: "No answer file is written at a non-Ship gate when `holder_status` is `NoHolder` or `Recycled` (D-05.3)"
      status: resolved
      verification: explicit
    - statement: "Waiter presence is never decided from `state.monitor_pid` (T-23-51)"
      status: resolved
      verification: explicit
    - statement: "A start-time mismatch is never classified `Unconfirmable` or `Live`; `Unconfirmable` is limited to a missing recorded start time, an unreadable holder, or no `/proc` start time (D-05)"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/lock.rs
      provides: "HolderStatus (NoHolder, Live, Recycled, Unconfirmable), HolderStatus::may_be_waiting, holder_status"
      contains: "pub fn holder_status"
    - path: crates/devflow-cli/src/commands.rs
      provides: "no_waiter_repair, waiter-aware gate_respond / stop_via_gate / gate_sweep"
      contains: "fn no_waiter_repair"
    - path: crates/devflow-cli/tests/gate_wedge_e2e.rs
      provides: "#200 wedge arm and self-resolving control"
  key_links:
    - from: crates/devflow-cli/src/commands.rs gate_respond / stop_via_gate / gate_sweep
      to: crates/devflow-core/src/lock.rs::holder_status
      via: "checked before any Gates::respond / Gates::reap"
      pattern: "holder_status"
    - from: crates/devflow-cli/src/commands.rs stop_via_gate
      to: crates/devflow-cli/src/commands.rs stop's post-answer state write (added by 48-12)
      via: "the HolderStatus computed before answering, handed back to stop in place of a bare answered flag"
      pattern: "HolderStatus"
---

<objective>
SURV-02 — honest gate answers (D-05.1-3, D-05.6, R-6) and the #200 reproduction for criterion 4. Every answer writer
checks for a live waiter; with none it says so and names the repair that works for that gate.

Purpose: `gate reject` and `stop` assert a poller without checking, and `resume` is not a general repair (D-05's
correction). Output: core holder status, shared repair function, waiter-aware verbs, e2e test. Depends on 48-12, after
which every gate waiter holds the per-phase lock.

Contracts touched (P3): C-4, C-6 (`stop_is_idempotent_against_an_already_answered_gate`), C-15 (`gate_sweep_e2e.rs`
live-waiter reap), C-16 (first-writer-wins stays benign).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-12-SUMMARY.md

RESEARCH Pattern 7, Findings F-6 and F-10, Pitfalls 7-8; PATTERNS.md § 12-13, § 21. GitHub #200 is summarised in
REQUIREMENTS.md SURV-02.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — #200 both arms end to end through a waiter-aware `gate reject`</name>
  <files>crates/devflow-core/src/lock.rs, crates/devflow-cli/src/commands.rs, crates/devflow-cli/tests/gate_wedge_e2e.rs</files>
  <read_first>
    - crates/devflow-core/src/lock.rs lines 170-215 (`holder_identity`, `holder`) and crates/devflow-core/src/agent.rs lines 30-50, 195-220
    - crates/devflow-cli/src/commands.rs lines 1381-1430 (`gate_respond`) and 1846-1921 (`stop_via_lock` identity matrix)
    - crates/devflow-cli/tests/gate_sweep_e2e.rs lines 1-90 and 270-365; crates/devflow-cli/tests/stop_e2e.rs lines 130-220
  </read_first>
  <behavior>
    - `lock::tests::holder_status_distinguishes_no_holder_live_recycled_and_unconfirmable`: no lock → `NoHolder`; in-process `acquire` → `Live { pid: own pid }`; a lock naming a dead pid → `NoHolder`; a lock in `acquire`'s two-line format naming the test's own pid with a start time one greater than its `/proc` start time → `Recycled { pid: own pid }`; a legacy single-line lock naming the test's own pid → `Unconfirmable { pid: own pid }`; `may_be_waiting()` is true for exactly the `Live` and `Unconfirmable` results
    - e2e `wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly`: start parks at `.devflow/gates/NN-define.json`, is SIGKILLed and reaped; `devflow gate reject N <root> --note "abort: #200 reproduction"` exits non-zero; output contains `nothing is waiting`, `devflow resume --phase N`, `devflow recover --clean --phase N`; output does not contain `rm -f`; `NN-define.response.json` does not exist
    - e2e `self_resolving_arm_live_start_consumes_the_rejection`: start parks; the identical `devflow gate reject N <root> --note "abort: #200 reproduction"` exits 0; the start child exits 0 within `e2e_child_timeout()` and `.devflow/state-NN.json` is gone; the test prints `pickup_ms=<n>`
  </behavior>
  <action>
Per D-05 and criterion 4: tests first. The e2e fixture follows RESEARCH Pattern 7: a repo with no `.planning/config.json`, `devflow start --phase N --mode auto <root>` as a child with `PATH` (stub `claude` plus `git`/`sh` symlinks) and `DEVFLOW_GATE_TIMEOUT_SECS` set on the `Command` only. This file must signal its `start` child (unlike `gate_sweep_e2e.rs`); always reap it.

Both arms run the identical answer command, `devflow gate reject N <root> --note "abort: #200 reproduction"`, so the SIGKILL is the only variable between them. `--note` is required (`Reject`'s `note: String`, main.rs:428-431), and the note must contain `abort`: any other note makes `GateAction::from_response` return `LoopBack(Code)` (gates.rs:70-80), `run_preflight`'s `LoopBack` arm cleans up and relaunches the stage (preflight.rs:1378-1383), preflight refuses again and fires a fresh gate (bounded by `MAX_PREFLIGHT_RETRIES` = 3, mode.rs:80), and the live child re-parks instead of exiting, so the control arm would time out for a fixture reason. With the abort note the child reaches `pipeline_gate::abort`, which clears state (pipeline_gate.rs:461-477). In the wedge arm the note is never read: `gate_respond` refuses before writing.

Core: add `pub enum HolderStatus { NoHolder, Live { pid: u32 }, Recycled { pid: u32 }, Unconfirmable { pid: u32 } }` with `pub fn may_be_waiting(&self) -> bool` (true for `Live` and `Unconfirmable` only), and `pub fn holder_status(project_root: &Path, phase: PhaseId) -> HolderStatus` composing `holder`, `holder_identity`, `agent::agent_running` and `agent::process_start_time` in the order `stop_via_lock` uses. No lock, a corrupt pid, or a pid not running → `NoHolder`. A recorded start time and `process_start_time(pid)` equal to it → `Live`. A recorded start time and `process_start_time(pid)` returning a different value → `Recycled`: per D-05 the unconfirmable case is non-Linux or a legacy lock only, and `stop_via_lock` already reads a mismatch as a recycled pid. A recorded start time with `process_start_time(pid)` returning `None`, a legacy lock without a start time, or a holder that cannot be read back → `Unconfirmable`. Do not split these cases with `agent::is_same_process`: it returns false both for a mismatch and for a missing `/proc` entry, which must classify differently.

CLI: add `pub(crate) fn no_waiter_repair(phase: PhaseId, stage: Stage) -> String` returning `devflow ship --phase {phase}` for `Stage::Ship` and `devflow resume --phase {phase}` (relaunches the stage) or `devflow recover --clean --phase {phase}` (ends the phase) otherwise. In `gate_respond`, after stage resolution and before `Gates::respond`, decide on `holder_status`: no live waiter (`!may_be_waiting()`) + non-Ship → print `nothing is waiting on phase {phase}'s {stage} gate — no live process holds the per-phase lock, so no answer was written. Repair: {no_waiter_repair}` (for `Recycled { pid }`, say instead that the lock names pid {pid}, which is not the process that took it) and return `CliError::Message`; no live waiter + Ship → respond, then print that nothing is waiting and name the ship repair; `Live { pid }` → respond and print that the waiting process (pid) reads it on its next poll; `Unconfirmable { pid }` → respond and print that pid holds the lock but cannot be confirmed as waiting. Replace the old poller sentence entirely.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; cargo test -p devflow-core --lib lock::tests::holder_status_distinguishes_no_holder_live_recycled_and_unconfirmable -- --exact > "$L/c.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/c.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/c.log" | head -1); echo "holder_status_one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; for t in wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly self_resolving_arm_live_start_consumes_the_rejection; do cargo test -p devflow --test gate_wedge_e2e "$t" -- --exact --nocapture > "$L/$t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; pk=$(grep -oE "pickup_ms=[0-9]+" "$L/self_resolving_arm_live_start_consumes_the_rejection.log" | head -1); echo "observed_${pk:-pickup_ms=missing}"; [ -n "$pk" ] || rc=1; [ -f crates/devflow-cli/src/commands.rs ] || { echo commands_rs_missing; rc=1; }; old=$(rg -c -F "once the waiting monitor polls it" crates/devflow-cli/src/commands.rs || echo 0); echo "old_poller_claim=$old"; [ "$old" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; holder_status or either e2e arm one_passed=0 or filtered=none, pickup_ms missing from the self-resolving arm's output, commands_rs_missing printed, or old_poller_claim above 0 (read 1 before)</fails_when>
  </verify>
  <acceptance_criteria>
    - Both arms pass in the same run; if the wedge arm passed while the self-resolving arm timed out, the gate fails (harness broken, per CONTEXT). Before treating that timeout as a production defect, confirm both arms pass a note containing `abort`: a note without it re-parks the live child by design (see action)
    - SUMMARY records the printed `pickup_ms` and states it measures where the answer landed in the 1 s-doubling poll schedule, not wedge timing; "~40s" is not cited
    - SUMMARY records that `.devflow/lock-NN` now exists after the killed start (it names a dead pid) — #200's "no lock file is created" no longer holds
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>#200 reproduces on demand in both directions and `gate reject` tells the truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: stop_via_gate and gate sweep follow the same rule (R-6)</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs lines 1442-1535 (`gate_sweep` reap loop and counters) and 1806-1845 (`stop_via_gate`)
    - crates/devflow-cli/src/commands.rs gate tests near 4620-4800 (`gate_respond_auto_resolves_single_open_gate`, `gate_sweep_*`)
    - crates/devflow-cli/src/commands.rs `stop` and `persist_stopped_state` as 48-12 Task 2 left them (the `Contended`-after-answer arm and the answered flag `stop` passes to it); 48-12-SUMMARY.md for that arm's exact waiter sentence
  </read_first>
  <behavior>
    - `commands::tests::stop_via_gate_with_no_waiter_at_a_non_ship_gate_writes_nothing`
    - `commands::tests::stop_via_gate_with_a_live_waiter_writes_the_rejection` (holder: in-process lock)
    - `commands::tests::gate_sweep_leaves_a_no_waiter_non_ship_gate_alone`
    - `commands::tests::gate_approve_with_no_waiter_at_the_ship_gate_writes_and_names_ship`
    - `commands::tests::no_waiter_repair_names_the_verb_for_each_gate`
    - `commands::tests::gate_respond_with_a_recycled_lock_pid_at_a_non_ship_gate_writes_nothing`: an open Validate gate and a lock in `acquire`'s two-line format naming the test's own pid with a start time one greater than its `/proc` start time → `gate_respond` returns an error saying nothing is waiting, and no response file exists (a recycled pid is not a waiter)
    - `commands::tests::stop_via_gate_with_a_recycled_holder_at_a_non_ship_gate_writes_nothing`: the same open Validate gate and recycled-pid lock → no response file exists and `stop_via_gate` reports that it answered nothing (fails a check that matches `NoHolder` instead of `!may_be_waiting()`)
    - `commands::tests::stop_at_a_ship_gate_with_a_recycled_holder_claims_no_waiter`: a saved state, an open Ship gate and the same recycled-pid lock → `stop` writes the Ship response (C-6) and returns `Err` whose text says phase state was not marked stopped and does not contain 48-12's waiter sentence; the state file bytes are unchanged. Red before this task: `stop` returns `Ok` with the waiter sentence
    - `commands::tests::stop_at_a_non_ship_gate_with_an_unconfirmable_holder_claims_neither`: a saved state, an open Validate gate and a legacy single-line lock naming the test's own pid → `stop` writes the rejection and returns `Err` whose text says the pid holds the lock but cannot be confirmed as waiting and that phase state was not marked stopped, without 48-12's waiter sentence; the state file bytes are unchanged
    - These `stop` tests name the test's own pid on purpose: `stop` sends no signal once its gate path answers, and `stop_via_lock` refuses to signal a recycled or legacy-lock pid, so no path can signal the test process. Do not add an in-process `Live` case through `stop` (a regression reaching `stop_via_lock` would signal the test runner); C-4's test in `stop_e2e.rs`, run whole in Task 3, covers `Live` through the real binary
  </behavior>
  <action>
Per R-6 and D-05: tests first. `stop_via_gate`: check `holder_status` before `Gates::reap`; no live waiter (`!may_be_waiting()`, so `NoHolder` or `Recycled`) + non-Ship → write nothing, print the shared message, return the not-answered result (`Ok(false)` today) so `stop` continues to its lock path and marks state; no live waiter + Ship → reap and print the ship repair (C-6); `Live`/`Unconfirmable` → reap as today with honest wording (never the old "will pick this up" sentence). `gate_sweep`: same check before `Gates::reap`; a no-waiter non-Ship gate increments `left_alone` and prints the shared message. Update existing unit tests that assert a write with no waiter so they hold `lock::acquire` in-process for the duration, and list each updated test in SUMMARY.

Per D-05 (its unconfirmable-identity clause) and D-05.1, `stop`'s post-answer lines come from the waiter status, not from lock contention: change `stop_via_gate` to hand back the `lock::HolderStatus` it computed whenever it answered the gate or found it already answered (for example `Result<Option<lock::HolderStatus>, CliError>`, with `None` meaning fall through to `stop_via_lock`), and pass that status to the post-answer state write 48-12 added in place of its answered flag. A `Contended` result after an answer then resolves by status: `Live` keeps 48-12's waiter sentence and `Ok(())` (C-4); `Unconfirmable` returns `CliError::Message` saying pid {pid} holds the per-phase lock but cannot be confirmed as waiting on the gate, that the rejection was written, and that phase state was not marked stopped, claiming neither a waiter nor its absence (non-zero because D-05 has commands that take the lock refuse while that pid is alive); `NoHolder` (a holder that appeared after the check) or `Recycled` returns 48-12's `Contended`-otherwise error, which for `Recycled` also says the pid is not the process that took the lock. The `AlreadyResponded` arm's line follows the same status: it claims pickup only for `Live`, and at a Ship gate with no live waiter it names `no_waiter_repair`'s ship verb. Record the new `stop_via_gate` signature in SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in stop_via_gate_with_no_waiter_at_a_non_ship_gate_writes_nothing stop_via_gate_with_a_live_waiter_writes_the_rejection gate_sweep_leaves_a_no_waiter_non_ship_gate_alone gate_approve_with_no_waiter_at_the_ship_gate_writes_and_names_ship no_waiter_repair_names_the_verb_for_each_gate gate_respond_with_a_recycled_lock_pid_at_a_non_ship_gate_writes_nothing stop_via_gate_with_a_recycled_holder_at_a_non_ship_gate_writes_nothing stop_at_a_ship_gate_with_a_recycled_holder_claims_no_waiter stop_at_a_non_ship_gate_with_an_unconfirmable_holder_claims_neither; do cargo test -p devflow --bin devflow "commands::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; [ -f crates/devflow-cli/src/commands.rs ] || { echo commands_rs_missing; rc=1; }; old=$(rg -c -F "will pick this up on its next poll" crates/devflow-cli/src/commands.rs || echo 0); echo "old_stop_claim=$old"; [ "$old" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none (if the tests module path differs, use the real module-qualified path and record it), or old_stop_claim above 0 (read 1 before)</fails_when>
  </verify>
  <acceptance_criteria>
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Every answer writer obeys the no-waiter rule, and `stop` claims a waiter only for `Live`.</done>
</task>

<task type="auto">
  <name>Task 3: Keep the live-waiter and Ship-gate contracts green end to end</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/tests/gate_sweep_e2e.rs and crates/devflow-cli/tests/stop_e2e.rs (read only; fix production code, not these tests, if they fail)
  </read_first>
  <action>
Run the gate. If a pre-existing e2e test fails, the cause is in this plan's production change: fix `commands.rs` so the live-waiter path still writes (C-4, C-15) and the Ship gate still writes with no waiter (C-6). Do not edit the existing e2e assertions; if one pins the removed poller wording, report it in SUMMARY and change only that string assertion to the new honest wording.

Gate scope: workspace clippy runs because this plan changes `devflow-core` (`lock.rs`), which every `devflow-cli` target compiles against; `stop_e2e.rs` and `gate_sweep_e2e.rs` run whole because they hold the only pins for C-4, C-6 and C-15, and `gate_wedge_e2e.rs` runs whole as this plan's new file. No named-test subset covers those contracts.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for f in gate_sweep_e2e stop_e2e gate_wedge_e2e; do cargo test -p devflow --test "$f" > "$L/$f.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/$f.log" || true); echo "${f}_exit=$e ${f}_ok=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; done; cargo test -p devflow --bin devflow commands::tests:: > "$L/u.log" 2>&1; ue=$?; echo "commands_unit_exit=$ue"; [ "$ue" -eq 0 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any e2e file or the commands unit module failing or reporting no passed tests, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff <commit before this plan's first commit>..HEAD -- crates/devflow-cli/tests/gate_sweep_e2e.rs crates/devflow-cli/tests/stop_e2e.rs` (base recorded in SUMMARY) is empty, or limited to wording assertions listed in SUMMARY
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Live waiters and the Ship gate behave as before; only no-waiter non-Ship gates change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/automation verbs → gate response files → a waiting process (or none) | An unread answer silently decides the next firing of that gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-13-01 | Tampering | stale answer decides a later run's gate (pipeline_gate.rs:463-464 hazard) | high | mitigate | No answer at a no-waiter non-Ship gate (D-05.3) |
| T-48-13-02 | Repudiation | CLI claims a waiter that does not exist | medium | mitigate | `holder_status` check; honest per-status messages |
| T-48-13-03 | Spoofing | recycled pid treated as waiter | medium | mitigate | Start-time identity; a mismatch is `Recycled`, which every answer writer and `recover --clean` treat as no waiter — never `Live` or `Unconfirmable` |
| T-48-13-04 | Denial of Service | legacy-lock waiter stranded | low | mitigate | `Unconfirmable` (legacy lock, unreadable holder, or no `/proc` start time) still writes, with a message that claims nothing |
</threat_model>

<verification>
All three gates print `gate_rc=0`.
</verification>

<success_criteria>
No verb asserts a waiter it did not confirm; #200 is pinned in both directions.
</success_criteria>

## Artifacts this phase produces

- `devflow_core::lock::HolderStatus { NoHolder, Live { pid }, Recycled { pid }, Unconfirmable { pid } }`, `HolderStatus::may_be_waiting(&self) -> bool`, `devflow_core::lock::holder_status(project_root: &Path, phase: PhaseId) -> HolderStatus`
- `commands::no_waiter_repair(phase: PhaseId, stage: Stage) -> String` (`pub(crate)`)
- New file `crates/devflow-cli/tests/gate_wedge_e2e.rs` with `wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly`, `self_resolving_arm_live_start_consumes_the_rejection`
- Unit tests named in Task 2's `<behavior>`

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-13-SUMMARY.md` when done.
</output>
