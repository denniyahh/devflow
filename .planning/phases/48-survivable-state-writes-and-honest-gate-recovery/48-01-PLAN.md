---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [SURV-01, SURV-02, CHKPT-01, CHKPT-02, TEST-01]

estimate:
  tokens: 20000
  raw_tokens: 40000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-11, ROADMAP Phase 48's **Requirements** line and its progress-table row read exactly SURV-01, SURV-02, CHKPT-01, CHKPT-02, TEST-01, and criteria 5-7 are labelled CHKPT-01 (999.125), CHKPT-02 (999.126), TEST-01 (999.38 + 999.80)"
    - "Per D-11, REQUIREMENTS.md has unchecked v3.0.0 entries and Traceability rows for CHKPT-01, CHKPT-02 and TEST-01, the coverage block says 13 total and 13 mapped, and the Future Requirements rows GATE-01 (#184) and GATE-02 (#170) are unchanged"
    - "Per D-01, criteria 1-2 say a second writer for the same phase is excluded or refused loudly and never silently overwrites; the fails-on-current-implementation test and the single-writer negative control stay in the criteria"
    - "Per D-05, criterion 3 says that with no live waiter the verbs say nothing is waiting, write no answer file at a non-Ship gate, and name `devflow ship --phase N` at a Ship gate or `devflow resume --phase N` / `devflow recover --clean --phase N` at any other gate — and still do not prescribe `rm -f`"
    - "Per D-10, the 999.38 and 999.80 backlog entries each carry a line saying they were worked together as TEST-01 in Phase 48"
    - "Per an operator decision made during plan-phase on 2026-09-14 (not a 48-CONTEXT D-xx decision and not a planner recommendation), criterion 4 keeps both directions — the wedge requires the foreground `start` to be interrupted, and a run left alone self-resolves — drops its fixed duration figure with no replacement number, says the self-resolving arm's observed answer-to-pickup interval is what the 48-13 reproduction records, and keeps the self-resolving arm as the negative control (if both arms wedge, the reproduction is measuring something else)"
    - "`roadmap.analyze` still exits 0 and reports a non-zero phase count after the edits"
  prohibitions:
    - statement: "Criterion 4 changes only as the 2026-09-14 operator decision specifies: no replacement duration is written into it (including research's unmeasured pickup expectation), and neither arm nor the negative-control sentence is dropped"
      status: resolved
      verification: explicit
    - statement: "No Deferred Idea from 48-CONTEXT.md is added to ROADMAP or REQUIREMENTS as Phase 48 scope"
      status: resolved
      verification: explicit
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Phase 48 requirement IDs, amended criteria 1-4, 999.38/999.80 worked-together record"
      contains: "CHKPT-01 (999.125)"
    - path: .planning/REQUIREMENTS.md
      provides: "CHKPT-01, CHKPT-02, TEST-01 entries and traceability rows"
      contains: "**TEST-01**"
  key_links:
    - from: .planning/ROADMAP.md
      to: .planning/REQUIREMENTS.md
      via: "the same five requirement IDs on the Phase 48 requirements line and in the traceability table"
      pattern: "CHKPT-02"
---

<objective>
Apply the planning-document changes the Phase 48 discussion made (D-01, D-05, D-10, D-11), plus one operator decision
made during plan-phase on 2026-09-14, as Phase 47 did its ROADMAP amendment in a plan task.

- CHKPT-01 — ROADMAP alias GATE-01, backlog 999.125.
- CHKPT-02 — ROADMAP alias GATE-02, backlog 999.126.
- TEST-01 — ROADMAP alias 999.38, with backlog 999.80 folded in (D-10).
- SURV-01 criteria 1-2 are amended by D-01; SURV-02 criterion 3 by D-05.
- Criterion 4 (the #200 reproduction) is amended by the operator decision made during plan-phase on 2026-09-14, not by a
  48-CONTEXT decision: it loses its fixed duration figure and names what the reproduction records instead.

Purpose: every later plan tests the corrected behaviour, not the original criterion text (CONTEXT specifics: the
roadmap's premises were wrong twice). Output: edited `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/REQUIREMENTS.md

ROADMAP.md is ~440KB. Read ONLY these windows (locate with `rg -n`): the v3.0.0 progress table row `| 48 |`,
`### Phase 48:` through `### Phase 49:`, `### Phase 999.38:` and `### Phase 999.80:` up to their next `### Phase`
heading. Use `Edit` with scoped replacements; never `Write` the whole file.
</context>

<!-- The literals below are positive checks (count 1 or more) in this plan's gates that the plan-structure validator reads as negative greps; each must appear in the ROADMAP or REQUIREMENTS text the actions write. -->
<!-- planner-discipline-allow: excluded or refused loudly -->
<!-- planner-discipline-allow: single-writer -->
<!-- planner-discipline-allow: worked together -->
<!-- planner-discipline-allow: TEST-01 -->
<!-- planner-discipline-allow: interrupted -->
<!-- planner-discipline-allow: self-resolv -->
<!-- planner-discipline-allow: negative control -->
<!-- planner-discipline-allow: answer-to-pickup interval -->
<!-- planner-discipline-allow: 48-13 -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — rename the three IDs end to end (ROADMAP line, row, labels; REQUIREMENTS entries and traceability) and prove the roadmap still parses</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (whole file, 203 lines — section order, entry format, Traceability table, Coverage block)
    - .planning/ROADMAP.md windows listed in context (progress row near line 30, Phase 48 section near lines 146-184)
    - .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md D-11 and the domain block's Phase Boundary items 3-5
  </read_first>
  <action>
Per D-11 (requirement IDs):

ROADMAP.md:
- Progress-table row for 48: replace the Requirements cell with `SURV-01, SURV-02, CHKPT-01, CHKPT-02, TEST-01`. Leave Wave and Status cells alone.
- Phase 48 section: the `**Requirements**:` line becomes `**Requirements**: SURV-01, SURV-02, CHKPT-01, CHKPT-02, TEST-01`.
- Criterion 5 label becomes `**CHKPT-01 (999.125):**`, criterion 6 label `**CHKPT-02 (999.126):**`, criterion 7 label `**TEST-01 (999.38 + 999.80):**`. Label text only; criterion bodies 5-7 unchanged.

REQUIREMENTS.md:
- After the `### Run Survivability (SURV)` block and before `### Live Verification (VERIFY)`, add `### Checkpoint Consistency (CHKPT)` with two unchecked entries in the file's existing format (`- [ ] **ID**: statement` plus an indented `*Traces to …*` provenance paragraph):
  - CHKPT-01: preflight and the resume route use one parsed checkpoint predicate; a marker outside a task-opening line neither blocks preflight nor arms resume, and a proper task-level `blocking-human` gate is human-only on both paths. Traces to 999.125. Provenance: Phase 47 D-13 found the whole-file substring match on the resume route versus the line-anchored preflight match (cite `verify.rs:131-137` and `:207-220` as "at time of filing").
  - CHKPT-02: a human-only checkpoint that is new or changed relative to the set DevFlow recorded at Code's preflight parks at a human gate before the resume decision can auto-decide it; a state file with no recorded set gates. Traces to 999.126.
- Then add `### Test Isolation (TEST)` with one entry:
  - TEST-01: tests that replace process-global `PATH` run in a child process with `PATH` set on the child `Command` only; tests kept from spawning a real agent only by an abort fixture note (999.80) run the same way; a clippy `disallowed-methods` lint rejects new process-global environment mutation. Traces to 999.38 with 999.80 folded in (D-10). Record as "Not established": the full-suite run pinned to two CPUs is a sanity check and does not prove the flakes cannot recur.
- Traceability table: add rows `| CHKPT-01 | Phase 48 — Survivable State Writes and Honest Gate Recovery | 2 | Pending |`, and the same shape for CHKPT-02 and TEST-01, directly after the SURV-02 row.
- Coverage block: `- v3.0.0 requirements: 13 total` and `- Mapped to phases: 13`.
- Do not touch the Future Requirements table: GATE-01 (#184) and GATE-02 (#170) keep their meaning (D-11).
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; sed -n "/^### Phase 48:/,/^### Phase 49:/p" .planning/ROADMAP.md > "$L/sec"; sl=$(wc -l < "$L/sec"); echo "phase48_section_lines=$sl"; [ "$sl" -ge 20 ] || rc=1; for w in GATE-01 GATE-02; do c=$(grep -c "$w" "$L/sec" || true); echo "section_${w}=$c"; [ "$c" -eq 0 ] || rc=1; done; rq=$(grep -c "^\*\*Requirements\*\*: SURV-01, SURV-02, CHKPT-01, CHKPT-02, TEST-01$" "$L/sec" || true); echo "requirements_line=$rq"; [ "$rq" -eq 1 ] || rc=1; row=$(grep -c "^| 48 | Survivable State Writes and Honest Gate Recovery | SURV-01, SURV-02, CHKPT-01, CHKPT-02, TEST-01 |" .planning/ROADMAP.md || true); echo "progress_row=$row"; [ "$row" -eq 1 ] || rc=1; for w in "CHKPT-01 (999.125)" "CHKPT-02 (999.126)" "TEST-01 (999.38 + 999.80)"; do c=$(grep -cF "**$w:**" "$L/sec" || true); echo "label[$w]=$c"; [ "$c" -eq 1 ] || rc=1; done; R=.planning/REQUIREMENTS.md; for id in CHKPT-01 CHKPT-02 TEST-01; do e=$(grep -c "^- \[ \] \*\*$id\*\*:" "$R" || true); t=$(grep -c "^| $id | Phase 48" "$R" || true); echo "${id}_entry=$e ${id}_trace=$t"; [ "$e" -eq 1 ] && [ "$t" -eq 1 ] || rc=1; done; g1=$(grep -c "^| GATE-01 (#184) |" "$R" || true); g2=$(grep -c "^| GATE-02 (#170) |" "$R" || true); echo "future_gate01=$g1 future_gate02=$g2"; [ "$g1" -eq 1 ] && [ "$g2" -eq 1 ] || rc=1; cv=$(grep -c "^- v3.0.0 requirements: 13 total$" "$R" || true); mp=$(grep -c "^- Mapped to phases: 13$" "$R" || true); echo "coverage_13=$cv mapped_13=$mp"; [ "$cv" -eq 1 ] && [ "$mp" -eq 1 ] || rc=1; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query roadmap.analyze > "$L/an.json" 2>&1; ae=$?; echo "roadmap_analyze_exit=$ae"; [ "$ae" -eq 0 ] || rc=1; pc=$(grep -c -E "\"phase_count\": *[1-9]" "$L/an.json" || true); echo "nonzero_phase_count=$pc"; [ "$pc" -ge 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; specifically section_GATE-01 or section_GATE-02 above 0, requirements_line/progress_row/any label/entry/trace count not 1, future_gate01 or future_gate02 not 1, coverage_13 or mapped_13 not 1, roadmap_analyze_exit non-zero, or nonzero_phase_count=0</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14 before the edit: the Phase 48 section contains GATE-01 twice, the gate's section_GATE-01 must therefore move from 2 to 0 — a gate that reads 0 before editing is reading the wrong window
    - `.planning/REQUIREMENTS.md` contains `### Checkpoint Consistency (CHKPT)` and `### Test Isolation (TEST)` headings, each exactly once
    - The Future Requirements table rows `| GATE-01 (#184) |` and `| GATE-02 (#170) |` are byte-identical to `git show HEAD:.planning/REQUIREMENTS.md`
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Every Phase 48 reference to the old IDs is renamed, REQUIREMENTS.md carries the three new requirements, and roadmap.analyze parses the file.</done>
</task>

<task type="auto">
  <name>Task 2: Reword criteria 1-3 (D-01, D-05) and criterion 4 (operator decision, 2026-09-14), and record 999.38 + 999.80 as worked together</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md Phase 48 section (as edited by Task 1)
    - .planning/ROADMAP.md `### Phase 999.38:` and `### Phase 999.80:` windows
    - .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md D-01, D-05, D-10, the review findings R-1 and R-6, and lines 142-149 (the #200 reproduction note explaining what the old criterion-4 figure measured)
  </read_first>
  <action>
Criteria 1-2 (per D-01): rewrite so they say a test drives a second writer for the same phase and fails against the current implementation because the second writer silently overwrites; after the fix the second writer is excluded or refused loudly (the literal phrase `excluded or refused loudly` must appear) and never silently overwrites; a single-writer control exercising the same path still round-trips. Keep the existing "Not established, and not claimed" sentence (no interleaving observed in the wild). Remove the sentence claiming both writers' updates become observable in the final state file. Keep the numbering `  1.` / `  2.`.

Criterion 3 (per D-05, with R-6): `devflow gate approve`, `devflow gate reject`, `devflow stop` and `devflow gate sweep` check for a live waiter (the per-phase lock holder, identity-confirmed) before writing an answer. With no live waiter they say nothing is waiting (use the phrase `no live waiter`), write no answer file at a non-Ship gate, and name the repair that works: `devflow ship --phase N` at a Ship gate; `devflow resume --phase N` or `devflow recover --clean --phase N` at any other gate. They still do not prescribe `rm -f`. Add one sentence recording D-05's correction: `resume` relaunches the saved stage and never reads a pending answer.

Per D-10: add one line directly under the `### Phase 999.38:` heading and one under `### Phase 999.80:`, each reading `**Phase 48 (2026-09-14):** worked together with 999.80 as TEST-01` (swap the other number in the 999.80 line). Do not rename either heading.

Criterion 4 (per the operator decision made during plan-phase on 2026-09-14; record it in SUMMARY with that provenance, not as a 48-CONTEXT D-xx decision and not as a planner recommendation): keep both directions — the wedge requires the foreground `start` to be **interrupted**, and a run left alone self-resolves. Delete the duration figure that currently follows "self-resolves in" and write no replacement number, including research's expected pickup range, which has not been measured. Add a sentence saying the self-resolving arm's observed answer-to-pickup interval is what the 48-13 reproduction records; keep the words "answer-to-pickup interval" together on one line and "48-13" on one line. Keep the negative-control sentence: the self-resolving arm is the negative control, and if both arms wedge the reproduction is measuring something else. Do not restate the removed figure anywhere in criterion 4, even as a correction; the reason it goes (it measured the poll backoff, not the wedge — 48-CONTEXT.md lines 147-148) belongs in SUMMARY, not in the criterion.

Leave criteria 5-7 bodies unchanged.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; sed -n "/^### Phase 48:/,/^### Phase 49:/p" .planning/ROADMAP.md > "$L/sec"; k=$(grep -c -E "^  [0-9]+\." "$L/sec" || true); echo "criteria_count=$k"; [ "$k" -eq 7 ] || rc=1; b=$(grep -c "both writers. updates are observable" "$L/sec" || true); echo "old_both_observable=$b"; [ "$b" -eq 0 ] || rc=1; sed -n "/^  1\./,/^  3\./p" "$L/sec" > "$L/c12"; x=$(grep -c "excluded or refused loudly" "$L/c12" || true); s=$(grep -c "single-writer" "$L/c12" || true); echo "c12_excluded=$x c12_single_writer=$s"; [ "$x" -ge 1 ] && [ "$s" -ge 1 ] || rc=1; sed -n "/^  3\./,/^  4\./p" "$L/sec" > "$L/c3"; for w in "devflow ship --phase N" "devflow resume --phase N" "devflow recover --clean --phase N" "no live waiter" "rm -f"; do c=$(grep -cF "$w" "$L/c3" || true); echo "c3[$w]=$c"; [ "$c" -ge 1 ] || rc=1; done; sed -n "/^  4\./,/^  5\./p" "$L/sec" | sed "\$d" > "$L/c4"; c4l=$(wc -l < "$L/c4"); f=$(grep -c "40s" "$L/c4" || true); ia=$(grep -ci "interrupted" "$L/c4" || true); sr=$(grep -ci "self-resolv" "$L/c4" || true); nc=$(grep -ci "negative control" "$L/c4" || true); pk=$(grep -c "answer-to-pickup interval" "$L/c4" || true); r13=$(grep -c "48-13" "$L/c4" || true); echo "c4_lines=$c4l c4_40s=$f c4_interrupted=$ia c4_self_resolving=$sr c4_negative_control=$nc c4_pickup_interval=$pk c4_names_48_13=$r13"; [ "$c4l" -ge 3 ] && [ "$f" -eq 0 ] && [ "$ia" -ge 1 ] && [ "$sr" -ge 1 ] && [ "$nc" -ge 1 ] && [ "$pk" -ge 1 ] && [ "$r13" -ge 1 ] || rc=1; for e in 999.38 999.80; do sed -n "/^### Phase $e:/,/^### Phase /{/^### Phase $e:/!{/^### Phase /q};p}" .planning/ROADMAP.md > "$L/w"; wl=$(wc -l < "$L/w"); wt=$(grep -c "worked together" "$L/w" || true); ti=$(grep -c "TEST-01" "$L/w" || true); echo "window_${e}_lines=$wl worked_together=$wt test01=$ti"; [ "$wl" -ge 5 ] && [ "$wt" -ge 1 ] && [ "$ti" -ge 1 ] || rc=1; done; node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" query roadmap.analyze > "$L/an.json" 2>&1; ae=$?; echo "roadmap_analyze_exit=$ae"; [ "$ae" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; specifically criteria_count not 7, old_both_observable above 0, any c12_/c3[...] count 0, c4_40s above 0 (the removed duration figure is still in criterion 4), c4_pickup_interval=0 or c4_names_48_13=0 (the recorded-interval sentence is missing), c4_interrupted=0, c4_self_resolving=0 or c4_negative_control=0 (an arm or the negative-control sentence was lost), c4_lines under 3 (wrong window), a 999.38/999.80 window under 5 lines or missing "worked together"/TEST-01, or roadmap_analyze_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14 before the edit: `old_both_observable` reads 1 and `criteria_count` reads 7, so the gate discriminates the rewrite rather than an empty window
    - Negative control for criterion 4, measured 2026-09-14 on the same window before the edit: `c4_lines=4 c4_40s=1 c4_interrupted=1 c4_self_resolving=2 c4_negative_control=1 c4_pickup_interval=0 c4_names_48_13=0`, so the check fails on the unedited criterion. Control, probed on a scratch copy during planning: a rewording that keeps both arms passes the criterion-4 checks, and the same rewording with the interrupted arm dropped reads `c4_interrupted=0` and fails
    - SUMMARY records the criterion 4 change as an operator decision made during plan-phase on 2026-09-14
    - `git diff <commit before this plan's first commit>..HEAD -- .planning/ROADMAP.md` (base recorded in SUMMARY) touches only the Phase 48 section, the progress-table row, and the two backlog lines (no hunk inside any other `### Phase` section)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Criteria 1-3 state the amended behaviour, criterion 4 names both arms and the recorded interval with no duration figure, and both backlog entries record the fold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → downstream agents | Later plans, the verifier and Phase 49 read these criteria as the acceptance contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-01-01 | Tampering | ROADMAP.md whole-file rewrite | medium | mitigate | Scoped `Edit` only; acceptance checks no hunk lands outside the four named regions |
| T-48-01-02 | Repudiation | Requirement ID reuse (GATE-01/02 already mean #184/#170) | low | mitigate | New IDs CHKPT-01/02, TEST-01; Future Requirements rows verified unchanged |
</threat_model>

<verification>
Both task gates print `gate_rc=0`; `roadmap.analyze` exits 0.
</verification>

<success_criteria>
The Phase 48 acceptance contract in ROADMAP.md and REQUIREMENTS.md matches D-01, D-05, D-10 and D-11, and criterion 4
matches the operator decision made during plan-phase on 2026-09-14.
</success_criteria>

## Artifacts this phase produces

- `.planning/REQUIREMENTS.md` headings `### Checkpoint Consistency (CHKPT)`, `### Test Isolation (TEST)`; requirement IDs `CHKPT-01`, `CHKPT-02`, `TEST-01`
- `.planning/ROADMAP.md` criterion labels `CHKPT-01 (999.125)`, `CHKPT-02 (999.126)`, `TEST-01 (999.38 + 999.80)`

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-01-SUMMARY.md` when done.
</output>
