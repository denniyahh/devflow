---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 07
type: tdd
wave: 4
depends_on: ["48-02", "48-05", "48-06"]
files_modified:
  - crates/devflow-core/src/state.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/src/pipeline_launch.rs
autonomous: true
requirements: [CHKPT-02]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-07, `State` carries `#[serde(default)] checkpoint_approval: CheckpointApproval` with variants `Unrecorded` (serde default: a state file from an older binary), `Pending` (set by `State::new`), and `Recorded(Vec<ApprovedCheckpoint>)`; `Recorded(vec![])` round-trips distinct from `Unrecorded`"
    - "Per D-07, a plan that gains a `blocking-human` checkpoint after Code's preflight parks at a human re-scan gate at the resume decision instead of arming auto-decide (red today: auto-decided)"
    - "Per D-07, a rewritten body of an approved checkpoint is new relative to the recorded set and parks at the re-scan gate"
    - "Per D-07, an `Unrecorded` or `Pending` approval parks at the re-scan gate wherever the resume decision would otherwise arm auto-decide with a human-only checkpoint present (tests: `unrecorded_checkpoint_set_parks_at_the_rescan_gate`, `pending_checkpoint_set_parks_at_the_rescan_gate`)"
    - "Control: an unchanged plan whose checkpoints equal the recorded set still auto-decides exactly as today (relaunch_checkpoint_session reached)"
    - "Derived from D-07 (not a new decision): `run_preflight` records the set at a Code evaluation only when approval is `Pending`; an `Unrecorded` approval is never recorded by a Code evaluation, so an upgraded run whose first Code evaluation predates this binary gates at a loop-back or relaunched Code evaluation too"
    - "Recording from `Pending` at a Code evaluation has its own test that fails when recording is absent or scans the project root instead of the execution root: `preflight::tests::code_preflight_records_a_pending_set_from_the_execution_root`"
    - "Per D-07 and F-7, recording happens only in `run_preflight` (never inside `unattended_planned_checkpoint_condition`, and never on `resume`'s cloned handoff candidate), and the preflight D-09 fixed signature `fn preflight_unattended_launch_check(project_root: &Path, state: &State) -> Result<(), String>` is unchanged"
    - "Edge (CHKPT-02 adjacency/ordering, explicit): comparison is a multiset difference keyed by `(plan_file, element)`, insensitive to order; two identical elements in two plans count separately"
    - "Edge (CHKPT-02 empty, explicit): `Recorded(vec![])` with no current human-only checkpoint → no gate; with one → gate"
    - "Edge (CHKPT-02 concurrency, explicit): the resume decision re-reads the plan files at decision time (a fresh snapshot); a plan edited between recording and the decision is compared as edited"
    - "Contract C-8 kept: the static declaration check stays first; the re-scan compare sits inside `if checkpoint_confirmed`, in the `(Some(session_id), true)` arm only, immediately before `relaunch_checkpoint_session`; the route still has no mode check (applies in every mode, D-07). The `(Some(_), false)` and `(None, _)` arms are unchanged: they never arm auto-decide and already fall through to the never-silent per-stage gate"
    - "Contract C-10: of the six `advance_*checkpoint*` tests, the two relaunch tests (`advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records`, `advance_with_worktree_declared_checkpoint_reads_the_execution_root`) build state with `State::new`, which now yields `Pending` and would park. Their fixtures set `checkpoint_approval` to `Recorded` of the plan's current human-only declarations (the state a Code preflight leaves) and keep every assertion. The four fall-through tests (`advance_without_declared_checkpoint_falls_through_to_generic_gate`, `advance_with_declared_checkpoint_but_unreported_gate_falls_through`, `advance_with_confirmed_checkpoint_and_no_session_id_falls_through`, `advance_at_checkpoint_resume_ceiling_falls_through_to_generic_gate`) are not edited"
    - "Contract C-12 (D-18f) is already pinned by `preflight::tests::run_preflight_advance_skips_recheck_on_idempotently_failing_check` and `preflight::tests::run_preflight_loopback_bounds_recursion` (RESEARCH listed it as not located); both run unedited in Task 3's gate"
  prohibitions:
    - statement: "No hashing crate or `DefaultHasher` is used to store the set; element text is stored (RESEARCH alternatives)"
      status: resolved
      verification: explicit
    - statement: "The re-scan gate's approval does not advance the stage to Validate and does not reuse `handle_stage_failure`'s dispatch"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/state.rs
      provides: "CheckpointApproval, ApprovedCheckpoint, State.checkpoint_approval, CheckpointApproval::unapproved"
      contains: "pub checkpoint_approval: CheckpointApproval"
    - path: crates/devflow-cli/src/preflight.rs
      provides: "recording at Pending Code evaluation and on refusal-gate approval"
    - path: crates/devflow-cli/src/pipeline_launch.rs
      provides: "re-scan gate at the resume decision"
  key_links:
    - from: crates/devflow-cli/src/pipeline_launch.rs resume decision
      to: crates/devflow-core/src/verify.rs::phase_checkpoint_declarations
      via: "fresh scan of the execution root compared through CheckpointApproval::unapproved"
      pattern: "unapproved"
    - from: crates/devflow-cli/src/preflight.rs::run_preflight
      to: crates/devflow-core/src/state.rs::CheckpointApproval
      via: "Pending → Recorded at Stage::Code"
      pattern: "CheckpointApproval::Recorded"
---

<!-- plan-criteria-allow: R4 - Git worktree discovery is a command precondition; this scanner joins it to later non-Git pipelines on the same Bash line. -->

<objective>
CHKPT-02 — ROADMAP alias GATE-02, backlog 999.126. Record the human-only checkpoint set DevFlow checked at Code's
preflight and park any new or changed checkpoint at a human gate at the resume decision (D-07, R-8).

Purpose: agents may write plan files during Code; today a `blocking-human` checkpoint added after preflight reaches the
agent's auto-decide route without a human seeing it. Output: State field, recording, re-scan gate, tests.

Open Question 1 (RESEARCH) is resolved by D-07, not decided here: `Unrecorded` is the serde default for older state
files and is never recorded by a Code evaluation; `Pending` exists only for state created by this binary.

Contracts touched (P3): C-8; C-10 (the six `advance_*checkpoint*` tests: the two relaunch fixtures move to a recorded
set, the four fall-through tests are not edited); C-11 (`preflight_unattended_launch_check` signature unchanged); C-12 (D-18f: preflight gate
`Advance` skips the re-check — kept; recording added to that arm; already pinned by
`run_preflight_advance_skips_recheck_on_idempotently_failing_check` and `run_preflight_loopback_bounds_recursion`,
which RESEARCH missed).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-02-SUMMARY.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-05-SUMMARY.md

RESEARCH Pattern 5 and Finding F-7; PATTERNS.md § 5, § 16, § 17. 48-02 provides `verify::phase_checkpoint_declarations`
and `CheckpointDeclaration { plan_file, element, blocking_human, human_action }`. Tests in `pipeline_launch.rs` that
need a stub agent use the 48-03/48-05 child shape; gate timeouts go on the child `Command` only.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — record at Code preflight, compare at the resume decision, park an added checkpoint at a gate</name>
  <files>crates/devflow-core/src/state.rs, crates/devflow-cli/src/preflight.rs, crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - crates/devflow-core/src/state.rs lines 30-60, 280-400 (field pattern, `base_branch`/`canary` docs), `State::new`, and `preflight_retries_round_trips_through_serde` near :850
    - crates/devflow-cli/src/preflight.rs lines 1060-1200 (unattended check, D-08/D-09 docs) and 1320-1390 (`run_preflight`, the refusal gate arms)
    - crates/devflow-cli/src/pipeline_launch.rs lines 1560-1640 (resume decision), `relaunch_checkpoint_session` near :1115, and test `advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records` near :3596 (child-converted by 48-05)
    - crates/devflow-cli/src/pipeline_gate.rs `run_gate_with_timeout` near :352-430 and `GateAction` handling (read only)
  </read_first>
  <behavior>
    - `state::tests::checkpoint_approval_defaults_to_unrecorded_for_old_state_files`: the `absent_json` fixture shape loads with `Unrecorded`; `State::new` yields `Pending`; `Recorded(vec![])` round-trips as `Recorded(vec![])`
    - RED: `pipeline_launch::tests::checkpoint_added_after_code_preflight_parks_at_the_rescan_gate`: state `Recorded(vec![])` (Code preflight saw none), plan now declares a task-level `blocking-human` gate, capture confirms it → `advance` writes a Code gate file whose context names the plan file, emits no `checkpoint_auto_decided`, and does not relaunch (today it auto-decides)
  </behavior>
  <action>
Per D-07:
1. Tests first (RED confirmed on the assertion, then commit).
2. state.rs: add `ApprovedCheckpoint { plan_file: String, element: String }` and `enum CheckpointApproval { Unrecorded, Pending, Recorded(Vec<ApprovedCheckpoint>) }` with `#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize, Default)]`, `#[serde(rename_all = "snake_case")]`, `#[default] Unrecorded`. Add `#[serde(default)] pub checkpoint_approval: CheckpointApproval` to `State` with a doc comment stating each variant's meaning and that it must survive across the separate `advance` process. `State::new` sets `Pending`. Add `impl CheckpointApproval { pub fn unapproved<'a>(&self, current: &'a [verify::CheckpointDeclaration]) -> Vec<&'a verify::CheckpointDeclaration> }` returning every human-only declaration (`blocking_human || human_action`) not matched in a multiset of recorded `(plan_file, element)`; `Unrecorded` and `Pending` match nothing.
3. preflight.rs `run_preflight`: after the generic checks run at `Stage::Code`, if `state.checkpoint_approval` is `Pending`, set `Recorded(...)` from `verify::phase_checkpoint_declarations(execution_root, state.phase)` filtered to human-only, where `execution_root = state.worktree_path.as_deref().unwrap_or(&state.project_root)` (the root the resume decision scans), and persist with the function's existing save path. Never record when `Unrecorded`.
4. pipeline_launch.rs resume decision: inside `if checkpoint_confirmed`, in the `(Some(session_id), true)` arm only and immediately before `relaunch_checkpoint_session`, compute `unapproved` from a fresh `phase_checkpoint_declarations(execution_root, phase)`. If non-empty, fire a Code-stage gate through `run_gate_with_timeout` whose context lists each unapproved plan file and says the checkpoint was added or changed after Code preflight. Leave the `(Some(_), false)` and `(None, _)` arms untouched: they never arm auto-decide and already fall through to the never-silent per-stage gate, so a compare there would only rewrite the existing fall-through tests. This task only parks at the re-scan gate; 48-15 implements its response behavior.
5. Update the two relaunch contract tests whose fixtures start from `State::new` (now `Pending`, which parks): `advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records` (near :3596) and `advance_with_worktree_declared_checkpoint_reads_the_execution_root` (near :3682). In each, before the state is saved, set `checkpoint_approval` to `Recorded` of the human-only declarations `phase_checkpoint_declarations` returns for the fixture's execution root — the state a Code preflight leaves. Change no assertion, and list both in SUMMARY. Do not edit the four fall-through tests.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; cargo test -p devflow-core --lib state::tests::checkpoint_approval_defaults_to_unrecorded_for_old_state_files -- --exact > "$L/s.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/s.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/s.log" | head -1); echo "state_one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; t=pipeline_launch::tests::checkpoint_added_after_code_preflight_parks_at_the_rescan_gate; cargo test -p devflow --bin devflow "$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "tracer_one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; sig=$(rg -c "fn preflight_unattended_launch_check\(project_root: &Path, state: &State\) -> Result<\(\), String>" crates/devflow-cli/src/preflight.rs || echo 0); echo "check_signature_unchanged=$sig"; [ "$sig" -eq 1 ] || rc=1; for t in pipeline_launch::tests::advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records pipeline_launch::tests::advance_with_worktree_declared_checkpoint_reads_the_execution_root; do cargo test -p devflow --bin devflow "$t" -- --exact > "$L/r.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/r.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/r.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; state or tracer test one_passed=0 or filtered=none, either relaunch contract test one_passed=0 or filtered=none (its fixture was not moved to a recorded set, or the compare left the `(Some(session_id), true)` arm), or check_signature_unchanged=0 (the preflight D-09 fixed signature line of `preflight_unattended_launch_check` changed, including a parameter rename)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the RED run of `checkpoint_added_after_code_preflight_parks_at_the_rescan_gate` failing on its "no auto-decide" assertion before implementation
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>A checkpoint added after Code preflight parks at a gate instead of being auto-decided.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Changed bodies, missing records and the unchanged-plan control</name>
  <files>crates/devflow-core/src/state.rs, crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - crates/devflow-core/src/state.rs and crates/devflow-cli/src/pipeline_launch.rs as left by Task 1
    - .planning/phases/19-*/19-05-PLAN.md task at line 83 (real-plan fixture for the comparison rule)
  </read_first>
  <behavior>
    - `pipeline_launch::tests::rewritten_body_of_an_approved_checkpoint_parks_at_the_rescan_gate`: recorded set holds the 19-05 task element; the plan's element body is edited → gate
    - `pipeline_launch::tests::unrecorded_checkpoint_set_parks_at_the_rescan_gate`: `Unrecorded` + a declared checkpoint → gate
    - `pipeline_launch::tests::pending_checkpoint_set_parks_at_the_rescan_gate`: approval left at `State::new`'s `Pending`, a declared and confirmed checkpoint, a session id on record, ceiling not reached → a Code gate file is written, no `checkpoint_auto_decided` event, no relaunch. Fails if `Pending` is treated as approved
    - Control `pipeline_launch::tests::unchanged_recorded_checkpoint_still_auto_decides`: recorded set equals current → `checkpoint_auto_decided` emitted and relaunch reached, no gate file
    - `state::tests::unapproved_checkpoints_is_an_order_insensitive_multiset_difference`: order swapped → none unapproved; duplicate identical elements in two plans with one recorded → one unapproved; `Recorded(vec![])` with zero current → none
  </behavior>
  <action>
Per D-07: write the five tests, confirm the control passes and the others fail or pass for the right reason, then fix any gap in `unapproved` or the decision point. Every gating test sets a session id and an unexhausted ceiling, so it exercises the `(Some(session_id), true)` arm where the compare lives. Element comparison uses 48-02's normalized element text byte-for-byte; the 19-05 fixture pins that a whitespace-only trailing change is not a change and any other body edit is.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; cargo test -p devflow-core --lib state::tests::unapproved_checkpoints_is_an_order_insensitive_multiset_difference -- --exact > "$L/s.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/s.log" || true); echo "multiset_one_passed=$ok"; [ "$ok" -eq 1 ] || rc=1; for t in rewritten_body_of_an_approved_checkpoint_parks_at_the_rescan_gate unrecorded_checkpoint_set_parks_at_the_rescan_gate pending_checkpoint_set_parks_at_the_rescan_gate unchanged_recorded_checkpoint_still_auto_decides; do cargo test -p devflow --bin devflow "pipeline_launch::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none</fails_when>
  </verify>
  <acceptance_criteria>
    - The control and the three gating tests share one fixture builder, so the control passing while a gating test fails cannot be a fixture difference (reviewable in the diff)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>New, changed and unrecorded sets gate; an unchanged approved set still auto-decides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written PLAN.md during Code → resume auto-decide | An agent can add or rewrite a `blocking-human` task after preflight |
| state file across binaries | Older binaries write state without the field |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-07-01 | Elevation of Privilege | agent adds a checkpoint to self-route into auto-decide | high | mitigate | Fresh-scan compare against the recorded set; new or changed parks at a human gate (Tasks 1-2) |
| T-48-07-02 | Elevation of Privilege | loop-back Code evaluation silently widens the set | high | mitigate | Record only from `Pending`; `Unrecorded` never recorded by an evaluation (Task 3 test) |
| T-48-07-03 | Tampering | old state file loads with a permissive default | medium | mitigate | serde default is `Unrecorded`, which gates |
| T-48-07-04 | Denial of Service | re-scan gate fires on every resume after approval | low | mitigate | Approval records the set (R-8 test) |
</threat_model>

<verification>
Both gates print `gate_rc=0`; workspace clippy clean.
</verification>

<success_criteria>
A checkpoint added or changed after Code preflight parks at a human gate; unchanged recorded plans still auto-decide.
</success_criteria>

## Artifacts this phase produces

- `devflow_core::state::CheckpointApproval` (variants `Unrecorded`, `Pending`, `Recorded(Vec<ApprovedCheckpoint>)`), `devflow_core::state::ApprovedCheckpoint { plan_file, element }`, `CheckpointApproval::unapproved`
- `State.checkpoint_approval` (`#[serde(default)]`, JSON key `checkpoint_approval`)
- Re-scan gate at the resume decision in `pipeline_launch.rs` (Code-stage gate context naming changed plan files)
- Tests named in the three `<behavior>` blocks

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-07-SUMMARY.md` when done.
</output>
