---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 16
type: tdd
wave: 10
depends_on: ["48-13"]
files_modified:
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/tests/gate_wedge_e2e.rs
autonomous: true
requirements: [SURV-02]
estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Per D-05, gate approve and gate reject at a non-Ship gate write no answer for NoHolder or Recycled, return an honest error, and name devflow resume --phase N plus devflow recover --clean --phase N"
    - "Per D-05, Ship still writes an answer without a live waiter and names devflow ship --phase N; Live writes with the waiter pid; Unconfirmable writes while claiming neither a waiter nor its absence"
    - "Per R-6, stop_via_gate and gate sweep apply the same status rule before Gates::reap; sweep leaves a no-waiter non-Ship gate alone"
    - "Per D-05, stop post-answer messaging uses the HolderStatus returned by stop_via_gate, not lock contention alone"
    - "Criterion 4 has both #200 arms: an interrupted start rejects honestly without a response file, while the uninterrupted control consumes the same abort rejection and records pickup_ms"
    - "Per CX-5, a post-publish status recheck never claims pickup after Live becomes non-Live; SUMMARY records the remaining message TOCTOU limit"
  prohibitions:
    - statement: "No answer file is written at a non-Ship gate for NoHolder or Recycled, per D-05."
      status: resolved
      verification: explicit
    - statement: "Waiter status is never inferred from state.monitor_pid."
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-cli/src/commands.rs
      provides: "no_waiter_repair and waiter-aware gate, stop, and sweep behavior"
      contains: "fn no_waiter_repair"
    - path: crates/devflow-cli/tests/gate_wedge_e2e.rs
      provides: "both #200 response arms"
  key_links:
    - from: crates/devflow-cli/src/commands.rs
      to: crates/devflow-core/src/lock.rs
      via: "status is checked before any Gates::respond or Gates::reap"
      pattern: "holder_status"
    - from: crates/devflow-cli/src/commands.rs
      to: crates/devflow-cli/src/commands.rs
      via: "returns HolderStatus to prevent a recycled pid from being described as a waiter"
      pattern: "HolderStatus"
---

<objective>
Use 48-13's verified holder-status primitive to make every SURV-02 response truthful, and pin the real #200 wedge
versus live-consumer control.

Purpose: stale answer files must never make a later gate decision, and operator repair instructions must match the
actual wait state. Output: shared response wording, command behavior, and e2e proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-12-SUMMARY.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-13-SUMMARY.md

48-13 supplies HolderStatus and may_be_waiting. 48-12 makes gate waiters hold the per-phase lock.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — gate reject distinguishes the #200 wedge from a live waiter</name>
  <files>crates/devflow-cli/src/commands.rs, crates/devflow-cli/tests/gate_wedge_e2e.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs gate_respond and its existing response wording
    - crates/devflow-cli/tests/gate_sweep_e2e.rs and stop_e2e.rs fixtures
    - crates/devflow-core/src/lock.rs HolderStatus from 48-13
  </read_first>
  <behavior>
    - commands::tests::gate_response_message_does_not_claim_pickup_after_live_becomes_no_holder
    - gate_wedge_e2e::wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly
    - gate_wedge_e2e::self_resolving_arm_live_start_consumes_the_rejection
  </behavior>
  <action>
Before the first commit, capture BASE_SHA with git rev-parse HEAD, verify the resulting commit object, and record the
literal base_sha in this plan SUMMARY. Per D-05, write the three tests first. Add no_waiter_repair returning ship for
Ship and resume plus recover --clean for every other stage. In gate_respond, inspect holder_status after resolving the
gate and before response publication. NoHolder or Recycled at a non-Ship gate returns an error with nothing is waiting,
writes no answer, and names the repair. Ship writes an answer and names ship. Live writes and rechecks holder status
before claiming a current waiter; Unconfirmable writes but says only that a process holds the lock without confirmation.
Put post-publish wording in a pure helper for the Live-to-NoHolder unit test. Replace the former poller claim.

For the e2e arms, use the same gate reject command with an abort note in each arm. The wedge arm kills and reaps the
foreground start before the command; it proves no response file appears. The control leaves start running, proves it
exits with cleared state, and prints pickup_ms. If either arm does not complete, the harness result is a failure.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; cargo test -p devflow --bin devflow commands::tests::gate_response_message_does_not_claim_pickup_after_live_becomes_no_holder -- --exact > "$L/message.log" 2>&1; me=$?; mok=$(grep -c "test result: ok. 1 passed" "$L/message.log" || true); mfo=$(grep -oE "[1-9][0-9]* filtered out" "$L/message.log" | head -1); echo "message_exit=$me one_passed=$mok filtered=$mfo"; [ "$me" -eq 0 ] && [ "$mok" -eq 1 ] && [ -n "$mfo" ] || rc=1; for t in wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly self_resolving_arm_live_start_consumes_the_rejection; do cargo test -p devflow --test gate_wedge_e2e "$t" -- --exact --nocapture > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=$fo"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; pk=$(grep -oE "pickup_ms=[0-9]+" "$L/self_resolving_arm_live_start_consumes_the_rejection.log" | head -1); echo "observed_pickup=$pk"; [ -n "$pk" ] || rc=1; old=$(rg -c -F "once the waiting monitor polls it" crates/devflow-cli/src/commands.rs || true); echo "old_poller_claim=$old"; [ "$old" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; the unit message test or either e2e arm fails or matches no exact test, pickup_ms is absent from the live arm, or the former poller claim remains</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY records the exact base_sha captured before Task 1 first commit and the live-arm pickup_ms with its poll-backoff limitation
    - The two arms use the same answer command; the interrupted start is their only behavioral variable
    - The automated gate prints gate_rc=0
  </acceptance_criteria>
  <done>Gate reject publishes only when its waiter-status rule permits it and #200 distinguishes a wedge from a live consumer.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stop and sweep use the same status matrix and preserve live-gate contracts</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs gate_sweep, stop_via_gate, stop, and the 48-12 post-answer state-write path
    - 48-16-SUMMARY.md base_sha and Task 1 response helper
  </read_first>
  <behavior>
    - commands::tests::stop_via_gate_with_no_waiter_at_a_non_ship_gate_writes_nothing
    - commands::tests::stop_via_gate_with_a_live_waiter_writes_the_rejection
    - commands::tests::gate_sweep_leaves_a_no_waiter_non_ship_gate_alone
    - commands::tests::gate_approve_with_no_waiter_at_the_ship_gate_writes_and_names_ship
    - commands::tests::gate_respond_with_a_recycled_lock_pid_at_a_non_ship_gate_writes_nothing
    - commands::tests::stop_at_a_ship_gate_with_a_recycled_holder_claims_no_waiter
    - commands::tests::stop_at_a_non_ship_gate_with_an_unconfirmable_holder_claims_neither
  </behavior>
  <action>
Per D-05 and R-6, write the status-matrix tests first. Thread the HolderStatus determined before answering from
stop_via_gate back to stop. Gate sweep and stop_via_gate leave no-waiter non-Ship gates untouched; Live and
Unconfirmable retain their permitted response behavior; Ship retains C-6 response behavior. Stop describes a waiter
only for Live, describes Unconfirmable without a waiter claim, and treats Recycled as no waiter so state is not marked
through 48-12's live-waiter branch. Gates::reap remains the only sweep write path.

After the commits, read base_sha from SUMMARY, run git rev-parse --verify on that exact value and capture its exit
before use. Run git diff --check with the explicit base_sha..HEAD range and capture its exit before evaluating the
result. Record both exits and every changed gate-sweep or stop-e2e wording assertion in SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in stop_via_gate_with_no_waiter_at_a_non_ship_gate_writes_nothing stop_via_gate_with_a_live_waiter_writes_the_rejection gate_sweep_leaves_a_no_waiter_non_ship_gate_alone gate_approve_with_no_waiter_at_the_ship_gate_writes_and_names_ship gate_respond_with_a_recycled_lock_pid_at_a_non_ship_gate_writes_nothing stop_at_a_ship_gate_with_a_recycled_holder_claims_no_waiter stop_at_a_non_ship_gate_with_an_unconfirmable_holder_claims_neither; do cargo test -p devflow --bin devflow "commands::tests::$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=$fo"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; for f in gate_sweep_e2e stop_e2e gate_wedge_e2e; do cargo test -p devflow --test "$f" > "$L/$f.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/$f.log" || true); echo "\${f}_exit=$e passed_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; done; cargo test -p devflow --bin devflow commands::tests:: > "$L/commands.log" 2>&1; ue=$?; echo "commands_unit_exit=$ue"; [ "$ue" -eq 0 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ce=$?; echo "clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any named matrix test fails or matches no exact test, any listed e2e file has a non-zero exit or no passed-test line, commands_unit_exit is non-zero, or clippy_exit is non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - A concrete base_sha from Task 1 SUMMARY resolves through git rev-parse --verify and git diff --check base_sha..HEAD exits 0; both captured exit values are recorded
    - SUMMARY lists every changed assertion in gate_sweep_e2e.rs and stop_e2e.rs, if any, with old and new wording
    - The automated gate prints gate_rc=0
  </acceptance_criteria>
  <done>Every CLI gate response and doctor-facing stop result tells the truth for Live, Recycled, Unconfirmable, and no-holder states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator verbs to gate response files | A response can decide a later gate if the original waiter is gone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-16-01 | Tampering | stale response decides a later non-Ship gate | high | mitigate | NoHolder and Recycled write no response |
| T-48-16-02 | Repudiation | command claims a waiter without evidence | medium | mitigate | HolderStatus matrix and post-publish Live recheck |
| T-48-16-03 | Spoofing | recycled pid treated as a waiter | high | mitigate | Recycled handling stays no-waiter in every response path |
</threat_model>

<verification>
Both gates print gate_rc=0. The post-publish check narrows a status-message race; it does not establish a waiter lifetime.
</verification>

<success_criteria>
Gate answers are honest and safe for every holder status, and the #200 live arm proves the wedge harness discriminates.
</success_criteria>

<output>
Create .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-16-SUMMARY.md when done.
</output>
