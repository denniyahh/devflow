---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 17
type: execute
wave: 7
depends_on: ["48-09"]
files_modified:
  - .planning/codebase/TESTING.md
  - crates/devflow-cli/src/test_support.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 35000
  raw_tokens: 70000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-08 and D-09, TESTING.md says PATH is set only on a child Command through run_test_in_child and guarded by assert_child_ran_exactly_one_passing_test; remaining mutations require a reasoned Clippy expect"
    - "Per D-08, comments and env_lock documentation describe the post-conversion child-process premise and no longer name the deleted PATH guard"
    - "Per D-08, SUMMARY records a full-suite timing after conversion and a two-CPU pinned sanity run beside the 48-03 baseline, including their limits"
  artifacts:
    - path: .planning/codebase/TESTING.md
      provides: "D-09 Environment Mutation Rule"
      contains: "run_test_in_child"
  key_links:
    - from: .planning/codebase/TESTING.md
      to: clippy.toml
      via: "The documented exception rule names the enforced lint"
      pattern: "clippy.toml"
---

<objective>
Document the D-08 and D-09 test-isolation contract and record bounded post-conversion measurements separately from
the lint implementation.

Purpose: documentation and evidence should not enlarge the atomic lint-and-annotation change. Output: current
environment-mutation guidance and an evidence-backed SUMMARY measurement.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-09-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — publish the enforced PATH child-process rule and update stale helper commentary</name>
  <files>.planning/codebase/TESTING.md, crates/devflow-cli/src/test_support.rs, crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - .planning/codebase/TESTING.md Environment Mutation Rule and False-Green Traps
    - crates/devflow-cli/src/test_support.rs env_lock documentation and trailing_reap_call_is_skipped_when_a_later_assertion_panics
    - comments in pipeline_outcomes.rs returned by rg NeutralPath
  </read_first>
  <action>
Per D-08 and D-09, update the Environment Mutation Rule: PATH is supplied only to a child Command through
devflow_core::test_support::run_test_in_child, is a directory rather than a removed value, and every child result is
guarded by assert_child_ran_exactly_one_passing_test with a module-qualified test name. State that other
process-global test mutations require a reasoned expect for clippy::disallowed_methods and retain the one-mutex-per-
variable rule while their conversion is deferred. Rewrite env_lock and outcome-test comments to the child-process
premise; remove stale names for the deleted PATH guard without changing test behavior. Record that clippy.toml does not
match the repository post-commit checklist trigger and therefore needs no DEV-SETUP-CHECKLIST change.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; T=.planning/codebase/TESTING.md; for w in run_test_in_child clippy.toml "expect(clippy::disallowed_methods"; do c=$(grep -cF "$w" "$T" || true); echo "testing_md_$w=$c"; [ "$c" -ge 1 ] || rc=1; done; na=$(rg -n NeutralPath crates --glob "*.rs" | wc -l); echo "neutral_path_all_lines=$na"; [ "$na" -eq 0 ] || rc=1; t=test_support::tests::trailing_reap_call_is_skipped_when_a_later_assertion_panics; cargo test -p devflow --bin devflow "$t" -- --exact > "$L/c25.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/c25.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/c25.log" | head -1); echo "c25_exit=$e one_passed=$ok filtered=$fo"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any documented rule term is absent, a deleted guard name remains under crates, or the C-25 test fails or matches no exact test</fails_when>
  </verify>
  <done>Documentation and comments describe the actual enforced isolation rule without stale guard terminology.</done>
</task>

<task type="auto">
  <name>Task 2: Record full-suite and two-CPU post-conversion evidence with its limits</name>
  <files>.planning/codebase/TESTING.md</files>
  <read_first>
    - 48-03-SUMMARY.md TEST-01 baseline
    - Task 1 documentation change
  </read_first>
  <action>
Per D-08, run /usr/bin/time -p scripts/check.sh test once and taskset -c 0,1 scripts/check.sh test once, each under
timeout 45m with separate logs. Record both real times, per-binary test-result lines, failures if any, and the 48-03
baseline in SUMMARY. State the evidence limit: one run under varying concurrent load and a green pinned result are a
sanity check, not a reliability guarantee that PATH flakes cannot recur. Do not change TESTING.md unless a measured
failure exposes a documentation contradiction; report that contradiction in SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; timeout 45m /usr/bin/time -p scripts/check.sh test > "$L/full.log" 2>&1; full=$?; timeout 45m taskset -c 0,1 scripts/check.sh test > "$L/pinned.log" 2>&1; pinned=$?; full_results=$(grep -c "^test result:" "$L/full.log" || true); pinned_results=$(grep -c "^test result:" "$L/pinned.log" || true); full_real=$(grep -c "^real " "$L/full.log" || true); echo "full_exit=$full pinned_exit=$pinned full_result_lines=$full_results pinned_result_lines=$pinned_results full_real_lines=$full_real"; [ "$full" -eq 0 ] && [ "$pinned" -eq 0 ] && [ "$full_results" -ge 1 ] && [ "$pinned_results" -ge 1 ] && [ "$full_real" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; either timed run exits non-zero including timeout 124, either log lacks test-result lines, or the full timing log lacks exactly one real line</fails_when>
  </verify>
  <done>SUMMARY has the post-conversion measurements beside the baseline and states precisely what one-run evidence does not establish.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented test rule to future contributors | An inaccurate rule can reintroduce global PATH mutation or hide its evidence limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-17-01 | Tampering | stale test guidance | low | mitigate | Documentation uses the helper and lint names verified by Task 1 |
| T-48-17-02 | Repudiation | timing result presented as reliability proof | low | mitigate | SUMMARY records one-run and concurrent-load limits |
</threat_model>

<verification>
Both gates print gate_rc=0. The timing gate proves only those two completed runs, not a long-term flake rate.
</verification>

<success_criteria>
The test-isolation contract is documented separately from the lint, and its limited post-conversion evidence is recorded.
</success_criteria>

<output>
Create .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-17-SUMMARY.md when done.
</output>
