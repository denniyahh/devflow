---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 08
type: execute
wave: 4
depends_on: ["48-04", "48-06"]
files_modified:
  - crates/devflow-cli/src/pipeline_outcomes.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-10, every function in `pipeline_outcomes.rs` containing an abort fixture note either runs in a child (`in_child_test`) or asserts `CHILD_TEST_ENV` as its first statement; the fixture-coverage gate reads 0 (it read 18 on 2026-09-14)"
    - "Per D-08, each converted test runs with an agent-free `PATH` directory and a guard; shared fixture helpers (`arm_a_ambiguous_outcome_gates_on_cycle_one`, `arm_b_genuine_failures_reach_the_ceiling`, `drive_validate_advance_and_read_gate_context`) panic when called outside a child"
    - "Reachability to an agent spawn is not measured per site (D-10 asked the planner to confirm it); converting all sites makes the protection structural whether or not a site can reach a spawn"
    - "No test is deleted: `#[test]` count ≥ 72; `stage_failure_retry_cleans_stale_response` (C-7) still passes"
  prohibitions:
    - statement: "The abort fixture notes are not reworded or removed (they still decide the tested gate path)"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-cli/src/pipeline_outcomes.rs
      provides: "structurally agent-free abort-fixture tests"
      contains: "CHILD_TEST_ENV"
  key_links:
    - from: pipeline_outcomes.rs fixture helpers
      to: crates/devflow-core/src/test_support.rs::CHILD_TEST_ENV
      via: "first-statement assertion"
      pattern: "CHILD_TEST_ENV"
---

<!-- plan-criteria-allow: R4 - The c0fb193 baseline object is explicitly checked before its content is read; the scanner also joins root setup to later pipelines. -->

<objective>
TEST-01 — ROADMAP alias 999.38, with backlog 999.80 folded in (D-10). Make every abort-fixture test in
`pipeline_outcomes.rs` structurally agent-free (18 fixture functions measured; 999.80's recorded lines are stale).
Tests that 48-04 already converted count as covered.

Purpose: 999.80 — a magic substring in a JSON note is the only thing keeping these tests off `launch_stage`. Output:
converted tests and helper assertions.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-04-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — convert `external_verify_disagreement_gates_immediately` and prove the helper assertion fires outside a child</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - crates/devflow-cli/src/pipeline_outcomes.rs: the test named above and every function listed by `awk "/^    (pub(\\(crate\\))? )?fn /{name=\$0} /abort: test cleanup/{print name}" crates/devflow-cli/src/pipeline_outcomes.rs`
    - The 48-04 conversions in this file (template)
  </read_first>
  <action>
Per D-10: convert `external_verify_disagreement_gates_immediately` to a guarded child run with an agent-free `PATH` directory. Add `pipeline_outcomes::tests::abort_fixture_helper_refuses_to_run_outside_a_child` as a `#[should_panic(expected = "must run inside a child test")]` test that calls `drive_validate_advance_and_read_gate_context` (or the smallest helper that writes an abort fixture) directly in the parent after that helper gains its first-statement assertion `assert!(std::env::var_os(devflow_core::test_support::CHILD_TEST_ENV).is_some(), "<helper name> must run inside a child test with an agent-free PATH (999.80)")`. This is the negative control that the structural guard is live.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in external_verify_disagreement_gates_immediately abort_fixture_helper_refuses_to_run_outside_a_child; do cargo test -p devflow --bin devflow "pipeline_outcomes::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; either test one_passed=0 or filtered=none (the should_panic control passes only when the helper assertion panics with the expected text)</fails_when>
  </verify>
  <acceptance_criteria>
    - The should-panic control's `expected` string is a substring of the helper's assertion message (reviewable in the diff), so it cannot pass on an unrelated panic
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>One fixture test is child-isolated and the helper guard is proven live.</done>
</task>

<task type="auto">
  <name>Task 2: Cover every remaining abort-fixture function</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - The functions still reported by the Task 2 awk gate (run it first)
  </read_first>
  <action>
Per D-10: for each remaining function the gate reports, convert the test to a guarded child with an agent-free `PATH` directory, or — for a shared helper — add the first-statement `CHILD_TEST_ENV` assertion and convert every caller. Re-run the gate until it reads 0.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; git cat-file -e c0fb193^{commit} || { echo base_missing; exit 1; }; f=crates/devflow-cli/src/pipeline_outcomes.rs; nf=$(awk "/^    (pub(\\(crate\\))? )?fn /{name=\$0} /CHILD_TEST_ENV|in_child_test\\(/{c[name]=1} /abort: test cleanup/ && !/^[[:space:]]*\\/\\//{f[name]=1} END{b=0; for (n in f) if (!(n in c)) b++; print b}" "$f"); fx=$(grep -v "^[[:space:]]*//" "$f" | grep -c "abort: test cleanup" || true); bt=$(git show "c0fb193:$f" | grep -c "#\[test\]"); nt=$(grep -c "#\[test\]" "$f"); echo "fixture_fns_without_child=$nf fixture_lines=$fx base_tests=$bt now_tests=$nt"; [ "$nf" -eq 0 ] && [ "$fx" -ge 18 ] && [ "$nt" -ge "$bt" ] || rc=1; cargo test -p devflow --bin devflow pipeline_outcomes::tests:: > "$L/m.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; cargo clippy -p devflow --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; fixture_fns_without_child above 0, fixture_lines under 18 (fixtures deleted instead of protected), now_tests below base_tests, module run failing, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, run 2026-09-14 against this file: the same awk gate printed `fixture_fns_without_child=18`, so reading 0 reflects conversions, not a broken scan
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>No abort-fixture test in pipeline_outcomes.rs can reach an agent spawn through the developer's PATH.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo test process → spawned agent CLI | 999.80 / T-33-09 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-08-01 | Elevation of Privilege | fixture reworded or parser changed → LoopBack → real `claude` spawn | medium | mitigate | Child with agent-free PATH; helpers panic outside a child; should-panic control |
</threat_model>

<verification>
Both gates print `gate_rc=0`.
</verification>

<success_criteria>
999.80's protection is structural for every fixture in pipeline_outcomes.rs.
</success_criteria>

## Artifacts this phase produces

- Test `pipeline_outcomes::tests::abort_fixture_helper_refuses_to_run_outside_a_child`
- First-statement child-mode assertions in the abort-fixture helpers

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-08-SUMMARY.md` when done.
</output>
