---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 15
type: tdd
wave: 5
depends_on: ["48-07"]
files_modified:
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/preflight.rs
autonomous: true
requirements: [CHKPT-02]

estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-07 and R-8, approving the re-scan gate records the fresh human-only checkpoint set and relaunches the existing checkpoint session; a second unchanged decision auto-decides without another gate"
    - "Per D-07, rejecting a re-scan gate records nothing: a non-abort rejection reaches the existing non-auto-decide route with the changed plan file in its reason, and an abort rejection terminates through the existing abort path"
    - "Per D-07 and R-8, only human approval records the set after the initial Pending Code evaluation: a preflight-refusal approval records from the execution root, while LoopBack and an Unrecorded state record nothing"
    - "The D-18f preflight Advance path still skips its re-check and the LoopBack path still observes the recursion bound"
  artifacts:
    - path: crates/devflow-cli/src/pipeline_launch.rs
      provides: "Explicit re-scan GateAction handling at the resume decision"
      contains: "CheckpointApproval::Recorded"
    - path: crates/devflow-cli/src/preflight.rs
      provides: "Checkpoint-set recording on a human-approved Code preflight refusal"
      contains: "phase_checkpoint_declarations"
  key_links:
    - from: crates/devflow-cli/src/pipeline_launch.rs re-scan gate Advance arm
      to: crates/devflow-core/src/state.rs::CheckpointApproval
      via: "records fresh declarations before relaunch_checkpoint_session"
      pattern: "relaunch_checkpoint_session"
    - from: crates/devflow-cli/src/preflight.rs refusal-gate Advance arm
      to: crates/devflow-core/src/verify.rs::phase_checkpoint_declarations
      via: "uses the execution root rather than the project root"
      pattern: "worktree_path"
---

<objective>
Complete CHKPT-02's human response paths after 48-07 has recorded and compared checkpoint declarations: approval accepts
the current set; rejection follows the established safe route (D-07, R-8).

Purpose: a re-scan gate only protects unattended execution if a human approval becomes durable and a rejection cannot
silently widen the approved set. Output: response handling and regression tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-07-SUMMARY.md

48-07 provides `CheckpointApproval`, normalized declaration elements, the Pending Code-preflight recording path, and the
re-scan gate before `relaunch_checkpoint_session`.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — approve one re-scan gate, record its current declarations, and relaunch</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <precondition>48-07-SUMMARY.md records that the re-scan gate is emitted before `relaunch_checkpoint_session` and the test fixture can load `CheckpointApproval::Recorded`.</precondition>
  <read_first>
    - crates/devflow-cli/src/pipeline_launch.rs re-scan decision as left by 48-07, including `relaunch_checkpoint_session` and the existing unresolved-checkpoint reason path
    - crates/devflow-cli/src/pipeline_gate.rs `run_gate_with_timeout` and `abort`
  </read_first>
  <behavior>
    - RED: `pipeline_launch::tests::approving_the_rescan_gate_records_the_set_and_relaunches` — an approved response produces `Recorded(current)`, reaches the relaunch path, and the next unchanged advance emits `checkpoint_auto_decided` without a gate
    - `pipeline_launch::tests::rejecting_the_rescan_gate_records_nothing_and_falls_through` — a non-abort rejection leaves approval unchanged, does not relaunch, and names the changed plan file in the reason
    - `pipeline_launch::tests::rejecting_the_rescan_gate_with_abort_aborts` — an abort note terminates through the same abort implementation as other gates
  </behavior>
  <action>
Per D-07 and R-8, write the three tests first and record the RED assertion for approval before implementing. In the
`(Some(session_id), true)` resume arm that 48-07 parks, handle the re-scan `GateAction` explicitly: Advance records the
fresh human-only declarations from the execution root, saves state, and calls the existing `relaunch_checkpoint_session`;
LoopBack does not record, augments the unresolved reason with every unapproved plan file, and reaches the present
non-auto-decide dispatch; Abort calls `pipeline_gate::abort` with the existing arguments. Do not move the compare into
the `(Some(_), false)` or `(None, _)` arms (D-07).
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in approving_the_rescan_gate_records_the_set_and_relaunches rejecting_the_rescan_gate_records_nothing_and_falls_through rejecting_the_rescan_gate_with_abort_aborts; do cargo test -p devflow --bin devflow "pipeline_launch::tests::$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any named response test has exit non-zero, one_passed other than 1, or filtered=none</fails_when>
  </verify>
  <done>Human approval records the exact current set and relaunches once; either rejection follows the specified safe path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Human-approved preflight refusal records from the execution root only</name>
  <files>crates/devflow-cli/src/preflight.rs</files>
  <read_first>
    - crates/devflow-cli/src/preflight.rs `run_preflight`, its Code refusal gate arms, and the 48-07 Pending-recording code
    - crates/devflow-core/src/verify.rs `phase_checkpoint_declarations`
  </read_first>
  <behavior>
    - `preflight::tests::code_preflight_does_not_record_an_unrecorded_set` — an older state stays Unrecorded through Code evaluation
    - `preflight::tests::code_preflight_records_a_pending_set_from_the_execution_root` — a worktree-only checkpoint records exactly one declaration and reloads as Recorded
    - `preflight::tests::approving_the_preflight_refusal_gate_records_the_set` — Advance records the current execution-root set; LoopBack records nothing
    - Controls: `run_preflight_advance_skips_recheck_on_idempotently_failing_check` and `run_preflight_loopback_bounds_recursion` retain their existing outcomes
  </behavior>
  <action>
Per D-07 and R-8, write the tests first. In the Code preflight refusal gate's Advance arm, record the current
human-only declarations from `state.worktree_path` when present (otherwise `state.project_root`) before its existing
save and continuation. Leave Unrecorded untouched at every Code evaluation, leave the LoopBack arm unrecorded, and
preserve D-18f's Advance skip and LoopBack re-check order.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in code_preflight_does_not_record_an_unrecorded_set code_preflight_records_a_pending_set_from_the_execution_root approving_the_preflight_refusal_gate_records_the_set run_preflight_advance_skips_recheck_on_idempotently_failing_check run_preflight_loopback_bounds_recursion; do cargo test -p devflow --bin devflow "preflight::tests::$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; cargo test -p devflow --bin devflow preflight::tests:: > "$L/module.log" 2>&1; me=$?; mok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/module.log" || true); echo "preflight_module_exit=$me module_ok_lines=$mok"; [ "$me" -eq 0 ] && [ "$mok" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; a named preflight test exits non-zero, does not report exactly one passed test with filtered output, preflight_module_exit is non-zero, or module_ok_lines is not 1</fails_when>
  </verify>
  <done>Only a human-approved Code gate records an updated checkpoint set, and it always records the execution-root declaration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human gate response → persisted checkpoint approval | The response decides whether a later resume may auto-decide an agent-written plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-15-01 | Elevation of Privilege | re-scan approval | high | mitigate | Record fresh declarations only on Advance before relaunch |
| T-48-15-02 | Tampering | Code preflight records from project root | high | mitigate | Worktree fixture proves recording and comparison use the same execution root |
| T-48-15-03 | Repudiation | LoopBack silently accepts changed plans | medium | mitigate | LoopBack keeps approval unchanged and names unapproved plan files |
</threat_model>

<verification>
Both task gates print `gate_rc=0`; targeted preflight and resume tests prove both response paths.
</verification>

<success_criteria>
The checkpoint set widens only after a human approval, while unchanged recorded checkpoints still auto-decide.
</success_criteria>

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-15-SUMMARY.md` when done.
</output>
