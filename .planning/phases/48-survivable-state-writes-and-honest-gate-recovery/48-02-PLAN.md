---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/verify.rs
autonomous: true
requirements: [CHKPT-01]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Per D-06, `phase_has_human_only_checkpoint` and `phase_has_blocking_human_checkpoint` are filters over one parser (`phase_checkpoint_declarations`); a `blocking-human` marker outside a task-opening line neither blocks preflight nor arms resume"
    - "Per D-06 (Phase 47 D-02 carve-out kept), the human-only predicate matches both `gate=\"blocking-human\"` and `type=\"checkpoint:human-action\"`, while the resume predicate matches `blocking-human` only"
    - "A declaration opens only on a line whose leading whitespace is followed directly by the task-element opener (`TASK_ELEMENT_OPENING`) and then whitespace or a closing angle bracket (F-4; the exact regex is in RESEARCH Pattern 4): an inline task tag inside prose is not a declaration"
    - "Per D-06, a task tag inside a properly closed fence is not a declaration, and the pinned fenced-example known-limit test is flipped to assert that"
    - "Per R-3, from an unclosed fence to end of file the line-anchored match still applies, so an unterminated fence before a real `blocking-human` task still declares it (fail closed)"
    - "Per CX-2, a closing fence has at most three leading spaces, a run of the opening character at least as long as the opener, and only trailing whitespace; a `~~~` line inside a backtick fence, a shorter backtick run, or a same-character run followed by text does not close it (Pitfall 5)"
    - "Edge (CHKPT-01 empty, explicit): a phase with no plan files, or plans with no task declarations, yields no declarations and both predicates return false"
    - "Edge (CHKPT-01 encoding, explicit): marker matching is byte-exact on the quoted attribute (closing quote load-bearing); a CRLF plan declares the same checkpoint as its LF copy"
    - "Per CX-3, each declaration carries its whole task element (opening line through the first non-fenced line that closes the task element, the next non-fenced opening, or EOF) with per-line trailing whitespace trimmed and LF line endings, which D-07 compares"
    - "Real-plan fixtures copied from the plans research measured (F-3) classify as measured: 4 task-line declarations, 5 prose-only mentions that do not declare"
  prohibitions:
    - statement: "No full XML/Markdown parser crate is added; the scan stays std-only (D-06: 0 multi-line openings in 216 plans)"
      status: resolved
      verification: explicit
    - statement: "The resume predicate is not widened to `checkpoint:human-action` (Phase 47 D-02, operator-reconfirmed 2026-09-10)"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/verify.rs
      provides: "CheckpointDeclaration, parse_checkpoint_declarations, phase_checkpoint_declarations; both predicates delegate"
      contains: "pub fn phase_checkpoint_declarations"
  key_links:
    - from: crates/devflow-core/src/verify.rs::phase_has_blocking_human_checkpoint
      to: crates/devflow-core/src/verify.rs::phase_checkpoint_declarations
      via: "filter on blocking_human"
      pattern: "phase_checkpoint_declarations"
    - from: crates/devflow-cli/src/pipeline_launch.rs (resume decision, `checkpoint_confirmed`)
      to: crates/devflow-core/src/verify.rs::phase_has_blocking_human_checkpoint
      via: "unchanged public name and signature; caller-owned execution root (C-9)"
      pattern: "phase_has_blocking_human_checkpoint"
---

<!-- plan-criteria-allow: R4 - Git worktree discovery is a command precondition; this scanner joins it to later non-Git pipelines on the same Bash line. -->

<objective>
CHKPT-01 — ROADMAP alias GATE-01, backlog 999.125. Replace the two divergent plan scans with one parser whose
declarations both public predicates filter (D-06), with closed-fence exclusion and the fail-closed unclosed-fence
rule (R-3).

Purpose: today the resume route arms on a whole-file substring, so a prose mention of the marker can route a run into
the agent's auto-decide resume; five of this repository's own plans would do so (F-3). Output: `verify.rs` parser,
predicates, and tests. The element text this plan emits is the input CHKPT-02 (48-07) records and compares.

Contracts touched (P3): C-9 caller-owned roots (pinned by verify.rs tests near :447-487, must stay green); C-10
Phase 47 D-02 carve-out (pinned here by the control test); C-23 prose marker is not a declaration (verify.rs:572 test);
C-24 fenced-example limit (flipped).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-RESEARCH.md
@.planning/codebase/TESTING.md

RESEARCH sections to use: Findings F-3, F-4; Pattern 4 (rules in order); Pitfall 5. PATTERNS.md § 4 has the current
code excerpts and fixture conventions.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — one parser, both predicates delegate; a prose marker no longer arms resume</name>
  <files>crates/devflow-core/src/verify.rs</files>
  <read_first>
    - crates/devflow-core/src/verify.rs lines 100-230 (phase_plan_files, both predicates, marker constants and their docs) and the test module fixture helpers near 320-345 plus the prose-marker test near 571-592
    - crates/devflow-cli/src/preflight.rs lines 1060-1090 and crates/devflow-cli/src/pipeline_launch.rs lines 1590-1600 (the two production callers; read only, do not edit)
  </read_first>
  <behavior>
    - RED: `verify::tests::blocking_human_checkpoint_ignores_a_marker_mentioned_only_in_prose` — the prose-only fixture used by the existing human-only prose test makes `phase_has_blocking_human_checkpoint` return false (today it returns true: whole-file substring)
    - Control: `verify::tests::task_level_blocking_human_gate_is_human_only_on_both_predicates` — a task-opening line whose type is the human-verify checkpoint and whose gate attribute carries the blocking-human value (assembled from `HUMAN_GATE_VALUE`) makes both predicates true; a task-opening line whose type carries the human-action checkpoint value (assembled from `HUMAN_ACTION_TYPE_VALUE`) with a plain blocking gate makes only the human-only predicate true
  </behavior>
  <action>
Per D-06 (one shared checkpoint parser):
1. Write both tests first, following the file's fixture convention: marker values assembled at runtime from the existing `HUMAN_GATE_VALUE` / `HUMAN_ACTION_TYPE_VALUE` consts via `write_phase_file`, so no raw marker literal is written into test bodies. Run the RED test and confirm it fails on the assertion (not a compile error), then commit the tests.
2. Add `pub struct CheckpointDeclaration { pub plan_file: String, pub element: String, pub blocking_human: bool, pub human_action: bool }` (derive Debug, Clone, PartialEq, Eq). `plan_file` is the plan's file name only (e.g. `19-05-PLAN.md`), never an absolute path, so a worktree scan and a project-root scan key the same plan identically.
3. Add `pub fn parse_checkpoint_declarations(plan_file: &str, contents: &str) -> Vec<CheckpointDeclaration>`: a line opens a declaration only when its leading whitespace is followed by `<task` and then whitespace or `>` (F-4). Class flags come from the existing marker literals (`HUMAN_ONLY_CHECKPOINT_MARKERS`, keeping the closing-quote doc). Emit only declarations with at least one flag set. Fences are Task 2; element text is Task 3 — for now set `element` to the opening line.
4. Add `pub fn phase_checkpoint_declarations(project_root: &Path, phase: PhaseId) -> Vec<CheckpointDeclaration>` over `phase_plan_files(project_root, phase)`, skipping unreadable files as today.
5. Re-implement `phase_has_human_only_checkpoint` as `any(blocking_human || human_action)` and `phase_has_blocking_human_checkpoint` as `any(blocking_human)` over it. Keep both public names, signatures and the caller-owned-root docs; delete the whole-file substring const inside the resume predicate and the now-unused per-line helper.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in verify::tests::blocking_human_checkpoint_ignores_a_marker_mentioned_only_in_prose verify::tests::task_level_blocking_human_gate_is_human_only_on_both_predicates verify::tests::human_only_checkpoint_ignores_a_marker_mentioned_only_in_prose; do cargo test -p devflow-core --lib "$t" -- --exact > "$L/t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; if [ "$e" -ne 0 ] || [ "$ok" -ne 1 ] || [ -z "$fo" ]; then rc=1; fi; done; cargo test -p devflow-core --lib verify::tests:: > "$L/all.log" 2>&1; ae=$?; af=$(grep -c "test result: FAILED" "$L/all.log" || true); echo "verify_module_exit=$ae failed_lines=$af"; [ "$ae" -eq 0 ] && [ "$af" -eq 0 ] || rc=1; d=$(rg -c "pub fn phase_checkpoint_declarations" crates/devflow-core/src/verify.rs || echo 0); echo "shared_parser_fn=$d"; [ "$d" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test prints one_passed=0 or filtered=none, verify_module_exit non-zero, failed_lines above 0, or shared_parser_fn not 1</fails_when>
  </verify>
  <acceptance_criteria>
    - `git log --format=%s -- crates/devflow-core/src/verify.rs` shows a commit adding the RED test before the commit adding `phase_checkpoint_declarations`
    - The RED run's log (recorded in SUMMARY) shows `blocking_human_checkpoint_ignores_a_marker_mentioned_only_in_prose` failing on its assertion message, not a compile error
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Both predicates read one parser; the prose marker no longer arms the resume predicate; the task-level control holds on both.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Closed fences are ignored, unclosed fences fail closed, and the fenced-example limit is flipped</name>
  <files>crates/devflow-core/src/verify.rs</files>
  <read_first>
    - crates/devflow-core/src/verify.rs (as left by Task 1) — the parser, the doc paragraphs near 180-200 (known limit and the stale rationale), and the pinned fenced-example test near 605-630
  </read_first>
  <behavior>
    - `verify::tests::human_only_checkpoint_ignores_a_task_tag_inside_a_closed_fence` (the flipped known-limit test): fenced task tag → both predicates false
    - `verify::tests::unterminated_fence_before_a_real_task_still_declares_the_checkpoint` (R-3): an opening backtick fence never closed, followed by a real `blocking-human` task line → both predicates true
    - `verify::tests::tilde_fence_inside_backtick_fence_does_not_close_it`: backtick opener, a `~~~` line, a task tag, then the backtick closer → no declaration
    - `verify::tests::shorter_backtick_run_does_not_close_a_longer_fence`: a four-backtick opener, a three-backtick line, a task tag, then the four-backtick closer → no declaration
    - `verify::tests::fence_closer_with_trailing_text_does_not_close_a_fence` (CX-2): a syntactically opening backtick fence, a same-character run followed by text, a task tag, then a proper closer → no declaration
    - `verify::tests::task_opening_must_start_the_line`: a prose line containing, after other words, an inline-code task tag with a gate attribute (assembled from `TASK_ELEMENT_OPENING` and `HUMAN_GATE_VALUE`) → no declaration
  </behavior>
  <action>
Per D-06, R-3, and review concern CX-2: write the six tests first and confirm the fence tests fail. Then add fence tracking to `parse_checkpoint_declarations`: a fence opens on a line whose first non-space character is at column 3 or less and starts a run of 3+ backticks or 3+ tildes; record the character and run length; it closes only on a line with at most three leading spaces, the same character with a run at least as long, and whitespace only after that run. Lines inside a closed fence are skipped. If a fence is still open at EOF, rescan from that opening line to EOF with no fence tracking (fail closed) — implement as a second pass over that tail, not a toggle.

Rename the pinned fenced-example known-limit test (verify.rs near :609) to `human_only_checkpoint_ignores_a_task_tag_inside_a_closed_fence`, flip its assertion to `!phase_has_human_only_checkpoint(...)`, add the same negative assertion for `phase_has_blocking_human_checkpoint`, and rewrite its doc comment. Replace the known-limit doc paragraph (near :185-189) with the closed/unclosed fence rule, and replace the stale rationale paragraph (near :191-196) with the actual consequence: at its production call site a match arms the agent's auto-decide resume, so both predicates share one parser (D-06).
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in human_only_checkpoint_ignores_a_task_tag_inside_a_closed_fence unterminated_fence_before_a_real_task_still_declares_the_checkpoint tilde_fence_inside_backtick_fence_does_not_close_it shorter_backtick_run_does_not_close_a_longer_fence fence_closer_with_trailing_text_does_not_close_a_fence task_opening_must_start_the_line; do cargo test -p devflow-core --lib "verify::tests::$t" -- --exact > "$L/t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; if [ "$e" -ne 0 ] || [ "$ok" -ne 1 ] || [ -z "$fo" ]; then rc=1; fi; done; [ -f crates/devflow-core/src/verify.rs ] || { echo verify_rs_missing; rc=1; }; old=$(rg -c "fn human_only_checkpoint_still_matches_a_task_tag_inside_a_fenced_example" crates/devflow-core/src/verify.rs || echo 0); echo "old_known_limit_test=$old"; [ "$old" -eq 0 ] || rc=1; fs=$(rg -c "fails SAFE" crates/devflow-core/src/verify.rs || echo 0); echo "stale_rationale=$fs"; [ "$fs" -eq 0 ] || rc=1; cargo test -p devflow-core --lib verify::tests:: > "$L/all.log" 2>&1; ae=$?; echo "verify_module_exit=$ae"; [ "$ae" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any fence test one_passed=0 or filtered=none, old_known_limit_test above 0, stale_rationale above 0, or verify_module_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14 before the change: `old_known_limit_test` reads 1 and `stale_rationale` reads 1, so both gates discriminate
    - Before implementation, `unterminated_fence_before_a_real_task_still_declares_the_checkpoint` passes only if Task 1's parser has no fence handling; after implementation it must still pass (it guards against a toggle that fails open) — record both runs in SUMMARY
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Fence handling follows D-06 and R-3, the known limit is flipped, and the stale rationale is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Whole-element extraction, real-plan fixtures, and the empty/CRLF edges</name>
  <files>crates/devflow-core/src/verify.rs</files>
  <read_first>
    - crates/devflow-core/src/verify.rs (as left by Task 2)
    - .planning/milestones/v2.0.0-phases/19-release-integrity-main-rs-decomposition/19-05-PLAN.md line 83 and 19-11-PLAN.md line 160, .planning/milestones/v2.8.0-phases/44-codex-end-to-end-verification/44-04-PLAN.md line 168, and .planning/milestones/v2.0.0-phases/15-oss-readiness/15-05-PLAN.md line 73 (PI-2; locate with `rg --files .planning/milestones | rg "/(19-05|19-11|44-04|15-05)-PLAN\\.md$"`), plus the prose mentions in 33-02, 34-04, 43-01, 47-03, 47-05
  </read_first>
  <behavior>
    - `verify::tests::checkpoint_declaration_element_spans_to_its_closing_tag`: element = opening line through the first non-fenced line that closes the task element; a following non-fenced task opening ends an unclosed element; EOF ends the last
    - `verify::tests::checkpoint_declaration_element_ignores_a_fenced_closing_tag` (CX-3): a fenced `&lt;/task&gt;` before a later changed task-body line does not truncate the recorded element
    - `verify::tests::checkpoint_parser_matches_real_plan_fixtures`: task lines copied from 19-05, 19-11, 44-04 (blocking_human) and 15-05 (human_action only) each yield one declaration of the measured class; lines copied from 33-02, 34-04, 43-01, 47-03, 47-05 yield none
    - `verify::tests::crlf_plan_declares_the_same_checkpoint`: a CRLF copy of a fixture yields a declaration equal to the LF copy's (same element text)
    - `verify::tests::phase_without_plan_files_has_no_checkpoint_declarations`: missing phase dir, and a plan with no task lines → empty vector, both predicates false
  </behavior>
  <action>
Write the five tests first. Real-plan fixtures are copied into the test as string fixtures with the marker value substituted from the existing consts (per the file's convention); cite the source plan and line in a comment. Per review concern CX-3, make `element` the declaration's full text with the same fence-state machine and exact opening/closing rules Task 2 placed in `parse_checkpoint_declarations`: a fenced closing tag or opening tag is content, not an element boundary. Outside a fence, end on the first line whose trimmed content starts with the closing task tag, or up to (not including) the next opening line, or EOF. Normalize for comparison stability as D-07's discretion allows: strip a trailing `\r` and trailing whitespace from each line and join with `\n`; keep everything else byte-exact. Document the normalization and the shared fence rule on `CheckpointDeclaration::element`.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in checkpoint_declaration_element_spans_to_its_closing_tag checkpoint_declaration_element_ignores_a_fenced_closing_tag checkpoint_parser_matches_real_plan_fixtures crlf_plan_declares_the_same_checkpoint phase_without_plan_files_has_no_checkpoint_declarations; do cargo test -p devflow-core --lib "verify::tests::$t" -- --exact > "$L/t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; if [ "$e" -ne 0 ] || [ "$ok" -ne 1 ] || [ -z "$fo" ]; then rc=1; fi; done; cargo test -p devflow --bin devflow checkpoint > "$L/cli.log" 2>&1; ce=$?; cok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/cli.log" || true); echo "cli_checkpoint_tests_exit=$ce cli_ok_lines=$cok"; [ "$ce" -eq 0 ] && [ "$cok" -ge 1 ] || rc=1; cargo clippy -p devflow-core --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none, cli_checkpoint_tests_exit non-zero or cli_ok_lines=0 (the CLI callers' checkpoint tests regressed or matched nothing), or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --bin devflow checkpoint` runs at least the six `pipeline_launch::tests::advance_with_*checkpoint*` tests (the log names them) and all pass — the resume route's callers still behave on task-line declarations
    - The SUMMARY lists the nine real-plan fixture sources with their measured class and records the CX-3 fenced-closing-tag regression as a failing RED assertion before the shared fence scanner is implemented
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>Declarations carry normalized whole elements, real plans classify as measured, and the CLI callers' checkpoint tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-written PLAN.md → DevFlow resume decision | Agents may write plan files during Code (verify.rs:118-121); the parser's verdict decides whether auto-decide arms |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-02-01 | Elevation of Privilege | resume predicate whole-file match | high | mitigate | Shared line-start-anchored parser; RED prose-marker test (Task 1) |
| T-48-02-02 | Elevation of Privilege | fence tracking fails open (toggle desync hides a real declaration) | high | mitigate | Same-char, run-length close rule plus R-3 unclosed-fence rescan; unterminated-fence negative test (Task 2) |
| T-48-02-03 | Tampering | malformed plan text (CRLF, odd fences) | low | mitigate | Normalization and CRLF test (Task 3) |
</threat_model>

<verification>
All three gates print `gate_rc=0`; `cargo clippy -p devflow-core --all-targets -- -D warnings` exits 0.
</verification>

<success_criteria>
Preflight and resume read one parser; a prose marker neither blocks nor arms; a task-level gate does both; unclosed fences fail closed.
</success_criteria>

## Artifacts this phase produces

- `devflow_core::verify::CheckpointDeclaration` (fields `plan_file`, `element`, `blocking_human`, `human_action`)
- `devflow_core::verify::parse_checkpoint_declarations(plan_file: &str, contents: &str) -> Vec<CheckpointDeclaration>`
- `devflow_core::verify::phase_checkpoint_declarations(project_root: &Path, phase: PhaseId) -> Vec<CheckpointDeclaration>`
- Tests in `verify::tests`: `blocking_human_checkpoint_ignores_a_marker_mentioned_only_in_prose`, `task_level_blocking_human_gate_is_human_only_on_both_predicates`, `human_only_checkpoint_ignores_a_task_tag_inside_a_closed_fence`, `unterminated_fence_before_a_real_task_still_declares_the_checkpoint`, `tilde_fence_inside_backtick_fence_does_not_close_it`, `shorter_backtick_run_does_not_close_a_longer_fence`, `task_opening_must_start_the_line`, `checkpoint_declaration_element_spans_to_its_closing_tag`, `checkpoint_parser_matches_real_plan_fixtures`, `crlf_plan_declares_the_same_checkpoint`, `phase_without_plan_files_has_no_checkpoint_declarations`

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-02-SUMMARY.md` when done.
</output>

## Review Dispositions Ledger

### Round 1 — e62eca9

### Review Feedback Addressed

| Concern | Severity | How Addressed |
|---------|----------|---------------|
| CX-2 L43@e62eca9 — fence closer accepts trailing text | MEDIUM | Plan 48-02 Task 2 now requires whitespace-only closing-fence tails and adds the opposite-result regression test. |
| CX-3 L50@e62eca9 — fenced closing tag truncates element | HIGH | Plan 48-02 Task 3 reuses Task 2 fence tracking for element boundaries and tests a fenced closing tag before a changed task body. |
| PI-2 L83@e62eca9 — fixture paths are stale | LOW | Plan 48-02 Task 3 names the four verified milestone archive paths and retains the bounded `rg --files` locator. |

### Review Feedback Deferred

| Concern | Reason |
|---------|--------|
| None | Every actionable finding assigned to Plan 48-02 is implemented in its task contract. |
