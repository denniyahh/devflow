---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 06
type: execute
wave: 3
depends_on: ["48-03", "48-05"]
files_modified:
  - crates/devflow-cli/src/staleness.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/src/pipeline_gate.rs
  - crates/devflow-cli/src/test_support.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 55000
  raw_tokens: 110000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Per D-08, no test in `staleness.rs`, `preflight.rs` or `pipeline_gate.rs` mutates process-global `PATH`; each former block runs through `run_test_in_child` guarded by `assert_child_ran_exactly_one_passing_test` with a module-qualified name"
    - "Per D-10, the 3 abort-fixture tests in `preflight.rs` and the 2 in `pipeline_gate.rs` run in a child with an agent-free `PATH` directory (fixture-coverage gate reads 0; it read 3 and 2 on 2026-09-14)"
    - "`staleness::tests::ahead_build_from_descendant_commit_warns_instead_of_blocking` — 999.38's observed flake victim — passes, and `staleness.rs` itself replaces `PATH` nowhere"
    - "No test is deleted: `#[test]` counts are at least 30 (staleness.rs), 61 (preflight.rs), 17 (pipeline_gate.rs) — their counts at c0fb193"
    - "Per D-08, `prepend_path` is deleted from `crates/devflow-cli/src/test_support.rs` in Task 3. 48-05 removes its `pipeline_launch.rs` callers and this plan removes the last ones (`staleness.rs`, `preflight.rs`), so this plan leaves it uncalled; an uncalled `pub(crate)` helper in that `#[cfg(test)]` module of a binary crate fails `clippy -D warnings` on `dead_code`. That is why this plan runs after 48-05 rather than beside it. Any other helper with no caller left after 48-05 and this plan is deleted in the same task"
  prohibitions:
    - statement: "No production code changes in these three files (D-08); preflight recording is 48-07 scope"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-cli/src/preflight.rs
      provides: "PATH-isolated preflight tests"
      contains: "run_test_in_child"
    - path: crates/devflow-cli/src/pipeline_gate.rs
      provides: "PATH-isolated gate tests"
      contains: "run_test_in_child"
    - path: crates/devflow-cli/src/staleness.rs
      provides: "PATH-isolated staleness test"
  key_links:
    - from: crates/devflow-cli/src/preflight.rs tests
      to: crates/devflow-core/src/test_support.rs::run_test_in_child
      via: "parent/child split"
      pattern: "in_child_test"
---

<!-- plan-criteria-allow: R4 - The c0fb193 baseline object is explicitly checked before its content is read; the scanner also joins root setup to later pipelines. -->

<objective>
TEST-01 — ROADMAP alias 999.38, with backlog 999.80 folded in (D-10). Convert the PATH blocks in `staleness.rs`
(3 lines), `preflight.rs` (21 lines; 999.38 names :627/:701) and `pipeline_gate.rs` (12 lines; 999.38 names two
trailing-restore regions), plus the abort-fixture tests in `preflight.rs` (near :1850, :2022, :2061) and
`pipeline_gate.rs` (near :1085, :1405), per D-08 and D-10.

Output: converted tests, and `prepend_path` deleted from `test_support.rs`. Contracts touched (P3): C-11 preflight
D-08/D-09 test `unattended_check_is_not_bypassed_by_yes_ship` must stay green; C-17 (none located; untouched).

Ordering: this plan depends on 48-05 as well as 48-03. Together they remove every caller of `prepend_path`
(`pipeline_launch.rs` in 48-05; `staleness.rs` and `preflight.rs` here), and the plan that removes the last caller
deletes the helper. Running beside 48-05 would leave it uncalled at the wave merge, where `scripts/check.sh all`
fails clippy on `dead_code` with no plan owning the fix.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-05-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — convert staleness.rs's PATH block, the file whose test 999.38 caught flaking</name>
  <files>crates/devflow-cli/src/staleness.rs</files>
  <read_first>
    - crates/devflow-cli/src/staleness.rs test module (locate the block with `rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" crates/devflow-cli/src/staleness.rs` and read its test; also `run_git_stdout` near :106)
    - crates/devflow-cli/src/test_support.rs lines 320-530 and 745-805; crates/devflow-core/src/test_support.rs
  </read_first>
  <action>
Per D-08: convert the staleness.rs test that replaces `PATH` to the parent/child shape (module-qualified `NAME`, parent-built directory `PATH`, repo root passed as an extra child variable, guard). Its child `PATH` must still contain `git` if the body runs git (symlink it into the directory), because a directory without `git` is only correct for tests asserting git is absent.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; f=crates/devflow-cli/src/staleness.rs; pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); g=$(grep -v "^[[:space:]]*//" "$f" | grep -c "assert_child_ran_exactly_one_passing_test(" || true); echo "path_mutation_lines=$pm child_guards=$g"; [ "$pm" -eq 0 ] && [ "$g" -ge 1 ] || rc=1; t=staleness::tests::ahead_build_from_descendant_commit_warns_instead_of_blocking; cargo test -p devflow --bin devflow "$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "flake_victim_one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; cargo test -p devflow --bin devflow staleness::tests:: > "$L/m.log" 2>&1; e=$?; mok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$mok"; [ "$e" -eq 0 ] && [ "$mok" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; path_mutation_lines above 0 (read 3 before), child_guards=0, flake victim one_passed=0 or filtered=none, or the staleness module run failing</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14 before the change: comment-stripped PATH mutation lines in staleness.rs read 3
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>staleness.rs no longer replaces PATH and its flake-victim test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Convert preflight.rs PATH blocks and its three abort-fixture tests</name>
  <files>crates/devflow-cli/src/preflight.rs</files>
  <read_first>
    - Every test enclosing a hit of `rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" -e "abort: test cleanup" crates/devflow-cli/src/preflight.rs`
    - `agent_binary_available` near preflight.rs:65-79 (in-process PATH lookup the child PATH must satisfy or deny deliberately)
  </read_first>
  <action>
Per D-08 and D-10: convert each PATH-replacing test and each abort-fixture test (near :1850, :2022, :2061) to guarded child runs with an agent-free `PATH` directory (stub agent only where the test needs one available). Fixture helpers get the `CHILD_TEST_ENV` assertion described in 48-05 Task 2. Leave the 10 non-PATH environment mutations for 48-09.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; git cat-file -e c0fb193^{commit} || { echo base_missing; exit 1; }; f=crates/devflow-cli/src/preflight.rs; pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); nf=$(awk "/^    (pub(\\(crate\\))? )?fn /{name=\$0} /CHILD_TEST_ENV|in_child_test\\(/{c[name]=1} /abort: test cleanup/ && !/^[[:space:]]*\\/\\//{f[name]=1} END{b=0; for (n in f) if (!(n in c)) b++; print b}" "$f"); bt=$(git show "c0fb193:$f" | grep -c "#\[test\]"); nt=$(grep -c "#\[test\]" "$f"); echo "path_mutation_lines=$pm fixture_fns_without_child=$nf base_tests=$bt now_tests=$nt"; [ "$pm" -eq 0 ] && [ "$nf" -eq 0 ] && [ "$nt" -ge "$bt" ] || rc=1; cargo test -p devflow --bin devflow preflight::tests::unattended_check_is_not_bypassed_by_yes_ship -- --exact > "$L/c11.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/c11.log" || true); echo "c11_one_passed=$ok"; [ "$ok" -eq 1 ] || rc=1; cargo test -p devflow --bin devflow preflight::tests:: > "$L/m.log" 2>&1; e=$?; mok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$mok"; [ "$e" -eq 0 ] && [ "$mok" -eq 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; path_mutation_lines (read 21 before) or fixture_fns_without_child (read 3 before) above 0, now_tests below base_tests, c11_one_passed not 1 (C-11 test renamed, moved or failing), or the preflight module run failing</fails_when>
  </verify>
  <acceptance_criteria>
    - If `preflight::tests::unattended_check_is_not_bypassed_by_yes_ship` is not the exact module path of the C-11 test, the executor locates it with `rg -n "fn unattended_check_is_not_bypassed_by_yes_ship"`, uses the real path, and records the correction in SUMMARY — the gate must not be weakened to a substring filter
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>preflight.rs tests are PATH-isolated and its abort-fixture tests are structurally agent-free.</done>
</task>

<task type="auto">
  <name>Task 3: Convert pipeline_gate.rs PATH blocks and its two abort-fixture tests, then delete the helper nothing calls</name>
  <files>crates/devflow-cli/src/pipeline_gate.rs, crates/devflow-cli/src/test_support.rs</files>
  <read_first>
    - Every test enclosing a hit of `rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" -e "abort: test cleanup" crates/devflow-cli/src/pipeline_gate.rs`
    - crates/devflow-cli/src/test_support.rs lines 1-14 (module doc: the `dead_code` trap) and `prepend_path` (near :537)
  </read_first>
  <action>
Per D-08 and D-10: same conversion as Task 2 for `pipeline_gate.rs` (12 PATH lines; fixtures near :1085, :1405). Leave its 6 non-PATH environment mutations for 48-09.

Then, per D-08, delete `prepend_path` from `crates/devflow-cli/src/test_support.rs`. First confirm nothing outside that file still calls it with `rg -n "prepend_path\(" crates --glob "*.rs"`: on 2026-09-14 its 21 callers were all in `pipeline_launch.rs` (48-05), `staleness.rs` and `preflight.rs` (Tasks 1-2). If a caller remains, stop and report which plan missed it rather than keeping the helper. Delete any other helper in that file with no caller left after 48-05 and this plan, and name each deletion in SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; git cat-file -e c0fb193^{commit} || { echo base_missing; exit 1; }; f=crates/devflow-cli/src/pipeline_gate.rs; pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); nf=$(awk "/^    (pub(\\(crate\\))? )?fn /{name=\$0} /CHILD_TEST_ENV|in_child_test\\(/{c[name]=1} /abort: test cleanup/ && !/^[[:space:]]*\\/\\//{f[name]=1} END{b=0; for (n in f) if (!(n in c)) b++; print b}" "$f"); bt=$(git show "c0fb193:$f" | grep -c "#\[test\]"); nt=$(grep -c "#\[test\]" "$f"); echo "path_mutation_lines=$pm fixture_fns_without_child=$nf base_tests=$bt now_tests=$nt"; [ "$pm" -eq 0 ] && [ "$nf" -eq 0 ] && [ "$nt" -ge "$bt" ] || rc=1; ts=crates/devflow-cli/src/test_support.rs; [ -f "$ts" ] || { echo test_support_missing; rc=1; }; pd=$(grep -v "^[[:space:]]*//" "$ts" | grep -c "fn prepend_path" || true); pc=$(rg -n "prepend_path\(" crates --glob "*.rs" | rg -v -e "^[^:]+:[0-9]+:\s*//" | wc -l); echo "prepend_path_defs=$pd prepend_path_code_refs=$pc"; [ "$pd" -eq 0 ] && [ "$pc" -eq 0 ] || rc=1; cargo test -p devflow --bin devflow pipeline_gate::tests:: > "$L/m.log" 2>&1; e=$?; mok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$mok"; [ "$e" -eq 0 ] && [ "$mok" -eq 1 ] || rc=1; cargo clippy -p devflow --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; path_mutation_lines (read 12 before) or fixture_fns_without_child (read 2 before) above 0, now_tests below base_tests, prepend_path_defs above 0 (read 1 before) or prepend_path_code_refs above 0 (read 22 at c0fb193: 21 callers plus the definition), test_support_missing printed, module run failing, or clippy_exit non-zero (including `dead_code` on a helper this plan left uncalled)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY names every `test_support.rs` helper this task deleted
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>pipeline_gate.rs tests are PATH-isolated, its abort-fixture tests are structurally agent-free, and no test helper is left uncalled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo test process → spawned agent CLI | Preflight and gate tests that reach a launch with a real agent on PATH |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-06-01 | Elevation of Privilege | abort-fixture tests (999.80) | medium | mitigate | Child with agent-free PATH; helper child-mode assertion |
| T-48-06-02 | Denial of Service | vacuous child | medium | mitigate | Guards; test counts ≥ base |
</threat_model>

<verification>
All three gates print `gate_rc=0`.
</verification>

<success_criteria>
The three files have no process-global PATH mutation and no content-only agent-spawn protection.
</success_criteria>

## Artifacts this phase produces

- No new production symbols. Per-test `NAME` constants in converted tests.
- Deleted: `prepend_path` in `crates/devflow-cli/src/test_support.rs`, plus any helper SUMMARY names as left without callers after 48-05 and this plan

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-06-SUMMARY.md` when done.
</output>
