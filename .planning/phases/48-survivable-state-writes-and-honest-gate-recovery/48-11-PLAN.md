---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 11
type: tdd
wave: 6
depends_on: ["48-09"]
files_modified:
  - crates/devflow-core/src/lock.rs
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/main.rs
autonomous: true
requirements: [SURV-01]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-02.2, `advance` waits for the per-phase lock with a bounded wait (`ADVANCE_LOCK_WAIT`, 10 minutes, a chosen number not a measured one per D-03) instead of refusing immediately; the wait retries through `acquire`, so a dead holder's lock is reclaimed"
    - "Per D-02.2, when the wait expires `advance` emits `advance_failed` with the phase and the holder pid and returns an error (the Legacy monitor sends output to /dev/null, so the event is the only record)"
    - "Per R-2, the monitor passes the stage it launched (`--stage` in the Legacy `advance` tail and in the PipeOwning `__monitor` args); after acquiring the lock, `advance` refuses with `advance_failed` when `state.stage` no longer matches"
    - "Per F-8, `--stage` is optional: an `advance` without it (a script written by an older binary) skips the stage check and emits `advance_stage_unbound`; it never fails for the missing flag"
    - "`lock::acquire_blocking(project_root, phase, timeout)` waits for release and times out with `LockError::Contended` against a live holder"
    - "Edge (SURV-01 boundary, explicit): with the holder releasing before the bound, `advance` proceeds; with the bound shorter than the hold, it fails with `advance_failed` — tests control the lock directly, not by timing races"
    - "Per D-03 (informational): two concurrent monitors for the same stage both pass the stage check; a launcher holding the lock past the bound still stalls, now logged and recoverable via doctor → resume"
    - "`advance` and `__monitor` stay hidden: `help_snapshot` passes unchanged"
  prohibitions:
    - statement: "No environment variable controls the lock-wait bound (would add operator surface and a test-time env mutation D-09 forbids)"
      status: resolved
      verification: explicit
    - statement: "A missing `--stage` is never a hard error"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/lock.rs
      provides: "acquire_blocking"
      contains: "pub fn acquire_blocking"
    - path: crates/devflow-cli/src/pipeline_launch.rs
      provides: "ADVANCE_LOCK_WAIT, bounded wait and stage binding in advance, stage threaded through run_monitor"
      contains: "ADVANCE_LOCK_WAIT"
    - path: crates/devflow-core/src/monitor.rs
      provides: "--stage in the Legacy tail and __monitor args"
    - path: crates/devflow-cli/src/main.rs
      provides: "--stage on hidden Advance and Monitor subcommands"
  key_links:
    - from: crates/devflow-core/src/monitor.rs advance tail
      to: crates/devflow-cli/src/main.rs Advance { stage }
      via: "`--stage {state.stage}` parsed back through `Stage`'s Display/FromStr"
      pattern: "--stage"
    - from: crates/devflow-cli/src/pipeline_launch.rs::advance
      to: crates/devflow-core/src/lock.rs::acquire_blocking
      via: "bounded wait"
      pattern: "acquire_blocking"
---

<objective>
SURV-01 writer rule, `advance` half (D-02.2, R-2, F-8). `advance` waits for the per-phase lock instead of failing, and
refuses loudly when the stage it was launched for has moved on.

Purpose: once `start` holds the lock across spawning (48-12), a fast-exiting agent's `advance` would otherwise fail
immediately into /dev/null. This plan must land before 48-12. Output: lock primitive, advance changes, monitor and CLI
flag wiring, tests.

Contracts touched (P3): C-18/C-19 (per-phase lock, reclaim — relied on), C-20 (14-CR-06 `advance_failed` logging —
reused), C-21 (WR-04/18b monitor_pid save — untouched).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md

RESEARCH Pattern 3 (bound rationale), Finding F-8, Pitfall 3; PATTERNS.md § 3, § 7, § 15, § 18.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — acquire_blocking in core, bounded wait in advance, expiry logged</name>
  <files>crates/devflow-core/src/lock.rs, crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - crates/devflow-core/src/lock.rs lines 1-125, 170-215, and the blocking tests near 430-470
    - crates/devflow-cli/src/pipeline_launch.rs lines 900-990 (`run_monitor`) and 1450-1520 (`advance`, `advance_failed` emission, the duplicate-exclusion comment)
    - crates/devflow-cli/src/pipeline_launch.rs test helpers used by `advance_*` tests (fixture state, `events_of_kind`)
  </read_first>
  <behavior>
    - `lock::tests::phase_lock_blocking_waits_for_release` and `lock::tests::phase_lock_blocking_times_out_against_live_holder` (mirror the project-lock pair)
    - RED: `pipeline_launch::tests::advance_waits_for_a_held_lock_then_proceeds`: a thread holds `lock::acquire(root, phase)` and drops it after 300 ms; `advance` with a 10 s wait returns Ok and saves state (today: immediate "already running" error)
    - `pipeline_launch::tests::advance_lock_wait_expiry_emits_advance_failed`: lock held for the whole test; a 300 ms wait → Err, and one `advance_failed` event whose payload names the phase and holder pid
  </behavior>
  <action>
Per D-02.2: tests first (RED confirmed). Add `pub fn acquire_blocking(project_root: &Path, phase: PhaseId, timeout: Duration) -> Result<LockGuard, LockError>` copying `acquire_project_blocking`'s loop around `acquire`. In pipeline_launch.rs add `pub(crate) const ADVANCE_LOCK_WAIT: Duration = Duration::from_secs(600);` with a doc comment giving the rationale (post-spawn holders do seconds of work; a holder present after minutes is at a gate or hung, and D-03 accepts doctor → resume as the recovery; chosen, not measured). Split `advance` into the public entry and an inner `advance_with(project_root, phase, stage: Option<Stage>, lock_wait: Duration)` used by tests; on `LockError::Contended { pid, .. }` after the wait, emit `advance_failed` with `{"reason": "lock wait expired", "holder_pid": pid, "waited_secs": ...}` for the resolved phase and return an error naming the holder. Rewrite the comment that says a duplicate advance is excluded by the lock (a bounded wait means it queues; R-2's stage check handles it). If a test's evaluation path can reach `launch_stage`, run it through the 48-03 child helper with an agent-free PATH.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in lock::tests::phase_lock_blocking_waits_for_release lock::tests::phase_lock_blocking_times_out_against_live_holder; do cargo test -p devflow-core --lib "$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; for t in pipeline_launch::tests::advance_waits_for_a_held_lock_then_proceeds pipeline_launch::tests::advance_lock_wait_expiry_emits_advance_failed; do cargo test -p devflow --bin devflow "$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; ar=$(grep -v "^[[:space:]]*//" crates/devflow-cli/src/pipeline_launch.rs | grep -c "acquire_blocking(" || true); echo "advance_uses_blocking_acquire=$ar"; [ "$ar" -ge 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none, or advance_uses_blocking_acquire=0</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY records `advance_waits_for_a_held_lock_then_proceeds` failing before the change with the "already running" error text
    - Negative control, measured 2026-09-14: `pub fn acquire_blocking` occurs 0 times in lock.rs before the change
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>A monitor's advance queues behind the lock holder and a stall is recorded in events.jsonl.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind advance to its launched stage — optional --stage from both monitor shapes</name>
  <files>crates/devflow-core/src/monitor.rs, crates/devflow-cli/src/main.rs, crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - crates/devflow-core/src/monitor.rs lines 300-330, 415-490 (`__monitor` args, Legacy `advance_tail`) and the existing script-shape tests (locate with `rg -n "advance_tail|fn .*script" crates/devflow-core/src/monitor.rs`)
    - crates/devflow-cli/src/main.rs lines 110-165 (hidden `Advance`, `Monitor`) and their dispatch near 575-600
    - crates/devflow-cli/src/pipeline_launch.rs `advance_with` and `run_monitor` (as left by Task 1)
  </read_first>
  <behavior>
    - `monitor::tests::legacy_advance_tail_passes_the_launched_stage`: rendered Legacy script contains `advance <root> --phase N --stage code` for a Code launch
    - `monitor::tests::pipe_owning_monitor_args_pass_the_stage`: the `__monitor` argv contains `--stage` followed by the stage before `--`
    - `pipeline_launch::tests::advance_refuses_when_the_saved_stage_moved_on` (R-2): state at Validate, `advance_with(.., Some(Stage::Code), ..)` → Err, one `advance_failed` naming expected and actual stage, state file bytes unchanged
    - `pipeline_launch::tests::advance_without_stage_proceeds_and_records_the_legacy_invocation` (F-8): `None` → no stage refusal, one `advance_stage_unbound` event
    - Control: `Some(state.stage)` proceeds with no refusal event
  </behavior>
  <action>
Per R-2 and F-8: tests first. main.rs: add `#[arg(long)] stage: Option<Stage>` to the hidden `Advance` and `Monitor` variants and pass it through dispatch. monitor.rs: append `--stage {state.stage}` to the Legacy tail and add `.arg("--stage").arg(state.stage.to_string())` before `--` in the `__monitor` args. pipeline_launch.rs: thread `stage: Option<Stage>` through `run_monitor` into `advance_with`; after loading state under the lock, `Some(expected) if expected != state.stage` → emit `advance_failed` with `{"reason": "stage mismatch", "expected": .., "actual": ..}` and return an error; `None` → emit `advance_stage_unbound` and continue. Confirm `Stage` round-trips through its CLI parser using an existing value parser; do not add a new string format.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in legacy_advance_tail_passes_the_launched_stage pipe_owning_monitor_args_pass_the_stage; do cargo test -p devflow-core --lib "monitor::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; for t in advance_refuses_when_the_saved_stage_moved_on advance_without_stage_proceeds_and_records_the_legacy_invocation; do cargo test -p devflow --bin devflow "pipeline_launch::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; cargo test -p devflow --test help_snapshot > "$L/h.log" 2>&1; he=$?; hok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/h.log" || true); echo "help_snapshot_exit=$he help_ok=$hok"; [ "$he" -eq 0 ] && [ "$hok" -eq 1 ] || rc=1; cargo test -p devflow --bin devflow pipeline_launch::tests:: > "$L/m.log" 2>&1; me=$?; echo "launch_module_exit=$me"; [ "$me" -eq 0 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none, help_snapshot failing or matching no tests, the launch module run failing, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14: the Legacy tail format string `advance {project_root} --phase {phase}` occurs once in monitor.rs with no `--stage`; after the change the rendered-script test proves `--stage` is present
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>A queued advance cannot evaluate a stage it was not launched for, and scripts from older binaries still advance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| monitor process → `devflow advance` | A detached script from possibly another binary version invokes `advance` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-11-01 | Tampering | queued advance evaluates a moved-on stage | high | mitigate | R-2 stage binding with `advance_failed` refusal |
| T-48-11-02 | Denial of Service | advance waits forever | medium | mitigate | 10-minute bound + `advance_failed` event |
| T-48-11-03 | Repudiation | refusal invisible (/dev/null) | medium | mitigate | Every refusal emits an event with phase, reason and holder/stages |
| T-48-11-04 | Tampering | old script without `--stage` | low | accept | Unbound path proceeds and records `advance_stage_unbound` (F-8) |
</threat_model>

<verification>
Both gates print `gate_rc=0`.
</verification>

<success_criteria>
`advance` queues behind the lock, refuses a moved-on stage, and every refusal is recorded.
</success_criteria>

## Artifacts this phase produces

- `devflow_core::lock::acquire_blocking(project_root: &Path, phase: PhaseId, timeout: Duration) -> Result<LockGuard, LockError>`
- `pipeline_launch::ADVANCE_LOCK_WAIT: Duration` (600 s), `pipeline_launch::advance_with(project_root, phase, stage: Option<Stage>, lock_wait: Duration)`
- CLI flags: `devflow advance --stage <STAGE>` and `devflow __monitor --stage <STAGE>` (both hidden)
- Events: `advance_failed` payload reasons `lock wait expired` and `stage mismatch`; new event `advance_stage_unbound`
- Tests named in the two `<behavior>` blocks

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-11-SUMMARY.md` when done.
</output>
