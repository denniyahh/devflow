---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/test_support.rs
  - crates/devflow-core/src/agents/pi.rs
  - crates/devflow-core/src/agents/opencode.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-08, `devflow_core::test_support::run_test_in_child` re-invokes the current test binary with the module-qualified test name, `--exact` and `--test-threads=1`, setting `PATH` (always a directory, never removed) and any extra variables on the child `Command` only"
    - "Per D-08, every child run is guarded by `devflow_core::test_support::assert_child_ran_exactly_one_passing_test` (exit 0, `test NAME ... ok`, `1 passed`, non-zero `filtered out`), and a misspelled test name makes that guard panic (negative control)"
    - "A child given an empty `PATH` directory cannot spawn `git` while the parent can (discriminating control for the isolation itself)"
    - "Per D-08, no test in `agents/pi.rs` or `agents/opencode.rs` mutates process-global `PATH`; `PathGuard` is gone from both files; the `PI_CODING_AGENT_DIR` override moves onto the child `Command`"
    - "Edge (TEST-01 empty, explicit): `run_test_in_child` panics when the given `PATH` value is not an existing directory, because a removed `PATH` falls back to an OS default that still resolves `git`"
    - "Edge (TEST-01 adjacency, explicit): the child-mode marker carries the exact test name, so a child half runs only in the test it was spawned for"
    - "The full-suite wall time before any TEST-01 conversion is recorded in the SUMMARY (D-08 'Not measured'), with its measurement limits stated"
  prohibitions:
    - statement: "`devflow_core::test_support` gains no `tempfile` use outside `#[cfg(test)]` — the module compiles under the `test-support` feature without dev-dependencies"
      status: resolved
      verification: explicit
    - statement: "No production code changes (D-08)"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-core/src/test_support.rs
      provides: "run_test_in_child, in_child_test, assert_child_ran_exactly_one_passing_test, CHILD_TEST_ENV"
      contains: "pub fn run_test_in_child"
    - path: crates/devflow-core/src/agents/pi.rs
      provides: "pi driver tests running in child processes"
    - path: crates/devflow-core/src/agents/opencode.rs
      provides: "opencode driver tests running in child processes"
  key_links:
    - from: crates/devflow-core/src/agents/pi.rs tests
      to: crates/devflow-core/src/test_support.rs::run_test_in_child
      via: "parent half builds the stub dir and spawns; child half runs the driver call"
      pattern: "run_test_in_child"
---

<!-- plan-criteria-allow: R4 - Git worktree discovery is a command precondition; this scanner joins it to later non-Git pipelines on the same Bash line. -->

<objective>
TEST-01 — ROADMAP alias 999.38 (999.80 folded in, D-10). Land the generic child-process test helper in devflow-core
(D-08) and convert the devflow-core `PATH` mutators (`PathGuard` in `agents/pi.rs`, 8 call sites, and
`agents/opencode.rs`, 7 call sites — 999.38's third site family).

Purpose: the devflow-core lib reported 668 passed / 101 failed once, every panic a spawn `NotFound`, because these
guards replace `PATH` while `git.rs`, `hooks.rs`, `version.rs` and `worktree.rs` tests spawn concurrently. The helper
is also what every devflow-cli conversion (48-04, 48-05, 48-06, 48-08) calls. Output: helper, its tests, converted
driver tests, and the pre-conversion suite timing.

Contracts touched (P3): C-25 `env_lock` premise (cli; untouched here, re-verified in 48-09).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/codebase/TESTING.md

RESEARCH Pattern 8 (helper, PATH-must-be-a-directory probe, cost probe) and PATTERNS.md § 8-9 (compile hazard: no
`tempfile` in this module outside `#[cfg(test)]`; do not copy the substring re-exec at monitor.rs:2854-2882).
</context>

<!-- `tempfile` and `PathGuard` are negative checks on comment-stripped code, and the actions must name both; the helper-call literal is a positive count. -->
<!-- planner-discipline-allow: tempfile -->
<!-- planner-discipline-allow: PathGuard -->
<!-- planner-discipline-allow: assert_child_ran_exactly_one_passing_test( -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — record the baseline, land the helper with its controls, and convert one pi test end to end</name>
  <files>crates/devflow-core/src/test_support.rs, crates/devflow-core/src/agents/pi.rs</files>
  <read_first>
    - crates/devflow-core/src/test_support.rs (whole file) and crates/devflow-core/src/lib.rs lines 75-85 (the `#[cfg(any(test, feature = "test-support"))]` gate)
    - crates/devflow-cli/src/test_support.rs lines 360-490 (`run_test_without_git`, `assert_child_ran_exactly_one_passing_test`) and lines 745-805 (the parent/child test shape)
    - crates/devflow-core/src/agents/pi.rs test module from line 180 (ENV_MUTEX, `PathGuard`, `EnvGuard`, stub builders, `preflight_invokes_pi_auth_check_and_accepts_ready`)
  </read_first>
  <action>
Before any edit, per D-08's "Not measured": run `/usr/bin/time -p scripts/check.sh test` once from the worktree root, log to a file, and record in the SUMMARY under `TEST-01 baseline` the `real` seconds and each binary's `test result:` line. State the limits: one run, other wave-1 executors may be running concurrently, and a red result is recorded, not fixed (it is the flake being removed). This run is a D-08 measurement, not this task's feedback loop: nothing narrower measures full-suite wall time, and the task's automated gate below stays the targeted tests. Launch it with the Bash tool's background mode and a log file (cold runs take minutes), wait for it to finish before the first edit so the baseline measures the unmodified tree, then read the log.

In `crates/devflow-core/src/test_support.rs` add (all `pub`, std-only, no `tempfile`):
- `pub const CHILD_TEST_ENV: &str = "DEVFLOW_CHILD_TEST";` — its value is the exact module-qualified test name.
- `pub fn in_child_test(test_name: &str) -> bool` — true only when `CHILD_TEST_ENV` equals `test_name` (reading the environment is not a mutation).
- `pub fn run_test_in_child(test_name: &str, path_dir: &Path, extra_env: &[(&str, &OsStr)]) -> std::process::Output` — panics with a clear message unless `path_dir.is_dir()`; runs `std::env::current_exe()` with args `test_name`, `--exact`, `--test-threads=1`, `.env("PATH", path_dir)`, `.env(CHILD_TEST_ENV, test_name)`, and each extra pair via `.env`. The caller owns any temporary directory.
- `pub fn assert_child_ran_exactly_one_passing_test(output: &std::process::Output, test_name: &str)` — the same four assertions as the cli copy (exit success; stdout contains `test {test_name} ... ok`; `1 passed`; a non-zero `filtered out` count), printing child stdout/stderr on failure.

Add a `#[cfg(test)] mod tests` there with:
- `test_support::tests::child_with_an_empty_path_dir_cannot_spawn_git_while_the_parent_can` — parent asserts `git --version` spawns; child half (under `in_child_test`) asserts `Command::new("git")` fails with `ErrorKind::NotFound`; parent passes a fresh empty `tempfile::tempdir()`.
- `test_support::tests::child_guard_rejects_a_name_that_matches_no_test` — `#[should_panic]` with an `expected` substring from the guard's `1 passed` assertion; calls `run_test_in_child` with a module-qualified name that does not exist, then the guard.
- `test_support::tests::run_test_in_child_refuses_a_path_that_is_not_a_directory` — `#[should_panic]`, passes a path that does not exist.

Convert `agents::pi::tests::preflight_invokes_pi_auth_check_and_accepts_ready` to the parent/child shape: the parent builds the stub dir with the existing builder, then calls `run_test_in_child(NAME, stub_dir.path(), &[("PI_CODING_AGENT_DIR", stub_dir.path().as_os_str())])` and the guard; the child half runs `PiDriver.health(&test_state())` and its assertions with no `ENV_MUTEX`, `PathGuard` or `EnvGuard`. If the stub script needs utilities beyond its `#!/bin/sh` interpreter, the parent symlinks exactly those into the stub dir; never append the developer's `PATH`.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for t in test_support::tests::child_with_an_empty_path_dir_cannot_spawn_git_while_the_parent_can test_support::tests::child_guard_rejects_a_name_that_matches_no_test test_support::tests::run_test_in_child_refuses_a_path_that_is_not_a_directory agents::pi::tests::preflight_invokes_pi_auth_check_and_accepts_ready; do cargo test -p devflow-core --lib "$t" -- --exact > "$L/t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; if [ "$e" -ne 0 ] || [ "$ok" -ne 1 ] || [ -z "$fo" ]; then rc=1; fi; done; tf=$(sed -n "/^#\[cfg(test)\]/q;p" crates/devflow-core/src/test_support.rs | grep -v "^[[:space:]]*//" | grep -c "tempfile" || true); echo "tempfile_outside_cfg_test=$tf"; [ "$tf" -eq 0 ] || rc=1; cargo check -p devflow --tests > "$L/chk.log" 2>&1; ce=$?; echo "cli_tests_build_with_feature_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; any listed test one_passed=0 or filtered=none, tempfile_outside_cfg_test above 0, or cli_tests_build_with_feature_exit non-zero (the test-support feature build broke)</fails_when>
  </verify>
  <acceptance_criteria>
    - `child_guard_rejects_a_name_that_matches_no_test` passing proves the guard fails a vacuous child run (the negative control); if it were removed or its `expected` text did not match, it would fail
    - The converted pi test's child half contains no `PathGuard`, `EnvGuard` or `ENV_MUTEX` reference (`sed -n "/fn preflight_invokes_pi_auth_check_and_accepts_ready/,/^    }/p" crates/devflow-core/src/agents/pi.rs | grep -cE "PathGuard|EnvGuard|ENV_MUTEX"` prints 0)
    - SUMMARY contains a `TEST-01 baseline` section with `real` seconds and the per-binary `test result:` lines
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>The helper exists with a discriminating isolation control and a vacuity control, and one pi test runs through it.</done>
</task>

<task type="auto">
  <name>Task 2: Convert every remaining PathGuard test in pi.rs and opencode.rs and delete the process-global guards</name>
  <files>crates/devflow-core/src/agents/pi.rs, crates/devflow-core/src/agents/opencode.rs</files>
  <read_first>
    - crates/devflow-core/src/agents/pi.rs test module (as left by Task 1)
    - crates/devflow-core/src/agents/opencode.rs test module from line 350 (ENV_MUTEX, `PathGuard` near 423-441, its 7 call sites)
  </read_first>
  <action>
Per D-08: convert every remaining test that calls `PathGuard::set` (7 more in pi.rs, 7 in opencode.rs) to the parent/child shape from Task 1, each with a `const NAME: &str` holding its module-qualified name (`agents::pi::tests::…` / `agents::opencode::tests::…`) and each guarded by `assert_child_ran_exactly_one_passing_test`. Move every `EnvGuard::set("PI_CODING_AGENT_DIR", …)` onto the child `Command` via `extra_env`. Then delete `PathGuard` (struct and `Drop`) from both files, and delete `EnvGuard` and each module-local `ENV_MUTEX` if nothing else uses them. A test that asserts behaviour with an agent binary absent passes an empty directory, never a removed `PATH`.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; for m in pi opencode; do f=crates/devflow-core/src/agents/$m.rs; pg=$(grep -v "^[[:space:]]*//" "$f" | grep -c "PathGuard" || true); pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); ag=$(grep -v "^[[:space:]]*//" "$f" | grep -c "assert_child_ran_exactly_one_passing_test(" || true); echo "${m}_pathguard=$pg ${m}_path_mutations=$pm ${m}_child_guards=$ag"; [ "$pg" -eq 0 ] && [ "$pm" -eq 0 ] || rc=1; done; pa=$(grep -v "^[[:space:]]*//" crates/devflow-core/src/agents/pi.rs | grep -c "assert_child_ran_exactly_one_passing_test(" || true); oa=$(grep -v "^[[:space:]]*//" crates/devflow-core/src/agents/opencode.rs | grep -c "assert_child_ran_exactly_one_passing_test(" || true); [ "$pa" -ge 8 ] && [ "$oa" -ge 7 ] || rc=1; pn=$(grep -v "^[[:space:]]*//" crates/devflow-core/src/agents/pi.rs | grep -E "set_var\(|remove_var\(" | grep -vc "\"PATH\"" || true); echo "pi_non_path_mutations=$pn"; [ "$pn" -eq 0 ] || rc=1; for m in pi opencode; do cargo test -p devflow-core --lib "agents::${m}::tests::" > "$L/$m.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/$m.log" || true); echo "${m}_module_exit=$e ${m}_ok_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; done; cargo clippy -p devflow-core --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; pi/opencode pathguard or path_mutations above 0, pi child guards under 8 or opencode under 7, pi_non_path_mutations above 0, a module run with exit non-zero or no `ok. N passed; 0 failed` line, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14 before the change: `PathGuard::set(` appears 8 times in pi.rs and 7 in opencode.rs, and comment-stripped `set_var("PATH"`/`remove_var("PATH"` lines number 3 in each — the zero gates discriminate
    - `cargo test -p devflow-core --lib agents::pi::tests::` and `agents::opencode::tests::` each report a passed count at least as large as before the conversion (record both numbers in SUMMARY)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>devflow-core has no process-global PATH mutation in any test; both driver test modules pass through guarded child runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo test process → spawned agent CLI | A test that resolves a real `pi`/`opencode`/`claude` through the developer's `PATH` runs it with real credentials (T-33-09/T-33-17) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-03-01 | Elevation of Privilege | converted driver tests | medium | mitigate | Child `PATH` is only the stub directory; never the parent's `PATH`; `run_test_in_child` refuses a non-directory |
| T-48-03-02 | Denial of Service | vacuous child run reported green | medium | mitigate | Four-way guard plus the should-panic misspelled-name control |
| T-48-03-03 | Tampering | concurrent PATH race corrupting unrelated tests | medium | mitigate | No process-global PATH mutation remains in devflow-core tests (Task 2 zero gates) |
</threat_model>

<verification>
Both gates print `gate_rc=0`; `cargo clippy -p devflow-core --all-targets -- -D warnings` exits 0.
</verification>

<success_criteria>
The generic child helper is available to both test binaries and devflow-core's PATH mutators are gone.
</success_criteria>

## Artifacts this phase produces

- `devflow_core::test_support::CHILD_TEST_ENV` (value `DEVFLOW_CHILD_TEST`)
- `devflow_core::test_support::in_child_test(test_name: &str) -> bool`
- `devflow_core::test_support::run_test_in_child(test_name: &str, path_dir: &Path, extra_env: &[(&str, &OsStr)]) -> std::process::Output`
- `devflow_core::test_support::assert_child_ran_exactly_one_passing_test(output: &Output, test_name: &str)`
- Tests: `test_support::tests::child_with_an_empty_path_dir_cannot_spawn_git_while_the_parent_can`, `test_support::tests::child_guard_rejects_a_name_that_matches_no_test`, `test_support::tests::run_test_in_child_refuses_a_path_that_is_not_a_directory`

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md` when done.
</output>
