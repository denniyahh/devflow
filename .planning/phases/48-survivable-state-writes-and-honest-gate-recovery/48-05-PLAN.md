---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 05
type: execute
wave: 2
depends_on: ["48-03"]
files_modified:
  - crates/devflow-cli/src/pipeline_launch.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-08, no test in `pipeline_launch.rs` mutates process-global `PATH`; every former block runs through `devflow_core::test_support::run_test_in_child` guarded by `assert_child_ran_exactly_one_passing_test` with a `pipeline_launch::tests::…` name"
    - "Per D-10, each of the 3 tests in this file whose only protection from a real agent spawn was an abort fixture note now runs in a child with an agent-free `PATH` directory (fixture-coverage gate reads 0; it read 3 on 2026-09-14)"
    - "Contract C-8 (T-28-01 static declaration check first) is still pinned: the three `relaunch_checkpoint_session_*` tests and the six `advance_with_*` checkpoint tests pass"
    - "No test is deleted: `#[test]` count is at least 54 (its count at c0fb193)"
  prohibitions:
    - statement: "No production code in `pipeline_launch.rs` changes (D-08); `advance` and the resume route are 48-07/48-11 scope"
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-cli/src/pipeline_launch.rs
      provides: "PATH-isolated launch tests"
      contains: "run_test_in_child"
  key_links:
    - from: crates/devflow-cli/src/pipeline_launch.rs
      to: crates/devflow-core/src/test_support.rs
      via: "pipeline launch tests invoke run_test_in_child: parent builds fixture + PATH dir; child runs the body"
      pattern: "run_test_in_child"
---

<!-- plan-criteria-allow: R4 - The c0fb193 baseline object is explicitly checked before its content is read; the scanner also joins root setup to later pipelines. -->

<objective>
TEST-01 — ROADMAP alias 999.38, with backlog 999.80 folded in (D-10). Convert the 57 `PATH` set/restore lines in
`crates/devflow-cli/src/pipeline_launch.rs` tests (D-08) and the 3 abort-fixture tests (F-2: near :2584, :2745, :3587).

Purpose: 999.38 names `pipeline_launch.rs:590` as an original site; this file has the most PATH lines of any. Output:
converted tests. Contracts touched (P3): C-8 (relaunch tests near :2781-2876), C-9, C-10 (advance checkpoint tests
near :3596-3901).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — convert `advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records` through the core helper</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - crates/devflow-cli/src/pipeline_launch.rs lines 3560-3680 (that test, its fixture helpers, `ReapMonitorOnDrop`)
    - crates/devflow-cli/src/test_support.rs lines 320-530 (agent-free PATH helpers) and 745-805 (parent/child shape)
    - crates/devflow-core/src/test_support.rs (48-03 helper)
  </read_first>
  <action>
Per D-08: first run `cargo test -p devflow --bin devflow pipeline_launch::tests::` and record the passed count in SUMMARY. Convert the named test: `const NAME: &str = "pipeline_launch::tests::advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records";`; the parent creates the temp repo root and the `PATH` directory (stub `claude` plus `git`/`sh` symlinks, from the existing cli helper matching what the old block installed), passes the root as an extra child variable, runs the child and the guard; the child half reads the root via `std::env::var_os`, runs the original body (fixture writes, `advance(root, Some(phase))`, event assertions, `ReapMonitorOnDrop`) and returns. Delete that test's `env_lock()` and PATH set/restore lines.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; t=pipeline_launch::tests::advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records; cargo test -p devflow --bin devflow "$t" -- --exact > "$L/t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "tracer_exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; body=$(sed -n "/^    fn advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records()/,/^    }/p" crates/devflow-cli/src/pipeline_launch.rs); ic=$(printf "%s\n" "$body" | grep -c "in_child_test(" || true); pm=$(printf "%s\n" "$body" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); echo "tracer_in_child=$ic tracer_path_mutations=$pm"; [ "$ic" -ge 1 ] && [ "$pm" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; tracer one_passed=0 or filtered=none, tracer_in_child=0 (not converted, or the sed window matched nothing), or tracer_path_mutations above 0</fails_when>
  </verify>
  <acceptance_criteria>
    - The parent's child run prints `test pipeline_launch::tests::advance_with_declared_checkpoint_and_reported_gate_relaunches_and_records ... ok` in the child stdout (the guard asserts it)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>The checkpoint-relaunch test runs in a guarded child with a directory-only PATH.</done>
</task>

<task type="auto">
  <name>Task 2: Convert every remaining PATH block and the three abort-fixture tests in pipeline_launch.rs</name>
  <files>crates/devflow-cli/src/pipeline_launch.rs</files>
  <read_first>
    - Every test enclosing a hit of `rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" -e "abort: test cleanup" crates/devflow-cli/src/pipeline_launch.rs`
  </read_first>
  <action>
Per D-08 and D-10: convert every remaining PATH-replacing test with Task 1's shape. Convert each test containing an abort fixture note (near :2584, :2745, :3587) the same way, giving it an agent-free `PATH` directory even where it had no PATH block — the structural guard replaces the content-dependent one, so per-site reachability to an agent spawn does not need proving. If a fixture sits in a shared helper function rather than a test body, add `assert!(std::env::var_os(devflow_core::test_support::CHILD_TEST_ENV).is_some(), "<helper> must run inside a child test with an agent-free PATH (999.80)")` as the helper's first statement and convert every caller. Leave the 7 non-PATH environment mutations for 48-09. Remove imports your changes leave unused.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; git cat-file -e c0fb193^{commit} || { echo base_missing; exit 1; }; f=crates/devflow-cli/src/pipeline_launch.rs; pm=$(grep -v "^[[:space:]]*//" "$f" | grep -cE "set_var\(\"PATH\"|remove_var\(\"PATH\"" || true); nf=$(awk "/^    (pub(\\(crate\\))? )?fn /{name=\$0} /CHILD_TEST_ENV|in_child_test\\(/{c[name]=1} /abort: test cleanup/ && !/^[[:space:]]*\\/\\//{f[name]=1} END{b=0; for (n in f) if (!(n in c)) b++; print b}" "$f"); bt=$(git show "c0fb193:$f" | grep -c "#\[test\]"); nt=$(grep -c "#\[test\]" "$f"); echo "path_mutation_lines=$pm fixture_fns_without_child=$nf base_tests=$bt now_tests=$nt"; [ "$pm" -eq 0 ] && [ "$nf" -eq 0 ] && [ "$nt" -ge "$bt" ] || rc=1; r=$(grep -v "^[[:space:]]*//" "$f" | grep -c "run_test_in_child(" || true); g=$(grep -v "^[[:space:]]*//" "$f" | grep -c "assert_child_ran_exactly_one_passing_test(" || true); echo "child_runs=$r child_guards=$g"; [ "$r" -ge 10 ] && [ "$g" -ge "$r" ] || rc=1; for t in relaunch_checkpoint_session_emits_exactly_one_audit_event relaunch_checkpoint_session_increments_and_persists_counter relaunch_checkpoint_session_does_not_change_stage advance_without_declared_checkpoint_falls_through_to_generic_gate; do cargo test -p devflow --bin devflow "pipeline_launch::tests::$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; cargo test -p devflow --bin devflow pipeline_launch::tests:: > "$L/m.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed" "$L/m.log" || true); echo "module_exit=$e module_ok_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; cargo clippy -p devflow --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ke=$?; echo "clippy_exit=$ke"; [ "$ke" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; path_mutation_lines or fixture_fns_without_child above 0, now_tests below base_tests, child_runs under 10 or guards below runs, any named contract test one_passed=0, module run failing, or clippy_exit non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14 before the change: this file's comment-stripped PATH mutation lines read 57 and `fixture_fns_without_child` reads 3, so both zero gates discriminate
    - Module passed count after conversion ≥ the Task 1 baseline (both in SUMMARY)
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>pipeline_launch.rs has zero PATH mutations and every abort-fixture test is structurally agent-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cargo test process → spawned agent CLI | A resume-route test that reaches `launch_stage` with a real `claude` on PATH spawns it with real credentials |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-05-01 | Elevation of Privilege | abort-fixture tests (999.80) | medium | mitigate | Child with agent-free PATH; helpers assert child mode |
| T-48-05-02 | Denial of Service | vacuous child | medium | mitigate | Guard per child; test count ≥ base; contract tests run by exact name |
</threat_model>

<verification>
Both gates print `gate_rc=0`.
</verification>

<success_criteria>
Launch tests are PATH-isolated and 999.80's protection is structural in this file.
</success_criteria>

## Artifacts this phase produces

- No new production symbols. Per-test `NAME` constants in converted `pipeline_launch::tests` functions.

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-05-SUMMARY.md` when done.
</output>
