---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 20
type: tdd
wave: 14
depends_on: ["48-19"]
files_modified:
  - crates/devflow-cli/tests/start_lock_e2e.rs
  - crates/devflow-cli/tests/auto_chain_leak_repair_e2e.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/src/pipeline_launch.rs
autonomous: true
gap_closure: true
requirements: [SURV-01]
estimate:
  tokens: 60000
  raw_tokens: 120000
  tasks: 2
  confidence: med
must_haves:
  truths:
    - "Criterion 2, D-01, D-02: when the phase's state records a live monitor pid, `devflow resume --phase N` exits non-zero and writes nothing. The state file and the agent pid file stay byte-identical, no lock-NN is left behind, no stage_launched event is emitted, and the recorded process is still running afterwards (DevFlow signals nothing)"
    - "The same refusal holds when only the recorded agent pid is live and the recorded monitor is dead, with the state already marked stopped. This is the orphaned-agent state that `status` routes to `devflow resume` whether or not the state is marked stopped (doctor's `check_dead_monitor` names resume for it only when the state is not stopped), and the mark `devflow stop` leaves on a lock-free run (999.138). The stop mark survives the refusal"
    - "Operator's control: a state whose recorded monitor and agent pids are dead is resumed exactly as today, for stopped=false and stopped=true. resume exits 0, clears the stop mark, records a new monitor pid and emits stage_launched. This is doctor's dead-monitor repair and the stop-then-resume recovery"
    - "The resume refusal says `refusing to resume` and names each live pid with its role (`monitor pid N` or `agent pid N`). It says `devflow stop` would only mark the state stopped, and that a state already marked stopped does not mean the processes exited. It says `status` and `doctor` name `devflow resume` for a dead monitor with a live agent, and that resume refuses until every named process has exited. It keeps 48-19's identity check, monitor-first SIGTERM and advance-child guidance. It names the repair that works for resume: run resume again once the processes have exited. For a recycled pid, it says no DevFlow verb keeps the phase's work: after `recover --clean`, `start` refuses while the phase's worktree or branch exists, and `start --force` discards the phase's work. The tests assert each fragment, and assert that start-only text is absent"
    - "48-19's `start` refusal text is byte-identical after the refactor. A golden test passes at RED against the unchanged binary and passes again at GREEN, and the test files are unchanged since the RED commit"
    - "The live-run check in `resume` runs under the per-phase lock (D-02): after `lock::acquire` and before `load_state`, the `--agent` handoff checks, `repair_leaked_auto_chain_flag`, any `save_state` and `launch_stage`"
    - "`start` and `resume` share one liveness implementation (48-19's, unchanged: recorded monitor pid plus the agent pid file through `agent::agent_running`). `start`'s guard call site and behaviour are unchanged"
    - "`pipeline_launch::resume` has exactly one production caller, the `devflow resume` CLI arm in main.rs. No monitor or advance path calls it, so a recorded pid is never the caller itself. A source gate pins this"
    - "No-state carve-out carried over: with no state file, resume fails on its missing state as today and never prints the live-run refusal, even when the agent pid file names a live process"
    - "Both resume refusal arms failed against the pre-fix implementation with their named assertion (RED) while production source was untouched"
    - "SURV-01 boundary (probe fallback): with both recorded pids dead (0x7FFF_FFFE, above Linux pid_max) resume proceeds; with exactly one live pid, monitor only or agent only, it refuses"
    - "SURV-01 precision (probe fallback): liveness compares the exact recorded pid. A test-owned live pid reads as live (refusal arms) and a pid above pid_max reads as dead (control). Nothing is rounded or approximated"
    - "SURV-01 idempotency (probe fallback): running the refused resume twice refuses both times and leaves the state file and agent pid file byte-identical after the second run"
    - "SURV-01 concurrency (probe fallback): the check runs after `lock::acquire` in `resume`, so a concurrent resume or advance holding lock-NN stops this resume at the existing contention refusal before the check. Gate: the lock line comes before the guard line, which comes before the load_state line"
  prohibitions:
    - statement: "The resume refusal never signals, kills or otherwise acts on a process named by a recorded pid. It only declines to launch."
      status: resolved
      verification: test
    - statement: "The resume refusal never tells the operator that `devflow stop` ends a lock-free run, or that a state marked stopped means its processes have exited (999.138 honesty)."
      status: resolved
      verification: test
    - statement: "For a state file that exists but will not load (999.139 stays backlog), this plan changes no launch and no write: resume launched nothing before and still does not, and its writes are unchanged (the lock file is created and removed in both cases). Only the error changes: when the agent pid file names a live process, resume now prints the live-run refusal instead of the load error (both exit non-zero). This plan changes no production hint that names `devflow resume`: every site in the context section's hint inventory (H1-H10: `status`, doctor's findings, `cleanup`, `no_waiter_repair` as printed by `gate approve`/`gate reject`, `gate sweep`, `stop` and doctor, the park message and the rate-limit hints) keeps its text. Whether to change them is the open 999.140 hint question."
      status: resolved
      verification: judgment
  artifacts:
    - path: crates/devflow-cli/src/commands.rs
      provides: "shared live-run guard; resume entry point"
      contains: "pub(crate) fn refuse_resume_over_a_live_run"
    - path: crates/devflow-cli/src/pipeline_launch.rs
      provides: "resume refuses a live run under its lock"
      contains: "refuse_resume_over_a_live_run(project_root, phase)"
    - path: crates/devflow-cli/tests/start_lock_e2e.rs
      provides: "two resume refusal arms, the dead-leftover control, the no-state carve-out and the start golden"
      contains: "fn resume_refuses_a_phase_whose_recorded_monitor_is_alive()"
    - path: crates/devflow-cli/tests/auto_chain_leak_repair_e2e.rs
      provides: "SIGKILL-then-resume fixture that always knows and ends the orphaned agent before resume"
      contains: "fixture.child_pid().is_some()"
  key_links:
    - from: crates/devflow-cli/src/pipeline_launch.rs
      to: crates/devflow-cli/src/commands.rs
      via: "resume calls the shared guard between its lock::acquire statement and load_state"
      pattern: "refuse_resume_over_a_live_run(project_root, phase)"
    - from: crates/devflow-cli/src/commands.rs
      to: crates/devflow-core/src/agent.rs
      via: "monitor_pid and the agent pid file checked with agent::agent_running (shared with start)"
      pattern: "agent::agent_running"
    - from: crates/devflow-cli/src/commands.rs
      to: crates/devflow-cli/src/commands.rs
      via: "start's call site is unchanged and still sits between the `_phase_lock` binding and Gates::cleanup"
      pattern: "refuse_start_over_a_live_run(project_root, phase)"
---

<objective>
Close the one open gap in 48-VERIFICATION.md (criterion 2, `resume`). On 2026-09-23 (commit 6d31299, ROADMAP
999.140 "PROMOTED") the operator decided to close it in Phase 48: reuse the 48-19 live-run refusal under the lock,
test first.

Today `devflow resume --phase N` takes the per-phase lock, loads the state and relaunches, with no live-run check.
The verifier reproduced the failure at d5376fa with a real Legacy launch. Over a live, lock-free run, resume exited
0, rewrote `monitor_pid` (M1→M2), overwrote the agent pid file (A1→A2) and started a second agent beside the live
one.

Add 48-19's refusal to `resume`, test first. The liveness check moves into a helper that `start` and `resume` share.
`resume` calls it right after its lock and before anything is loaded or written. The refusal text says "resume",
names the repair that actually works in the lock-free window, and leaves `start`'s text byte-identical. The control:
when the recorded pids are dead, resume proceeds exactly as today.

Purpose: D-01 makes exclusion rest on "one writer per phase". 48-19 closed that for `start`. `resume` is the
sibling launch verb.

Output: RED tests (Task 1, committed by the orchestrator as `test(48-20): …`), then the shared guard and the resume
call (Task 2, committed as `feat(48-20): …`). The GSD tdd.review-checkpoint audit greps `^feat(48-20):`.
48-TDD-OVERRIDE.md is not extended.

Out of scope, by operator decision: 999.139 (unloadable state hides a live monitor from `start`), 999.136, 999.138,
999.141. Also out of scope: whether any production hint that names `devflow resume` should stop doing so in the
states this guard refuses. The hint inventory in the context section lists every such site (H1-H10) and what resume
does there after this plan. That is the open half of 999.140's fix shape; the operator did not decide it, so this
plan leaves every hint text unchanged.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-VERIFICATION.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-19-SUMMARY.md
@crates/devflow-cli/tests/start_lock_e2e.rs

Execution constraint (Phase 48 #4799 workaround): the executor does NOT commit. RED and GREEN are separate tasks.
The orchestrator commits `test(48-20): …` after Task 1 and `feat(48-20): …` after Task 2. Task 2's gate checks
that HEAD is the `test(48-20):` commit. Do not run git commit, git stash or git reset.

Gate runtime: Task 1's gate runs six named e2e tests, clippy and fmt. Task 2's gate runs eight e2e files, the bin
unit suite, four named unit tests, clippy and fmt. It can take well over 30 minutes cold. Run each gate with a tool timeout of at least 45 minutes, or in the background with
explicit polling. A killed gate reports nothing, and a partial run is not evidence.

Tracked interface facts, verified from source at 6d31299 (line numbers are from that commit):
- `pipeline_launch::resume` (pipeline_launch.rs:1408-1527). Its `let _lock = match lock::acquire(project_root,
  phase) {` statement is at :1414-1422, and `let mut state = workflow::load_state(project_root, phase)?;` is at
  :1423. Then come the `--agent` handoff block (:1424-1470: `generic_preflight_checks`, `save_state`, the
  `agent_handoff` emit), the stop-mark clear (:1471), `apply_legacy_launch_opt_out` (:1478),
  `repair_leaked_auto_chain_flag` (:1497, which may write and commit `.planning/config.json`), `save_state`
  (:1503) and `launch_stage` (:1504). `launch_stage` → `spawn_agent_and_record` clears and re-saves
  `monitor_pid`, archives the capture, removes the agent pid file (agent_result.rs:3184) and registers the root in
  the cache (pipeline_launch.rs:1093).
- 48-19's guard, in commands.rs:
  - `refuse_start_over_a_live_run` (:2426-2444) is called from `start` at :370, between `_phase_lock` (:360)
    and the `Gates::cleanup` loop (:371).
  - `live_run_refusal(phase, live)` (:2449-2489) builds the text.
  - `agent_pid_from_file` (:2492).
  - Liveness is `workflow::load_state(..).monitor_pid` plus the agent pid file, each filtered by
    `agent::agent_running`. With no state file (load fails and the path is absent) it returns Ok, which is the
    no-state carve-out.
- The refusal is printed as `error: {message}` on stderr (main.rs:533). Tracing also writes to stderr.
- `status` (`liveness`, `recovery_hints`, commands.rs:1012-1038) and doctor's `check_dead_monitor` (:3718-3736)
  prescribe `devflow resume --phase N` whenever the recorded monitor is dead, even while the agent is alive
  ("an agent that outlived its monitor is orphaned too"). On the pipe-owning launch, `__monitor` installs no
  SIGTERM handler, and its agent runs in its own process group (48-REVIEW WR-04, IN-05). So "SIGTERM the monitor
  first" leaves exactly that state.
- `recover --clean` removes the state, gate files and cron record, but not the agent pid file (48-19 context).
  Without `--force`, `start` then refuses when the phase's worktree exists: `ensure_phase_worktree`
  (parallel.rs:24-51) → `worktree::add` returns `Exists`, and `git worktree add -b` fails on an existing branch.
  In `--no-worktree` mode, `feature_start`'s `git checkout -b` fails on an existing branch (git.rs:157). With
  `--force`, start removes the worktree and force-deletes the branch, or resets it with `checkout -B`, from the
  base.
- `auto_chain_leak_repair_e2e.rs::auto_chain_flag_survives_sigkill_and_is_repaired_on_the_next_start` spawns a
  real `__monitor`. It waits only for the chain flag, and `run_monitor` sets that flag BEFORE it spawns the agent
  (pipeline_launch.rs:965 then :977). `run_pipe_owning_monitor` writes the agent pid file right after spawn
  (monitor.rs:~985). So `fixture.child_pid()` can still be `None` when read. The sleeper then survives the monitor's
  SIGKILL (own process group) and is not terminated. With this plan's guard, the following `devflow resume` would
  correctly refuse over that live recorded agent, and the test's repair assertion would fail intermittently.
  Reasoned from source, not reproduced. Task 1 makes the fixture wait for the pid; the test's expectations do not
  change.

## Self-refusal analysis (gap risk 1): resolved, no self or parent exclusion needed

- `pipeline_launch::resume` has one production caller: `Command::Resume` in main.rs:612, the operator-facing CLI.
  `rg -n "(^|[^a-z_])resume\(" crates/*/src` finds only that call, the definition and `#[cfg(test)]` unit tests in
  pipeline_launch.rs (below `mod tests {` at :1945). Task 2's gate pins it.
- No DevFlow process spawns `devflow resume`. The only argv that names it:
  - `ship::build_single_agent_cron_instructions` (ship.rs:271-301). This is a Hermes cron manifest that the
    operator's external scheduler runs at a minute-granular time after `retry_after`. The monitor that wrote it
    has exited by then, because the Legacy script ends after its `advance` tail and `run_monitor` returns after
    `advance`.
  - Hint strings in commands.rs and pipeline_launch.rs.
- `relaunch_checkpoint_session` and `resume_launch_shape` (pipeline_launch.rs:244, :1236) are the in-process
  checkpoint relaunch inside `advance_for_stage`. They never call `resume`.
- Decision (planner remit): the guard sits only on the operator-facing path, inside `resume` itself. It has no
  self or parent pid exclusion. A recorded pid can equal the caller's own pid or its ancestor only if a live agent
  runs `devflow resume` for its own phase. Refusing that is correct, because it would launch a second agent.

## Legitimate recovery paths that call `resume` (gap risk 2) and what gates them

Every test named in the Gate column runs `devflow resume` (the e2e files) or `resume(` (the bin unit tests). A test
that only asserts hint text, or never touches resume, is labelled as such. H-numbers point at the hint inventory
below. "Refuses (changed)" means that before 48-20 resume launched a second monitor and agent there.

| Path | Recorded pids when resume runs | After 48-20 | Gate |
|------|--------------------------------|-------------|------|
| doctor `check_dead_monitor` (H2) / status "stuck — needs devflow resume" (H1), agent also gone | monitor dead, agent dead or absent | proceeds | new control, both stop-mark values |
| same finding, agent still alive (orphaned; pipe-owning monitor SIGTERMed first, IN-05) | monitor dead, agent live | **refuses (changed)**, text explains | new agent arm (stopped=true: H1's shape; H2 fires only when not stopped, and the guard does not read the stop mark, reasoned); Known limits |
| doctor `check_dead_agent` (H3): agent recorded dead at Define/Plan/Code, not stopped; monitor liveness not consulted | monitor live, agent dead | **refuses (changed)** when no process holds lock-NN, i.e. in the moments around that monitor's own `advance`; the live monitor advances the phase itself. Monitor dead or unrecorded: proceeds | monitor arm has this exact pid shape (stopped=false, Code, live monitor, dead agent pid); doctor itself not run; Known limits |
| `devflow cleanup` refusing to remove a phase's worktree: "run `devflow resume --phase N` or wait for it to finish" (H7, commands.rs:925-938). Reached only when the state is not marked stopped or `--force` is passed | agent live, or monitor live (Healthy/BetweenStages) | **refuses (changed)** whenever the state loads and no process holds lock-NN. With no state file and a live agent (phase taken from the worktree path) resume fails on the missing state, as before. A state file that will not load is skipped by `list_states`; resume then refuses on the agent pid file (only the error changes). The hint's `resume` half cannot succeed while a named process is live; its "wait for it to finish" half still holds | monitor arm has the BetweenStages shape; cleanup itself not run; Known limits; Still open for the operator |
| `devflow cleanup` keeping a stopped phase's worktree: "halted via --until; run `devflow resume --phase N` first, or pass --force" (H6, commands.rs:913-923). Checked before the live-phase refusal, so it fires at any liveness | stopped; pids dead, or live after a lock-free `devflow stop` (999.138) | dead: proceeds. Live: **refuses (changed)**. The text says "halted via --until" in both cases | new control (stopped=true) for dead; agent arm (stopped, live agent) for live; a stop-marked live monitor is not tested; cleanup itself not run; Known limits |
| `no_waiter_repair` (H5): `gate approve`/`gate reject`, `gate sweep`, `stop` and doctor's `check_open_gate_without_waiter` name "run `devflow resume --phase N` or `devflow recover --clean --phase N`" for an open non-Ship gate with no confirmed live lock holder | waiter died with its run: dead. A gate request that outlived its waiter while a later run is live and lock-free: live | dead: proceeds. Live: **refuses (changed)** | not tested; Known limits |
| doctor `check_gate_pending_without_gate` (H4, #200 wedge after an interrupted `start`) | `monitor_pid` None, no agent pid file | proceeds | bin unit tests with `monitor_pid` None and `gate_pending` (`resume_relaunches_without_consuming_a_pending_gate_answer`, N-48-01). `gate_wedge_e2e` runs in Task 2's gate as a regression file only; it pins the wedge's `gate reject` and never runs resume |
| the same finding (H4) during the run that resume relaunched from it (reasoned: resume does not clear `gate_pending`) | live | **refuses (changed)** while no process holds lock-NN | not tested; Known limits |
| `devflow stop` ended a lock-holding run, then resume | holder exited; the Legacy monitor exits once its advance child returns | proceeds | new control (stopped=true). `stop_e2e` runs in Task 2's gate as a regression file only; it never runs resume (stop_e2e.rs:335 asserts the hint text) |
| `devflow stop` on a lock-free live run (999.138), then resume | live, stopped=true | refuses; text says the stop mark is not an exit | new agent arm (stopped=true) |
| park-for-supervised-repair (H8) then resume | production: the monitor whose `advance` parked the phase, dead once that `advance` returns and the monitor exits; the unit test records none | proceeds; **refuses** only between that `advance` returning and its monitor exiting, and a rerun then works | `auto_rescan_rejection_parks_for_supervised_repair` (records no monitor) |
| crash recovery: monitor SIGKILLed, orphan terminated | dead | proceeds; leak repair runs | `auto_chain_leak_repair_e2e` (fixture made deterministic) |
| failed launch re-marks stopped | none recorded | proceeds to its launch failure | `resume_re_marks_stopped_when_launch_stage_fails_outright` |
| rate-limit hints (H9 `status` cron hint and Hermes cron, H10 gate reason "resume manually") | writer monitor exited | proceeds; while the rate-limit gate waits, its `advance` holds lock-NN and resume stops at the contention refusal as before; same brief window as H8 | reasoned (Known limits) |

## Hint inventory: every production string that tells the operator to run `devflow resume` (gap risk 3)

Method, at 6d31299: `rg -n 'devflow resume|resume --phase'` over crates/devflow-cli/src and crates/devflow-core/src,
dropping comment lines and everything below each file's `mod tests {`, plus a sweep of production string literals
for a bare `resume` (it found H10). Cross-checked a second, independent way: `strings target/debug/devflow`. That
binary sits in this worktree's target/, was built at 12:52 (after the last crates/ commit, with crates/ clean), and
contains 48-19's refusal text and no test-only literal. It shows exactly these hint texts plus two help-text mentions. Excluded: the clap help at
main.rs:110 and :184 (it says a flag persists across `devflow resume`, it does not ask for one), comments,
`#[cfg(test)]` code, and the argv words at ship.rs:276-280 that H9 covers. Outcomes are reasoned from source, not
reproduced. status and doctor read the same liveness inputs as the guard: the state's `monitor_pid`, the agent pid
file and `agent::agent_running`. resume takes lock-NN before the guard, so wherever a live process holds lock-NN
resume stops at the existing contention refusal first; that is unchanged by this plan and not repeated per row.

| ID | Site (commands.rs unless named) | Fires when | resume after 48-20 |
|----|----------------------------------|-----------|--------------------|
| H1 | `status`: `Liveness::describe` Stuck label "stuck — needs devflow resume" (:1003) and `recovery_hints` (:1031-1038), printed at :1123 and :1132 | recorded monitor dead; any agent liveness; any stop mark | agent live: **refuses (changed)**. Agent dead or absent: proceeds |
| H2 | doctor `check_dead_monitor` (:3721-3737, repair :3735) | not stopped; recorded monitor dead | agent live: **refuses (changed)**. Agent dead: proceeds |
| H3 | doctor `check_dead_agent` (:3698-3712, repair :3710) | agent pid file names a dead pid; not stopped; stage Define, Plan or Code; the monitor is not consulted | monitor live: **refuses (changed)**, transient (the live monitor is about to run, or has just run, its own `advance`). Monitor dead or unrecorded: proceeds |
| H4 | doctor `check_gate_pending_without_gate` (:3640-3653, repair :3651) | `gate_pending` set, no gate file open; pids not consulted | the #200 wedge (nothing recorded live): proceeds. resume does not clear `gate_pending`: its production writers are pipeline_gate.rs:75, :100, :180, :369, :425, preflight.rs:1409, pipeline_outcomes.rs:918 and pipeline_launch.rs:1156, :1831. So the finding can persist into the relaunched run, and there resume **refuses (changed)** |
| H5 | `no_waiter_repair` non-Ship arm (:1466-1475, text :1472). Callers: `gate_respond` (`gate approve`/`gate reject`, :1423, :1453), `gate_sweep` (:1565), `stop_via_gate` (:1921 stale request, :1931 no live waiter), `stop_with_existing_response` (:2014), `answered_gate_contention_message` (:2213, :2219), doctor `check_open_gate_without_waiter` (:3658-3672) | an open non-Ship gate request with no confirmed live lock holder; pids not consulted | the waiter died with its run: proceeds. A request that outlived its waiter while a later run is live: **refuses (changed)**. One route: resume leaves the gate request untouched when it relaunches (`resume_relaunches_without_consuming_a_pending_gate_answer` asserts this), so through the relaunched run every H5 caller names resume again. `stop` then also plants the stop mark on that lock-free run (999.138). The two `answered_gate_contention_message` lines fire only while a live pid holds lock-NN, so resume stops at contention there, as before |
| H6 | `cleanup` halted-via-until branch (:913-923, text :919) | matched state marked stopped, no `--force`; any liveness | pids dead: proceeds. Stop-marked live run (999.138): **refuses (changed)** |
| H7 | `cleanup` live-phase refusal (:925-938, text :934, plus H1's label via `describe()` at :936) | not stopped or `--force`; agent live, or monitor live (Healthy/BetweenStages) | state loads: **refuses (changed)**. No state file: missing-state error, as before. State file that will not load: refuses on the agent pid file, only the error changes |
| H8 | pipeline_launch.rs `park_auto_rescan_repair` (:1147-1184, text :1181) | printed by the `advance` that parks the phase, after its agent exited, under that `advance`'s lock | proceeds once that monitor has exited. **Refuses** only between its `advance` returning and the monitor process exiting; a rerun works |
| H9 | `cron_hint_line` (:2509-2544; empty-schedule text :2513, otherwise the Hermes command built at ship.rs:299), printed by `status`; the Hermes manifest itself (ship.rs:270-301) | a cron-instructions file exists: written by the rate-limited run's `advance`, consumed at the next launch (pipeline_launch.rs:1069) | as H8. The scheduled job is the Hermes cron known limit |
| H10 | pipeline_outcomes.rs:122, gate reason "auto-resume cron not scheduled; resume manually" | the rate-limit gate for an unparseable retry time | while that gate waits, its `advance` holds lock-NN (contention, as before). An abort clears the state (missing-state error, as before). Otherwise as H8 |

## Artifacts this phase produces

| Artifact | Change |
|----------|--------|
| crates/devflow-cli/src/commands.rs | New `pub(crate) fn refuse_resume_over_a_live_run(project_root: &Path, phase: PhaseId) -> Result<(), CliError>`. New private `fn refuse_launch_over_a_live_run(project_root, phase, verb: LaunchVerb)`, the shared core holding 48-19's liveness logic unchanged. New private `enum LaunchVerb { Start, Resume }`. `live_run_refusal` gains a `verb: LaunchVerb` parameter. `refuse_start_over_a_live_run` keeps its name, signature and call site, and delegates |
| crates/devflow-cli/src/pipeline_launch.rs | One guard call in `resume` between its lock statement and `load_state`, plus one doc paragraph on `resume` |
| crates/devflow-cli/tests/start_lock_e2e.rs | Five tests: `resume_refuses_a_phase_whose_recorded_monitor_is_alive`, `resume_refuses_a_stopped_phase_whose_recorded_agent_is_alive`, `resume_relaunches_a_phase_whose_recorded_processes_are_dead`, `resume_without_phase_state_reports_the_missing_state_not_a_live_run`, `start_refusal_text_stays_byte_identical_for_a_live_monitor`. New helpers: `START_REFUSAL_GOLDEN_PHASE_94`, `plant_resumable_run`, `resume_command`, `LaunchedRunReaper`, `events_contain`, `RefusalCase`, `assert_resume_refuses_live_run` |
| crates/devflow-cli/tests/auto_chain_leak_repair_e2e.rs | One `wait_for` on `fixture.child_pid().is_some()` before the SIGKILL |
| 48-20-SUMMARY.md (by the executor) | RED evidence, GREEN gate output, the self-refusal result, known limits |

## Known limits (recorded, not fixed here)

- **Recycled pid on `resume`: accepted in planner remit, not by the operator. Still open for the operator.**
  Option A (operator, 2026-09-22) accepted pid-only liveness for `start`, when the named repair was `recover
  --clean` then `start`. It did not cover this cost. D-03 accepted a related but different case: a recycled
  *lock* pid blocking `resume`. On `resume`, a live unrelated process that reuses a recorded monitor or agent pid
  makes resume refuse again, and no DevFlow verb clears a recorded pid while keeping the phase's work.
  - After `recover --clean`, plain `start` refuses while the phase's worktree or branch exists: `worktree::add -b`,
    or `feature_start`'s `checkout -b`, fails (parallel.rs:24-51, git.rs:157-172).
  - `start --force` removes the worktree and force-deletes or resets the branch from the base, which discards the
    phase's work.
  - The refusal says so instead of offering `start` as a clean way through. The only other way out is waiting for
    the unrelated process to exit.
  - Reasoned from source, not tested. The probability is low: it needs a recycled pid that is live at resume time.
  - Identity for recorded pids is deferred with D-03 / Phase 50.
  - Why the planner accepted it rather than blocking the plan: the refusal fails closed (it launches nothing and
    writes nothing), and the alternative is identity, which is Phase 50's work. The operator has not ruled on this
    cost. The SUMMARY lists it under "Still open for the operator"; it is not recorded as an operator decision.
- **Production hints still name `devflow resume` in states this guard refuses** (999.140's undecided fix-shape
  half). The hint inventory (H1-H10) is the complete list at 6d31299.
  - Lasting states, for as long as the run is live and no process holds lock-NN:
    - `status` (H1) and doctor's `check_dead_monitor` (H2), for a dead monitor with a live agent. Doctor does so
      only when the state is not stopped; `status` does so either way.
    - doctor's `check_gate_pending_without_gate` (H4), during a run that resume relaunched with `gate_pending`
      still set.
    - `no_waiter_repair` (H5), printed by `gate approve`/`gate reject`, `gate sweep`, `stop` and doctor's
      `check_open_gate_without_waiter`, when a gate request outlived its waiter into a later live run. Its other
      half, `recover --clean`, clears state even while an agent runs (the refusal's own warning).
    - `cleanup`'s "halted via --until" message (H6), for a live run that a lock-free `devflow stop` marked stopped
      (999.138). The message says "halted via --until" there too.
    - `cleanup`'s live-phase refusal (H7), "run `devflow resume --phase N` or wait for it to finish", reached when
      the state is not marked stopped or `--force` is passed, whenever the agent or monitor is live.
  - Transient states: doctor's `check_dead_agent` (H3), for a live monitor in the moments around its own
    `advance`. The park message and the rate-limit hints (H8-H10), between the printing `advance` returning and
    its monitor exiting. A rerun of resume works once the monitor has exited.
  - Resume's refusal names the status/doctor contradiction (F05) and says resume refuses until every named process
    has exited (F06). It names no other site; F06 covers them.
  - What tests pin: the agent arm has H1's and H6's pid shape (stopped, dead monitor, live agent). The monitor arm
    has H3's and H7's (not stopped, Code, live monitor, dead agent pid). No test runs `status`, doctor, `cleanup`,
    the gate verbs or `stop`. H2 differs from the agent arm only in the stop mark, which the guard does not read
    (reasoned). H4, H5 and H8-H10 are not tested. H4's and H5's routes are reasoned from source, not reproduced.
  - Every hint text is unchanged. Whether to change any of them is still open for the operator.
- **IN-05.** On the pipe-owning launch, SIGTERM to `__monitor` does not end its agent. The shared "monitor first"
  advice therefore leaves an orphaned agent, which the resume text covers. Not tested end to end.
- **999.139 on `resume`.** A state file that exists but will not load is checked on the agent pid file only. For
  resume this branch cannot launch, because resume's own `load_state` fails right after the check. The one
  visible change: when the agent pid file names a live process, resume prints the live-run refusal instead of the
  load error. Both exit non-zero, and neither launches or writes anything new. 999.139 stays a `start` concern in
  the backlog. Not tested.
- **999.136 and 999.138 are unchanged.** The agent arm pins that a stop-marked state with a live agent is refused.
- **Hermes cron.** A rate-limit cron that fired while the writing monitor still ran would be refused, and a
  `once: true` job would not retry. Reasoned impossible in practice (minute-granular schedule, monitor exits right
  after its `advance`). Not tested.
- **"nothing was written" (48-REVIEW IN-03) applies here as it does to `start`.** The lock file is created and
  removed, and `load_state` may migrate a legacy state file.
- **Pre-existing 999.141.** Task 2's gate runs regression files that still write the real `~/.cache/devflow`. The
  new resume helper sets `DEVFLOW_CACHE_DIR` per child and reaps what it launches, so this plan adds no new entries.

## Flagged assumptions (spec-less probe fallback, no silent drops)

Edge report: 14 applicable. The four SURV-01 edges (boundary, precision, idempotency, concurrency) are authored as
truths above. This plan does not touch the other ten. They are owned by the already-executed plans and left
unresolved here:
- SURV-02 empty and encoding: owned by 48-10, 48-12, 48-13, 48-14, 48-16, 48-18.
- CHKPT-01 unclassified: owned by 48-02.
- CHKPT-02 adjacency, empty, ordering and concurrency: owned by 48-07, 48-15.
- TEST-01 adjacency, empty and ordering: owned by 48-03..48-06, 48-08, 48-09, 48-17.

Additional flagged assumption: an agent pid file that does not parse as `u32` counts as no agent pid. This is
existing `agent_pid_from_file` behaviour, unchanged and untested here.

Prohibition recall kept three items (see `must_haves.prohibitions`), with no check descriptors. Dropped as routine
engineering: "must not leak test monitors" (the reaper covers it, and 999.141 owns the class) and "must not break
`--agent` handoff" (the bin unit suite covers it).

<assumption_delta_decision>
The detector fired on "second"/"another" in the phase goal. That wording names a concurrent second writer to
exclude: here, a `resume` beside a live run. It does not name a second identity variant. Primary noun: one live run
per phase, identified by its state file plus its recorded monitor and agent pids. Decision: no-change. The guard
enforces the singular assumption on a second launch verb; it does not generalize it.
</assumption_delta_decision>

<!-- planner-discipline-allow: refuse_resume_over_a_live_run(project_root, phase) -->
<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer RED — `resume` over a live run fails the refusal assertions end to end; controls and the start golden pass</name>
  <files>crates/devflow-cli/tests/start_lock_e2e.rs, crates/devflow-cli/tests/auto_chain_leak_repair_e2e.rs</files>
  <read_first>
    - crates/devflow-cli/tests/start_lock_e2e.rs, the whole file (613 lines). Reuse `init_repo`, `fake_bin_dir`, `child`, `LiveProcess`, `NEVER_LIVE_PID`, `GATE_TIMEOUT_SECS`, `write_agent_pid`, `phase_file_snapshot`, `snapshot_paths`, `plant_leftover_run` and `run_start`. Leave the seven existing tests and the existing helpers as they are
    - crates/devflow-cli/src/commands.rs lines 2408-2495: 48-19's guard and the exact `live_run_refusal` format string the golden must reproduce
    - crates/devflow-cli/src/pipeline_launch.rs lines 1380-1527 (`resume`) and 1003-1100 (`spawn_agent_and_record`: what a pre-fix resume writes)
    - crates/devflow-cli/tests/auto_chain_leak_repair_e2e.rs lines 180-470 (`wait_for`, `Reaper`, `Fixture`, `run_resume`, and the SIGKILL test)
    - crates/devflow-core/src/agent.rs lines 30-130 (`agent_running`, `terminate_and_verify`, `TERMINATE_VERIFY_WAIT`, `TERMINATE_VERIFY_POLL`)
  </read_first>
  <behavior>
    - resume_refuses_a_phase_whose_recorded_monitor_is_alive (phase 85): stopped=false, the state records a live test-owned monitor pid, and the agent pid file names a dead pid (0x7FFF_FFFE), exactly as in the stopped=false control. resume exits non-zero, stderr carries every required fragment, the state file and the agent pid file are byte-identical, no lock-NN is left, no stage_launched event is emitted, the monitor is still alive, and the dead agent pid is never named
    - resume_refuses_a_stopped_phase_whose_recorded_agent_is_alive (phase 86): stopped=true, the monitor pid is recorded dead (0x7FFF_FFFE) and the agent pid file names a live test-owned process. The same refusal and no-effects assertions hold, run twice (idempotency). The stop mark survives, and the dead monitor pid is never named
    - resume_relaunches_a_phase_whose_recorded_processes_are_dead (phases 87 and 88, stopped=false then stopped=true): both pids are recorded dead. resume exits 0, the state is no longer stopped, the new monitor pid differs from 0x7FFF_FFFE, and stage_launched is emitted
    - resume_without_phase_state_reports_the_missing_state_not_a_live_run (phase 84): there is no state file, and the agent pid file names a live process. resume exits non-zero without the live-run refusal, the agent pid file is byte-identical, and the process is still alive
    - start_refusal_text_stays_byte_identical_for_a_live_monitor (phase 94): start's stderr contains `error: ` plus the golden 48-19 text rendered with the live pid, plus a newline. A verb-swapped copy is not contained
    - auto_chain_flag_survives_sigkill_and_is_repaired_on_the_next_start keeps its expectations and now waits until the sleeper's pid is known before the SIGKILL
  </behavior>
  <action>
Per criterion 2, D-01 and D-02, and the operator's 2026-09-23 decision (test first), write the tests only. Do not
touch production source.

**New helpers in start_lock_e2e.rs.** Put them under a section header naming 48-20, after the 48-19 section. You may
widen the module doc comment to mention `resume`.
- `START_REFUSAL_GOLDEN_PHASE_94: &str`. This is the full message `live_run_refusal` builds today for phase 94 with
  one live monitor. Every occurrence of the pid is the literal placeholder `{pid}`, and the message has no `error: `
  prefix.
  - Render it by hand from the format string at commands.rs:2449-2489, applying Rust's `\n\` and `\` line-continuation
    rules.
  - It must start with `phase 94: a run is still live (monitor pid {pid} alive) — refusing to start; nothing was written`.
  - It must end with ``a later `start` would launch a second agent beside it.``.
  - With one live pid, the rendered message is 7 lines: the template's 6 lines plus one `{checks}` line, which
    starts with two spaces.
  - It is correct only when the golden test passes against today's unchanged binary. If it fails, fix the constant.
    Never fix production.
- `events_contain(root: &Path, event: &str) -> bool`. True when the events file contains the event name in double
  quotes. An absent file is false.
- `plant_resumable_run(root, phase, monitor_pid: Option<u32>, agent_pid: Option<u32>, stopped: bool)`.
  - It saves `State::new(phase, AgentKind::Claude, Mode::Supervise, root.to_path_buf())` with stage Code, the given
    `monitor_pid` and `legacy_claude_launch = true`.
  - It sets `stopped`. When `stopped` is true, `stop_reason` is `Some("planted: marked stopped by devflow stop while
    the run was live")`; otherwise it is None.
  - When `agent_pid` is Some, it writes the agent pid file with `write_agent_pid`.
  - It plants no gate files. Supervise mode is deliberate: the relaunch preflight passes in Supervise
    (`resume_relaunches_without_consuming_a_pending_gate_answer`), so the control launches rather than parking at a
    gate.
- `resume_command(root, phase, fake_bin, cache_dir: &Path) -> Command`.
  - It runs `devflow_bin()` with `resume`, `--phase`, the phase and `--legacy-claude-launch`, then the root as the
    positional argument, with current_dir the root.
  - PATH is built exactly as `child` builds it.
  - It sets `DEVFLOW_GATE_TIMEOUT_SECS` to `GATE_TIMEOUT_SECS` and `DEVFLOW_CACHE_DIR` to `cache_dir`, both through
    `Command::env` only. Never use `std::env::set_var`: clippy.toml disallows it (TEST-01). Per-child
    `DEVFLOW_CACHE_DIR` keeps the registry write at pipeline_launch.rs:1093 out of the operator's real cache
    (999.141).
- `LaunchedRunReaper { root: PathBuf, phase: PhaseId, spare: Vec<u32> }`, whose `Drop` never panics (no `expect`,
  `unwrap` or assert).
  - It reads the monitor pid from `devflow_core::workflow::load_state(..).ok()`, then the agent pid from the agent
    pid file (trim, `parse::<u32>().ok()`).
  - For each pid that is not in `spare` and for which `devflow_core::agent::agent_running` is true, monitor first,
    it calls `devflow_core::agent::terminate_and_verify(pid, TERMINATE_VERIFY_WAIT, TERMINATE_VERIFY_POLL)`.
  - In every test, bind it after the cache TempDir and the LiveProcess, and before resume runs. It then drops first,
    so a pre-fix resume's launched monitor and agent are reaped even when an assertion panics, and before their
    TempDirs are deleted.
  - Every resume in these tests uses the fake agent `fake_bin_dir("exec sleep 60")`. A launched Legacy monitor then
    waits on its agent, so SIGTERM runs its `cleanup` trap, which kills the agent before `advance` ever runs.
- `struct RefusalCase<'a> { role: &'a str, live_pid: u32, required: &'a [PathBuf], never_named: &'a [String],
  refusal: &'a str }`, the per-arm expectations. It exists so the assert helper stays within clippy's argument
  limit (see the lint rule below).
- `assert_resume_refuses_live_run(root, phase, fake_bin, cache_dir, case: &RefusalCase<'_>)`, five parameters. In
  the bullets below, `role`, `live_pid`, `required`, `never_named` and `refusal` are the fields of `case`. It runs
  one refused resume and asserts:
  - Before the run, the snapshot covers every `required` path. This is the coverage precheck, so byte-identity
    cannot pass vacuously.
  - The first assertion after the run is a non-zero exit with exactly the message `refusal`. The message also carries
    stdout and stderr.
  - stderr contains every fragment below, rendered with the phase, `padded`, `role` and `live_pid`. These are W1,
    and Task 2 must emit them verbatim. Each fragment is the raw text after its `Fnn: ` label, to the end of the
    line. Backticks inside it are part of the fragment:
    - F01: a run is still live ({role} pid {live_pid} alive) — refusing to resume; nothing was written
    - F02: `devflow stop --phase {phase}` would only mark the state stopped: it signals nothing and does not end this run
    - F03: once it takes the lock after this refusal
    - F04: A state already marked stopped does not mean these processes have exited
    - F05: `devflow status` and `devflow doctor` name `devflow resume` for a phase whose monitor is dead even while its agent is still alive
    - F06: resume refuses until every named process has exited
    - F07: Check each pid before signalling it
    - F08: `ps -p` shows only the executable name
    - F09: {role} pid {live_pid}: ps -ww -o pid=,lstart=,args= -p {live_pid}
    - F10: `__monitor`
    - F11: `--phase {phase}`
    - F12: `.devflow/phase-{padded}-`
    - F13: `ps -o ppid= -p <pid>`
    - F14: so do not signal it on the pid alone
    - F15: send SIGTERM to the confirmed monitor first
    - F16: signalling the agent first lets the monitor launch the next stage
    - F17: may be running `devflow advance` as a foreground child
    - F18: the child may take the lock as soon as this resume exits
    - F19: `ps -ww -o pid=,args= --ppid <monitor pid>`
    - F20: signal that child, not only the monitor
    - F21: If the named processes have exited, run `devflow resume --phase {phase}` again
    - F22: If a named pid is live but is not this phase's process (a recycled pid), `resume` refuses again
    - F23: no DevFlow verb clears a recorded pid while keeping the phase's work
    - F24: `devflow recover --clean --phase {phase}` removes the phase state, after which `resume` has nothing to resume, and `devflow start` refuses while the phase's worktree or branch exists
    - F24b: `devflow start --force` recreates them from the base and discards the phase's work
    - F25: Do neither while this phase's processes are live
    - F26: a later `start` would launch a second agent beside it
  - stderr contains none of these, labelled the same way:
    - N01: refusing to start
    - N02: run `devflow start` again
    - N03: as soon as this start exits
    - N04: ps -p {live_pid}
    - N05: each entry of `never_named`
  - The snapshot after the run equals the snapshot before it.
  - `.devflow/lock-{padded}` is absent.
  - `events_contain(root, "stage_launched")` is false.
  - `devflow_core::agent::agent_running(live_pid)` is still true, with the message "a refused resume must not signal
    the recorded {role}".

**Lint and format rule.** Task 2's gate runs `cargo clippy --workspace --all-targets -- -D warnings` and
`cargo fmt --all -- --check`, and it also fails if any test file changed after the RED commit. A lint or format
problem in the test files cannot be fixed at GREEN, so Task 1's gate runs both and the test files must pass them
now.
- No new function may take more than 7 parameters. `clippy::too_many_arguments` (default limit 7) is not raised
  in clippy.toml or the workspace lints, and a 9-parameter helper fails `-D warnings` ("too many arguments
  (9/7)"). Group parameters in a struct, as `RefusalCase` does.
- Do not silence that lint, or any other clippy lint, with an allow or expect attribute in the test files.
- Format the two test files with rustfmt before running the gate. `cargo fmt --all` is safe: HEAD is fmt-clean
  (the planner's run at 6d31299 printed fmt_exit=0 and clippy_exit=0), so it can only change files you edited.
  The gate's production-untouched check catches anything else.

**Tests.** Give each its own repo TempDir, cache TempDir, LiveProcess where needed, and LaunchedRunReaper with the
live pid in `spare`.
- `resume_refuses_a_phase_whose_recorded_monitor_is_alive`, phase 85.
  - Fixture: `plant_resumable_run(Some(monitor.pid()), Some(NEVER_LIVE_PID), false)`. The dead agent pid file
    matches the stopped=false control, so the arm and its control differ only in the monitor pid's liveness.
  - Assert helper: role `monitor`, `required` = [state file, agent pid file], `never_named` = [`agent pid
    {NEVER_LIVE_PID}`], refusal message "resume must refuse a phase whose recorded monitor is alive".
- `resume_refuses_a_stopped_phase_whose_recorded_agent_is_alive`, phase 86.
  - Fixture: `plant_resumable_run(Some(NEVER_LIVE_PID), Some(agent.pid()), true)`.
  - Call the assert helper twice (the idempotency edge): role `agent`, `required` = [state file, agent pid file],
    `never_named` = [`monitor pid {NEVER_LIVE_PID}`], refusal message "resume must refuse a phase whose recorded
    agent is alive".
  - Then assert that the reloaded state still has `stopped == true`, with the message "the stop mark must survive a
    refused resume".
- `resume_relaunches_a_phase_whose_recorded_processes_are_dead`: a loop over `stopped` in [false, true], on phases
  87 and 88. Each iteration is a scoped block with its own TempDirs and reaper.
  - Fixture: `plant_resumable_run(Some(NEVER_LIVE_PID), Some(NEVER_LIVE_PID), stopped)`.
  - Run resume.
  - Assert success with the message "a resume over recorded processes that are dead must relaunch as today
    (stopped={stopped})".
  - The reloaded state has `stopped == false`, and `monitor_pid` is `Some(p)` with `p != NEVER_LIVE_PID`.
  - `events_contain(root, "stage_launched")` is true.
  - This is the stop-then-resume and doctor dead-monitor recovery. It must pass today.
- `resume_without_phase_state_reports_the_missing_state_not_a_live_run`, phase 84.
  - Fixture: no state file. Only `write_agent_pid(root, phase, agent.pid())`. Assert the state file is absent.
  - Snapshot before the run.
  - Assert a non-zero exit with the message "resume without phase state must still fail".
  - stderr does not contain `refusing to resume`, with the message "without phase state, resume must report the
    missing state, not a live run". This pins the carve-out.
  - The snapshot is unchanged, the agent is still alive, and no stage_launched event exists.
- `start_refusal_text_stays_byte_identical_for_a_live_monitor`, phase 94.
  - Fixture: `plant_leftover_run(root, phase, Some(monitor.pid()), None)`, then `run_start(root, phase, &fake_bin,
    &[])` with fake agent `exit 0`.
  - Assert non-success with the message "start must still refuse a phase whose recorded monitor is alive".
  - Build `expected` as `error: ` plus the golden with `{pid}` replaced by the live pid, plus a newline. Assert
    stderr contains `expected`, with the message "the start refusal must stay byte-identical to 48-19's text".
  - Negative control: stderr does not contain `expected` with its first `refusing to start` replaced by `refusing to
    resume`. This proves the comparison is sensitive to the verb.
  - Also assert the golden's starts_with and ends_with sentences above, so an empty or truncated constant cannot
    pass.

**auto_chain_leak_repair_e2e.rs.** In `auto_chain_flag_survives_sigkill_and_is_repaired_on_the_next_start`, insert
a `wait_for(|| fixture.child_pid().is_some(), Duration::from_secs(30), …)` directly before `let child_pid =
fixture.child_pid();`. Its `what` text says three things: the monitor sets the chain flag before it spawns the
sleeper; `devflow resume` now refuses over a live recorded agent (48-20); so the orphan must be known and terminated
before resume runs. Change nothing else in that file.

Run the Task 1 gate. Both refusal arms must fail on their named first assertion: today's `resume` proceeds,
relaunches and exits 0. Every control must pass, and clippy and fmt must be clean. If an arm fails for any other
reason, a control fails, or clippy or fmt fails, fix the test, never production code. That includes the golden
constant, fixture shape (for example, a preflight that parks the control at a gate), helper wiring and helper
signatures. Record any fixture adjustment in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'R=$(git rev-parse --show-toplevel) || exit 1; cd "$R" || exit 1; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; red() { t=$1; why=$2; cargo test -p devflow --test start_lock_e2e "$t" -- --exact > "$L/$t.log" 2>&1; e=$?; fl=$(grep -c "test result: FAILED. 0 passed; 1 failed" "$L/$t.log" || true); rs=$(grep -cF "$why" "$L/$t.log" || true); echo "$t exit=$e failed_line=$fl reason=$rs"; [ "$e" -ne 0 ] && [ "$fl" -eq 1 ] && [ "$rs" -ge 1 ]; }; one() { f=$1; t=$2; cargo test -p devflow --test "$f" "$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed; 0 failed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ]; }; red resume_refuses_a_phase_whose_recorded_monitor_is_alive "resume must refuse a phase whose recorded monitor is alive" || rc=1; red resume_refuses_a_stopped_phase_whose_recorded_agent_is_alive "resume must refuse a phase whose recorded agent is alive" || rc=1; one start_lock_e2e resume_relaunches_a_phase_whose_recorded_processes_are_dead || rc=1; one start_lock_e2e start_refusal_text_stays_byte_identical_for_a_live_monitor || rc=1; one start_lock_e2e resume_without_phase_state_reports_the_missing_state_not_a_live_run || rc=1; one auto_chain_leak_repair_e2e auto_chain_flag_survives_sigkill_and_is_repaired_on_the_next_start || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ce=$?; echo "clippy_exit=$ce"; [ "$ce" -eq 0 ] || { rc=1; tail -n 40 "$L/clippy.log"; }; cargo fmt --all -- --check > "$L/fmt.log" 2>&1; fe=$?; echo "fmt_exit=$fe"; [ "$fe" -eq 0 ] || { rc=1; head -n 40 "$L/fmt.log"; }; git diff --quiet HEAD -- crates/devflow-cli/src crates/devflow-core/src; pe=$?; echo "production_untouched_diff_exit=$pe"; [ "$pe" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>The gate prints gate_rc=1 and exits non-zero. Causes: either resume refusal arm passes, fails to compile, or fails without its named "resume must refuse …" reason (reason=0); any control (dead-leftover relaunch, start golden, no-state carve-out, the auto_chain SIGKILL test) fails or matches no test (one_passed=0 or filtered=none); clippy with -D warnings or fmt --check fails (clippy_exit or fmt_exit non-zero; the gate prints the log tail); or production source under crates/devflow-cli/src or crates/devflow-core/src differs from HEAD (production_untouched_diff_exit=1)</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate prints `gate_rc=0`, `failed_line=1` and `reason` of 1 or more for both refusal arms, `one_passed=1` for all four controls, `clippy_exit=0`, `fmt_exit=0` and `production_untouched_diff_exit=0`
    - The SUMMARY quotes both RED failure reasons from separate re-runs, since the gate deletes its logs. For each arm it states that captured stdout shows resume ran to a launch
    - Apart from the phase number, the monitor arm differs from its control (stopped=false) only in the monitor pid's liveness: both plant `Some(NEVER_LIVE_PID)` as the agent pid. The agent arm differs from its control (stopped=true) only in the agent pid's liveness. Both arms list the agent pid file in `required`
    - No function added to either test file takes more than 7 parameters, and the Task 1 diff adds no attribute that silences a clippy lint
    - `git diff --stat HEAD -- crates/devflow-cli/tests/start_lock_e2e.rs` shows additions only, apart from the module doc comment. The seven existing tests and existing helpers are unchanged
    - `bash -c 'pgrep -fc "^sleep 60$" || true'` prints a count after the gate that is no higher than the count
      before it. Record both counts in the SUMMARY. This is a leak check on the reaper and LiveProcess. It proves
      nothing about other hosts, and a concurrent unrelated `sleep 60` would make it noisy
    - When one `sleep 60` is started deliberately, the same command counts it (a negative control). The planner's
      own run printed `with_one_sleeper=1 after_kill=0`
  </acceptance_criteria>
  <done>The failing end-to-end resume arms exist and fail for the right reason, the controls and the start golden pass against today's binary, and the auto_chain fixture no longer races. Ready for the orchestrator's `test(48-20): …` commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — one shared live-run guard; `resume` refuses under its lock before anything is loaded or written</name>
  <files>crates/devflow-cli/src/commands.rs, crates/devflow-cli/src/pipeline_launch.rs</files>
  <precondition>HEAD is the orchestrator's `test(48-20): …` commit containing Task 1's tests (the gate checks the subject)</precondition>
  <read_first>
    - crates/devflow-cli/src/commands.rs lines 355-372 (the `start` guard call) and 2408-2495 (`refuse_start_over_a_live_run`, `live_run_refusal`, `agent_pid_from_file`)
    - crates/devflow-cli/src/pipeline_launch.rs lines 1380-1527 (`resume` and its doc comment)
    - crates/devflow-cli/src/commands.rs lines 985-1040 (`Liveness`, `liveness`, `recovery_hints`) and 3700-3740 (`check_dead_monitor`). Read these to word the status/doctor sentence; do not modify them
    - crates/devflow-cli/tests/start_lock_e2e.rs: the 48-20 section Task 1 added, for the exact fragments to emit
  </read_first>
  <behavior>
    - All five Task 1 start_lock_e2e tests and the auto_chain SIGKILL test pass
    - start_lock_e2e reports 12 passed. auto_chain_leak_repair_e2e, stop_e2e, gate_wedge_e2e, recover_clean_e2e, start_reachability_e2e, yes_ship_config and phase7_cli stay green. The bin unit suite stays green, including the named resume unit tests
    - Inside `resume`, the guard call's line falls strictly between the `let _lock = match lock::acquire(project_root, phase) {` line and `let mut state = workflow::load_state(project_root, phase)?;`. Inside `start`, the 48-19 call line is unchanged and still between `_phase_lock` and `Gates::cleanup`
  </behavior>
  <action>
Per D-01 and D-02 (one writer per phase; resume holds the lock before any effect), criterion 2 and the operator's
2026-09-23 decision (reuse the 48-19 refusal in `resume`):

commands.rs:
- Add a private `enum LaunchVerb { Start, Resume }`, deriving `Clone, Copy, Debug, PartialEq, Eq`.
- Move the body of `refuse_start_over_a_live_run` unchanged into a private
  `fn refuse_launch_over_a_live_run(project_root: &Path, phase: PhaseId, verb: LaunchVerb) -> Result<(), CliError>`.
  The one difference is that it passes `verb` to `live_run_refusal`. Liveness, the no-state carve-out and the
  unloadable-state branch stay exactly 48-19's.
- Keep `refuse_start_over_a_live_run(project_root: &Path, phase: PhaseId)` with its name, signature and doc comment.
  Its body delegates with `LaunchVerb::Start`. You may add one doc sentence naming the shared core. Its call line in
  `start` stays byte-identical. The reason is that 48-19's must_haves artifacts and key_links grep that name and
  that line.
- Add `pub(crate) fn refuse_resume_over_a_live_run(project_root: &Path, phase: PhaseId) -> Result<(), CliError>`,
  delegating with `LaunchVerb::Resume`. Start the `pub(crate) fn` line at column 0, with its `///` doc comment
  directly above it and no attribute in between. The doc must say:
  - criterion 2, D-01, D-02, and that 999.140 was promoted by the operator on 2026-09-23;
  - that `pipeline_launch::resume` calls it right after its lock, and that the `devflow resume` CLI arm is resume's
    only production caller. No monitor or advance path calls resume, so a recorded pid is never this process. An
    agent that runs `devflow resume` for its own phase is refused, which is correct;
  - that liveness is shared with `start` (pid liveness, Option A as approved for `start`). Do not describe the
    recycled-pid cost on resume as operator-approved; name it as a known limit recorded in 48-20;
  - the no-state carve-out: without a state file resume reports its own missing-state error;
  - the unloadable-state branch: agent pid file only (999.139, backlog). For resume that branch cannot launch,
    because resume's own `load_state` fails right after;
  - that production hints still name resume in states where this check refuses, unchanged pending the operator:
    `status` and doctor for a dead monitor with a live agent, doctor's dead-agent and gate-pending findings,
    `cleanup`'s two worktree messages, and `no_waiter_repair` as printed by the gate verbs, `gate sweep`, `stop` and
    doctor (48-20's hint inventory, H1-H10);
  - that the agent-exit → `advance` window stays 999.136, and that the check says nothing about a gate waiter.
- Change `live_run_refusal(phase, live)` to `live_run_refusal(phase, verb, live)`.
  - For `LaunchVerb::Start` the returned string must be byte-identical to today's. Task 1's golden test enforces
    this against a constant captured from the unchanged binary.
  - For `LaunchVerb::Resume` the string is the Start text with exactly four differences, and everything else is the
    same bytes. The quoted text below is wrapped for width only: each quoted passage is a single line, with words
    joined by single spaces. Task 1's F-fragments are the authoritative bytes.
    1. In the first line, `refusing to start` becomes `refusing to resume`.
    2. Directly after the stop paragraph (the one ending ``once it takes the lock after this refusal.``), insert a
       newline and then this one line: ``A state already marked stopped does not mean these processes have exited.
       `devflow status` and `devflow doctor` name `devflow resume` for a phase whose monitor is dead even while its
       agent is still alive; resume refuses until every named process has exited.``
    3. `as soon as this start exits` becomes `as soon as this resume exits`.
    4. The final paragraph, from ``If the named processes have exited`` to the end, is replaced by this one line:
       ``If the named processes have exited, run `devflow resume --phase {phase}` again. If a named pid is live but
       is not this phase's process (a recycled pid), `resume` refuses again, and no DevFlow verb clears a recorded
       pid while keeping the phase's work: `devflow recover --clean --phase {phase}` removes the phase state, after
       which `resume` has nothing to resume, and `devflow start` refuses while the phase's worktree or branch
       exists; `devflow start --force` recreates them from the base and discards the phase's work. Do neither while
       this phase's processes are live: `recover --clean` clears state even while an agent runs, and a later `start`
       would launch a second agent beside it.``
  - Why difference 4 differs from `start`'s text: resume's operator wants to keep the phase's work. Plain `start`
    refuses on an existing worktree (`ensure_phase_worktree`, parallel.rs:24-51) or branch (`feature_start`,
    git.rs:157), and `--force` discards them. Offering `start` without saying that would be the dishonest-repair
    class 48-REVIEW CR-01 and WR-01 fixed.
  - Structure the shared and verb-specific pieces however reads best. Do not duplicate the liveness logic.

pipeline_launch.rs:
- In `resume`, add `crate::commands::refuse_resume_over_a_live_run(project_root, phase)?;` on the line directly after
  the `let _lock = match lock::acquire(project_root, phase) { … };` statement and directly before
  `let mut state = workflow::load_state(project_root, phase)?;`. The check therefore runs under lock-NN, before the
  handoff checks and their `save_state`/`agent_handoff` emit, the stop-mark clear, `repair_leaked_auto_chain_flag`,
  `save_state` and `launch_stage`.
- Add one paragraph to `resume`'s doc comment: criterion 2 (48-20) refuses while the recorded monitor or agent is
  alive, before anything is loaded or written; see `refuse_resume_over_a_live_run`.
- Two things Task 2's gate counts, so do not write them anywhere else:
  - Inside `resume`'s body, the guard's call text (the function name followed by its argument list) must appear
    exactly once. Do not repeat it in a comment inside the body. The doc comment above the fn is not counted.
  - The token sequence `resume(` must not appear on any non-comment line other than the existing definition,
    because the gate counts production calls of resume.

Do not change `status`, `liveness`, `Liveness::describe`, `recovery_hints`, doctor's checks, `cleanup`,
`no_waiter_repair`, `cron_hint_line`, any other hint text in the H1-H10 inventory, `lock`, `recover`, main.rs or any
State field.

If an existing test fails because it resumes over a run it left live, do not weaken the guard or silently edit that
test. Record the test name and its old and new behaviour in the SUMMARY, and stop for the orchestrator (P4: one
automated fix round, then the operator decides).
  </action>
  <reversibility rating="reversible">A refusal that writes nothing and adds no persisted field; deleting the one call in `resume` and the verb parameter restores today's behaviour exactly.</reversibility>
  <verify>
    <automated>bash -c 'R=$(git rev-parse --show-toplevel) || exit 1; cd "$R" || exit 1; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; F=crates/devflow-cli/src/commands.rs; P=crates/devflow-cli/src/pipeline_launch.rs; hs=$(git log -1 --format=%s HEAD); echo "head_subject=$hs"; case "$hs" in "test(48-20):"*) ;; *) rc=1;; esac; git diff --quiet HEAD -- crates/devflow-cli/tests; te=$?; echo "tests_untouched_since_red_exit=$te"; [ "$te" -eq 0 ] || rc=1; ch=$(git diff --name-only HEAD -- crates | sort | tr "\n" " "); echo "changed_crates_files=$ch"; [ "$ch" = "crates/devflow-cli/src/commands.rs crates/devflow-cli/src/pipeline_launch.rs " ] || rc=1; one() { f=$1; t=$2; cargo test -p devflow --test "$f" "$t" -- --exact > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed; 0 failed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ]; }; for t in resume_refuses_a_phase_whose_recorded_monitor_is_alive resume_refuses_a_stopped_phase_whose_recorded_agent_is_alive resume_relaunches_a_phase_whose_recorded_processes_are_dead resume_without_phase_state_reports_the_missing_state_not_a_live_run start_refusal_text_stays_byte_identical_for_a_live_monitor; do one start_lock_e2e "$t" || rc=1; done; for t in resume_relaunches_without_consuming_a_pending_gate_answer resume_clears_stop_marker_and_advances_past_stop_point resume_re_marks_stopped_when_launch_stage_fails_outright auto_rescan_rejection_parks_for_supervised_repair; do cargo test -p devflow --bin devflow "pipeline_launch::tests::$t" -- --exact > "$L/u-$t.log" 2>&1; e=$?; p1=$(grep -cE "^test result: ok\. 1 passed; 0 failed" "$L/u-$t.log" || true); p0=$(grep -cE "^test result: ok\. 0 passed" "$L/u-$t.log" || true); echo "unit $t exit=$e one_passed_lines=$p1 zero_passed_lines=$p0"; [ "$e" -eq 0 ] && [ "$p1" -ge 1 ] && [ "$p0" -eq 0 ] || rc=1; done; for f in start_lock_e2e auto_chain_leak_repair_e2e stop_e2e gate_wedge_e2e recover_clean_e2e start_reachability_e2e yes_ship_config phase7_cli; do cargo test -p devflow --test "$f" > "$L/$f.log" 2>&1; e=$?; ok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed; [0-9]+ ignored; [0-9]+ measured; 0 filtered out" "$L/$f.log" || true); echo "file $f exit=$e ok_lines=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; done; twelve=$(grep -c "test result: ok. 12 passed; 0 failed" "$L/start_lock_e2e.log" || true); echo "start_lock_twelve_passed=$twelve"; [ "$twelve" -eq 1 ] || rc=1; cargo test -p devflow --bin devflow > "$L/bin.log" 2>&1; be=$?; bok=$(grep -cE "^test result: ok\. [1-9][0-9]* passed; 0 failed; [0-9]+ ignored; [0-9]+ measured; 0 filtered out" "$L/bin.log" || true); echo "bin_exit=$be ok_lines=$bok"; [ "$be" -eq 0 ] && [ "$bok" -eq 1 ] || rc=1; sed -n "/^pub(crate) fn resume(/,/^}/p" "$P" > "$L/resume.rs"; l=$(grep -nF "let _lock = match lock::acquire(project_root, phase) {" "$L/resume.rs" | head -1 | cut -d: -f1); g=$(grep -nF "refuse_resume_over_a_live_run(project_root, phase)" "$L/resume.rs" | head -1 | cut -d: -f1); d=$(grep -nF "let mut state = workflow::load_state(project_root, phase)?;" "$L/resume.rs" | head -1 | cut -d: -f1); gc=$(grep -cF "refuse_resume_over_a_live_run(project_root, phase)" "$L/resume.rs" || true); echo "resume lock_line=${l:-none} guard_line=${g:-none} load_line=${d:-none} guard_calls_in_resume=$gc"; [ -n "$l" ] && [ -n "$g" ] && [ -n "$d" ] && [ "$l" -lt "$g" ] && [ "$g" -lt "$d" ] && [ "$gc" -eq 1 ] || rc=1; sed -n "/^pub(crate) fn start(/,/^}/p" "$F" > "$L/start.rs"; sl=$(grep -nF "let _phase_lock = match lock::acquire(project_root, phase) {" "$L/start.rs" | head -1 | cut -d: -f1); sg=$(grep -nF "    refuse_start_over_a_live_run(project_root, phase)?;" "$L/start.rs" | head -1 | cut -d: -f1); sc=$(grep -nF "Gates::cleanup(project_root, phase, stage)" "$L/start.rs" | head -1 | cut -d: -f1); sgc=$(grep -cF "refuse_start_over_a_live_run(project_root, phase)" "$L/start.rs" || true); echo "start lock_line=${sl:-none} guard_line=${sg:-none} cleanup_line=${sc:-none} guard_calls_in_start=$sgc"; [ -n "$sl" ] && [ -n "$sg" ] && [ -n "$sc" ] && [ "$sl" -lt "$sg" ] && [ "$sg" -lt "$sc" ] && [ "$sgc" -eq 1 ] || rc=1; n=$(grep -n "^pub(crate) fn refuse_resume_over_a_live_run(" "$F" | head -1 | cut -d: -f1); if [ -n "$n" ]; then head -n "$((n-1))" "$F" | tac | sed -n "/^\/\/\//!q;p" > "$L/doc.txt"; else : > "$L/doc.txt"; fi; d139=$(grep -c "999\.139" "$L/doc.txt" || true); d136=$(grep -c "999\.136" "$L/doc.txt" || true); echo "resume_guard_fn_line=${n:-none} doc_names_999_139=$d139 doc_names_999_136=$d136"; [ -n "$n" ] && [ "$d139" -ge 1 ] && [ "$d136" -ge 1 ] || rc=1; tl=$(grep -n "^mod tests {" "$P" | head -1 | cut -d: -f1); grep -rnE --include="*.rs" "(^|[^a-z_])resume\(" crates/devflow-cli/src crates/devflow-core/src | grep -vE "^[^:]+:[0-9]+:[[:space:]]*//" | grep -v "fn resume(" | awk -F: -v tl="$tl" -v p="$P" "\$1 != p || \$2+0 <= tl+0" > "$L/prod_calls.txt"; pc=$(wc -l < "$L/prod_calls.txt"); pm=$(grep -c "^crates/devflow-cli/src/main.rs:" "$L/prod_calls.txt" || true); echo "production_resume_calls=$pc from_main_rs=$pm"; [ "$pc" -eq 1 ] && [ "$pm" -eq 1 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ce=$?; echo "clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; cargo fmt --all -- --check > "$L/fmt.log" 2>&1; fe=$?; echo "fmt_exit=$fe"; [ "$fe" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>The gate prints gate_rc=1 and exits non-zero. The printed field names the cause:
      - head_subject does not start with test(48-20):
      - tests_untouched_since_red_exit=1, meaning a test file was edited after RED
      - changed_crates_files is anything other than commands.rs and pipeline_launch.rs
      - any named e2e or unit test fails or matches no test (one_passed=0, filtered=none, zero_passed_lines≥1)
      - any listed e2e file or the bin suite has a non-zero exit or not exactly one top-level ok line
      - start_lock_twelve_passed=0
      - in `resume` the guard is missing, duplicated or not strictly between lock_line and load_line
      - in `start` the 48-19 call moved or duplicated
      - the resume guard's doc does not name 999.139 and 999.136
      - production_resume_calls is not 1 or does not come from main.rs
      - clippy_exit or fmt_exit is non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate prints `gate_rc=0`, `start_lock_twelve_passed=1`, `guard_calls_in_resume=1` with lock_line < guard_line < load_line, `guard_calls_in_start=1` with 48-19's ordering, and `production_resume_calls=1 from_main_rs=1`. Record every printed line number in the SUMMARY
    - The SUMMARY has a "Self-refusal result" section with the production-caller output and the reasoning in this plan's analysis. It states that no self or parent pid exclusion was added, and why
    - The SUMMARY repeats every Known limits item and says which ones tests exercise. The agent arm exercises resume in H1's and H6's pid shape (dead monitor, live agent, stop-marked), the monitor arm in H3's and H7's (live monitor, dead agent pid), and the phase-84 test the no-state carve-out. No test runs a hint site itself. Recycled pid, H2's unstopped shape, H4, H5, H8-H10, IN-05, 999.139, 999.136, 999.138's end-to-end repair and the Hermes cron are not exercised
    - The SUMMARY lists two items under "Still open for the operator", neither decided here. (1) Whether the production hints that name `devflow resume` in states this guard refuses should change (999.140's undecided half). The item lists every site from the H1-H10 inventory with resume's new behaviour there: `status` (H1) and doctor's `check_dead_monitor` (H2) for a dead monitor with a live agent; doctor's `check_dead_agent` (H3) for a live monitor, transient; doctor's `check_gate_pending_without_gate` (H4) during a run relaunched with `gate_pending` set; `no_waiter_repair` (H5) as printed by `gate approve`/`gate reject`, `gate sweep`, `stop` and doctor's `check_open_gate_without_waiter` when a gate request outlived its waiter into a live run; `cleanup`'s "halted via --until" message (H6) for a stop-marked live run; `cleanup`'s live-phase refusal (H7) for a live monitor or agent; and the park message and rate-limit hints (H8-H10) in the brief window before the printing monitor exits. (2) Whether the operator accepts the recycled-pid cost on `resume`: the only DevFlow way out, `recover --clean` then `start --force`, discards the phase's work. The planner accepted it in its own remit; Option A did not cover it, and the SUMMARY must not record it as an operator decision
    - `git diff HEAD -- crates/devflow-cli/src/commands.rs` shows no change to the `start` function body and no change to `liveness`, `Liveness::describe`, `recovery_hints`, `cleanup`, `no_waiter_repair`, `cron_hint_line`, `check_dead_monitor`, `check_dead_agent`, `check_gate_pending_without_gate` or `check_open_gate_without_waiter`. `git diff HEAD -- crates/devflow-cli/src/pipeline_launch.rs` shows no change to `park_auto_rescan_repair`
  </acceptance_criteria>
  <done>`resume` over a live run is refused loudly with nothing written and honest guidance for the lock-free window, a dead leftover still resumes, and `start`'s refusal is byte-identical. Ready for the orchestrator's `feat(48-20): …` commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI (`devflow resume`) to a phase's persisted run | A resume can overwrite the state, agent pid file and capture a live monitor and agent depend on |
| recorded pids to live processes | The pids carry no start time, so liveness is not identity |
| external scheduler (Hermes cron) to `devflow resume` | A rate-limit record schedules an unattended resume |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-20-01 | Tampering | `resume` overwriting a live run's `monitor_pid`, agent pid file and capture | high | mitigate | `refuse_resume_over_a_live_run` under lock-NN before `load_state` and every write; byte-identity e2e arms (state, agent pid file), no stage_launched |
| T-48-20-02 | Elevation of Privilege | a second agent launched beside a live one for the same worktree | high | mitigate | Same guard; both roles refused (monitor arm, agent arm); placement gate |
| T-48-20-03 | Repudiation | operator believes `devflow stop` ended a lock-free run, then resumes (999.138) | medium | mitigate | The refusal says stop only marks the state and that a stop-marked state is not an exit; the agent arm runs on a stop-marked state and asserts those fragments |
| T-48-20-04 | Denial of Service | a legitimate recovery (doctor dead-monitor repair, stop→resume, #200 wedge, crash recovery) is wrongly refused | medium | mitigate | Dead-leftover control for both stop-mark values; no-state carve-out test; named resume unit tests, the bin suite and eight e2e files in the GREEN gate; auto_chain fixture made deterministic |
| T-48-20-05 | Denial of Service | a recycled pid makes a dead run look live; resume refuses, and its only DevFlow repair (`recover --clean`, then `start --force`) discards the phase's work | low | accept | Planner-remit acceptance, pending the operator: Option A (operator 2026-09-22) accepted pid-only liveness for `start`, not this cost on `resume`. Fail-closed; the text names the costly repair honestly. Identity for recorded pids is deferred with D-03 / Phase 50. Listed under "Still open for the operator" in the SUMMARY |
| T-48-20-06 | Denial of Service | operator signals an unrelated process named by a recycled pid | medium | mitigate | Shared 48-19 text requires `ps -ww -o pid=,lstart=,args=` identity before SIGTERM; DevFlow signals nothing (asserted: the recorded process is still alive after refusal) |
| T-48-20-07 | Elevation of Privilege | the guard refuses its own caller (self-refusal) and blocks legitimate work | medium | mitigate | Only production caller is the CLI arm (source gate); no monitor/advance path calls resume (analysis in context) |
| T-48-20-08 | Tampering | unloadable state hides a live monitor (999.139) | low | accept | For resume this branch cannot launch (its own `load_state` fails next); operator kept 999.139 in backlog |
| T-48-20-09 | Tampering | writes in the agent-exit → advance lock-free window (999.136) | medium | accept | Operator-deferred; unchanged |
| T-48-20-10 | Denial of Service | Hermes cron resume fires while the writer monitor still runs and is refused once | low | accept | Minute-granular schedule after the monitor has exited; reasoned, recorded in Known limits |
| T-48-20-11 | Information Disclosure | e2e tests write the operator's real cache and leak monitors | low | mitigate | New resume helper sets `DEVFLOW_CACHE_DIR` per child and reaps launched monitors (`LaunchedRunReaper`); pre-existing files remain 999.141 |
</threat_model>

<verification>
Task 1's gate proves RED for the right reason with production untouched, and proves that the controls, the
no-state carve-out, the start golden and the de-raced auto_chain fixture pass against today's binary. It also runs
clippy with `-D warnings` and `fmt --check`, because Task 2 forbids test-file edits after the RED commit. Task 2's
gate proves:
- GREEN;
- that the test files are unchanged since the RED commit, so the start golden captured pre-change still holds;
- guard placement in both verbs;
- the single production caller;
- the regression files, the bin suite, clippy and fmt.

Both gates were exercised before this plan was written. A stub `cargo` ran them in a scratch git repo, with the
guard simulated in a copy of the source. RED passed on correct fixtures and failed each of these:
- a red arm that already passes;
- a wrong failure reason;
- a failing control;
- a no-match test name;
- a failing clippy (revision 1);
- a failing fmt check (revision 1);
- touched production.

GREEN passed on the simulated fix and failed each of these:
- a failing new test;
- a no-match unit test;
- a failing regression file;
- the wrong start_lock count;
- a failing bin suite;
- a failing clippy;
- a failing fmt check (revision 1);
- an edited test file;
- an extra changed file;
- a HEAD that is not the RED commit;
- a missing guard;
- a guard after load_state;
- a doc missing 999.139;
- a second production resume caller;
- a rewritten start call site.

That exercise tested the gates' parsing and ordering logic only. It assumed libtest's output format, which the
same patterns matched in 48-19's real runs. It did not compile or run the real tests.

Revision 1 added real lint checks, outside the stub:
- At 6d31299, `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all -- --check` both exit
  0 in this worktree. Task 1's new clippy and fmt checks therefore fail only on what Task 1 adds.
- In a scratch crate (edition 2024, integration test target), a helper shaped like the old 9-parameter
  `assert_resume_refuses_live_run` failed clippy `-D warnings` with "too many arguments (9/7)", exit 101. The
  5-parameter `RefusalCase` shape passed, exit 0.
- `cargo fmt --all -- --check` exited 1 on an unformatted file there and 0 after `cargo fmt`.
- These probes cover the lint and format behaviour only. They do not show that the executor's real helpers are
  lint-clean; Task 1's gate checks that.

What this does NOT establish:
- The fixtures fabricate a live run: a test-owned `sleep` is recorded as the monitor or agent. They prove resume
  reads the recorded pids. They do not prove a real launch always records a pid before a second resume can run.
- Identity: see the recycled-pid limit.
- That the refusal's guidance works end to end. `stop`, `ps`, SIGTERM ordering and `recover --clean` are asserted
  as text only.
- The hint inventory's outcomes. The site list is cross-checked two ways (source scan, compiled-binary strings),
  but what resume does at each site is reasoned from source. No test runs a hint site, and the H4 and H5 routes
  into a live run are not reproduced.
- The downstream question of which monitor's `advance` wins after a pre-fix double launch. It no longer arises,
  because resume refuses.
- Container parity. That is the orchestrator's `scripts/check-in-container.sh` run.
- Each test is a single run, which bounds nothing about flakiness under load.

Plan-lint note: `verify.plan-structure` warns R4 ("a fallible `git` in a non-final pipeline stage"). Two matches,
both harmless:
- `R=$(git rev-parse --show-toplevel) || exit 1`, which the regex reads as a pipe (the same false positive as
  48-19).
- `git diff --name-only HEAD -- crates | sort | tr …` in Task 2. That one is a real pipe, but it fails closed: a
  failing `git diff` yields an empty list, which cannot equal the required two-file string, so the gate prints
  rc=1.
</verification>

<success_criteria>
Criterion 2's `resume` gap is closed. `resume` over a live run is refused loudly under the lock with nothing
written, and names the repair that works in the lock-free window. A dead leftover resumes as before, and the
no-state carve-out holds. `start`'s 48-19 refusal is byte-identical. The refusal was demonstrated red before the
fix. 999.139, 999.136, 999.138 and 999.141 remain recorded and unchanged. Two items remain open for the operator:
the hint question (every H1-H10 site that names `devflow resume` in a state this guard refuses, all texts
unchanged) and the recycled-pid cost on `resume`.
</success_criteria>

<output>
Create .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-20-SUMMARY.md when done.
</output>
