---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 18
type: execute
wave: 12
depends_on: ["48-14"]
files_modified:
  - crates/devflow-cli/tests/gate_wedge_e2e.rs
autonomous: true
gap_closure: true
requirements: [SURV-02]
estimate:
  tokens: 40000
  raw_tokens: 80000
  tasks: 3
  confidence: med
must_haves:
  truths:
    - "Criterion 4, D-05: a foreground `devflow start` parked at its Define preflight gate and then killed leaves a gate that `gate reject` refuses. The command exits non-zero, says 'no confirmed waiter', names `devflow recover --clean --phase N` and writes no response file. `recover --clean --phase N` then removes the phase's state, every gate file, orphaned write temps and lock-NN"
    - "Criterion 4 negative control: the same `gate reject` against a `start` left running at the same gate is consumed. reject reports the live holder, or, in the sub-millisecond race after `start` has already consumed the answer and exited, the no-holder wording. `start` exits 0 with its state file and Define gate request removed. The test prints reject_wording and pickup_ms"
    - "The #200 shape itself is pinned. While `start` is parked at the Define gate, the per-phase lock's recorded holder pid is the foreground `start` child's pid in both start arms"
    - "The start pin discriminates. A one-token mutation that drops `start`'s lock guard immediately turns the self-resolving start arm red, citing 'the parked start must hold its own phase lock'. commands.rs is restored byte-identical afterwards"
    - "The two advance-driven arms stay and are renamed so no test name says `start` while the test kills `advance`. The file-level doc states which driver each pair exercises"
  prohibitions:
    - statement: "No production source changes; this plan is test-only (the mutation control is transient and restored within the gate)."
      status: resolved
      verification: explicit
  artifacts:
    - path: crates/devflow-cli/tests/gate_wedge_e2e.rs
      provides: "#200 pinned for both drivers: start parked at Define (criterion 4's shape) and advance parked at Code"
      contains: "fn start_self_resolving_arm_live_start_consumes_the_define_rejection()"
  key_links:
    - from: crates/devflow-cli/tests/gate_wedge_e2e.rs
      to: crates/devflow-cli/src/commands.rs
      via: "real `devflow start --mode auto` child with no .planning/config.json parks at the Define preflight gate while holding lock-NN"
      pattern: "lock::holder_identity"
    - from: crates/devflow-cli/tests/gate_wedge_e2e.rs
      to: crates/devflow-cli/src/commands.rs
      via: "the same `gate reject N define` command in both start arms; only the start child's liveness differs"
      pattern: "\"reject\""
---

<objective>
Close verification gap A (criterion 4). The committed #200 reproduction interrupts a foreground `advance` at a Code
gate. Criterion 4 requires interrupting the foreground `start`. Add the two start-driven arms the operator chose on
2026-09-22 (Gap A). Arm 1: an interrupted `start` leaves a gate that `gate reject` reports honestly, and that
`recover --clean` clears (D-05). Arm 2: a live `start` left alone consumes the rejection and exits 0. That second arm
is the self-resolving negative control. Rename the existing advance arms truthfully and keep them, because they
still cover the advance-owned gate.

Purpose: nothing committed pins the #200 `start` shape today. If a regression stopped `start` holding its lock while
parked, the self-resolving case would wedge and no committed test would catch it. The verifier's scratch probe showed
the behaviour holds at HEAD a692468. This plan turns that probe into a regression pin and proves the pin discriminates.

Output: test-only changes in crates/devflow-cli/tests/gate_wedge_e2e.rs.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-VERIFICATION.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-16-SUMMARY.md
@crates/devflow-cli/tests/gate_wedge_e2e.rs
@crates/devflow-cli/tests/start_lock_e2e.rs

Execution constraint (Phase 48 #4799 workaround): the executor does NOT commit. The orchestrator commits after
verifying each task. Do not run git commit, git stash or git reset.

Gate runtime: each gate compiles and runs cargo e2e tests. The Task 2 gate builds twice (clean, then mutant) and
Task 3's gate runs clippy and fmt, so a cold run can exceed 10 minutes. Run each gate with a tool timeout of at
least 30 minutes, or in the background with explicit polling. Never run a gate under a short default timeout. A
killed gate reports nothing. A killed Task 2 gate can also leave the mutant in place, because SIGKILL skips the
restore trap. The recovery is `git restore crates/devflow-cli/src/commands.rs`, followed by
`git diff --quiet -- crates/devflow-cli/src/commands.rs`.

The verifier's working probe (session scratch, not in git) supplies the technique reused below. It spawns
`devflow start --phase N --agent claude --mode auto --no-worktree --legacy-claude-launch <root>` with a fake
`claude` (`sleep 60`) first on the child's PATH, in a repo that has a `.planning/ROADMAP.md` phase heading on
`develop` and no `.planning/config.json`. `start` parks at the Define preflight gate while holding lock-NN.
At HEAD a692468 the probe gave these results. Interrupted: the lock pid equalled the start pid, reject was
refused, no response file was written, and `recover --clean` removed the state, lock and gate files. Live: reject
was consumed, start exited 0, state was cleared, and pickup took 990 ms. The probe helpers are the same bytes as
start_lock_e2e.rs's `init_repo`, `FakeBin`, `fake_bin_dir` and `child`.

Tracked interface facts, all public and all used by existing tests:
- `devflow_core::lock::holder_identity(root, phase) -> Option<(u32, Option<u64>)>`. It is read-only. Do not use
  `lock::holder` for this: it returns the pid as a String and deletes empty lock files as a side effect.
- `devflow_core::gates::Gates::{gate_path, response_path, dir}`
- `devflow_core::workflow::state_path(root, phase)`
- On a no-waiter non-Ship gate, `gate reject` prints "no confirmed waiter … no response was written" plus
  "run `devflow resume --phase N` or `devflow recover --clean --phase N` …" (commands.rs `no_waiter_repair`).
- On a Live holder it prints "a live lock holder may pick up this response" (commands.rs `response_pickup_message`).
</context>

## Artifacts this phase produces

| Artifact | Change |
|----------|--------|
| crates/devflow-cli/tests/gate_wedge_e2e.rs | Adds start-fixture helpers, the start-driven wedge arm and the start-driven self-resolving arm. Renames the two advance-driven arms. Updates the file doc. |
| 48-18-SUMMARY.md (by the executor) | Observed pickup_ms, the mutation-control result, the old→new test-name mapping, and the correction to 48-16-SUMMARY's stated reason |

No production source file changes. The Task 2 gate mutates commands.rs transiently and restores it from a copy.

<assumption_delta_decision>
The detector fired on "second"/"another" in the phase goal ("a second writer", "another's state update"). Those
words name a concurrent writer process to exclude, not a second variant of an identity model. Primary noun: one
live run per phase. Decision: no-change. This plan only pins existing single-owner behaviour.
</assumption_delta_decision>

<!-- planner-discipline-allow: the parked start must hold its own phase lock -->
<tasks>

<task type="tracer">
  <name>Task 1: Tracer — interrupted `start` at the Define gate: reject reports no waiter, recover clears it</name>
  <files>crates/devflow-cli/tests/gate_wedge_e2e.rs</files>
  <read_first>
    - crates/devflow-cli/tests/start_lock_e2e.rs: `init_repo`, `FakeBin`, `fake_bin_dir`, `child`, `wait_for`, and `start_clears_leftover_gate_files_after_taking_the_lock`, the same Define-gate parking shape
    - crates/devflow-cli/tests/gate_wedge_e2e.rs: the whole file (271 lines); its wedge arm's recovery assertions are the model
    - .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-VERIFICATION.md: the gaps section, which lists what is missing
  </read_first>
  <action>
Per criterion 4 and D-05, add one path only: the interrupted-start arm.

Helpers, all in gate_wedge_e2e.rs. Integration test files cannot share helpers.
- Add `init_start_repo(root, phase)` copying start_lock_e2e.rs `init_repo` byte-for-byte in behaviour: a develop
  branch whose base commit contains README.md and a `.planning/ROADMAP.md` with a `### Phase {phase}: ` heading.
  There is no `.planning/config.json` and no feature branch. Keep the existing `init_repo` unchanged: the advance
  arms need its feature branch.
- Add `FakeBin`, `fake_bin_dir` and a start child builder named `start_child`, copied from start_lock_e2e.rs. The
  builder takes mode `auto`, the agent `claude`, `--no-worktree` and `--legacy-claude-launch`, and sets the child's
  PATH to the fake bin dir followed by the inherited PATH. Set it through `Command::env` only, never through a
  process-global env mutation (TEST-01, clippy.toml).
- Add a `ReapOnDrop(Child)` guard whose Drop kills the child if `try_wait` shows it still running, then waits. Every
  spawned `start` must be owned by one. A panicking assertion must never leak a parked `devflow start`: the gate
  timeout would keep it alive for tens of seconds.
- Extend the `use` lines as needed: `std::fs`, `std::os::unix::fs::{PermissionsExt, symlink}`, `std::path::PathBuf`.

Test `start_wedge_arm_interrupted_start_leaves_a_define_gate_nothing_answers`:
- Use phase 93, a `sleep 60` fake agent and DEVFLOW_GATE_TIMEOUT_SECS=30 on the child.
- Wait up to 20 s for `Gates::gate_path(root, phase, Stage::Define)` to exist, failing with a message naming the
  Define gate if it does not.
- Assert `lock::holder_identity(root, phase)` gives the start child's pid (`Child::id`), with the message
  "the parked start must hold its own phase lock". This is the #200 shape: the foreground start is the lock holder.
- Kill the child and reap it.
- Run the `gate reject N define --note "abort: #200 start e2e"` command against the root. Assert that it exits
  non-zero, that its combined stdout+stderr contains "no confirmed waiter" and `devflow recover --clean --phase
  {phase}`, and that `Gates::response_path(root, phase, Stage::Define)` does not exist.
- Run `recover --clean --phase N <root>` and assert that it succeeds. Then assert all of these are absent: the state
  file, every entry in `Gates::dir(root)` whose name starts with the padded phase and `-` (treat a missing gates
  dir as empty), every `.devflow/*.tmp` entry, and `.devflow/lock-{padded}`.

Do not touch the existing tests in this task.
  </action>
  <verify>
    <automated>bash -c 'R=$(git rev-parse --show-toplevel) || exit 1; cd "$R" || exit 1; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; t=start_wedge_arm_interrupted_start_leaves_a_define_gate_nothing_answers; cargo test -p devflow --test gate_wedge_e2e "$t" -- --exact --nocapture > "$L/t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed; 0 failed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with a non-zero exit. Causes: the test fails, does not compile, or matches no test (one_passed=0, or filtered=none)</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate prints `gate_rc=0` and `one_passed=1`
    - The arm asserts in this order: the lock holder is the start pid, then kill, then an honest refusal with no
      response file, then `recover --clean` removes state, gate files, temps and lock-NN
    - Every spawned start child is owned by `ReapOnDrop`
  </acceptance_criteria>
  <done>The interrupted-start half of criterion 4 is pinned on the real `start` shape and committed by the orchestrator.</done>
</task>

<task type="auto">
  <name>Task 2: Self-resolving start arm, proven to discriminate by a transient lock-drop mutation</name>
  <files>crates/devflow-cli/tests/gate_wedge_e2e.rs</files>
  <precondition>crates/devflow-cli/src/commands.rs has no uncommitted changes (`git diff --quiet -- crates/devflow-cli/src/commands.rs` exits 0); the gate enforces this before mutating</precondition>
  <read_first>
    - crates/devflow-cli/tests/gate_wedge_e2e.rs: the Task 1 helpers and the existing advance self-resolving arm's wait loop and pickup_ms print
    - crates/devflow-cli/src/commands.rs lines 306-375: `pub(crate) fn start` up to and including the per-phase lock acquisition bound to `_phase_lock`
  </read_first>
  <action>
Per criterion 4's negative control, add test `start_self_resolving_arm_live_start_consumes_the_define_rejection`:
- Use phase 94, a `sleep 60` fake agent and DEVFLOW_GATE_TIMEOUT_SECS=60, with the child owned by `ReapOnDrop`.
- Wait up to 20 s for the Define gate.
- Assert that the `lock::holder_identity` pid equals the child pid, with the same message as Task 1: "the parked
  start must hold its own phase lock".
- Record an Instant.
- Run the same `gate reject N define` command shape as Task 1, and assert that it succeeds.
- Accept either of two stdout wordings, and assert that one of them is present:
  - "a live lock holder may pick up this response", the expected case; or
  - "response was written, but no confirmed live holder will act". This is accepted only when the start child is
    then observed to exit successfully within the bound below, which the next assertions check.
  The second wording is legitimate. `gate_respond` re-checks the holder after publishing, and a `start` that
  consumes the response and releases its lock within that sub-millisecond window yields it (commands.rs
  `response_pickup_message` and the comment above it).
- Print `reject_wording=live` or `reject_wording=no_holder_after_exit` so SUMMARY can report which one occurred.
- Poll `try_wait` every 20 ms for up to 30 s. Fail with "live start did not consume the rejection" if it does not
  exit.
- Print `pickup_ms=<elapsed ms>`.
- Assert that the exit status is success, that the state file is absent, and that the Define gate request is absent.

The two start arms differ only in whether the start child is alive when the same reject command runs. If both
wedge, or both resolve, the harness is broken.

The mutation control needs no source edit from you. The gate below proves the pin discriminates in three steps.
1. It copies commands.rs aside. It refuses (gate_rc=1) before touching anything if `mktemp -d` returned no path
   or that path is not an existing directory, if commands.rs has uncommitted changes, or if `cmp -s` shows the copy
   differs from the original. The
   restore trap is armed only after that check, so a bad copy can never overwrite the file.
2. It rewrites the single `start` lock binding so the guard drops immediately. The named binding becomes an
   underscore pattern, a one-token change.
3. It requires the self-resolving start arm to fail with the holder message, then restores the copy and requires
   `git diff --quiet` on commands.rs.
If the gate reports `mutation_sites` other than 1, the lock binding in `start` has changed shape. Stop and report
it; do not edit commands.rs to fit the gate.

If the gate is killed (timeout, SIGKILL), the EXIT trap does not run. Before doing anything else, run
`git restore crates/devflow-cli/src/commands.rs` and confirm with
`git diff --quiet -- crates/devflow-cli/src/commands.rs`.
  </action>
  <verify>
    <automated>bash -c 'R=$(git rev-parse --show-toplevel) || exit 1; cd "$R" || exit 1; rc=0; L=$(mktemp -d); if [ -z "$L" ] || [ ! -d "$L" ]; then echo "scratch_dir=missing"; echo "gate_rc=1"; exit 1; fi; F=crates/devflow-cli/src/commands.rs; git diff --quiet -- "$F"; pre=$?; echo "commands_clean_before=$pre"; if [ "$pre" -ne 0 ]; then echo "gate_rc=1"; rm -rf "$L"; exit 1; fi; cp "$F" "$L/commands.rs.orig"; cmp -s "$F" "$L/commands.rs.orig"; bk=$?; echo "backup_matches_exit=$bk"; if [ "$bk" -ne 0 ]; then echo "gate_rc=1"; rm -rf "$L"; exit 1; fi; trap "cp \"$L/commands.rs.orig\" \"$F\"; rm -rf \"$L\"" EXIT; for t in start_wedge_arm_interrupted_start_leaves_a_define_gate_nothing_answers start_self_resolving_arm_live_start_consumes_the_define_rejection; do cargo test -p devflow --test gate_wedge_e2e "$t" -- --exact --nocapture > "$L/$t.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 1 passed; 0 failed" "$L/$t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/$t.log" | head -1); echo "$t exit=$e one_passed=$ok filtered=${fo:-none}"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; done; pk=$(grep -oE "pickup_ms=[0-9]+" "$L/start_self_resolving_arm_live_start_consumes_the_define_rejection.log" | head -1); echo "observed_${pk:-pickup_ms=absent}"; [ -n "$pk" ] || rc=1; n=$(grep -cF "let _phase_lock = match lock::acquire(project_root, phase) {" "$F" || true); echo "mutation_sites=$n"; [ "$n" -eq 1 ] || rc=1; sed -i "s/let _phase_lock = match lock::acquire(project_root, phase) {/let _ = match lock::acquire(project_root, phase) {/" "$F"; m=$(grep -cF "let _ = match lock::acquire(project_root, phase) {" "$F" || true); echo "mutant_applied=$m"; [ "$m" -eq 1 ] || rc=1; t=start_self_resolving_arm_live_start_consumes_the_define_rejection; cargo test -p devflow --test gate_wedge_e2e "$t" -- --exact > "$L/mutant.log" 2>&1; me=$?; mf=$(grep -c "test result: FAILED. 0 passed; 1 failed" "$L/mutant.log" || true); mr=$(grep -cF "the parked start must hold its own phase lock" "$L/mutant.log" || true); echo "mutant_exit=$me mutant_failed_line=$mf mutant_reason=$mr"; [ "$me" -ne 0 ] && [ "$mf" -eq 1 ] && [ "$mr" -ge 1 ] || rc=1; cp "$L/commands.rs.orig" "$F"; git diff --quiet -- "$F"; de=$?; echo "commands_restored_diff_exit=$de"; [ "$de" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with a non-zero exit. Causes: scratch_dir=missing; commands_clean_before or backup_matches_exit is non-zero (the gate stops before mutating); either start arm fails or matches no test; pickup_ms=absent; mutation_sites is not 1; the mutant arm passes or fails without "the parked start must hold its own phase lock" (mutant_reason=0); or commands_restored_diff_exit is non-zero. A gate killed mid-run prints no gate_rc line; recover with `git restore crates/devflow-cli/src/commands.rs`</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate prints `gate_rc=0`, `mutant_failed_line=1`, `mutant_reason` of 1 or more, and `commands_restored_diff_exit=0`
    - SUMMARY records which reject_wording occurred
    - SUMMARY records the observed pickup_ms. SUMMARY also states that the value is set by the gate poll's first
      backoff step (1 s doubling, gates.rs) and says nothing about the wedge itself
    - `git status --short crates/devflow-cli/src/commands.rs` prints nothing after the gate
  </acceptance_criteria>
  <done>Both start-driven #200 arms pass, and a start that drops its lock while parked turns the control red for the stated reason.</done>
</task>

<task type="auto">
  <name>Task 3: Rename the advance-driven arms truthfully and document both driver shapes</name>
  <files>crates/devflow-cli/tests/gate_wedge_e2e.rs</files>
  <read_first>
    - crates/devflow-cli/tests/gate_wedge_e2e.rs: the file doc (lines 1-2) and the two existing arms that call `spawn_gated_advance`
    - .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-16-SUMMARY.md: the "Execution constraint" bullet about the wedge wording (around line 85)
  </read_first>
  <action>
Per the operator's Gap A instruction, a test name must not say `start` while the test kills `advance`. Rename, and
keep the bodies unchanged:
- `wedge_arm_killed_start_leaves_a_gate_that_reject_reports_honestly` →
  `advance_wedge_arm_killed_advance_leaves_a_code_gate_that_reject_reports_honestly`
- `self_resolving_arm_live_start_consumes_the_rejection` →
  `advance_self_resolving_arm_live_advance_consumes_the_code_rejection`

In the second test, change the `pickup_ms=` print to `advance_pickup_ms=`. Task 2's gate reads the first
`pickup_ms=` in the start arm's own log only, but distinct keys keep a whole-file `--nocapture` run unambiguous.

Rewrite the file doc (`//!`) to say what is pinned. #200 regression: the same `gate reject` command must tell a
gate left by an interrupted foreground driver apart from a live consumer. The file pins both drivers. The `start_*`
arms cover criterion 4's shape: a foreground `start` parked at its Define preflight gate holds lock-NN itself. The
`advance_*` arms cover a foreground `advance` parked at a Code gate.

Do not edit 48-16-SUMMARY.md or the 48-14/48-16 PLAN files. 48-18-SUMMARY.md must record three things instead:
- the old→new name mapping;
- that 48-14-PLAN.md and 48-16-PLAN.md verify gates name the old tests, so a literal re-run of those historical
  gates now fails loudly (one_passed=0) rather than passing falsely;
- that 48-16-SUMMARY's reason for driving `advance` ("the live gate owner is the foreground `advance` invocation")
  does not hold for the #200 shape, where the parked `start` holds the lock.
  </action>
  <verify>
    <automated>bash -c 'R=$(git rev-parse --show-toplevel) || exit 1; cd "$R" || exit 1; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; F=crates/devflow-cli/tests/gate_wedge_e2e.rs; cargo test -p devflow --test gate_wedge_e2e > "$L/all.log" 2>&1; e=$?; ok=$(grep -c "test result: ok. 5 passed; 0 failed" "$L/all.log" || true); echo "file_exit=$e five_passed_line=$ok"; [ "$e" -eq 0 ] && [ "$ok" -eq 1 ] || rc=1; for n in advance_wedge_arm_killed_advance_leaves_a_code_gate_that_reject_reports_honestly advance_self_resolving_arm_live_advance_consumes_the_code_rejection start_wedge_arm_interrupted_start_leaves_a_define_gate_nothing_answers start_self_resolving_arm_live_start_consumes_the_define_rejection dry_run_sweep_reports_no_waiter_gates_as_left_alone; do c=$(grep -cF "fn $n()" "$F" || true); echo "defined $n=$c"; [ "$c" -eq 1 ] || rc=1; done; old=$(grep -cE "fn (wedge_arm_killed_start|self_resolving_arm_live_start)" "$F" || true); echo "misnamed_arms=$old"; [ "$old" -eq 0 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clippy.log" 2>&1; ce=$?; echo "clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; cargo fmt --all -- --check > "$L/fmt.log" 2>&1; fe=$?; echo "fmt_exit=$fe"; [ "$fe" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with a non-zero exit. Causes: the file does not report exactly 5 passed and 0 failed; any of the five names is not defined exactly once; misnamed_arms is non-zero; or clippy_exit or fmt_exit is non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate prints `gate_rc=0`, `five_passed_line=1` and `misnamed_arms=0`
    - The `//!` doc names both the `start` (Define gate, criterion 4) and `advance` (Code gate) drivers
    - The SUMMARY contains the name mapping, the historical-gate note, and the 48-16-SUMMARY correction
  </acceptance_criteria>
  <done>gate_wedge_e2e.rs pins #200 truthfully for both drivers, and every test name matches the process it interrupts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness to real `devflow` binary | Spawned children outlive a panicking test unless reaped |
| verify gate to production source | The Task 2 mutation control edits commands.rs transiently |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-18-01 | Repudiation | criterion 4 evidence: a pin that names `start` but drives another process | medium | mitigate | Both start arms assert the lock holder pid equals the `start` child pid; advance arms renamed (Task 3) |
| T-48-18-02 | Denial of Service | a parked `devflow start` leaked by a panicking assertion | low | mitigate | `ReapOnDrop` owns every spawned start child |
| T-48-18-03 | Tampering | commands.rs left mutated after the Task 2 gate | medium | mitigate | Before mutating, the gate requires that `mktemp -d` returned a path to an existing directory, a clean `git diff --quiet`, and a copy that `cmp -s` confirms identical. Only then does it arm an EXIT trap that restores the copy, and a post-restore `git diff --quiet` fails the gate if the file differs. Residual: SIGKILL skips the trap. Recovery is `git restore crates/devflow-cli/src/commands.rs`, named in the context, the action and fails_when |
| T-48-18-04 | Repudiation | pickup_ms read as a wedge measurement | low | accept | SUMMARY states it measures the 1 s poll backoff step, as the verifier recorded |
</threat_model>

<verification>
All three task gates print gate_rc=0. The mutation control shows the self-resolving start arm fails when `start`
stops holding its lock while parked.

What this does NOT establish:
- A lost-update or waiter-lifetime guarantee. The arms pin one parked shape: the Define preflight gate in auto mode
  with no config.
- Other gates `start` can park at.
- Container parity. That is the orchestrator's `scripts/check-in-container.sh` run.

Plan-lint note: `verify.plan-structure` warns R4 ("a fallible `git` in a non-final pipeline stage") on the gates.
This is a false positive. Its regex (`\bgit\s+[a-z][^\n|]*\|`, gsd-core bin/lib/verify.cjs) matches the `||` in
the `R=$(git rev-parse --show-toplevel) || exit 1` prefix as if it were a pipe. The git statuses are captured, by
`|| exit 1` and by `git diff --quiet …; pre=$?`, and no gate pipes git output.
</verification>

<success_criteria>
Criterion 4 is pinned on the `start` shape in both directions, the pin is shown to discriminate, and no test name
misstates the process it interrupts.
</success_criteria>

<output>
Create .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-18-SUMMARY.md when done.
</output>
