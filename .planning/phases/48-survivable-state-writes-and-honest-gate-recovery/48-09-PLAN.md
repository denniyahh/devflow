---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 09
type: execute
wave: 6
depends_on: ["48-04", "48-05", "48-06", "48-08", "48-15"]
files_modified:
  - clippy.toml
  - crates/devflow-cli/src/test_support.rs
  - crates/devflow-cli/src/pipeline_gate.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-core/src/config.rs
  - crates/devflow-core/src/gates.rs
  - crates/devflow-core/src/monitor.rs
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Per D-09, workspace-root clippy.toml disallows std::env::set_var and std::env::remove_var with a reason, and workspace clippy under -D warnings exits 0"
    - "Per D-09, every remaining non-PATH test mutation carries a reasoned expect for clippy::disallowed_methods on its smallest enclosing item; no production code carries that expectation"
    - "Per D-08 and D-09, comment-stripped process-global PATH mutator lines in crates read 0, and neither NeutralPath nor prepend_path remains in code"
    - "Per CX-6 and PI-S1, a temporary negative-control test makes clippy reject fully qualified, module-qualified, and renamed-alias spellings, and the probe file is restored byte-identical"
  prohibitions:
    - statement: "No non-PATH environment mutation is converted and ENV_MUTEX remains, per Deferred Ideas."
      status: resolved
      verification: explicit
    - statement: "No allow for clippy::disallowed_methods is introduced; D-09 requires reasoned expect attributes."
      status: resolved
      verification: explicit
  artifacts:
    - path: clippy.toml
      provides: "D-09 disallowed-methods configuration"
      contains: "std::env::set_var"
  key_links:
    - from: clippy.toml
      to: cargo clippy --workspace --all-targets -- -D warnings
      via: "Clippy loads the workspace-root configuration and each expected mutation remains self-policing"
      pattern: "disallowed-methods"
---

<!-- planner-discipline-allow: disallowed method -->
<!-- plan-criteria-allow: R4 - The only Git check verifies a restored temporary probe with its exit captured; the scanner joins it to a later non-Git pipeline. -->

<objective>
Make D-09 enforce the TEST-01 boundary: PATH changes are child-Command-only, while remaining test-only environment
mutations are explicit and self-policing under Clippy.

Purpose: preserve the deferred non-PATH mutations without allowing a future PATH race to re-enter under an import alias.
Output: lint configuration, narrowly scoped test expectations, and a discriminating lint probe.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-CONTEXT.md
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md

48-04, 48-05, 48-06 and 48-08 remove PATH mutators. 48-15 finishes checkpoint writes before its test-only mutations
are annotated here.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — configure the lint and annotate the remaining CLI test mutations</name>
  <files>clippy.toml, crates/devflow-cli/src/test_support.rs, crates/devflow-cli/src/pipeline_gate.rs, crates/devflow-cli/src/pipeline_launch.rs, crates/devflow-cli/src/pipeline_outcomes.rs, crates/devflow-cli/src/preflight.rs</files>
  <read_first>
    - Cargo.toml workspace clippy configuration and scripts/check.sh clippy invocation
    - crates/devflow-cli/src/test_support.rs env_lock and its remaining test-only mutation helpers
    - each CLI file's set_var or remove_var sites, located with rg
  </read_first>
  <action>
Per D-09, create clippy.toml with disallowed-methods entries for std::env::set_var and std::env::remove_var. Each
reason says process-global mutation races concurrent spawns and names the child-Command alternative. Run Clippy JSON
output for the devflow package, use its spans to add a reasoned expect for clippy::disallowed_methods on the smallest
enclosing CLI test item, and keep every expectation inside test-only code. Retain ENV_MUTEX and the non-PATH behavior
per Deferred Ideas. Verify NeutralPath, prepend_path, and process-global PATH mutations have already been removed; if a
code definition or caller remains, stop and identify the prior plan that missed it.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; test -f clippy.toml; te=$?; echo "clippy_toml_exists=$te"; [ "$te" -eq 0 ] || rc=1; for m in set_var remove_var; do c=$(grep -v "^[[:space:]]*#" clippy.toml | grep -c "std::env::$m" || true); echo "config_\${m}=$c"; [ "$c" -eq 1 ] || rc=1; done; pm=$(rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" crates --glob "*.rs" | rg -v -e "^[^:]+:[0-9]+:\s*//" | wc -l); np=$(rg -n NeutralPath crates --glob "*.rs" | rg -v -e "^[^:]+:[0-9]+:\s*//" | wc -l); pp=$(rg -n "fn prepend_path" crates --glob "*.rs" | wc -l); al=$(rg -n "allow\(clippy::disallowed_methods" crates --glob "*.rs" | wc -l); ex=$(rg -n "expect\(clippy::disallowed_methods" crates/devflow-cli --glob "*.rs" | wc -l); echo "path_mutation_lines=$pm neutral_path_code_lines=$np prepend_path_defs=$pp allow_attrs=$al cli_expect_attrs=$ex"; [ "$pm" -eq 0 ] && [ "$np" -eq 0 ] && [ "$pp" -eq 0 ] && [ "$al" -eq 0 ] && [ "$ex" -ge 1 ] || rc=1; cargo clippy -p devflow --all-targets --message-format=json -- -D warnings > "$L/cli-clippy.json" 2>&1; ce=$?; echo "cli_clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; either configuration entry is not exactly one, PATH or deleted-helper code remains, an allow attribute appears, no CLI expectation is found, or cli_clippy_exit is non-zero</fails_when>
  </verify>
  <done>The lint config and all remaining CLI test annotations are present; no process-global PATH code survives.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate core test mutations and prove three import spellings trip the lint</name>
  <files>clippy.toml, crates/devflow-core/src/config.rs, crates/devflow-core/src/gates.rs, crates/devflow-core/src/monitor.rs, crates/devflow-core/src/lib.rs</files>
  <read_first>
    - crates/devflow-core/src/config.rs, gates.rs, and monitor.rs test mutation sites located with rg
    - Task 1 clippy.toml and its SUMMARY annotation inventory
  </read_first>
  <action>
Per D-09, annotate every Clippy-reported core test mutation at the smallest enclosing test-only item with a specific
reason and leave deferred behavior intact. Then run workspace Clippy under -D warnings. Per CX-6 and PI-S1,
temporarily append three test-only probe functions to crates/devflow-core/src/lib.rs: one fully qualified mutation, one
through use std::env, and one through a renamed imported function. Capture the probe Clippy exit before parsing
diagnostics; it must be non-zero with at least three disallowed method diagnostics. Restore lib.rs byte-for-byte and
record the current HEAD commit as base_sha only after git rev-parse --verify accepts it. Capture lib.rs's pre-probe
Git diff against that base_sha; after restoration, recapture the diff against the same resolved revision and require it
to match the pre-probe diff. This treats any planned change already present before the probe as baseline, not residue.
Record base_sha and the restoration result in SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'cd .; rc=0; L=$(mktemp -d); p=crates/devflow-core/src/lib.rs; cp "$p" "$L/lib.rs.bak"; trap "cp \"$L/lib.rs.bak\" \"$p\"; rm -rf \"$L\"" EXIT; base_sha=$(git rev-parse --verify HEAD^{commit}); be=$?; echo "probe_base_resolve_exit=$be probe_base_sha=$base_sha"; [ "$be" -eq 0 ] || { echo "gate_rc=1"; exit 1; }; git diff --binary "$base_sha" -- "$p" > "$L/pre-probe.diff"; pe=$?; echo "pre_probe_diff_exit=$pe"; [ "$pe" -eq 0 ] || { echo "gate_rc=1"; exit 1; }; cargo clippy --workspace --all-targets -- -D warnings > "$L/clean.log" 2>&1; ce=$?; echo "clean_clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; printf "%s\n" "#[cfg(test)] mod lint_negative_control { use std::env; use std::env::set_var as mutate_env; #[test] fn fully_qualified() { unsafe { std::env::set_var(\"DEVFLOW_LINT_PROBE_FQ\", \"1\") } } #[test] fn module_import() { unsafe { env::set_var(\"DEVFLOW_LINT_PROBE_MODULE\", \"1\") } } #[test] fn renamed_alias() { unsafe { mutate_env(\"DEVFLOW_LINT_PROBE_ALIAS\", \"1\") } } }" >> "$p"; cargo clippy -p devflow-core --all-targets -- -D warnings > "$L/negative.log" 2>&1; ne=$?; nd=$(grep -c "disallowed method" "$L/negative.log" || true); echo "negative_control_exit=$ne negative_control_disallowed_lines=$nd"; [ "$ne" -ne 0 ] && [ "$nd" -ge 3 ] || rc=1; cp "$L/lib.rs.bak" "$p"; git diff --binary "$base_sha" -- "$p" > "$L/post-probe.diff"; po=$?; cmp -s "$L/pre-probe.diff" "$L/post-probe.diff"; re=$?; cmp -s "$p" "$L/lib.rs.bak"; rb=$?; echo "post_probe_diff_exit=$po probe_file_restored_clean=$re probe_file_restore_bytes=$rb"; [ "$po" -eq 0 ] && [ "$re" -eq 0 ] && [ "$rb" -eq 0 ] || rc=1; ex=$(rg -n "expect\(clippy::disallowed_methods" crates/devflow-core --glob "*.rs" | wc -l); echo "core_expect_attrs=$ex"; [ "$ex" -ge 1 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; probe_base_resolve_exit or either explicit-base diff exit is non-zero, clean_clippy_exit is non-zero, the negative control exits zero or yields fewer than three diagnostics, probe_file_restored_clean or probe_file_restore_bytes is non-zero, or core_expect_attrs is zero</fails_when>
  </verify>
  <done>Workspace Clippy is clean with explicit test-only exceptions, and all three known mutation spellings are proven to fail the guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test threads to process environment | A global environment mutation affects unrelated concurrent process spawns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-09-01 | Tampering | future PATH mutation | medium | mitigate | Clippy guard plus three-spelling negative control |
| T-48-09-02 | Repudiation | unreasoned lint suppression | low | mitigate | Only reasoned expect attributes allowed; gate rejects allow attributes |
| T-48-09-03 | Denial of Service | retained non-PATH mutation | low | accept | Deferred by D-09; ENV_MUTEX and reasons remain visible |
</threat_model>

<verification>
Both gates print gate_rc=0; the negative control establishes lint detection, not the absence of future test flakiness.
</verification>

<success_criteria>
PATH mutation cannot silently re-enter the test binary, while each deferred non-PATH mutation is explicit and audited.
</success_criteria>

<output>
Create .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-09-SUMMARY.md when done.
</output>
