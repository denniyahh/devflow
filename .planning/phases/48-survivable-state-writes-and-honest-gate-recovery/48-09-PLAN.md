---
phase: 48-survivable-state-writes-and-honest-gate-recovery
plan: 09
type: execute
wave: 5
depends_on: ["48-04", "48-05", "48-06", "48-07", "48-08"]
files_modified:
  - clippy.toml
  - crates/devflow-cli/src/test_support.rs
  - crates/devflow-cli/src/pipeline_gate.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-core/src/config.rs
  - crates/devflow-core/src/gates.rs
  - crates/devflow-core/src/monitor.rs
  - .planning/codebase/TESTING.md
autonomous: true
requirements: [TEST-01]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Per D-09, workspace-root `clippy.toml` lists `std::env::set_var` and `std::env::remove_var` under `disallowed-methods`, each with a `reason`, and `cargo clippy --workspace --all-targets -- -D warnings` exits 0"
    - "Per D-09, every remaining non-PATH environment mutation in tests carries `#[expect(clippy::disallowed_methods, reason = \"...\")]` on the smallest enclosing item inside `#[cfg(test)]` code; the inventory is clippy's own output, not an rg count"
    - "TEST-01 evidence (a): comment-stripped `set_var(\"PATH\"` / `remove_var(\"PATH\"` lines across `crates/` read 0 (they read 156 on 2026-09-14)"
    - "TEST-01 evidence (b), negative control: a deliberately added `std::env::set_var` in a test makes workspace clippy fail with `disallowed method` text, and the probe file is restored byte-identical afterwards"
    - "Per D-08, no process-global PATH mutator remains in `crates/devflow-cli/src/test_support.rs`: 48-04 deleted `NeutralPath` and 48-06 deleted `prepend_path`, each in the plan that removed its last caller. Task 1 verifies both are gone from code; Task 2 removes the comments and docs that still name the deleted guard type; `env_lock`'s doc states the post-conversion premise; `trailing_reap_call_is_skipped_when_a_later_assertion_panics` (C-25) still passes"
    - "Per D-09, `.planning/codebase/TESTING.md` § Environment Mutation Rule states: PATH is set only on a child `Command` via the child-test helper; other mutations need a reasoned `#[expect]`; the lint enforces it"
    - "The full-suite wall time after conversion and one `taskset -c 0,1 scripts/check.sh test` run are recorded in SUMMARY as a sanity check that does not prove the PATH flakes cannot recur (TEST-01 evidence standard)"
  prohibitions:
    - statement: "No non-PATH environment mutation is converted or `ENV_MUTEX` deleted in this plan (Deferred Ideas)"
      status: resolved
      verification: explicit
    - statement: "No `#[allow(clippy::disallowed_methods)]` is used; only `#[expect]` with a reason"
      status: resolved
      verification: explicit
    - statement: "Production code never gains an `#[expect(clippy::disallowed_methods)]` (production does not mutate the environment)"
      status: resolved
      verification: explicit
  artifacts:
    - path: clippy.toml
      provides: "disallowed-methods lint config"
      contains: "std::env::set_var"
    - path: .planning/codebase/TESTING.md
      provides: "updated Environment Mutation Rule"
  key_links:
    - from: clippy.toml
      to: scripts/check.sh clippy step
      via: "`cargo clippy --workspace --all-targets -- -D warnings` reads the workspace-root clippy.toml"
      pattern: "disallowed-methods"
---

<objective>
TEST-01 — ROADMAP alias 999.38, with backlog 999.80 folded in (D-10). Enforce "PATH only" with a clippy lint (D-09),
verify the process-global PATH mutators are gone (48-04 and 48-06 deleted them in the plans that removed their last
callers), record the TEST-01 evidence, and update TESTING.md.

The one task touching more than five files is deliberate: `clippy.toml` and every `#[expect]` must land in one commit,
because the lint without annotations fails `-D warnings`, and an `#[expect]` without the lint fails as an unfulfilled
expectation.

Contracts touched (P3): C-25 (`env_lock` premise), C-26 (TESTING.md one-mutex-per-variable invariant).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md
@.planning/codebase/TESTING.md

RESEARCH Pattern 8 (clippy probe results: the lint fires on qualified and imported calls; `#[expect]` suppresses and is
self-policing), Finding F-9 (counts), F-13 (post-commit hook regex does not match `clippy.toml`), Pitfall 6.
Measured 2026-09-14 non-PATH mutation lines: pipeline_gate.rs 6, pipeline_launch.rs 7, pipeline_outcomes.rs 4,
preflight.rs 10, config.rs 3, gates.rs 1, monitor.rs 8, pi.rs 3 (48-03 removes pi.rs's).
</context>

<!-- `disallowed method` is a positive check: the negative control must print it. -->
<!-- planner-discipline-allow: disallowed method -->

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — lint config, annotations and PATH-mutator deletion land together; the negative control fails for the right reason</name>
  <files>clippy.toml, crates/devflow-cli/src/test_support.rs, crates/devflow-cli/src/pipeline_gate.rs, crates/devflow-cli/src/pipeline_launch.rs, crates/devflow-cli/src/pipeline_outcomes.rs, crates/devflow-cli/src/preflight.rs, crates/devflow-core/src/config.rs, crates/devflow-core/src/gates.rs, crates/devflow-core/src/monitor.rs</files>
  <read_first>
    - Cargo.toml `[workspace.lints.clippy]` (near lines 43-46) and scripts/check.sh clippy invocation
    - crates/devflow-cli/src/test_support.rs lines 20-110 (`env_lock`, premise docs); the process-global PATH helpers that sat near 320-540 were deleted by 48-04 and 48-06
    - crates/devflow-core/src/config.rs lines 489-530 (`EnvOverride` target shape)
  </read_first>
  <action>
Per D-09 and D-08:
1. Confirm the process-global PATH helpers are already gone from code: `rg -n "NeutralPath|prepend_path" crates --glob "*.rs"` must show no code line (comment and doc lines are Task 2's). 48-04 deleted the guard type and 48-06 deleted `prepend_path`, each in the plan that removed its last caller; if a definition or a caller remains, stop and report which plan missed it. Delete any other helper that still sets or removes `PATH` process-globally.
2. Create `clippy.toml` at the workspace root with `disallowed-methods` entries `{ path = "std::env::set_var", reason = "process-global env mutation races concurrent spawns in the test binary; set it on the child Command instead (TEST-01)" }` and the same for `std::env::remove_var` with reason naming `Command::env_remove`.
3. Run `cargo clippy --workspace --all-targets --message-format=json -- -D warnings`, collect every `clippy::disallowed_methods` span, and add `#[expect(clippy::disallowed_methods, reason = "<variable> is process-global test configuration serialized by ENV_MUTEX; conversion deferred (48-CONTEXT Deferred Ideas)")]` on the smallest enclosing item at each span (the guard's `fn set`/`fn clear`/`fn drop`, or the statement). Keep every attribute inside `#[cfg(test)]` code. Record the site count in SUMMARY.
4. Run the gate below; it adds a probe test to `crates/devflow-core/src/lib.rs`, checks clippy fails with `disallowed method`, and restores the file.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); p=crates/devflow-core/src/lib.rs; cp "$p" "$L/lib.rs.bak"; trap "cp \"$L/lib.rs.bak\" \"$p\"; rm -rf \"$L\"" EXIT; test -f clippy.toml; echo "clippy_toml_exists=$?"; for m in set_var remove_var; do c=$(grep -v "^[[:space:]]*#" clippy.toml | grep -c "std::env::$m" || true); echo "config_${m}=$c"; [ "$c" -eq 1 ] || rc=1; done; pm=$(rg -n -e "set_var\(\"PATH\"" -e "remove_var\(\"PATH\"" crates --glob "*.rs" | rg -v -e "^[^:]+:[0-9]+:\s*//" | wc -l); np=$(rg -n NeutralPath crates --glob "*.rs" | rg -v -e "^[^:]+:[0-9]+:\s*//" | wc -l); pp=$(rg -n "fn prepend_path" crates --glob "*.rs" | wc -l); al=$(rg -n "allow\(clippy::disallowed_methods" crates --glob "*.rs" | wc -l); echo "path_mutation_lines=$pm neutral_path_code_lines=$np prepend_path_defs=$pp allow_attrs=$al"; [ "$pm" -eq 0 ] && [ "$np" -eq 0 ] && [ "$pp" -eq 0 ] && [ "$al" -eq 0 ] || rc=1; ex=$(rg -n "expect\(clippy::disallowed_methods" crates --glob "*.rs" | wc -l); echo "expect_attrs=$ex"; [ "$ex" -ge 1 ] || rc=1; cargo clippy --workspace --all-targets -- -D warnings > "$L/clean.log" 2>&1; ce=$?; echo "clean_clippy_exit=$ce"; [ "$ce" -eq 0 ] || rc=1; printf "%s\n" "#[cfg(test)] mod lint_negative_control { #[test] fn probe() { unsafe { std::env::set_var(\"DEVFLOW_LINT_PROBE\", \"1\") } } }" >> "$p"; cargo clippy -p devflow-core --all-targets -- -D warnings > "$L/neg.log" 2>&1; ne=$?; nd=$(grep -c "disallowed method" "$L/neg.log" || true); echo "negative_control_exit=$ne negative_control_disallowed_lines=$nd"; [ "$ne" -ne 0 ] && [ "$nd" -ge 1 ] || rc=1; cp "$L/lib.rs.bak" "$p"; git diff --quiet -- "$p"; rs=$?; echo "probe_file_restored_clean=$rs"; [ "$rs" -eq 0 ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; a config entry count not 1, path_mutation_lines above 0, neutral_path_code_lines above 0 (comment and doc lines are excluded here; Task 2 removes those), prepend_path_defs above 0, any allow attribute, expect_attrs=0, clean_clippy_exit non-zero, negative_control_exit=0 or negative_control_disallowed_lines=0 (the lint did not fire for the intended reason), or probe_file_restored_clean non-zero</fails_when>
  </verify>
  <acceptance_criteria>
    - Negative control, measured 2026-09-14: `path_mutation_lines` read 156 and `#[expect(` occurrences read 0 before phase 48, so both gates discriminate
    - `neutral_path_code_lines` read 13 and `prepend_path_defs` read 1 at c0fb193; 48-04 and 48-06 take both to 0, so here they are regression checks, not this plan's change
    - The negative control is asserted on `disallowed method` text, not only the exit code (a usage error would exit non-zero without that text)
    - SUMMARY lists the `#[expect]` site count per file from clippy's JSON output
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>The lint is live, every remaining mutation is explicitly expected, and no process-global PATH mutation exists anywhere in crates/.</done>
</task>

<task type="auto">
  <name>Task 2: Update TESTING.md and the env_lock premise, remove stale mentions of the deleted guard type, and decide the dev-setup checklist question</name>
  <files>.planning/codebase/TESTING.md, crates/devflow-cli/src/test_support.rs, crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - .planning/codebase/TESTING.md § Environment Mutation Rule (near lines 80-110) and § False-Green Traps
    - crates/devflow-cli/src/test_support.rs `env_lock` docs (as left by Task 1) and `trailing_reap_call_is_skipped_when_a_later_assertion_panics`
    - scripts/hooks/post-commit lines 75-90 (checklist trigger regex) and CLAUDE.md § Development Setup Checklist
  </read_first>
  <action>
Per D-08: rewrite every comment and doc comment under `crates/` that still names the process-global PATH guard type 48-04 deleted — `env_lock`'s premise docs in `test_support.rs` and the test comments in `pipeline_outcomes.rs` (locate them with the search in Task 1 step 1) — so they describe the child-process rule instead. Comments only; no code changes in `pipeline_outcomes.rs`.

Per D-09: rewrite § Environment Mutation Rule to state: tests never set or remove `PATH` process-globally — they call `devflow_core::test_support::run_test_in_child` with a directory `PATH` (never removed) and guard with `assert_child_ran_exactly_one_passing_test` using a module-qualified name; other process-global mutations are banned by `clippy.toml` and allowed only with a reasoned `#[expect(clippy::disallowed_methods, reason = ...)]` pending the deferred conversion; the one-mutex-per-variable invariant still governs those remaining sites. Update `env_lock`'s doc comment so its soundness premise names only the remaining non-PATH users. Record in SUMMARY the checklist decision: `clippy.toml` does not match the post-commit trigger regex (F-13) and adds no installed tool, so `.planning/user/DEV-SETUP-CHECKLIST.md` is not changed.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; T=.planning/codebase/TESTING.md; for w in run_test_in_child clippy.toml "expect(clippy::disallowed_methods"; do c=$(grep -cF "$w" "$T" || true); echo "testing_md[$w]=$c"; [ "$c" -ge 1 ] || rc=1; done; na=$(rg -n NeutralPath crates --glob "*.rs" | wc -l); echo "neutral_path_all_lines=$na"; [ "$na" -eq 0 ] || rc=1; t=test_support::tests::trailing_reap_call_is_skipped_when_a_later_assertion_panics; cargo test -p devflow --bin devflow "$t" -- --exact > "$L/t.log" 2>&1; ok=$(grep -c "test result: ok. 1 passed" "$L/t.log" || true); fo=$(grep -oE "[1-9][0-9]* filtered out" "$L/t.log" | head -1); echo "c25_one_passed=$ok filtered=${fo:-none}"; [ "$ok" -eq 1 ] && [ -n "$fo" ] || rc=1; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit; a TESTING.md term count 0, neutral_path_all_lines above 0 (a comment or doc still names the deleted guard type; 23 lines at c0fb193, 10 of them comment or doc lines), or the C-25 test one_passed=0 or filtered=none (if the test lives in a different module path, locate it with rg and use the real path; never a bare name)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY states the checklist decision and its F-13 rationale
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>TESTING.md and the env_lock docs describe the enforced rule.</done>
</task>

<task type="auto">
  <name>Task 3: Record TEST-01 evidence — suite time after conversion and the two-CPU sanity run</name>
  <files>.planning/codebase/TESTING.md</files>
  <read_first>
    - .planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-03-SUMMARY.md `TEST-01 baseline`
  </read_first>
  <action>
Per D-08 ("the plan records full-suite time before and after") and the TEST-01 evidence standard: run `/usr/bin/time -p scripts/check.sh test` and `taskset -c 0,1 scripts/check.sh test` (run long commands in the background with a log; the container and cold builds take minutes). This task is the TEST-01 measurement itself: nothing narrower measures full-suite wall time or a two-CPU pinned run, so its gate is necessarily the full suite rather than a short check. Launch the timing runs and the gate with the Bash tool's background mode and a log file, and read the logs when they finish. Record in SUMMARY: both `real` times beside the 48-03 baseline, each binary's `test result:` line, and any failing test names with their first panic line. State the limits: one run each; the before and after runs differ in concurrent load; a green pinned run does not prove the flakes cannot recur. If the gate needs its second attempt, record both attempts. Add no text to TESTING.md in this task unless a failure reveals a rule gap (then describe it in SUMMARY first).
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; rc=0; L=$(mktemp -d); trap "rm -rf \"$L\"" EXIT; taskset -c 0,1 scripts/check.sh test > "$L/p1.log" 2>&1; k1=$?; nf1=$(grep -c "kind: NotFound" "$L/p1.log" || true); k2=not_run; if [ "$k1" -ne 0 ]; then grep -E "^---- .* stdout ----$" "$L/p1.log" | head -8; taskset -c 0,1 scripts/check.sh test > "$L/p2.log" 2>&1; k2=$?; fi; echo "pinned_attempt1_exit=$k1 pinned_attempt1_notfound_lines=$nf1 pinned_attempt2_exit=$k2"; if [ "$k1" -ne 0 ] && [ "$k2" != "0" ]; then rc=1; fi; echo "gate_rc=$rc"; exit "$rc"'</automated>
    <fails_when>gate_rc=1 with non-zero exit: both pinned attempts exited non-zero; a non-zero pinned_attempt1_notfound_lines is reported in SUMMARY as evidence against the fix even when attempt 2 passes</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains `TEST-01 after` with the two `real` times, the 48-03 baseline beside them, and the limits sentence
    - The automated gate prints `gate_rc=0`
  </acceptance_criteria>
  <done>TEST-01's structural evidence and its sanity run are recorded with their limits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test binary threads ↔ process environment | Any thread's spawn reads the process-global environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-48-09-01 | Tampering | a future test reintroduces process-global PATH mutation | medium | mitigate | `disallowed-methods` lint under `-D warnings`; negative control |
| T-48-09-02 | Repudiation | a silent `#[allow]` hides a mutation | low | mitigate | Gate requires zero `allow(clippy::disallowed_methods)`; `#[expect]` is self-policing |
| T-48-09-03 | Denial of Service | remaining non-PATH mutations still race readers | low | accept | Deferred (CONTEXT Deferred Ideas); each carries a reasoned `#[expect]` |
</threat_model>

<verification>
All three gates print `gate_rc=0`; `scripts/check.sh all` passes.
</verification>

<success_criteria>
PATH mutation is structurally impossible to reintroduce without failing clippy, and the evidence is recorded with its limits.
</success_criteria>

## Artifacts this phase produces

- `clippy.toml` (new, workspace root) with `disallowed-methods` for `std::env::set_var` and `std::env::remove_var`
- `#[expect(clippy::disallowed_methods, reason = ...)]` attributes at clippy-reported test sites
- Verified absent (deleted by 48-04 and 48-06, each in the plan that removed the last caller): `NeutralPath` and `prepend_path` in `crates/devflow-cli/src/test_support.rs`; no comment or doc under `crates/` names the deleted guard type

<output>
Create `.planning/phases/48-survivable-state-writes-and-honest-gate-recovery/48-09-SUMMARY.md` when done.
</output>
