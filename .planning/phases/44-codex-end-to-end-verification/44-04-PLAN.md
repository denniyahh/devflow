---
phase: 44-codex-end-to-end-verification
plan: 04
type: execute
wave: 4
depends_on: ["44-00", "44-01", "44-02", "44-03"]
files_modified:
  - .planning/phases/44-codex-end-to-end-verification/44-CODEX-E2E.md
  - .planning/phases/44-codex-end-to-end-verification/44-evidence/
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [CODE-01]

estimate:
  tokens: 30000
  raw_tokens: 60000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A real phase was driven through `--agent codex` on a real repository, and the raw agent output from that run exists on disk (CODE-01, D-01)."
    - "`44-CODEX-E2E.md` states unambiguously whether the run completed the phase or surfaced a gap, and never presents local test results as end-to-end proof (D-03)."
    - "Every gap the run surfaced is either closed in this phase or filed as a GitHub issue carrying the evidence that produced it."
    - "The Codex launch argv, signing-disable environment, and Define/Plan interactivity contract are byte-identical to what they were before this phase (D-04)."
    - "The target phase had the artifact Codex's `RequiresExistingArtifact` policy needs before the run started (D-02)."
  artifacts:
    - ".planning/phases/44-codex-end-to-end-verification/44-evidence/ — raw captured agent output and command transcripts"
    - ".planning/phases/44-codex-end-to-end-verification/44-CODEX-E2E.md — the outcome record and gap disposition"
  key_links:
    - "44-01/44-02/44-03 hardening -> the dogfood run exercises it (D-01), so this plan runs last"
    - "surfaced gap -> GitHub issue -> REQUIREMENTS.md / ROADMAP.md backlog entry (a gap is not disposed of until it is traceable)"
  assumptions:
    - id: A-EDGE-01
      status: flagged-unresolved
      source: "spec-less deterministic edge probe, CODE-01 — category `unclassified`, status `unresolved`, no verification or backstop assigned"
      claim: "CODE-01's requirement text does not define its own boundary conditions: what counts as a 'real phase', whether a run that reaches Ship through only some stages counts as end-to-end, and whether a re-filed gap satisfies the requirement or merely defers it. D-03 partially answers the last of these by demanding the record say which happened, but the probe could not classify the requirement and this was NOT auto-resolved with a backstop. Task 3 must state which reading it applied and why, so the ambiguity is disposed of visibly rather than silently."
  prohibitions:
    - id: P-01
      status: flagged-unverified
      statement: "Files under `44-evidence/` must be output actually captured from a running agent. Reconstructing, summarizing, or hand-authoring a file that reads as a capture is forbidden, including when a real run failed and a plausible capture would make the phase read as complete."
    - id: P-02
      status: flagged-unverified
      statement: "If more than one dogfood attempt is made, every attempt is recorded — including abandoned and failed ones. Reporting only the attempt that went well, or silently restarting until one succeeds and describing that as 'the run', is forbidden."
    - id: P-03
      status: flagged-unverified
      statement: "A gap surfaced by the run must never be reclassified as out-of-scope in order to make the phase read as complete. Every gap is closed in this phase or filed with a traceable issue reference; 'not worth filing' is not a disposition."
---

<objective>
Drive one real phase through `--agent codex` on a real repository and record what actually happened
— completion or gap — with the raw agent output that proves it.

Purpose: CODE-01. Codex has been a fully modular driver since 37-03 with its own conformance tests,
and has still never been driven through a full phase. This plan is the deliverable; the code in
44-01/44-02/44-03 is the surface it exercises (D-01).

Output: `44-evidence/` with real captures, and `44-CODEX-E2E.md` with the outcome and gap
disposition.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-codex-end-to-end-verification/44-CONTEXT.md
@.planning/phases/44-codex-end-to-end-verification/44-RESEARCH.md
@.planning/phases/44-codex-end-to-end-verification/44-00-SUMMARY.md
@.planning/phases/44-codex-end-to-end-verification/44-01-SUMMARY.md
@.planning/phases/44-codex-end-to-end-verification/44-02-SUMMARY.md
@.planning/phases/44-codex-end-to-end-verification/44-03-SUMMARY.md
</context>

## Artifacts this phase produces

Files **created by this plan** (no new code symbols):

| Kind | Path | Notes |
|---|---|---|
| directory | `.planning/phases/44-codex-end-to-end-verification/44-evidence/` | raw captures; `43-evidence/` is the shape precedent |
| document | `.planning/phases/44-codex-end-to-end-verification/44-CODEX-E2E.md` | outcome record and gap disposition |
| edits | `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md` | CODE-01 traceability only |

Code symbols produced elsewhere in this phase: `resume --agent`, `agent_handoff`,
`cron_instructions_consumed` (44-01/44-02); `ship::hermes_schedule_from_retry_after`,
`RetryTimestamp::to_iso_utc` (44-03).

<tasks>

<task type="auto">
  <name>Task 1: Establish the pre-run baseline and prepare the dogfood target (D-02, D-04)</name>

  <files>.planning/phases/44-codex-end-to-end-verification/44-evidence/</files>

  <read_first>
    - `crates/devflow-core/src/agents/codex.rs` in full — the launch argv (`-a never exec --sandbox
      workspace-write --json`), the writable-roots handling, the five signing-disable environment
      variables, and the `interactivity_mode` match declaring `Stage::Define` and `Stage::Plan` as
      `RequiresExistingArtifact`. This is the literal content of D-04's parity guard.
    - `crates/devflow-cli/src/preflight.rs` — `preflight_interactivity_check` and
      `phase_artifact_on_develop`, which determine what the target phase must already have on
      develop before a Codex run can progress headlessly.
    - `.planning/phases/43-opencode-driver-completion/43-evidence/` — list its contents; it is the
      shape precedent for this directory (raw per-outcome-class captures, not a prose summary).
    - `.planning/ROADMAP.md` — the entries for Phase 45 and any other unstarted phase, to identify
      candidate dogfood targets.
    - `CLAUDE.md` at the repository root — in particular the rules about not running git operations
      while an executor holds the working tree, and about creating a worktree for phase work.
  </read_first>

  <action>
    Create `44-evidence/` and capture the pre-run baseline into it. Everything written here is
    captured command output, never transcribed by hand (P-01).

    Capture, each into its own file under `44-evidence/`:
      - `codex --version` and `command -v codex`, proving the binary under test and its location.
      - `git rev-parse HEAD` and `git status --short`, pinning the exact tree the run starts from.
      - The full output of `cargo test --workspace`, so the "no regression" claim in success
        criterion 3 has a real before-state to compare against rather than a remembered one.
      - The output of the five Codex driver tests, run with the valid single filter
        `cargo test -p devflow-core --lib agents::tests::codex`. Assert the run reports `5 passed`;
        a filter matching nothing exits 0 and would read as success.
      - `git diff` of `crates/devflow-core/src/agents/codex.rs` against the phase's base commit. D-04
        requires the Codex launch contract to be unchanged, and an empty diff is the direct proof;
        a non-empty one is a finding to surface before any run, not to explain away.

    Then propose the dogfood target. It must satisfy D-02: Codex declares `Define` and `Plan` as
    `RequiresExistingArtifact`, so either the target phase already has the required artifacts
    (`-CONTEXT.md` / `-PLAN.md`) on develop before `devflow start`, or it is a phase that has reached
    an artifact-bearing stage (e.g. `Stage::Code`) and is resumed under Codex via
    `devflow resume --phase N --agent codex`. Write a short `44-evidence/target-proposal.md`
    recording: the candidate phase, which artifacts it already has on develop (checked, not assumed),
    which stage the run would start/resume at, the exact `devflow` command that would launch it, and
    what the run would cost if it goes wrong. Phase 45 (DECN-01) is the obvious candidate as the next
    unstarted real phase, but confirm its artifact state rather than inheriting that suggestion.

    Do not launch anything in this task. This task ends with a proposal and a baseline.
  </action>

  <verify>
    <automated>test -d .planning/phases/44-codex-end-to-end-verification/44-evidence &amp;&amp; ls .planning/phases/44-codex-end-to-end-verification/44-evidence/ | rg -c . | rg -v '^0$'</automated>
    <automated>cargo test -p devflow-core --lib agents::tests::codex 2>&amp;1 | rg '5 passed'</automated>
  </verify>

  <acceptance_criteria>
    - `44-evidence/` exists and contains a file per baseline item above, each holding captured
      output rather than prose.
    - The Codex driver test capture shows `5 passed`, not merely a zero exit status.
    - The `codex.rs` diff against the phase base is empty, or the SUMMARY names the change and why
      it does not violate D-04.
    - `44-evidence/target-proposal.md` names one candidate phase, states which of its artifacts were
      confirmed present on develop by an actual check, and gives the verbatim launch command.
    - `cargo test --workspace` baseline output is captured with its result lines intact.
  </acceptance_criteria>

  <done>
    The pre-run state of the repository and the Codex driver is captured, and a concrete dogfood
    target with a verbatim launch command awaits operator confirmation.
  </done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Operator chooses the dogfood target and runs it</name>

  <files>.planning/phases/44-codex-end-to-end-verification/44-evidence/</files>

  <read_first>
    - `44-evidence/target-proposal.md` and every baseline capture from Task 1.
    - `CLAUDE.md` — the rule forbidding any git operation against a tree an executor holds, and the
      worktree-per-phase rule.
    - `.planning/phases/43-opencode-driver-completion/43-evidence/` — the per-outcome-class capture
      shape to reproduce.
  </read_first>

  <precondition>
    `codex` is on PATH and authenticated, and the target phase named in
    `44-evidence/target-proposal.md` already has on develop whatever artifact its starting stage
    requires under Codex's `RequiresExistingArtifact` policy (D-02).
  </precondition>

  <decision>
    Which phase the live Codex dogfood run drives, and which working tree it runs against.
  </decision>

  <context>
    This is where the phase stops being code and becomes a real agent run. Launching it creates
    branches and commits, may open a PR, and spends Codex quota — none of it undone by re-running
    this plan. CONTEXT.md deliberately names no target, so the choice is the operator's, not the
    planner's or the executor's.

    It also stops here for a mechanical reason: a supervised run outlives an executor agent's turn.
    An agent that launches it does not own it — the process is orphaned rather than supervised, and
    nobody is watching when it stalls. The operator runs it directly, then a fresh executor resumes
    at Task 3 to classify the captures.

    Present Task 1's baseline and proposal, then stop. Do not launch anything, and do not pick a
    target because one looked obvious in the proposal.

    **Rules that hold for whichever option is chosen:**
    - While the run is live, run no git operation against the tree it holds. This repository has
      already paid twice for ignoring that: a mid-run push failed a provenance gate over a file the
      executor had momentarily deleted, and a blanket `git add -A` swept an executor's in-progress
      evidence into an unrelated commit.
    - Capture, do not narrate — redirect Codex's `--json` output into `44-evidence/` as the run
      proceeds, one file per outcome class, the way `43-evidence/` does. Nothing under
      `44-evidence/` may be written by hand (P-01).
    - A failure or stall is a result. Capture it, then decide whether to retry. Every attempt keeps
      its own capture; a retry never replaces the record of what came before it (P-02).
  </context>

  <options>
    <option id="target-phase-45">
      <name>Drive Phase 45 (DECN-01) — the next unstarted real phase</name>
      <pros>Real work with real value; a completed run advances the roadmap as well as CODE-01; small, well-bounded scope</pros>
      <cons>A Codex failure mid-run leaves a genuine phase half-built; requires 45's starting-stage artifact to exist on develop first (D-02)</cons>
    </option>
    <option id="target-throwaway">
      <name>Drive a disposable scratch phase created only for this run</name>
      <pros>A failure costs nothing beyond quota; the tree can be discarded wholesale</pros>
      <cons>A synthetic phase exercises a synthetic workload — the weakest possible evidence for "Codex can drive a real phase", which is the exact claim CODE-01 makes</cons>
    </option>
    <option id="host-fresh-worktree">
      <name>Host: a dedicated fresh worktree off develop</name>
      <pros>Isolates the run's commits from this phase's branch; matches this repository's standing worktree-per-phase rule</pros>
      <cons>Linked-worktree git metadata is exactly the case Codex's writable-roots handling exists for, so a defect there surfaces as a run failure rather than a clean signal</cons>
    </option>
    <option id="host-main-checkout">
      <name>Host: the main checkout</name>
      <pros>Removes the worktree as a variable, the way phases 30-02 and 30-04 deliberately did for process experiments</pros>
      <cons>Leaves the main checkout on a half-finished branch if the run dies; a later commit lands on whatever is checked out</cons>
    </option>
  </options>

  <resume-signal>
    Reply with the chosen target option id and the chosen host option id, then run the launch command
    yourself. Signal "run complete" once the captures are on disk, stating how many attempts were made
    in total — Task 3 records that number and cannot recover it afterwards.
  </resume-signal>

  <acceptance_criteria>
    - The operator has stated a target option id and a host option id explicitly; neither was inferred.
    - `44-evidence/` contains at least one file of raw agent output whose modification times fall
      inside the recorded run window.
    - The verbatim launch command and the wall-clock span are recorded in `44-evidence/`.
    - The total attempt count is recorded and there is one capture per attempt (P-02).
  </acceptance_criteria>

  <reversibility rating="one-way">
    A live agent run spends quota that cannot be refunded and may create commits and a PR on a real
    repository. Branches and PRs can be cleaned up by hand, but the run itself cannot be un-run and
    re-running this plan does not undo it — which is why the target and host are an operator decision
    gated `blocking-human` rather than anything an unattended run may select for itself.
  </reversibility>

  <done>
    The operator has chosen a target and host, run the dogfood themselves, and confirmed the raw
    captures and the attempt count are on disk.
  </done>
</task>

<task type="auto">
  <name>Task 3: Classify the outcome and dispose of every gap (D-03, D-01)</name>

  <files>
    .planning/phases/44-codex-end-to-end-verification/44-CODEX-E2E.md,
    .planning/REQUIREMENTS.md,
    .planning/ROADMAP.md
  </files>

  <read_first>
    - Every file under `44-evidence/`, including the baseline captures from Task 1.
    - `.planning/phases/44-codex-end-to-end-verification/44-CONTEXT.md` — D-01 and D-03 verbatim.
    - `.planning/REQUIREMENTS.md` — the CODE-01 entry (~line 62) and the Traceability table
      (~line 117).
    - `.planning/ROADMAP.md` — the Phase 44 entry (~line 101) and its three success criteria.
    - `.planning/phases/44-codex-end-to-end-verification/44-0{1,2,3}-SUMMARY.md` — what the hardening
      plans actually delivered, so D-01's "which gaps were closed vs re-filed" can be answered from
      the record rather than from memory.
  </read_first>

  <action>
    Write `44-CODEX-E2E.md`. Its job is to make the outcome unambiguous, including when the outcome
    is unfavourable.

    Open with the verdict in one sentence, using exactly one of two framings (D-03): the run
    **completed a phase** through `--agent codex`, or the run **surfaced gaps**. If both are partly
    true — the run reached some stages and stopped — say which stages and do not round it up to
    completion. Local test results are not evidence for this section and must not appear in it;
    they belong in the separate regression section below.

    Then record, each traced to a file under `44-evidence/`:
      - The target phase, the host tree, the verbatim launch command, and the wall-clock span.
      - Every attempt made, in order, with its outcome (P-02).
      - The stages reached and where the run ended.
      - **What this run does not establish.** One successful phase is a single observation, not a
        reliability claim; name the stages that were never exercised, the outcome classes that never
        occurred, and the failure modes this run therefore says nothing about. Anyone reading this
        to decide whether to trust `--agent codex` for unattended work needs that gap stated, not
        inferred.
      - Whether the run exercised any of 44-01/44-02/44-03's surface — handoff, cron hint, cron
        consumption — and if not, say so plainly. D-01 says the run *should* exercise the hardening
        "where practical"; a run that did not is a legitimate outcome to record, not one to imply.

    Then dispose of every gap. For each: a one-line description, the evidence file that shows it, a
    DevFlow-vs-external classification (was this DevFlow's Codex driver, or Codex/the environment?),
    and a disposition — closed in this phase with the commit that closed it, or filed as a GitHub
    issue with its number. Every gap gets one of those two. Declaring a gap out-of-scope so the phase
    reads as complete is forbidden (P-03); if something genuinely belongs to another phase, it still
    gets an issue and a roadmap backlog entry naming that phase.

    Add a short section resolving A-EDGE-01: state which reading of "verified end-to-end" this phase
    applied, and whether CODE-01 is being marked satisfied by completion or by evidenced re-filing.
    The edge probe could not classify CODE-01's boundary conditions and nothing auto-resolved them,
    so this is where the ambiguity gets disposed of in writing.

    Record the regression check separately, as its own clearly-labelled section: `cargo test
    --workspace` after all four plans, compared against Task 1's captured baseline, plus the five
    Codex driver tests reporting `5 passed`. Label it as what it is — evidence for ROADMAP success
    criterion 3 only, and explicitly not evidence of end-to-end Codex operation.

    Finally, update traceability. In `REQUIREMENTS.md`, mark CODE-01 and set its Traceability row to
    match the actual outcome. In `ROADMAP.md`, fill Phase 44's plan list and mark the three success
    criteria against the evidence. Use `Edit` for scoped replacements in both files — never `Write`,
    which would destroy every entry outside the edited region.
  </action>

  <verify>
    <automated>test -f .planning/phases/44-codex-end-to-end-verification/44-CODEX-E2E.md &amp;&amp; rg -c 'does not establish' .planning/phases/44-codex-end-to-end-verification/44-CODEX-E2E.md | rg -v '^0$'</automated>
    <automated>cargo test --workspace 2>&amp;1 | rg -c '^test result: FAILED' | rg '^0$'</automated>
    <automated>cargo test -p devflow-core --lib agents::tests::codex 2>&amp;1 | rg '5 passed'</automated>
  </verify>

  <acceptance_criteria>
    - `44-CODEX-E2E.md` exists and its opening sentence uses exactly one of the two D-03 framings
      (completed a phase / surfaced gaps), with no local test result in that section.
    - It contains a "what this run does not establish" section naming unexercised stages and outcome
      classes.
    - It states whether the run exercised the 44-01/44-02/44-03 hardening surface, including if the
      answer is "it did not".
    - Every gap listed has an evidence file reference, a DevFlow-vs-external classification, and
      either a closing commit or a GitHub issue number. No gap is dispositioned as out-of-scope
      without an issue (P-03).
    - Every attempt from Task 2 is listed, not only the last one (P-02).
    - The A-EDGE-01 section states which reading of "verified end-to-end" was applied.
    - The regression section is separately labelled and states it is not end-to-end evidence.
    - `cargo test --workspace` reports zero `FAILED` result lines; the five Codex driver tests
      report `5 passed`.
    - `git diff .planning/ROADMAP.md` shows only the Phase 44 entry region changed — no other phase
      entry or the `## Progress` table was disturbed.
  </acceptance_criteria>

  <done>
    The phase's record says what actually happened, what it does not prove, and where every surfaced
    gap went.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow -> live Codex process | a real agent executes with `--sandbox workspace-write` and signing disabled against a real repository |
| live run -> the working tree | concurrent git operations during the run corrupt both the run and the record |
| evidence files -> the project record | downstream readers treat `44-evidence/` as ground truth for whether Codex works |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-44-15 | Elevation of Privilege | live Codex run writing outside the intended tree | medium | mitigate | Codex's existing `-a never` and `--sandbox workspace-write` launch contract is unchanged (D-04, proven by an empty diff in Task 1); the host tree is an explicit operator decision in Task 2's checkpoint |
| T-44-16 | Tampering | a concurrent git operation during the live run corrupting the tree or the evidence | high | mitigate | Task 2 forbids any git operation against the tree the run holds, per this repository's two recorded incidents; the operator supervises the window |
| T-44-17 | Repudiation | evidence that is authored rather than captured, making a failed run read as a success | high | mitigate | P-01 forbids hand-authored captures; acceptance criteria require timestamps inside the run window; Task 1's baseline is captured before anything can be motivated to look good |
| T-44-18 | Repudiation | selective reporting of only the attempt that succeeded | medium | mitigate | P-02 requires one capture per attempt and an attempt count confirmed by the operator at the checkpoint |
| T-44-19 | Information Disclosure | agent captures embedding absolute home paths, usernames, or credentials into a committed record | medium | mitigate | Review each evidence file before commit for the leak class this repository has already fixed three times (999.10 / WR-02); redact before committing, and note any redaction in the SUMMARY |
| T-44-SC | Tampering | package-manager installs | low | accept | This plan installs nothing; 44-RESEARCH.md's Package Legitimacy Audit records N/A for the phase |
</threat_model>

<verification>
- `44-evidence/` contains captured output for the baseline and for every run attempt.
- `44-CODEX-E2E.md` states the outcome in D-03's terms, its own limits, and every gap's disposition.
- `cargo test --workspace` — zero `FAILED` result lines; the five Codex driver tests report `5 passed`.
- `REQUIREMENTS.md` and `ROADMAP.md` updated by scoped `Edit`, with no collateral changes.
</verification>

<success_criteria>
A real phase has been driven through `--agent codex` with its raw output on disk; the record says
unambiguously whether that completed a phase or surfaced gaps and what it does not prove; every gap
is closed or filed with evidence; and Codex's launch contract is unchanged.
</success_criteria>

<output>
Create `.planning/phases/44-codex-end-to-end-verification/44-04-SUMMARY.md` when done.
</output>
