---
phase: 25-end-to-end-dogfood-blockers
plan: 05
type: execute
wave: 2
depends_on: ["25-03"]
files_modified:
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/src/commands.rs
autonomous: false
requirements: ["25a"]

must_haves:
  truths:
    - "A run never forks its worktree from a base branch that is behind its remote-tracking ref — the silent 'heading present but code stale' case is closed, not only the loud 'heading absent' one (999.51 binding constraint)"
    - "The currency verdict is computed against a freshly-updated remote-tracking ref, so 'the base looks current' cannot merely mean 'nobody has fetched recently'"
    - "When the remote-tracking ref cannot be resolved at all, start proceeds with a warning rather than refusing — the existing fail-open-where-blind convention for probes is preserved"
    - "A base branch that is AHEAD of its remote (unpushed local work) is not treated as stale"
    - "No refusal or warning message embeds an absolute filesystem path (WR-02)"
  artifacts:
    - path: "crates/devflow-cli/src/preflight.rs"
      provides: "base-ref currency probe and its enforcement wrapper, alongside the existing phase-reachability probe"
      contains: "fn base_ref_currency"
      exports: ["BaseRefCurrency", "base_ref_currency", "ensure_base_ref_current"]
    - path: "crates/devflow-cli/src/commands.rs"
      provides: "the start-path call site, ordered before the existing reachability guard"
      contains: "ensure_base_ref_current"
  key_links:
    - from: "crates/devflow-cli/src/commands.rs::start"
      to: "crates/devflow-cli/src/preflight.rs::ensure_base_ref_current"
      via: "called immediately before ensure_phase_reachable_on_base, so a stale base is diagnosed as staleness rather than as a missing phase"
      pattern: "ensure_base_ref_current\\("
    - from: "crates/devflow-cli/src/preflight.rs::base_ref_currency"
      to: "crates/devflow-core/src/git.rs::AncestorStatus"
      via: "the three-way ref-absent / ancestor / diverged distinction follows origin_main_ancestor_status's existing shape"
      pattern: "merge-base|is-ancestor"
---

<objective>
Stop `devflow start` from resolving its base branch out of a local ref that may be arbitrarily
behind the remote — the defect that forced a human to run `git fetch` by hand before two of
three acceptance attempts, on a goal whose entire definition is "unattended".

Purpose: 999.51 / DEN-76. On 2026-07-27 local `develop` was 0 ahead / 21 behind
`origin/develop`; Phase 24's heading existed on the remote and not locally, so
`ensure_phase_reachable_on_base` would have refused a phase that was plainly reachable. The
operator ran `git fetch origin develop:develop` by hand. `23-ACCEPTANCE-SETUP-2.md` records the
same class on 2026-07-26 at 120 behind. **The dangerous variant is the silent one**: when the
phase heading *does* exist on a stale local `develop` while the code does not, the guard passes
and the run forks from an outdated base, producing a green run against the wrong source — the
false-evidence shape D-18 exists to prevent, arriving through the base ref instead of the binary.

Output: a currency probe next to the existing reachability probe, a one-line ordered call in
`start`, and a decision checkpoint resolving the operator's recorded tension about what a stale
base should *do*.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-end-to-end-dogfood-blockers/25-CONTEXT.md
@.planning/phases/25-end-to-end-dogfood-blockers/25-RESEARCH.md
@.planning/phases/25-end-to-end-dogfood-blockers/25-PATTERNS.md
@.planning/phases/25-end-to-end-dogfood-blockers/25-VALIDATION.md

@crates/devflow-cli/src/preflight.rs
@crates/devflow-core/src/git.rs
@.planning/phases/25-end-to-end-dogfood-blockers/25-03-SUMMARY.md
</context>

<unresolved_decision>
## Why this plan opens with a checkpoint

CONTEXT.md D-17 records that 25a was offered for discussion and the operator elected not to open
it, so 999.51's three fix directions stand as the planner's input — **unchosen**, with the
operator's tension recorded verbatim and unresolved: *"refuse-loudly is the S-sized variant the
entry sizes for, but a refusal still means an unattended run does not start."*

That tension is real and it is on the phase's headline goal. Refusing loudly closes the
false-evidence hazard but leaves an unattended run dead in the water — trading a silent wrong
answer for a loud non-start. Fetching makes the run start but changes a design property this
codebase states explicitly in two places (`commands.rs:1877` and `:1980`: checks run "against
ALREADY-FETCHED local refs, issuing NO `git fetch`"). Reversing a twice-documented design
property inside an autonomous plan is exactly what `checkpoint:decision` exists for.

Task 1 puts the choice to the operator. Task 2 implements whichever arm is chosen; both arms
share the same probe, so the checkpoint changes one branch of one function, not the design.
</unresolved_decision>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: What should a stale base ref do?</name>
  <read_first>
    - .planning/ROADMAP.md § "Phase 999.51" (lines 947-965) — the three fix directions and the binding constraint
    - .planning/phases/25-end-to-end-dogfood-blockers/25-CONTEXT.md D-17 (the recorded, unresolved operator tension)
    - crates/devflow-cli/src/commands.rs lines 140-150 and 1870-1885 and 1975-1985 (the existing start-path guard, and the two places the no-fetch design is stated in source)
    - The `<unresolved_decision>` block above
  </read_first>
  <decision>
    Both options implement the same probe: fetch `origin/<base>` so the comparison is trustworthy,
    then compare local `<base>` against it. They differ only in what happens when the local ref is
    strictly behind. The binding constraint — the "heading present but code stale" case must be
    closed, not just "heading absent" — is satisfied by both.
  </decision>
  <context>
    999.51 has now forced manual intervention on two of three acceptance attempts. Option A is the
    S-sized variant the backlog entry sizes for and is strictly safe. Option B additionally makes
    the unattended run actually start in the common case, at the cost of `devflow start` performing
    one write to a local ref. The operator's own recorded tension is precisely between these two.
  </context>
  <options>
    <option id="option-a">
      <name>Fetch, compare, refuse loudly (999.51 option 3, plus the fetch that makes the comparison trustworthy)</name>
      <pros>Strictly safe — a stale base can never silently produce a green run against the wrong source. Never writes to any local ref. Smallest diff. Matches the backlog entry's S sizing. The refusal names the exact repair command, so a human recovers in one paste.</pros>
      <cons>An unattended run still does not start when the base is behind, which is the operator's recorded objection. The manual `git fetch` step 999.51 identifies as "the entire finding" is reduced from mandatory-every-time to required-only-when-behind, not eliminated.</cons>
    </option>
    <option id="option-b">
      <name>Fetch, compare, fast-forward the local base when it is safe, else refuse loudly</name>
      <pros>An unattended run starts on a current base with no human, which is the phase's headline goal. The fast-forward is only attempted when the local ref is a strict ancestor of the remote (no divergence, no possible content loss) and the base branch is not the currently checked-out branch. Every other case still refuses loudly, so option A's safety property is retained as the fallback.</pros>
      <cons>Reverses the twice-documented "no `git fetch` in the start path" design property, adds a write to a local ref inside `start`, and adds two failure modes to test (base checked out; fast-forward refused by git). Moves the unit from S toward M.</cons>
    </option>
  </options>
  <verify>
    <human-check>Operator selects one option; the selection is recorded verbatim in 25-05-SUMMARY.md with its date, in the same shape CONTEXT.md records its own operator adjudications.</human-check>
  </verify>
  <acceptance_criteria>
    - The response names exactly one of `option-a` or `option-b`
    - The selection and its rationale are recorded verbatim in `25-05-SUMMARY.md`
    - If `option-b` is selected, the summary records the operator's explicit acknowledgement that
      the no-fetch design property stated at `commands.rs:1877` and `:1980` is being reversed for
      the start path, so a future reader does not treat those comments as a contradiction
  </acceptance_criteria>
  <resume-signal>Select: option-a or option-b</resume-signal>
  <reversibility rating="reversible">
    Either arm is one branch of one function; switching later is a small, local change with no
    persisted-state or published-API consequence.
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Base-ref currency probe, wired ahead of the reachability guard</name>
  <files>crates/devflow-cli/src/preflight.rs, crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/preflight.rs lines 100-260 (`PhaseReachability`, `phase_reachability_on_base`, `unreachable_message`, `ensure_phase_reachable_on_base` — the probe/message/enforce triple this task mirrors, including the documented "fails open where it cannot see" contract on lines 250-251)
    - crates/devflow-cli/src/preflight.rs lines 980-1110 (the existing `phase_reachability_on_base` and `unreachable_message` test suites — the fixture idioms and assertion style the new tests must match)
    - crates/devflow-core/src/git.rs lines 462-508 (`origin_main_ancestor_status` / `AncestorStatus` — the direct template: `rev-parse --verify --quiet` for ref existence, then `merge-base --is-ancestor`, with spawn failure and non-zero exit collapsed into the same fail-open branch)
    - crates/devflow-cli/src/commands.rs lines 140-150 (the existing `ensure_phase_reachable_on_base(project_root, phase, DEVELOP)?` call at line 146 and the doc comment above it explaining why it precedes both fork paths) and line 285 (the worktree fork's start point)
    - crates/devflow-core/src/config.rs lines 15-19 (`DEVELOP` and the other hardcoded branch constants)
    - crates/devflow-core/src/test_support.rs lines 55-85 (`git_command`/`hermetic_command`)
    - `25-05-SUMMARY.md`'s recorded selection from Task 1
  </read_first>
  <behavior>
    - Test: local base equal to its remote-tracking ref → `Current` → `Ok(())`, no output.
    - Test: local base strictly behind the remote (local is an ancestor of the remote, refs differ) → `Behind { count }` → under option-a, `Err` whose message names the base, the remote-tracking ref, the commit count and the exact repair command; under option-b, a fast-forward is attempted first and only its failure yields that `Err`.
    - Test: local base strictly ahead of the remote (unpushed local work) → `Ahead` → `Ok(())`. This must NOT be treated as stale.
    - Test: local and remote genuinely diverged (mutual non-ancestors) → `Diverged` → `Err` naming both refs; a divergent base is not silently forked from.
    - Test: the remote-tracking ref does not resolve at all (no remote configured, offline clone) → `Undeterminable` → `Ok(())` with a printed warning, matching the module's existing fail-open-where-blind contract.
    - Test: the fetch subprocess failing (non-zero exit or spawn error) does not itself fail the check — the comparison proceeds against whatever the remote-tracking ref currently is, and the warning says the comparison may be stale.
    - Test: no message produced on any branch contains an absolute filesystem path.
  </behavior>
  <action>
Add to `crates/devflow-cli/src/preflight.rs`, immediately alongside the existing
phase-reachability triple so the two probes read as siblings:

1. `pub(crate) enum BaseRefCurrency { Current, Ahead, Behind { count: u32 }, Diverged, Undeterminable }`
   — the same shape as `PhaseReachability`, with `Undeterminable` carrying the same
   fail-open-where-blind meaning the module already documents.

2. `pub(crate) fn base_ref_currency(project_root: &Path, base: &str) -> BaseRefCurrency`
   — a pure probe with no side effects beyond the fetch described below. Follow
   `origin_main_ancestor_status`'s shape verbatim: `git rev-parse --verify --quiet
   origin/<base>` for ref existence, collapsing spawn failure and non-zero exit into the same
   `Undeterminable` branch, then `git merge-base --is-ancestor` in **both** directions to
   distinguish behind, ahead, equal and diverged. Get the behind-count with
   `git rev-list --count <base>..origin/<base>` so the message can state how far behind.

   Before comparing, issue `git fetch --quiet <remote> <base>`, which updates only the
   remote-tracking ref — it never touches the local branch or the working tree, so it cannot
   fail with the "branch is checked out" error that a refspec fetch into the local branch
   would. **The fetch must fail soft**: on spawn error or non-zero exit, print a warning naming
   the base and stating the comparison may be stale, then continue with whatever the
   remote-tracking ref currently holds. This is Phase 24's inherited D-06 precedent — a new
   code path must not introduce a false hard-block on correct work, and an offline machine is
   correct work. Do not let the fetch block indefinitely; if the codebase has no existing
   subprocess timeout idiom, note that in the summary rather than inventing one here.

3. `pub(crate) fn stale_base_message(base: &str, remote_ref: &str, count: u32) -> String`
   — modelled on `unreachable_message`: self-contained, actionable, naming the exact repair
   command, and containing **no absolute filesystem path** (WR-02 — on Linux a path embeds the
   operator's username, and this string reaches operator output). Name refs and commands only.

4. `pub(crate) fn ensure_base_ref_current(project_root: &Path, base: &str) -> Result<(), CliError>`
   — mirroring `ensure_phase_reachable_on_base` exactly: `Current`, `Ahead` and `Undeterminable`
   all return `Ok(())` (the last with a warning); `Behind` and `Diverged` return
   `Err(CliError::Message(...))`. **Implement the `Behind` arm according to Task 1's recorded
   selection.** If `option-b` was selected, attempt a fast-forward of the local ref before
   refusing, but only when the local ref is a strict ancestor of the remote AND the base branch
   is not the currently checked-out branch (check with `git symbolic-ref --quiet --short HEAD`);
   on any failure, fall through to the same refusal option-a produces. `Diverged` refuses under
   both options — a divergent base is never silently forked from.

Wire the call into `crates/devflow-cli/src/commands.rs`'s `start`: insert
`ensure_base_ref_current(project_root, DEVELOP)?;` **immediately before** the existing
`ensure_phase_reachable_on_base(project_root, phase, DEVELOP)?;` at line 146. **The order is
load-bearing**: a stale base is the most common cause of a phase heading appearing absent, and
running reachability first would diagnose the symptom (phase not found) instead of the cause
(base is 21 commits behind) — which is exactly the misdiagnosis the existing doc comment above
line 146 already warns about for the Codex leg. Extend that doc comment to record the new
ordering and its reason. Do not disturb plan 25-03's `enforce_build_staleness` call further down
in the same function.

Write the `<behavior>` tests in `preflight.rs`'s existing test module, constructing fixtures with
`devflow_core::test_support::git_command` — never a bare git command — and building a real local
repository with a real second repository serving as its remote, so `origin/<base>` genuinely
exists and can be moved independently of the local branch. Model the fixture shape on the
existing `phase_reachability_on_base` tests.
  </action>
  <verify>
    <automated>cargo test --package devflow --lib preflight:: 2>&1 | tee /tmp/p1.log; grep -E '[0-9]+ passed' /tmp/p1.log; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -c 'fn base_ref_currency' crates/devflow-cli/src/preflight.rs` returns 1
    - Source assertion: `rg -c 'fn ensure_base_ref_current' crates/devflow-cli/src/preflight.rs` returns 1
    - Source assertion (ordering — currency precedes reachability in `start`): in
      `crates/devflow-cli/src/commands.rs`, the line number of `ensure_base_ref_current(` is LESS
      than the line number of `ensure_phase_reachable_on_base(` — verify with `rg -n` on both
    - Source assertion (fail-open preserved for the blind case): the `Undeterminable` arm of
      `ensure_base_ref_current` returns `Ok` —
      `rg -A20 'fn ensure_base_ref_current' crates/devflow-cli/src/preflight.rs | rg -c 'Undeterminable'` returns ≥ 1
      and that arm is grouped with the other `Ok(())` arms
    - Source assertion (WR-02): `rg -A20 'fn stale_base_message' crates/devflow-cli/src/preflight.rs | rg -c 'project_root|display\(\)'` returns 0
    - Test command: `cargo test --package devflow --lib preflight::` exits 0 AND its output
      contains a `N passed` line with `0 failed`, N greater than the pre-change count (the package
      is `devflow`, not `devflow-cli`; assert on the passed count, never on exit code alone)
    - Test command: `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - Behavior assertion: the ahead-of-remote fixture returns `Ok(())` — unpushed local work is
      not misreported as staleness
    - Behavior assertion: the offline fixture (no remote configured) returns `Ok(())` with a
      warning, and `start` proceeds
    - Behavior assertion: the refusal message from the behind fixture contains the base name, the
      remote-tracking ref name, the commit count and a runnable repair command, and does not
      contain the fixture's absolute root path
  </acceptance_criteria>
  <done>
    `devflow start` refuses to fork from a base branch that is behind or diverged from its remote,
    proceeds normally when the base is current or ahead, proceeds with a warning when the remote
    cannot be seen at all, and diagnoses staleness before reachability. Task 1's selected arm is
    the one implemented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote repository → local base ref | `git fetch` brings remote-controlled refs into the local repository before any comparison is made |
| base ref → forked worktree | Whatever `develop` points at becomes the start point for the phase's worktree and therefore the source every stage runs against |
| refusal/warning message → operator output | Absolute paths on Linux embed the operator's username (WR-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-40 | Spoofing | A run forking from a stale base produces a green result against the wrong source — false evidence of the same class D-18 exists to prevent, arriving through the base ref instead of the binary. This is 999.51's explicitly-named "dangerous variant" | high | mitigate | `ensure_base_ref_current` refuses on `Behind` and `Diverged` and is ordered before the reachability guard, so the silent "heading present but code stale" case is closed, not only the loud "heading absent" one. Asserted by a line-ordering source criterion and by the behind/diverged behavior tests |
| T-25-41 | Denial of Service | A `git fetch` that hangs, or that hard-fails on an offline machine, wedges or blocks `devflow start` on correct work | medium | mitigate | The fetch fails soft — spawn error and non-zero exit both fall through to a warning and the comparison proceeds; `Undeterminable` returns `Ok(())`. This preserves the module's documented fail-open-where-blind contract and Phase 24's inherited D-06 (a new path must not introduce a false hard-block on correct work). If no subprocess timeout idiom exists in this codebase, the residual is recorded in the summary rather than papered over |
| T-25-42 | Information Disclosure | The refusal and warning strings reaching operator output with an absolute path embedded | medium | mitigate | `stale_base_message` names refs, counts and commands only — modelled on `unreachable_message`, which the existing test `unreachable_message_names_the_base_branch_and_each_missing_half` already asserts this property for. Asserted by a negative source criterion and a behavior test |
| T-25-43 | Tampering | Under option-b, `start` writes to a local ref, so a compromised or misconfigured remote could advance the local base | medium | accept | The fast-forward is attempted only when the local ref is a strict ancestor of the remote, which is a lossless operation, and only when the base is not checked out. This is the same trust in `origin` the repository already extends every time a human runs the fetch this automates. Not applicable at all if option-a is selected |
| T-25-44 | Spoofing | Misclassifying unpushed local work as staleness would refuse correct, deliberate work | medium | mitigate | The probe compares in both directions and returns `Ahead` for a local ref strictly ahead of its remote, which maps to `Ok(())`. Directly asserted by a behavior test |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 25-05)

New symbols created by this plan — exclude these from drift verification against pre-phase source:

| Symbol | Kind | File |
|---|---|---|
| `BaseRefCurrency` | `pub(crate) enum` (`Current`/`Ahead`/`Behind`/`Diverged`/`Undeterminable`) | `crates/devflow-cli/src/preflight.rs` |
| `base_ref_currency` | `pub(crate) fn` | `crates/devflow-cli/src/preflight.rs` |
| `stale_base_message` | `pub(crate) fn` | `crates/devflow-cli/src/preflight.rs` |
| `ensure_base_ref_current` | `pub(crate) fn` | `crates/devflow-cli/src/preflight.rs` |

Modified (not new): `crates/devflow-cli/src/commands.rs::start` (one inserted call plus a doc
comment). `ensure_phase_reachable_on_base`, `phase_reachability_on_base` and `unreachable_message`
are unchanged.
</artifacts_produced>

<verification>
- `cargo test --package devflow --lib preflight::` — explicit passed count, `0 failed`
- `cargo build --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` exit 0
- `cargo fmt --check` clean
- Task 1's selection recorded verbatim in the summary with its date
- Manual sanity check against this repository: with local `develop` deliberately reset one commit
  behind `origin/develop`, `devflow start` produces the refusal (or, under option-b, the
  fast-forward) rather than proceeding — then restore the local ref
</verification>

<success_criteria>
- Both 999.51 cases are closed: heading absent AND heading present but code stale
- The currency check runs before the reachability check
- Ahead, current and unresolvable all proceed; behind and diverged do not silently proceed
- No message embeds a filesystem path
- The operator's option selection is recorded
</success_criteria>

<output>
Create `.planning/phases/25-end-to-end-dogfood-blockers/25-05-SUMMARY.md` when done.
Record: Task 1's verbatim selection and date; whether a subprocess timeout idiom existed for the
fetch or whether that residual remains open; the pre- and post-change `preflight::` passed counts;
and, if option-b was selected, the operator's acknowledgement that the no-fetch design property
documented at `commands.rs:1877`/`:1980` is reversed for the start path.
</output>
