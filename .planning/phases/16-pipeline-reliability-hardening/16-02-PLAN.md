---
phase: 16-pipeline-reliability-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/devflow-core/Cargo.toml
  - crates/devflow-core/src/config.rs
  - crates/devflow-core/src/lib.rs
autonomous: false
requirements: [16a, 16b, 16d, 16e]
user_setup: []
must_haves:
  truths:
    - "A minimal devflow.toml at the project root, when present, is parsed into a typed Config carrying ONLY Phase 16's new knobs; when absent, built-in defaults are used with zero behavior change."
    - "Precedence is env var > devflow.toml > built-in default: a DEVFLOW_* env var always overrides the file, and the file always overrides the default."
    - "The config.rs module doc comment no longer claims 'no config file' — it documents the deliberate D-03 reopening for a minimal file."
    - "GitFlowConfig's hardcoded main/develop/feature_prefix constants are unchanged."
  artifacts:
    - "Cargo.toml — toml = \"1\" workspace dependency"
    - "crates/devflow-core/Cargo.toml — toml.workspace = true"
    - "crates/devflow-core/src/config.rs — DevflowConfig struct (review_angles, capture_retention, verification) + loader with env>file>default precedence"
  key_links:
    - "load_config(project_root) → toml::from_str on devflow.toml if present → else DevflowConfig::default()"
    - "Existing gates.rs env-var-first idiom is the precedence layer that stays ON TOP of the new file"
---

<objective>
Introduce the minimal `devflow.toml` config foundation (D-03) that 16a (external-verification settings), 16b (capture-retention N), and 16d/16e (review-angle override) all read. D-03 deliberately REOPENS the Phase 11/15a "no config file" decision — with full knowledge of it — for a minimal TOML file carrying ONLY Phase 16's new knobs. Existing `DEVFLOW_*` env vars are untouched and remain the top precedence layer (env > file > default). Env-var migration and full pipeline configurability stay out of scope (deferred).

This plan adds the one new dependency of the phase (`toml`, verified to resolve to v1.1.3 in this session), gates that dependency edit behind a blocking package-legitimacy checkpoint per the ASSUMED-package protocol, defines the typed config struct with all Phase 16 knobs up front so downstream plans only READ config, implements the precedence loader, and corrects the now-false "no config file" doc comment.

Purpose: give 16a/16b/16d/16e a single typed, tested config surface so no downstream plan reinvents parsing or precedence.
Output: `toml` dependency, `DevflowConfig` struct + `load_config` loader, corrected doc comment, tests.
</objective>

<planning_notes>
- Spec-less probe fallback: no SPEC.md Edge/Prohibitions sections; fallback probe visibly skipped (no requirement IDs). must_haves derived goal-backward from D-03 and the 16a/16b/16d/16e config needs. Recorded, deliberate skip.
- Package legitimacy: RESEARCH.md `## Package Legitimacy Audit` present; `toml` verdict OK but tagged [ASSUMED] (provenance rule) → Task 1 is a blocking-human legitimacy checkpoint, non-auto-approvable, before the Cargo.toml edit.
- API-coverage / assumption-delta detectors: both `detected:false` — no COVERAGE.md, no assumption-delta block. Schema-gate: no ORM, skipped.
</planning_notes>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-pipeline-reliability-hardening/16-CONTEXT.md
@.planning/phases/16-pipeline-reliability-hardening/16-RESEARCH.md
@.planning/phases/16-pipeline-reliability-hardening/16-PATTERNS.md
@Cargo.toml
@crates/devflow-core/Cargo.toml
@crates/devflow-core/src/config.rs
@crates/devflow-core/src/gates.rs
</context>

## Artifacts this phase produces (this plan)

- `Cargo.toml`: `[workspace.dependencies] toml = "1"`
- `crates/devflow-core/Cargo.toml`: `toml.workspace = true`
- `crates/devflow-core/src/config.rs`: `pub struct DevflowConfig` with fields `review_angles: Option<Vec<String>>`, `capture_retention: usize`, `verification_enabled: bool` (final field set is the implementer's, but MUST cover 16b retention-N + 16d angle override + 16a verification toggle); `impl Default for DevflowConfig`; `pub fn load_config(project_root: &Path) -> DevflowConfig`; a `retention()`/`review_angles()` style resolver honoring env>file>default
- New config file recognized at runtime: `<project_root>/devflow.toml` (operator-authored)

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0 (blocking-human): Confirm `toml` package legitimacy before the Cargo.toml edit</name>
  <what-built>Nothing yet — this checkpoint gates the ONE new external dependency of the phase before it is added.</what-built>
  <action>
    The `toml` crate is the only new dependency (D-03). RESEARCH.md's Package Legitimacy Audit marks it OK but [ASSUMED] (discovered via websearch/training, not a first-party docs fetch), so per the ASSUMED-package protocol the operator confirms it by hand before the Cargo.toml edit is committed. This checkpoint is non-auto-approvable (workflow.auto_advance is ignored for package-legitimacy gates). Do NOT edit Cargo.toml until approved.
  </action>
  <how-to-verify>
    1. Open https://crates.io/crates/toml — confirm it is `toml-rs/toml` (repo github.com/toml-rs/toml), ~11 years old, ~13M weekly downloads, actively maintained.
    2. Confirm the version to pin: `cargo add toml -p devflow-core --dry-run` (verified in planning to resolve to v1.1.3; default features include `serde` + `parse` + `display`). Pin as `toml = "1"` in [workspace.dependencies] to match the existing `serde = "1"` idiom.
    3. Confirm no typosquat: the crate name is exactly `toml` (not `toml-rs`, `toml2`, `toml_edit` — toml_edit is the sibling low-level crate; we want `toml` for serde Deserialize).
  </how-to-verify>
  <verify>Operator confirms crates.io metadata (repo, age, downloads) and the exact `toml = "1"` pin; no typosquat.</verify>
  <done>Operator approved `toml` legitimacy and the `toml = "1"` pin; the Cargo.toml edit in Task 1 is unblocked.</done>
  <resume-signal>Type "approved" to confirm `toml` legitimacy and the `toml = "1"` pin, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Add the toml dependency and the typed DevflowConfig struct + precedence loader</name>
  <files>Cargo.toml, crates/devflow-core/Cargo.toml, crates/devflow-core/src/config.rs, crates/devflow-core/src/lib.rs</files>
  <read_first>
    - Cargo.toml (root — [workspace.dependencies] block: serde = { version = "1", features = ["derive"] }, serde_json = "1", etc. — the pin idiom to match)
    - crates/devflow-core/Cargo.toml ([dependencies] uses `serde.workspace = true` etc.; [dev-dependencies] tempfile = "3")
    - crates/devflow-core/src/config.rs (whole file — module doc comment 1-6 claiming "no .devflow.yaml"/"no config file", GitFlowConfig struct 20-38, Default impl 30-38, test 40-51)
    - crates/devflow-core/src/gates.rs (fire_gate_notify 282-288 — the env-var-first read idiom that defines the top precedence layer to preserve)
    - crates/devflow-core/src/lib.rs (module declarations — confirm config is already `pub mod config;`)
  </read_first>
  <action>
    Add `toml = "1"` to the root `Cargo.toml` `[workspace.dependencies]` (pin approved in the checkpoint above; default features suffice — they include serde Deserialize support). Add `toml.workspace = true` to `crates/devflow-core/Cargo.toml` `[dependencies]`.

    In config.rs, keep `GitFlowConfig` and its MAIN/DEVELOP/FEATURE_PREFIX constants exactly as-is (D-03 leaves the git-flow model hardcoded). Add a NEW sibling struct `DevflowConfig` deriving `Debug, Clone, PartialEq, Eq, serde::Deserialize`, with `#[serde(default)]` on the struct so a partial or absent file fills missing fields from Default. Fields must cover all three Phase 16 knob families named in D-03: `capture_retention: usize` (16b — how many per-stage capture generations to keep; default a small N such as 5), `review_angles: Option<Vec<String>>` (16d — overrides the built-in angle list; None = use built-ins), and a verification toggle/settings field for 16a (e.g. `external_verify_enabled: bool`, default true). Implement `impl Default for DevflowConfig` with those defaults, matching the existing `Default`-constructor idiom.

    Implement `pub fn load_config(project_root: &Path) -> DevflowConfig`: read `project_root.join("devflow.toml")` if it exists; on a successful `toml::from_str::<DevflowConfig>` return it; on a missing file OR parse error, warn (fail-soft, `tracing::warn!`) and return `DevflowConfig::default()` — a malformed config must never abort the workflow. Do NOT hand-roll TOML parsing (RESEARCH Don't-Hand-Roll: never extend version.rs's narrow value-replacement into a parser). Add per-knob resolver accessors that layer env var > file > default (e.g. `capture_retention(project_root)` reads `DEVFLOW_CAPTURE_RETENTION` env first, else the loaded file, else default) — mirror gates.rs::fire_gate_notify's `std::env::var(...).ok().filter(!empty)` idiom for the env layer so downstream plans call ONE function and get correct precedence.

    Update the module doc comment (config.rs:1-6): remove the "DevFlow has no .devflow.yaml and no config file" claim; document that Phase 16 (D-03) deliberately reopened that decision for a MINIMAL devflow.toml carrying only Phase 16's new knobs, that DEVFLOW_* env vars still win over the file, and that the git-flow branch model remains hardcoded. Reference D-03. Ensure config's public surface is exported (config is already a pub mod in lib.rs — add nothing there unless a re-export is needed).
  </action>
  <verify>
    <automated>cargo test -p devflow-core config:: 2>&1 | tail -15 && cargo clippy -p devflow-core -- -D warnings 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `Cargo.toml` contains `toml = "1"` under [workspace.dependencies]; `crates/devflow-core/Cargo.toml` contains `toml.workspace = true`
    - `crates/devflow-core/src/config.rs` contains `pub struct DevflowConfig`, `impl Default for DevflowConfig`, and `pub fn load_config`
    - The config.rs module doc no longer contains the phrase claiming no config file exists (`rg -c 'no config file' crates/devflow-core/src/config.rs` returns no match)
    - `cargo build -p devflow-core` succeeds with the new dependency
    - `cargo clippy -p devflow-core -- -D warnings` exits 0
  </acceptance_criteria>
  <done>toml is a workspace dependency, DevflowConfig with all Phase 16 knobs plus a fail-soft precedence loader exists in config.rs, GitFlowConfig is untouched, and the doc comment reflects the D-03 reopening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the config precedence contract (env > file > default)</name>
  <files>crates/devflow-core/src/config.rs</files>
  <read_first>
    - crates/devflow-core/src/config.rs (the DevflowConfig + load_config added in Task 1; existing test default_uses_hardcoded_constants 44-50 as the assertion idiom)
    - crates/devflow-core/src/gates.rs (env-var read idiom being layered)
  </read_first>
  <behavior>
    - Absent devflow.toml → load_config returns DevflowConfig::default() (defaults for retention, None angles, verify enabled).
    - Present devflow.toml with a capture_retention value → that value is used (file over default).
    - DEVFLOW_CAPTURE_RETENTION env set → env value wins over both file and default.
    - Malformed devflow.toml → fail-soft to Default, no panic.
  </behavior>
  <action>
    Add `#[test]` functions in config.rs's test module (use `tempfile::tempdir()` per the existing dev-dependency and the git-flow test idiom). Cover: (a) no-file → default; (b) a written `devflow.toml` overrides the default for at least the capture_retention knob; (c) the corresponding DEVFLOW_* env var overrides the file — set the env var within the test, assert the resolver returns the env value, and clean it up (env is process-global: use a distinct value and remove the var after asserting, matching STATE.md's note about process-global env races in gate tests); (d) a syntactically invalid TOML body falls back to Default without panicking. Keep each test single-purpose and table-free where clarity wins.
  </action>
  <verify>
    <automated>cargo test -p devflow-core config:: 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Tests exist asserting no-file→default, file-over-default, env-over-file, and malformed→default-fallback
    - `cargo test -p devflow-core config::` exits 0
    - No test leaves a DEVFLOW_* env var set for sibling tests (var removed after assertion)
  </acceptance_criteria>
  <done>The env>file>default precedence and fail-soft parsing are proven by tests, giving 16a/16b/16d a trusted config surface to build on.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| devflow.toml (operator-authored) → DevflowConfig | Project-local config file; operator-trusted, same trust level as CLI flags |
| crates.io supply chain → build | The new `toml` dependency enters the build graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-SC | Tampering | cargo install of `toml` | high | mitigate | Blocking-human legitimacy checkpoint (verify on crates.io: repo, age, downloads, exact pin, no typosquat) before the Cargo.toml edit; pin `toml = "1"` |
| T-16-06 | DoS | Malformed devflow.toml aborting the workflow | medium | mitigate | load_config is fail-soft: parse error → warn + Default, never panic (Task 1, tested Task 2) |
| T-16-07 | Tampering | Config values silently overriding safety-critical hardcoded git-flow constants | low | accept | DevflowConfig is a separate struct; GitFlowConfig (main/develop/feature_prefix) stays hardcoded and out of the file's reach (D-03) |
| T-16-08 | Information disclosure | Reading an unexpected file path as config | low | mitigate | Only `<project_root>/devflow.toml` is read; no path comes from untrusted runtime input |
</threat_model>

<verification>
- `cargo build -p devflow-core` succeeds with `toml`.
- `cargo test -p devflow-core config::` green (precedence + fail-soft).
- `cargo clippy --workspace -- -D warnings` and `cargo fmt --check` clean.
- `rg 'no config file' crates/devflow-core/src/config.rs` returns nothing.
</verification>

<success_criteria>
- Minimal devflow.toml is parsed into a typed DevflowConfig with all Phase 16 knobs.
- Precedence env > file > default holds and is tested.
- The false "no config file" doc comment is corrected (D-03).
- GitFlowConfig unchanged.
- Downstream plans (16-03, 16-04) can read config without touching config.rs.
</success_criteria>

<output>
Create `.planning/phases/16-pipeline-reliability-hardening/16-02-SUMMARY.md` when done. Record: the final DevflowConfig field set and their env-var names, the pinned toml version, and the resolver accessor signatures 16a/16b/16d will call.
</output>
