---
phase: 43-opencode-driver-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/tests/fixtures/opencode/opencode_success.jsonl
  - crates/devflow-core/tests/fixtures/opencode/opencode_tool_use.jsonl
  - crates/devflow-core/tests/fixtures/opencode/opencode_error.jsonl
  - crates/devflow-core/tests/fixtures/opencode/opencode_success_with_marker.jsonl
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-core/src/agents/opencode.rs
  - crates/devflow-core/src/agents/mod.rs
autonomous: true
requirements: [OPCD-01, OPCD-02]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "`OpenCodeDriver::build_command` returns `(\"opencode\", vec![\"run\", <prompt>, \"--auto\", \"--format\", \"json\"])` as five separate argv elements (OPCD-01, D-01)"
    - "`OpenCodeDriver::render_prompt` still delegates byte-identically to `crate::prompt::render_claude_style(intent)` (D-02)"
    - "`is_opencode_event_stream` returns true only when an event's top-level `type` is `step_start` or `step_finish`, and returns false for a Codex, Claude, or Antigravity capture (D-03)"
    - "`parse_opencode_event_result` extracts a `DEVFLOW_RESULT` marker from the LAST `type:\"text\"` event's `part.text` field, never from a raw top-level stdout line (OPCD-02, D-04)"
    - "A `type:\"error\"` event anywhere in the stream resolves to `AgentStatus::Failed` with `reason` from `error.data.message`, falling back to `error.name` (D-05)"
    - "An earlier success marker never overrides a later `error` event, and never survives a torn trailing line (D-05, D-06, 999.107 #1)"
    - "A torn trailing JSON line after the last parsed event returns `indeterminate_capture_failure()` (D-06)"
    - "A marker-less OpenCode stream returns `None` and defers to Layer 2 — it never resolves to Success (OPCD-02)"
    - "`OpenCodeDriver::parse_completion` is a one-line delegation to `crate::agent_result::parse_opencode_event_result` — no reimplementation (RESEARCH Pattern 2)"
    - "`evaluate_layer1`'s `.or_else` chain calls `parse_opencode_event_result` after `parse_codex_event_result` and before `detect_codex_rate_limit`"
    - "All three REAL captures from `43-evidence/` are vendored into a git-tracked fixture directory and parsed verbatim by regression tests (OPCD-02 success criterion: 'regression-tested against a real capture, not an assumed schema')"
    - "A marker derived from model-generated text always carries `decided_by_layer: Some(1)` via `normalise_stream_marker_provenance`, even if the model's own JSON claims layer 0 (T-43-02)"
  artifacts:
    - "`crates/devflow-core/tests/fixtures/opencode/opencode_success.jsonl` — verbatim copy of the real success capture"
    - "`crates/devflow-core/tests/fixtures/opencode/opencode_tool_use.jsonl` — verbatim copy of the real tool-invoking capture"
    - "`crates/devflow-core/tests/fixtures/opencode/opencode_error.jsonl` — verbatim copy of the real negative-control error capture"
    - "`crates/devflow-core/tests/fixtures/opencode/opencode_success_with_marker.jsonl` — DERIVED from the real success capture with a DEVFLOW_RESULT marker injected into the `text` event's `part.text`; the real captures contain no marker"
    - "`crates/devflow-core/src/agent_result.rs` — `is_opencode_event_stream`, `parse_opencode_event_result`, `evaluate_layer1` chain entry, and their regression tests"
    - "`crates/devflow-core/src/agents/opencode.rs` — real `build_command` argv and `parse_completion` delegation"
    - "`crates/devflow-core/src/agents/mod.rs` — the two argv-asserting tests updated to the new five-element argv"
  key_links:
    - "`evaluate_layer1` → `parse_opencode_event_result`: without this `.or_else` line the parser is dead code and every OpenCode run falls through to Layer 2's exit-code heuristic"
    - "`OpenCodeDriver::parse_completion` → `agent_result::parse_opencode_event_result`: the driver-trait entry point for the same function"
    - "`parse_opencode_event_result` → `normalise_stream_marker_provenance`: the only thing stopping a model-authored marker from forging Layer-0 provenance"
    - "`parse_opencode_event_result` → `ParsedCapture::torn_json_after_last_matching`: the shared torn-tail primitive; a bespoke JSONL scanner would silently drop the R1 hardening"
    - "`build_command` argv → `agents/mod.rs`'s two argv assertions: these live in a different file than the change and break the moment D-01 lands (RESEARCH Pitfall 3)"
  prohibitions:
    - "P-01: The `--auto` auto-approve posture must not widen beyond a phase launch. `--auto` (opencode's own label for 'auto-approve permissions not explicitly denied (dangerous!)') must appear only in `OpenCodeDriver::build_command`'s launch argv, never in a health or capability probe, and the driver's doc comment must name the flag's danger rather than describing it as a neutral headless switch."
    - "P-02: An OpenCode verdict must never be presented as anything other than an agent self-report. A `DEVFLOW_RESULT` marker dug out of model-generated `part.text` is text the model fully controls; it must never survive with a provenance claiming any layer other than 1, even when the model's own marker JSON sets `decided_by_layer` to something else."
    - "P-03: The parser must never invent a completion the agent did not claim. A stream with no marker, an unreadable tail, or an `error` event must never resolve to Success — the absence of a failure signal is not evidence of success."
    - "P-04: Vendored evidence captures must never carry credential material or operator-identifying data into git — no API keys or fragments, no absolute home paths, no OS usernames. This repository has paid for this leak class three times (999.10 and the WR-02 follow-ups); a capture taken from a live authenticated session on the operator's own machine is exactly where it recurs."
---

<objective>
Complete the OpenCode driver's headless launch and completion-parsing path end to end: `build_command` emits the real `--auto --format json` argv (OPCD-01), and a new `parse_opencode_event_result` in `agent_result.rs` resolves an OpenCode JSONL capture into an `AgentResult` through `evaluate_layer1` (OPCD-02) — grounded in the three REAL `opencode --format json` captures rather than an assumed schema.

Purpose: `devflow start --agent opencode` currently launches a Phase-37 stub with no JSON output and no completion signal, so every OpenCode run falls through to Layer 2's exit-code heuristic. This plan makes the launch and the verdict real.
Output: Four git-tracked fixture files, `is_opencode_event_stream` + `parse_opencode_event_result` wired into `evaluate_layer1`, a real `build_command`, and a regression suite that loads the real captures verbatim.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
**Reading the `.planning/` references from a phase worktree.** `.planning/` is listed in `.gitignore`
(line 46) and is tracked ONLY on the personal `workspace/denniyahh` branch — it does not exist on
`develop`, and therefore does not exist in a worktree branched from `develop`
(ROADMAP 999.110). Resolve the main checkout first and read every `.planning/` path through it:

`MAIN="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")"` — verified to resolve
`/var/home/denniyahh/Github/devflow` from both the main checkout and `.worktrees/phase-43`.

@$MAIN/.planning/phases/43-opencode-driver-completion/43-CONTEXT.md
@$MAIN/.planning/phases/43-opencode-driver-completion/43-RESEARCH.md
@$MAIN/.planning/phases/43-opencode-driver-completion/43-PATTERNS.md
@$MAIN/.planning/phases/43-opencode-driver-completion/43-evidence/
@crates/devflow-core/src/agents/opencode.rs
@crates/devflow-core/src/agents/codex.rs
@crates/devflow-core/src/agents/mod.rs
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01)

**New functions in `crates/devflow-core/src/agent_result.rs`:**
- `pub(crate) fn is_opencode_event_stream(events: &[serde_json::Value]) -> bool`
- `pub(crate) fn parse_opencode_event_result(stdout: &str) -> Option<AgentResult>`

**Modified functions:**
- `evaluate_layer1` — one new `.or_else(|| parse_opencode_event_result(&stdout))` link
- `OpenCodeDriver::build_command` — argv changes from 2 elements to 5
- `OpenCodeDriver::parse_completion` — new trait-method override (was the trait default `None`)

**New file paths (git-tracked, under `crates/`, NOT under `.planning/`):**
- `crates/devflow-core/tests/fixtures/opencode/opencode_success.jsonl`
- `crates/devflow-core/tests/fixtures/opencode/opencode_tool_use.jsonl`
- `crates/devflow-core/tests/fixtures/opencode/opencode_error.jsonl`
- `crates/devflow-core/tests/fixtures/opencode/opencode_success_with_marker.jsonl` (derived, not a live capture)

**CLI-visible behavior changes:**
- `devflow start --agent opencode` spawns `opencode run "<prompt>" --auto --format json` instead of `opencode run "<prompt>"`.
- An OpenCode run that emits a `DEVFLOW_RESULT` marker now resolves at Layer 1 instead of falling through to Layer 2's exit-code heuristic.
- An OpenCode run that emits an `error` event now reports the provider's own failure message instead of a bare non-zero exit.

**Struct fields:** none added. `AgentResult`, `AgentStatus`, and `DriverCapabilities` are unchanged by this plan.
</artifacts_this_phase_produces>

<flagged_assumptions>
## Flagged assumptions — surfaced, NOT resolved

The deterministic edge probe classified all three phase requirements as `unclassified — review
manually` (`resolved: 0`, `unresolved: 3`). Per the spec-less probe fallback protocol these rows are
surfaced here as flagged assumptions and are deliberately NOT auto-resolved into `must_haves.truths`.
Two of the three belong to this plan; OPCD-03's row is carried in 43-02.

| # | Requirement | Unresolved edge question | Why it stays open |
|---|-------------|--------------------------|-------------------|
| E-01 | OPCD-01 | The probe produced no edge classification for the launch-argv requirement. No boundary/encoding/ordering edge was derived for argv construction. | Reviewed manually: the only real edge is argv token ordering, which RESEARCH Open Question 1 resolves (five separate elements, never `--format=json`). Recorded, not minted as a truth by the probe. |
| E-02 | OPCD-02 | The probe produced no edge classification for the event-parsing requirement — no boundary, adjacency, encoding, or ordering edges were derived. | Reviewed manually: the ordering edges (error-after-marker, torn-tail-after-marker, last-text-wins) are authored as explicit truths from D-04/D-05/D-06, not from the probe. The probe surfaced nothing; that absence is recorded rather than treated as coverage. |

**No-silent-drop equality (edges):** 3 probe-surfaced rows == 0 authored into `must_haves.truths` +
3 surfaced as flagged assumptions (E-01, E-02 here; E-03 in 43-02).

### Carried research assumptions relevant to this plan

- **A3 (RESEARCH Assumptions Log)** — All three captures show `error` as the sole and final event. The
  parser deliberately scans for ANY `error` event rather than assuming trailing placement. If a future
  OpenCode version emits a recoverable mid-stream `error`, treating it as an unconditional hard failure
  would be too aggressive. Recorded as the parser's current, capture-grounded understanding.
- **NEW — none of the three real captures contains a `DEVFLOW_RESULT` marker.** Verified this session:
  `opencode_success.jsonl`'s text is `"OK"` and `opencode_tool_use.jsonl`'s is `"DONE"`. The real
  captures therefore prove the schema, the detector gate, and the marker-less fallthrough — but they
  cannot prove marker extraction. That path needs the DERIVED fixture
  (`opencode_success_with_marker.jsonl`), which must be named and commented as derived, never presented
  as a live capture.

### Deliberate: the `one-way` rating on Task 1 carries NO `checkpoint:decision`

`verify.plan-structure` warns that Task 1's `<reversibility rating="one-way">` has no preceding
`checkpoint:decision`. That omission is deliberate, not an oversight, for two independent reasons:

1. **The door is already walked through.** `planner-reversibility.md`'s own anti-patterns list names
   this case: *"Gating a decision already made. If the phase CONTEXT.md records the human choosing
   this exact option, the door is already walked through. Keep the rating for the record; do not
   insert a checkpoint to re-ask."* 43-CONTEXT.md D-03 records exactly that — the operator captured
   three live `opencode --format json` runs specifically to settle this schema, precisely so it would
   not have to be decided blind. A checkpoint here would re-ask a question already answered with
   evidence.
2. **A blocking checkpoint would wedge an unattended run.** A `checkpoint:decision` with
   `gate="blocking-human"` has no way to receive an operator answer under DevFlow's one-shot
   `claude -p` launch model (filed as 999.57 / DEN-82). Placing one at the phase's first task would
   stall a `devflow start` run before any work happened.

The rating itself is kept on the task so the signal survives for a future reader, which is what the
reference asks for when the checkpoint is skipped.

### Canon-referral breadcrumbs (prohibitions considered and dropped, not minted)

- Malformed / adversarially-shaped JSON must not panic the parser — canon (OWASP V5 input validation);
  covered by `/gsd-secure-phase` and by `T-43-04` in this plan's threat register; not minted as a
  bespoke prohibition.
- Unbounded agent-controlled text flowing into a persisted `reason` or log line — canon (log and
  terminal-escape injection); covered by `/gsd-secure-phase`; not minted here.

**No-silent-drop equality (prohibitions):** 30 raw Stage-1 candidates across OPCD-01/02/03 → Stage-2
precision keeps 5 (P-01..P-04 in this plan, P-05 in 43-02) + 2 canon breadcrumbs recorded above + 23
dropped as routine engineering hygiene (owned by the edge probe or by code review).
</flagged_assumptions>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — one OpenCode run, launch argv through to a parsed Layer-1 verdict</name>
  <reversibility rating="one-way">The event-schema contract (`step_start`/`text`/`tool_use`/`step_finish`/`error`, marker inside `part.text`) becomes the published parser contract every later OpenCode capture is read against; changing it after downstream phases rely on it is a re-derivation against fresh live captures, not a refactor. No checkpoint is inserted: 43-CONTEXT.md D-03 records the operator choosing this exact schema against three live captures, so the door is already walked through (planner-reversibility.md, "Gating a decision already made").</reversibility>
  <precondition>The real capture files are readable at `$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")/.planning/phases/43-opencode-driver-completion/43-evidence/` — `.planning/` is gitignored and absent from a `develop`-based worktree, so a plain relative path will not find them. Halt and report if that directory does not list three `.jsonl` files.</precondition>
  <files>crates/devflow-core/tests/fixtures/opencode/ (4 new .jsonl files), crates/devflow-core/src/agent_result.rs, crates/devflow-core/src/agents/opencode.rs, crates/devflow-core/src/agents/mod.rs</files>
  <read_first>
    crates/devflow-core/src/agents/opencode.rs (the 28-line stub being replaced)
    crates/devflow-core/src/agents/codex.rs (the delegation and argv model)
    crates/devflow-core/src/agent_result.rs lines 728-849 (is_codex_event_stream / parse_codex_event_result — the direct structural model)
    crates/devflow-core/src/agent_result.rs lines 905-1002 (ParsedCapture, torn_json_after_last_matching)
    crates/devflow-core/src/agent_result.rs lines 1822-1901 (indeterminate_capture_failure, normalise_stream_marker_provenance, parse_marker_lines)
    crates/devflow-core/src/agent_result.rs lines 2115-2134 (evaluate_layer1's .or_else chain)
    crates/devflow-core/src/agents/mod.rs lines 219-242 and 575-582 (the two argv-asserting tests this change breaks)
    $MAIN/.planning/phases/43-opencode-driver-completion/43-CONTEXT.md (D-01 through D-06, D-12)
    $MAIN/.planning/phases/43-opencode-driver-completion/43-evidence/opencode_success.jsonl
    $MAIN/.planning/phases/43-opencode-driver-completion/43-evidence/opencode_tool_use.jsonl
    $MAIN/.planning/phases/43-opencode-driver-completion/43-evidence/opencode_error.jsonl
  </read_first>
  <behavior>
    RED first — write the end-to-end test before any production change, and watch it fail:
    - `opencode_marker_in_text_event_resolves_at_layer1`: feeding the contents of
      `opencode_success_with_marker.jsonl` to `parse_opencode_event_result` yields
      `Some(AgentResult { status: Success, decided_by_layer: Some(1), .. })`.
    THEN green, then these expansion behaviors in the same task:
    - `opencode_real_success_capture_is_recognised_and_marker_less`: the verbatim real
      `opencode_success.jsonl` satisfies `is_opencode_event_stream` and parses to `None`
      (no marker present in the real capture → defer to Layer 2, never Success).
    - `opencode_detector_rejects_foreign_streams`: `is_opencode_event_stream` returns false for a
      Codex `thread.started`/`turn.completed` capture and for a Claude `system`/`init` capture.
    - `opencode_build_command_is_headless_json`: `build_command` returns exactly five argv elements.
    - `opencode_render_prompt_unchanged`: `render_prompt` still equals `render_claude_style(intent)`.
  </behavior>
  <acceptance_criteria>
    - `crates/devflow-core/tests/fixtures/opencode/` contains exactly four `.jsonl` files and all four are git-tracked: `git ls-files crates/devflow-core/tests/fixtures/opencode/ | wc -l` prints `4`
    - The three vendored captures are byte-identical to their `43-evidence/` sources: `diff` against each source file exits 0
    - Leak scan over the vendored fixtures returns no matches: `rg -c 'denniyahh|/home/|/var/home|/Users/|sk-[A-Za-z0-9]|AIza|Bearer ' crates/devflow-core/tests/fixtures/opencode/` exits non-zero (no match) — P-04
    - `crates/devflow-core/tests/fixtures/opencode/opencode_success_with_marker.jsonl` carries the literal `DEVFLOW_RESULT:` inside a `text` event's `part.text` value. Its derived provenance is recorded in the FILENAME and in a doc comment on the test that loads it — never as a comment line inside the `.jsonl`, which would both pollute the leak scan and add a line `ParsedCapture::parse` has to classify
    - The packaged crate carries the fixtures: `cargo package --list -p devflow-core --allow-dirty 2>/dev/null | rg -c 'tests/fixtures/opencode/'` prints `4`
    - `crates/devflow-core/src/agents/opencode.rs` `build_command` returns `("opencode", vec!["run".into(), prompt.to_string(), "--auto".into(), "--format".into(), "json".into()])` — five separate elements, no `key=value` joining
    - `rg -v '^\s*//' crates/devflow-core/src/agents/opencode.rs | rg -c -- '--format=json'` returns no match
    - `crates/devflow-core/src/agents/opencode.rs` `parse_completion` body is a single call to `crate::agent_result::parse_opencode_event_result(output)`
    - `rg -n 'parse_opencode_event_result' crates/devflow-core/src/agent_result.rs` shows both the definition and one call inside `evaluate_layer1`
    - The `evaluate_layer1` chain order is `parse_codex_event_result` → `parse_opencode_event_result` → `detect_codex_rate_limit`
    - `cargo test -p devflow-core --lib opencode_ -- --nocapture` reports a non-zero `passed` count and `0 failed`
    - `cargo test -p devflow-core --lib agents::` reports `0 failed` (proves the two updated argv assertions in `mod.rs` are green)
  </acceptance_criteria>
  <action>
    Resolve the main checkout with `MAIN="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")"` and copy the three real captures verbatim from `$MAIN/.planning/phases/43-opencode-driver-completion/43-evidence/` into a new `crates/devflow-core/tests/fixtures/opencode/` directory, keeping their filenames. Run the P-04 leak scan over the copies before staging them. Then hand-author a fourth file, `opencode_success_with_marker.jsonl`, by taking the real success capture and replacing only the `text` event's `part.text` value with a two-line string whose second line is the marker — the marker prefix is `DEVFLOW_RESULT:` followed by a space and JSON deserializing into `AgentResult` (`parse_marker_lines` scans whole lines, so the newline must be an escaped `\n` inside the JSON string value, not a raw newline, or the JSONL line breaks). Name and comment this file as derived; it is not a live capture.

    Write the RED test first (see `<behavior>`), load fixtures with `include_str!` from `agent_result.rs` using the relative path `../tests/fixtures/opencode/<name>.jsonl`, and confirm it fails before writing production code.

    In `crates/devflow-core/src/agent_result.rs`, immediately after `parse_codex_event_result`, add `is_opencode_event_stream` (an `.any(...)` over events whose top-level `type` is `step_start` or `step_finish` — do NOT gate on `error` alone, it is generic enough to collide with another adapter's error envelope) and `parse_opencode_event_result` following `parse_codex_event_result`'s three-part shape: build a `ParsedCapture`, return `None` if the detector rejects, then torn-tail check, then terminal resolution, then marker scan, then `None`. In this tracer task implement only the happy path fully — detector, `ParsedCapture`, and the marker scan over `type:"text"` events in reverse via `parse_marker_lines(part.text)`, passing the result through `normalise_stream_marker_provenance` — and leave the `error` and torn-tail branches to Task 2. Do NOT port Codex's `turn.completed`/`turn.failed` terminal lookup: D-03 establishes OpenCode has no terminal-status event. Do NOT write a bespoke JSONL scanner, marker matcher, or provenance check — `ParsedCapture::parse`, `parse_marker_lines`, and `normalise_stream_marker_provenance` are the shared primitives every other adapter uses.

    Add the `.or_else(|| parse_opencode_event_result(&stdout))` link to `evaluate_layer1`, positioned after the `parse_codex_event_result` link and before the trailing `detect_codex_rate_limit` link (which stays last as the least-authoritative heuristic).

    In `crates/devflow-core/src/agents/opencode.rs`, replace the stub `build_command` per D-01 and add the `parse_completion` delegation. Update the module doc comment so it names `--auto` as opencode's own auto-approve-permissions flag and states the danger explicitly rather than describing a neutral headless switch (P-01); keep `render_prompt` exactly as-is (D-02).

    In `crates/devflow-core/src/agents/mod.rs`, update the two argv assertions RESEARCH Pitfall 3 names — `drivers_reproduce_legacy_adapter_behavior` (around line 235) and `opencode_wraps_prompt_in_run` (around line 575) — to expect the five-element argv. Leave their `render_prompt` assertions untouched. Do not touch `default_preflight_is_ok_for_built_in_adapters` in this plan; 43-02 owns it.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib opencode_ -- --nocapture 2>&1 | rg 'test result' && cargo test -p devflow-core --lib agents:: 2>&1 | rg 'test result' && cargo test -p devflow-core --lib agent_result:: 2>&1 | rg 'test result'</automated>
  </verify>
  <done>A single OpenCode run's happy path works end to end and is committed: the driver builds the real headless argv, the JSONL capture is recognised by the detector, a marker inside a `text` event resolves to a Layer-1 `AgentResult`, `evaluate_layer1` reaches the new parser, the real captures are vendored and regression-tested, and the two previously-passing argv tests in `mod.rs` are green again.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Failure and indeterminacy precedence — `error` events and torn tails</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    crates/devflow-core/src/agent_result.rs (the `parse_opencode_event_result` written in Task 1)
    crates/devflow-core/src/agent_result.rs lines 756-849 (how `parse_codex_event_result` orders its torn-tail check against its terminal lookup)
    crates/devflow-core/src/agent_result.rs lines 977-1002 (`torn_json_after_last_matching`'s contract)
    crates/devflow-core/src/agent_result.rs lines 1822-1835 (`indeterminate_capture_failure`)
    crates/devflow-core/tests/fixtures/opencode/opencode_error.jsonl
    $MAIN/.planning/phases/43-opencode-driver-completion/43-CONTEXT.md (D-05, D-06)
  </read_first>
  <behavior>
    Write each test first and watch it fail before implementing the branch:
    - `opencode_real_error_capture_is_failed`: the verbatim real `opencode_error.jsonl` parses to
      `Some(status: Failed)` with `reason == Some("Unexpected server error. Check server logs for details.")`
      and `decided_by_layer == Some(1)`.
    - `opencode_error_reason_falls_back_to_name`: a synthetic `error` event carrying `error.name` but no
      `error.data.message` yields a `reason` of `"UnknownError"`.
    - `opencode_error_reason_survives_non_string_message`: a synthetic `error` event whose
      `error.data.message` is a number, not a string, yields the generic fallback reason and does not panic.
    - `opencode_error_event_overrides_earlier_success_marker` (negative control, 999.107 #1): a synthetic
      capture of `step_start`, a `text` event carrying a success marker, then an `error` event, resolves to
      `Failed` — the marker must lose.
    - `opencode_torn_tail_after_marker_is_indeterminate`: a synthetic capture of `step_start`, a
      marker-bearing `text` event, then a truncated JSON line, returns
      `indeterminate_capture_failure()`'s reason and `Failed` — not the marker's Success.
    - `opencode_torn_tail_beats_error_event_ordering_is_stable`: a capture with both a torn tail and an
      `error` event resolves deterministically (torn-tail check runs first, matching Codex's order).
  </behavior>
  <acceptance_criteria>
    - `parse_opencode_event_result` calls `capture.torn_json_after_last_matching(|_| true)` BEFORE any `error`-event scan, matching `parse_codex_event_result`'s ordering
    - The `error` branch reads `error.data.message` first, then `error.name`, then a fixed generic string; every access is `Option`-returning (`.get(...).and_then(...)`) with no `unwrap`, no indexing, and no `expect`
    - Source assertion (reviewable, not greppable — a file-wide count cannot be scoped to one function): the body of `parse_opencode_event_result` contains no reference to `turn.completed`, `turn.failed`, or any terminal-status event lookup. OpenCode has no terminal-status event (D-03); the only decisive terminal signal is an `error` event.
    - Six named tests from `<behavior>` exist and pass: `cargo test -p devflow-core --lib opencode_error -- --nocapture` and `cargo test -p devflow-core --lib opencode_torn -- --nocapture` each report a non-zero `passed` count and `0 failed`
    - `cargo test -p devflow-core --lib agent_result::` reports `0 failed`
    - `cargo clippy -p devflow-core --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <action>
    Extend `parse_opencode_event_result` with the two branches Task 1 deferred, in this exact order after the detector gate.

    First, the torn-tail branch (D-06): if `capture.torn_json_after_last_matching(|_| true)` is true, return `Some(indeterminate_capture_failure())`. The predicate is `|_| true` — every emitted event matters equally, because OpenCode has no `is_top_level`-style filtered predicate. This must run before the error scan and before the marker scan, for the same reason Codex runs it first: the torn tail is precisely where a failure event would have lived, so nothing that survived before the tear may stand in for it.

    Second, the error branch (D-05): find any event whose top-level `type` is `error`. Scan for presence anywhere rather than assuming trailing placement — the real capture is a single-line stream, so "last event" placement is untested, and RESEARCH assumption A3 records this as deliberate. Build `AgentResult` with `status: AgentStatus::Failed`, `exit_code: None`, `commits: None`, `summary: None`, `verdict: None`, `decided_by_layer: Some(1)`, and `reason` resolved from `error.data.message` when it is a string, else `error.name` when it is a string, else a fixed generic string. Do not merge marker-derived fields onto the failed result the way Codex's `turn.failed` arm does — OpenCode's error event carries no marker-shaped fields.

    The error branch must return before the marker scan is consulted, so an earlier success marker can never override a later failure (999.107 #1). Do not copy marker fields onto the error result and do not fall through.

    Keep every field access `Option`-returning so an adversarially-shaped event falls through to a safe default rather than panicking (T-43-04). Do not echo the raw event JSON into the reason string.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib opencode_ -- --nocapture 2>&1 | rg 'test result' && cargo test -p devflow-core --lib agent_result:: 2>&1 | rg 'test result' && cargo clippy -p devflow-core --all-targets -- -D warnings</automated>
  </verify>
  <done>An `error` event anywhere in an OpenCode stream resolves to `Failed` carrying the provider's own message, a torn trailing line resolves to indeterminate, and neither can be overridden by an earlier success marker — each proven by a named regression test, with the real error capture parsed verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Multi-step robustness — tool-use captures, marker-less fallthrough, malformed shapes</name>
  <files>crates/devflow-core/src/agent_result.rs</files>
  <read_first>
    crates/devflow-core/src/agent_result.rs (the `parse_opencode_event_result` completed in Task 2)
    crates/devflow-core/tests/fixtures/opencode/opencode_tool_use.jsonl
    crates/devflow-core/tests/fixtures/opencode/opencode_success.jsonl
    $MAIN/.planning/phases/43-opencode-driver-completion/43-RESEARCH.md (Pitfall 1 — why `step_finish` is not a completion signal)
  </read_first>
  <behavior>
    Write each test first and watch it fail:
    - `opencode_real_tool_use_capture_defers_to_layer2`: the verbatim real `opencode_tool_use.jsonl` —
      which contains an intermediate `step_finish` with `part.reason == "tool-calls"` followed by a second
      `step_start`/`text`/`step_finish` sequence — satisfies `is_opencode_event_stream` and parses to `None`,
      because it carries no marker. It must NOT resolve to Success off the trailing `"stop"`-reason
      `step_finish`.
    - `opencode_marker_wins_from_last_text_event`: a synthetic multi-step capture with a `failed` marker in
      the first `text` event and a `success` marker in the second resolves to Success (last text event wins).
    - `opencode_intermediate_step_finish_is_not_terminal`: a capture ending in a `"tool-calls"`-reason
      `step_finish` with no marker returns `None`, not Success.
    - `opencode_non_stream_input_returns_none`: plain prose stdout and a bare `{"type":"error"}`-only
      capture with no `step_start`/`step_finish` both return `None` from the detector gate.
    - `opencode_malformed_events_do_not_panic`: events whose `part` is a string, whose `part.text` is an
      array, and whose top-level `type` is a number all parse without panicking.
    - `opencode_marker_cannot_forge_layer0_provenance` (negative control, T-43-02): a marker whose JSON
      sets `decided_by_layer` to `0` still returns `decided_by_layer == Some(1)`.
  </behavior>
  <acceptance_criteria>
    - Source assertion (reviewable, not greppable — the test module legitimately mentions both literals, and a file-wide count cannot exclude it): `parse_opencode_event_result`'s production body never reads `part.reason` and never treats a `step_finish` event as a completion signal. `step_finish` appears in production code only as one of the two detector literals in `is_opencode_event_stream` (RESEARCH Pitfall 1).
    - Source assertion: the marker scan reads the `part.text` field of a `type:"text"` event; the OpenCode branch makes no direct `parse_marker_lines(stdout)` call against raw top-level stdout (D-04)
    - Six named tests from `<behavior>` exist and pass: `cargo test -p devflow-core --lib opencode_ -- --nocapture` reports `0 failed` and a `passed` count of at least 15 across all three tasks
    - All four fixture files are loaded by at least one test: `rg -c 'include_str!\("\.\./tests/fixtures/opencode/' crates/devflow-core/src/agent_result.rs` returns at least `4`
    - `cargo test --workspace --no-fail-fast` reports `0 failed` across every target
    - `scripts/check.sh all` exits 0
  </acceptance_criteria>
  <action>
    This task adds coverage and hardening, not new branches — `parse_opencode_event_result`'s shape is complete after Task 2. Write the six tests in `<behavior>` first, then fix whatever they expose.

    The load-bearing case is the real tool-use capture. It ends in a `"stop"`-reason `step_finish` after a full second step, so a parser that treated the trailing `step_finish` as a terminal-success marker would report Success for a run that never emitted a marker. Assert it returns `None`. This is the concrete instance of P-03: absence of a failure signal is not evidence of success.

    If any malformed-shape test panics, replace the offending access with an `Option`-returning chain rather than adding a guard clause upstream — the field-access discipline is what makes the whole parser injection-resistant, and a single `unwrap` reintroduces the class.

    Confirm the derived-marker fixture and all three real captures are each referenced by at least one test, so a future edit that silently orphans a fixture is caught.

    Close the task by running the full workspace suite and `scripts/check.sh all` (fmt, clippy `--workspace --all-targets -- -D warnings`, and the full test suite) — the phase gate. Do not run `git push` from a worktree: the container-backed pre-push hook cannot resolve a worktree's `.git` file and will fail for reasons unrelated to this change.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib opencode_ -- --nocapture 2>&1 | rg 'test result' && cargo test --workspace --no-fail-fast 2>&1 | rg 'test result: FAILED' | wc -l | rg '^0$' && scripts/check.sh all</automated>
  </verify>
  <done>The real tool-use capture, the real success capture, the real error capture, and the derived marker fixture are each parsed verbatim by a regression test; a multi-step run with no marker defers to Layer 2 rather than reporting Success; malformed event shapes cannot panic the parser; a model-authored marker cannot forge Layer-0 provenance; and the full workspace suite plus `scripts/check.sh all` are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow monitor → `opencode` CLI subprocess | DevFlow spawns `opencode run "<prompt>" --auto --format json`. `--auto` auto-approves every permission not explicitly denied, so the spawned agent executes tool calls without a human in the loop. |
| `opencode` stdout (JSONL) → `parse_opencode_event_result` | Every byte on this boundary is model-influenced or provider-controlled. The event stream is untrusted input that decides whether a commit-gated stage advances. |
| Model-generated `part.text` → `parse_marker_lines` → `AgentResult` | The DEVFLOW_RESULT marker is authored by the model itself. It is a self-report crossing into DevFlow's verdict system. |
| Operator's live authenticated session → committed test fixtures | The vendored captures were produced by a real authenticated `opencode` run on the operator's machine and are being written into git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-43-01 | Elevation of Privilege | `build_command` `--auto` flag | high | mitigate | `--auto` appears only in the launch argv, never in a health or capability probe; the driver doc comment names it as auto-approve-permissions rather than a neutral headless switch, so the posture is visible at the call site (P-01). Scope is otherwise unchanged from the Pi `--no-approve` / Codex `-a never` precedent already shipped. |
| T-43-02 | Spoofing | `parse_opencode_event_result` marker path | high | mitigate | Every marker-derived `AgentResult` passes through `normalise_stream_marker_provenance`, which force-sets `decided_by_layer = Some(1)`. A model that writes `"decided_by_layer":0` into its own marker cannot forge Layer-0 external-probe provenance (T-30-26 precedent). Proven by `opencode_marker_cannot_forge_layer0_provenance`. |
| T-43-03 | Tampering | Torn / truncated capture | high | mitigate | `torn_json_after_last_matching(\|_\| true)` runs before any verdict resolution; a torn tail returns `indeterminate_capture_failure()` so an earlier surviving success marker can never stand in for an unreadable tail where a failure event would have lived (D-06). |
| T-43-04 | Denial of Service | JSONL field access | medium | mitigate | Every event field is read through an `Option`-returning chain (`.get(...).and_then(...)`) with no `unwrap`, `expect`, or indexing; a malformed or adversarially-shaped event falls through to a safe default instead of panicking the monitor. Proven by `opencode_malformed_events_do_not_panic`. |
| T-43-05 | Information Disclosure | Vendored evidence fixtures | medium | mitigate | The three captures are leak-scanned for home paths, OS usernames, and key material before staging; this session's scan over the source files found no matches, and the fixtures carry only opaque `ses_`/`prt_`/`msg_`/`call_` identifiers. Re-run as an acceptance criterion at commit time (P-04). This repository has three prior instances of this leak class (999.10, WR-02). |
| T-43-06 | Repudiation | Marker-less run | high | mitigate | A stream with no marker returns `None` and defers to Layer 2 rather than resolving Success, so a run the agent never claimed to complete cannot silently advance a commit-gated stage (P-03). Proven against both real marker-less captures. |
| T-43-07 | Tampering | Cross-adapter parser collision | low | mitigate | `is_opencode_event_stream` requires an OpenCode-unique `step_start`/`step_finish` sighting rather than gating on the generic `error` shape, so another adapter's error envelope cannot be routed into this parser by `evaluate_layer1`'s cascade. |
| T-43-08 | Information Disclosure | `reason` string content | low | accept | The `reason` carries the provider's own `error.data.message` verbatim, which is agent/provider-controlled text landing in a persisted result. Accepted: this matches the existing Codex `turn.failed` and Claude envelope-failure handling exactly, the operator needs the real message to act, and changing it here would diverge one adapter from four. Generic log-injection hardening is canon and is referred to `/gsd-secure-phase` rather than solved per-adapter. |
</threat_model>

<verification>
- `cargo test -p devflow-core --lib opencode_` — all new tests green, `0 failed`
- `cargo test -p devflow-core --lib agent_result::` — the existing Claude/Codex/Antigravity parser suites are unregressed
- `cargo test -p devflow-core --lib agents::` — the two updated argv assertions and `every_driver_passes_the_conformance_suite` are green
- `cargo test --workspace --no-fail-fast` — `0 failed` across every target
- `scripts/check.sh all` — fmt, clippy `--workspace --all-targets -- -D warnings`, and the full suite
- `git ls-files crates/devflow-core/tests/fixtures/opencode/ | wc -l` prints `4` — the fixtures are tracked, not gitignored
- `cargo package --list -p devflow-core --allow-dirty | rg -c 'tests/fixtures/opencode/'` prints `4` — `include_str!` from the lib target will still resolve in a published crate
</verification>

<success_criteria>
1. `OpenCodeDriver::build_command` emits `opencode run "<prompt>" --auto --format json` as five separate argv elements (OPCD-01).
2. `parse_opencode_event_result` exists in `agent_result.rs`, is reachable from both `evaluate_layer1` and `OpenCodeDriver::parse_completion`, and resolves marker, error, torn-tail, and no-signal cases per D-04/D-05/D-06 (OPCD-02).
3. All three real `43-evidence/` captures are vendored as git-tracked fixtures and parsed verbatim by regression tests; the marker path is proven against a clearly-labelled derived fixture, because no real capture contains a marker.
4. No marker-less OpenCode run resolves to Success at Layer 1.
5. `cargo test --workspace --no-fail-fast` and `scripts/check.sh all` are green.
</success_criteria>

<output>
Create `.planning/phases/43-opencode-driver-completion/43-01-SUMMARY.md` when done. Resolve the path through the main checkout (`MAIN="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")"`) — `.planning/` does not exist inside a `develop`-based worktree.
</output>
