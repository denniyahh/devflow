---
phase: 28-close-the-checkpoint-answer-return-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md
  - crates/devflow-core/src/verify.rs
autonomous: true
requirements: ["999.57", "D-01", "D-02"]

must_haves:
  truths:
    - "The exact captured-stdout shape a `blocking-human` checkpoint produces under a fully headless `claude -p` run is an observed fact recorded in 28-PROBE.md, not an assumption (RESEARCH A1 / Pitfall 2)."
    - "Given a phase whose plans declare a human-blocking checkpoint task, DevFlow can answer `true` to `does this phase declare one?` without launching anything (D-01, static half)."
    - "Given a phase whose plans declare none, the same question answers `false`, so today's ordinary-error path is reached unchanged (D-01)."
    - "PLAN.md discovery for a phase exists in exactly one place in the codebase; the D-01 scan and `external_verify_commands` share it (RESEARCH Don't-Hand-Roll row 1)."
  artifacts:
    - ".planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md — the A1 observation record"
    - "crates/devflow-core/src/verify.rs — `phase_has_blocking_human_checkpoint` and the extracted `phase_plan_files` helper"
  key_links:
    - "28-PROBE.md's recorded literal → the confirmation reader keyed to it in plan 28-02 Task 2. If the probe observes a different shape, 28-02 keys on what was observed, not on what RESEARCH predicted."
    - "`phase_plan_files` → both `external_verify_commands` (existing behavior must not change) and `phase_has_blocking_human_checkpoint` (new)."
  prohibitions:
    - statement: "No file matching `.planning/phases/28-*/28-*-PLAN.md` may contain the literal human-blocking task-gate attribute. Phase 28's own plans are scanned by the very function this phase builds; a literal occurrence would make DevFlow believe phase 28 declares a checkpoint and route phase 28's own ordinary failures into the auto-decide relaunch path."
      status: enforced
      verification: "rg -l 'gate=\"blocking[-]human\"' .planning/phases/28-*/28-*-PLAN.md ; expect no matching files (rg exits 1)."
    - statement: "The D-01 scan must not inspect agent output, failure text, or any runtime capture — it reads declared PLAN.md content only (D-02: no re-implementation of `what does the agent mean`)."
      status: enforced
      verification: "rg -n 'stdout|phase-.*-stdout|reason' crates/devflow-core/src/verify.rs — the new function's body must reference none of them."
---

<objective>
Prove the checkpoint-recognition seam end-to-end against a live headless run,
then encode the half of D-01 that needs no agent output: the static PLAN.md
scan.

Purpose: RESEARCH assumption **A1** is this phase's one genuine unknown. The
`Gate:` field is confirmed in source at both endpoints (`gsd-executor.md:356`
emits it; `execute-phase.md:1053` keys its own carve-out on it) but has never
been observed surviving the two indirections between them and DevFlow's
captured stdout under a run with no human present. Everything 28a builds
downstream is keyed to that string. Observing it first costs one bounded
sub-session; discovering it wrong later costs the confirmation half of D-01
and reshapes the rest of 28a.

Output: `28-PROBE.md` (the observation record and the literal the confirmation
reader must key on), plus `verify::phase_has_blocking_human_checkpoint` and the
shared `phase_plan_files` discovery helper.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-close-the-checkpoint-answer-return-path/28-CONTEXT.md
@.planning/phases/28-close-the-checkpoint-answer-return-path/28-RESEARCH.md
@.planning/phases/28-close-the-checkpoint-answer-return-path/28-PATTERNS.md
@.claude/skills/ai-change-acceptance/SKILL.md
@crates/devflow-core/src/verify.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Observe a blocking-human checkpoint's real captured stdout (A1 probe)</name>
  <files>.planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md</files>
  <precondition>The `claude` CLI is on PATH (`command -v claude`) and `mktemp -d` yields a writable directory outside this repository. If either is unmet, halt and record the reason — this probe cannot be simulated.</precondition>
  <read_first>
    - .planning/phases/28-close-the-checkpoint-answer-return-path/28-RESEARCH.md § "Architecture Patterns / Pattern 2" and § "Common Pitfalls / Pitfall 2" and § "Open Questions" item 1 — the exact chain under test and both endpoints already confirmed in source.
    - crates/devflow-core/src/monitor.rs (the `script` string, roughly lines 130-150) — DevFlow's capture is literally `"$@" > stdout_file 2> stderr_file`, so a plain shell redirect of `claude -p ...` reproduces the captured file byte-for-byte. Do NOT stand up DevFlow's state machine for this probe.
    - $HOME/.claude/agents/gsd-executor.md § `checkpoint_return_format` (around line 349-380) — the emitting endpoint.
    - $HOME/.claude/gsd-core/workflows/execute-phase.md § `checkpoint_handling` (around line 1040-1080) — the relaying endpoint.
  </read_first>
  <action>
Build the cheapest workload that crosses the seam under test and nothing else,
in a scratch directory OUTSIDE this repository (`PROBE=$(mktemp -d)`), then run
it once under a hard timeout.

Scaffold in `$PROBE`: `git init` plus a user.email/user.name; a minimal
`.planning/PROJECT.md`; a minimal `.planning/ROADMAP.md` carrying one entry
headed `### Phase 91: Checkpoint Probe` with a Goal line and `**Plans:** 1 plan`;
a minimal `.planning/STATE.md` with `current_phase: 91`; and
`.planning/phases/91-checkpoint-probe/91-01-PLAN.md` containing valid plan
frontmatter (`phase`, `plan`, `type: execute`, `wave: 1`, `depends_on: []`,
`files_modified: []`, `autonomous: false`, `requirements: []`, and a `must_haves`
block) and exactly ONE task: a `checkpoint:human-verify` task asking a trivial
question (for example, confirming that the string `probe-ok` appears in a file
the plan also creates).

**Assemble that task's gate attribute from a shell variable** — set
`GATE=blocking-human` in the probe script and emit the attribute as
`gate="${GATE}"` inside the heredoc. The expanded literal must never appear in
any file under `.planning/phases/28-*/`, because `phase_has_blocking_human_checkpoint`
(Task 2) scans exactly that glob for exactly that literal and phase 28 must not
be seen as declaring a checkpoint of its own.

Run, from `$PROBE`:
`timeout 900 claude -p "/gsd-execute-phase 91" --output-format json --dangerously-skip-permissions > probe-stdout.json 2> probe-stderr.log; echo $? > probe-exit`
— matching DevFlow's own launch argv (`agents/claude.rs::exec_command`) and its
redirect shape exactly.

Then write `28-PROBE.md` with these headings, verbatim:
`## Command`, `## Exit code`, `## Observed literal`, `## Full checkpoint region`,
`## A1 verdict`, `## Reader contract`.

`## Observed literal` records the exact line (or lines) in `probe-stdout.json`
that carry the executor's gate field, quoted byte-for-byte after JSON unescaping
— or the word NONE if no gate-bearing line is present. `## A1 verdict` is exactly
one of CONFIRMED (the predicted rendering is present), DIVERGENT (a gate field is
present in a different rendering — quote it), or HUNG (exit code 124: the
orchestrator never terminated, which is itself the load-bearing finding and must
be recorded as such). `## Reader contract` states, in one or two sentences, the
exact substring the confirmation reader built in plan 28-02 Task 2 must match,
derived from what was observed rather than from RESEARCH's prediction.

If the run terminates early because the synthetic project is missing scaffolding
the orchestrator names in its output, add exactly what it names and re-run ONCE.
Do not exceed two timed runs; if the second also fails to reach the checkpoint,
record `## A1 verdict` as DIVERGENT with the observed early-exit output and state
in `## Reader contract` that the reader must key on the RESEARCH-predicted
rendering as an unconfirmed default, flagged for the phase's verification step.

Copy `probe-stdout.json` into the phase directory as `28-probe-stdout.json` only
if it is under 64 KB; otherwise embed the checkpoint region (200 lines around the
match, or the tail if there is no match) under `## Full checkpoint region` and say
so. Delete `$PROBE` when finished.
  </action>
  <verify>
    <automated>test -f .planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md &amp;&amp; rg -c '^## (Command|Exit code|Observed literal|Full checkpoint region|A1 verdict|Reader contract)$' .planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md | rg -q '^6$' &amp;&amp; rg -q '^## A1 verdict$' -A3 .planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md &amp;&amp; rg -q 'CONFIRMED|DIVERGENT|HUNG' .planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md &amp;&amp; echo PROBE_RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `28-PROBE.md` exists and contains all six required headings; the verify command prints `PROBE_RECORDED`.
    - `## Exit code` records an integer actually read from the probe run's `probe-exit` file — not a guess, and not omitted.
    - `## A1 verdict` is exactly one of the three words CONFIRMED, DIVERGENT, or HUNG.
    - `## Reader contract` names a concrete substring (or explicitly names the unconfirmed default and why) that plan 28-02 Task 2 can implement without further interpretation.
    - `rg -l 'gate="blocking[-]human"' .planning/phases/28-*/28-*-PLAN.md` matches no files (rg exits 1) — the probe's gate attribute was assembled from a shell variable and its expansion never landed in this phase's own plan files.
    - No file was created or modified anywhere under `crates/` by this task.
  </acceptance_criteria>
  <done>The real captured-stdout shape of a headless `blocking-human` checkpoint is recorded as observed fact, and the exact substring the confirmation reader must match is written down.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Static PLAN.md scan for a declared human-blocking checkpoint (D-01)</name>
  <files>crates/devflow-core/src/verify.rs</files>
  <read_first>
    - crates/devflow-core/src/verify.rs lines 1-97 — the module doc comment (which already states the agent-writable trust boundary this reuses) and `external_verify_commands`, whose directory walk is the analog to extract, per 28-PATTERNS.md § "verify.rs — D-01 static PLAN.md scan".
    - .planning/phases/28-close-the-checkpoint-answer-return-path/28-RESEARCH.md § "Security Domain", threat row 1 — why the static scan is the PRIMARY gate and why reusing this existing trust boundary introduces no new one.
    - .claude/skills/ai-change-acceptance/SKILL.md — requirement 1 (a regression test that fails before the change) and requirement 2 (an assertion at a public boundary); both new functions here are `pub` in `devflow-core`.
  </read_first>
  <behavior>
    - A phase directory containing one plan file whose body carries the human-blocking gate attribute → `true`.
    - A phase directory whose plans carry only the plain `blocking` gate attribute (no `-human` suffix) → `false`. This is the Phase 26 near-miss distinction and must not collapse.
    - A phase directory with plans but no gate attribute at all → `false`.
    - A phase number with no matching `.planning/phases/{NN}-*` directory → `false`, never a panic and never an error.
    - Two plan files in the phase, only the second carrying the attribute → `true` (every plan is inspected, not just the first).
    - A file in the phase directory that is not a `{NN}-*-PLAN.md` (for example a `-SUMMARY.md` or `-RESEARCH.md`) carrying the attribute → `false` (only plan files are scanned).
    - `external_verify_commands`' existing behavior is byte-for-byte unchanged after the shared helper is extracted — its existing tests stay green with no edits.
  </behavior>
  <action>
Extract the directory-and-file discovery loop currently inlined in
`external_verify_commands` (verify.rs lines 35-60) into a new
`pub fn phase_plan_files(project_root: &Path, phase: u32) -> Vec<PathBuf>` that
returns the sorted plan paths, and rewrite `external_verify_commands` to call it
— its remaining body becomes the read-and-`command_from_frontmatter` tail only.
Do not change the discovery semantics (`{phase:02}-` prefix on both the phase
directory and the plan file, `-PLAN.md` suffix, sorted, unreadable directories
yield an empty result).

Add `pub fn phase_has_blocking_human_checkpoint(project_root: &Path, phase: u32) -> bool`
as its sibling: read each path from `phase_plan_files` and return `true` on the
first whose contents contain the exact literal task-gate attribute for a
human-blocking checkpoint — that is, `gate=` followed by a double-quoted string
whose value is the word blocking, a hyphen, and the word human. Write it as a
plain `str::contains` of that raw literal, not a regex and not an XML parse:
GSD's task attributes are always double-quoted, and this module's own doc comment
commits it to intentionally small parsers. The plain `blocking` value must not
match, which the trailing quote in the searched literal already guarantees.

Document on the new function, in its doc comment: that it reads declared plan
content only and never any runtime capture or agent output (D-02); that it is the
PRIMARY gate for the auto-decide path added in plan 28-03, so an agent cannot
route itself into that path for a phase whose plans never declared a checkpoint;
and that it reuses the same agent-writable trust boundary the module doc comment
already describes rather than introducing a new one.

Write the tests first, from the behavior list above, using `tempfile::tempdir`
plus `std::fs::create_dir_all`/`write` to build `.planning/phases/{NN}-slug/`
fixtures — the same fixture shape `verify.rs`'s existing test module already uses.
In the fixture plan bodies, assemble the gate attribute with `format!` from a
`const` holding the value, or use a string built at runtime, so the test source
itself is not what makes a scan of this repository's own phase 28 plans succeed.
  </action>
  <verify>
    <automated>cargo test -p devflow-core verify::tests:: 2>&amp;1 | rg -q 'test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test -p devflow-core verify::tests::phase_has_blocking_human 2>&amp;1 | rg -q '[1-9] passed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow-core verify::tests::phase_has_blocking_human` prints a non-zero `N passed` count (a bare filter that matches nothing exits 0 — assert on the count, never the exit code).
    - Each behavior bullet above has a named test; the `blocking`-vs-`blocking-human` discrimination test is present and green.
    - `rg -n 'fn phase_plan_files' crates/devflow-core/src/verify.rs` matches exactly one definition, and `rg -c 'read_dir' crates/devflow-core/src/verify.rs` counts no more occurrences than before the change (the walk was moved, not duplicated).
    - `external_verify_commands`' pre-existing tests are unmodified and green: `cargo test -p devflow-core verify::tests::` reports `test result: ok` with zero failures.
    - `cargo clippy -p devflow-core --all-targets -- -D warnings` is clean.
  </acceptance_criteria>
  <done>DevFlow can determine, without launching anything, whether a phase's plans declare a human-blocking checkpoint — and a phase that declares none reaches exactly today's behavior.</done>
  <reversibility rating="reversible">A pure read-only lookup over static plan content; no persisted format, no external contract, no runtime side effect.</reversibility>
</task>

</tasks>

<assumption_delta_decision>
The `assumption-delta scan` detector fired (`pluralization` on "fallback",
`chosen` on "chosen"), both matching ROADMAP prose about the deliberately
deferred Part B answer-file fallback and about why phase 28 was selected over
999.25 — neither is an identity-model transition in this phase's scope.

There is, however, one real transition worth recording: **stage launch** is no
longer exclusively "spawn a fresh agent"; plan 28-03 adds "resume an exited
session". Decision: **add-alongside**. Rationale: D-05 deliberately scopes the
resume primitive to Claude and explicitly declines an `AgentAdapter` trait
change, so promoting "launch" to a general kind-tagged concept now would
generalize across one implementation. What would force a promote: a second
agent (Codex/OpenCode) gaining a resume primitive — at that point the
`(program, args)` construction belongs behind a launch-kind on the adapter
trait rather than an inherent `ClaudeAgent` method.
</assumption_delta_decision>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → `.planning/phases/**/*-PLAN.md` | Plan files are agent-writable during the Code stage; the D-01 scan reads them. This is the SAME boundary `verify::external_verify_commands` already documents and accepts — reused, not newly introduced. |
| agent → `.devflow/phase-NN-stdout` | Agent-controlled output that plan 28-02's confirmation reader will parse. Not read by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-28-01 | Spoofing / Elevation of Privilege | `verify::phase_has_blocking_human_checkpoint` reading agent-writable plan files | medium | accept | The scan is the PRIMARY gate for the later auto-decide path, and it reuses an already-accepted trust boundary (verify.rs module doc comment). Worst case within a phase that legitimately declares a checkpoint: an ordinary failure routes to auto-decide instead of the generic gate — made unconditionally auditable by the `checkpoint_auto_decided` event in plan 28-03. Recorded honestly: this disposition rests on the operator's own D-03 risk acceptance, not on an independently validated control. |
| T-28-07 | Tampering (self-inflicted) | Phase 28's own PLAN.md files | medium | mitigate | A literal human-blocking gate attribute in this phase's own plans would make the new scan report `true` for phase 28 itself, routing phase 28's ordinary failures into the auto-decide relaunch. Mitigated by the `must_haves.prohibitions` grep gate above and by assembling every probe/test occurrence from a variable rather than a literal. |
| T-28-SC | Tampering | package-manager installs | — | n/a | This phase installs no packages (RESEARCH § "Package Legitimacy Audit": not applicable — no `cargo add`, no new dependency). No legitimacy checkpoint is required and none is fabricated. |
</threat_model>

<verification>
- `28-PROBE.md` records an observed exit code and one of the three A1 verdicts.
- `cargo test -p devflow-core verify::tests::` reports `test result: ok` with a non-zero passed count.
- `rg -l 'gate="blocking[-]human"' .planning/phases/28-*/28-*-PLAN.md` matches no files.
- `scripts/check.sh test` is green before this plan's final commit.
</verification>

<success_criteria>
- The A1 assumption is resolved to an observation, and the exact substring the confirmation reader must match is written down in `28-PROBE.md § Reader contract`.
- `verify::phase_has_blocking_human_checkpoint` answers D-01's static half for both the positive and negative cases, discriminating `blocking` from `blocking-human`.
- PLAN.md discovery lives in exactly one function shared by both callers.
- No file under `.planning/phases/28-*/28-*-PLAN.md` contains the human-blocking gate literal.
</success_criteria>

## Artifacts this phase produces

New symbols created across all six plans of phase 28 (listed in every plan so
the source-grounding pass can exclude them from drift verification):

**`devflow-core`**
- `verify::phase_plan_files(project_root: &Path, phase: u32) -> Vec<PathBuf>` (28-01)
- `verify::phase_has_blocking_human_checkpoint(project_root: &Path, phase: u32) -> bool` (28-01)
- `agent_result::claude_session_id(stdout: &str) -> Option<String>` (28-02)
- `agent_result::session_id_from_capture(project_root: &Path, phase: u32) -> Option<String>` (28-02)
- `agent_result::blocking_human_checkpoint_reported(stdout: &str) -> bool` (28-02)
- `agent_result::checkpoint_reported_in_capture(project_root: &Path, phase: u32) -> bool` (28-02)
- `state::State::session_id: Option<String>` — new `#[serde(default)]` field (28-02)
- `state::State::checkpoint_resumes: u32` — new `#[serde(default)]` field (28-02)
- `prompt::checkpoint_auto_decide_prompt(phase: u32) -> String` (28-03)
- `prompt::define_stage_prompt(phase: u32) -> String` — private (28-04)
- `agents::claude::ClaudeAgent::exec_resume_command(session_id: &str, instruction: &str) -> (&'static str, Vec<String>)` — inherent method, NOT on `AgentAdapter` (28-03)
- `mode::MAX_CHECKPOINT_RESUMES: u32` (28-03)
- `config::DevflowConfig::yes_ship: bool` — new field, default `false` (28-06)
- `config::yes_ship(project_root: &Path) -> bool` (28-06)

**`devflow-cli`**
- `pipeline_launch::spawn_agent_and_record(...)` — extracted from `launch_stage_inner` (28-03)
- `pipeline_launch::relaunch_checkpoint_session(state: &mut State, session_id: &str) -> Result<(), CliError>` (28-03)

**New runtime/config surface**
- `checkpoint_auto_decided` — new event kind in `.devflow/events.jsonl` (28-03)
- `yes_ship` — new key in `devflow.toml` (28-06)
- `DEVFLOW_YES_SHIP` — new environment variable override (28-06)

**New files**
- `.planning/phases/28-close-the-checkpoint-answer-return-path/28-PROBE.md` (28-01)
- `crates/devflow-cli/tests/yes_ship_config.rs` (28-06)

<output>
Create `.planning/phases/28-close-the-checkpoint-answer-return-path/28-01-SUMMARY.md` when done.
Record the A1 verdict verbatim in the summary — plan 28-02 Task 2 reads it.
</output>
