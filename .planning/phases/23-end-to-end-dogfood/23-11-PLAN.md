---
phase: 23-end-to-end-dogfood
plan: 11
type: execute
wave: 11
depends_on: ["23-10"]
files_modified:
  - .planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md
autonomous: false
requirements: [23a, yes-ship]

must_haves:
  truths:
    - "A recovery point exists on this repository before any unattended merge is authorized, and restoring `develop` from it has been verified as a real, working operation — not assumed"
    - "The phase the acceptance run will drive is low-stakes by an explicit, recorded standard, not by impression"
    - "`devflow --version` matches the workspace version, so the acceptance run exercises the supervisor this phase built and not a stale binary"
    - "The full workspace suite, clippy and fmt are green immediately before the run, so a failure during it is attributable to the unattended pipeline rather than to a broken tree"
    - statement: "A human has explicitly acknowledged D-07's accepted risk immediately before the irreversible run is authorized"
      verification: backstop
  artifacts:
    - .planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md
  key_links:
    - "recovery tag/branch → `develop` (the only thing that makes D-07's one-way merge recoverable)"
    - "rebuilt binary → the acceptance run (a stale binary would invalidate the whole result, as it has repeatedly on this project)"
---

<objective>
Prepare for the one irreversible act in this phase, and get a human's explicit
acknowledgement immediately before it.

D-02 requires the final acceptance run to be **self-hosted on this repository**,
not in a scratch repo — and that is deliberate coverage repair, not convenience.
`staleness_outcome` (`crates/devflow-cli/src/staleness.rs:276-284`) returns
`Block` only for `(is_self_dogfood: true, Stale)` and merely `Warn` for
`(false, Stale)`. **The staleness hard block — the most frequent observed
dogfood killer, which stopped two of the three recorded runs — is structurally
unreachable in a scratch repo.** So are the build-provenance and worktree-aware
paths. A scratch-only acceptance would ship this phase without ever exercising
the case that broke every prior run.

D-07 is the price: combined with D-02, `--yes-ship` on this repository means the
acceptance run will **unattended-merge a real phase into `develop`, bump the
version, and append the changelog** here. The operator considered a
self-dogfood refusal guard and **explicitly declined it**, on the grounds that a
genuine hands-off proof is the point of the phase and D-06 keeps the decision
auditable. That is a recorded, deliberate acceptance, not an oversight.

The operator specified two mitigations. This plan **encodes** them; it does not
re-decide the risk and it does not add a refusal guard.

Purpose: make the irreversible run recoverable and attributable before it
starts.
Output: a verified recovery point, a chosen low-stakes target, a rebuilt binary
and a green tree — all recorded.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-dogfood/23-CONTEXT.md
@.planning/phases/23-end-to-end-dogfood/23-RESEARCH.md
@.planning/phases/23-end-to-end-dogfood/23-VALIDATION.md
@.planning/phases/23-end-to-end-dogfood/23-PROBE-FINDINGS.md
@.planning/phases/23-end-to-end-dogfood/23-10-SUMMARY.md
</context>

## Artifacts this phase produces

This plan **creates**:
- a git tag or branch serving as the pre-run recovery point on this repository
- `.planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md`

This plan **removes**: nothing. It changes no source.

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Authorize D-07 — an unattended merge to `develop` on this repository</name>
  <decision>
Run the acceptance proof self-hosted on this repository with `--yes-ship`,
accepting that it will perform a real merge into `develop`, a real version bump
and a real changelog commit with no human answering the Ship gate.
  </decision>
  <context>
D-07 is **locked** in `23-CONTEXT.md` and rated **one-way**: undoing it means
rewriting `develop` history. `REVERSIBILITY_GATES` requires this
acknowledgement immediately before the irreversible act.

**This checkpoint is not re-opening the risk.** CONTEXT.md records it as
knowingly accepted, and its `<deferred>` section records that a self-dogfood
refusal guard was considered and explicitly declined this phase. Do not propose
one. What this checkpoint is for is the human confirmation that the *conditions*
are right, and the choice of target.

Why self-hosted rather than scratch, restated because it is the reason the risk
is worth taking: the staleness hard block is unreachable in a scratch repo
(`Block` only for `is_self_dogfood: true`), and that block stopped two of the
three runs in the recorded history. A scratch-only acceptance would prove the
supervisor while leaving the actual historical killer untested.

What is already true when this checkpoint is reached: the supervisor is built
and tested (23-05..23-07), a dead supervisor is now distinguishable from a
healthy pause (23-08), the Ship gate can be pre-authorized with an auditable
recorded decision (23-09), and a live phase can be stopped cleanly if the run
goes wrong (23-10).

**Two things this checkpoint must settle, and they are the substance of it:**

1. **Which phase the run drives.** The operator's mitigation is "drive a
   low-stakes phase." Propose a concrete candidate from `ROADMAP.md`'s backlog
   and state why it qualifies against a recorded standard — small diff, no
   public API change, no release-critical path, and recoverable by a plain
   revert if its content turns out wrong. State the candidate's identifier and
   its expected diff shape. Do not pick the phase by impression.
2. **What the recovery point is.** The operator's second mitigation is
   "establish a recovery point (tag or branch) before it starts." Task 2 creates
   it; this checkpoint confirms the form (tag versus branch) and that pushing it
   to `origin` — so it survives local loss — is wanted.

Also confirm before approving: no other phase of yours is mid-run, and nothing
else is depending on `develop`'s current head this hour.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed — self-hosted acceptance run with `--yes-ship`, on the named low-stakes phase</name>
      <pros>Exercises the self-dogfood staleness `Block` path that broke two of three prior runs and is structurally unreachable in a scratch repo; produces the phase's actual behavioural acceptance criterion; Task 2's recovery point makes `develop` restorable</pros>
      <cons>A real merge, version bump and changelog commit land on `develop` unattended; undoing them means rewriting history even with a recovery point</cons>
    </option>
    <option id="proceed-different-target">
      <name>Proceed, but drive a different phase than the one proposed</name>
      <pros>Correct if the proposed candidate is not as low-stakes as it looked — name the replacement and why</pros>
      <cons>None, if the replacement meets the same standard</cons>
    </option>
    <option id="halt">
      <name>Halt — do not run self-hosted</name>
      <pros>Zero risk to `develop`</pros>
      <cons>The phase's behavioural acceptance criterion goes unproven, and the staleness `Block` path stays untested. If chosen, record explicitly that the phase closes with its acceptance criterion unmet, so `/gsd-verify-work` does not treat scratch-only evidence as sufficient</cons>
    </option>
  </options>
  <resume-signal>Select: proceed, proceed-different-target (naming the phase), or halt</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Establish and verify a recovery point on `develop`</name>
  <files>.planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md</files>
  <read_first>
    - .planning/phases/23-end-to-end-dogfood/23-CONTEXT.md § D-07 — the operator's two specified mitigations, verbatim
    - scripts/ — list it and read `scripts/sync-main-to-develop.sh` for this project's conventions around tagging, pushing and branch handling
    - CONTRIBUTING.md — the `core.hooksPath` / pre-push hermeticity setup, and anything about signed tags
    - .planning/STATE.md § "Recently Shipped" — how prior releases recorded their signed tags, so the recovery point's form is consistent with them
  </read_first>
  <precondition>Task 1's checkpoint was answered `proceed` or `proceed-different-target`, and the working tree is clean (`git status --porcelain` is empty).</precondition>
  <action>
Create the pre-run recovery point the operator specified, in the form confirmed
at the checkpoint, and **prove it works** rather than assuming it does.

1. Record the current `develop` head SHA.
2. Create the recovery point at that SHA — a tag or a branch, per the
   checkpoint's answer. Name it so its purpose is obvious to a future reader
   and so it cannot be mistaken for a release tag (a release-shaped name would
   collide with this project's tag conventions and could confuse
   `devflow release --check`).
3. Push it to `origin` if the checkpoint said to, so it survives local loss.
4. **Verify the recovery point actually restores.** Do not assert this — do it:
   in a throwaway clone or a scratch worktree (never in the primary checkout,
   and never with a force-push), check out the recovery point and confirm its
   tree matches `develop`'s current tree exactly. `git diff --stat <point> develop`
   returning empty is the proof. A recovery point that has never been exercised
   is not a mitigation.
5. Write `23-ACCEPTANCE-SETUP.md` recording: the recovery point's name and SHA,
   whether it was pushed, the verbatim output of the restore verification, the
   phase chosen at the checkpoint and the standard it was judged against, and
   the exact `git` command an operator would run to restore `develop` from it.
   That last item is the whole point of the document — if the run goes wrong,
   nobody should have to reconstruct the command under pressure.

Do not modify `develop` itself. Do not force-push anything.
  </action>
  <acceptance_criteria>
    - `git rev-parse <recovery-point>` resolves and equals the recorded `develop` head SHA
    - `git diff --stat <recovery-point> develop` is empty
    - If pushing was requested: `git ls-remote --tags origin` (or `--heads`) lists the recovery point
    - `git status --porcelain` is empty — the primary checkout was not disturbed
    - `git rev-parse develop` is unchanged from the SHA recorded at the start of the task
    - `.planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md` exists and contains the recovery point's name, its SHA, the verbatim restore-verification output, and a literal restore command
    - The recovery point's name does not match this project's release-tag pattern (`v<major>.<minor>.<patch>`)
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; grep -qE 'restore|recover' .planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md &amp;&amp; echo RECOVERY_POINT_RECORDED</automated>
  </verify>
  <done>`develop` can be restored to its pre-run state by a command that has been written down and demonstrated to work.</done>
</task>

<task type="auto">
  <name>Task 3: Rebuild, re-verify the binary, and confirm the tree is green before the run</name>
  <files>.planning/phases/23-end-to-end-dogfood/23-ACCEPTANCE-SETUP.md</files>
  <read_first>
    - .planning/phases/23-end-to-end-dogfood/23-01-SUMMARY.md — how the same rebuild-and-verify was done at the start of the phase, so the check is identical
    - .planning/phases/23-end-to-end-dogfood/23-RESEARCH.md § "Common Pitfalls" → Pitfall 6
    - Cargo.toml — `[workspace.package].version`
    - .github/workflows/ci.yml — the three CI jobs, so the local gate matches CI exactly
  </read_first>
  <action>
Close Pitfall 6 a second time, and for the same reason as the first: **ten plans
have landed since the binary was last rebuilt.** Every one of the supervisor,
`devflow stop`, and `--yes-ship` changes exists only in `target/` until this
runs. An acceptance run against a pre-supervisor binary would prove nothing and
would be indistinguishable from a real failure — this project's own recorded
history has this exact mistake in it more than once.

1. `cargo build --release --workspace`.
2. Confirm `devflow --version` matches `[workspace.package].version` in
   `Cargo.toml`, and that `command -v devflow` still resolves into this
   checkout's `target/release/`.
3. Confirm the binary actually carries this phase's work — a version match alone
   would not catch a rebuild that silently used a stale artifact. Assert on
   behaviour: `devflow stop --help` exits 0, `devflow start --help` lists
   `--yes-ship`, and `devflow --help` does not offer the verb deleted in plan
   23-03.
4. Run the full local gate, matching CI exactly: `cargo test --workspace`,
   `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --check`.
   All three must be green **before** the run, so any failure during it is
   attributable to the unattended pipeline rather than to a broken tree.
5. Run `devflow doctor` against this repository and record its output. Resolve
   or explicitly note every finding before proceeding — starting an unattended
   run with a known outstanding finding makes the result ambiguous.
6. Append all of the above, verbatim, to `23-ACCEPTANCE-SETUP.md`.
  </action>
  <acceptance_criteria>
    - `devflow --version` prints a version containing the exact value of `[workspace.package].version` from `Cargo.toml`
    - `command -v devflow` resolves into this checkout's `target/release/`
    - `devflow stop --help` exits 0
    - `devflow start --help` output contains `--yes-ship`
    - `devflow --help` output does not contain the verb deleted in plan 23-03
    - `cargo test --workspace 2>&1 | grep -E '^test result:'` shows `0 failed` on every line
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
    - `devflow doctor .` output is captured verbatim in `23-ACCEPTANCE-SETUP.md`, with every finding either resolved or explicitly noted with a reason for proceeding
  </acceptance_criteria>
  <verify>
    <automated>test "$(devflow --version | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')" = "$(grep -m1 '^version' Cargo.toml | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')" &amp;&amp; devflow stop --help >/dev/null &amp;&amp; devflow start --help | grep -q -- '--yes-ship' &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]' &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <done>The binary on PATH provably carries this phase's supervisor, stop verb and ship flag, and the tree is green by CI's own three gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recovery point → `develop`'s restorability | It is the only thing standing between an unattended merge and unrecoverable history |
| binary on PATH → the acceptance run's validity | A stale binary makes every observation meaningless |
| `git push` of a recovery point → `origin` | A misnamed or misplaced ref could be mistaken for a release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-58 | Denial of Service | an unattended merge to `develop` with no working way back | high | mitigate | The recovery point is created **and demonstrated to restore** in a throwaway clone before the run is authorized, with the literal restore command written into `23-ACCEPTANCE-SETUP.md`. An unexercised recovery point is not a mitigation, so the verification is a required step rather than an assertion |
| T-23-59 | Spoofing | a stale binary making the acceptance run prove nothing while looking like a genuine failure | high | mitigate | Version match **plus** three behavioural assertions against the live binary (`stop --help`, `--yes-ship` present, deleted verb absent). A version match alone would not catch a stale artifact; this project has made exactly this mistake before |
| T-23-60 | Tampering | a recovery ref misread as a release tag by tooling or by an operator | medium | mitigate | The recovery point's name is required not to match the `v<major>.<minor>.<patch>` release pattern, asserted by an acceptance criterion |
| T-23-61 | Tampering | disturbing the primary checkout while verifying the recovery point | high | mitigate | Verification happens in a throwaway clone or scratch worktree; acceptance criteria assert `git status --porcelain` is empty and `git rev-parse develop` is unchanged. No force-push anywhere |
| T-23-62 | Repudiation | an ambiguous run result because a pre-existing `doctor` finding was outstanding | medium | mitigate | `devflow doctor` is run and captured before the run, and every finding must be resolved or explicitly noted with a reason for proceeding |

**Supply chain:** not applicable — zero new packages.
</threat_model>

<verification>
1. `git diff --stat <recovery-point> develop` is empty and
   `git rev-parse develop` is unchanged.
2. `git status --porcelain` is empty.
3. `devflow --version` matches `Cargo.toml`'s workspace version.
4. `devflow stop --help` exits 0; `devflow start --help` lists `--yes-ship`;
   `devflow --help` does not list the deleted verb.
5. `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`
   and `cargo fmt --check` are all green.
6. `23-ACCEPTANCE-SETUP.md` records the recovery point, the restore
   verification, the chosen phase and its standard, and the captured `doctor`
   output.
7. The blocking D-07 checkpoint has been answered by a human.
</verification>

<success_criteria>
- D-07's two operator-specified mitigations are encoded and executed, not
  restated.
- The recovery point has been demonstrated to restore, not merely created.
- The binary provably carries ten plans' worth of work.
- The tree is green by CI's own gates, so anything that fails during the run is
  attributable to the pipeline.
- A human has authorized the irreversible run with the target phase named.
</success_criteria>

<output>
Create `.planning/phases/23-end-to-end-dogfood/23-11-SUMMARY.md` when done.
</output>
