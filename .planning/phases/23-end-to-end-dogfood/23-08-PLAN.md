---
phase: 23-end-to-end-dogfood
plan: 08
type: execute
wave: 6
depends_on: [23-07]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-core/src/agent.rs
  - crates/devflow-core/src/git.rs
  - crates/devflow-core/tests/devflow_dir_gitignore.rs
autonomous: true
requirements: [23d]

must_haves:
  truths:
    - "The workspace contains zero references to the deleted two-agent construct — no types, no functions, no doc comments naming a capability that no longer exists."
    - "The two monitor API functions that existed only to serve the deleted verb are gone, and the one surviving monitor spawn function is untouched."
    - "The `.devflow` directory-creation guarantee still has a constructor-coverage point for every constructor it previously covered — coverage is re-pointed, never dropped."
    - "The git helper coverage the deleted verb's test exercised survives under an accurate name, or is deleted together with helpers that provably have no other caller."
    - statement: "No behaviour outside the deleted verb changed — the full workspace suite passes with a strictly larger or equal count of passing tests than before, once the deleted verb's own tests are accounted for."
      verification: backstop
  key_links:
    - "`monitor::spawn_monitor` — the surviving spawn function; `devflow_dir_gitignore.rs`'s constructor coverage must be re-pointed at it, not deleted."
    - "`agent_result::archive_phase_files` — the fourth constructor in the same coverage test, which must keep its own coverage point independent of the re-pointing."
---

<objective>
Finish unit **23d**: delete the two-agent construct's core-side surface and take
the workspace reference count to zero.

Purpose: plan 23-07 removed the CLI verb and its implementation, which left the
core-side types, the slot record read/write functions, and two monitor API
functions with no non-test callers. Verified this session, the remaining
distribution is: `agent_result.rs` 48 references (the slot kind type, its
record read/write functions, and their own test module — real production plus
test surface), `monitor.rs` 3 (doc comments on the two orphaned functions plus a
test name), `agent.rs` 1 (a module doc comment), `git.rs` 1 (a test name), and
`tests/devflow_dir_gitignore.rs` 2 (comments on the constructor-coverage call
this plan must re-point).

Output: zero workspace references, with coverage re-pointed rather than dropped.

**The one thing this plan must not do.** `crates/devflow-core/tests/devflow_dir_gitignore.rs`
uses `monitor::spawn_monitor_no_advance` as **constructor coverage point #3** in
a numbered list of the code paths that must create the `.devflow` directory with
its ignore file. That test's subject is the directory guarantee, not the deleted
verb — the deleted function is merely the vehicle it happened to use, chosen
because it never calls `save_state`. Deleting the call would silently narrow a
multi-constructor guarantee from N points to N-1 with no failing test to
announce it. The cross-AI review flagged this specifically. The call gets
re-pointed at the surviving spawn function; the coverage point stays.

**Visible skip, recorded per protocol:** spec-less probe fallback skipped —
phase has no requirement IDs to probe (visible skip). `must_haves.truths` are
derived from the ROADMAP phase goal and the probe evidence.

<!-- planner-discipline-allow: sequentagent -->
<!-- planner-discipline-allow: Sequentagent -->
<!-- planner-discipline-allow: SequentagentSlotKind -->
This plan necessarily names the identifiers it deletes in order to specify the
deletion. The acceptance criteria grep `crates/`, never this plan file.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-end-to-end-dogfood/23-RESEARCH.md
@.planning/phases/23-end-to-end-dogfood/23-REVIEWS.md
@.planning/phases/23-end-to-end-dogfood/23-07-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Delete the core-side surface end-to-end and take the workspace count to zero</name>
  <files>crates/devflow-core/src/agent_result.rs, crates/devflow-core/src/monitor.rs, crates/devflow-core/src/agent.rs, crates/devflow-core/src/git.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs lines 895-1000 (the slot kind enum, its `as_str`, the slot record type and its path-free-by-construction doc comment) plus every other reference in the file, located with a grep before editing — 48 references span production code and a colocated test module
    - crates/devflow-core/src/monitor.rs lines 44-70 and 181-230 and the test module near `:340-400` (`spawn_monitor` versus `spawn_monitor_no_advance`, `wait_for_agent_pid` versus `wait_for_agent_exit`, and the SIGTERM regression test near `:340-382` which covers the *surviving* spawn path and must not be collaterally removed)
    - crates/devflow-core/src/agent.rs line 7 (a module doc comment referencing the deleted verb; the module's functions are untouched, including `terminate` and `looks_like_devflow_process` added by plan 23-05)
    - crates/devflow-core/src/git.rs near line 1403 (a test name referencing the deleted verb; determine whether the git helpers that test exercises have any surviving caller before deciding its fate)
    - crates/devflow-cli/src/parallel.rs (as delivered by plan 23-07 — confirm the two functional call sites of the orphaned monitor APIs are already gone, so this deletion cannot break a live caller)
  </read_first>
  <action>
Work in dependency order, letting the compiler find anything missed.

In `agent_result.rs`: delete the slot-kind enum together with its `as_str`
method, the slot record type, and the functions that read and write that record,
plus their colocated tests. Grep first — 48 references is enough surface that a
by-eye pass will miss some, and some are doc comments on surviving functions
that need rewording rather than deleting.

In `monitor.rs`: delete `spawn_monitor_no_advance` and `wait_for_agent_exit`
together with their doc comments, after confirming plan 23-07 already removed
their two functional call sites in `parallel.rs`. **Do not** touch
`spawn_monitor`, `wait_for_agent_pid`, `spawn_monitor_inner`, the generated
script, or the SIGTERM regression test near `:340-382` — that test covers the
surviving spawn path's agent-teardown guarantee and is unrelated to the deleted
verb despite sitting nearby. Reword the test-module comment near `:384` that
frames the no-advance monitor as the deleted verb's mechanism.

In `agent.rs`: reword the module doc comment so it describes the current
monitor-owned execution model without naming the removed capability. Change no
code.

In `git.rs`: read the test near `:1403` and decide by evidence. If the git
helpers it exercises still have surviving callers, keep the test and rename it
to describe what it actually covers. If they have none, delete helpers and test
together. Record which of the two applies, and the caller grep that decided it,
in the summary — this is the one reference in the inventory whose disposition
could not be settled at plan time.
  </action>
  <verify>
    <automated>cargo build --workspace 2>&amp;1 | rg -q '^error' &amp;&amp; exit 1 || cargo test -p devflow-core 2>&amp;1 | rg -q 'test result: FAILED' &amp;&amp; exit 1 || cargo test -p devflow-core agent_result:: 2>&amp;1 | rg -q '^test result: ok\. [1-9][0-9]* passed'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -c 'sequentagent|Sequentagent|SequentAgent' crates/` returns 0 across every file in the workspace except `crates/devflow-core/tests/devflow_dir_gitignore.rs`, which Task 2 finishes.
    - Source assertion: `rg -n 'pub fn spawn_monitor\b' crates/devflow-core/src/monitor.rs` still matches exactly one line.
    - Source assertion: `rg -n 'pub fn wait_for_agent_pid' crates/devflow-core/src/monitor.rs` still matches exactly one line.
    - Source assertion: `rg -c 'spawn_monitor_no_advance|wait_for_agent_exit' crates/devflow-core/src/monitor.rs` returns 0.
    - Source assertion: the SIGTERM regression test covering the surviving spawn path is still present in `monitor.rs` — assert by its test name, recorded in the summary.
    - Source assertion: `rg -n 'pub fn terminate' crates/devflow-core/src/agent.rs` still matches — plan 23-05's additions survive the doc-comment rewording.
    - Test command: `cargo test -p devflow-core agent_result::` reports `test result: ok.` with a passed count strictly greater than zero; record the before and after counts in the summary.
    - Test command: `cargo test -p devflow-core` reports no `test result: FAILED`.
    - The summary records, for `git.rs`, which disposition was taken and the caller grep that decided it.
  </acceptance_criteria>
  <done>The core-side surface is gone, the workspace builds, the surviving monitor API and its regression coverage are intact. Committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-point the .devflow constructor coverage instead of narrowing it</name>
  <files>crates/devflow-core/tests/devflow_dir_gitignore.rs</files>
  <read_first>
    - crates/devflow-core/tests/devflow_dir_gitignore.rs (whole file — the numbered constructor list, the `wait_for_file` and `wait_for_pid_to_die` helpers and the `waitpid` reaping note explaining why this test binary must reap what it spawns, and the `failures` accumulator that names each constructor in its assertion message)
    - crates/devflow-core/src/monitor.rs lines 44-52 (`spawn_monitor`'s signature and the fact that it appends an `advance` tail the no-advance variant omitted — the behavioural difference the re-pointing must accommodate)
    - crates/devflow-core/src/workflow.rs — `ensure_devflow_dir`, the function whose guarantee this test actually covers
    - .planning/phases/23-end-to-end-dogfood/23-REVIEWS.md § "23-04" (the finding that this coverage must be replaced, not deleted, and that test-count-based acceptance is brittle — assert named tests and constructor labels instead)
  </read_first>
  <behavior>
    - The constructor-coverage test still enumerates the same number of constructors as before this plan, with each one named in its failure message.
    - The re-pointed constructor still creates `.devflow` with its ignore file, and the test still detects a regression if it stops doing so.
    - The test reaps any process it spawns, leaving no zombie behind.
    - The test's assertion failure message names the specific constructor that failed, not just a count.
  </behavior>
  <action>
Re-point constructor coverage point #3 from the deleted no-advance spawn
function to the surviving `monitor::spawn_monitor`, and update the two comments
naming the removed verb to describe what the point actually covers: a spawn path
that creates the capture directory without going through `save_state`.

Accommodate the one behavioural difference: `spawn_monitor` appends a `devflow
advance` tail that the deleted variant omitted. In this fixture that tail runs
against a temporary root with no valid workflow state and will fail harmlessly,
but it must not make the test flaky or slow. Bound it — the existing
`wait_for_file` on the exit-code path and `wait_for_pid_to_die` already give the
test deterministic synchronisation points; use them rather than a fixed sleep,
and set any timeout override on the spawned `Command` itself via `.env(...)`,
never `std::env::set_var` (999.37).

Keep the coverage point's identity intact: the same numbered label, the same
entry in the `failures` accumulator, the same assertion message shape. The
cross-AI review's finding is that a count-based acceptance is brittle — assert
on the named constructor labels, so a future change that drops a constructor
fails with a message naming it rather than an off-by-one on a number.

If `spawn_monitor`'s advance tail proves genuinely unworkable in this fixture,
the fallback is to add a different, real constructor as point #3 rather than
renumbering down to N-1 points. Record which of the two you did, and why, in the
summary. Narrowing the guarantee is not an available option.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --test devflow_dir_gitignore 2>&amp;1 | rg -q '^test result: ok\. [1-9][0-9]* passed' &amp;&amp; cargo test --workspace 2>&amp;1 | rg -q 'test result: FAILED' &amp;&amp; exit 1 || cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `rg -c 'sequentagent|Sequentagent|SequentAgent' crates/` returns 0 across the entire workspace — the phase-wide deletion target is met.
    - Source assertion: the number of numbered constructor points in `devflow_dir_gitignore.rs` is the same as before this plan; record both numbers in the summary.
    - Source assertion: `rg -c 'set_var' crates/devflow-core/tests/devflow_dir_gitignore.rs` returns 0.
    - Behaviour: the test's failure path names the specific constructor, verified by reading the `failures` accumulator's assertion message.
    - Behaviour: the test still calls its pid-reaping helper for any process it spawns.
    - Test command: `cargo test -p devflow-core --test devflow_dir_gitignore` reports `test result: ok.` with a passed count of at least 1.
    - Gate: `cargo test --workspace` reports no `test result: FAILED`; `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` both exit 0.
    - Record the full-workspace passing test count before and after this plan in the summary, alongside the count of tests deliberately removed with the deleted verb, so the delta is explained rather than merely reported.
  </acceptance_criteria>
  <done>The workspace reference count is zero, and the `.devflow` directory guarantee covers exactly as many constructors as it did before. Committed.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces (plan 23-08)

**Removed public symbols (`devflow_core::agent_result`)**
- `SequentagentSlotKind` and its `as_str` method
- The two-agent slot record type and the functions that read and write it
- Their colocated test module

**Removed public symbols (`devflow_core::monitor`)**
- `spawn_monitor_no_advance`
- `wait_for_agent_exit`

**Explicitly preserved (must still exist after this plan)**
- `monitor::spawn_monitor`, `monitor::wait_for_agent_pid`, `monitor::spawn_monitor_inner`
- The SIGTERM regression test covering the surviving spawn path's agent teardown
- `agent::agent_running`, `agent::terminate`, `agent::looks_like_devflow_process`
- Every numbered constructor point in `devflow_dir_gitignore.rs`

**Modified documentation-only surfaces**
- `agent.rs` module doc comment; `monitor.rs` test-module comment; `git.rs` test name (kept-and-renamed or deleted-with-helpers, decided by caller grep at execution time and recorded in the summary)

**New dependencies:** none. **New config keys:** none. **New environment variables:** none. **New CLI surface:** none.
</artifacts>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deletion sweep → surviving test coverage | a grep-driven deletion can remove tests whose subject is unrelated to the deleted construct |
| test fixture → spawned child process | the re-pointed constructor spawns a real detached process from a test binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-81 | Tampering | `.devflow` directory-creation guarantee | high | mitigate | Deleting constructor coverage point #3 would silently narrow a multi-constructor guarantee with no failing test to announce it — a regression in the ignore-file guarantee would then ship unnoticed. Mitigated by re-pointing at the surviving spawn function, asserting the constructor count is unchanged, and forbidding narrowing as an option. |
| T-23-82 | Tampering | the surviving SIGTERM regression test | high | mitigate | That test sits adjacent to the deleted functions in `monitor.rs` and covers the surviving spawn path's agent-teardown guarantee. Deleting it by proximity would remove the only coverage of a real, previously-fixed orphaning bug. Mitigated by an acceptance criterion asserting the test is still present by name. |
| T-23-83 | Denial of Service | zombie processes from the re-pointed fixture | medium | mitigate | The re-pointed constructor spawns a real process whose advance tail now runs; without reaping, the test binary accumulates zombies and may hang CI. Mitigated by reusing the file's existing `wait_for_pid_to_die` helper and its documented `waitpid` reasoning, and by bounding the tail with a per-`Command` timeout env rather than a fixed sleep. |
| T-23-84 | Repudiation | unexplained test-count delta | low | mitigate | A shrinking workspace test count after a deletion is indistinguishable from accidentally dropped coverage. Mitigated by requiring before and after counts plus the count of tests deliberately removed, so the delta is explained rather than reported. |
| T-23-SC | Tampering | cargo installs | high | mitigate | **No package installs in this plan** — it is purely subtractive. `23-RESEARCH.md` § Package Legitimacy Audit: "Not applicable." |
</threat_model>

<verification>
- `rg -c 'sequentagent|Sequentagent|SequentAgent' crates/` returns 0 for every file in the workspace.
- `cargo test --workspace` reports no `test result: FAILED`.
- `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
- `cargo fmt --check` exits 0.
- `cargo build --workspace` produces no `error` line.
- The summary records: before/after workspace test counts, the count deliberately removed, the `git.rs` disposition and its deciding grep, and the `devflow_dir_gitignore.rs` constructor count before and after.
</verification>

<success_criteria>
- The two-agent construct is entirely absent from the workspace.
- The surviving monitor spawn API and its regression coverage are provably intact.
- The `.devflow` constructor guarantee is re-pointed, never narrowed.
- Every test-count change is explained, not merely observed.
</success_criteria>

<output>
Create `.planning/phases/23-end-to-end-dogfood/23-08-SUMMARY.md` when done.
</output>
