---
phase: 23-end-to-end-dogfood
plan: 06
type: execute
wave: 6
depends_on: ["23-05"]
files_modified:
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-core/tests/supervisor_e2e.rs
  - crates/devflow-cli/src/main.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - OPERATIONS.md
  - ARCHITECTURE.md
autonomous: true
requirements: [23b]

must_haves:
  truths:
    - "The supervisor is `devflow` itself, not a shell script — it binds the socket, owns the agent, and answers `ping` and `shutdown` on it"
    - "A stop is not a completion: an agent torn down by a socket `shutdown` or by a signal produces a distinct return value that makes running `advance` afterwards impossible, rather than a flag a caller could ignore (R-M)"
    - "On natural agent exit the stage machine advances by a direct function call inside the supervisor process — there is no forked tail process left to orphan (D-10, R-E)"
    - "SIGTERM and SIGINT to the supervisor perform the same clean shutdown as the socket `shutdown` command: the group is torn down, an exit code is recorded, and the socket file is removed"
    - "The socket rejects any line that is not `ping` or `shutdown`, answering `unknown` rather than acting on it (ASVS V5)"
    - "A supervisor refuses to start against a socket a live supervisor already owns; only a stale socket may be reclaimed"
    - "Capture parity holds: the agent runs in the worktree cwd, stdout and stderr land in separate files, the agent pid file is written, and a definite exit code is recorded on every exit path including teardown (R-A, R-B, R-C, R-D, R-K)"
    - "Adapter-scoped environment reaches the agent and its grandchildren (R-F)"
  artifacts:
    - crates/devflow-core/tests/supervisor_e2e.rs
  key_links:
    - "monitor::serve → SupervisorExit → pipeline_launch::supervise (only the NaturalExit variant can reach `advance`)"
    - "CLI Command::Supervise → doc_check::source_devflow_env_vars_and_subcommands_are_documented (every enum variant must appear as `devflow <variant>` in the scoped operator docs)"
    - "SIGTERM/SIGINT handler → the same teardown path as the socket `shutdown` command"
---

<objective>
Build the supervisor process itself: the run loop that owns the agent, and the
`devflow` subcommand that *is* the supervisor.

This is where D-10 — the phase's single most load-bearing property — lands.
Today `monitor.rs:138-147` builds a shell string ending
`; {binary} advance {project_root} --phase {phase}`, a **separate forked
process**. That process is exactly what Phase 17's incident orphaned: the shell
trap only ever tracked the agent pid, so a signal arriving during the tail had
nothing to kill. Because the supervisor becomes `devflow` itself, it calls the
advance logic directly and then exits. **There is no tail process left to
orphan.** The original failure mode is removed by construction, not by better
signal handling.

**R-M is enforced by the type system here, not by a flag.** `serve` returns
`SupervisorExit`, whose `Stopped` variant carries no exit code and therefore
gives the caller nothing to advance with. A stopped phase cannot advance its own
state machine because the shape of the return value makes it unexpressible —
which the spike audit calls out as "a genuine behavioural improvement, not just
parity."

**Note on D-08's big-bang.** This plan *adds* the new path while the `sh -c`
path still exists; plan 23-07 deletes the old one and swaps the call site. That
is a two-commit landing sequence, **not** a dual-path period: there is no
feature flag, no config toggle, and no run in which an operator could choose
between the two. Nothing calls `serve` until 23-07 rewires
`pipeline_launch.rs:126`. D-08 forbids running both process models behind a
toggle; it does not require the addition and the deletion to be one commit.

Purpose: make the supervisor a real, tested Rust process before anything
depends on it.
Output: `monitor::serve`, the hidden `devflow supervise` subcommand, in-process
advance, signal handling, and fake-agent integration coverage of every
R-A..R-M parity item that does not require a real agent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-end-to-end-dogfood/23-CONTEXT.md
@.planning/phases/23-end-to-end-dogfood/23-RESEARCH.md
@.planning/phases/23-end-to-end-dogfood/23-PATTERNS.md
@.planning/audits/2026-07-24-socket-supervisor-spike.md
@.planning/spikes/socket-supervisor/main.rs
@.planning/phases/23-end-to-end-dogfood/23-05-SUMMARY.md
</context>

## Recorded decision — Claude's Discretion item #1: supervisor signal handling

**DECIDED: land it in 23b (this plan), not deferred.**

CONTEXT.md left this to the planner and required an explicit, recorded call.
The spike installs no handler, so a SIGTERM to the supervisor kills it without
cleanup and leaves a stale socket. That degrades *correctly* — the sweep finds
it and the pgid backstop reaps the group — but "degrades correctly" here means
the operator's agent keeps running unsupervised until someone notices, which is
a milder version of the exact Phase 17 incident this phase exists to close.

It is landed now because it is cheap in this design and expensive later: the
run loop built in Task 1 is a non-blocking poll, so a handler that does nothing
but set an `AtomicBool` the loop already checks each iteration costs a dozen
lines and reuses the teardown path verbatim. Deferring it would mean revisiting
the loop's structure in a later phase to make it interruptible.

## Artifacts this phase produces

This plan **creates**:
- `monitor::SupervisorConfig` — struct carrying `socket_path`, `workdir`,
  `stdout_path`, `stderr_path`, `pid_path`, `exit_path`, `program`, `args`,
  `envs`, `shutdown_grace`
- `monitor::SupervisorExit` — enum: `NaturalExit(i32)`, `Stopped(StopSource)`
- `monitor::StopSource` — enum: `Socket`, `Signal`
- `monitor::serve(cfg: SupervisorConfig) -> Result<SupervisorExit, MonitorError>`
- CLI `Command::Supervise` (hidden) and `pipeline_launch::supervise()`
- `crates/devflow-core/tests/supervisor_e2e.rs` (new integration test file)
- events `supervisor_stopped` and `supervisor_advance_suppressed`

This plan **removes**: nothing. The `sh -c` path is still present and still the
one `pipeline_launch.rs:126` calls; plan 23-07 performs the swap and the
deletion.

<tasks>

<task type="auto">
  <name>Task 1: The supervisor run loop — socket service, agent ownership, signal handling</name>
  <files>crates/devflow-core/src/monitor.rs</files>
  <read_first>
    - crates/devflow-core/src/monitor.rs — the whole file, including everything plan 23-05 added (`SupervisorHandle`, `SocketState`, `socket_state`, `supervisor_socket_path`, `MonitorError`'s new variants) and the surviving `sh -c` implementation at :67-179, whose capture-path block at :91-104 and env-propagation comment at :168-170 state the parity obligations
    - .planning/spikes/socket-supervisor/main.rs :68-161 — the full monitor function: takeover safety, bind, 0600, agent spawn with `process_group(0)`, pid file, the natural-exit path, and the `ping`/`shutdown`/`unknown` dispatch
    - .planning/audits/2026-07-24-socket-supervisor-spike.md :142-190 — the R-A..R-M table and the two prose sections explaining why the advance tail moves in-process and why R-M is more than parity
    - .planning/phases/23-end-to-end-dogfood/23-PATTERNS.md § "crates/devflow-core/src/monitor.rs (service, event-driven) — the rewrite" — every excerpt to adapt, including the note that all the spike's `.expect(...)` calls become `?`
    - crates/devflow-core/src/agent_result.rs — `stdout_path`, `stderr_path`, `exit_code_path`, `agent_pid_path`, so the capture-file contract is exact
  </read_first>
  <action>
Add the supervisor run loop to `monitor.rs`. This code runs **inside** the
supervisor process; it does not spawn one (plan 23-07 adds `spawn_supervisor`).

**`SupervisorConfig`** — a plain public struct carrying everything the loop
needs, so `serve` reads no globals: `socket_path: PathBuf`, `workdir: PathBuf`,
`stdout_path: PathBuf`, `stderr_path: PathBuf`, `pid_path: PathBuf`,
`exit_path: PathBuf`, `program: String`, `args: Vec<String>`,
`envs: Vec<(String, String)>`, `shutdown_grace: Duration`.

**`SupervisorExit`** — public enum, `NaturalExit(i32)` and
`Stopped(StopSource)`; **`StopSource`** — public enum, `Socket` and `Signal`.
This shape is the R-M enforcement: `Stopped` carries no exit code, so a caller
holding it has nothing to advance with. Do not add a `bool` alongside — the
point is that the suppression is not a flag someone can forget to check.

**`serve(cfg: SupervisorConfig) -> Result<SupervisorExit, MonitorError>`**:

1. **Takeover safety.** Probe `socket_state(&cfg.socket_path)` first. `Alive`
   → return `MonitorError::LiveSupervisorOwnsSocket`; never steal a socket a
   live supervisor owns. `Stale` → remove the file and continue. `Gone` →
   continue.
2. Ensure the socket's parent directory exists (plan 23-05's
   `supervisor_socket_dir` already creates it at 0700), `UnixListener::bind`,
   then set the socket file to **0600**, then `set_nonblocking(true)` on the
   listener so the loop can poll it alongside the child.
3. Create the stdout and stderr files (separate files — R-B: stderr must never
   corrupt the JSON stdout capture DevFlow parses for its result marker).
4. Spawn the agent: `Command::new(&cfg.program)` with `cfg.args`,
   `.current_dir(&cfg.workdir)` (R-A: the worktree, not the project root),
   `.stdin(Stdio::null())`, stdout and stderr from the two files,
   `.process_group(0)` (whole agent subtree in one killable group, and — because
   the agent becomes its own group leader — its pid *is* its pgid, which is what
   plan 23-07's `spawn_supervisor` records), and every pair in `cfg.envs`
   applied with `.env(k, v)` (R-F: adapter-scoped env must reach the agent and
   its grandchildren).
5. Write the agent pid to `cfg.pid_path` (R-C).
6. **Install the signal handlers** — a module-level `static AtomicBool`, and an
   `extern "C"` handler registered for `SIGTERM` and `SIGINT` that does exactly
   one thing: store `true`. A plain atomic store is async-signal-safe;
   allocating, locking, formatting or logging inside a signal handler is not, so
   the handler must contain nothing else.
7. **Poll loop**, ~25-50 ms per iteration, checking three things each pass, in
   this order:
   - `child.try_wait()` returned `Some(status)` → natural exit. Compute the code
     as `status.code()`, falling back to `128 + signal` for a signal death
     (shell convention, R-D). Write it to `cfg.exit_path`. Remove the socket
     file. Return `SupervisorExit::NaturalExit(code)`.
   - the signal flag is set → teardown via the shared path below, returning
     `SupervisorExit::Stopped(StopSource::Signal)`.
   - a connection is pending on the non-blocking listener → accept it, read one
     line with a read timeout, and dispatch: `ping` answers a single line
     naming the supervisor pid and the agent pid; `shutdown` runs the shared
     teardown and returns `SupervisorExit::Stopped(StopSource::Socket)` after
     replying `stopped`; **anything else answers `unknown <input>` and is
     otherwise ignored** — never parsed further, never executed (ASVS V5). A
     `WouldBlock` from `accept` is the normal no-connection case, not an error.
8. **Shared teardown path**, used identically by the socket command and by a
   signal: `libc::killpg` the agent's group with `SIGTERM`, poll for the child
   for `cfg.shutdown_grace`, escalate to `SIGKILL` and reap if the grace
   expires, write `143` to `cfg.exit_path` (R-K: teardown still records a
   definite outcome, never silence), remove the socket file. Do **not** run any
   advance-shaped work here.

Every fallible call propagates with `?` against `MonitorError`. No
`.unwrap()`/`.expect()` outside `#[cfg(test)]`. `serve` must not call
`std::process::exit` — the spike does, because it is throwaway proof code;
production returns so the caller can decide what happens next, which is exactly
what R-M depends on.

Add unit tests in `monitor.rs`'s own test module for the pieces that need no
child process: the `unknown` dispatch answering without acting, and the
takeover refusal when the socket is `Alive`. Everything requiring a real agent
belongs in Task 2.

<!-- planner-discipline-allow: process::exit -->
  </action>
  <acceptance_criteria>
    - `crates/devflow-core/src/monitor.rs` declares `pub enum SupervisorExit` with a `NaturalExit` variant carrying an `i32` and a `Stopped` variant carrying a `StopSource`, and `SupervisorExit::Stopped` carries no exit code
    - `rg -n 'process::exit' crates/devflow-core/src/monitor.rs` reports no matches
    - `rg -n 'fn serve' -A 3 crates/devflow-core/src/monitor.rs` shows a return type of `Result<SupervisorExit, MonitorError>`
    - The signal handler function body contains exactly one statement (an atomic store) — verify by reading it, and by `rg -n 'extern "C" fn' -A 4 crates/devflow-core/src/monitor.rs`
    - `rg -n '\.unwrap\(\)|\.expect\(' crates/devflow-core/src/monitor.rs` matches only lines inside the `#[cfg(test)]` module
    - `cargo test -p devflow-core -- monitor:: 2>&1 | grep -E '^test result:'` shows `0 failed`
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p devflow-core -- monitor:: 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; ! rg -q 'process::exit' crates/devflow-core/src/monitor.rs &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <done>A supervisor loop exists that owns an agent, serves `ping`/`shutdown`, tears down identically on a signal, and returns a value whose shape makes advancing after a stop unexpressible.</done>
</task>

<task type="auto">
  <name>Task 2: Fake-agent integration coverage for every automatable R-A..R-M parity item</name>
  <files>crates/devflow-core/tests/supervisor_e2e.rs</files>
  <read_first>
    - crates/devflow-core/tests/monitor_e2e.rs — full file. It is the shape to reproduce: a fake agent driven through `sh`, polling for the capture files, asserting on their contents. Its hermetic git helper at :26-58 and its bounded polling loop at :100-113 are both reused verbatim in spirit
    - crates/devflow-core/src/test_support.rs :69-84 — `hermetic_command`/`git_command` and the `REPO_LOCAL_GIT_VARS` scrub. Every git invocation in a new test must go through these (999.37)
    - .planning/audits/2026-07-24-socket-supervisor-spike.md :148-162 — the R-A..R-M table with the exact assertion each item was proven by in the spike
    - .planning/phases/23-end-to-end-dogfood/23-VALIDATION.md § "Per-Task Verification Map" — the 23b rows and their Wave-0 status
    - crates/devflow-core/Cargo.toml — the `test-support` feature gate, so the new test file declares the right `required-features` or cfg
  </read_first>
  <action>
Create `crates/devflow-core/tests/supervisor_e2e.rs` — fake-agent integration
coverage for `monitor::serve`. Fast and deterministic: the agent is a `sh -c`
one-liner, never a real coding agent. This proves the *mechanism*; the
behavioural acceptance criterion stays outside `cargo test` entirely (plans
23-02 and 23-11).

`serve` blocks, so each test runs it on a spawned thread and drives it from the
test thread through the socket, or lets the fake agent exit on its own.

Cases, one per parity obligation:

- **R-A (cwd)** — fake agent is `sh -c 'pwd'`; assert the captured stdout is the
  configured worktree directory, not the project root. Build the two directories
  as siblings under one `tempfile::tempdir`, never nested, so a
  "contains the worktree path" and "does not contain the project root path" pair
  of assertions cannot both be satisfied by a prefix accident.
- **R-B (separate streams)** — fake agent writes a known line to stdout and a
  different known line to stderr; assert each file contains its own line and
  **not** the other's. This is what protects result-marker parsing from stderr
  contamination.
- **R-C (pid file)** — assert `pid_path` exists and parses to a positive integer
  matching the running child.
- **R-D (exit code)** — fake agent `sh -c 'exit 7'`; assert `exit_path` contains
  `7` and that `serve` returned `SupervisorExit::NaturalExit(7)`.
- **R-D signal death** — fake agent kills itself with a signal; assert the
  recorded code is `128 + signal`.
- **R-F (env to grandchildren)** — pass an env pair in `cfg.envs`; fake agent is
  `sh -c 'sh -c "echo $THE_VAR"'` (two levels deep); assert the value appears in
  the stdout capture. This is the Codex unsigned-commit-override propagation
  path.
- **R-K + R-M (a stop is not a completion)** — fake agent sleeps; send
  `shutdown` on the socket; assert three things: `serve` returned
  `SupervisorExit::Stopped(StopSource::Socket)`, `exit_path` contains `143`
  (a definite outcome, not silence), and the socket file no longer exists.
  Assert the returned value is **not** any `NaturalExit`. This is the single
  regression test RESEARCH names as most needing to exist.
- **Signal parity** — same setup, but send `SIGTERM` to the supervisor thread's
  process instead of a socket message, and assert the identical three outcomes
  with `StopSource::Signal`. (If signalling the test process itself is not
  workable, drive this case through a spawned `devflow supervise` child once
  Task 3 lands, and say in the SUMMARY which approach you used.)
- **R-G (detachment)** — spawn a short-lived process that itself launches a
  supervisor over a sleeping fake agent, let that launcher exit, then assert
  from the test thread that `socket_state` still reports `Alive` and that the
  agent is still running. The supervisor must outlive the process that started
  it — that is the entire reason the monitor exists rather than a CLI-scoped
  capture thread, and it is the only R-item that a same-process test cannot
  prove by construction.
- **R-L (permissions)** — assert the bound socket file's mode is `0o600` and its
  parent directory's mode is `0o700`.
- **V5 (input validation)** — send the line `rm -rf /` on the socket; assert the
  reply begins with `unknown`, the agent is still running afterwards, and no
  file outside the temp root was touched.
- **Takeover safety** — bind a live supervisor, then call `serve` again against
  the same socket path; assert it returns `MonitorError::LiveSupervisorOwnsSocket`
  and that the first supervisor's agent is still alive. Then kill the first
  supervisor without cleanup, leaving a stale socket, and assert a second
  `serve` reclaims it successfully.
- **R-J (sweep)** — start two supervisors on distinct socket paths in one temp
  cache dir, kill one, and assert `monitor::sweep` reports one `Alive` and one
  `Stale` with no state file involved.

Every test must clean up its supervisor thread and reap its child, mirroring
`devflow_dir_gitignore.rs`'s `wait_for_pid_to_die` helper — a leaked sleeping
agent would make the suite flaky for every later plan. Use bounded polling
loops with an explicit assertion message on timeout; never an unbounded wait.
  </action>
  <acceptance_criteria>
    - `crates/devflow-core/tests/supervisor_e2e.rs` exists
    - `cargo test -p devflow-core --test supervisor_e2e 2>&1 | grep -E '^test result:'` shows at least `12 passed` and `0 failed`
    - `cargo test -p devflow-core --test supervisor_e2e -- --list` includes a test whose name contains `outlives` or `detach` — the R-G case
    - `cargo test -p devflow-core --test supervisor_e2e -- --list` includes a test whose name contains both `stop` and `suppress` (or `not_a_completion`) — the R-M case
    - `cargo test -p devflow-core --test supervisor_e2e -- --list` includes a test whose name contains `unknown` or `rejects` — the V5 case
    - `rg -n 'Command::new\("git"' crates/devflow-core/tests/supervisor_e2e.rs` reports no matches
    - `rg -n 'std::env::set_var' crates/devflow-core/tests/supervisor_e2e.rs` reports no matches
    - Running the file twice back to back both times reports `0 failed` (no leaked agent from run one wedges run two)
    - `cargo test --workspace 2>&1 | grep -E '^test result:'` shows `0 failed` on every line
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p devflow-core --test supervisor_e2e 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; cargo test -p devflow-core --test supervisor_e2e 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; echo SUPERVISOR_E2E_STABLE</automated>
  </verify>
  <done>Every R-A..R-M parity obligation that a fake agent can prove is proven, including the stop-is-not-a-completion regression and the unrecognised-input rejection.</done>
</task>

<task type="auto">
  <name>Task 3: The `devflow supervise` subcommand and the in-process advance tail (D-10)</name>
  <files>crates/devflow-cli/src/main.rs, crates/devflow-cli/src/pipeline_launch.rs, OPERATIONS.md, ARCHITECTURE.md</files>
  <read_first>
    - crates/devflow-cli/src/main.rs — the `Command` enum, specifically the hidden `Advance` variant at ~:80-92 (`#[command(hide = true)]`, its `project: PathBuf` default and its `phase: Option<u32>`) — the exact precedent for a hidden, internal, monitor-invoked subcommand — and the dispatch block around :478-510
    - crates/devflow-cli/src/pipeline_launch.rs :255-330 — `advance`'s full body: how it resolves the phase, acquires the per-phase lock, and loads state. This answers RESEARCH's assumption A2 — read it and record in the SUMMARY whether it was already callable as a plain function or needed extraction
    - crates/devflow-cli/src/pipeline_launch.rs :55-140 — `launch_stage_inner`, so the shape of the config the supervisor will be handed is in view (plan 23-07 builds it)
    - crates/devflow-core/src/doc_check.rs :7-12 (`SCOPED_DOCS`), :181-207 (`enum_variants` — note it lowercases each variant and requires the name be purely alphabetic) and :404-422 (`source_devflow_env_vars_and_subcommands_are_documented`). **Every `Command` variant must appear as `devflow <lowercased-variant>` in a scoped doc or the test fails**
    - OPERATIONS.md around :50 — the existing one-line treatment of the internal `devflow advance`, the pattern to copy
    - ARCHITECTURE.md :230-250 — the passage describing the monitor as a shell process that "runs `devflow advance <project> --phase N`", which this plan makes untrue
    - .planning/phases/23-end-to-end-dogfood/23-RESEARCH.md § "Common Pitfalls" → Pitfall 4 (env/adapter propagation when the advance tail moves in-process)
  </read_first>
  <action>
Add the CLI half: the subcommand that *is* the supervisor process, and the
in-process advance tail.

**`Command::Supervise`** — a new variant on the CLI `Command` enum, marked
`#[command(hide = true)]` exactly as `Advance` is. Its fields carry everything
`SupervisorConfig` needs that cannot be re-derived: `phase: u32`,
`project: PathBuf` (positional, default `.`), the socket path, the workdir, and
the program plus its arguments. Prefer re-deriving the four capture paths from
`project` and `phase` through `agent_result::{stdout_path, stderr_path,
exit_code_path, agent_pid_path}` rather than passing them on argv — fewer
arguments, and no way for the two sides to disagree about where captures land.
Adapter env is inherited from the spawning process rather than passed on argv,
so it never appears in `ps` output.

Name the variant `Supervise` and nothing else: `doc_check::enum_variants`
requires a purely alphabetic variant name and lowercases it, so this becomes the
token `devflow supervise`, which must then appear in a scoped operator document
(see below) or `source_devflow_env_vars_and_subcommands_are_documented` fails.
Do not route around that by adding a `doc-check-allowlist.toml` exception.

**`pipeline_launch::supervise(...)`** — the handler. It builds a
`SupervisorConfig`, calls `monitor::serve`, and then branches on the returned
`SupervisorExit`:
- `NaturalExit(code)` → emit a `stage_launched`-adjacent event recording the
  code, then call `advance(project_root, Some(phase))` **directly, as a Rust
  function call in this same process**. Not `Command::new(binary).arg("advance")`.
  This is D-10 and it is the whole point: there is no forked tail process left
  to orphan.
- `Stopped(source)` → emit `supervisor_stopped` with the source, and
  `supervisor_advance_suppressed`, then return without calling `advance`
  (R-M). Events are append-only and never-silent: an unattended operator must be
  able to see from `events.jsonl` alone that the phase stopped and that advance
  was deliberately not run.

**Pitfall 4 — audit before you wire.** `advance`'s call chain reaches
`transition` → checkout hooks → git operations. As a fresh CLI process it
inherited whatever the operator's shell had; as a function call inside the
long-lived supervisor it inherits whatever the supervisor was spawned with,
which includes `adapter.extra_env()`. Read `advance`'s chain and record in the
SUMMARY which environment variables it depends on and whether the supervisor's
environment supplies them equivalently. If it does not, thread what is needed
explicitly rather than relying on inheritance.

**Documentation, required by the test suite, not optional:**
- `OPERATIONS.md` — add a line for `devflow supervise` in the same register as
  the existing `devflow advance` note: internal, invoked by `devflow start`,
  not run by hand.
- `ARCHITECTURE.md` :230-250 — the monitor section currently describes a
  detached shell process that runs `devflow advance <project> --phase N` as step
  3. Rewrite it to describe the supervisor: a `devflow` process that binds a
  socket, owns the agent in its own process group, answers liveness and
  shutdown on that socket, and calls advance in-process on natural exit. Keep
  the section's existing explanation of *why* the monitor outlives the CLI —
  that reason is unchanged.

Do **not** change `pipeline_launch.rs:126` in this task. `spawn_monitor` remains
the spawn path until plan 23-07 performs the swap.

<!-- planner-discipline-allow: arg("advance") -->
  </action>
  <acceptance_criteria>
    - `crates/devflow-cli/src/main.rs` contains a `Supervise` variant on the `Command` enum, preceded by `#[command(hide = true)]`
    - `rg -in 'devflow supervise' OPERATIONS.md` matches at least once
    - `cargo test -p devflow-core -- doc_check 2>&1 | grep -E '^test result:'` shows `0 failed`
    - `git diff --stat doc-check-allowlist.toml` is empty
    - `rg -n 'arg\("advance"\)|arg\(.advance.\)' crates/devflow-cli/src/` reports no matches — D-10 asserted: no code path re-invokes advance as a subprocess
    - `rg -n 'monitor::spawn_monitor' crates/devflow-cli/src/pipeline_launch.rs` still matches — the swap is plan 23-07's, not this plan's
    - `ARCHITECTURE.md` no longer contains the string `devflow advance <project> --phase N` as a description of the monitor's step 3
    - `cargo test -p devflow -- help_snapshot 2>&1 | grep -E '^test result:'` shows `0 failed` (the variant is hidden, so the snapshot should be unchanged; if it is not, regenerate it and say why in the SUMMARY)
    - `cargo test --workspace 2>&1 | grep -E '^test result:'` shows `0 failed` on every line
  </acceptance_criteria>
  <verify>
    <automated>cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]' &amp;&amp; rg -qi 'devflow supervise' OPERATIONS.md &amp;&amp; ! rg -q 'arg\("advance"\)' crates/devflow-cli/src/ &amp;&amp; test -z "$(git diff --stat doc-check-allowlist.toml)"</automated>
  </verify>
  <done>`devflow` can be the supervisor process, it advances the stage machine by a direct function call on natural exit and provably not at all after a stop, and both operator documents describe the process model that actually exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| socket line protocol → supervisor command dispatch | Untrusted text from any process that can open the socket reaches a command match |
| OS signals → supervisor teardown | SIGTERM/SIGINT from anywhere in the session can initiate a process-group teardown |
| supervisor argv → `ps` output | Anything passed on the command line is visible to every local user |
| supervisor process environment → `advance`'s git operations | The advance tail now inherits the supervisor's environment rather than a fresh shell's |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-25 | Tampering | an unrecognised line on the socket being parsed or executed | high | mitigate | Explicit two-arm match on `ping` and `shutdown` with a catch-all that replies `unknown <input>` and takes no action. Proven by a dedicated test that sends a shell-shaped string and asserts the agent survives untouched (ASVS V5) |
| T-23-26 | Denial of Service | a signal killing the supervisor without cleanup, leaving the agent running unsupervised behind a stale socket | high | mitigate | Claude's-Discretion #1 decided in favour of landing signal handling here: SIGTERM/SIGINT set an atomic flag the poll loop already checks, and take the identical teardown path as a socket `shutdown`. Proven by the signal-parity test |
| T-23-27 | Elevation of Privilege | a stopped phase continuing to advance its own state machine, reaching Ship and merging after the operator stopped it | high | mitigate | R-M enforced by the return type: `SupervisorExit::Stopped` carries no exit code, so the caller has nothing to advance with. The `Stopped` arm of `supervise` emits `supervisor_advance_suppressed` and returns. Proven by the stop-is-not-a-completion test asserting the return value is not `NaturalExit` |
| T-23-28 | Information Disclosure | adapter credentials or tokens appearing in `ps` output via supervisor argv | high | mitigate | Adapter environment is inherited from the spawning process, never passed on argv; only the phase, project, socket path, workdir and agent program/args are command-line arguments. Asserted by reading the `Supervise` variant's field list |
| T-23-29 | Repudiation | a supervisor that stops leaving no trace an unattended operator can find | medium | mitigate | The `Stopped` arm emits both `supervisor_stopped` (with its `StopSource`) and `supervisor_advance_suppressed` before returning, per the project's append-only, never-silent event convention |
| T-23-30 | Spoofing | a second supervisor stealing a socket a live supervisor owns, orphaning the first agent | high | mitigate | `serve` probes before binding and returns `LiveSupervisorOwnsSocket` on `Alive`; only a `Stale` socket may be reclaimed. Proven by the takeover-safety test, which also asserts the first supervisor's agent survives the refusal |
| T-23-31 | Tampering | `advance`'s git operations behaving differently in-process than as a fresh CLI invocation, e.g. signing or hook configuration silently changing | medium | mitigate | Pitfall 4 audit is a required deliverable of Task 3: the environment `advance`'s chain depends on is enumerated in the SUMMARY, and anything not supplied by inheritance is threaded explicitly |

**Supply chain:** not applicable — zero new packages.
</threat_model>

<verification>
1. `cargo test --workspace` — every `test result:` line reports `0 failed`.
2. `cargo test -p devflow-core --test supervisor_e2e` passes on two consecutive
   runs (no leaked agent from the first wedges the second).
3. `cargo clippy --workspace --all-targets -- -D warnings` — clean.
4. `cargo fmt --check` — clean.
5. `rg -n 'process::exit' crates/devflow-core/src/monitor.rs` returns no matches.
6. `rg -n 'arg\("advance"\)' crates/devflow-cli/src/` returns no matches.
7. `rg -in 'devflow supervise' OPERATIONS.md` matches; `cargo test -p devflow-core -- doc_check`
   passes; `git diff --stat doc-check-allowlist.toml` is empty.
8. `rg -n 'monitor::spawn_monitor' crates/devflow-cli/src/pipeline_launch.rs`
   still matches — the swap belongs to plan 23-07.
</verification>

<success_criteria>
- The supervisor is a real Rust process with a tested run loop, not a shell
  script string.
- Advancing after a stop is unexpressible rather than merely discouraged.
- The advance tail is a function call, so Phase 17's orphaned-tail failure mode
  has no process to happen to.
- Signal handling landed rather than deferred, with the decision and its
  reasoning recorded.
- Both operator documents describe the process model the code now has.
</success_criteria>

<output>
Create `.planning/phases/23-end-to-end-dogfood/23-06-SUMMARY.md` when done.
</output>
