---
phase: 23-end-to-end-dogfood
plan: 10
type: execute
wave: 10
depends_on: ["23-09"]
files_modified:
  - crates/devflow-cli/src/main.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-core/src/hooks.rs
  - README.md
  - OPERATIONS.md
autonomous: true
requirements: [23c]

must_haves:
  truths:
    - "`devflow stop --phase N` tears down a running phase's whole process tree and records a definite outcome — the operator no longer needs `ps` and `kill` to abort a run"
    - "A stopped phase never advances its own state machine afterwards (R-M): no `advance_evaluated` event, no stage transition, no Ship"
    - "Stopping an already-stopped or already-dead phase succeeds and changes nothing — it is idempotent, not an error"
    - "A pre-existing `stop_reason` (e.g. from `--until`) is preserved rather than overwritten by a later explicit stop"
    - "`devflow stop` emits a `workflow_stopped` event before returning, so an unattended run's abort is visible in `events.jsonl` alone"
    - "A completed Ship removes its own phase worktree, and a stop archives its phase captures — the two untested-on-success paths this phase is the first to exercise"
    - "`Hook::WorktreeRemove` can actually see and act on what it must remove: `HookContext` carries `worktree_path`, populated at the one production constructor, and `HookError` carries a worktree variant so the handler propagates with `?` like every other hook"
    - "The worktree-removal mechanism is proven by a hermetic `cargo test` against a real temporary worktree, so its correctness does not depend on plan 23-12's single acceptance run succeeding"
    - "Gate resolution during a stop is best-effort: an already-responded gate produces a warning and the stop still succeeds, emits its event and records its state"
  artifacts: []
  key_links:
    - "commands::stop → monitor::request_shutdown (socket happy path) → monitor::reap_stale_group (STALE backstop)"
    - "commands::stop → State.stopped / stop_reason → the same fields `--until` (20c) already sets, and that `cleanup` already respects"
    - "State.worktree_path → pipeline_outcomes.rs:484's HookContext literal → HookContext.worktree_path → Hook::WorktreeRemove → worktree::remove (the full path the hook needs; the middle two links do not exist today)"
    - "worktree::WorktreeError → HookError::Worktree (the `#[from]` impl that makes the handler writable at all)"
    - "hooks::hooks_after_ship → Hook::WorktreeRemove → worktree::remove (an existing primitive, already called this way from cleanup)"
    - "commands::stop → agent_result::archive_phase_files (retention-aware archival, the same primitive relaunch rollover already uses)"
---

<objective>
Give the operator an explicit, clean phase abort — the verb that was missing
when Phase 17 needed one.

`devflow cleanup --force` deliberately refuses to touch a live agent or monitor.
That refusal is correct, and it left a hole: there has been **no** command that
actually stops a running phase. The operator's only recourse was `ps` and
`kill`, which is exactly the manual intervention this phase's goal forbids.

23c falls out cheaply now that the socket handle exists (999.34 / DEN-59): the
happy path is a `shutdown` line on the socket the supervisor is already
listening on, and the STALE path is the identity-validated process-group
backstop plan 23-05 already built and tested.

**R-M is the behavioural property that matters most here.** The spike audit
calls it out as "a genuine behavioural improvement, not just parity": *a stopped
phase must not advance its own state machine.* That was never expressible in the
shell script. It is expressible now — plan 23-06 made `SupervisorExit::Stopped`
carry no exit code, so the supervisor cannot advance after a stop. This plan
adds the operator-facing verb that triggers it, and the regression test that
keeps it true.

This plan also lands **Claude's Discretion item #4** — the `hooks_after_ship`
worktree removal and the per-phase capture sweep.

Purpose: make aborting a run a first-class, auditable operation.
Output: `devflow stop`, an R-M regression test, and the two ship/stop cleanup
paths.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-end-to-end-dogfood/23-CONTEXT.md
@.planning/phases/23-end-to-end-dogfood/23-RESEARCH.md
@.planning/phases/23-end-to-end-dogfood/23-PATTERNS.md
@.planning/audits/2026-07-24-socket-supervisor-spike.md
@.planning/phases/23-end-to-end-dogfood/23-09-SUMMARY.md
</context>

## Recorded decision — Claude's Discretion item #4: `hooks_after_ship` worktree removal and the capture sweep

**DECIDED: land both, each by reusing an existing primitive. No new mechanism.**

CONTEXT.md left open whether `hooks_after_ship` should gain a worktree-removal
step and whether per-phase capture files should be swept, noting DEN-59's
operator note and that both are untested-on-success paths this phase is the
first to exercise. RESEARCH verified both are answerable from primitives that
already exist and are already trusted:

- **`Hook::WorktreeRemove`** — `worktree::remove(project_root, &path, force)`
  already exists and is already called this way from `commands.rs`'s `cleanup`.
  Landing it now matters because plan 23-11's acceptance run is the first run
  ever to *complete* Ship: without it, that run leaves its worktree behind and
  the phase's own success path is the thing that proves it broken.
- **Capture sweep on stop** — `agent_result::archive_phase_files` already
  exists and is already called on relaunch rollover. Reusing it means the stop
  path inherits the project's existing retention semantics rather than
  inventing a second deletion path with different ones. Archival, not deletion:
  a stopped run's captures are exactly the evidence an operator wants after
  aborting.

The alternative — deferring both — would leave the phase's own acceptance run
exercising an untested success path for the first time with nothing watching.

## Artifacts this phase produces

This plan **creates**:
- CLI `Command::Stop` → `devflow stop --phase N`
- `commands::stop(project_root, phase) -> Result<(), CliError>`
- `Hook::WorktreeRemove` (enum variant + its `Hook::run` arm + its handler in
  `hooks.rs`)
- `HookContext.worktree_path: Option<PathBuf>` — a new field on the existing
  struct, populated at `pipeline_outcomes.rs:484` and defaulted to `None` in
  `hooks.rs`'s test helper
- `HookError::Worktree` — a new variant carrying `worktree::WorktreeError` via
  `#[from]`, so the removal handler can propagate with `?`
- event `workflow_stopped`

This plan **removes**: nothing.

<tasks>

<task type="auto">
  <name>Task 1: Add `devflow stop` — socket happy path, validated backstop, recorded outcome</name>
  <files>crates/devflow-cli/src/main.rs, crates/devflow-cli/src/commands.rs, README.md, OPERATIONS.md</files>
  <read_first>
    - crates/devflow-cli/src/main.rs :186-234 — the `Cleanup` and `Doctor` variants. `Cleanup`'s shape (`project: PathBuf` defaulting to `.`, plus a `force` flag) is the closest analog, and `Doctor` is the simplest recent subcommand's full plumbing; read the dispatch arms for both around :494-503
    - crates/devflow-cli/src/commands.rs :380-420 — `cleanup`, specifically its refuse-to-touch-a-live-thing shape and its handling of `state.stopped` requiring `--force`. `devflow stop` is the verb that makes that refusal actionable
    - crates/devflow-cli/src/commands.rs — the `liveness` predicate as plan 23-08 left it, and the recovery-verb helper, so `stop` reads the same answer `status` shows
    - crates/devflow-core/src/monitor.rs — `request_shutdown`, `ShutdownOutcome`, `socket_state`, `reap_stale_group` (plan 23-05) and `SupervisorHandle`
    - crates/devflow-core/src/state.rs — `stopped`, `stop_reason` and `stop_until` with their 20c doc comments, so the new verb sets the same fields `--until` already sets and `cleanup` already respects
    - crates/devflow-core/src/events.rs — the `emit` signature and payload convention
    - crates/devflow-core/src/gates.rs :140 (`Gates::list_open`), :179-195 (`Gates::respond`, whose `AlreadyResponded { phase, stage }` early return at :190 fires when a response file already exists) and :96 (the `GateError::AlreadyResponded` variant). Read the early return before writing step 5 — it is the reason gate resolution in `stop` is best-effort rather than fallible
    - README.md and OPERATIONS.md — how `devflow cleanup` and `devflow doctor` are documented; `devflow stop` must appear in at least one scoped doc or `doc_check::source_devflow_env_vars_and_subcommands_are_documented` fails on the new `Command` variant
  </read_first>
  <action>
Add the `Stop` CLI variant and its handler.

**`main.rs`** — `Stop { phase: u32 (required, `#[arg(long)]`), project: PathBuf
(positional, default `.`) }`. **No `--force` flag**: a stop is always explicit
by construction — the operator typed the verb and named the phase — so there is
nothing for a force flag to override. Dispatch arm:
`Command::Stop { phase, project } => stop(&project_root(project)?, phase)`.

**`commands::stop(project_root, phase)`** — the handler:

1. Load the phase's state. If there is none, print that there is nothing to stop
   and return `Ok(())`. Not an error: stopping a phase that does not exist is
   the same outcome the operator wanted.
2. Branch on `monitor::socket_state(&handle.socket_path)` when
   `state.supervisor` is present:
   - `Alive` → `monitor::request_shutdown(...)`. The supervisor holds the real
     handle: it kills its own agent group, records exit code `143`, removes the
     socket and exits, and its `Stopped` return provably suppresses advance.
   - `Stale` → `monitor::reap_stale_group(&handle, grace)`. That function
     already refuses unless the recorded boot id and process start time both
     validate, and refuses rather than signalling blind when either is
     unavailable. Report how many group members it reaped.
   - `Gone` → nothing to tear down; fall through to the bookkeeping.
   - `Unknown` → report it plainly and do not signal anything.
3. When `state.supervisor` is absent but `state.monitor_pid` is populated, this
   is the pre-supervisor phase plan 23-08's `doctor` finding names. **Say so and
   refuse to signal.** Print the same guidance the finding gives — the monitor
   was started by an older binary and cannot be reached by this one; locate and
   signal it by hand, or let it complete. Silently doing nothing here would be
   the loss-of-control failure the finding exists to prevent.
4. Bookkeeping, on every branch: set `state.stopped = true`; set
   `state.stop_reason` **only if it is currently `None`** — a phase parked by
   `--until` (20c) already carries a reason, and the explicit stop that follows
   must not overwrite the record of why it stopped in the first place. Persist
   with `workflow::save_state`.
5. Resolve any gate the phase left open, so a stopped phase does not sit
   holding a gate no one will answer. Use `Gates::list_open` and `Gates::respond`
   with a non-approving response whose `responded_by` names the stop.
   **This step is best-effort and must never fail the stop
   (review finding — OpenCode, LOW).** `Gates::respond` returns
   `GateError::AlreadyResponded { phase, stage }` (`gates.rs:190`) when a
   response file already exists — which happens whenever the operator answered
   the gate by hand before reaching for `stop`, and again on the second call of
   an idempotent double-stop. There is also a narrow race between
   `list_open`'s read and `respond`'s write. In every one of those cases the
   desired end state — the gate is not left hanging — is already true, so
   treating the error as a stop failure would refuse to stop a phase for a
   reason that is cosmetic. Iterate the open gates, call `respond` for each,
   and on **any** error print a warning naming the gate and continue to the next
   one; never propagate with `?` from this step. Continue on to the bookkeeping,
   the event and the summary regardless. Record in the summary how many gates
   were resolved and how many were skipped, so the best-effort behaviour is
   visible rather than silent.
6. Emit `workflow_stopped` **before returning**, carrying the phase, the socket
   state observed, and which branch was taken. Events are append-only and never
   silent — an unattended abort must be reconstructable from `events.jsonl`
   alone. Emit the event even on the "nothing to stop" branches, so the record
   distinguishes "operator stopped a live phase" from "operator stopped a phase
   that was already gone".
7. Print an operator-facing summary. **Never print the absolute socket path** —
   the file name is enough (999.10 / WR-02).

**Documentation** — add `devflow stop` to `README.md` and `OPERATIONS.md`
alongside `cleanup` and `doctor`. This is required, not cosmetic: every
`Command` enum variant must appear as `devflow <variant>` in a scoped document
or `doc_check::source_devflow_env_vars_and_subcommands_are_documented` fails.
Do not add a `doc-check-allowlist.toml` exception instead. Say explicitly in the
docs that `stop` is the verb for a live phase and `cleanup` is the verb for a
finished one, since the pair is the thing an operator has to choose between.

<!-- planner-discipline-allow: socket_path -->
  </action>
  <acceptance_criteria>
    - `crates/devflow-cli/src/main.rs` contains a `Stop` variant on the `Command` enum with `phase` and `project` fields and no `force` field
    - `rg -in 'devflow stop' README.md OPERATIONS.md` matches in both files
    - `cargo test -p devflow-core -- doc_check 2>&1 | grep -E '^test result:'` shows `0 failed`
    - `git diff --stat doc-check-allowlist.toml` is empty
    - `cargo run -q -p devflow -- stop --help` exits 0 and its output contains `--phase`
    - `cargo test -p devflow -- help_snapshot 2>&1 | grep -E '^test result:'` shows `0 failed` — the snapshot is regenerated to include the new verb
    - `rg -n 'socket_path' crates/devflow-cli/src/commands.rs | rg 'println!'` reports no matches
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo run -q -p devflow -- stop --help | grep -q -- '--phase' &amp;&amp; rg -qi 'devflow stop' README.md &amp;&amp; rg -qi 'devflow stop' OPERATIONS.md &amp;&amp; cargo test -p devflow -- help_snapshot 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <done>An operator can abort a running phase with one command that tears down the tree, records the outcome, resolves the open gate and emits an event — with no `ps` and no `kill`.</done>
</task>

<task type="auto">
  <name>Task 2: Prove a stop is not a completion, is idempotent, and preserves an existing reason</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-cli/src/commands.rs — the `stop` handler Task 1 just wrote, and the `#[cfg(test)] mod tests` block starting at :2292
    - crates/devflow-core/tests/supervisor_e2e.rs — the fake-agent + supervisor-thread harness plan 23-06 built. Its stop case is the mechanism proof; these tests are the **command** proof, driving `commands::stop` rather than `monitor::serve`
    - crates/devflow-core/src/state.rs — `stopped`, `stop_reason`, `stop_until` and the 20c semantics
    - crates/devflow-core/src/events.rs and `.devflow/events.jsonl`'s line format, so a test can assert on the absence of an `advance_evaluated` event
    - crates/devflow-core/src/test_support.rs :69-84 — `hermetic_command` / `git_command`; any git in a fixture goes through these (999.37)
    - .planning/phases/23-end-to-end-dogfood/23-VALIDATION.md — the 23c row: R-M advance suppression, idempotency on an already-stopped or dead phase, and `stop_reason` preservation
  </read_first>
  <action>
Write the behavioural tests for `devflow stop`. These are the command-level
counterparts to plan 23-06's mechanism-level supervisor tests.

- **`stop_suppresses_advance` (R-M — the load-bearing one).** Launch a
  supervisor over a fake agent that sleeps, call `commands::stop`, wait for the
  supervisor to exit, then assert on `.devflow/events.jsonl`: a
  `workflow_stopped` event is present and **no `advance_evaluated` event was
  appended after it**. Also assert the phase's stage did not change. RESEARCH
  names this as "the one behavioral property that most needs a dedicated
  regression test" — assert on the absence of the advance, not merely on the
  presence of the stop, because it is the absence that is the property.
- **`stop_records_exit_code_rather_than_silence`** — assert the exit-capture
  file contains `143` after the stop (R-K: teardown still records a definite
  outcome).
- **`stop_is_idempotent_on_an_already_stopped_phase`** — call `stop` twice;
  assert the second returns `Ok`, changes no state field, and does not signal
  anything.
- **`stop_is_idempotent_on_a_dead_phase`** — kill the supervisor without
  cleanup (leaving a stale socket), then `stop`; assert `Ok`, `stopped == true`,
  and that the stale socket file is gone afterwards.
- **`stop_preserves_a_pre_existing_stop_reason`** — set `stop_reason` to a
  known `--until`-shaped value, call `stop`, assert the original value is
  unchanged.
- **`stop_sets_stop_reason_when_none_was_recorded`** — the complementary case.
- **`stop_resolves_an_open_gate`** — write a gate, `stop`, assert the gate is
  resolved with a non-approving response whose `responded_by` names the stop.
- **`stop_succeeds_when_a_gate_was_already_responded`** — write a gate,
  `Gates::respond` to it by hand first (reproducing an operator who answered
  before reaching for `stop`), then call `stop`. Assert `stop` returns `Ok`,
  that `state.stopped` is `true`, and that the `workflow_stopped` event was
  still emitted. This is the regression guard for the best-effort rule: pre-fix,
  `AlreadyResponded` would have turned a legitimate stop into an error
  (review finding — OpenCode).
- **`stop_refuses_to_signal_a_pre_supervisor_phase`** — construct a state with
  `monitor_pid: Some(_)` and `supervisor: None`; assert `stop` returns `Ok`,
  prints the guidance, and signals nothing.
- **`stop_on_a_phase_with_no_state_is_not_an_error`**.

Bound every wait with an explicit assertion message; reap every spawned
supervisor and agent, mirroring `devflow_dir_gitignore.rs`'s
`wait_for_pid_to_die`. A leaked sleeping agent would make the suite flaky for
plan 23-11's acceptance run.
  </action>
  <acceptance_criteria>
    - `cargo test -p devflow -- stop 2>&1 | grep -E '^test result:'` shows at least `10 passed` and `0 failed`
    - `cargo test -p devflow -- stop_suppresses_advance 2>&1` reports `1 passed`
    - `cargo test -p devflow -- stop_preserves_a_pre_existing_stop_reason 2>&1` reports `1 passed`
    - `cargo test -p devflow -- stop_succeeds_when_a_gate_was_already_responded 2>&1` reports `1 passed`. **Assert on the `1 passed` text, not the exit code** — a filter matching nothing still exits 0, and the package is `devflow`, not `devflow-cli`
    - `rg -n 'Gates::respond' -B 3 -A 6 crates/devflow-cli/src/commands.rs` shows the call inside `stop` handled with a warning-and-continue, not a `?` — grep the `stop` function's span for `Gates::respond(.*)?;` and expect no match
    - `cargo test -p devflow -- stop --list` includes a test whose name contains `idempotent`
    - `rg -n 'Command::new\("git"' crates/devflow-cli/src/commands.rs` reports no matches within the new tests
    - `rg -n 'std::env::set_var' crates/devflow-cli/src/commands.rs` reports no matches
    - Running `cargo test --workspace` twice back to back reports `0 failed` both times
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p devflow -- stop 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; cargo test -p devflow -- stop_suppresses_advance 2>&amp;1 | grep -qE '1 passed' &amp;&amp; cargo test -p devflow -- stop_succeeds_when_a_gate_was_already_responded 2>&amp;1 | grep -qE '1 passed' &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]' &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]'</automated>
  </verify>
  <done>A stop is proven not to be a completion by asserting on the absence of an advance, and the verb is proven idempotent and non-destructive of an existing stop reason.</done>
</task>

<task type="auto">
  <name>Task 3: Add the after-ship worktree removal and the stop-time capture archival</name>
  <files>crates/devflow-core/src/hooks.rs, crates/devflow-cli/src/pipeline_outcomes.rs, crates/devflow-cli/src/commands.rs</files>
  <read_first>
    - crates/devflow-core/src/hooks.rs :16-64 — the `Hook` enum (six variants, `#[derive(Debug, Clone, Copy, PartialEq, Eq)]`), `HookContext` at :33-50 (fields `phase`, `project_root`, `stage`, `git_flow`, `shipped_version` — and **no `worktree_path`**), and `HookError` at :52-64 (variants `Git`, `Version`, `Io` only — **no worktree variant**). These two absences are the finding; read them before writing anything
    - crates/devflow-core/src/hooks.rs :66-78 — `Hook::run`'s `match` over every variant, which the new variant must be added to
    - crates/devflow-core/src/hooks.rs :97-140 — `hooks_after_ship()`'s current four-hook vector and the `BranchCleanup` handler immediately after, whose tolerance of an already-deleted branch (around :127-135) is the exact shape a missing worktree must be treated with
    - crates/devflow-core/src/worktree.rs :10-22 — `WorktreeError`'s three variants (`Io`, `Command`, `Exists`), which has **no `From` impl into `HookError`**, so a handler cannot use `?` against it today
    - crates/devflow-core/src/worktree.rs :104-112 — `pub fn remove(project_root: &Path, path: &Path, force: bool) -> Result<(), WorktreeError>`, and its existing call sites in `commands.rs` (cleanup, ~:278) so the invocation matches proven usage
    - crates/devflow-cli/src/pipeline_outcomes.rs :476-495 — the **only production `HookContext` constructor**, a struct literal at :484 with all five fields, built from `state` and `hook_context_root`. Every field added to the struct must be populated here
    - crates/devflow-core/src/hooks.rs :318-330 — the test-module `fn ctx(root: &Path, stage: Stage) -> HookContext` helper, the **only other** constructor. Adding a field without updating it breaks every existing hook test
    - crates/devflow-core/src/state.rs — `worktree_path: Option<PathBuf>`, its doc comment, and what `None` means (the `--no-worktree` case, where the agent ran in the primary checkout)
    - crates/devflow-core/src/agent_result.rs :1025-1040 — `archive_phase_files(project_root, evidence_root, phase, retain)` and its retention semantics, plus its existing relaunch-rollover call site in `pipeline_launch.rs`
    - crates/devflow-core/src/test_support.rs :69-84 — `hermetic_command` / `git_command`, needed by the hermetic worktree test below (999.37)
  </read_first>
  <action>
Land the two cleanup paths this phase is the first to exercise.

**First, give the hook layer what it needs to see and to fail with
(review finding — Codex + Hermes, HIGH). Without these two changes the handler
below cannot be written at all.**

- **`HookContext.worktree_path: Option<PathBuf>`** — add the field, with a doc
  comment stating that `None` means the agent ran in the primary checkout
  (`--no-worktree`) and that `WorktreeRemove` is therefore a no-op, not a
  failure. Then update **both** constructors, which are the only two in the
  workspace:
  - `pipeline_outcomes.rs:484`'s struct literal — populate it from
    `state.worktree_path.clone()`. Note that the context is built from
    `hook_context_root(project_root, state, terminal_batch)` for
    `project_root`, so read that helper before deciding what `worktree_path`
    should be relative to; the removal is executed *by* git from
    `ctx.project_root`, so the path must be the one `git worktree remove`
    accepts from that directory.
  - `hooks.rs:321`'s test helper `ctx(root, stage)` — default it to `None` and
    add a second helper (or a mutation in the individual test) for the tests
    that need a populated path, so existing hook tests keep compiling unchanged
    in intent.
- **`HookError::Worktree`** — add a variant carrying `worktree::WorktreeError`,
  with `#[from]` so the handler can propagate with `?` like every other hook
  does. Match the file's existing `thiserror` style: `Git` and `Version` use
  `#[error(transparent)]`, `Io` uses a prefixed message. Choose one and say why
  in the SUMMARY — a prefixed message (`"hook worktree operation failed: {0}"`)
  is preferable here because `WorktreeError::Command` carries raw git stderr and
  a bare transparent forward would surface it with no indication that a hook was
  the caller.

**`Hook::WorktreeRemove`** — add the enum variant, add its arm to `Hook::run`'s
match, and append it to `hooks_after_ship()`'s vector, **after `BranchCleanup`**.
Ordering matters: the branch must be cleaned up while the worktree still exists,
because git refuses to delete a branch that a live worktree has checked out.

Its handler reads `ctx.worktree_path`, calls
`worktree::remove(&ctx.project_root, &path, true)`, and must be **tolerant of
the worktree already being gone** — exactly as `BranchCleanup` tolerates an
already-deleted branch. A missing worktree at this point means an operator
removed it by hand, or a previous partial run already did; that is not a Ship
failure and must not turn a completed Ship into a failed one. Tolerance means
inspecting the error and returning `Ok(())` for the already-gone case
specifically, **not** discarding every error — a genuine git failure must still
propagate as `HookError::Worktree`.

If `worktree_path` is `None` the hook is a no-op. It must never fall back to
`ctx.project_root`, and it must never attempt to remove anything derived from
it.

**Capture archival on stop** — in `commands::stop`, after the bookkeeping and
before the summary, call
`agent_result::archive_phase_files(project_root, evidence_root, phase, retain)`
with the same evidence root and retention the relaunch-rollover call site uses.
This is **archival, not deletion**: a stopped run's captures are precisely the
evidence an operator wants afterwards, and reusing this primitive means the stop
path inherits the project's existing retention semantics rather than inventing a
second one. Emit the same `capture_archived` event `pipeline_launch.rs` already
emits, so the two paths are indistinguishable to anything reading the event log.

**Tests:**
- `hooks_after_ship_includes_worktree_remove_after_branch_cleanup` — assert both
  the presence and the relative order
- `worktree_remove_hook_tolerates_an_already_missing_worktree` — the completed
  Ship must not fail
- `worktree_remove_hook_is_a_noop_when_no_worktree_path_is_recorded` — and
  assert the project root still exists afterwards
- **`worktree_remove_hook_removes_a_real_worktree`** — build a real repository
  and a real linked worktree with `test_support::git_command`, populate a
  `HookContext` whose `worktree_path` names it, run
  `Hook::WorktreeRemove.run(&mut ctx)`, assert the handler returned `Ok(())`,
  assert the worktree directory no longer exists, and assert
  `git worktree list --porcelain` no longer lists it (a directory can be gone
  while git's administrative entry survives, and only the second assertion
  catches that). **This test is load-bearing beyond its own scope
  (review finding — Hermes, MEDIUM):** it is what decouples the "does the
  worktree-removal mechanism work" proof from plan 23-12's single acceptance
  run. Without it, the only evidence that this hook works would be a real
  completed Ship, so an unrelated failure in that run — a Claude rate limit
  will do — would leave the mechanism unproven and would cost another
  irreversible merge to re-test. Build the worktree as a **sibling** directory
  of the repo, never nested inside it, following `18-06`'s recorded fixture
  lesson: a nested worktree path contains the project root's path as a string
  prefix, which makes "contains the worktree path" and "does not contain the
  project root path" mutually unsatisfiable
- `worktree_remove_hook_propagates_a_genuine_git_failure` — the complement to
  the tolerance test: assert that an error which is *not* the already-gone case
  surfaces as `HookError::Worktree` rather than being swallowed
- `hook_context_carries_worktree_path_from_state` — in
  `pipeline_outcomes.rs`'s tests, assert the constructed context's
  `worktree_path` matches the state's, so the field cannot be added to the
  struct and left unpopulated at the one production call site
- `stop_archives_phase_captures` — assert the capture files moved into the
  history location and that a `capture_archived` event was emitted
  </action>
  <acceptance_criteria>
    - `rg -n 'worktree_path' crates/devflow-core/src/hooks.rs` matches the `HookContext` field declaration, and `rg -n 'worktree_path' crates/devflow-cli/src/pipeline_outcomes.rs` matches within the `HookContext` struct literal — the field exists **and** is populated at the only production constructor
    - `rg -n 'enum HookError' -A 20 crates/devflow-core/src/hooks.rs` shows a worktree variant carrying `WorktreeError` with `#[from]`
    - `rg -n 'WorktreeRemove' crates/devflow-core/src/hooks.rs` matches the enum variant, the `Hook::run` match arm, the `hooks_after_ship` vector entry and the handler
    - `rg -n 'fn hooks_after_ship' -A 10 crates/devflow-core/src/hooks.rs` shows `WorktreeRemove` listed after `BranchCleanup`
    - `cargo test -p devflow-core -- hooks 2>&1 | grep -E '^test result:'` shows at least `5 passed` more than before this task and `0 failed`
    - `cargo test -p devflow-core -- worktree_remove_hook 2>&1 | grep -E '^test result:'` shows at least `4 passed` and `0 failed`
    - `cargo test -p devflow-core -- hooks::tests::worktree_remove_hook_removes_a_real_worktree 2>&1` reports `1 passed` in its output. **Assert on the `1 passed` text, not the exit code** — a name filter that matches nothing still exits 0, and this is the test that decouples the mechanism proof from plan 23-12's run, so a silent no-match here would defeat the whole point
    - `cargo test -p devflow -- hook_context_carries_worktree_path_from_state 2>&1` reports `1 passed`
    - `cargo test -p devflow -- stop_archives_phase_captures 2>&1` reports `1 passed`
    - `rg -n 'Command::new\("git"' crates/devflow-core/src/hooks.rs` reports no matches
    - `cargo test --workspace 2>&1 | grep -E '^test result:'` shows `0 failed` on every line
    - `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` both exit 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p devflow-core -- hooks 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; cargo test -p devflow-core -- hooks::tests::worktree_remove_hook_removes_a_real_worktree 2>&amp;1 | grep -qE '1 passed' &amp;&amp; cargo test -p devflow -- hook_context_carries_worktree_path_from_state 2>&amp;1 | grep -qE '1 passed' &amp;&amp; cargo test -p devflow -- stop_archives_phase_captures 2>&amp;1 | grep -qE '1 passed' &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]'</automated>
  </verify>
  <done>The hook layer can see the worktree it must remove and can fail with a worktree error; a completed Ship removes its own worktree without being able to fail the Ship; the removal mechanism is proven hermetically under `cargo test` rather than only by plan 23-12's single run; and a stop archives its captures through the same retention-aware primitive relaunch rollover already uses.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `devflow stop` → a running process tree | The verb's whole purpose is to signal processes; a mistargeted signal kills the wrong thing |
| `devflow stop` → the phase's state machine | A stop that leaves advance reachable would let a phase the operator aborted go on to merge |
| `Hook::WorktreeRemove` → the filesystem | A worktree removal with a wrong or empty path could delete the project root |
| stop-time archival → captured agent output | Captures contain raw agent stdout, which has previously carried absolute paths and usernames |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-50 | Elevation of Privilege | a stopped phase advancing to Ship and merging after the operator aborted it | high | mitigate | R-M, enforced structurally by plan 23-06's `SupervisorExit::Stopped` carrying no exit code, and guarded here by `stop_suppresses_advance`, which asserts on the **absence** of an `advance_evaluated` event rather than on the presence of the stop |
| T-23-51 | Spoofing | the process-group backstop signalling a recycled PID's group | high | mitigate | The `Stale` branch delegates to `monitor::reap_stale_group`, which plan 23-05 built to refuse unless the recorded boot id and start time both validate, and to refuse rather than signal blind when either is unavailable. `stop` adds no second signalling path |
| T-23-52 | Denial of Service | another local user connecting to the socket and stopping the operator's phase | high | mitigate | The control is the socket's mode `0600` and its `0700` parent directory (plan 23-05). `stop` deliberately introduces no alternative reach — anyone who can already connect could already stop the phase, which is the accepted property R-L bounds |
| T-23-53 | Tampering | `Hook::WorktreeRemove` deleting the project root when no worktree was recorded | high | mitigate | The path is carried explicitly as `HookContext.worktree_path: Option<PathBuf>` (a field added by this plan, populated only from `state.worktree_path` at `pipeline_outcomes.rs:484`) rather than derived at the hook, and the handler is an explicit no-op on `None` with **no fallback to `ctx.project_root`**. A dedicated test asserts the project root still exists afterwards |
| T-23-54 | Denial of Service | a missing worktree turning a successfully completed Ship into a failed one | high | mitigate | The handler tolerates the already-gone case specifically — inspecting the error rather than discarding all errors, so a genuine git failure still surfaces as the new `HookError::Worktree` — following `BranchCleanup`'s existing already-deleted-branch precedent. Two tests: the tolerance case and `worktree_remove_hook_propagates_a_genuine_git_failure`. The mechanism itself is additionally proven hermetically by `worktree_remove_hook_removes_a_real_worktree`, which **decouples this proof from plan 23-12's single acceptance run** (review finding — Hermes): a rate-limit failure there must not leave the hook unproven, since 23-12 is the first run ever to complete Ship |
| T-23-55 | Repudiation | an unattended abort leaving no trace | medium | mitigate | `workflow_stopped` is emitted on every branch — including the nothing-to-stop branches — before returning, so `events.jsonl` alone distinguishes stopping a live phase from stopping one already gone |
| T-23-56 | Information Disclosure | archived captures carrying absolute paths and usernames into a retained location | medium | accept | Archival reuses `archive_phase_files` and inherits its existing location and retention, which are already `.gitignore`-covered by the seven-constructor guarantee. The captures' own content is a pre-existing condition tracked in 999.10, not introduced here |
| T-23-57 | Denial of Service | silently doing nothing when asked to stop a pre-supervisor phase | high | mitigate | The handler explicitly detects `monitor_pid: Some(_) && supervisor: None`, refuses to signal, and prints the same guidance plan 23-08's `doctor` finding gives. Guarded by `stop_refuses_to_signal_a_pre_supervisor_phase` |
| T-23-72 | Denial of Service | `devflow stop` refusing to stop a live phase because a gate it tried to resolve was already answered — `Gates::respond` returns `AlreadyResponded` (`gates.rs:190`) whenever a response file exists, which is the normal state after an operator answers a gate by hand or on the second call of an idempotent double-stop | medium | mitigate | Gate resolution is best-effort by construction: `stop` iterates `Gates::list_open`, calls `respond` per gate, and on any error warns and continues rather than propagating with `?`; the bookkeeping, the `workflow_stopped` event and the summary all run regardless, and the summary reports resolved-versus-skipped counts so the behaviour is visible. Guarded by `stop_succeeds_when_a_gate_was_already_responded` and by a grep criterion that no `Gates::respond(…)?` appears in `stop`'s span (review finding — OpenCode) |

**Supply chain:** not applicable — zero new packages.
</threat_model>

<verification>
1. `cargo test --workspace` — every `test result:` line reports `0 failed`, on
   two consecutive runs.
2. `cargo clippy --workspace --all-targets -- -D warnings` — clean.
3. `cargo fmt --check` — clean.
4. `cargo run -q -p devflow -- stop --help` exits 0 and mentions `--phase`.
5. `rg -in 'devflow stop' README.md OPERATIONS.md` matches in both;
   `cargo test -p devflow-core -- doc_check` passes;
   `git diff --stat doc-check-allowlist.toml` is empty.
6. `stop_suppresses_advance`, `stop_preserves_a_pre_existing_stop_reason` and
   `stop_succeeds_when_a_gate_was_already_responded` all pass.
7. `hooks_after_ship()` lists `WorktreeRemove` after `BranchCleanup`;
   `HookContext` declares `worktree_path` and `pipeline_outcomes.rs` populates
   it; `HookError` carries a worktree variant; and
   `worktree_remove_hook_removes_a_real_worktree` reports `1 passed` when run by
   exact name (checking the output text, not the exit code).
8. `rg -n 'socket_path' crates/devflow-cli/src/commands.rs | rg 'println!'`
   returns no matches.
</verification>

<success_criteria>
- The operator can abort a live phase with a single command instead of `ps` and
  `kill` — the gap `cleanup --force`'s correct refusal left open.
- A stop provably does not advance, proven by asserting on an absent event.
- The verb is idempotent and never overwrites the record of why a phase stopped,
  and a gate someone already answered cannot turn a legitimate stop into an
  error.
- The two untested-on-success cleanup paths land before the run that would be
  first to exercise them — and the worktree-removal half is proven hermetically
  under `cargo test`, so its correctness does not ride on that run succeeding.
- The hook layer is given what it needs to do the job: a `worktree_path` on the
  context and a worktree variant on the error, rather than a handler that cannot
  compile against the types that exist.
</success_criteria>

<output>
Create `.planning/phases/23-end-to-end-dogfood/23-10-SUMMARY.md` when done.
</output>
