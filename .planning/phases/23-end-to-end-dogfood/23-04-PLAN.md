---
phase: 23-end-to-end-dogfood
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-core/src/ship.rs
  - crates/devflow-core/src/agent.rs
  - crates/devflow-core/src/git.rs
  - crates/devflow-core/src/monitor.rs
  - crates/devflow-core/tests/devflow_dir_gitignore.rs
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/tests/phase7_cli.rs
  - .gitignore
autonomous: true
requirements: [23d]

must_haves:
  truths:
    - "No code path anywhere in the workspace can construct a resume-cron command string naming a CLI verb that no longer exists"
    - "The per-phase two-agent slot record type, its path constructor and its read/write/remove trio are gone from devflow-core's public API"
    - "`.devflow/` path coverage stays complete — the seven-constructor gitignore regression test still enumerates seven live constructors, none of them narrowed away"
    - "The single-agent resume-cron path (`build_single_agent_cron_instructions` → `pipeline_outcomes.rs:101`) is untouched and still tested"
    - "A workspace-wide, case-insensitive search for the deleted verb across `crates/`, the scoped operator docs and `.gitignore` returns nothing"
  artifacts: []
  key_links:
    - "ship::build_single_agent_cron_instructions → pipeline_outcomes.rs:101 (the surviving rate-limit resume path)"
    - "agent_result::archive_phase_files → constructor #4 of the seven-constructor gitignore test (must keep working; plan 23-10 reuses it for the capture sweep)"
---

<objective>
Finish the subtractive unit by removing the two-agent handoff's remaining
surface from `devflow-core`, and reduce the workspace-wide reference count from
142 to zero.

Plan 23-03 removed the CLI verb, its dispatch and its eight exclusively-owned
CLI helpers. What survives at the start of this plan is core-side: a per-phase
slot record type and its accessors in `agent_result.rs` (48 references — the
single largest file in the inventory), a two-agent resume-cron builder in
`ship.rs` (8), and doc-comment references in `agent.rs`, `git.rs`,
`monitor.rs` and one integration test.

**Deliberate scope boundary — read this before touching `monitor.rs`.**
`monitor::spawn_monitor_no_advance` and `monitor::wait_for_agent_exit` lost
their last callers in plan 23-03, but they are **not deleted here.** They are
monitor API, and `crates/devflow-core/tests/devflow_dir_gitignore.rs` still
uses `spawn_monitor_no_advance` as constructor #3 of its seven-constructor
`.devflow/` coverage guarantee — specifically because it is the variant that
does *not* fork a `devflow advance` tail into a temp directory. Repointing that
test onto today's `spawn_monitor` would make it spawn a real advance subprocess
against a throwaway root. Both functions and that test are re-pointed together
in plan 23-07, where `monitor.rs` is rewritten and a no-advance test
configuration exists again. In this plan `monitor.rs` gets **doc-comment edits
only**.

Purpose: complete 23d so that 23b's rewrite starts against the smallest
possible surface.
Output: a `devflow-core` free of the deleted capability, with every surviving
consumer proven still wired.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/23-end-to-end-dogfood/23-CONTEXT.md
@.planning/phases/23-end-to-end-dogfood/23-RESEARCH.md
@.planning/phases/23-end-to-end-dogfood/23-PATTERNS.md
@.planning/phases/23-end-to-end-dogfood/23-03-SUMMARY.md
</context>

## Artifacts this phase produces

This plan **removes** (core crate):
- `agent_result::SequentagentSlotKind` (enum + its `as_str` impl) <!-- planner-discipline-allow: SequentagentSlotKind -->
- `agent_result::SequentagentSlot` (struct) <!-- planner-discipline-allow: SequentagentSlot -->
- `agent_result::sequentagent_slot_path` <!-- planner-discipline-allow: sequentagent_slot_path -->
- `agent_result::write_sequentagent_slot` <!-- planner-discipline-allow: write_sequentagent_slot -->
- `agent_result::read_sequentagent_slot` <!-- planner-discipline-allow: read_sequentagent_slot -->
- the best-effort slot-record removal helper alongside them
- `ship::build_cron_instructions` (the two-agent variant)

This plan **preserves explicitly**:
- `ship::build_single_agent_cron_instructions`, `ship::write_cron_instructions`,
  `ship::load_cron_instructions`, `ship::list_cron_instructions`,
  `ship::delete_cron_instructions`, `ship::cron_schedule_from_retry_after`,
  `ship::HermesCronJob`, `ship::CronInstructions`
- `agent_result::archive_phase_files` and the six other `.devflow/` constructors

This plan **creates**: no new symbols.

**Deferred to plan 23-07 by design:** `monitor::spawn_monitor_no_advance` and
`monitor::wait_for_agent_exit` (see the objective's scope boundary).

<tasks>

<task type="auto">
  <name>Task 1: Remove the two-agent slot record from agent_result.rs and clean the doc-comment references in agent.rs and git.rs</name>
  <files>crates/devflow-core/src/agent_result.rs, crates/devflow-core/src/agent.rs, crates/devflow-core/src/git.rs</files>
  <read_first>
    - crates/devflow-core/src/agent_result.rs — the slot-record region (roughly :890-1000: the slot-kind enum at ~:903, the slot struct at ~:930, the path constructor at ~:941, the writer at ~:954, the reader at ~:971, and the best-effort remover just after), plus the `#[cfg(test)]` module starting at :1208 and every test in it that exercises those items
    - crates/devflow-core/src/agent_result.rs :310-330 and :490-500 — two doc comments that explain *classification* rules by reference to the deleted verb's handoff; these explain live behaviour and must be rewritten, not deleted
    - crates/devflow-core/src/agent.rs :1-15 — the module doc comment
    - crates/devflow-core/src/git.rs around :1403 — a test function whose *name* embeds the deleted verb
    - crates/devflow-core/src/agent_result.rs around :980-990 — the doc comment referencing the CLI-side slot guard that plan 23-03 already deleted
  </read_first>
  <action>
Delete the per-phase two-agent slot record surface from `agent_result.rs`: the
slot-kind enum and its `as_str` impl, the slot struct, the slot path
constructor (which builds `.devflow/phase-NN-<verb>`), the writer, the reader,
and the best-effort remover. Delete every `#[test]` in the file's test module
that exercises only those items.

Two doc comments in the same file explain **live** classification behaviour by
contrasting it with the deleted verb's handoff — one around :316 about which
result source wins, one around :495 about why `is_error: true` results are not
classified `Failed`. Those rules still apply to the surviving single-agent
path. Rewrite each sentence so the rule is stated on its own terms, without
naming a command that no longer exists. Do not delete the doc comments; the
behaviour they document is unchanged and is the reason those code paths look
the way they do.

`agent.rs`: the module doc comment's opening paragraph explains that execution
moved behind monitor ownership, using the deleted verb as its example. Rewrite
that sentence to state the same fact about monitor-owned execution generally.

`git.rs`: one test function name embeds the deleted verb. That test exercises
the integrate-and-rebase git helpers, which survive (they are used by
`parallel`'s worktree flow). Rename the test to describe what it actually
asserts — the integrate/rebase helpers producing a clean result — and leave its
body unchanged. If, on reading it, the test turns out to exercise only the
deleted verb's specific two-branch handoff and nothing that survives, delete it
instead and say so in the SUMMARY.

Do not touch `ship.rs`, `monitor.rs` or any test outside `agent_result.rs` and
`git.rs` in this task.
  </action>
  <acceptance_criteria>
    - `rg -c -i 'sequentagent' crates/devflow-core/src/agent_result.rs crates/devflow-core/src/agent.rs crates/devflow-core/src/git.rs` reports no matches (exit code 1)
    - `rg -n 'pub fn archive_phase_files' crates/devflow-core/src/agent_result.rs` still matches
    - `cargo build --workspace` exits 0
    - `cargo test -p devflow-core -- agent_result 2>&1 | grep -E '^test result:'` shows a non-zero `passed` count and `0 failed`
    - `cargo test -p devflow-core -- git:: 2>&1 | grep -E '^test result:'` shows a non-zero `passed` count and `0 failed`
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p devflow-core -- agent_result 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings</automated>
  </verify>
  <done>The slot record type, its path constructor and its accessor trio are gone; the two live classification doc comments still explain their rules without naming a deleted command; the workspace builds clean.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the two-agent resume-cron builder and repoint its remaining test callers</name>
  <files>crates/devflow-core/src/ship.rs, crates/devflow-cli/src/commands.rs, crates/devflow-cli/tests/phase7_cli.rs, crates/devflow-core/tests/devflow_dir_gitignore.rs</files>
  <read_first>
    - crates/devflow-core/src/ship.rs :150-235 — `build_cron_instructions` (the two-agent variant, ~:155) and `build_single_agent_cron_instructions` (~:196) side by side, so the difference in their signatures and the shape of the `HermesCronJob` each produces is explicit before either is touched
    - crates/devflow-core/src/ship.rs :590-635 — the tests asserting the two-agent command string and the negative test asserting the single-agent variant does not contain it
    - crates/devflow-cli/src/commands.rs :2440-2495 — three `build_cron_instructions` call sites, all inside the `#[cfg(test)] mod tests` block that starts at :2292
    - crates/devflow-cli/tests/phase7_cli.rs around :760 — a `build_cron_instructions` call site
    - crates/devflow-core/tests/devflow_dir_gitignore.rs around :155-170 — constructor #6 of the seven-constructor coverage test uses `build_cron_instructions`; this one is load-bearing for `.devflow/` path coverage and must be repointed, never deleted
    - crates/devflow-cli/src/pipeline_outcomes.rs :95-120 — the surviving production caller, `build_single_agent_cron_instructions`, so the repoint target's real usage shape is in view
  </read_first>
  <action>
Delete `ship::build_cron_instructions` — the two-agent variant whose only
purpose is to emit a `HermesCronJob` command string invoking a CLI verb that no
longer exists. Its last production caller (`parallel::write_rate_limit_cron`)
was deleted in plan 23-03, so every remaining caller is a test.

This is a correctness removal, not just surface reduction: a surviving builder
would let DevFlow write a scheduled resume job that invokes a nonexistent
command, failing silently at fire time with no operator signal.

Repoint each remaining caller onto `build_single_agent_cron_instructions`,
which takes `(project_root, phase, retry_after)` and produces the same
`cron_instructions_path` output:

- `crates/devflow-core/tests/devflow_dir_gitignore.rs` — **must be repointed,
  not deleted.** It is one of the seven `.devflow/`-constructing entry points
  the file's coverage guarantee enumerates; deleting the case would silently
  narrow that guarantee from seven to six. Update the case's inline comment to
  name the new function.
- `crates/devflow-cli/src/commands.rs` (three test-only sites) and
  `crates/devflow-cli/tests/phase7_cli.rs` — repoint if the assertion still
  makes sense against a single-agent manifest; delete the case if its entire
  point was the two-agent command string. Say which you did for each, in the
  SUMMARY.
- `crates/devflow-core/src/ship.rs`'s own tests — delete the test asserting the
  two-agent command string. **Keep** the negative test asserting the
  single-agent manifest does not contain the deleted verb name only if it still
  proves something after the verb is gone; if it now asserts the absence of a
  string that can no longer be produced by anything, delete it rather than
  leaving a tautology.

Leave `build_single_agent_cron_instructions`, `write_cron_instructions`,
`load_cron_instructions`, `list_cron_instructions`, `delete_cron_instructions`,
`cron_schedule_from_retry_after`, `HermesCronJob` and `CronInstructions`
untouched — the single-agent rate-limit resume path is live and is exercised by
`pipeline_outcomes.rs:101`.

<!-- planner-discipline-allow: build_cron_instructions -->
  </action>
  <acceptance_criteria>
    - `rg -c 'build_cron_instructions' crates/` matches only `build_single_agent_cron_instructions` — i.e. `rg -c '\bbuild_cron_instructions\b' crates/` reports no matches (exit code 1)
    - `rg -n 'build_single_agent_cron_instructions' crates/devflow-cli/src/pipeline_outcomes.rs` still matches
    - `crates/devflow-core/tests/devflow_dir_gitignore.rs` still contains exactly seven numbered constructor cases, and `cargo test -p devflow-core --test devflow_dir_gitignore 2>&1 | grep -E '^test result:'` shows a non-zero `passed` count and `0 failed`
    - `cargo test -p devflow-core -- ship:: 2>&1 | grep -E '^test result:'` shows a non-zero `passed` count and `0 failed`
    - `cargo test --workspace 2>&1 | grep -E '^test result:'` shows `0 failed` on every line
  </acceptance_criteria>
  <verify>
    <automated>! rg -q '\bbuild_cron_instructions\b' crates/ &amp;&amp; cargo test -p devflow-core --test devflow_dir_gitignore 2>&amp;1 | grep -E '^test result:' | grep -q 'failed: 0' &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]'</automated>
  </verify>
  <done>No workspace code path can build a resume-cron command naming the deleted verb; the seven-constructor `.devflow/` coverage guarantee is still seven; the single-agent resume path is untouched and green.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the last references and prove the workspace-wide count is zero</name>
  <files>crates/devflow-core/src/monitor.rs, crates/devflow-core/tests/devflow_dir_gitignore.rs, .gitignore</files>
  <read_first>
    - crates/devflow-core/src/monitor.rs :53-66 — the doc comment on `spawn_monitor_no_advance`, which explains the function by naming the deleted verb's synchronous handoff, and :200-208 — the same for `wait_for_agent_exit`
    - crates/devflow-core/tests/devflow_dir_gitignore.rs :50-62 and :109-135 — comments naming the deleted verb around the `spawn_monitor_no_advance` constructor case
    - .gitignore around :39-41 — the comment above the `.worktrees/` rule names the deleted verb among the commands that create worktrees
    - .planning/phases/23-end-to-end-dogfood/23-RESEARCH.md § "23d Deletion Inventory" — the verified per-file counts, to confirm nothing in the inventory is unaccounted for
  </read_first>
  <action>
Rewrite the last remaining references, all of which are comments.

`monitor.rs` — **doc comments only in this task.** Both
`spawn_monitor_no_advance` and `wait_for_agent_exit` survive until plan 23-07
(see this plan's objective for why). Rewrite each function's doc comment so it
describes what the function does — spawn a monitor that owns the agent and
records captures but does not advance the stage machine; block until the
monitor records an exit code — without attributing it to a caller that no
longer exists. Add one sentence to each noting it currently has no caller and
is removed with the monitor rewrite, so a reader does not mistake it for live
API.

`devflow_dir_gitignore.rs` — rewrite the comments naming the deleted verb.
Constructor case #3's comment should say it drives the no-advance capture path
(the reason that variant is used here is that it must not fork an advance tail
into a throwaway root), and the `wait_for_pid_to_die` helper's doc comment
should describe the zombie-reaping problem without naming the deleted verb.

`.gitignore` — the comment above the `.worktrees/` rule lists the commands that
create worktrees. Drop the deleted verb from that list. Do not change the rule
itself.

Then prove the sweep is complete: a case-insensitive search across `crates/`,
the four scoped operator documents, `docs/`, and `.gitignore` must return
nothing. `CHANGELOG.md` is deliberately excluded — plan 23-03 preserved its
history and added the 2.0.0 removal entry, both of which name the verb on
purpose.
  </action>
  <acceptance_criteria>
    - `rg -c -i 'sequentagent' crates/ README.md ARCHITECTURE.md OPERATIONS.md CONTRIBUTING.md docs/ .gitignore` reports no matches (exit code 1)
    - `rg -c -i 'sequentagent' CHANGELOG.md` still reports matches
    - `rg -n 'pub fn spawn_monitor_no_advance' crates/devflow-core/src/monitor.rs` still matches — deferred to plan 23-07 by design, not forgotten
    - `rg -n 'pub fn wait_for_agent_exit' crates/devflow-core/src/monitor.rs` still matches
    - `cargo test --workspace 2>&1 | grep -E '^test result:'` shows `0 failed` on every line
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0
    - `cargo fmt --check` exits 0
  </acceptance_criteria>
  <verify>
    <automated>! rg -q -i 'sequentagent' crates/ README.md ARCHITECTURE.md OPERATIONS.md CONTRIBUTING.md docs/ .gitignore &amp;&amp; rg -q -i 'sequentagent' CHANGELOG.md &amp;&amp; cargo test --workspace 2>&amp;1 | grep -E '^test result:' | grep -qv 'failed: [1-9]' &amp;&amp; cargo clippy --workspace --all-targets -- -D warnings &amp;&amp; cargo fmt --check</automated>
  </verify>
  <done>The workspace-wide reference count is zero outside the changelog's deliberate history, and the two deferred monitor functions are still present and explicitly labelled as such.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated resume-cron manifest → operator's scheduler | A manifest is written to `.devflow/cron-instructions*.json` and is intended to be executed later, unattended, by the operator's scheduler |
| `devflow-core` public API → downstream crates.io consumers | Core is published; removing `pub` items breaks compilation for anyone depending on them |
| `.devflow/` path constructors → `.gitignore` coverage guarantee | Any constructor not covered by the gitignore rules leaks runtime artifacts into a user's commits (999.10 class) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-14 | Denial of Service | resume-cron manifest naming a deleted verb, executed unattended hours later | high | mitigate | Delete `ship::build_cron_instructions` outright rather than leaving it callable; acceptance criterion greps the workspace to zero for a whole-word match on the function name |
| T-23-15 | Information Disclosure | narrowing the seven-constructor `.devflow/` gitignore coverage guarantee by deleting a case instead of repointing it | high | mitigate | Named explicitly as must-repoint-never-delete, with an acceptance criterion asserting seven numbered cases remain and the test passes. This is the 999.10 leak class — DevFlow has previously swept unredacted agent stdout into a user's commit |
| T-23-16 | Repudiation | deleting the two surviving monitor functions here would leave the no-advance capture constructor untestable, silently reducing coverage | medium | mitigate | Explicit scope boundary in the objective; acceptance criteria assert both functions are *still present* after this plan, deferring their removal to plan 23-07 where a replacement test configuration exists |
| T-23-17 | Tampering | a doc comment rewritten into a tautology that asserts nothing | low | accept | Task 2 calls out the specific negative test at risk and instructs deletion over tautology; reviewed at plan-checker time, not enforced by a grep |

**Supply chain:** not applicable — this plan installs zero packages and removes
code only.
</threat_model>

<verification>
1. `cargo test --workspace` — every `test result:` line reports `0 failed`.
2. `cargo clippy --workspace --all-targets -- -D warnings` — clean.
3. `cargo fmt --check` — clean.
4. `rg -c -i 'sequentagent' crates/ README.md ARCHITECTURE.md OPERATIONS.md CONTRIBUTING.md docs/ .gitignore`
   returns no matches; the same grep against `CHANGELOG.md` still matches.
5. `rg -c '\bbuild_cron_instructions\b' crates/` returns no matches.
6. `crates/devflow-core/tests/devflow_dir_gitignore.rs` still enumerates seven
   constructors and its test passes.
7. `rg -n 'pub fn spawn_monitor_no_advance|pub fn wait_for_agent_exit' crates/devflow-core/src/monitor.rs`
   still matches both — the deferred pair is intact for plan 23-07.
</verification>

<success_criteria>
- Unit 23d is complete: 142 references reduced to zero across `crates/`, the
  scoped docs and `.gitignore`, with the changelog's history deliberately
  preserved.
- No workspace code path can emit a scheduled command invoking a deleted verb.
- The `.devflow/` seven-constructor coverage guarantee is intact at seven.
- 23b's migration inventory now contains two monitor functions with zero
  callers, ready to be deleted rather than ported.
</success_criteria>

<output>
Create `.planning/phases/23-end-to-end-dogfood/23-04-SUMMARY.md` when done.
</output>
