---
phase: 22-concurrency-governance-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/src/commands.rs
autonomous: true
requirements: ["999.30", "DEN-55", "C-01", "C-02", "C-03"]

must_haves:
  truths:
    - "gate_show and gate_respond use one shared omitted-stage resolver, so no-open, single-open, and ambiguous-open behavior and wording remain identical (C-01)."
    - "gate_show resolves and selects from one fetched open-gate collection, eliminating its redundant read without introducing a locking or transactional gate protocol (C-01)."
    - "doctor planning-document reconciliation uses devflow_core::config::MAIN as the named production-branch source of truth, with unchanged read-only output and semantics (C-01, C-02)."
    - "Focused CLI regressions cover shared gate resolution, gate_show success from the fetched collection, and the MAIN-backed reconciliation seam (C-03)."
  artifacts:
    - path: crates/devflow-cli/src/commands.rs
      provides: "Shared gate-stage resolution, one-read gate_show flow, MAIN import/use, and focused regression tests"
      contains: "resolve_single_open_gate_stage"
  key_links:
    - from: "gate_respond and gate_show"
      to: "resolve_single_open_gate_stage"
      via: "both omitted-stage branches use the same resolver contract"
      pattern: "resolve_single_open_gate_stage"
    - from: "gate_show"
      to: "Gates::list_open"
      via: "one Vec<OpenGate> remains available for phase resolution and selected-gate lookup"
      pattern: "let open = Gates::list_open"
    - from: "collect_planning_doc_findings"
      to: "devflow_core::config::MAIN"
      via: "tag reachability closure receives the named production branch"
      pattern: "tag_exists_and_reachable(project_root, tag, MAIN)"
---

<objective>
Close the three command-side Phase 21 review findings in the exact narrow
999.30 / DEN-55 dogfooding boundary: share omitted-stage gate resolution, make
gate_show reuse one open-gate read, and replace the planning-doc reconciliation
literal with devflow_core::config::MAIN. Preserve every existing CLI message,
stage ordering, explicit --stage path, response-file behavior, and doctor
read-only contract. This plan creates no dependency and does not broaden the
work into a concurrency protocol or unrelated backlog work.
</objective>

<context>
@.planning/phases/22-concurrency-governance-correctness/22-CONTEXT.md
@.planning/phases/22-concurrency-governance-correctness/22-RESEARCH.md
@.planning/phases/21-operator-usability-release-execution/21-REVIEW.md
@.planning/ROADMAP.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@crates/devflow-cli/src/commands.rs
@crates/devflow-core/src/gates.rs
@crates/devflow-core/src/config.rs
</context>

<decision_traceability>
- C-01: 22-CONTEXT.md § Constraints — preserve existing CLI output and gate semantics; the resolver and one-read flow must be behavior-preserving.
- C-02: 22-CONTEXT.md § Constraints — reuse existing helpers/types and add no dependencies; use OpenGate data and the existing MAIN constant.
- C-03: 22-CONTEXT.md § Constraints — add focused regression coverage for each changed behavior.
- S-01: 22-CONTEXT.md § Trial Boundary — this plan covers only WR-01, WR-02, and WR-03 from 999.30 / DEN-55.
</decision_traceability>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Share omitted-stage resolution and make gate_show single-read</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <behavior>
    - Omitted stage with no open gate returns the existing gate-list guidance from both commands.
    - Omitted stage with exactly one phase gate selects that gate in both commands.
    - Omitted stage with multiple phase gates returns the existing stage-listing and --stage guidance from both commands.
    - gate_show with an omitted stage renders the gate selected from the same open collection used for resolution; explicit --stage behavior and not-found wording remain unchanged.
  </behavior>
  <action>
    Add a private slice-based resolver near the gate handlers that accepts the
    already-filtered or phase-filterable open-gate data and owns the existing
    no-open, single-open, and ambiguous-open outcomes and exact messages (per
    C-01, C-02, and S-01). Refactor gate_respond to use that shared resolver
    only when stage is None; leave explicit Some(stage) behavior untouched.
    Refactor gate_show to read Gates::list_open(project_root) once, derive the
    phase candidates from that open-gate vector, resolve an omitted stage from
    the same candidates, and find the selected gate in the same vector. Do not add a
    lock, retry loop, transactional gate protocol, or a second reader. Extend
    the existing gate_show/gate_respond tests to prove none/single/multiple and
    explicit-stage behavior still agree, and retain a focused successful
    omitted-stage gate_show test that exercises the shared collection flow.
  </action>
  <verify>
    <automated>cargo test -p devflow gate_show &amp;&amp; cargo test -p devflow gate_respond 2&gt;&amp;1 | rg "test result: ok"</automated>
  </verify>
  <done>Both commands call one shared omitted-stage resolver; gate_show has one Gates::list_open read on every path; existing gate output and semantics are preserved; focused gate tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use MAIN in doctor reconciliation and lock the source-of-truth seam</name>
  <files>crates/devflow-cli/src/commands.rs</files>
  <behavior>
    - collect_planning_doc_findings passes devflow_core::config::MAIN to tag reachability checks.
    - Doctor reconciliation remains read-only and produces the same findings/output for the current MAIN value.
    - A focused test or source-level assertion proves the named constant is the branch argument rather than a duplicate literal.
  </behavior>
  <action>
    Extend the existing devflow_core::config import with MAIN and pass MAIN
    into collect_planning_doc_findings' tag lookup closure (per C-01, C-02,
    and S-01). Update only nearby explanatory prose as needed to describe the
    named production branch; do not introduce GitFlowConfig loading, alter
    reconciliation severity/cutoff rules, or change doctor output. Add the
    smallest practical focused regression at the pure reconciliation or
    fixture seam that proves the production-branch argument remains the
    existing named constant; preserve the existing planning-doc tests.
  </action>
  <verify>
    <automated>cargo test -p devflow collect_planning_doc_findings &amp;&amp; cargo test -p devflow reconcile_planning_docs 2&gt;&amp;1 | rg "test result: ok"</automated>
  </verify>
  <done>The hardcoded production-branch argument is gone, MAIN is imported and used at the reconciliation seam, focused doctor tests pass, and doctor remains read-only with unchanged current behavior.</done>
</task>

</tasks>

<verification>
<automated>cargo fmt --check &amp;&amp; cargo test -p devflow gate_show &amp;&amp; cargo test -p devflow gate_respond &amp;&amp; cargo test -p devflow collect_planning_doc_findings &amp;&amp; cargo test -p devflow reconcile_planning_docs</automated>
</verification>

<source_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | ROADMAP Phase 22 | Narrow Phase 22 correctness cleanup for the promoted 999.30 / DEN-55 work | 22-01, 22-02 | COVERED | ROADMAP goal is still a placeholder; locked CONTEXT concretizes the goal and is honored. |
| REQ | none | No REQUIREMENTS.md or Phase 22 REQ-IDs exist | — | COVERED | Requirements are represented by 999.30 / DEN-55 and CONTEXT constraints. |
| RESEARCH | WR-01 | Shared omitted-stage gate resolution | 22-01 Task 1 | COVERED | Resolver is reused by both gate commands. |
| RESEARCH | WR-02 | Replace planning-doc "main" source with MAIN | 22-01 Task 2 | COVERED | Existing constant is used without config loading. |
| RESEARCH | WR-03 | gate_show one open-gate read | 22-01 Task 1 | COVERED | One Vec is used for inference and selection. |
| RESEARCH | IN-01 | Fold stage_launched timestamp into one-pass event summary | 22-02 | COVERED | Planned in dependent event-summary plan. |
| CONTEXT | C-01 | Preserve CLI output and gate semantics | 22-01, 22-02 | COVERED | Explicitly preserved in actions and must-haves. |
| CONTEXT | C-02 | Reuse helpers/types; no dependencies | 22-01, 22-02 | COVERED | No new crates or broad abstractions planned. |
| CONTEXT | C-03 | Focused regression coverage | 22-01, 22-02 | COVERED | Each changed behavior receives targeted tests. |
| CONTEXT | C-04 | fmt, clippy, relevant tests, workspace tests; stop at Validate | 22-02 | COVERED | Final validation task stops before Ship. |
| CONTEXT | S-01 | Only four advisory findings; no broad concurrency redesign | 22-01, 22-02 | COVERED | All actions carry the narrow boundary. |
</source_audit>
