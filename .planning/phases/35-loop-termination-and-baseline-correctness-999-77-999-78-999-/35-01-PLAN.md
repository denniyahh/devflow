---
phase: 35-loop-termination-and-baseline-correctness-999-77-999-78-999-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-cli/src/test_support.rs
  - crates/devflow-core/src/test_support.rs
  - crates/devflow-core/src/agent_result.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
autonomous: true
requirements: [HARDEN-01, HARDEN-07]

estimate:
  tokens: 45000
  raw_tokens: 90000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "A Validate failure whose commit count could not be measured leaves state.last_validate_failure_commit_count byte-identical to what it was before that cycle, so the next successful measurement compares against the last real observation (D-08, A-04)"
    - "Across the sequence failure-with-unmeasurable-count then failure-with-an-unchanged-real-count, state.consecutive_failures accumulates and is never reset to 1 (999.77's two-cycle discriminating sequence)"
    - "phase_commit_count returns None only when the git child could not be executed, and Some(0) when git ran and reported the branch absent — the two are distinguishable at the type level (A-06)"
    - "evaluate_layer2 driven with exit_code 0, Stage::Code and an unrunnable git returns Ok(None) and falls through to Layer 3, never AgentStatus::Failed (D-09, HARDEN-07)"
    - "The NoGitPath guard genuinely prevents git from being spawned while installed, and PATH is byte-identical to its pre-guard value after the guard drops — proven by a harness-sanity test in each crate before any regression result is believed (NC-1)"
    - "HARDEN-07 boundary: the three commit-count observations None, Some(0) and Some(1) each produce a distinct downstream classification — None falls to Layer 3, Some(0) with a commit gate classifies Failed, Some(1) does not"
    - "HARDEN-07 precision: a git child that ran successfully but produced output that does not parse as a u32 count yields None (a measurement failure), never a silent Some(0)"
    - "The doc comment on phase_commit_count no longer claims every consumer treats the three zero-causes the same way, and the doc comment on handle_validate_outcome no longer promises a gating guarantee that a single transient fault defeats (999.77 deliverable)"
  artifacts:
    - path: "crates/devflow-cli/src/test_support.rs"
      provides: "NoGitPath RAII guard for the devflow-cli test binary"
      contains: "NoGitPath"
    - path: "crates/devflow-core/src/test_support.rs"
      provides: "NoGitPath RAII guard plus the crate's first PATH mutex, cfg(test)-only so devflow-cli can never reach it"
      contains: "NoGitPath"
    - path: "crates/devflow-core/src/agent_result.rs"
      provides: "phase_commit_count returning Option<u32>, and evaluate_layer2's Ok(None) arm"
      contains: "Option<u32>"
    - path: "crates/devflow-cli/src/pipeline_outcomes.rs"
      provides: "handle_validate_outcome's diverging Some/None arms and the corrected guarantee doc comment"
      contains: "last_validate_failure_commit_count"
  key_links:
    - from: "crates/devflow-core/src/agent_result.rs"
      to: "crates/devflow-cli/src/pipeline_outcomes.rs"
      via: "phase_commit_count's Option<u32> return forces handle_validate_outcome's baseline write open at compile time"
      pattern: "phase_commit_count"
    - from: "crates/devflow-core/src/agent_result.rs"
      to: "crates/devflow-core/src/agent_result.rs"
      via: "the same Option<u32> return forces evaluate_layer2's commits binding open at compile time"
      pattern: "evaluate_layer2"
---

<objective>
Close criteria 1 and 6 together: make "could not count" representable at the type level and honour
that distinction at both consumers of the commit count.

Purpose: a transient `git` fault currently forges a false-zero baseline that buys one free reset of
the `MAX_CONSECUTIVE_FAILURES` ceiling (999.77), and makes a successful agent read as
`Failed — no work done` (999.87). Both consumers collapse the same lossy `u32`.

Output: a `NoGitPath` harness in each crate, `phase_commit_count -> Option<u32>`, a `None` arm in
`handle_validate_outcome` that accumulates without touching the baseline, `evaluate_layer2`
returning `Ok(None)`, the two-cycle discriminating regression test, criterion 6's discriminating
test, and the two corrected doc comments.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-CONTEXT.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-RESEARCH.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-PATTERNS.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-VALIDATION.md
</context>

<planner_findings>
Three findings resolved here that are NOT in CONTEXT.md. Read them before starting.

**F-1 — the guard must make `git` ABSENT, not failing.** CONTEXT.md's A-13 says "a failing-`git`
shim placed first on `PATH`". That is the wrong shape and would produce a green test over an
unfixed defect. `phase_commit_count` reads the branch-existence probe as
`.output().map(|o| o.status.success()).unwrap_or(false)` — a shim that RUNS and exits non-zero
returns `Ok(status)`, so `unwrap_or(false)` is never reached and the function returns a real
observation, which under A-06 is `Some(0)`. Only `.output()` returning `Err` is the
could-not-measure case. That requires the binary to be unresolvable. Hence an empty-directory
`PATH`, never a script. This is NC-4 in `35-VALIDATION.md`.

**F-2 — the harness crate placement, DECIDED HERE.** The orchestrator's briefing stated that
`crates/devflow-core/src/test_support.rs` is exposed by a bare `pub mod test_support;` and is "not
feature-gated", and proposed a single shared guard on that basis. **That premise is false and was
checked against source during planning:** `crates/devflow-core/src/lib.rs:79` carries
`#[cfg(any(test, feature = "test-support"))]` directly above the `pub mod test_support;` at `:80`,
and the module's own header comment says so. Two consequences kill the shared-guard option:

1. `tempfile` is a **dev-dependency** of devflow-core (`crates/devflow-core/Cargo.toml`, under
   `[dev-dependencies]`). When devflow-cli builds devflow-core with `features = ["test-support"]`
   as a dev-dependency, `cfg(test)` is false for devflow-core and its dev-dependencies are absent —
   so a `tempfile`-based guard living under that feature gate would not compile for devflow-cli.
   Making it compile would require promoting `tempfile` to an optional real dependency of a
   crates.io-published crate: a fifth surface change in a phase whose deliverable is an exact
   enumeration of surface changes (D-08).
2. The shared option is also what creates the two-mutex `PATH` race the briefing warns about.

**Decision: one `NoGitPath` per crate, each using that crate's own single `PATH` mutex, and
devflow-core's is `#[cfg(test)]`-only.** The race is prevented *structurally*, not by discipline:
devflow-core's guard does not exist in any build devflow-cli can link, so a devflow-cli test can
never reach two guards holding two mutexes. Within devflow-cli there remains exactly one mutex
(`ENV_MUTEX`); within devflow-core there is exactly one (the new `PATH_MUTEX`); across crates cargo
gives each crate's tests a separate process. Criterion 6 gets the same harness *shape* rather than
the same symbol — recorded as a deliberate deviation from criterion 6's "the same harness" wording,
with this compile-level reason.

**F-1b — a residual risk this phase's source artifacts do not name, surfaced rather than hidden.**
`NeutralPath` is safe to hold while sibling tests run because it keeps a real `git` resolvable.
`NoGitPath` is the first guard in this workspace that makes `git` unresolvable process-wide, and
devflow-core has had **zero** `PATH` mutations until now (verified: `rg` for `set_var("PATH"` over
`crates/devflow-core/src/` returns nothing, while the same search over `crates/devflow-cli/src/`
returns six files — a real zero, not a broken search). Sibling tests in either crate that shell out
to `git` without holding the same mutex can therefore fail spuriously if they run inside a guarded
window. The mitigation is scope minimisation, enforced as an acceptance criterion below: each guard
region wraps exactly the one call under test. If flakes appear anyway, the recorded fallback is to
drive the `Err` branch through a non-existent `project_root` instead —
`hermetic_command` sets `cmd.current_dir(dir)` (`crates/devflow-core/src/git.rs:89`), so a
non-existent directory makes `.output()` return `Err` with no environment mutation at all. Do not
switch to that fallback pre-emptively; it deviates from F-1's directive and is recorded as an
escape hatch, not a plan.
</planner_findings>

<assumption_delta_decision>
The `assumption-delta scan` fired (`detected: true`, kind `pluralization`). The genuine transition
in this plan is **required to optional** on the commit count, not a second platform.

- **Primary noun:** the commit-count *observation*, now `Option<u32>`.
- **Decision: `promote`.** The optional form becomes the single representation and the lossy `u32`
  form is deleted rather than kept alongside — which is exactly D-08's stated rejection of the
  backlog's sibling-function proposal. Adding the optional form alongside the required one would
  leave the lossy call sites compiling untouched, and 999.87 is the named instance of that harm.
- **Invariant this encodes:** every consumer of the commit count must be reachable by the compiler
  when the measurement's representation changes. The `Option<u32>` signature is that invariant.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1: Build the absent-git harness in both crates, with its own sanity control first</name>
  <files>crates/devflow-cli/src/test_support.rs, crates/devflow-core/src/test_support.rs</files>
  <read_first>
    - `crates/devflow-cli/src/test_support.rs` — read `ENV_MUTEX`, `env_lock()`, and `NeutralPath`
      (struct, `install()`, `impl Drop`) in full. `NoGitPath` is a structural sibling of
      `NeutralPath`; copy its shape, its `// SAFETY:` comment discipline, and its
      caller-holds-the-mutex precondition wording.
    - `crates/devflow-core/src/test_support.rs` — read the whole file. Note it currently has no
      mutex and no environment mutation of any kind, and that its module header records the
      `#[cfg(any(test, feature = "test-support"))]` gate.
    - `crates/devflow-core/src/lib.rs` lines 75-85 — confirm the gate attribute above
      `pub mod test_support;` before adding anything to that module.
    - `crates/devflow-core/src/git.rs` lines 60-95 — `git_command` and `hermetic_command`, so the
      sanity control invokes git through the same constructor production code uses.
    - `35-PATTERNS.md` section 1 — the quoted `NeutralPath` body and the two-guard nesting shape.
  </read_first>
  <action>
    Add a `NoGitPath` RAII guard to BOTH crates, per the F-2 decision above.

    In `crates/devflow-cli/src/test_support.rs`, beside `NeutralPath`: `pub(crate) struct NoGitPath`
    with fields `_dir: tempfile::TempDir` and `original: Option<std::ffi::OsString>`, an
    associated `install()` that creates a deliberately EMPTY `tempfile::tempdir()`, captures
    `std::env::var_os("PATH")`, and replaces `PATH` with that directory alone; and an
    `impl Drop` that restores the captured value, or removes the variable when it was previously
    unset. Document the caller-holds-`ENV_MUTEX` precondition exactly as `NeutralPath` documents it,
    and document why the directory is empty rather than holding a stub executable: only a binary
    that cannot be resolved makes the spawn itself fail, which is the sole condition that surfaces
    as `Err` from `.output()`.

    In `crates/devflow-core/src/test_support.rs`, add a `#[cfg(test)]`-gated region containing:
    `static PATH_MUTEX: std::sync::Mutex<()>`, a poison-tolerant `path_lock()` accessor mirroring
    devflow-cli's `env_lock()`, and an identically-shaped `NoGitPath`. The `#[cfg(test)]` gate is
    load-bearing and must be stated in a comment: it keeps this guard out of every build devflow-cli
    can link, so no test binary can ever reach two guards holding two different mutexes.

    Then write the harness-sanity control (NC-1) in each crate, named
    `no_git_path_makes_git_unresolvable_and_restores_it`. Each asserts three things in order against
    `devflow_core::test_support::git_command(<tempdir>)` with the argument `--version`: the call
    returns `Ok` before the guard is installed; it returns `Err` while the guard is installed; and
    it returns `Ok` again after the guard has dropped. The middle assertion additionally asserts the
    error kind is `std::io::ErrorKind::NotFound`. Acquire the crate's mutex at the top of each test
    and nest the guard in its own block so `Drop` runs before the final assertion.
  </action>
  <verify>
    <automated>cargo test -p devflow --lib test_support::tests::no_git_path_makes_git_unresolvable_and_restores_it -- --exact 2>&1 | grep -E 'test result: ok\. 1 passed;.*[1-9][0-9]* filtered out'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --lib test_support::tests::no_git_path_makes_git_unresolvable_and_restores_it -- --exact 2>&1` prints a `test result: ok. 1 passed` line with a non-zero `filtered out` count. Exit code alone is not evidence: a name matching nothing also exits 0.
    - `cargo test -p devflow-core --lib test_support::tests::no_git_path_makes_git_unresolvable_and_restores_it -- --exact 2>&1` prints the same shape.
    - NC-1's own control is inside each test: the pre-guard call asserts `Ok` and the post-drop call asserts `Ok`, so a guard that silently did nothing cannot produce a green result.
    - `rg -n 'cfg\(test\)' crates/devflow-core/src/test_support.rs` matches on a line above the new guard region, and `cargo build -p devflow --tests` succeeds — together these prove devflow-cli compiles without ever seeing devflow-core's guard.
    - Each guard region in the new tests wraps at most one statement that calls the subject under test; no guard is held across an assertion block.
  </acceptance_criteria>
  <done>Both crates carry a `NoGitPath` guard and a passing sanity control proving it blocks and restores; devflow-core's guard is `#[cfg(test)]`-only and unreachable from devflow-cli.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: TRACER — drive an unmeasurable count end to end, from harness through both consumers</name>
  <reversibility rating="one-way">D-08 changes the return type of `phase_commit_count`, a `pub` item of the crates.io-published `devflow-core`; undo is a re-publication, not a revert. No `checkpoint:decision` is emitted: CONTEXT.md records the operator choosing this exact break as an escalated carve-out, and `references/planner-reversibility.md` names re-asking an already-decided door as an anti-pattern.</reversibility>
  <files>crates/devflow-core/src/agent_result.rs, crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - `crates/devflow-core/src/agent_result.rs` around `phase_commit_count` (the function begins at
      `:1841`) — read its doc comment and both `.output()` sites in full before editing.
    - `crates/devflow-core/src/agent_result.rs` around `evaluate_layer2` (begins at `:1892`) — read
      the whole function. The `Err(_) => return Ok(None)` fall-to-Layer-3 idiom sits a few lines
      above the commits binding and is the shape D-09 requires you to reuse verbatim.
    - `crates/devflow-cli/src/pipeline_outcomes.rs` — `handle_validate_outcome` in full, including
      its doc comment and the CR-01 note explaining why the two root-consuming reads deliberately
      use different roots. That note is load-bearing and must survive unedited.
    - `crates/devflow-core/src/mode.rs` — `consecutive_failures_made_progress` and
      `MAX_CONSECUTIVE_FAILURES`.
    - `crates/devflow-core/src/agent_result.rs` existing tests
      `phase_commit_count_reports_zero_without_a_branch` and the three `evaluate_layer2_*` tests
      (near `:6631`-`:6700`) — these compile against the old signature and need type updates, not
      behaviour rewrites.
    - `35-VALIDATION.md` — NC-2, NC-3, NC-4.
  </read_first>
  <behavior>
    Write these as failing tests BEFORE changing any production signature. The two-cycle test
    compiles and runs against today's code and must go RED on the streak assertion — that RED is
    the proof it discriminates, and a version of it that passes today is the proxy NC-3 forbids.

    - `validate_failure_with_unmeasurable_count_accumulates_the_streak` in `pipeline_outcomes.rs`'s
      test module. Establish a real baseline of `Some(N)` for a non-zero `N` and a
      `consecutive_failures` of at least 1. Cycle 1: call `handle_validate_outcome` with the Validate
      outcome that records a failure, with `NoGitPath` installed for exactly that call. Cycle 2: call
      it again with real git present and the branch's real count still `N` (nothing committed
      between cycles). Assert: after cycle 1 the persisted baseline is still `Some(N)` and
      `consecutive_failures` incremented; after cycle 2 `consecutive_failures` incremented again and
      was never reset to 1.
    - `phase_commit_count_reports_none_when_git_cannot_run` in `agent_result.rs`. With the guard
      installed, the result is `None`. Keep `phase_commit_count_reports_zero_without_a_branch` as
      the paired opposite-result case: git runs, the branch is genuinely absent, result is `Some(0)`.
      The pair is what proves the split is on "did the command run", not on "was the answer zero".
  </behavior>
  <action>
    RED first, then GREEN.

    RED: write the two tests described in the behavior block. Run the two-cycle test against
    unmodified production code and record its failure output — this is NC-3's evidence that a
    single-cycle test would not have discriminated.

    GREEN: change `phase_commit_count`'s return type to `Option<u32>` per D-08 and split its two
    `.output()` sites per A-06. Branch-existence probe: an `Err` from `.output()` means the child
    could not be executed, so return `None`; an `Ok` whose status is unsuccessful means git ran and
    reported the branch absent, which is a real observation, so return `Some(0)`. Count probe: an
    `Err` returns `None`; an `Ok` whose stdout does not parse as a `u32` also returns `None`,
    because the child ran but yielded no usable count — record that choice in the doc comment,
    since A-06 splits only the run/did-not-run axis and leaves this third case to the planner.

    Then take the two compile errors the signature change forces open.

    `evaluate_layer2`: per D-09, when the count is `None`, return `Ok(None)` so the cascade falls
    through to Layer 3. Use the identical construction the function already uses a few lines above
    for an unreadable exit file, with a comment naming D-09 and stating that an unmeasurable count
    is not evidence of absent work. When the count is `Some`, today's classification is unchanged.

    `handle_validate_outcome`: per A-04 the `Some` and `None` arms must diverge. The `Some(current)`
    arm keeps today's logic verbatim — the progress check against
    `mode::consecutive_failures_made_progress`, then the baseline write. The `None` arm treats the
    cycle as not-progress, so `consecutive_failures` increments via `saturating_add`, and must leave
    `state.last_validate_failure_commit_count` untouched. Do not widen
    `consecutive_failures_made_progress`'s signature: the `None` arm never calls it. Add a comment
    at the `None` arm stating that skipping the baseline write is the fix, so the next real
    measurement compares against the last real observation.

    Finally update the existing tests named in `read_first` to the new types. These are type
    updates, not behaviour changes; their assertions' meaning must be preserved.
  </action>
  <verify>
    <automated>cargo test -p devflow --lib pipeline_outcomes:: 2>&1 | grep -E 'test result: ok\. [0-9]+ passed; 0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --lib pipeline_outcomes::tests::validate_failure_with_unmeasurable_count_accumulates_the_streak -- --exact 2>&1` prints `test result: ok. 1 passed` with a non-zero `filtered out` count.
    - NC-2 performed and recorded: restore the unconditional baseline write with the old lossy zero mapping, re-run the same test, and confirm the run prints a `test result: FAILED` line with `1 failed`; then revert the mutation and confirm it passes again. Both outputs go in the SUMMARY. A test that passes under both forms is measuring nothing.
    - NC-3 recorded: the RED output from running the two-cycle test against unmodified production code is in the SUMMARY, together with a one-line statement that a single-cycle variant passes against both the buggy and the fixed code.
    - NC-4 satisfied structurally: `phase_commit_count_reports_zero_without_a_branch` still asserts `Some(0)` for a git that ran and found no branch, and `phase_commit_count_reports_none_when_git_cannot_run` asserts `None`. Both pass in the same run.
    - `rg -n 'fn phase_commit_count' crates/devflow-core/src/agent_result.rs` shows a signature returning `Option<u32>`.
    - `cargo test -p devflow-core --lib agent_result:: 2>&1` prints `test result: ok.` with `0 failed`.
    - The CR-01 note in `handle_validate_outcome` explaining the deliberate different-roots read is present and unedited: `git diff -- crates/devflow-cli/src/pipeline_outcomes.rs` shows no change to those comment lines.
  </acceptance_criteria>
  <done>An unmeasurable commit count is representable, both consumers honour it, the two-cycle sequence accumulates rather than resetting, and NC-2 through NC-4 have each been run and their opposite-result case seen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criterion 6's discriminating test, and the two doc comments the fix falsifies</name>
  <files>crates/devflow-core/src/agent_result.rs, crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <read_first>
    - `crates/devflow-core/src/agent_result.rs` — `evaluate_layer2` as left by Task 2, and the
      existing `evaluate_layer2_exit_zero_no_commits_is_failed` at `:6668`. Read that test in full:
      it is the contrast case, not the base to extend.
    - `crates/devflow-core/src/agent_result.rs` doc comment on `phase_commit_count` (the sentence
      claiming every consumer treats the three zero-causes alike sits just above the `fn` line at
      `:1841`).
    - `crates/devflow-cli/src/pipeline_outcomes.rs` doc comment on `handle_validate_outcome` — the
      passage promising that an unrunnable git counts zero every cycle so the gate stays reachable.
    - `35-VALIDATION.md` — NC-11 and the "What the evidence does NOT establish" section.
  </read_first>
  <behavior>
    - `evaluate_layer2_unrunnable_git_falls_through_to_layer3` in `agent_result.rs`. Drive
      `evaluate_layer2` with an exit file recording exit code 0, `Stage::Code` (a commit-gated
      stage), and `NoGitPath` installed so the internal count call cannot run. Assert the result is
      `Ok(None)`. Assert explicitly that it is not a `Failed` classification, so a future change that
      returns a different non-`Failed` value still has to confront this test.
    - `evaluate_layer2_exit_zero_no_commits_is_failed` stays byte-unchanged and passing in the same
      run. It is the required opposite-result case: real git, a genuinely empty branch, still
      `Failed`. Extending that test instead of adding a sibling would be the proxy NC-11 names.
  </behavior>
  <action>
    Add the discriminating test described above. Acquire devflow-core's `path_lock()` at the top and
    nest the guard around only the `evaluate_layer2` call.

    Then correct both doc comments — these are phase deliverables named by criterion 1, not cleanup.

    On `phase_commit_count`: the sentence asserting that every consumer treats the three
    zero-producing causes identically is deliberately falsified by this plan. Replace it with the
    contract the function now has: the return is `None` when the git child could not be executed or
    produced no parseable count, and `Some(n)` when git ran and reported a real number, with the
    branch-absent case reported as a real zero. Name the two consumers and state that each now
    handles the absent case distinctly.

    On `handle_validate_outcome`: the passage promising that the failure direction is toward gating
    because an unrunnable git counts zero every cycle currently documents a property the code did
    not have — it held only while git stayed broken, and was false for exactly one transient
    failure, which is the likelier event. Replace it with the guarantee the code now delivers: an
    unmeasurable cycle is treated as not-progress and does not disturb the recorded baseline, so a
    single transient fault can no longer buy a reset of the ceiling.

    Do not edit any other comment in either file. In particular leave `idle_timeout_result`'s doc
    comment in `agent_result.rs` alone — it is backlog 999.85 and is explicitly out of scope in
    `REQUIREMENTS.md`.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib agent_result::tests::evaluate_layer2_unrunnable_git_falls_through_to_layer3 -- --exact 2>&1 | grep -E 'test result: ok\. 1 passed;.*[1-9][0-9]* filtered out'</automated>
  </verify>
  <acceptance_criteria>
    - The named test passes with a real `1 passed` line and a non-zero `filtered out` count.
    - NC-11 satisfied: `cargo test -p devflow-core --lib agent_result::tests::evaluate_layer2_exit_zero_no_commits_is_failed -- --exact 2>&1` prints `1 passed`, and `git diff -- crates/devflow-core/src/agent_result.rs` shows that test's body unchanged.
    - The negative control for the new test is performed and recorded: replace the `None` arm's return with today's zero-treatment, re-run the new test, confirm a `test result: FAILED` line, then revert. Output in the SUMMARY.
    - `rg -n 'treats all three the same way' crates/devflow-core/src/agent_result.rs` returns no matches.
    - `rg -n 'the gate stays reachable' crates/devflow-cli/src/pipeline_outcomes.rs` returns no matches.
    - `rg -n 'idle_timeout_result' crates/devflow-core/src/agent_result.rs` still matches, and `git diff` shows no change within that function's doc comment — 999.85 was left alone.
    - `scripts/check.sh all` completes with `clippy` and `fmt` clean and the suite reporting `0 failed`.
  </acceptance_criteria>
  <done>Criterion 6 has a test that only passes against the fixed code, its contrast case is intact and unmodified, and both over-promising doc comments now describe the guarantee the code actually delivers.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan CREATES (new — exclude from drift verification):
- `NoGitPath` (struct), `NoGitPath::install`, `impl Drop for NoGitPath` — in
  `crates/devflow-cli/src/test_support.rs` AND, separately, in
  `crates/devflow-core/src/test_support.rs`
- `PATH_MUTEX` (static), `path_lock()` — `crates/devflow-core/src/test_support.rs`, `cfg(test)`-only
- Tests: `no_git_path_makes_git_unresolvable_and_restores_it` (one per crate),
  `validate_failure_with_unmeasurable_count_accumulates_the_streak`,
  `phase_commit_count_reports_none_when_git_cannot_run`,
  `evaluate_layer2_unrunnable_git_falls_through_to_layer3`

Symbols this plan CHANGES:
- `devflow_core::agent_result::phase_commit_count` — `pub`, return type `u32` becomes `Option<u32>`
  (D-08, breaking)

Symbols this plan REMOVES: none.

Files this plan creates: none — every edit lands in an existing file.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow → the `git` child process | An external binary whose availability and output DevFlow cannot assume; its failure modes cross into DevFlow's safety-gate arithmetic |
| Test process → process-global `PATH` | A test binary's threads share one environment; a guard held too long or dropped too late changes behaviour for unrelated tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-01 | Denial of Service | `phase_commit_count` → `handle_validate_outcome` baseline write | high | mitigate | Represent "could not measure" as `None` at the type level and skip the baseline write on that arm, so a transient `git` fault can no longer forge a false zero that pushes the human gate further away on every occurrence (Task 2) |
| T-35-02 | Spoofing | `evaluate_layer2` | medium | mitigate | An unmeasurable count is not evidence of absent work; return `Ok(None)` to Layer 3 rather than classifying a successful agent as `Failed` (Task 2, D-09) |
| T-35-03 | Tampering | `NoGitPath` mutation of process-global `PATH` | medium | mitigate | RAII restore in `Drop` on every exit path including panic; the caller holds the crate's single `PATH` mutex; devflow-core's guard is `cfg(test)`-only so no binary can reach two guards under two mutexes (Task 1, F-2) |
| T-35-04 | Information Disclosure | new doc comments and test assertions | low | accept | The corrected comments and new assertions carry no path, credential, or key material; nothing in this plan emits operator-environment data |
| T-35-SC | Tampering | package-manager installs | low | accept | This plan installs no packages. `35-RESEARCH.md` § Standard Stack records zero new dependencies for the phase, and its Package Legitimacy Audit section is marked not-applicable, so the legitimacy gate has no rows to audit |
</threat_model>

<verification>
- `cargo test -p devflow-core --lib agent_result::` and `cargo test -p devflow --lib pipeline_outcomes::`
  each print `test result: ok.` with `0 failed`.
- Both `no_git_path_makes_git_unresolvable_and_restores_it` tests pass, and each was run BEFORE any
  criterion 1 or criterion 6 result was believed (NC-1's ordering requirement).
- NC-2, NC-3, NC-4 and NC-11 each have their opposite-result case run and its output recorded in the
  SUMMARY — asserted from a performed mutation, never from reading the fix.
- `scripts/check.sh all` exits with fmt clean, clippy clean under `-D warnings`, and `0 failed`.
- Record the measured full-suite runtime in `35-VALIDATION.md`'s Test Infrastructure table; it is
  currently marked unmeasured, and assuming a value is exactly the habit this phase exists to break.
</verification>

<success_criteria>
- ROADMAP criterion 1 (999.77) satisfied: the baseline is not overwritten by a measurement failure,
  "could not count" is distinguished from "counted zero", and both doc comments are corrected.
- ROADMAP criterion 6 (999.87) satisfied: `evaluate_layer2` returns `Ok(None)` for the
  exit-code-0 + `Stage::Code` + unrunnable-`git` case, proven by a test that would pass against
  neither the buggy code nor a proxy built on the ordinary zero-commits case.
- The harness exists in both crates with a passing sanity control in each.
</success_criteria>

<output>
Create `.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-01-SUMMARY.md` when done.
Record in it: the NC-1 through NC-4 and NC-11 outputs, the RED output of the two-cycle test against
unmodified code, the F-2 placement decision as executed, and the measured full-suite runtime.
</output>
