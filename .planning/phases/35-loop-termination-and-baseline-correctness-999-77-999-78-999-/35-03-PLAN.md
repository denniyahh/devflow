---
phase: 35-loop-termination-and-baseline-correctness-999-77-999-78-999-
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/devflow-core/src/git.rs
  - crates/devflow-cli/tests/release_check.rs
autonomous: true
requirements: [HARDEN-05]

estimate:
  tokens: 50000
  raw_tokens: 100000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "release --check reports Viable or NotViable from the exit code of a real ssh-keygen -Y sign over a throwaway payload, never from an ssh-add -l fingerprint comparison (D-02, HARDEN-05)"
    - "A configured signing key whose private sibling is on disk and which the agent does not hold reports Viable — the exact live false negative 999.86 was filed for twice"
    - "The probe runs with SSH_ASKPASS_REQUIRE set to never AND under a wall-clock timeout that kills and reaps the child, so neither a passphrase prompt nor a wedged agent can hang an unattended preflight (D-01)"
    - "An inline key:: or raw ssh- signing key returns Unknown with a fixed reason and is never probed, and inline_signing_key_blob is retained because it is what classifies the value as inline (D-03, A-17)"
    - "classify_ssh_add_status and SigningStatus are removed from devflow_core::git together with the single test that references them, and inline_key_fingerprint is removed with its own test as an orphan this phase's own change created (D-04, A-15)"
    - "No NotViable or Unknown reason string contains the configured user.signingkey value, any filesystem path, or any part of ssh-keygen's own stderr (D-02, D-08 redaction contract)"
    - "The signing namespace passed to the probe is the three-byte value git itself writes into a real SSHSIG blob, taken from a git-produced signature rather than from documentation"
    - "The two pre-probe early returns — user.signingkey unset, and a path form whose file does not exist — keep their existing reason strings unchanged (D-02)"
  prohibitions:
    - requirement_id: HARDEN-05
      category: safety
      status: resolved
      verification: judgment
      statement: "The preflight MUST NOT produce a signature over anything but throwaway bytes it generated itself in its own private directory, and MUST NOT write a payload or a signature anywhere inside the operator's repository working tree — a viability check must never become an unauthorised signing operation over real content"
  artifacts:
    - path: "crates/devflow-core/src/git.rs"
      provides: "check_ssh_signing_viability rewritten as a bounded ssh-keygen -Y sign probe, with the ssh-add predictor and its types removed"
      contains: "SSH_SIGN_NAMESPACE"
    - path: "crates/devflow-cli/tests/release_check.rs"
      provides: "the rewritten absent-tooling and inline-key surface tests"
      contains: "ssh-keygen not found"
  key_links:
    - from: "crates/devflow-core/src/git.rs"
      to: "crates/devflow-cli/src/commands.rs"
      via: "check_signing maps the three SigningViability variants onto release --check output; the enum shape is unchanged so this consumer needs no edit"
      pattern: "SigningViability"
---

<objective>
Replace `release --check`'s tag-signing predictor with the operation it was predicting.

Purpose: the predictor infers viability from `ssh-add -l` and has false-negatived live on two
separate release cuts with signing genuinely working. The mechanism is unreliable by construction,
not by circumstance: `ssh-add -l` cannot see on-disk private key material, so agent membership is
not a necessary condition for `git tag -s` to succeed. A probe cannot drift out of sync with git's
real behaviour because it has no independent behaviour — it does the actual operation on disposable
input.

Output: a rewritten `check_ssh_signing_viability`, the deletion of the predictor and its now-orphaned
types and helper, probe fixtures with their negative controls, and the rewritten `release --check`
surface tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-CONTEXT.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-RESEARCH.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-PATTERNS.md
@.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-VALIDATION.md
</context>

<planner_findings>
**Source anchors, all re-verified against current source during planning.** `crates/devflow-core/src/git.rs`:
the enum to remove at `:728`, the classifier to remove at `:743`, `SigningViability` (kept, unchanged)
at `:758`, `public_key_fingerprint` (kept) at `:786`, `inline_signing_key_blob` (kept) at `:814`,
`inline_key_fingerprint` (removed) at `:841`, the function to rewrite at `:874`, the untouched GPG
branch at `:949`, the untouched dispatcher at `:983`. Tests: the classifier's test at `:1829`
(removed), `inline_signing_key_blob_follows_git_prefix_precedence` at `:1914` (**kept** — D-03
retains the classifier, and 20d's prefix precedence must not be re-derived), and
`inline_key_fingerprint_matches_the_path_branch_for_the_same_key` at `:1996` (removed).
`crates/devflow-cli/tests/release_check.rs`: `git_only_path` at `:446`, the two tests to rewrite at
`:462` and `:513`, their assertions at `:488`, `:535` and `:536`.

**Reason-string convention already in the file.** The existing fail-soft reasons use the prefix
"cannot verify signing viability — ". Keep that prefix for the `Unknown` class so the surface tests
and any operator's muscle memory stay consistent; the current absent-tooling reason lives at `:905`
and a second at `:935`.

**Do not widen the scope.** The GPG/openpgp branch is untouched — the backlog scopes this to the SSH
branch only and CONTEXT.md states that scope is not the planner's to widen. `devflow release`'s real
signing executor (DEN-50) is a separate, unaffected item; it must still run the real signed
`git tag` and must never call this probe as a substitute.
</planner_findings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace the ssh-add predictor with a bounded, non-interactive ssh-keygen sign probe</name>
  <reversibility rating="one-way">D-04 removes two `pub` items from `devflow-core`, which is published to crates.io; undo is a re-publication rather than a code revert. No `checkpoint:decision` is emitted — CONTEXT.md D-08 records the operator choosing this break and holding the release at v2.5.0, and a `blocking-human` gate has no answer path under this project's one-shot agent launch model (999.57).</reversibility>
  <precondition>`ssh-keygen` is on PATH on the build host — the probe's own tests generate real keypairs with it. `ssh-keygen -V` should print a version; if it does not, halt and report rather than stubbing the probe.</precondition>
  <files>crates/devflow-core/src/git.rs</files>
  <read_first>
    - `crates/devflow-core/src/git.rs` lines 720-1000 — every symbol named in the findings block
      above, read in full including the doc comments. Those comments record phase 20d's D-01, D-02,
      D-03, D-06, D-08 and D-12 and explain why each branch spawns what it spawns, in what order.
    - `crates/devflow-core/src/canary.rs` — the bounded reap helper, the in-crate precedent for
      giving a blocking child a wall-clock ceiling. Copy the spawn, poll-until-deadline, kill, wait
      shape; do NOT copy the surrounding channel and reader-thread machinery, which a probe that
      needs only an exit code does not require.
    - `crates/devflow-core/src/agent.rs` — `terminate_and_verify`'s deadline loop, the simpler
      second precedent for the same idiom.
    - `crates/devflow-cli/src/commands.rs` — `check_signing`, the sole consumer, so you can confirm
      the enum shape stays an output contract and needs no edit.
    - `35-RESEARCH.md` section F — the verified namespace extraction and the measured behaviour
      table with its stated n=1 limitation.
    - `35-PATTERNS.md` sections 5 and 6 — the reap template and the exact current bodies of every
      symbol being removed or kept.
  </read_first>
  <behavior>
    After this task the function's contract is:

    - `user.signingkey` unset → `NotViable` with the existing reason, unchanged.
    - value classifies as inline via `inline_signing_key_blob` → `Unknown`, not probed (D-03).
    - path form whose file does not exist → `NotViable` with the existing reason, unchanged.
    - probe spawn fails because `ssh-keygen` is absent → `Unknown`, fail-soft, never a hard-fail and
      never a panic.
    - probe exceeds the wall-clock ceiling → child killed and reaped → `NotViable`.
    - probe exits non-zero → `NotViable`.
    - probe exits zero → `Viable` carrying `public_key_fingerprint(key_path)`.

    Exit code is the sole verdict. `ssh-keygen`'s stderr is read by nobody and reproduced nowhere.
  </behavior>
  <action>
    Rewrite the body of `check_ssh_signing_viability` from the agent-listing spawn at `:901` onward.
    Preserve the two pre-probe early returns byte-for-byte: the unset-key branch and the missing-file
    branch
    keep their current reason strings. Insert the inline-form early return between them, positioned
    so classification happens before the path is treated as a filesystem path.

    Add three module constants beside the function: `SSH_SIGN_NAMESPACE` with the value `git`,
    `SSH_SIGN_PROBE_TIMEOUT` of ten seconds, and `SSH_SIGN_PROBE_POLL` of twenty-five milliseconds.
    Document on `SSH_SIGN_NAMESPACE` that the value was decoded byte-for-byte out of a real
    git-produced SSHSIG blob from this repository's own signed tag, and that it must not be
    re-derived from documentation or memory — the probe's entire value is being the operation rather
    than an approximation of it.

    Build the probe workspace with `std::fs::create_dir`, not `create_dir_all`, under the system
    temporary directory with a name unique to this process. `create_dir` failing on an existing path
    is the point: it means a pre-planted directory or symlink cannot redirect where the payload is
    written. `tempfile` is a dev-dependency of this crate and is unavailable to production code, so
    this is std only. If creation fails, return `Unknown` fail-soft. Write a small payload of bytes
    the probe generates itself into that directory. Remove the directory and everything in it on
    every exit path, including the timeout path — the signing subcommand writes its signature as a
    sibling of the payload, so both live and die inside that one directory.

    Spawn `ssh-keygen` with the sign subcommand, the namespace flag carrying `SSH_SIGN_NAMESPACE`,
    the identity flag carrying the configured key path, and the payload path. Set the environment
    variable `SSH_ASKPASS_REQUIRE` to `never` on the command builder before spawning; this is a new
    addition, no existing site in the workspace sets it. Then apply the bounded wait: poll for exit
    until the deadline, and on expiry kill and reap the child. A spawn error is the absent-tooling
    class; a timeout and a non-zero exit are their own classes.

    Map the four failure classes to fixed reason strings keyed by class, never composed from
    `ssh-keygen`'s output. Use the file's existing fail-soft prefix for the two `Unknown` classes.
    The absent-tooling reason must end with the tool name that is now actually being looked for, so
    the surface test in Task 3 can assert on it. Add a comment stating why the stderr is discarded:
    it embeds the configured key path, and reproducing it would violate the redaction contract.

    Then perform D-04's removals in the same change: delete the status enum at `:728`, the exit-code
    classifier at `:743`, and the private inline fingerprint helper at `:841`. Delete the single test
    that asserts the classifier's mappings and the single test that compares the inline fingerprint
    against the path branch. Keep `public_key_fingerprint`, keep `inline_signing_key_blob`, and keep
    `inline_signing_key_blob_follows_git_prefix_precedence`.
    <!-- planner-discipline-allow: classify_ssh_add_status -->
    <!-- planner-discipline-allow: SigningStatus -->
    <!-- planner-discipline-allow: inline_key_fingerprint -->
  </action>
  <verify>
    <automated>cargo build --workspace --all-targets && ! rg -q 'classify_ssh_add_status|SigningStatus|inline_key_fingerprint' crates/</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c 'classify_ssh_add_status' crates/` finds no matches (ripgrep exits 1). Same for `SigningStatus` and for `inline_key_fingerprint`.
    - `rg -n 'inline_signing_key_blob' crates/devflow-core/src/git.rs` still matches, and `cargo test -p devflow-core --lib git::tests::inline_signing_key_blob_follows_git_prefix_precedence -- --exact 2>&1` prints `test result: ok. 1 passed` with a non-zero `filtered out` count — D-03 keeps the classifier and 20d's prefix precedence.
    - The rewritten function no longer reaches the agent-listing tool. Region-scoped, because the tool name legitimately survives in unrelated prose elsewhere in the file: `! awk '/^fn check_ssh_signing_viability/,/^fn check_gpg_signing_viability/' crates/devflow-core/src/git.rs | grep -q 'ssh-add'` succeeds.
    - `rg -n 'SSH_ASKPASS_REQUIRE' crates/devflow-core/src/git.rs` matches, and `rg -n 'create_dir_all' crates/devflow-core/src/git.rs` does not match inside the probe's workspace setup.
    - `rg -n 'fn check_gpg_signing_viability' crates/devflow-core/src/git.rs` still matches and `git diff -- crates/devflow-core/src/git.rs` shows no change within that function — the GPG branch is untouched.
    - The two pre-probe early-return reason strings are unchanged: `git diff` shows no edit to the unset-key branch's reason or the missing-file branch's reason.
    - `cargo build --workspace --all-targets` succeeds, proving no surviving reference to any removed item anywhere in either crate.
    - `cargo clippy --workspace --all-targets -- -D warnings` is clean.
  </acceptance_criteria>
  <done>The SSH branch reports viability from a real, non-interactive, time-bounded signing probe; the predictor, its enum and the orphaned inline helper are gone along with their tests; the GPG branch and both pre-probe early returns are untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Probe fixtures with their two mandatory negative controls</name>
  <precondition>`ssh-keygen` can generate an ed25519 keypair non-interactively on the build host, and `sh` is available for the askpass fixture script used by the blocking control.</precondition>
  <files>crates/devflow-core/src/git.rs</files>
  <read_first>
    - `crates/devflow-core/src/git.rs` — the function as left by Task 1, and its test module's
      existing keypair-generation fixture shape (the removed inline-fingerprint test generated a
      real ed25519 key with an empty passphrase; reuse that generation shape, not its assertions).
    - `35-VALIDATION.md` — NC-9 and NC-10, and the "What the evidence does NOT establish" entry
      recording that the operator's measurements are n=1 on one host, one OpenSSH build, one key
      type, and must not be cited as coverage.
    - `35-CONTEXT.md` Traps — why any fixture asserting `Viable` must not depend on agent membership,
      and why the signing subcommand resolves the private key by stripping the public suffix from
      that same path.
  </read_first>
  <behavior>
    - `ssh_signing_probe_reports_viable_with_on_disk_private_key`: generate a real ed25519 keypair
      with an empty passphrase in a tempdir, point `user.signingkey` at the public half, and assert
      `Viable`. The assertion must not read, set, or depend on agent state in any way — agent
      membership is not a necessary condition for signing, and assuming it was is the false premise
      that produced both live false negatives.
    - `ssh_signing_probe_reports_not_viable_without_a_private_key` (NC-9): the same fixture with the
      private half deleted, leaving only the public file. Must report `NotViable`. Without this the
      positive assertion is vacuously true regardless of key state.
    - `ssh_signing_probe_does_not_block_on_an_encrypted_key` plus its control
      `encrypted_key_blocks_without_the_askpass_require_env_var` (NC-10): generate an encrypted key,
      point an askpass helper script at a sleep long enough to be unmistakable, and run the raw
      command twice. With `SSH_ASKPASS_REQUIRE` set to never the child must exit quickly; with the
      variable omitted and the askpass route otherwise available the child must still be alive after
      a short observation window. The control is what proves the environment variable, and not the
      timeout alone nor the fixture, is what prevents the hang.
    - `inline_signing_key_returns_unknown_without_probing`: an inline key value returns `Unknown`
      with the fixed inline reason, and does so with full tooling present — proving it is never
      probed rather than merely failing to probe.
  </behavior>
  <action>
    Add the tests described in the behavior block to `git.rs`'s test module.

    For the blocking control, drive the raw command rather than the probe, so the observation is of
    the environment variable's effect and not of the probe's own ceiling — otherwise the measured
    interval is pinned by the timeout constant and reports nothing about the variable. Assert
    liveness with a poll rather than a fixed sleep-then-check, and keep the observation window well
    under the probe's ten-second ceiling so the test stays fast.

    If the control's blocking arm does NOT block on the build host, the test must fail loudly rather
    than pass — a control that agrees with its positive case is a broken measurement, not evidence.
    State that in the test's own failure message.

    Add a leak assertion to the positive and negative probe tests, following the existing surface
    test's shape: the returned reason and fingerprint must not contain the key path, must not
    contain private key material, and must not contain any fragment of the tool's own stderr.

    In the plan SUMMARY, record what these fixtures do NOT establish, carried verbatim in substance
    from `35-VALIDATION.md`: they are one host, one OpenSSH build, one key type, and the operator's
    measured table is explicitly not coverage.
  </action>
  <verify>
    <automated>cargo test -p devflow-core --lib git::tests::ssh_signing_probe 2>&1 | grep -E 'test result: ok\. [0-9]+ passed; 0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow-core --lib git::tests::ssh_signing_probe_reports_viable_with_on_disk_private_key -- --exact 2>&1` prints `test result: ok. 1 passed` with a non-zero `filtered out` count.
    - NC-9 is a committed test, not a one-off: `ssh_signing_probe_reports_not_viable_without_a_private_key` passes in the same run, so the positive case cannot be vacuously true.
    - NC-10 is performed and recorded: the SUMMARY carries the observed outcome of both arms — the child exiting promptly with the variable set, and the child still alive in the observation window with it omitted. If the blocking arm did not block, the SUMMARY records that the control failed and the conclusion about the variable is unsupported.
    - No new test depends on agent state. `! rg -q 'SSH_AUTH_SOCK' crates/devflow-core/src/git.rs` succeeds once Task 3 has retired the predictor's reason constants, and none of the five new test bodies invokes the agent-listing tool or asserts on agent contents — a test that touches agent state near a `Viable` assertion has reproduced the original defect's premise.
    - The leak assertions are present: each probe test asserts the rendered reason and fingerprint contain neither the fixture key path nor private key material.
    - `cargo test -p devflow-core --lib git:: 2>&1` prints `test result: ok.` with `0 failed`.
  </acceptance_criteria>
  <done>The probe has a positive case, a negative control that flips it, a blocking control that isolates the environment variable's effect, and an inline-key case proving inline values are never probed — with the stated limits of all of it recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Retire every test and comment whose premise the deletion changed, in both files</name>
  <files>crates/devflow-cli/tests/release_check.rs, crates/devflow-core/src/git.rs</files>
  <read_first>
    - `crates/devflow-cli/tests/release_check.rs` lines 440-562 — `git_only_path`, both tests to
      rewrite, and their assertions. Read the leak-check test near the top of the file for the
      assertion shape any signing surface test should also carry.
    - `crates/devflow-core/src/git.rs` lines 1880-1930, 1955-1995 and 2050-2083 — three surviving
      tests that reference the deleted predictor by its *reason strings* rather than by its symbols,
      so the compiler will not find them. Read all three in full.
    - `crates/devflow-core/src/git.rs` — the fixed reason strings as written in Task 1, so the
      assertions match the strings that actually exist.
    - `crates/devflow-cli/src/commands.rs` — `check_signing`, to confirm which variant maps to which
      status and which carries an install hint; that mapping is the discriminator the surface tests
      assert on.
  </read_first>
  <planner_note>
    **Scope addition found during planning, not present in `35-RESEARCH.md` or `35-PATTERNS.md`.**
    Both documents enumerate the deletion's fallout by searching for the two removed *symbols* and
    conclude that exactly one test is affected. That search is incomplete: three further tests and
    one doc comment reference the predictor through its reason strings and its behaviour, so they
    break **behaviourally at run time rather than at compile time** — the failure mode this phase
    exists to eliminate. `check_signing_viability_never_hard_fails_on_an_unparseable_inline_key`
    (`:2060`) is the clear one: it pins an accepted-reason set built from the no-agent and
    agent-empty reasons, neither of which the code can produce any more. Handle all of them here.
  </planner_note>
  <action>
    Rename `release_check_signing_degrades_when_ssh_add_absent` to name the tool the probe now
    actually needs, and change its assertion from the old tool's absent-reason to the new one's.
    `git_only_path` already produces exactly the right PATH shape — only git resolves — so the
    fixture needs no change; the tool whose absence is now being exercised is simply a different one.

    Rewrite `release_check_inline_signingkey_degrades_to_warn_when_ssh_tooling_absent`. Its premise
    has changed rather than its wording: under D-03 an inline key returns `Unknown` regardless of
    whether any tooling is present, because it is never probed at all. Re-derive its assertions from
    that contract — rename it to say the inline form warns without probing, run it with a normal
    PATH so tooling presence is not what produces the result, and assert the fixed inline reason.
    Do not merely swap the tool name in the old assertion; that would leave a test whose name and
    body claim a tooling-dependence the code no longer has.

    Keep both tests' existing structural assertions that the output contains no panic, and keep the
    file's existing leak-check discipline: no filesystem path and no key material in the output.

    Then handle the three `git.rs` tests the symbol search missed.

    `check_signing_viability_never_hard_fails_on_an_unparseable_inline_key` (`:2060`) pins two
    reason constants the predictor produced and the code can no longer emit. Under D-03 an
    unparseable inline value classifies as inline and returns `Unknown` with the fixed inline reason
    without being probed. Replace the accepted-reason set with that single reason and delete the two
    obsolete constants. Preserve the test's actual guarantee — that this phase introduces no NEW
    hard fail for these values — and preserve its agent-independence note, which is now true by
    construction rather than by argument.

    `check_signing_viability_never_reports_key_file_missing_for_inline_key` (`:1884`) should still
    pass unchanged; confirm that it does rather than assuming it, and only touch it if its accepted
    reasons name a string the code can no longer produce.

    `check_signing_viability_still_reports_missing_file_for_a_path_value` (`:1962`) keeps its
    assertions — that early return is preserved byte-for-byte — but its doc comment justifies itself
    by the spawn this plan deletes. Restate the comment in terms of the mechanism that now follows
    the early return. This is an orphan created by this plan's own change, which the standing rule
    says to clean up; it is not the pre-existing stale-comment backlog item, which stays out of
    scope.
  </action>
  <verify>
    <automated>cargo test -p devflow --test release_check 2>&1 | grep -E 'test result: ok\. [0-9]+ passed; 0 failed'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p devflow --test release_check 2>&1` prints `test result: ok.` with `0 failed`, and the passed count equals the count before this plan (two tests renamed and rewritten, none added or dropped).
    - `rg -n 'ssh-add not found' crates/devflow-cli/tests/release_check.rs` finds no matches.
    - `rg -c 'ssh-keygen not found' crates/devflow-cli/tests/release_check.rs` is at least 1.
    - The inline-key test no longer takes its PATH from `git_only_path`: `rg -n 'git_only_path' crates/devflow-cli/tests/release_check.rs` matches only inside the absent-tooling test and the helper's own definition.
    - Both rewritten tests still assert the output contains no panic and leaks no path or key material.
    - `! rg -q 'SSH_AUTH_SOCK' crates/devflow-core/src/git.rs` succeeds — the predictor's two reason constants are gone from the surviving test.
    - `cargo test -p devflow-core --lib git:: 2>&1` prints `test result: ok.` with `0 failed`, and every one of the three named `check_signing_viability_*` tests appears in the run rather than having been deleted to make the suite green.
    - `scripts/check.sh all` exits with fmt clean, clippy clean under `-D warnings`, and `0 failed`.
  </acceptance_criteria>
  <done>Every test and comment whose premise the deletion changed has been retired or restated in both files, and none was deleted merely to make the suite pass.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this plan CREATES (new — exclude from drift verification):
- `SSH_SIGN_NAMESPACE`, `SSH_SIGN_PROBE_TIMEOUT`, `SSH_SIGN_PROBE_POLL` — module constants,
  `crates/devflow-core/src/git.rs` (private)
- Tests in `git.rs`: `ssh_signing_probe_reports_viable_with_on_disk_private_key`,
  `ssh_signing_probe_reports_not_viable_without_a_private_key`,
  `ssh_signing_probe_does_not_block_on_an_encrypted_key`,
  `encrypted_key_blocks_without_the_askpass_require_env_var`,
  `inline_signing_key_returns_unknown_without_probing`
- Renamed tests in `release_check.rs`: the absent-tooling test and the inline-key test

Symbols this plan CHANGES:
- `devflow_core::git::check_ssh_signing_viability` — private; body rewritten, signature unchanged
- `check_signing_viability_never_hard_fails_on_an_unparseable_inline_key` — test; accepted-reason
  set replaced, its two predictor-era reason constants removed
- `check_signing_viability_still_reports_missing_file_for_a_path_value` — test; doc comment restated,
  assertions unchanged

Symbols this plan REMOVES (deliberate, D-04 / A-15 — record so the removal reads as intentional):
- `devflow_core::git::classify_ssh_add_status` — **`pub`, breaking**
- `devflow_core::git::SigningStatus` — **`pub`, breaking**
- `devflow_core::git::inline_key_fingerprint` — private, orphaned by D-03
- `classify_ssh_add_status_maps_all_three_documented_exit_codes` — test
- `inline_key_fingerprint_matches_the_path_branch_for_the_same_key` — test

Symbols explicitly KEPT that a reader might expect to go: `public_key_fingerprint`,
`inline_signing_key_blob`, `inline_signing_key_blob_follows_git_prefix_precedence`,
`SigningViability` (shape unchanged — it is a de-facto output contract),
`check_gpg_signing_viability`, `check_signing_viability`.

Files this plan creates: none.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DevFlow → `ssh-keygen` child | An external binary handling private key material; its stdout, stderr and exit code cross back into operator-visible output |
| DevFlow → the shared system temporary directory | The probe creates a directory and writes a payload and a signature there; the directory is world-reachable before creation |
| `release --check` → operator terminal | Every reason string is printed; the configured signing key value must never appear in it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-35-11 | Information Disclosure | `NotViable` / `Unknown` reason strings | high | mitigate | Exit code is the sole verdict; the child's stderr is read by nobody and reproduced nowhere; the four reasons are fixed strings keyed by failure class and contain no path and no key material. Enforced by the leak assertions in Task 2 and the surviving leak-check tests in Task 3 (D-02, D-08) |
| T-35-12 | Tampering | probe workspace under the system temp dir | medium | mitigate | Create the directory with the non-recursive constructor that fails when the path already exists, under a name unique to this process, so a pre-planted directory or symlink cannot redirect the payload write; fail soft to `Unknown` when creation fails (Task 1) |
| T-35-13 | Elevation of Privilege | `ssh-keygen` sign invocation | medium | mitigate | Sign only bytes the probe generated itself inside its own private directory; never sign repository content; remove the directory and its contents on every exit path including the timeout path. Recorded as this plan's `must_haves.prohibitions` entry (Task 1) |
| T-35-14 | Denial of Service | `release --check` preflight | high | mitigate | `SSH_ASKPASS_REQUIRE` set to never closes the passphrase-prompt route, and a wall-clock ceiling with kill and reap closes the rest, so no blocking mode can hang an unattended preflight. Both are required — the environment variable alone leaves non-askpass blocking routes open, and the timeout alone would turn a working graphical askpass into a false `NotViable` (D-01) |
| T-35-15 | Spoofing | signing viability verdict | high | mitigate | Replace the predictor with the operation itself, so there is no second implementation of "will signing work" that can drift out of sync with git's real behaviour |
| T-35-SC | Tampering | package-manager installs | low | accept | This plan installs no packages; `35-RESEARCH.md` records zero new dependencies for the phase and marks its Package Legitimacy Audit not applicable. The timeout uses an in-crate precedent rather than a timeout crate specifically so no dependency is added |

**Canon referral (not minted as a prohibition):** "secrets must not reach logs or terminal output" is
canon security and is covered by T-35-11 above plus `/gsd-secure-phase`; it is recorded there rather
than duplicated as a bespoke prohibition.
</threat_model>

<verification>
- `cargo test -p devflow-core --lib git::` prints `test result: ok.` with `0 failed`.
- `cargo test -p devflow --test release_check` prints `test result: ok.` with `0 failed`.
- `cargo build --workspace --all-targets` succeeds with no reference to any removed item.
- NC-9 is a committed passing test; NC-10's two arms have been run and both outcomes recorded.
- `scripts/check.sh all` exits with fmt clean, clippy clean under `-D warnings`, and `0 failed`.
</verification>

<success_criteria>
- ROADMAP criterion 5 (999.86) satisfied: `release --check`'s tag-signing preflight reports from a
  real signing probe over a throwaway payload, not from a fingerprint comparison, closing the
  predictor that produced a live false negative with the correct key present on two release cuts.
- The public-API removals are complete and the workspace builds, ready for the enumeration
  `35-06` records.
</success_criteria>

<output>
Create `.planning/phases/35-loop-termination-and-baseline-correctness-999-77-999-78-999-/35-03-SUMMARY.md` when done.
It must carry NC-10's two observed arms, the leak-assertion results, and the statement of what the
fixtures do not establish (one host, one OpenSSH build, one key type).
</output>
