---
phase: 46-ci-load-shape-and-operator-input-validation
plan: 07
type: tdd
wave: 3
depends_on: ["46-06"]
gap_closure: true
files_modified:
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/test_support.rs
  - crates/devflow-core/src/test_support.rs
  - crates/devflow-core/src/agent_result.rs
autonomous: true
requirements: [INFRA-01]

estimate:
  tokens: 38000
  raw_tokens: 76000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "All five remaining PATH-emptying windows in `pipeline_outcomes.rs` run in a CHILD process, using the helper 46-06 proved. After this plan the count of PATH-emptying windows in the `devflow` bin target is ZERO — that is the precondition 46-08's revert depends on, and it is the reason these five cannot be left for later."
    - "Each migrated test keeps EVERY assertion it makes today, moved rather than dropped. Four of the five assert on a value returned by the call under an unresolvable `git`; those assertions move wholesale into the child. The fifth is different and is handled separately."
    - "`validate_failure_with_unmeasurable_count_accumulates_the_streak` is the hard one and cannot be moved wholesale. It runs TWO cycles against one accumulating `State`: cycle 1 with `git` unresolvable, cycle 2 with a real `git`. The child cannot run cycle 2 (it has no `git`) and the parent cannot run cycle 1 (that is the hazard). The split is: child runs cycle 1, asserts cycle 1's two properties, and persists the resulting `State`; parent reloads it, RE-ASSERTS the same two properties, and runs cycle 2."
    - "The parent's re-assertion after reload is not redundant. It is the control on the round trip: if a field the test depends on does not survive `save_state`/`load_state`, the child would pass and the parent's cycle-2 expectations would be built on a silently different `State`. Asserting the same two properties on the reloaded value turns that into a loud failure at the boundary where it happens."
    - "`NoGitPath` is DELETED once its sixth and last call site is gone, and the deletion is COMPILER-ENFORCED rather than optional: a `pub(crate)` type with no remaining users trips `dead_code`, and `scripts/check.sh clippy` runs `-D warnings`. Its absence is therefore proof that all six `NoGitPath` sites migrated, not merely a tidy-up."
    - "Deleting `NoGitPath` also removes the reason `d525f9a` gave for the 47 locks. Its doc comment is the document of record for the hazard — 'the first guard in the workspace that makes `git` unresolvable process-wide, so any sibling test shelling out to `git` inside the guarded window fails spuriously'. With the type gone, that warning must not be left standing anywhere as though it still applied."
    - "Prose references to `NoGitPath` outside the deleted type are found and corrected. MEASURED: five in `pipeline_outcomes.rs` doc comments, three in `crates/devflow-core/src/agent_result.rs` (which explains why it chose an unspawnable working directory INSTEAD of `NoGitPath`), and two in `crates/devflow-core/src/test_support.rs` (which explains why that crate has no such harness). A symbol search does not find these — they are prose, and prose that describes a deleted mechanism as current is how a future reader reintroduces it."
    - "Every migrated test is proven to have RUN. The helper's three-part check — `test <name> ... ok`, a `1 passed` summary, and a NON-ZERO `filtered out` count — applies unchanged. A libtest filter that matches nothing exits 0, so an exit-status check alone would let a typo in any of the five names read as a pass."
    - "All five migrated tests DROP their `let _guard = env_lock();`. None of those five holders is one of `d525f9a`'s 47 additions — measured: `d525f9a`'s three `pipeline_outcomes.rs` additions land in `terminal_hook_failure_stops_before_branch_cleanup`, `run_checkout_hooks_keeps_changelog_in_sync_with_tag_when_no_version_file` and `handle_ship_outcome_with_yes_ship_auto_approves_exactly_once_with_attribution`, all of them SIBLING victims rather than the hazard itself."
  prohibitions:
    - "Must NOT revert any of `d525f9a`'s 47 `env_lock` lines here. 46-08 owns that, and it is only safe once this plan lands."
    - "Must NOT touch the `neutral_path_dir` sites in this file. `NeutralPath` installs a `PATH` that still contains a real `git`, so it is not the hazard. MEASURED: `pipeline_outcomes.rs` carries several such sites; none is one of the five."
    - "Must NOT weaken any assertion during the move. In particular keep the paired positive/negative assertions the existing tests make — `evaluate_layer2_unrunnable_git_falls_through_to_layer3` asserts both `is_none()` AND `assert_ne!(.., Some(Failed))` on purpose, so that a future change returning some other classification still has to confront the test."
    - "Must NOT let the two-cycle test's cycle 1 run in the parent 'just for the state'. That is the hazard verbatim."
    - "Must NOT delete `NoGitPath` before the fifth site is migrated — the intermediate build would not compile, and a plan that cannot be verified between tasks cannot report which task broke it."
    - "Must NOT reduce the child-ran check to `status.success()`."
    - "Must NOT use `cargo test -p devflow --lib`; `devflow` is binary-only. Use `-p devflow --bin devflow`. `-p devflow-core --lib` IS valid for the core crate."
    - "Must NOT accept a bare `cargo test ... --exact <name>` as evidence — pair `N passed` with a non-zero `filtered out`."
    - "Must NOT use `rg -c <pat> | rg '^0$'` as a zero-check. It exits 1 on both a green and a red input."
    - "Must NOT count occurrences over raw source when proving a construct is gone. Strip comment lines first: this repo has already had a region check report a surviving call that existed only inside a comment."
    - "Must NOT edit `.github/workflows/ci.yml`, `scripts/**` or `crates/devflow-cli/tests/**` (46-04 and 46-05 own those), and must NOT edit `crates/devflow-cli/src/pipeline_launch.rs` (46-06 owns it)."
    - "Must NOT edit `CLAUDE.md` — untracked on develop, and the hook refuses it on a `feature/*` branch."
  artifacts:
    - crates/devflow-cli/src/pipeline_outcomes.rs
    - crates/devflow-cli/src/test_support.rs
  key_links:
    - "Task 3's deletion of `NoGitPath` is what MAKES Tasks 1 and 2 verifiable. Until the type is gone, 'all six sites migrated' is a claim about a grep; once it is gone, the compiler enforces it, because a `pub(crate)` type with no users trips `dead_code` under `-D warnings`. Run the tasks in order and do not fold Task 3 forward."
    - "The four `evaluate_*` tests share one fixture shape: a tempdir, `.devflow/` created, an exit-code file written, then the call under an unresolvable `git`. Only `evaluate_agent_result_with_unrunnable_git_does_not_report_failed` additionally needs `init_repo` and `commit_on_feature_branch`, which shell out to git and must therefore stay in the parent. Its existing comment already says the repo is built BEFORE the guard goes on — that ordering constraint becomes the process boundary."
    - "`crates/devflow-core/src/agent_result.rs:7867-7885` argues at length for why it used an unspawnable working directory RATHER than `NoGitPath`, and flags `NoGitPath` as a latent fragility. That passage becomes the record of a type that no longer exists. Rewrite it to describe the current mechanism; do not delete the reasoning, which is still the reason that test is shaped the way it is."
---

<!-- Comment-text discipline: every negative grep in this plan is scoped to a SOURCE tree
     (crates/, scripts/, .github/) and never to .planning/, so a literal appearing in this
     plan's prose cannot satisfy or invalidate it. The identifiers below have to be named
     to be actionable. -->
<!-- planner-discipline-allow: NoGitPath::install -->
<!-- planner-discipline-allow: NoGitPath -->

<objective>
Finish removing process-global `PATH` emptying from the `devflow` bin target, and let the compiler
prove it.

Purpose: 46-06 built and proved the child-process mechanism and migrated two of the seven sites.
Five remain, all in `pipeline_outcomes.rs`, and they are what keeps `NoGitPath` — and therefore the
whole hazard C-01 describes — alive. Once the last one moves, the type becomes dead code under
`-D warnings`, so deleting it is forced rather than chosen, and its absence is a mechanical proof
that no PATH-emptying window remains. That proof is the precondition for 46-08's revert of the 47
`env_lock` lines.

Output: five migrated tests, `NoGitPath` deleted, and the prose across both crates that describes it
as current brought back into line with the code.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-REVIEWS.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-06-SUMMARY.md
@CLAUDE.md
@crates/devflow-cli/src/pipeline_outcomes.rs
@crates/devflow-cli/src/test_support.rs
</context>

## The five sites this plan owns

| # | Line | Test | Fixture needs `git`? | Shape |
|---|---|---|---|---|
| 3 | 2418 | `validate_failure_with_unmeasurable_count_accumulates_the_streak` | yes (cycle 2) | two cycles, accumulating `State` — Task 2 |
| 4 | 3224 | `evaluate_layer2_unrunnable_git_falls_through_to_layer3` | no | single call, assert on return — Task 1 |
| 5 | 3290 | `evaluate_layer2_unrunnable_git_still_classifies_exit_137_as_resource_killed` | no | single call — Task 1 |
| 6 | 3343 | `evaluate_layer2_unrunnable_git_keeps_success_for_a_non_commit_gated_stage` | no | single call — Task 1 |
| 7 | 3413 | `evaluate_agent_result_with_unrunnable_git_does_not_report_failed` | yes (`init_repo`) | single call, git-built fixture — Task 1 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migrate the four single-call `evaluate_*` sites</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <behavior>
Each of the four keeps its assertions exactly, relocated into the child half:

- `evaluate_layer2_unrunnable_git_falls_through_to_layer3` — child asserts BOTH `result.is_none()`
  and `assert_ne!(result.map(status), Some(Failed))`. Both, not either: the second exists so a
  future change returning a different non-`None` classification still confronts the test.
- `evaluate_layer2_unrunnable_git_still_classifies_exit_137_as_resource_killed` — child asserts the
  137 classification survives an unmeasurable commit count.
- `evaluate_layer2_unrunnable_git_keeps_success_for_a_non_commit_gated_stage` — child asserts
  `Success`, `commits == None`, and the `decide_action` outcome for `Stage::Validate`.
- `evaluate_agent_result_with_unrunnable_git_does_not_report_failed` — child asserts `status`
  is not `Failed`, IS `Unknown`, `decided_by_layer == Some(3)`, and `commits == None`. The
  `decided_by_layer` assertion is load-bearing: without it the test can pass for the wrong reason,
  which is why it is there.

The parent half in every case builds the fixture, spawns the child, and asserts the child ran
exactly one passing test. For the fourth, `init_repo` and `commit_on_feature_branch` stay in the
parent — they shell out to git.

The pre-call sanity assertions stay in the PARENT where they read the filesystem (the exit file
exists; `stdout_path` does not), because they need no `git` and a parent-side failure is easier to
attribute than one buried in captured child output.
  </behavior>
  <action>
Rewrite each of the four to the two-half shape from 46-06, using `child_no_git_root()`,
`run_test_without_git` and `assert_child_ran_exactly_one_passing_test`.

Take the fixture root in the child from `child_no_git_root()`. Do not recompute it and do not create
a second tempdir in the child — the child must operate on the fixture the parent built, or the test
measures a different filesystem than the one it set up.

Delete each test's `let _guard = env_lock();` and its `NoGitPath::install()` binding. The lock
guarded a mutation that no longer happens.

Rewrite the doc comments that explain the guard. Three of them currently reason about `NoGitPath`
directly — one says it is 'unavoidable here' unlike the Layer 3 tests, another says the test uses
the 'same guard against the same fixture shape'. Both statements are about a mechanism being
removed. Preserve what each comment says about WHY the test exists and what it would otherwise pass
for; replace only the mechanism description.

Verify after each of the four, not once at the end. Four rewrites landing together with one red
suite gives no information about which one broke.

For at least one of the four, run the failing direction deliberately: invert an assertion in the
child, confirm the parent reports a failing child WITH the child's own assertion message visible,
then restore it. Record what it printed. A migration whose failing direction has never been observed
is not known to be testing anything, and this repo has already had a proof attempt HANG instead of
failing — which is a different observation and not evidence.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --bin devflow evaluate_layer2_unrunnable_git 2>&1 | tee /tmp/46-07-t1a.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep -E "test result: ok\. 3 passed" /tmp/46-07-t1a.log; echo "three_passed_grep_exit=$?"; grep -oE "[0-9]+ filtered out" /tmp/46-07-t1a.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --bin devflow evaluate_agent_result_with_unrunnable_git_does_not_report_failed 2>&1 | tee /tmp/46-07-t1b.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep -E "test result: ok\. 1 passed" /tmp/46-07-t1b.log; echo "one_passed_grep_exit=$?"; grep -oE "[0-9]+ filtered out" /tmp/46-07-t1b.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; f=crates/devflow-cli/src/pipeline_outcomes.rs; code=$(grep -v "^[[:space:]]*//" "$f"); n=$(printf "%s\n" "$code" | grep -c "NoGitPath::install" || true); echo "remaining_install_sites=$n"; test "$n" -eq 1; echo "exactly_one_left_exit=$?"'</automated>
  </verify>
  <done>
The three `evaluate_layer2_unrunnable_git_*` tests report a literal `3 passed` with a non-zero
`filtered out`, and the end-to-end one reports `1 passed` the same way. Exactly ONE guard
installation remains in non-comment lines of the file — the two-cycle test, which is Task 2. That
count is asserted as `-eq 1` rather than `-eq 0` on purpose: at this point in the plan a zero would
mean Task 2's site was migrated out of order and Task 2's own verification would have nothing left
to discriminate. The deliberate-failure probe was run on at least one migration and its output
recorded.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Split the two-cycle test across the process boundary</name>
  <files>crates/devflow-cli/src/pipeline_outcomes.rs</files>
  <behavior>
`validate_failure_with_unmeasurable_count_accumulates_the_streak` becomes a three-stage test:

- PARENT, before: build the tempdir and repo, set `last_validate_failure_commit_count = Some(1)` and
  `consecutive_failures = 1`, save the state, and seed the Validate gate response. Spawn the child.
- CHILD: run cycle 1 — `handle_validate_outcome(root, &mut state, ValidateOutcome::Failed)` — then
  assert cycle 1's two existing properties: the baseline is still `Some(1)` (a cycle whose commit
  count could not be measured must leave the last real observation byte-identical, because
  overwriting it with a forged zero IS 999.77), and `consecutive_failures` is 2 (an unmeasurable
  count is not evidence of forward progress). Then persist the resulting `State`.
- PARENT, after: assert the child ran exactly one passing test; reload the state; RE-ASSERT the same
  two properties on the reloaded value; then restore `stage = Validate`, re-seed the gate response,
  and run cycle 2 with a real `git`, keeping cycle 2's existing assertions unchanged.

The parent's re-assertion is a control, not a duplicate: it is what catches a field that does not
survive the `save_state`/`load_state` round trip. Without it the child could pass while the parent
built cycle 2 on a silently different `State`.
  </behavior>
  <action>
Rewrite the test to the three-stage shape. Persist the child's post-cycle-1 `State` with the same
`workflow::save_state` the test already uses, and reload it in the parent with
`workflow::load_state(root, phase)`. Do not invent a side-channel file format; the state file is the
existing, tested carrier and using anything else adds a second thing that can be wrong.

Read the round trip before trusting it: confirm that `last_validate_failure_commit_count` and
`consecutive_failures` are genuinely serialised (neither skipped nor defaulted) before writing the
parent's re-assertion. If either does not survive, STOP and report rather than working around it —
a field the state file drops is a finding in its own right and is not this plan's to paper over.

Note that the loop-back inside `handle_validate_outcome` moves the stage to Code and cleans up the
Validate gate; the existing test restores both before cycle 2 and the rewrite must keep doing so, in
the parent, after the reload.

Delete the test's `let _guard = env_lock();` and its `NoGitPath::install()` binding. Rewrite the doc
comment paragraph that contrasts the two cycles' guards — the contrast is now child-versus-parent,
not one guard type versus another — while keeping what it says about why two cycles are needed.

Run the failing direction: make the child assert the baseline is `Some(0)` instead of `Some(1)`,
confirm the parent surfaces a failing child with that message, restore it, and record the output.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --bin devflow validate_failure_with_unmeasurable_count_accumulates_the_streak 2>&1 | tee /tmp/46-07-t2.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep -E "test result: ok\. 1 passed" /tmp/46-07-t2.log; echo "one_passed_grep_exit=$?"; grep -oE "[0-9]+ filtered out" /tmp/46-07-t2.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; f=crates/devflow-cli/src/pipeline_outcomes.rs; code=$(grep -v "^[[:space:]]*//" "$f"); n=$(printf "%s\n" "$code" | grep -c "NoGitPath::install" || true); echo "remaining_install_sites=$n"; test "$n" -eq 0; echo "all_migrated_exit=$?"; m=$(printf "%s\n" "$code" | grep -c "set_var(\"PATH\"" || true); echo "path_setvar_sites=$m"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --bin devflow 2>&1 | tail -8; echo "cargo_exit=${PIPESTATUS[0]}"'</automated>
  </verify>
  <done>
The two-cycle test passes with `1 passed` and a non-zero `filtered out`, with cycle 1 in a child and
cycle 2 in the parent. Zero guard installations remain in non-comment lines of the file. The
round-trip fields were checked before being relied on, and the parent re-asserts them after reload.
The whole bin suite is green — `NoGitPath` is now defined and unused, which Task 3 turns into a
compiler error.
  </done>
</task>

<task type="auto">
  <name>Task 3: Delete `NoGitPath` and correct the prose that still describes it</name>
  <files>crates/devflow-cli/src/test_support.rs, crates/devflow-cli/src/pipeline_outcomes.rs, crates/devflow-core/src/test_support.rs, crates/devflow-core/src/agent_result.rs</files>
  <action>
Before deleting anything, run `cargo clippy --workspace --all-targets -- -D warnings` and confirm it
FAILS with a `dead_code` diagnostic naming the now-unused type. That failure is the compiler
certifying that every one of the six call sites migrated; a clean clippy at this point would mean a
call site was missed and the whole premise of 46-08's revert is unsound. Record the diagnostic.

Then delete the `NoGitPath` struct, its `impl` and its `Drop` from
`crates/devflow-cli/src/test_support.rs`. Remove any import that becomes unused as a result.

Find and correct every remaining PROSE reference. A symbol search will not find these — they are
doc comments and message text, and this repo has already shipped a break where a deleted item was
still referenced through strings that compile fine and fail at run time. Search both crates for the
name as plain text. The known set:

- `crates/devflow-cli/src/pipeline_outcomes.rs` — doc comments on the migrated tests that name the
  guard. Rewrite them to describe the child-process boundary.
- `crates/devflow-core/src/agent_result.rs` — a passage arguing why that test used an unspawnable
  working directory RATHER than the guard, and flagging the guard as a latent fragility. Keep the
  reasoning; it is still why that test is shaped as it is. Update it to say the fragility was
  resolved by moving the window into a child process, and that the alternative it chose remains
  valid.
- `crates/devflow-core/src/test_support.rs` — a section explaining why THIS crate has no
  absent-`git` harness. Its premise was that the cli crate had one. Rewrite it to name the
  child-process approach as the mechanism a future core-crate need should copy.

Also correct the `NoGitPath` warning wherever it survives — the claim that a sibling test shelling
out to `git` inside the window fails spuriously. That is no longer true of anything in the tree, and
leaving it standing is how the hazard gets reintroduced by someone reading it as current guidance.

Do not weaken `NeutralPath` or any `neutral_path_dir` site; those install a `PATH` that still
contains a real `git` and are a different mechanism.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; n=$(grep -rn "NoGitPath" crates/ | grep -c . || true); echo "surviving_text_refs_in_crates=$n"; test "$n" -eq 0; echo "prose_clean_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -6; echo "clippy_exit=${PIPESTATUS[0]}"; cargo fmt --check 2>&1 | tail -3; echo "fmt_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test --workspace --no-fail-fast 2>&1 | tee /tmp/46-07-t3.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/46-07-t3.log; f=$(grep -c "test result: FAILED" /tmp/46-07-t3.log || true); echo "failed_binaries=$f"; test "$f" -eq 0; echo "suite_green_exit=$?"'</automated>
  </verify>
  <done>
The type is gone and the name appears nowhere under `crates/` — including in prose, which a symbol
search would have missed. Clippy was observed FAILING on `dead_code` before the deletion and passing
after; that before/after pair is the measurement, because a clean clippy beforehand would have meant
a call site was missed. `cargo test --workspace --no-fail-fast` is green with zero failing binaries,
using `--no-fail-fast` deliberately so a failure in `devflow-core` does not hide one in
`devflow-cli`.
  </done>
</task>

</tasks>

<verification>
- `cargo test --workspace --no-fail-fast` — green.
- `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` — green.
- Zero PATH-emptying windows remain in the `devflow` bin target. This is the stated precondition of
  46-08; the SUMMARY must assert it as a measured fact rather than as a conclusion.
- `d525f9a`'s 47 `env_lock` lines are still present and untouched.
</verification>

<success_criteria>
- All five remaining sites migrated; seven of seven done across 46-06 and 46-07.
- `NoGitPath` deleted, with the pre-deletion `dead_code` failure recorded as the proof that nothing
  still used it.
- No text reference to the deleted type survives under `crates/`, prose included.
- The two-cycle test's split is real: cycle 1 in a child, cycle 2 in the parent, with the state round
  trip checked before it is relied on.
</success_criteria>

<output>
Create `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-07-SUMMARY.md` when done.

Record: the clippy `dead_code` diagnostic seen before deletion; the per-test `N passed` /
`filtered out` pairs; what the deliberate-failure probes printed; and an explicit statement that
zero PATH-emptying windows remain, since 46-08 acts on that.
</output>
