---
phase: 46-ci-load-shape-and-operator-input-validation
plan: 08
type: execute
wave: 4
depends_on: ["46-07"]
gap_closure: true
files_modified:
  - crates/devflow-cli/src/commands.rs
  - crates/devflow-cli/src/parallel.rs
  - crates/devflow-cli/src/pipeline_gate.rs
  - crates/devflow-cli/src/pipeline_launch.rs
  - crates/devflow-cli/src/pipeline_outcomes.rs
  - crates/devflow-cli/src/preflight.rs
  - crates/devflow-cli/src/staleness.rs
autonomous: true
requirements: [INFRA-01]

estimate:
  tokens: 21000
  raw_tokens: 42000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "The 47 `let _guard = env_lock();` lines `d525f9a` added are REMOVED, and only those 47. MEASURED: `d525f9a` is purely additive — 47 insertions, 0 deletions, across seven files (`commands.rs` 7, `parallel.rs` 2, `pipeline_gate.rs` 5, `pipeline_launch.rs` 1, `pipeline_outcomes.rs` 3, `preflight.rs` 14, `staleness.rs` 17)."
    - "The pre-existing holders are LEFT ALONE, per the operator disposition. They guard genuine environment mutation, not this hazard. MEASURED before any of 46-06/46-07 ran: 135 total `let _guard = env_lock();` lines under `crates/`, of which 47 came from `d525f9a`, leaving 88 pre-existing."
    - "Seven of those 88 were removed by 46-06 and 46-07 as part of migrating the seven PATH-emptying tests, which no longer mutate anything. The expected post-revert total is therefore 81, not 88. Verify the arithmetic against the tree rather than against this sentence — 46-06 and 46-07's SUMMARYs record what they actually removed, and if the number disagrees the discrepancy is the finding."
    - "The revert is SAFE ONLY BECAUSE the hazard is gone. Reverting while any PATH-emptying window survived would reintroduce the flake `d525f9a` was written to suppress. 46-07's SUMMARY asserts zero remaining windows as a measured fact; this plan reads that assertion and re-measures it rather than trusting it."
    - "`d525f9a`'s commit message is CORRECTED on the record. It claims 'Fixed as a class, not as this instance.' C-01 established that claim is false — the scan matched only DIRECT git spawns, so 31 further tests reaching git through one of 31 spawning helpers were never matched, and codex traced one end to end. The commit cannot be edited (it is already pushed), so the correction is recorded in this plan's SUMMARY, which is the phase's document of record."
    - "The suite is re-run UNDER CONTENTION, not on an idle machine. `d525f9a`'s own measurement notes that four earlier idle runs 'were all green and discriminated nothing'; the failure only appeared under `taskset -c 0,1` with competing load. A green idle run after this revert would be the same non-measurement."
    - statement: "A green contended run does NOT establish that the flake is gone. `d525f9a` measured 1 failure in 8 runs without its fix, and it recorded honestly that 0-of-8 against a 12.5% baseline has roughly a one-in-three chance of occurring with no fix at all. The same arithmetic binds this plan in the other direction: a handful of green contended runs is a weak bound. The load-bearing argument here is MECHANICAL — the process boundary makes the overlap impossible by construction, in a way the mutex only made improbable-by-serialisation — and the runs are corroboration, not proof."
      verification: backstop
    - "The three specific tests named in the flake record are exercised: `wr01_clean_tree_strict_ancestor_build_is_stale_and_hard_blocks` (Phase 34's deferred-items §2, and the instance `d525f9a` was prompted by), and the transitive case codex traced — `preflight.rs:2557` reaching a git spawn through `reachability_fixture`, which shared a process with a PATH-emptying window in `pipeline_outcomes.rs`."
  prohibitions:
    - "Must NOT remove any `env_lock()` line that predates `d525f9a`. The operator disposition is explicit: leave the pre-existing holders alone. Removing one silently widens the change into unrelated environment mutation this phase never analysed."
    - "Must NOT run this plan before 46-07 reports zero remaining PATH-emptying windows. The dependency is not bookkeeping — reverting first reintroduces the flake."
    - "Must NOT accept an idle-machine green run as evidence. Four such runs were already recorded as discriminating nothing."
    - "Must NOT claim the flake is eliminated on the strength of the run counts. State the mechanical argument as the load-bearing one and report the counts as the weak bound they are."
    - "Must NOT amend or force-push `d525f9a`. It is on a pushed branch; the correction goes in the SUMMARY."
    - "Must NOT adopt `cargo nextest`, and must NOT change `scripts/check.sh` or `.github/workflows/ci.yml`. Nextest was measured and deliberately deferred as backlog: identical test sets, 76s vs 78s (a wash), zero doctests — but `devflow::phase7_cli suite_reap_audit` is STRUCTURALLY incompatible, being a cross-test invariant auditor that must observe siblings in one process and that reports a false leak under isolation, plus 6 further tests flagged leaky and untraced."
    - "Must NOT use `rg -c <pat> | rg '^0$'` as a zero-check. It exits 1 on a green input and a red one alike."
    - "Must NOT use `cargo test -p devflow --lib`; `devflow` is binary-only. Use `-p devflow --bin devflow`, or `--workspace --no-fail-fast` for the whole suite."
    - "Must NOT edit `.github/workflows/ci.yml`, `scripts/**`, `crates/devflow-cli/tests/**` or `crates/devflow-core/**`. Earlier plans own those."
    - "Must NOT edit `CLAUDE.md` — untracked on develop, and the hook refuses it on a `feature/*` branch."
  artifacts:
    - crates/devflow-cli/src/commands.rs
    - crates/devflow-cli/src/parallel.rs
    - crates/devflow-cli/src/pipeline_gate.rs
    - crates/devflow-cli/src/pipeline_launch.rs
    - crates/devflow-cli/src/pipeline_outcomes.rs
    - crates/devflow-cli/src/preflight.rs
    - crates/devflow-cli/src/staleness.rs
  key_links:
    - "`git show d525f9a -- '*.rs' | git apply --reverse --check -` succeeded cleanly at planning time, but 46-06 and 46-07 edit two of the seven files afterwards, so it may no longer apply by the time this plan runs. Try it first; if it fails, fall back to removing the 47 lines individually, guided by `git show d525f9a` as the authoritative list. Do NOT use `git revert`, which would also try to reverse the commit message and create a merge-shaped record for a purely additive change."
    - "None of `d525f9a`'s 47 additions lands in one of the seven PATH-emptying tests. MEASURED: its three `pipeline_outcomes.rs` additions are in `terminal_hook_failure_stops_before_branch_cleanup`, `run_checkout_hooks_keeps_changelog_in_sync_with_tag_when_no_version_file` and `handle_ship_outcome_with_yes_ship_auto_approves_exactly_once_with_attribution`; its single `pipeline_launch.rs` addition is in `code_unknown_does_not_transition_to_validate`. All are SIBLING victims. This is why the two edit sets do not overlap and why the revert is a clean subtraction."
    - "`scripts/check-in-container.sh` runs the suite under the same `taskset -c 0,1` pin CI uses, sourced from `scripts/lib/ci-cpus.sh`. It is the right instrument for the contended re-run because it reproduces the load shape the flake needed, rather than approximating it."
---

<objective>
Remove the 47 mutex acquisitions that are no longer load-bearing, and say plainly what the evidence
for their removal does and does not support.

Purpose: `d525f9a` added `let _guard = env_lock();` to 47 tests so they could not overlap a
PATH-emptying window, and its message claimed the class was fixed. C-01 established it closed 47 of
at least 78 — tests reaching git through a helper were never matched. The operator's disposition
rejects widening that sweep in favour of removing the hazard, which 46-06 and 46-07 did. With no
process-global `PATH` mutation left in the bin target, the 47 locks guard nothing; they serialise
tests against a window that no longer exists, and they encode a claim that was not true.

Output: the 47 lines removed, the 81 legitimate holders untouched, a contended re-run, and a SUMMARY
that corrects the record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-REVIEWS.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-06-SUMMARY.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-07-SUMMARY.md
@CLAUDE.md
</context>

## Measured baselines, taken before 46-06 ran

| Fact | Value | How measured |
|---|---|---|
| `d525f9a` insertions / deletions in `*.rs` | 47 / 0 | `git show --numstat d525f9a` |
| Per file | commands 7, parallel 2, pipeline_gate 5, pipeline_launch 1, pipeline_outcomes 3, preflight 14, staleness 17 | same |
| Total `let _guard = env_lock();` under `crates/` | 135 | `grep -rc` summed |
| Pre-existing holders | 88 | 135 − 47 |
| Of those 88, sitting in the seven PATH-emptying tests | 7 | inspected each of the seven |
| Expected total after 46-06, 46-07 and this revert | 81 | 135 − 47 − 7 |
| Reverse patch applied cleanly at planning time | yes | `git show d525f9a -- '*.rs' \| git apply --reverse --check -` |

<tasks>

<task type="auto">
  <name>Task 1: Confirm the precondition, then remove exactly the 47 lines</name>
  <files>crates/devflow-cli/src/commands.rs, crates/devflow-cli/src/parallel.rs, crates/devflow-cli/src/pipeline_gate.rs, crates/devflow-cli/src/pipeline_launch.rs, crates/devflow-cli/src/pipeline_outcomes.rs, crates/devflow-cli/src/preflight.rs, crates/devflow-cli/src/staleness.rs</files>
  <precondition>46-07 has landed and reports zero remaining PATH-emptying windows in the `devflow` bin target. Re-measure it here; do not take the SUMMARY's word for it. If any window survives, HALT — reverting the locks while one remains reintroduces the flake they were added to suppress.</precondition>
  <action>
First, re-measure the precondition independently. Count, over comment-stripped source under
`crates/`, any surviving construct that points `PATH` at a directory with no `git` in it: the
deleted guard type by name, and a direct `set_var` on `PATH` outside the `NeutralPath` sites (which
install a `PATH` that still contains a real `git` and are legitimately unaffected). Strip comment
lines before counting — a raw count counts prose, and this repo has already had a region check
report a surviving call that existed only inside a comment.

Then remove the 47 lines. Try the reverse patch first:
`git show d525f9a -- '*.rs' | git apply --reverse --check -`, and apply it if the check passes. If
it does not — 46-06 and 46-07 edited two of the seven files, so drift is expected — remove the lines
individually, using `git show d525f9a` as the authoritative list of which line sits in which test.
Do not use `git revert`; the change is purely additive and a revert record would misdescribe it.

Then prove the subtraction was exactly right, in both directions:
- the count of removed lines is exactly 47, and the diff for this change contains ZERO additions;
- the seven files touched are exactly the seven `d525f9a` touched;
- the remaining total matches what 46-06's and 46-07's SUMMARYs say they removed. Reconcile against
  those recorded numbers, not against the table above. If they disagree, the discrepancy is the
  finding and it is reported rather than resolved by adjusting the expectation.

Remove any import that becomes unused as a result — clippy runs with `-D warnings`.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; code=$(grep -rn --include=*.rs "" crates/ | grep -v ":[[:space:]]*//"); n=$(printf "%s\n" "$code" | grep -c "NoGitPath" || true); echo "surviving_guard_refs=$n"; test "$n" -eq 0; echo "precondition_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; git diff --numstat -- "crates/devflow-cli/src/*.rs" | tee /tmp/46-08-numstat.txt; added=$(awk "{a+=\$1} END{print a+0}" /tmp/46-08-numstat.txt); removed=$(awk "{d+=\$2} END{print d+0}" /tmp/46-08-numstat.txt); files=$(wc -l < /tmp/46-08-numstat.txt); echo "added=$added removed=$removed files=$files"; test "$added" -eq 0; echo "no_additions_exit=$?"; test "$removed" -eq 47; echo "exactly_47_removed_exit=$?"; test "$files" -eq 7; echo "seven_files_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; total=$(grep -rc "let _guard = env_lock();" crates/ --include=*.rs | awk -F: "{s+=\$2} END{print s+0}"); echo "remaining_env_lock_holders=$total"; test "$total" -eq 81; echo "expected_81_exit=$?"; echo "NOTE: reconcile against 46-06/46-07 SUMMARY counts, not against this literal"'</automated>
  </verify>
  <done>
No reference to the deleted guard type survives in comment-stripped source under `crates/`, so the
precondition was re-measured rather than assumed. The working diff over `crates/devflow-cli/src`
shows exactly 47 deletions, ZERO additions, across exactly 7 files. Zero additions is the control: a
revert that also introduced something would show a non-zero add count and this criterion fails. The
remaining holder count is 81 and it reconciles with what 46-06 and 46-07 recorded removing; if it
does not, the mismatch is reported and the task HALTS rather than adjusting the expectation to match
the tree.
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run under contention and state the strength of the result</name>
  <files>crates/devflow-cli/src/staleness.rs, crates/devflow-cli/src/preflight.rs</files>
  <action>
Re-run the suite under the same load shape the flake needed. An idle green run is not evidence:
`d525f9a` recorded four idle runs, on host and in the pinned container, with and without its fix,
and all four were green and discriminated nothing.

Use `scripts/check-in-container.sh test`, which pins to the CPU list from `scripts/lib/ci-cpus.sh` —
the same pin CI's sequential job applies. Add competing CPU load for the duration so the pinned
cores are genuinely contended, matching how `d525f9a` produced its one observed failure. Run it more
than once; record the exact number of runs and the exact number of failures. Do not round, and do
not describe the outcome in words where a count exists.

Then run the specific tests the flake record names, and confirm they pass under the same contention:
`wr01_clean_tree_strict_ancestor_build_is_stale_and_hard_blocks` (Phase 34's deferred-items §2, and
the instance that prompted `d525f9a`), and the transitive case codex traced — the `preflight.rs`
test that reaches a git spawn through `reachability_fixture`, which previously shared a process with
a PATH-emptying window.

Write the strength of the result, not just the result. N consecutive green runs is a weak bound;
`d525f9a`'s own measurement puts the unfixed failure rate near one in eight, so a small number of
green runs is consistent with no fix at all. Say which conclusion the evidence does NOT carry, and
say that the load-bearing argument is mechanical: the process boundary makes the overlap impossible
by construction, where the mutex made it improbable by serialisation. Those are different claims
with different strengths and the SUMMARY must not merge them.

Also record explicitly, in the SUMMARY, that `d525f9a`'s commit message claim — "Fixed as a class,
not as this instance" — is false as written, and why: the scan matched direct git spawns only, so 31
tests reaching git through one of 31 spawning helpers were never matched. The commit is pushed and
is not being amended, so the SUMMARY is the correction.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test --workspace --no-fail-fast 2>&1 | tee /tmp/46-08-suite.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/46-08-suite.log; f=$(grep -c "test result: FAILED" /tmp/46-08-suite.log || true); echo "failed_binaries=$f"; test "$f" -eq 0; echo "suite_green_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --bin devflow wr01_clean_tree_strict_ancestor_build_is_stale_and_hard_blocks 2>&1 | tee /tmp/46-08-wr01.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep -E "test result: ok\. 1 passed" /tmp/46-08-wr01.log; echo "one_passed_grep_exit=$?"; grep -oE "[0-9]+ filtered out" /tmp/46-08-wr01.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; runs=0; fails=0; for i in 1 2 3; do log=/tmp/46-08-contended-$i.log; for c in 1 2 3 4; do nice -n 0 timeout 600 dd if=/dev/zero of=/dev/null >/dev/null 2>&1 & done; taskset -c 0,1 cargo test --workspace --no-fail-fast > "$log" 2>&1; rc=$?; pkill -f "dd if=/dev/zero of=/dev/null" >/dev/null 2>&1 || true; runs=$((runs+1)); if [ "$rc" -ne 0 ]; then fails=$((fails+1)); fi; echo "contended_run_${i}_exit=$rc"; done; echo "contended_runs=$runs"; echo "contended_failures=$fails"; test "$fails" -eq 0; echo "contended_gate_exit=$?"'</automated>
  </verify>
  <done>
`cargo test --workspace --no-fail-fast` reports zero failing binaries. The named flake instance
passes with `1 passed` and a non-zero `filtered out`.

The contended harness prints an explicit `contended_runs` and `contended_failures` pair rather than
a verdict. Both numbers go into the SUMMARY verbatim. The harness pins to the same two CPUs the
local gate and the CI job use and holds four competing hogs on them for the duration, because an
idle run was already recorded four times as discriminating nothing.

The SUMMARY states, in the same paragraph as those counts, that N green runs is a WEAK bound —
`d525f9a` measured roughly one failure in eight without its fix, so a small N is consistent with no
fix at all — and that the load-bearing argument is mechanical: the process boundary makes the
overlap impossible by construction where the mutex made it improbable by serialisation. It also
records that `d525f9a`'s "Fixed as a class" claim is false as written, with the reason.
  </done>
</task>

</tasks>

<verification>
- Exactly 47 deletions and zero additions across exactly the seven files `d525f9a` touched.
- 81 `let _guard = env_lock();` holders remain, reconciled against 46-06's and 46-07's recorded
  removals rather than against a literal in this plan.
- `cargo test --workspace --no-fail-fast`, `cargo clippy --workspace --all-targets -- -D warnings`
  and `cargo fmt --check` — green.
- Three contended runs on the pinned CPU pair, with the run and failure counts recorded as numbers.
- `scripts/check.sh`, `.github/workflows/ci.yml` and the test runner are unchanged — nextest stays
  deferred.
</verification>

<success_criteria>
- The 47 lines are gone; the 81 pre-existing holders are untouched.
- The precondition (zero PATH-emptying windows) was re-measured in this plan, not inherited.
- The contended re-run reports counts, and the SUMMARY reports the counts' strength honestly rather
  than converting them into a claim that the flake is eliminated.
- `d525f9a`'s false class-fix claim is corrected on the record.
</success_criteria>

<output>
Create `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-08-SUMMARY.md` when done.

Record: the diff's added/removed/file counts; the final holder count and its reconciliation against
46-06 and 46-07; `contended_runs` and `contended_failures` verbatim; the explicit statement of what
those counts do NOT establish; and the correction to `d525f9a`'s commit message.

Because this is the last plan of the gap-closure set, the SUMMARY must also carry a one-table
roll-up of the five findings the operator dispositioned as in-scope (C-01, C-02, C-03, C-04, C-06)
against the plan that closed each, and must restate that C-05 and C-07 remain deferred under GitHub
#207 and that `cargo nextest` remains backlog — so a reader of this SUMMARY alone cannot mistake the
set for complete coverage of the review.
</output>
