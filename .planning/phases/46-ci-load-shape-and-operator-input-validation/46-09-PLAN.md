---
phase: 46-ci-load-shape-and-operator-input-validation
plan: 09
type: tdd
wave: 2
depends_on: ["46-05"]
gap_closure: true
files_modified:
  - scripts/lint-plan-bashisms.sh
  - crates/devflow-cli/tests/plan_bashism_scanner.rs
autonomous: true
requirements: [INFRA-01]

estimate:
  tokens: 19000
  raw_tokens: 38000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "C-08 CLOSED. `lint-plan-bashisms.sh --staged` evaluates the exact blob currently in the Git index for every selected plan, so staging a violating plan and then replacing only its working-tree text with a clean plan still returns a non-zero refusal."
    - "The C-08 opposite-direction control holds: an indexed clean plan stays accepted when its working-tree copy alone is changed to a violating plan; the staged scan reports that it inspected zero staged plan files rather than reading the unstaged text."
    - "C-09 CLOSED. A staged rename whose destination is a violating `*PLAN.md` is selected and refused; the old deletion half of that rename is not scanned as a file."
    - "A staged deletion of a plan remains an intentional zero-file scan with a successful exit and a visible `scanned 0 file(s)` count, so adding rename support does not turn absent working-tree content into a read failure."
    - "C-10 CLOSED. `crates/devflow-cli/tests/plan_bashism_scanner.rs` creates disposable Git repositories and invokes the production scanner with `--staged`; the suite exercises index content, a clean index versus a dirty worktree, rename discovery, and deletion exclusion rather than only named fixture paths."
    - "All disposable-repository Git commands use `devflow_core::test_support::git_command(repo)` so inherited repository-local Git variables cannot redirect a fixture into the developer checkout."
  prohibitions:
    - "The scanner must not substitute a working-tree read when `--staged` is active; the index blob is the sole content source for that mode."
    - "The remediation must not alter the completed child-process PATH work, C-05, C-07, or the deferred nextest evaluation; none is an artifact, task, or verification target of this gap closure."
    - "The remediation must not add packages or modify either Cargo manifest; `tempfile` and the `devflow-core` test-support feature already exist in `crates/devflow-cli/Cargo.toml`."
  artifacts:
    - path: scripts/lint-plan-bashisms.sh
      provides: "A staged-mode scanner that selects added, copied, modified, and renamed plans from the index and feeds indexed bytes to the existing AWK parser."
    - path: crates/devflow-cli/tests/plan_bashism_scanner.rs
      provides: "Disposable-Git-repository regression tests for staged blob, clean-index, rename, and deletion semantics."
  key_links:
    - from: crates/devflow-cli/tests/plan_bashism_scanner.rs
      to: scripts/lint-plan-bashisms.sh
      via: "The integration test runs the executable scanner from each disposable repository with the literal --staged argument."
      pattern: "--staged"
    - from: scripts/lint-plan-bashisms.sh
      to: Git index
      via: "NUL-delimited diff discovery selects ACMR paths and Git object lookup supplies each selected staged blob to AWK."
      pattern: "diff --cached.*diff-filter=ACMR|git show"
---

<objective>
Close the final staged-plan scanner review findings without changing unrelated Phase 46 work.

Purpose: C-08 allows a bad staged plan to commit after its working-tree copy is made clean; C-09
omits renamed plans; C-10 leaves that exact mode untested. The scanner is a pre-commit safety
boundary, so its input must be the index that Git will commit and its tests must use an actual
disposable index.

Output: an index-blob staged scanner and four hermetic disposable-Git-repository regressions in
the existing scanner integration-test binary.
</objective>

<execution_context>
@/home/denniyahh/.codex/gsd-core/workflows/execute-plan.md
@/home/denniyahh/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-REVIEWS.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-05-PLAN.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-05-SUMMARY.md
@scripts/lint-plan-bashisms.sh
@crates/devflow-cli/tests/plan_bashism_scanner.rs
@crates/devflow-core/src/test_support.rs
</context>

## Review scope

This is a review-driven gap closure for C-08, C-09, and C-10 only. C-05, C-07, and the deferred
nextest evaluation remain outside the plan because they are not part of these staged-scanner
findings.

## Artifacts this phase produces

- Test-private disposable-repository setup and scanner invocation helpers in
  `crates/devflow-cli/tests/plan_bashism_scanner.rs`.
- `staged_bad_working_tree_clean_is_blocked`,
  `staged_clean_working_tree_bad_is_accepted`,
  `staged_renamed_bad_plan_is_blocked`, and `staged_deleted_plan_is_skipped` regression tests.
- Updated staged-mode discovery and index-blob input handling in
  `scripts/lint-plan-bashisms.sh`.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: RED — prove the scanner follows the working tree and overlooks a staged rename</name>
  <files>crates/devflow-cli/tests/plan_bashism_scanner.rs</files>
  <read_first>
    - `crates/devflow-cli/tests/plan_bashism_scanner.rs` — existing `run`, fixture, accepted, and blocked helpers that must continue to test named paths.
    - `scripts/lint-plan-bashisms.sh:68-140` — the current staged-file discovery and file-read branches under test.
    - `crates/devflow-core/src/test_support.rs` — `git_command`, which hermetically removes repository-local Git environment variables for each fixture command.
    - `crates/devflow-cli/Cargo.toml:24-27` — the already-present `tempfile` and `devflow-core` test-support dependencies; do not add a dependency.
    - `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-REVIEWS.md:341-372` — the C-08/C-09/C-10 reproductions this task turns into executable tests.
  </read_first>
  <behavior>
    - A temp-repo plan staged with the existing violating `comment-bypass-PLAN.md` content and then overwritten in the working tree with `legit-PLAN.md` content must be refused by `--staged` and must name the staged path in its diagnostic.
    - A committed clean plan whose working-tree copy alone becomes violating must be accepted by `--staged` with a visible zero-file count; the test proves the index is clean and the working copy is violating before invoking the scanner.
    - A committed violating `old-PLAN.md` renamed in the index to a tab-containing `new-PLAN.md` path must be refused; the test proves the staged name-status output reports a rename before invoking the scanner.
    - A committed plan removed with `git rm` must leave `--staged` successful with a visible zero-file count; this is the deletion-exclusion control.
  </behavior>
  <action>
Extend the existing integration-test file with a small disposable-Git-repository harness. Keep the
existing direct-fixture tests intact; they still exercise named-path mode. Refactor only the
test-private scanner launcher enough to pass an owned `--staged` argument and choose a temporary
repository as the scanner process's current directory. Continue spawning the production script at
the repository-under-test path, and keep stdout plus stderr together for assertions.

Create each fixture with `tempfile::tempdir()` and use
`devflow_core::test_support::git_command(repo)` for every `git init`, configuration, add, commit,
move, remove, diff, and index-blob inspection. Check every command's exit status and include its
stderr in a failing assertion. Seed a committed baseline before scenarios that need Git to classify
a change as a rename or deletion. Do not invoke a bare `Command::new("git")`, and do not mutate
process-global Git environment variables.

Add the four behavior-named tests listed in this plan's artifact section. Copy the existing clean
and violating plan fixtures into `docs/` inside each temporary repository rather than introducing
new tracked fixture files. Before each scanner call, assert the intended index and working-tree
contents independently: use the Git index blob for the staged assertion and `std::fs::read_to_string`
for the working-tree assertion. The two refusing tests must assert a non-zero scanner exit, the
single diagnostic line containing both the temporary-repo plan path and the violating expression,
and a non-zero scanned-file count. The two acceptance controls must assert a zero exit and the
literal zero-file summary. The rename test must use `git mv` after a baseline commit and assert the
NUL-safe name-status result identifies the operation as a rename, not an addition.

Run the four-test `staged_` filter before changing the production scanner. It must be red because
the current implementation reads the cleaned working-tree bytes and filters out the rename, while
the clean-index and deletion controls remain green. Commit this RED test state with a
`test(46-09):` message before modifying the shell script.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; cargo test -p devflow --test plan_bashism_scanner staged_ -- --nocapture 2>&1 | tee /tmp/46-09-red.log; cargo_status=${PIPESTATUS[0]}; printf "cargo_exit=%s\n" "$cargo_status"; if [ "$cargo_status" -eq 0 ]; then echo "expected_red_exit=0"; exit 1; fi; rg -F "test staged_bad_working_tree_clean_is_blocked ... FAILED" /tmp/46-09-red.log; rg -F "test staged_renamed_bad_plan_is_blocked ... FAILED" /tmp/46-09-red.log; rg -F "test staged_clean_working_tree_bad_is_accepted ... ok" /tmp/46-09-red.log; rg -F "test staged_deleted_plan_is_skipped ... ok" /tmp/46-09-red.log; rg -F "test result: FAILED" /tmp/46-09-red.log'</automated>
    <fails_when>`cargo_exit=0`, a missing `test result: FAILED` line, either C-08/C-09 test not reporting `FAILED`, or either clean-index/deletion control not reporting `ok`; those outcomes mean the RED suite did not distinguish the documented bypasses from its opposite-direction controls.</fails_when>
  </verify>
  <acceptance_criteria>
    - The four tests run the production scanner with `--staged` from a `tempfile::TempDir` Git repository, not against only a named fixture path.
    - The staged-bad/working-clean test fails on the current scanner and proves the index bytes are violating while the filesystem bytes are clean.
    - The staged-clean/working-bad control passes on the current scanner and proves the inverse byte arrangement before invocation.
    - The renamed bad-plan test fails on the current scanner after asserting a staged rename status; the target filename contains a tab to exercise the NUL-delimited discovery path.
    - The staged deletion control passes and prints `scanned 0 file(s)` before the implementation change.
    - Every fixture Git subprocess comes from `devflow_core::test_support::git_command`, and the test file's existing named-fixture coverage remains present.
    - The recorded RED command reports the two documented bypasses as failed tests and the two controls as passing tests, rather than a suite that uniformly passes or fails.
  </acceptance_criteria>
  <done>The disposable-repository regression suite exists, its index-versus-working-tree and rename cases fail for the documented current behavior, and its clean-index plus deletion controls prove the suite is discriminating.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — select renamed index entries and scan the staged blob</name>
  <files>scripts/lint-plan-bashisms.sh, crates/devflow-cli/tests/plan_bashism_scanner.rs</files>
  <read_first>
    - `scripts/lint-plan-bashisms.sh:68-268` — staged argument handling, NUL-delimited discovery, unreadable-input handling, the AWK scanner, and its reporting contract.
    - `crates/devflow-cli/tests/plan_bashism_scanner.rs` — the committed Task-1 disposable-repository tests and existing named-path controls.
    - `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-05-SUMMARY.md` — the original scanner's containment rule, direct-fixture coverage, and deliberate deletion exclusion.
    - `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-REVIEWS.md:341-372` — C-08/C-09/C-10 acceptance target.
    - `crates/devflow-core/src/test_support.rs:1-75` — the hermetic Git-command contract used by the tests.
  </read_first>
  <behavior>
    - `--staged` selects added, copied, modified, and renamed `*PLAN.md` destinations from the index while not selecting deleted paths.
    - For every selected staged path, the AWK parser consumes bytes returned by Git for that index entry; a changed working-tree copy cannot change the scanner verdict.
    - A selected staged path whose index blob cannot be read returns the scanner's input-error exit and names that path; it must not fall back to filesystem bytes.
    - Named-path mode keeps its current readable-filesystem behavior and existing fixture tests remain green.
  </behavior>
  <action>
Update only `scripts/lint-plan-bashisms.sh` and the Task-1 test file to close C-08, C-09, and C-10.

In the staged branch, retain NUL-delimited `read -r -d ''` discovery but extend the cached-diff
filter to include renamed entries as well as added, copied, and modified entries. Keep deletions
out of the selected path list. Treat the path emitted for a rename as the destination path whose
index blob will be committed.

Split the scanner's input acquisition by mode. Named paths continue through the existing readable
filesystem check and AWK-file input. For a selected staged path, obtain the blob from the Git index
with Git's index-path syntax as one quoted argument, preserve the blob bytes through the AWK scan,
and keep the original path as AWK's diagnostic filename. Do not use command substitution for blob
content because it discards terminal newlines; use an explicit temporary input with cleanup or an
equivalent byte-preserving stream whose Git-read error status is checked independently. If index
blob retrieval fails, print an input error that names the staged path and exit 2. There is no
working-tree fallback in staged mode, including for a renamed or deleted filesystem path.

Keep the existing containment parser, violation formatting, zero-file summary, and named-path
semantics unchanged. Amend the nearby staged-mode comments so they explain that deletion exclusion
is intentional and that rename destinations are selected. Do not change the pre-commit hook, any
other Phase 46 artifacts, C-05, C-07, nextest, or dependency manifests.

Make the four Task-1 tests green. Assert their exact scanner outcomes, then run the full existing
scanner integration-test binary so named-path fixtures still exercise both accepted and rejected
directions. Commit the production change with a `fix(46-09):` message after the suite is green.
  </action>
  <verify>
    <automated>bash -c 'set -o pipefail; cargo test -p devflow --test plan_bashism_scanner staged_ -- --nocapture 2>&1 | tee /tmp/46-09-green.log; cargo_status=${PIPESTATUS[0]}; printf "cargo_exit=%s\n" "$cargo_status"; if [ "$cargo_status" -ne 0 ]; then exit 1; fi; rg -E "test result: ok\. 4 passed; 0 failed; .* [1-9][0-9]* filtered out" /tmp/46-09-green.log; rg -F "test staged_bad_working_tree_clean_is_blocked ... ok" /tmp/46-09-green.log; rg -F "test staged_clean_working_tree_bad_is_accepted ... ok" /tmp/46-09-green.log; rg -F "test staged_renamed_bad_plan_is_blocked ... ok" /tmp/46-09-green.log; rg -F "test staged_deleted_plan_is_skipped ... ok" /tmp/46-09-green.log'</automated>
    <fails_when>a non-zero `cargo_exit`, no `4 passed; 0 failed` filtered-test summary, a zero `filtered out` count, or any one of the four staged-semantic tests not reporting `ok`; the command would then be testing the wrong set or leaving a C-08/C-09/C-10 branch unproven.</fails_when>
    <automated>bash -c 'set -o pipefail; cargo test -p devflow --test plan_bashism_scanner -- --nocapture 2>&1 | tee /tmp/46-09-full.log; cargo_status=${PIPESTATUS[0]}; printf "cargo_exit=%s\n" "$cargo_status"; if [ "$cargo_status" -ne 0 ]; then exit 1; fi; rg -E "test result: ok\. [1-9][0-9]* passed; 0 failed" /tmp/46-09-full.log; rg -F "test legitimate_bash_c_wrapped_block_is_accepted ... ok" /tmp/46-09-full.log; rg -F "test multiline_block_is_blocked ... ok" /tmp/46-09-full.log'</automated>
    <fails_when>a non-zero `cargo_exit`, no all-green test summary, the legitimate containment control not reporting `ok`, or the known violating multiline fixture not reporting `ok`; either missing direction would make the scanner suite compatible with a blanket accept or blanket refuse regression.</fails_when>
    <automated>bash -c 'set -euo pipefail; bash -n scripts/lint-plan-bashisms.sh; shellcheck scripts/lint-plan-bashisms.sh; cargo fmt --all --check'</automated>
    <fails_when>a non-zero exit from Bash parsing, ShellCheck, or rustfmt; each signals malformed shell, a shell-analysis finding, or Rust formatting drift before the phase-level clippy gate.</fails_when>
  </verify>
  <acceptance_criteria>
    - Staged discovery uses a NUL-delimited cached diff that includes rename destinations and excludes deletions.
    - The test that stages violating bytes and cleans only the working tree now returns a scanner refusal, proving the index blob is the content source.
    - The inverse clean-index/dirty-working-tree test remains accepted with `scanned 0 file(s)`, proving the staged branch does not inspect unstaged content.
    - The staged rename test returns a scanner refusal and the staged deletion test remains a successful visible zero-file scan.
    - An index-blob read error exits 2 with the affected path in stderr rather than routing to a filesystem read.
    - All existing direct-path scanner tests remain green, including the legitimate bash-contained case and a known violating fixture, so both acceptance directions stay covered.
    - `scripts/lint-plan-bashisms.sh` passes `bash -n` and `shellcheck`; the modified Rust test file passes `cargo fmt --all --check`.
    - Neither `Cargo.toml` nor `crates/devflow-cli/Cargo.toml` appears in this task's commit.
  </acceptance_criteria>
  <done>The staged scanner consumes only selected index blobs, renamed bad plans are refused, deletions remain excluded, and real disposable-Git-repository tests prove both the failure and opposite directions.</done>
  <reversibility rating="reversible">The change is confined to one repository-local hook helper and its tests. Reverting restores the prior staged-input behavior without changing persisted state, public CLI contracts, or dependencies.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staged plan content → pre-commit scanner | A contributor-controlled plan is inspected before Git writes its index blob into a commit. |
| Git diff path → Git index object lookup | A repository-controlled filename selected from NUL-delimited Git output becomes one quoted index-path argument. |
| disposable test repository → Git subprocess | Tests create and mutate real repositories; Git environment leakage could otherwise redirect commands to the developer checkout. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-46-09 | Tampering | `scripts/lint-plan-bashisms.sh --staged` content source | high | mitigate | Read each selected plan from its Git index blob and make staged-bad/working-clean refusal an executable disposable-repo regression. |
| T-46-10 | Tampering | cached-diff selection | high | mitigate | Include rename destinations in the NUL-delimited cached diff filter, retain deletion exclusion, and prove each branch with renamed-bad and deleted-plan controls. |
| T-46-11 | Repudiation | scanner test coverage | medium | mitigate | Invoke the real script with `--staged` in disposable repositories; assert both C-08/C-09 failures and the clean-index/deletion opposite directions. |
| T-46-12 | Tampering | fixture Git subprocesses | high | mitigate | Use `devflow_core::test_support::git_command(repo)` for every fixture Git call so repository-local redirecting environment variables are removed per command. |
| T-46-SC | Tampering | dependency supply chain | low | accept | This plan adds no package-manager task and keeps existing `tempfile` plus `devflow-core` test-support dependencies unchanged. |
</threat_model>

<source_audit>

| Source | ID | Item | Coverage | Status |
|--------|----|------|----------|--------|
| GOAL | — | CI/load-shape and operator-validation outcomes remain delivered while their staged-plan enforcement is made commit-accurate | 46-01 through 46-05, 46-09 | COVERED |
| REQ | INFRA-01 | CI and its pre-commit-facing planning guard retain an executable, non-vacuous safety boundary | 46-01, 46-04, 46-05, 46-09 | COVERED |
| REQ | VALID-01 | Computed-revision base branch is refused | 46-02 | COVERED by existing completed plan; no change in 46-09 |
| REQ | VALID-02 | `devflow stop` accepts the same positional project root shape as peer commands | 46-03 | COVERED by existing completed plan; no change in 46-09 |
| RESEARCH | — | No new packages; use existing Rust integration-test and hermetic Git-command patterns | 46-09 Tasks 1–2 | COVERED |
| CONTEXT | D-01–D-06 | CI load-shape decisions | 46-01, 46-04 | COVERED by existing completed plans; untouched here |
| CONTEXT | D-07–D-10 | `base_branch` validation decisions | 46-02 | COVERED by existing completed plan; untouched here |
| CONTEXT | D-11–D-13 | `stop` root-routing decisions | 46-03 | COVERED by existing completed plan; untouched here |
| REVIEW | C-08 | staged content must be the index blob | 46-09 Tasks 1–2 | COVERED |
| REVIEW | C-09 | renamed staged plans must be scanned and deletions excluded | 46-09 Tasks 1–2 | COVERED |
| REVIEW | C-10 | staged mode requires disposable-Git-repository test coverage | 46-09 Tasks 1–2 | COVERED |

</source_audit>

<verification>
<automated>bash -c 'set -o pipefail; timeout 60s cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tee /tmp/46-09-clippy.log; clippy_status=${PIPESTATUS[0]}; printf "clippy_exit=%s\n" "$clippy_status"; if [ "$clippy_status" -ne 0 ]; then exit 1; fi; rg -F "Finished" /tmp/46-09-clippy.log'</automated>
<fails_when>a non-zero `clippy_exit`, a timeout exit, or no completion line; this is the workspace lint backstop after the staged-semantic tests have already exercised both acceptance directions.</fails_when>
</verification>

<success_criteria>
- C-08, C-09, and C-10 are represented in executable tasks, behavior, tests, and must-haves.
- The staged scanner reads the index blob for selected plans and no test relies on a working-tree proxy for its staged assertion.
- Disposable repositories prove staged-bad/working-clean fails, staged-clean/working-bad passes, a renamed bad plan fails, and a deletion is excluded.
- Direct-path scanner behavior, shell syntax/lint, Rust formatting, and workspace clippy remain green.
</success_criteria>

<output>
Create `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-09-SUMMARY.md` when done. Record the RED result, GREEN result, each disposable-repository test's observed scanner output, and the limits of the test evidence: it proves index-versus-working-tree and rename/deletion semantics locally, not that a hosted Git hook is installed or invoked on another checkout.
</output>
