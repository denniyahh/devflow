---
phase: 46-ci-load-shape-and-operator-input-validation
plan: 04
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - .github/workflows/ci.yml
  - scripts/lib/ci-cpus.sh
  - scripts/check-in-container.sh
  - scripts/assert-cpu-pin.sh
  - crates/devflow-cli/tests/ci_parity_guards.rs
  - crates/devflow-cli/tests/fixtures/ci-parity/valid.yml
  - crates/devflow-cli/tests/fixtures/ci-parity/commented-out.yml
  - crates/devflow-cli/tests/fixtures/ci-parity/decoy-job.yml
  - crates/devflow-cli/tests/fixtures/ci-parity/echoed-command.yml
  - crates/devflow-cli/tests/fixtures/ci-parity/no-pin.yml
autonomous: true
requirements: [INFRA-01]

estimate:
  tokens: 34000
  raw_tokens: 68000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "C-06 CLOSED. `DEVFLOW_CI_CPUS=all` no longer breaks CI. The `all` special-case exists in exactly ONE place — `cpu_pin_prefix()` in `scripts/lib/ci-cpus.sh` — and BOTH CI consumers (the load-shape measurement step and the suite step, today `ci.yml:134` and `:138`) go through it, as does the local gate `scripts/check-in-container.sh:99-103`. MEASURED baseline for the defect: `taskset -c all nproc` exits non-zero with `failed to parse CPU list: all`."
    - "The `all` conditional is not DUPLICATED to fix C-06. A second copy of `if [ \"$CPUS\" = all ]` in `ci.yml` would be a parity break inside the one file whose only purpose is parity — the same class D-03 created `scripts/lib/ci-cpus.sh` to close. After this plan, `grep -c 'CPUS.*=.*\"\\?all' .github/workflows/ci.yml` is 0."
    - "C-03 CLOSED. The pinned-vs-unpinned `nproc` comparison is ASSERTED, not echoed. Today `ci.yml:131-135` is three bare `echo`s that always exit 0, under a comment claiming the unpinned count is the pinned one's negative control — the repo's own documented anti-pattern (documenting a gate is not implementing one)."
    - "The C-03 assertion is CONDITIONAL, exactly as the operator disposed: it fires only when `CPUS != all` AND `unpinned_nproc > 2`. Unconditional would contradict C-06 (there is no pin to narrow anything when `CPUS=all`) and would fire falsely on a genuine 2-core host, where a 2-CPU pin cannot narrow a 2-CPU machine. Both skip paths PRINT the reason they skipped; a silent skip is indistinguishable from a pass."
    - "The C-03 decision is proven in BOTH directions, off the runner. `scripts/assert-cpu-pin.sh` takes the three measured values as ARGUMENTS and decides; the measurement stays real `nproc`/`taskset` in the job. That split is what makes the failing direction testable at all — a decider embedded in `ci.yml` can only ever be observed on a runner that happens to disagree with itself."
    - "`nproc` stays DIAGNOSTIC, not the sole oracle (operator disposition, C-03). The assertion establishes that the pin narrowed the visible core count. It does NOT establish that rustc or the test-harness threads were actually confined, nor that the runner was allocated 2 cores — CPU affinity is inherited across fork/exec, which is the mechanism being relied on, and that mechanism is not what `nproc` measures."
    - "C-02 CLOSED for scoping. All three assertions in `ci_workflow_runs_the_sequential_check_under_a_cpu_pin` run against the BODY OF THE `sequential` JOB, not against the whole workflow text. Today `:331`, `:341` and `:357` each search the entire file independently, so a decoy job — or a comment — can supply the job name, the `ci-cpus.sh` path and the pin invocation while `sequential` itself carries none of them."
    - "C-02 CLOSED for vacuity. The recogniser is a FUNCTION over `&str` that the fixture tests drive directly, so its negative cases are exercised. Today the guards only ever see the live file, which cannot fail in the interesting direction on demand. MEASURED: commenting out the suite step leaves the guard reporting `1 passed`."
    - "The job splitter stops mistaking `on:` block keys for jobs. MEASURED on the current file: it parses 6 headers — `push`, `pull_request`, `test`, `clippy`, `fmt`, `sequential` — for 4 real jobs, so `assert!(jobs.len() >= 4)` is satisfiable by two non-jobs. After this plan the splitter is anchored to the top-level `jobs:` key and `push`/`pull_request` are absent from its output."
    - "`code_lines` is NOT used for the scoping step, and its `str::trim` is the reason. Indentation is the ONLY signal that distinguishes a top-level YAML key from a nested one; a helper that trims leading whitespace has destroyed that signal before the splitter sees it. `code_lines` remains correct for the occurrence-COUNTING guards that already use it and is left alone there."
    - "The recogniser distinguishes RUNNING the pinned command from MENTIONING it. Rule: within the `sequential` job body, some non-comment line's TRIMMED content must START WITH the pin token and also contain `scripts/check.sh all`. `- run: echo 'taskset -c \"$CPUS\" scripts/check.sh all'` trims to a line starting with `- run: echo`, so it is rejected — that exact string is a required fixture case."
    - "Comment stripping for the recogniser is whole-line only (drop lines whose first non-space character is `#`) and is applied AFTER indentation has been used for scoping, never before. A mid-line `#` inside a YAML value is not a comment in general, and a stripper that assumes it is would corrupt the very lines being recognised."
    - "The five fixture cases the operator named all exist and all discriminate: valid job PASSES; commented-out command FAILS; decoy job FAILS; `on.push` creates no job; an echoed pin command FAILS. Four of the five are negative controls — without the passing case the recogniser could reject everything and still look green, and without the failing cases it could accept everything."
  prohibitions:
    - "Must NOT weaken `assert!(jobs.len() >= 4)` by deleting it. Replace it with an anchored assertion that names the four known job KEYS and asserts `push`/`pull_request` are ABSENT. An anti-vacuity assert that a broken splitter can satisfy is worse than none, because it reads as coverage."
    - "Must NOT add `continue-on-error` to any job. `sequential_job_name_is_not_a_required_status_check` forbids it, and D-05 rejected it explicitly: it reports SUCCESS on a real suite failure, poisoning the one `gh pr checks` signal this repo's rules use as acceptance evidence."
    - "Must NOT rename the `Sequential 2-CPU check` job or add it to `REQUIRED_STATUS_CONTEXTS`. It is advisory by being outside the ruleset's required set; a rename hijacks an existing required context rather than adding a new one, and merges to that branch wedge (that happened here on 2026-07-26)."
    - "Must NOT re-type the CPU list literal `0,1` anywhere in `.github/workflows/ci.yml` or `scripts/check-in-container.sh`. `cpu_pin_has_exactly_one_definition_site` fails on it and D-03 exists to prevent it."
    - "Must NOT make the C-03 assertion unconditional, and must NOT make either skip path silent. A skip that prints nothing is read as a pass by anyone scanning the log, which is the C-03 defect wearing a different hat."
    - "Must NOT use `rg -c <pat> | rg '^0$'` as a zero-check. MEASURED in this repo: `rg -c` prints nothing and exits 1 on zero matches, so the downstream filter never receives a line and the check exits 1 for BOTH a green and a red input. Print the count and the command's own exit code on separate lines."
    - "Must NOT accept a bare `cargo test ... --exact <name>` as evidence. It exits 0 when the name matches nothing. Assert on a real `N passed` together with a non-zero `filtered out` count."
    - "Must NOT use `cargo test -p devflow --lib`. `devflow` is binary-only, so cargo exits non-zero with `no library targets found` before running a test. Use `-p devflow --test ci_parity_guards`."
    - "Must NOT edit `crates/devflow-cli/src/**`. Plans 46-06 and 46-07 own that tree; this plan touches only `tests/`, `scripts/` and `.github/`."
    - "Must NOT edit `scripts/hooks/pre-commit` or create `scripts/lint-plan-bashisms.sh`. Plan 46-05 owns both."
    - "Must NOT edit `CLAUDE.md` — it is untracked on develop and the pre-commit hook refuses it on a `feature/*` branch."
    - "Must NOT touch `.planning/user/DEV-SETUP-CHECKLIST.md` for `.github/workflows/` changes in the SAME commit as unrelated work — but DO update it in this plan's commits, because `scripts/hooks/post-commit`'s `setup_changed` pattern covers `.github/workflows/` and `scripts/hooks/`, and this plan changes the former."
  artifacts:
    - scripts/lib/ci-cpus.sh
    - scripts/assert-cpu-pin.sh
    - .github/workflows/ci.yml
    - crates/devflow-cli/tests/ci_parity_guards.rs
    - crates/devflow-cli/tests/fixtures/ci-parity/
  key_links:
    - "Task 1 and Task 2 are a PAIR that must land in that order and cannot be reordered. Task 1 replaces the literal `taskset -c \"$CPUS\" scripts/check.sh all` line with the wrapper form, which is precisely the string `ci_workflow_runs_the_sequential_check_under_a_cpu_pin:341` filters on — so the guard goes RED at the end of Task 1 and Task 2 is what makes it green again. That red is CORRECT: the guard was pinned to a spelling rather than to a behaviour, and C-02 is the report that this was always the defect."
    - "`scripts/check-in-container.sh:151` invokes `\"${PIN[@]}\" scripts/check.sh \"$TARGET\"` as an argv PREFIX inside a `docker run`, so the shared helper cannot be a wrapper function that executes the command — a function defined in the host shell does not exist inside the container. It must be a function that BUILDS an argv array. That is why the helper is `cpu_pin_prefix` setting a global `CPU_PIN`, not `run_pinned \"$@\"`."
    - "`scripts/lib/ci-cpus.sh` is SOURCED, never executed, and deliberately omits `set -euo pipefail` so those options do not leak into either caller. Adding a function keeps that property; adding a top-level `set` line breaks both consumers."
    - "Bash arrays require the `sequential` job's existing `defaults: run: shell: bash` declaration. It is already present (added after PR #208 died on `set -o pipefail` under dash) and `container_jobs_using_bash_syntax_declare_a_bash_shell` guards it. Do not remove it; the `\"${CPU_PIN[@]}\"` expansion depends on it."
    - "The `Read the CPU pin` step exports `CPUS` through `$GITHUB_ENV`, but a shell FUNCTION cannot cross a step boundary. Every step that needs `cpu_pin_prefix` must source `scripts/lib/ci-cpus.sh` itself. That re-source is not duplication — it is the single definition site being read again, which is what D-03 asks for."
---

<!-- Comment-text discipline: every negative grep in this plan is scoped to a SOURCE tree
     (crates/, scripts/, .github/) and never to .planning/, so a literal appearing in this
     plan's prose cannot satisfy or invalidate it. The identifiers below have to be named
     to be actionable. -->
<!-- planner-discipline-allow: 0,1 -->
<!-- planner-discipline-allow: cpu_pin_prefix -->

<objective>
Make the CI job's own guards guard the job, and make the job's negative control assert instead of
narrate.

Purpose: three findings from `46-REVIEWS.md`'s code-stage review share one root — a mechanism was
described in a comment and then not implemented. C-02: the parity guards search the whole workflow
text, so a decoy job or a comment satisfies them while the real job carries nothing (demonstrated:
commenting out the suite step leaves the guard reporting `1 passed`). C-03: the "unpinned is the
negative control for pinned" comment sits above three bare `echo`s that always exit 0. C-06:
`DEVFLOW_CI_CPUS=all`, which the local gate absorbs, fails CI with
`taskset: failed to parse CPU list: all` — a parity break inside the file whose only purpose is
parity.

Output: `cpu_pin_prefix()` as the single home of the `all` special-case, used by all three
consumers; `scripts/assert-cpu-pin.sh` as a testable decider driven by real measurements; and a
job-scoped, fixture-driven recogniser in `ci_parity_guards.rs` whose negative cases actually run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-REVIEWS.md
@.planning/phases/46-ci-load-shape-and-operator-input-validation/46-CONTEXT.md
@CLAUDE.md
@.github/workflows/ci.yml
@scripts/lib/ci-cpus.sh
@scripts/check-in-container.sh
@crates/devflow-cli/tests/ci_parity_guards.rs
</context>

## Measured baselines (do not re-derive; verify if you doubt them)

| Fact | Value | How it was measured |
|---|---|---|
| `taskset` sites in `ci.yml` | 2 — lines 134 and 138 | `rg -n taskset .github/workflows/ci.yml` |
| The `all` special-case today | `scripts/check-in-container.sh:99-103` only | read |
| `taskset -c all nproc` | non-zero, `failed to parse CPU list: all` | run |
| Job headers the current splitter parses | 6 — `push`, `pull_request`, `test`, `clippy`, `fmt`, `sequential` | replayed the splitter's logic over the live file |
| Real jobs | 4 | read |
| `code_lines` | maps `str::trim` over every line (`ci_parity_guards.rs:51-57`) | read |
| Commenting out the suite step | guard still reports `1 passed` | agy's demonstration, recorded in `46-REVIEWS.md` C-02 |

<tasks>

<task type="auto">
  <name>Task 1: Give the `all` special-case one home and route both CI call sites through it</name>
  <files>scripts/lib/ci-cpus.sh, scripts/check-in-container.sh, .github/workflows/ci.yml</files>
  <action>
Close C-06 without duplicating the conditional.

In `scripts/lib/ci-cpus.sh`, below the existing `CPUS=` assignment, add a function
`cpu_pin_prefix` that sets a global array `CPU_PIN` to `(taskset -c "$CPUS")` when `$CPUS` is
anything other than the literal `all`, and to an empty array when it is `all`. It must set an
array rather than execute the command, because the local gate needs the value as an argv prefix
inside a `docker run` where a host-shell function does not exist. Document that constraint in the
function's comment, and document that the function is the ONLY place the `all` case is decided.
Do not add `set -euo pipefail` to this file — it is sourced into both callers and those options
would leak.

In `scripts/check-in-container.sh`, replace the inline `if [ "$CPUS" = "all" ]` block (currently
lines 99-103, which builds `PIN`) with a call to `cpu_pin_prefix`, and change the invocation at
the end of the script to expand `"${CPU_PIN[@]}"` in place of `"${PIN[@]}"`. Behaviour is
unchanged for the local gate; the point is that the conditional now has one implementation.

In `.github/workflows/ci.yml`, inside the `sequential` job, change BOTH pin call sites to go
through the helper. The measurement step and the suite step must each source
`scripts/lib/ci-cpus.sh` and call `cpu_pin_prefix` themselves — a shell function cannot cross a
GitHub Actions step boundary, so re-sourcing is required and is not a second definition site. The
suite step changes from a one-line `- run:` into a block step; keep its existing comment about
affinity being inherited across fork/exec, because that is the reason `--test-threads=N` is not a
substitute. Leave the job's `defaults: run: shell: bash` declaration in place — the array
expansion depends on it.

Do not re-type `0,1` anywhere. Do not add a second `all` conditional to the workflow.

At the end of this task `ci_workflow_runs_the_sequential_check_under_a_cpu_pin` is EXPECTED to
fail: its filter at `ci_parity_guards.rs:341` looks for a line containing both the literal
`taskset -c` and `scripts/check.sh all`, and that line no longer exists. Record the failure in the
task's evidence; Task 2 is what makes it green, and the fact that a spelling change breaks it is
the C-02 finding restated.
  </action>
  <verify>
    <automated>bash -c 'set -u; cd "$(git rev-parse --show-toplevel)"; . scripts/lib/ci-cpus.sh; cpu_pin_prefix; echo "default_prefix=${CPU_PIN[*]-<empty>}"; DEVFLOW_CI_CPUS=all; CPUS=all; cpu_pin_prefix; echo "all_prefix=${CPU_PIN[*]-<empty>}"; echo "all_len=${#CPU_PIN[@]}"; test "${#CPU_PIN[@]}" -eq 0; echo "all_case_is_empty_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; code=$(grep -v "^[[:space:]]*#" .github/workflows/ci.yml); n=$(printf "%s\n" "$code" | grep -c "0,1" || true); echo "retyped_literal_count=$n"; test "$n" -eq 0; echo "retyped_gate_exit=$?"; m=$(printf "%s\n" "$code" | grep -c "CPUS.*all" || true); echo "duplicated_all_conditional_count=$m"; test "$m" -eq 0; echo "duplicate_gate_exit=$?"; p=$(printf "%s\n" "$code" | grep -c "cpu_pin_prefix" || true); echo "helper_call_sites=$p"; test "$p" -ge 2; echo "both_sites_routed_exit=$?"'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --test ci_parity_guards 2>&1 | tee /tmp/46-04-t1.log; echo "cargo_exit=${PIPESTATUS[0]}"; echo "--- expected RED on the pin guard ---"; grep -c "ci_workflow_runs_the_sequential_check_under_a_cpu_pin" /tmp/46-04-t1.log; grep "test result:" /tmp/46-04-t1.log'</automated>
  </verify>
  <done>
`cpu_pin_prefix` exists in `scripts/lib/ci-cpus.sh` and is the only place the `all` case is
decided. `scripts/check-in-container.sh` and both `ci.yml` pin sites call it. The literal `0,1`
appears nowhere in `ci.yml` outside comments, and no second `all` conditional exists there. The
first verify command prints a non-empty default prefix and an empty `all` prefix — that pair is
the negative control: an implementation that always returned `taskset -c` would print a non-empty
`all_prefix` and fail `all_case_is_empty_exit=0`. `ci_workflow_runs_the_sequential_check_under_a_cpu_pin`
is failing, and that failure is recorded as expected rather than treated as a defect.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Job-scope the parity recogniser and drive its negative cases from fixtures</name>
  <files>crates/devflow-cli/tests/ci_parity_guards.rs, crates/devflow-cli/tests/fixtures/ci-parity/valid.yml, crates/devflow-cli/tests/fixtures/ci-parity/commented-out.yml, crates/devflow-cli/tests/fixtures/ci-parity/decoy-job.yml, crates/devflow-cli/tests/fixtures/ci-parity/echoed-command.yml, crates/devflow-cli/tests/fixtures/ci-parity/no-pin.yml</files>
  <behavior>
The recogniser is a pure function over workflow text. Write these cases FIRST, against the
fixtures, and confirm each fails for the right reason before the implementation exists.

- `valid.yml` — a `sequential` job carrying `name: Sequential 2-CPU check`, a step that sources
  `scripts/lib/ci-cpus.sh`, and a step whose command line starts with the pin token and contains
  `scripts/check.sh all`. Recogniser returns Ok. This is the POSITIVE control; without it a
  recogniser that rejects every input passes every other case.
- `commented-out.yml` — identical to `valid.yml` except the pin/suite line is prefixed with `#`.
  Recogniser returns Err naming the missing pinned invocation. This is agy's demonstrated bypass.
- `decoy-job.yml` — the `sequential` job has the right `name:` and NOTHING else; a second,
  differently-keyed job carries the `ci-cpus.sh` source line and the pinned suite line.
  Recogniser returns Err. This is codex's demonstrated bypass.
- `echoed-command.yml` — identical to `valid.yml` except the suite step is
  `- run: echo 'taskset -c "$CPUS" scripts/check.sh all'`. Recogniser returns Err: the trimmed
  line starts with `- run: echo`, not with the pin token.
- `no-pin.yml` — the `sequential` job runs `scripts/check.sh all` with no pin at all. Recogniser
  returns Err.
- The job splitter over any fixture carrying a top-level `on:` block with `push:` and
  `pull_request:` keys yields job keys that EXCLUDE both. Assert on the exact key list, not on a
  count — a count is what let the current `>= 4` assert be satisfied by two non-jobs.
- The recogniser applied to the LIVE `.github/workflows/ci.yml` returns Ok. This is the guard's
  original job, now expressed through the same code path the fixtures exercise.
  </behavior>
  <action>
Add a job splitter and a recogniser as functions over `&str`, then re-express
`ci_workflow_runs_the_sequential_check_under_a_cpu_pin` in terms of them.

Splitter: anchor to the top-level `jobs:` key. Collect lines from there until the next line whose
indentation is zero and which is not blank. Within that region, a job header is a line with
exactly two spaces of leading whitespace whose trimmed form ends in `:` and does not begin with
`#`. This anchoring is what removes `push:` and `pull_request:` — they are two-space keys under
`on:`, not under `jobs:`. Work on RAW lines: indentation is the only signal separating a
top-level key from a nested one, so do not route these lines through `code_lines`, whose
`str::trim` destroys exactly that signal. Leave `code_lines` alone; the occurrence-counting
guards that use it are correct as they stand.

Recogniser: given the workflow text, find the job block whose body contains a line trimming to
`name: Sequential 2-CPU check`. Return an error if no such block exists. Within THAT body only,
after dropping lines whose first non-space character is `#`, require:
  (a) a line containing `scripts/lib/ci-cpus.sh`;
  (b) a line whose TRIMMED content STARTS WITH the pin token and also contains
      `scripts/check.sh all`.
Apply the comment drop only after the splitter has used indentation. Strip whole comment lines
only — a mid-line `#` is not reliably a comment in YAML, and a stripper that assumes it is would
corrupt the lines being recognised.

The starts-with rule in (b) is the whole of the echoed-command defence: state it in a comment on
the recogniser, and cite `echoed-command.yml` by name as the case that proves it.

Rewrite `ci_workflow_runs_the_sequential_check_under_a_cpu_pin` to call the recogniser on the live
file. Keep its existing failure prose — the explanation of why affinity beats `--test-threads=N` is
still what a reader needs. Replace `assert!(jobs.len() >= 4)` in
`container_jobs_using_bash_syntax_declare_a_bash_shell` with an assertion over the splitter's exact
key list: the four known job keys are present, and `push` and `pull_request` are absent. Do not
simply delete the anti-vacuity assert.

Also update the guard's doc comment to record that the pin is now invoked through
`cpu_pin_prefix`, so a future reader does not go looking for a literal `taskset` line.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --test ci_parity_guards 2>&1 | tee /tmp/46-04-t2.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/46-04-t2.log; grep -c "0 failed" /tmp/46-04-t2.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --test ci_parity_guards --exact recogniser_rejects_a_commented_out_pinned_suite_step 2>&1 | tee /tmp/46-04-t2b.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep -E "test result: ok\. 1 passed" /tmp/46-04-t2b.log; echo "one_passed_exit=$?"; grep -oE "[0-9]+ filtered out" /tmp/46-04-t2b.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; ls crates/devflow-cli/tests/fixtures/ci-parity/; n=$(ls crates/devflow-cli/tests/fixtures/ci-parity/*.yml | wc -l); echo "fixture_count=$n"; test "$n" -ge 5; echo "fixture_gate_exit=$?"'</automated>
  </verify>
  <done>
The full `ci_parity_guards` suite is green, including the pin guard Task 1 turned red. The named
per-case run prints a literal `test result: ok. 1 passed` together with a non-zero `filtered out`
count — the pair is required because a name matching nothing exits 0 with `0 passed`. Five
fixtures exist. The four negative fixtures each fail the recogniser and `valid.yml` passes it, so
the recogniser is proven to discriminate rather than merely to be callable. The splitter's key
list excludes `push` and `pull_request`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Turn the pinned/unpinned negative control into a conditional assertion</name>
  <files>scripts/assert-cpu-pin.sh, .github/workflows/ci.yml, crates/devflow-cli/tests/ci_parity_guards.rs</files>
  <behavior>
`scripts/assert-cpu-pin.sh <cpu_list> <unpinned_nproc> <pinned_nproc>` decides; the caller
measures. Write these four cases first:

- `0,1 4 2` — exit 0, stdout says the assertion PASSED and names both counts.
- `0,1 4 4` — exit 1, stderr says the pin did not narrow the visible core count and names both
  counts. This is the C-03 failing direction: the whole finding is that this case is currently
  unreachable.
- `all 4 4` — exit 0, stdout says SKIPPED and names `all` as the reason. Without this the fix
  would contradict C-06.
- `0,1 2 2` — exit 0, stdout says SKIPPED and names the 2-CPU host as the reason. A 2-CPU pin
  cannot narrow a 2-CPU machine, so a failure here would be an artefact of the host.

Every skip PRINTS its reason. A silent skip and a pass are indistinguishable in a job log, which
is the C-03 defect in another form.
  </behavior>
  <action>
Create `scripts/assert-cpu-pin.sh` with `set -euo pipefail`, a usage block, and exactly the four
outcomes above. Take the three values as positional arguments and do NO measuring of its own —
that separation is what makes the failing direction testable off a runner. Refuse a wrong argument
count with a usage error and a non-zero exit.

Guard the numeric comparisons: reject a non-numeric count argument rather than letting `[ ... -lt ... ]`
error out under `set -e` with a message about integer expressions, which would read as a broken
script rather than as bad input.

In `.github/workflows/ci.yml`, rewrite the `Record the load shape this job actually got` step so
that it sources `scripts/lib/ci-cpus.sh`, captures `unpinned=$(nproc)`, captures
`pinned=$("${CPU_PIN[@]}" nproc)` — which is plain `nproc` when the prefix is empty — echoes both
alongside `cpu_list`, and then invokes `scripts/assert-cpu-pin.sh "$CPUS" "$unpinned" "$pinned"`.
Replace the existing comment above the step: it currently claims the unpinned count is the
negative control for the pinned one, which was true as intent and false as implementation. Say
instead that the comparison is asserted by `scripts/assert-cpu-pin.sh`, and that the assertion is
conditional on `CPUS != all` and `unpinned > 2` for the two reasons above.

Say explicitly in that comment what the assertion does NOT establish: it shows the pin narrowed
the visible core count, not that rustc or the harness threads were confined, and not that the
runner was allocated 2 cores. The affinity-is-inherited mechanism is the thing being relied on and
`nproc` does not measure it.

Add the four decision cases to `crates/devflow-cli/tests/ci_parity_guards.rs` as tests that
execute the script and assert on exit status AND on the specific stdout/stderr text, so a script
that exits correctly while printing nothing still fails.
  </action>
  <verify>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; s=scripts/assert-cpu-pin.sh; "$s" 0,1 4 2; echo "narrowed_exit=$?"; "$s" all 4 4; echo "all_exit=$?"; "$s" 0,1 2 2; echo "twocore_exit=$?"; if "$s" 0,1 4 4; then echo "did_not_narrow_exit=0"; else echo "did_not_narrow_exit=$?"; fi'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo test -p devflow --test ci_parity_guards 2>&1 | tee /tmp/46-04-t3.log; echo "cargo_exit=${PIPESTATUS[0]}"; grep "test result:" /tmp/46-04-t3.log'</automated>
    <automated>bash -c 'cd "$(git rev-parse --show-toplevel)"; cargo clippy --workspace --all-targets -- -D warnings 2>&1 | tail -5; echo "clippy_exit=${PIPESTATUS[0]}"; cargo fmt --check 2>&1 | tail -3; echo "fmt_exit=$?"'</automated>
  </verify>
  <done>
The first verify prints `narrowed_exit=0`, `all_exit=0`, `twocore_exit=0` and a NON-ZERO
`did_not_narrow_exit`. That fourth value is the negative control: a decider that always exited 0
would print `did_not_narrow_exit=0` and this criterion fails. `ci.yml`'s measurement step calls the
decider instead of echoing, and its comment states both the conditions and the limits of what the
assertion establishes. The whole `ci_parity_guards` suite, clippy and fmt are green.
  </done>
</task>

</tasks>

<verification>
Run from the repo root:

- `cargo test -p devflow --test ci_parity_guards` — green, and the count of tests is higher than
  before this plan (new fixture cases and new decider cases).
- `bash scripts/assert-cpu-pin.sh 0,1 4 4` — exits non-zero. This single command is the C-03
  closure: before this plan there was no command that could fail.
- `DEVFLOW_CI_CPUS=all` sourced through `cpu_pin_prefix` yields an empty prefix. Before this plan
  the equivalent CI path produced `taskset: failed to parse CPU list: all`.
- `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --check` — green.
- `.planning/user/DEV-SETUP-CHECKLIST.md` updated in the same commit as the `.github/workflows/`
  change, per this repo's post-commit warning contract.
</verification>

<success_criteria>
- C-02: all three assertions scoped to the `sequential` job body; recogniser driven by five
  fixtures including four negative cases; splitter no longer counts `on:` keys as jobs.
- C-03: the comparison is asserted by a script that is proven to fail in the not-narrowed case,
  and skipped-with-a-printed-reason in the two cases where it cannot discriminate.
- C-06: one `all` special-case, three consumers, no duplicated conditional in `ci.yml`.
- No change to `crates/devflow-cli/src/**`, `scripts/hooks/pre-commit`, or the job's name/required
  status.
</success_criteria>

<output>
Create `.planning/phases/46-ci-load-shape-and-operator-input-validation/46-04-SUMMARY.md` when done.

The SUMMARY must record, as measured values rather than claims: the exit code of
`scripts/assert-cpu-pin.sh 0,1 4 4`; the recogniser's verdict on each of the five fixtures; and the
splitter's exact job-key list for the live `ci.yml`. It must also state plainly what the C-03
assertion does not establish.
</output>
