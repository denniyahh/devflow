> invocation: codex exec -m terra -c model_reasoning_effort=high --cd <worktree> "<prompt>"
> FAILED — HTTP 400: "The 'terra' model is not supported when using Codex with a ChatGPT account."
> No findings produced (the run echoed the prompt, then errored on model selection). Re-run with a ChatGPT-account-supported model.
