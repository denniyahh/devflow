> invocation: antigravity -p "<prompt>" (config model temporarily 'Gemini 3.1 Pro', restored after)
> SKIPPED — timeout: 'Error: timeout waiting for response' (exit 1). Not silently dropped; no findings recorded.
